<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Pico CSS
    |--------------------------------------------------------------------------
    |
    | Set THEME_PICO=true in .env to include Pico CSS from CDN in the public
    | layout. Disabled by default — the layout renders clean HTML with no
    | framework until a stylesheet is explicitly provided.
    |
    */

    'pico' => env('THEME_PICO', false),

];
