# Base Prompt — Session 349 (Table Widget)

**Start now.** Execute the reading list below, open the session prompt at `sessions/349. Table Widget.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **349. Table Widget.**

This is **release-plan entry E15 (Table widget)** — one of the housekeeping-promoted entries (E15–E17) that land **before the `── PUBLIC WEBSITE COMPLETE ──` divider** because they block the CMS from looking demo-ready (Rule 12). It is **not** Code Review & Cleanup-track work — it sits immediately after the Cycle-3 squash (348) purely as a scheduling convenience while the inline-editing / toolbar context is fresh (the 347 carve-out). The Cycle-3 squash is fully closed; nothing here depends on it beyond the clean schema baseline.

This session is **NOT boundary-touching** — no Fleet Manager surface. CRM contract stays **v2.3.0**, no `CONTRACT_VERSION` change. **No DB schema / migration, no bundle `format_version` bump** — the widget stores its config in the existing `page_widgets.config` jsonb like every other widget.

**Session shape:** feature-style (standard new-widget). Build a new Page-Builder widget called **Table** — a near-exact clone of `PricingChart` that emits a semantic `<table>`, with **rich-text inline-editable cells**, a **Google-Docs-style grid picker** for dimensions (one new custom Inspector field), and header-row + per-column alignment + border-style affordances + mobile horizontal overflow. Pattern-application, not pattern-invention: the architecture is fixed.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference.
2. Read `sessions/session-outlines.md` (active-tracks block + the Public-Website-Complete milestone context) and the **E15** entry in `sessions/release-plan.md` (canonical for scope/success-criterion/artifact).
3. **Read `sessions/349. Table Widget.md` carefully**, then read its origin spec `sessions/311. Table Widget.md` (the hanging spec 349 supersedes — useful detail on render shape + out-of-scope) for background.
4. **Read `app/Widgets/PricingChart/` end-to-end (Definition + `template.blade.php` + `styles.scss`) — it is the exact template.** It already solves count-driven dimensions, inline-edited cells (`'inspector' => false`), and non-destructive shrink. Mirror it.
5. Read the custom-Inspector-field system: `resources/js/page-builder-vue/components/fields/` (the 12 existing field components) and `resources/js/page-builder-vue/components/InspectorField.vue` (the `field.type` → component map) — the grid picker is a new field type here. Read the inline-eligibility path (`resources/js/page-builder-vue/composables/useInlineEdit.ts`, `components/PreviewRegion.vue`) and how `PricingChart` is registered as inline-eligible (the per-widget roster + its pinning test) — mirror that for `table`.
6. Read `docs/app-reference.md` (page-builder map, `build:public` vs `npm run build` rule, `help:sync`). No `docs/schema/` reads needed — no schema change.
7. Note drift vs the session prompt; proceed unless a decision is needed.

---

## Starting state inherited from session 348

- **CRM contract at v2.3.0** — unchanged. 348 was the Cycle-3 **migration squash** (13 migrations collapsed into a fresh `database/schema/pgsql-schema.sql`); mechanical, non-boundary. **348 closed at VERSION `0.348.NN`** (so 349 closes at `0.349.<iter>`).
- **Schema baseline:** bootstrapped from the **fresh 348 squash dump**; `database/migrations/` is empty post-squash. **This session adds no migration** — the widget is config-only. (If a fresh clone is in play, `migrate:fresh --seed` runs from the dump.)
- **Fast Pest baseline:** whatever 348 closed at — **2816 / 0** minus any squash-fallout test deletion (read the 348 log for the exact number). **New Pest expected** this session: the widget definition + render-shape tests (mirror `PricingChart`'s test).
- **Playwright:** the inline-editing + 347 `inline-formatting-toolbar.spec.ts` + `design`-parity specs are intact. **One new standing spec expected** — `tests/e2e/page-builder/table-widget.spec.ts` (the end-to-end authoring round-trip). Run Pest and Playwright **sequentially**, never in parallel.
- **Planning state:** Cycle 3 (Code Review & Cleanup) fully closed at 348; next cycle ~398. This session opens the **Public Website Complete** milestone's E15–E17 cluster (E15 Table here; E16 header/footer + E17 borders follow). The hanging 311 Table spec was archived at the 348 close (superseded by 349).
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — a new-widget session may absorb a small **widget-adjacent** inbox item if one fits Rule 2; otherwise leave the inbox untouched.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Mirror `PricingChart` exactly; deviate only where a table genuinely differs** (header row, `<thead>`/`<tbody>`, per-column alignment, border style). The count-driven-dimensions + inline-cells + non-destructive-shrink machinery is already proven there — copy it, don't reinvent it.
- **The grid picker is the one custom-admin-form exception.** It's a new `GridPickerField.vue` + a new `field.type` in `InspectorField.vue` — a bounded extension in the established field pattern. It is an **Inspector field that sets rows×columns, not an insert-time creation modal** (an insert-time flow is out of scope — pattern-invention).
- **Ship inline-eligibility from day one** (the cell shape is a copy of an already-eligible widget; mirror `PricingChart`'s wiring + update its pinning test + the "Deliberately Kept" register note 311 → 349).
- **`npm run build` after the `GridPickerField.vue` / `InspectorField.vue` changes** (Vue/TS compile signal). `build:public` only if widget SCSS lands in the public bundle path — match the touched-path build rule in the base prompt.
- **Settle the two open questions at session start** (grid-picker data model: composite key vs two sibling count keys; grid-picker max + shrink-reconcile) — architecture calls, decide first, log them.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation this is a standard new-widget build, a `PricingChart` clone emitting `<table>`, non-boundary, no schema; (2) the **grid-picker plan** — an Inspector custom field (Google-Docs hover-to-set rows×columns), with your chosen data-model (composite key vs two count keys) named; (3) the **inline-eligibility** default (ship eligible from day one). One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/349. Table Widget — Log.md` — what was built (Definition + template + styles + seeder + grid-picker field + help doc), the grid-picker data-model decision, the inline-eligibility note, Pest + Playwright results.
- **Bump `VERSION`** to `0.349.<iteration>`.
- **Release-plan / outlines:** mark **E15 closed**, one-line entry pointing at this log.
- **Run `help:sync`** after authoring `resources/docs/widget-table.md`.
- **Keep** `tests/e2e/page-builder/table-widget.spec.ts` as a standing spec (it's a permanent regression artifact, not a one-off).
- **Archive** `sessions/348. *.md` → `sessions/archived/` (and the superseded `sessions/311. Table Widget.md` if it wasn't swept at the 348 close).
- **Commit on `session-349/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: follow the `PricingChart` widget conventions and the existing `fields/` component conventions exactly; the win is a Table widget that renders a clean semantic `<table>`, authored via the grid picker + inline cell editing, with the standard new-widget surfaces (Pest + one standing Playwright spec) green — no schema, no contract change, no new interaction patterns beyond the one grid-picker field.
