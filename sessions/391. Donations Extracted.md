# 391. Donations Extracted

The D3 block closer: the Donations vertical — already a Stage B path package owning its four-table schema and carrying a 14-file testsuite — moves to its own repository (`crm-plugin--donations`) via the extraction runbook, walking its `distribution.json` source kind `path` → `vcs`. The fourth runbook run; every edge is a D2-proven edge at smaller scale (one asset instead of five, no cross-plugin asset input, 14 test files instead of 29).

---

## Stub reference

Arc position D3 per `sessions/plugin-architecture-plan.md` § Stage D, third session of the carve → package → extract block (settled decision 4 — the runbook is the per-block closer). Carve landed at 389, package + squash redraw at 390; this block session is extract only.

---

## The survey (ground truth, captured at 390 close)

**What Donations is:** `plugins/Donations/` — provider, `Http/` (checkout controller), `Importers/` (field registry + mapper), `Listeners/` (the three webhook-inversion listeners), `Mail/` (two mailables), `Observers/` (two), `Policies/` (three), `Filament/` (DonationResource + FundResource + DonorsPage + two import pages), `Widgets/` (DonationForm + RecentDonations, each with thumbnails; DonationForm carries `script.js`), `database/migrations/` (four create-table migrations, the 390 redraw), `resources/views`, `routes/web.php`, and `tests/` (14 files — 103 fast + 1 slow, the `Donations` phpunit testsuite). Stage B package `nonprofitcrm/donations` 0.1.0 with explicit `version` field (drops at extraction), path repository + `^0.1` require, Dockerfile manifest COPY ×2 stages. No third-party dependencies.

**Location-keyed touchpoints (the runbook step-0 survey):**

1. **Asset declaration to rewrite (runbook step 3.2), one string:** `DonationFormDefinition::assets()` — `plugins/Donations/Widgets/DonationForm/script.js` → `vendor/nonprofitcrm/donations/Widgets/DonationForm/script.js`. RecentDonations declares no assets. **No cross-plugin asset input** (the D2 BlogPager edge has no analogue here).
2. **Fixture migrator reaches to re-point, two:** `DonationsRemovedTestCase:43` and `DonationsDisabledViaFlagTestCase:38` — `$app->basePath('plugins/Donations/database/migrations')` → the vendor path (the Events fixture pattern at 388; disabled-≠-uninstalled must survive against the vendor'd migrations).
3. **In-plugin test path reads to re-point, four:** `DonationsSchemaSession390Test:27` (the migrator-path assertion — `base_path('plugins/Donations/database/migrations')` → vendor path) and `DonationCreditsImporterUntouchedTest:21–23` (three `base_path('plugins/Donations/…')` file reads). These files travel with the package and their reads must point at where the package lands.
4. **`PluginPackageGuardTest` schema-dump owner map:** the four `plugins/Donations` values → `vendor/nonprofitcrm/donations` (the guard globs `{$owner}/database/migrations/*_create_{table}_table.php`).
5. **The `Donations` phpunit testsuite** (`<directory>plugins/Donations/tests</directory>`) → `vendor/nonprofitcrm/donations/tests` (the settled D2 posture — same files, same counts, core CI unchanged).
6. **Dockerfile:** the Donations manifest COPY line ×2 stages — removed at extraction (`DistributionManifestGuardTest` enforces zero path-entry COPY lines once no path entries remain).
7. **`plugins/` empties again** — the 388 guard relaxations stand (in-repo assertions tolerate the empty set; `distribution.json` will carry vcs entries only, so the folder-entry PSR-4 conditional in `DistributionManifestGuardTest` simply doesn't fire). Root `"Plugins\\"` PSR-4 mapping stays (D12).
8. **Comment/doc-only mentions (wording, not behavior):** `DonationModuleBoundaryTest` docblock, the two fixture docblocks, `docs/app-reference.md` rows (5 donation-surface paths), `docs/schema/README.md` + the four table docs ("in-repo at `plugins/Donations`" → extracted wording), contract-doc prose at 391's own bump.
9. **Thumbnails:** both widgets ship `thumbnails/` inside the plugin — they travel automatically; serving resolves by reflection; `generate-thumbnails.js` can't regenerate extracted thumbnails (standing runbook note).
10. **No color-token baseline keys, no core test file-reads** of `plugins/Donations` outside the fixtures (verified at 390 close — the D2 `ContentListingWidgetsTest`-class edge has no analogue).

**What this run does NOT have (vs. D2):** no dependency move (php-only requires), no baseline-key re-point, no guard relaxation (already landed at 388), no cross-plugin asset input.

---

## Front-loaded decisions (drafted at 390 close — review at prompt review; reopen only what the Open Gate flags)

1. **Names:** repo `crm-plugin--donations` (the established pattern; handle `donations`); first tag `v0.1.0`; require stays bound `^0.1`; package name `nonprofitcrm/donations` unchanged. Repo owner-created, public, empty, sibling checkout — confirmed at the Open Gate.
2. **The `version` field drops** from the extracted manifest (the P9 inversion — tags are the version metadata under a vcs repository; a retained field is an install error).
3. **The testsuite travels; core CI keeps running it from the vendor path** (the settled D2 posture, contract § External repo — not an open decision this time). Per-plugin CI stays D11.
4. **The README** carries the FM-style release discipline (contract version programmed against, tags-are-releases, tests-travel note) — the Events README is the model.
5. **No dependency move, no schema work, no model/factory moves** (§ 6.7 stands — the four factories stay core), no events-handle migration.
6. **Contract doc at close: 0.14.0 → 0.15.0.** External repo section gains the fourth consumer (second migration-owning + testsuite-carrying extraction — the D2 postures re-proven, not novel); reference implementations updated; runbook deltas folded if the run surfaces any; changelog.

---

## Open questions to resolve at session start

One Open-Gate confirmation: the owner-created public `crm-plugin--donations` repo exists as a sibling checkout. Nothing else expected.

---

## Phases

### Phase 1 — Donations extracted (the runbook run)

The full runbook top to bottom: copy to the sibling checkout, drop the `version` field, rewrite the one asset path vendor-relative, write the README, commit/tag/push `v0.1.0`; manifest `path` → `vcs` + `plugins:manifest-sync` (provider-list no-op); composer repository swap + `composer update nonprofitcrm/donations` (composer before artisan — the 387 ordering caveat); `git rm -r plugins/Donations`; Dockerfile COPY lines removed; phpunit testsuite re-pointed; the two fixture migrator paths + four in-plugin test path reads + guard owner map re-pointed; doc wording updated. All runbook verification gates — gate 4 live for the one asset: `widgets:sync` + `build:public`, bundle proven to carry the `donationForm` Alpine component. In-image proof (package + migrations arrive from GitHub, autoloads `--no-dev`, `plugins/Donations` absent). One iteration expected; a second only if CI forces it.

---

## Success criterion (binary — no UAT)

- `nonprofitcrm/donations` lives at `github.com/abeuscher/crm-plugin--donations` (public, tag `v0.1.0`), consumed via the vcs repository + `^0.1` require, lock-pinned to the tag; `plugins/Donations/` gone; root PSR-4 mapping intact.
- `distribution.json` walked `path` → `vcs` with `plugins:manifest-sync` a no-op; both standing guards green with no relaxation beyond what 388 already landed.
- The `Donations` testsuite runs green from the vendor path — same 14 files, 103 fast + 1 slow, in core CI unchanged.
- Every Donations behavior net green with assertions unchanged beyond the surveyed re-points: enabled twin, removal mirror + flag-vanish against vendor'd migrations (disabled ≠ uninstalled survives extraction), the schema-ownership pair, checkout/acknowledgment/receipts/soft-credits/importer/widgets.
- Gate 4 proven with bundle content (the `donationForm` component from the vendor path); in-image proof clean; fast suite at the 390 baseline (3314, path re-points only); routes 272/272; `composer validate --strict` clean.
- Plugin contract at 0.15.0; plan doc D3 line marked ✅ (block closed) and arc header advanced to D4; log carries the **D4 kickoff input** (Memberships: the inline membership webhook branch in Payments, tiers, the two membership widgets, the importer); CI `Tests` green on the pushed branch.

---

## Out of scope (binding)

- Any D4+ work (Memberships) beyond the kickoff-input notes; no model/factory moves; no per-plugin CI (D11); no root-PSR-4 retirement (D12); no events-handle registry migration.
- Any FM-visible change (`deploy.yml`, `/api/health`, GHCR naming, VERSION semantics — FM contract stays v2.7.0).
- Per-distribution composer sets / code absence (Stage E, recorded boundary).
- Adjacent findings → housekeeping inbox or the plan doc's backlog, one line, move on.

---

## Testing

- **Slow test groups to run this session:** none locally (CI covers; the one slow Donations test rides the vendor-path suite).
- **New tests expected:** none — path re-points only; the existing nets are the oracle.
- **Front-end:** `build:public` runs as part of gate 4 (the asset-path re-point changes the `widget_types.assets` row via `widgets:sync` and the bundle must be proven); no `npm run build` (no Vite-compiled source changes).

---

## Closing steps

Follow the close gate in the base prompt. Session-specific:

- **Log file:** `sessions/391. Donations Extracted — Log.md` — include the **D4 kickoff input** section (what the Memberships block inherits) and mark the D3 block closed.
- **Branch:** `session-391/N` (final iteration); VERSION `0.391.<N>` at close.
- **Boundary: NO** (FM contract untouched at v2.7.0; plugin contract → 0.15.0).
