# Base Prompt — Session 392 (Memberships Carved)

**Start now.** Execute the reading list below, open the session prompt at `sessions/392. Memberships Carved.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **392. Memberships Carved**.

This is **arc position D4, first session of the block** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary). D3 closed at 391 (Donations extracted — the fourth `crm-plugin--*` repo); per settled decision 4, each vertical runs carve → package → extract as a block. This session is the **carve**: the Memberships behavioral surface — checkout controller + route, tiers, three admin resources, the importer, the MembershipStatus + PricingChart widgets, and the last two inline membership branches in the Payments webhook — moves into `plugins/Memberships/`, refilling the `plugins/` directory 391 emptied. The webhook inversion is the **second cross-repo inversion** (a `crm-plugin--payments` v0.3.0 release, the proven 389 mechanics).

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.15.0 → 0.16.0 at close (surface 10 gains the memberships slice; changelog).

**Session shape:** feature-style carve, behavior-preserving, single-phase. Runtime behavior identical throughout: routes 272/272 (dev/test), every membership behavior net passes with assertions unchanged, no schema work (the two membership tables stay in core's dump until the package phase). **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D3 ✅; D4 is the ladder's next rung) and § Stage D settled decisions 3–4 (widget disposition; per-vertical blocks).
3. Read the 391 session log (`sessions/391. Donations Extracted — Log.md`), especially its **D4 kickoff input** section — the survey below was captured at that close.
4. Read `docs/plugin-contract.md` at 0.15.0 — surface 10 in full (the events + donations inversions and the listener self-filter rule — this session adds the memberships slice), surfaces 2/3/8 (the contribution sockets the carve exercises), surface 13 (the capability layer the checkout controller already rides), § Package shape intro (the `folder` source kind the manifest entry starts at).
5. Read the 389 session log (`sessions/archived/389. Donations Carved — Log.md`) — the closest analogue: the carve playbook, the cross-repo inversion mechanics (Payments release, lock bump, same-commit constraint widening), and the rule-5 test-membership pass discipline.
6. Open `sessions/392. Memberships Carved.md` — the survey (captured at 391 close) and front-loaded decisions; note decision 8 (the block-shape call deferred to close) and the two Open-Gate items.
7. Skim the surface being moved: `app/Http/Controllers/MembershipCheckoutController.php` (confirm the capability-layer guards), `app/Filament/Resources/{MembershipResource,MembershipTierResource,MemberResource}.php` (MemberResource is a Contact-model resource — decision 2 sends it with the plugin), `app/Widgets/{MembershipStatus,PricingChart}/`, `app/Importers/Membership*.php`, `app/Observers/MembershipObserver.php`, `app/Policies/MembershipPolicy.php`.
8. Read the inversion surface: `vendor/nonprofitcrm/payments/Http/Controllers/StripeWebhookController.php` — `handleMembershipCheckout` + `handleMembershipInvoice` and the checkout-settled dispatch shape (Open-Gate question 2); the three Donations listeners under `vendor/nonprofitcrm/donations/Listeners/` as the pattern.
9. Read the portal seam the survey flags (touched only to preserve, not to change): `app/Widgets/PortalSignup/template.blade.php` (the `route('membership.checkout')` render) and `app/Http/Controllers/Portal/SignupController.php` (the pending-`Membership` write).
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry) and note the housekeeping-inbox posture for this shape.
11. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 391

- **CRM contract at v2.7.0** — 391 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D3 complete; **four extracted plugins**, every shipping plugin VCS-resolved from its own repo: `crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` v0.2.0, `crm-plugin--events` v0.1.1, `crm-plugin--donations` v0.1.0. `plugins/` is **empty** (vanishes from a fresh checkout — the 388 guard relaxations tolerate it; this carve refills it). Plugin contract **v0.15.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** unchanged since 390 — the seven plugin-owned tables (three events, four donations) live in their packages' migrations; `memberships` + `membership_tiers` are **core-dump tables** until this block's package phase.
- **Fast Pest suite green at 3314 / 0 parallel** (390 baseline, held exactly through 391 — path re-points only). The `Donations` suite runs from `vendor/nonprofitcrm/donations/tests` (14 files, 103 fast + 1 slow); the `Events` suite from its vendor path (29 files). Expected delta this session: **additive** — inversion-listener tests, twin fixtures, boundary guard; existing membership assertions unchanged.
- **Route baseline: 272 routes** (dev/test) — byte-identical this session (the checkout route re-registers plugin-side with identical name/URI/middleware).
- **Playwright suite** — CI signal; 62/1 at 391, the one failure the known parked chrome-parity spec (pre-existing, non-gating). No local Playwright.
- **Housekeeping inbox** — carve shape absorbs no items; the two 389-folded items (`[plugin-arch]` EmailTemplateResource tokenHint, portal_form_collision defaults) stay.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Checkout, promotion-on-settlement, renewal recording, tier admin, importer, both widgets, the member listing — every net passes with assertions unchanged. Routes 272/272. No schema, no visual change. Membership invoice failures were never handled — do not add handling (preservation cuts both ways).
- **The cross-repo inversion is contract-first** (the 389 mechanics): surface-10 wording in `docs/plugin-contract.md` first; the webhook change lands in the sibling `crm-plugin--payments` checkout, tagged **v0.3.0**; core's lock bump + `^0.3` require + `distribution.json` constraint all in the same commit as the consuming listeners.
- **The manifest is edited, never the generated file.** `distribution.json` gains the `folder` entry; `plugins:manifest-sync` regenerates `config/plugins.php`; the guards verify the rest.
- **Gate-4 discipline for the PricingChart scss move:** after the asset path re-points, `widgets:sync` + `build:public` and prove the bundle still carries the pricing-chart styles. A green suite alone is not the proof.
- **The portal seam is preserved, not changed** (decision 5): the `membership.checkout` route name stays byte-identical from the plugin's route file; `Portal/SignupController` is untouched. Any deeper portal work is D5's.
- **Scope is binding.** No schema move (package phase); no extraction (no new repo yet); no D5+ work; no model/factory moves (§ 6.7); no per-plugin CI (D11); no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.15.0 → 0.16.0 (surface 10 memberships slice — the second cross-repo inversion; changelog); plan doc § Stage D D4 line annotated (carve done).
- **Block-shape call at close (decision 8):** decide from what the carve surfaced whether the rest of the block is one combined package+extract session or two; draft the next prompt(s) accordingly, with the block handoff (two-table redraw input, portal-seam composition-safety items) in the log.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code, state the plan, note any drift found — and resolve the two open questions: **(1)** the sibling `crm-plugin--payments` checkout is current (`git pull` it), and **(2)** the checkout-settled dispatch shape (whether `CheckoutSettled` already fires for membership-metadata sessions, which sizes the Payments diff). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
