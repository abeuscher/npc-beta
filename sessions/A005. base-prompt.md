# Base Prompt — Session A005 (Test Audit — Cycle 1)

**Start now.** Execute the reading list below, open the session prompt at `sessions/A005. Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **A005. Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain).**

This is **an agentic cloud session** with no release-plan execution-order slot. The A-prefix series carries autonomous-or-discussion-shaped work that runs in the agent's remote-execution environment, scheduled out-of-band with the numeric Beta-1 track. A001 (Content Import/Export coverage), A002 (Public-Bundle Consolidation), A003 (Playwright e2e Re-Stabilization), and A004 (Code Shape & Fit — Cycle 1 Review) are the prior A-series sessions; A005 is **Cycle 1 of the newly-lifted Test Audit track** (`sessions/tracks/test-audit.md`). The track was lifted at A003 close after A003's flake-chasing surfaced a class of bug — Playwright specs committed without ever running locally, plus accumulated `[test-integrity]` housekeeping items — that audits would catch.

This session is **NOT boundary-touching** — no Fleet Manager surface in scope. CRM contract stays at v2.3.0. No schema changes. No application-code changes expected. **The session's writes are confined to test code, planning artifacts, and one new workflow doc** — `tests/e2e/**/*.spec.ts` (deletions and rewrites), `tests/**/*.php` (renames and assertion-tightening from the D2 drain), `sessions/housekeeping-inbox.md` (drain), `sessions/tracks/test-audit.md` (Cycle 1 retrospective at close), and the new `docs/testing/playwright-discipline.md` workflow doc.

**Session shape:** track-phase / audit. Cycle 1 picks two of the four track dimensions — **D1 Playwright spec discipline** and **D2 Spec-claim integrity drain** — and walks them deliberately. D3 (next mutation slice) and D4 (Pest file relevance refresh) are queued for later cycles per the track doc's forward plan. The carve-out shape is on the table if the dimensions overflow a single session.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). They are the canonical format reference — do not infer format from previous session logs.
2. Read `sessions/session-outlines.md` for the active-tracks block. The Test Audit row points here.
3. **Read the track doc end-to-end:** `sessions/tracks/test-audit.md`. The four audit dimensions, the cycle shape, the cadence rule, and the standing improvements all live there. Cycle 1's retrospective lands back into this doc at close.
4. **Read the precursor logs** named in the track doc as prior partial audits — at least skim:
   - `sessions/archived/094. Test Audit & Bug Fixes — Log.md` (s094 Pest audit precedent — per-file keep/update/remove table shape; fast/slow boundary).
   - `sessions/archived/241. Test Audit (Mutation Testing) — First Slice — Log.md` (s241 mutation-testing precedent — slice discipline, `// guards:` override-keep convention).
5. **Read the existing testing workflow doc:** `docs/testing/mutation-audits.md`. The new D1 workflow doc this session ships (`docs/testing/playwright-discipline.md`) mirrors its shape; consistency matters.
6. **Read the A003 log:** `sessions/A003. Playwright e2e Re-Stabilization — Log.md` (or its archived location if it's been moved). The forcing function for this track is documented there — the specific bugs A003 surfaced (URL-resolution mismatch, ambiguous selectors, hidden-modal-placeholder `.first()` pickups, Filament header-action `requiresConfirmation()` on custom Pages) are the patterns Cycle 1's D1 sweep validates against.
7. **Skim the housekeeping inbox** at `sessions/housekeeping-inbox.md` — every `[test-integrity]` entry is a D2 drain candidate. Map them to dispositions during the audit walk.
8. **Read `docs/app-reference.md`** for environment names and Pest infrastructure references. Read `tests/e2e/README.md` for the Playwright harness shape. The session will reference the FixtureRunner (`tests/Feature/Generated/ImportFixtureRunnerTest.php`) as the canonical Pest-covers-this-already check for the importer suite's D1 verdicts.
9. Open `sessions/A005. Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain).md` and read the session prompt.
10. Note any drift between the session prompt and the actual artifacts in a brief work-log entry.

---

## Starting state inherited from session A003

- **CRM contract at v2.3.0** — unchanged through A003 (test-infra only).
- **Schema baseline:** no schema changes since session 308; A003 added none.
- **Fast Pest at A003 close:** baseline carried unchanged (A003 touched only `.github/workflows/tests.yml`, `tests/e2e/`, `sessions/`, and `scripts/ci/` — no `tests/Feature/` or `tests/Unit/` changes that would shift the count).
- **Playwright suite at A003 close:** 51 passed / 0 failed / 1 retry-recovered flaky / 8 skipped on the final post-deletion run; six total flaky tests were deleted across A003 (one whole-file delete plus four single-test-file deletes plus one in-file delete plus one describe-block removal). Test count dropped accordingly. The `inline-editing-foundation.spec.ts:79` retry-recovered flake from A003's last verification run lands on this session's D1 disposition table as the first concrete trigger from the page-builder cluster.
- **Planning state:** **Test Audit track lifted at A003 close** (`sessions/tracks/test-audit.md`); A005 is Cycle 1. `sessions/session-outlines.md` carries a new Active-tracks row pointing here. No release-plan execution-order slot consumed.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this session's D2 dimension absorbs every `[test-integrity]` entry. Non-`[test-integrity]` items (s313-close inbox additions, A003 inbox additions of the deletion-explanation variety) stay where they are.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Audit-shape session.** D1 produces a verdict-per-spec table before any deletions land; D2 produces a disposition-per-`[test-integrity]`-item table before any renames or deletions land. Don't pipeline from finding to fix — surface the tables to the user, take input, then apply.
- **Carve-out is allowed.** If D1's verdict table reveals more rewrite work than this session can absorb (the page-builder cluster's cold-load flakes, the importer suite's redundancy cleanup, or both), lift the heavier work to a successor session per the 207 / 275 carve-out precedent. The track doc's carve-out rule is the canonical reference.
- **FixtureRunner is the safety net for importer D1 verdicts.** Before deleting any importer Playwright spec as "Redundant," verify the same data path is covered by a `tests/Feature/Generated/ImportFixtureRunnerTest.php` case (importer × shape pairing). The `BuilderRegistry::IMPORTERS` array names the 7 importers; the dataset names the 4 shapes (clean / messy / corrupt / pii). A spec asserting a path outside that matrix is genuine coverage and the verdict should re-evaluate.
- **No application-code changes.** Test-code changes only. If the audit surfaces a real bug in production code (a spec was right, the product is broken), surface to the user — fixing it is out of A005's scope per the audit-shape discipline, and the disposition is either a housekeeping inbox entry or a separate session lift.
- **Mutation slice continuation (D3) is out of scope this cycle.** Even if the audit walk surfaces a tempting slice from s241's forward queue, queue it for the next cycle rather than absorb it here. Single-cycle scope discipline.
- **Pest file relevance (D4) is out of scope this cycle.** The s094-style per-file walk is the next cycle's work, after Cycle 1 closes and the patterns settle.
- **Hard-no files for this session.** `app/`, `resources/`, `database/`, `routes/`, `config/`, `bin/`, and `public/` are read-only. The session writes to `tests/`, `sessions/`, and `docs/testing/playwright-discipline.md` only.
- **Cloud-session constraints.** No Docker daemon; the canonical local Pest run (`./dev test`) is not available. CI is the authoritative verification signal — push the branch, wait for the `Tests` aggregate to land green. If a D2 rename touches a Pest test, run `phpunit --filter='<TestName>'` discipline conceptually but rely on CI for the actual signal.
- **PM-level tone for status summaries.** Bookkeeping codes (D1/D2 verdicts, item numbers) stay in the doc edits and the log; the discussion uses plain language.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list above and the Starting state are read, surface a one-paragraph orientation — what stood out from the precursor logs (s094 + s241), what the agent considers the headline call for D1 (the page-builder cluster's cold-load flakes, the importer suite's redundancy footprint, or both) and the headline call for D2 (which `[test-integrity]` items have the biggest payoff to drain), and what (if anything) needs clarification on the carve-out call — and wait for the user to confirm before starting. Do not pipeline straight from reading into the verdict table.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

A005 has no immediate successor in the A-series. The numeric track continues at the user's pace. If D1 or D2 carved-out, the carve-out's prompts (base + session) are drafted at this phase — otherwise no next-session prompts to draft.

If carve-out happened: draft the successor's prompts using the established carve-out shape (single-dimension follow-on against the same track, branch naming `session-A005-N/1` or `session-A006/1` per user preference).

### Phase 2 — Close

When the user says to close:

- Write `sessions/A005. Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain) — Log.md` from `sessions/template-session-log.md`. Capture: the D1 verdict table (per spec) with applied dispositions, the D2 drain table (per `[test-integrity]` item) with applied dispositions, the new `docs/testing/playwright-discipline.md` shape, any standing improvements the cycle promoted, any items that promoted from the audit to the housekeeping inbox or to a release-plan stub.
- **Update `sessions/tracks/test-audit.md`** — Cycle 1 retrospective lands in the Cycle Retrospectives section using the shape `tracks/code-review-and-cleanup.md` Cycle 2 entry uses (quantitative outcomes, load-bearing decisions, blind spots that surfaced, process incidents, won't-fixes reaffirmed). Update the status snapshot (last-update date, complete, active, next-trigger).
- Update `sessions/completed-sessions.md` with the A005 row.
- Update `sessions/session-outlines.md` if the active-tracks block's Test Audit row needs adjustment.
- **Archive A003** per the archive rule — `sessions/A003. *.md` files move to `sessions/archived/`. A004 status is independent (the A-track ordering is not strictly chronological).
- Bump the `VERSION` file at the project root per the s313/2 close-gate rule. Format `0.<session>.<iteration>` — A-track sessions use the A-prefix in the iteration mark (e.g. `0.A005.01`) or the calling convention the user has settled by then; check `sessions/template-base-prompt.md` for the canonical form at session time.
- Commit on `session-A005/N`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- The log narrates the audit walk in prose (not bullet-soup); verdict + disposition tables land as discrete blocks.
- The new `docs/testing/playwright-discipline.md` doc mirrors `docs/testing/mutation-audits.md`'s tone and shape — concise, durable, links procedure to rationale.
- No new tests added in this session (per the audit discipline). Renames and assertion-tightening from D2 are not "new tests" — they are existing tests made honest.
