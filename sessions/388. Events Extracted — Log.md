# Session 388 — Events Extracted — Log

**Date:** 2026-08-19
**Branch:** session-388/1

---

## What was built

Arc D2 of the Plugin Architecture arc — the known-edges extraction. Events, a Stage B path package since 383, left the repo for the owner-created public `github.com/abeuscher/crm-plugin--events` (tag `v0.1.0`) via the extraction runbook's third run: the first migration-owning extraction, the first testsuite-carrying extraction, the first with real front-end assets since LogoGarden (gate 4 live), and the run that emptied `plugins/`. Behavior-preserving throughout: routes 272/272 (dev/test), fast suite at the 387 baseline with assertions unchanged, no visual surface, no schema. One iteration, one commit (`869ef80d`).

### Phase 1 — Events extracted (the runbook run)

- **Plugin repo:** 89 files copied verbatim to the sibling checkout; `version` field dropped (tags are the metadata); five asset paths in three widget definitions rewritten vendor-relative (EventMiniCalendar scss+js, EventRegistration js, EventsListing scss+js — the EventsListing→BlogPager **core** stylesheet input left as-is, per the survey); FM-style README (contract 0.12.0, tags-are-releases, the tests-travel-with-the-package note, the thumbnail-regeneration note); committed, tagged **`v0.1.0`**, pushed.
- **Core swap:** manifest entry `path` → `vcs` (`plugins:manifest-sync` a provider-list no-op); composer repository swapped to the vcs URL, require unchanged (`^0.1`); `plugins/Events/` removed; `composer update nonprofitcrm/events` re-pinned the lock (git source at the exact tag commit + dist zip, v0.1.0 — composer-before-artisan per the 387 caveat); Dockerfile COPY lines ×2 removed; `composer validate --strict` clean.
- **The testsuite move (decision 2):** phpunit's `Events` suite re-pointed `plugins/Events/tests` → `vendor/nonprofitcrm/events/tests` — same 29 files, 236 tests, identical counts under paratest and in CI. No `DistributionManifestGuardTest` edit needed (its testsuite assertion polices `plugins/` suites only), as predicted.
- **Re-points, all assertion-content-unchanged:** seven template file-reads (`ContentListingWidgetsTest` ×6, `AppearanceConfigRenameTest` ×1); the two fixture migrator reaches (`EventsRemovedTestCase`, `EventsDisabledViaFlagTestCase` → `vendor/nonprofitcrm/events/database/migrations` — disabled ≠ uninstalled proven against the vendor'd migrations); the `WidgetColorTokenConsumptionTest` baseline key; `PluginPackageGuardTest`'s schema-dump owner map. Comment/doc wording updated (`EventLandingPageFactory`, `EventModuleBoundaryTest`, `docs/app-reference.md`, the three schema docs + README).
- **The empty-`plugins/` relaxations (drift beyond the survey, adapted per the drift rules):** the survey predicted one guard needing relaxation (`PluginPackageGuardTest`'s non-empty glob); emptying the directory actually tripped **three** — git tracks no empty dirs, so `plugins/` vanished entirely and `PluginAdminCssGuardTest`'s `RecursiveDirectoryIterator` threw while `WidgetTemplateBoundaryTest`'s sanity check found no `plugins/` targets. All three got the same scoped fix — tolerate an empty in-repo set, assert the shape for whatever exists, content scans unchanged (the third also needed an explicit assertion so phpunit doesn't flag the empty loop risky). Folded into the runbook's step 8.
- **Docs:** plugin contract **0.11.0 → 0.12.0** (third extracted reference implementation; External repo gains the two-posture test-membership rule and the plugins-may-be-empty bullet; surface 5 fixture-vendor-path note; surface 11 at-scale vendor-path + cross-input note; changelog); runbook deltas folded (388 worked-example lines at steps 0/3/7, the step-8 empty-`plugins/` note, step 9 rewritten as the settled two postures, gate 4's cross-input grep); plan doc D2 ✅, arc header advanced to D3.
- 389 prompts drafted post-verification (owner-approved) from a fresh Donations survey — see D3 kickoff input below.

---

## D3 kickoff input (what D3 — Donations — inherits)

1. **The survey ran at 388 close (full results in the 389 session prompt):** models `Donation`/`DonationCredit`/`DonationReceipt`/`Fund` (+3 policies, 2 observers) stay core per § 6.7; `DonationCheckoutController` is already fully on the surface-13 capability layer (carve-ready); admin = `DonationResource` (4 pages) + `FundResource` (3) + `DonorsPage` (459 lines, year-end receipts) + the import page pair; importer registries ready for an `ImporterContribution`; widgets `DonationForm` (js asset) + `RecentDonations` (none); ~46 core test files reference the models; all four donation tables in the core dump (the package session's redraw input).
2. **The webhook inversion is cross-repo now:** the donation branches live in the **extracted** `crm-plugin--payments` — the `donation_id` checkout-settled branch *and* the recurring/invoice branches (`Donation::where('stripe_subscription_id', …)`). Inverting them is the arc's first post-extraction plugin release (drafted: one `v0.2.0` release covering both, a second core event for invoice settlement, core's require/constraint widened to `^0.2` in composer.json + distribution.json same-commit). The one-release-or-two split is 389's Open-Gate question.
3. **Surface 7 is genuinely forced:** `donation_receipt` + `donation_acknowledgment` live in `EmailTemplate::defaults()`'s hard-coded array; the carve builds the handle registry (ImporterRegistry pattern) with the donation handles as its first plugin contribution. **The three events handles also still sit in core's array** — Events' mailables read `forHandle()` against core defaults; migrating them is a `crm-plugin--events` release, backlogged, not D3 scope.
4. **Surface 9 scoped honestly:** the donations data shape already satisfies the normative rule (plugin-owned tables on `contact_id`); the contact-screen socket half is built only if the carve strands a contact-screen surface — promotion on exercise, not ambition.
5. **This run's mechanics learnings:** an extraction that empties `plugins/` trips guards that *assume* the directory exists, beyond what a path-grep survey finds (three relaxations recorded in runbook step 8 — they tolerate the refilled state too, so D3's carve needs no revert); the vendor-path testsuite runs clean under paratest and CI with zero special handling; a cross-plugin asset input (EventsListing→BlogPager) needs its own gate-4 grep and stays a core repo-relative path.
6. **plugins/ refills at D3:** the new `Donations` entry enters `distribution.json` as source-kind `folder` (the honest carve state); `plugins:manifest-sync` will NOT be a no-op this time (a provider line is added).

---

## Security considerations

No new endpoint, gate, or input path; the plugin's files moved verbatim (registration throttling, portal-auth stack, policy gating, and every boundary guard pass with assertions unchanged, now scanning the vendor path). Supply-chain posture: the plugin arrives from a public GitHub repo pinned in the lock to an exact tag commit (git source + dist zip) — the trust model established at 385, third consumer; core bumps deliberately via `composer update`. The plugin-owned migrations now execute from the vendor path — same DDL bytes, proven by the unchanged behavior nets and the fixtures. `deploy.yml` untouched; FM surfaces untouched; CRM contract stays v2.7.0; client-billing guards unaffected.

---

## Schema changes

None. (The three events tables remain plugin-owned and now ship inside the package — the fixture re-point is the proof that disabled ≠ uninstalled survives the move.)

---

## Production deployment notes

None beyond standard `migrate`. Images build identically from the deploy pipeline's perspective (`deploy.yml` zero-diff); `composer install` fetches all three plugin packages credential-free — proven in-image locally and by CI's image-building job (the e2e stack built, migrated, and seeded through the vendor'd migrations on a fresh runner). VERSION bumped 0.387.01 → **0.388.01**.

---

## Deferred decisions

- **Events adopts the surface-7 email-template registry** (its three handles stay in core's defaults array) — a future `crm-plugin--events` release, after D3 builds the registry.
- **Per-plugin CI + core-side plugin-subject test moves** — accepted debt, unchanged; resolves at D11.
- **`baseDir()`-derived asset declarations** — still backlog.
- **Products/store code's home** — plan doc § 6.8; resolves at D8's prompt draft.
- **Playwright chrome-parity failure** — still the pre-existing parked item, non-gating.
- **Stale local `.env.production`** — still the owner's to delete (384 rider).

---

## Test result

- **Fast Pest:** 3263 passed, 0 failed (parallel) — the 387 baseline exactly; assertion content unchanged (path re-points, the baseline-key re-point, three scoped guard relaxations).
- **`Events` testsuite from the vendor path:** 29 files, 236 tests, all green — identical counts.
- **Route parity:** 272/272 (dev/test environment).
- **Composer:** `validate --strict` clean; lock pins the tag (git source at the exact commit, dist zip, v0.1.0).
- **Runbook gates:** 1–3 pass; gate 4 **live and proven** — `widgets:sync` + `build:public`, built CSS carries `events-listing` (×28) / `mini-calendar` (×3) / the BlogPager pager styles (×2), JS registers all three Alpine components (`eventMiniCalendar`, `eventRegistration`, `eventsListing`), swiper in the manifest; gate 5 in-image proof clean (all three packages fetched from GitHub inside `docker build`, provider + migrations present at the vendor path as a real directory, autoload `--no-dev`, `plugins/` absent from the image); gate 6 **CI `Tests` aggregate green** on the pushed branch (fast + slow; Playwright = the parked non-gating chrome-parity item — 62 passed, 1 flaky-passed-on-retry, its stack built and seeded from the extracted plugin cold).
