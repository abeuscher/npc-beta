We are about to begin a new session: **185. Image Support Across Content Types**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, then read the individual table files under `docs/schema/` relevant to this session's scope (at minimum: `pages`, `media`, `sample_images`, and anything touching `Post`, `Event`, `Product`, `Contact`).
3. Review the session-184 log (`sessions/184. Sample Image Library & Demo Seeder Integration — Log.md` if present, otherwise the session prompt) to understand the pool API (`SampleImageLibrary`, `SampleImage::CATEGORY_*`).
4. Open `sessions/185. Image Support Across Content Types.md` and read the session prompt carefully.
5. Summarise your understanding and confirm you are ready to proceed.
