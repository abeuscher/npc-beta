# Base Prompt — Session 302 (Theme/Template Re-Taxonomy 4 / final — Naming & IA Polish)

**Start now.** Execute the reading list below, open the session prompt at `sessions/302. Naming & IA Polish — Re-Taxonomy Tail.md`, then begin the work. No confirmation needed.

---

We are about to begin: **302. Naming & IA Polish — Re-Taxonomy Tail (Theme/Template Re-Taxonomy 4 of 4, arc-closing)**.

This is the **arc's optional ≤0.5-session naming/IA polish tail** — stub-point 4 of the **"Theme & Template Re-Taxonomy" stub in `sessions/session-outlines.md`**, and the **final session of the arc**. 297 shipped the colour foundation; 300 migrated the widgets; 301 shipped schemes / chrome / the shared resolver. Nothing functional remains — this session is **relabel, regroup, and doc consolidation only**. On close it **fully retires the arc and `release-plan.md` § E6**; the session after 302 returns to the `release-plan.md` execution order (the Public Website Complete milestone continues; D2 is unblocked).

This session is **NOT boundary-touching** — no Fleet Manager surface. **CRM contract stays at v2.3.0.** If anything appears to need a Fleet / `/api/health` touch, stop and surface — scope drifted.

**Session shape:** small IA / cleanup — admin Filament relabels + one settings-section move + one doc consolidation. **No new feature, no behaviour change, no schema change expected.** May be *smaller* than 0.5 session; that is fine — do not pad it.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — stable since 276.
2. Read `sessions/session-outlines.md` — the Active-tracks block and **the "Theme & Template Re-Taxonomy" stub**, especially the **"Naming/IA mismatches to fix along the way"** paragraph (the catalogued list this session executes) and stub-point 4. Note which catalogued items 297/300/301 already resolved so they are not re-done.
3. Read `release-plan.md` § E6 only to confirm this session **fully retires** it (the arc supersedes & absorbs § E6; 302 closes the arc).
4. This session does **not** belong to an active track. The arc stub is canonical for scope.
5. Read the actual surfaces before touching them (the prompt grounds each to its file):
   - `app/Filament/Pages/Settings/CmsSettingsPage.php` — nav group `Settings`, label `CMS`; the mislabeled **"Header & Footer Code Snippets"** section that actually holds favicon + default OG image; the **Stripe-checkout branding** fields/section that belong in Finance.
   - `app/Filament/Pages/Settings/FinanceSettingsPage.php` — the existing Finance-group settings home the Stripe-checkout branding moves into (no new page needed).
   - `app/Filament/Resources/TemplateResource.php` — one resource/label (`Templates`) conflating `type=page` and `type=content`; the IA split.
   - `app/Filament/Pages/DesignSystemPage.php` — label `Theme`; review post-297 (it now owns Colors + typography + buttons, so the old "Theme can't set colour" mismatch is resolved — confirm, don't re-open).
   - `docs/theme-color-tokens.md` — stale session numbers (`session 298` → 300; `session 299` → 301; "no per-template colour between sessions 297 and 299" is resolved by 301) and the "consolidate the published widget-dev token contract doc" item.
6. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules. Several catalogued mismatches were partly resolved in 297/300/301 — **do not re-do resolved items**; only the genuinely-open ones below are in scope.

---

## Starting state inherited from session 301

*(Session 301 — Schemes, Template Page-Shell & Chrome, Shared Resolver — closed immediately before this session. Re-confirm against 301's actual log at start; adapt silently to drift.)*

- **CRM contract at v2.3.0** — unchanged; 301 and 302 are non-boundary.
- **301 shipped:** Default + Inverse schemes; the per-template **scheme selector** + **`no_header`/`no_footer`** suppression (migration `2026_05_18_120000_*`; `templates.scheme`/`no_header`/`no_footer`, concrete defaults); the **single shared `TemplateAppearanceResolver`** wired into the public layout (`<main>`) and the page-builder preview with no divergent branch (request-time inline, never bundled, outside the 296 guard by design); export/import carry-through of the three columns; the catalogued **`bg`/`text` content-region gap closed** (`.np-site main, .np-site.widget-preview-scope` consume the tokens — bundle-delivered, 296-guarded); the **"the Theme header"** chrome relabel (the `Default template` → `Theme` reframing is already done for the chrome sub-pages). Detail: `sessions/301. … — Log.md`.
- **Fast suite green at 301 close: 2474 / 0** (2465 at 300 close + 7 `TemplateAppearanceResolverTest` + 2 export round-trip). `DesignGroupIntegrityTest` pinned list count **19** (added `TemplateAppearanceResolverTest`). Confirm the exact number from 301's log.
- **Guards green at 301 close:** 296 `AssetBundleDriftGuardTest` FRESH (bundle rebuilt to `public-widgets-edf05ea9.css` after the `_base.scss` consumption change); 300 `WidgetColorTokenConsumptionTest` green. A relabel/IA session should not touch the bundle — if it does, rebuild and re-confirm FRESH.
- **e2e:** 301 added `tests/e2e/template-scheme.spec.ts` (5 tests, three-surface scheme-switched, computed + rendered paint) on the isolated stack via `npm run test:e2e:isolated`. `feedback_test_runs_not_parallel` applies.
- **Environment notes (carry, do not re-fix):** local + isolated postgres roles and `.env` are aligned to `DB_PASSWORD=Password123` (301 fix; `.env` is gitignored). The Playwright CI job in `.github/workflows/tests.yml` was reordered (npm ci before docker compose) — uncommitted on the branch with the rest of 301 unless the user has since committed. None of this is 302's concern unless it blocks the suite.
- **Housekeeping inbox** — walk `sessions/housekeeping-inbox.md` at start/close per Rule 2. 302 is a focused IA tail, **not** a batch — it does not absorb the inbox; only fold an inbox item if it is literally one of the catalogued naming/IA items.

---

## Process rules for every session

Base-prompt process rules (`sessions/template-base-prompt.md`) apply in full. Session-specific:

- **Relabel/regroup/move/doc only — zero behaviour change.** A user clicking through the admin must reach the same data and the same outcomes; only the labels, nav grouping, and where a settings section lives change. `SiteSetting` keys are **not** renamed (only their UI home/label moves) — a moved Stripe-branding field must read and write the exact same `SiteSetting` key as before.
- **Do not re-resolve done items.** 297 gave Theme a Colors editor; 301 did the "Theme header" reframing and the `bg` gap. Confirm these as done and leave them. Only the still-open catalogued items are in scope (see the session prompt).
- **Tests follow labels, they do not gate the rename.** If a Pest/e2e test asserts an old nav group, label, or section title, update the test in the same pass to the new string — that is expected churn for an IA session, not a regression. Keep the suite at the 301 baseline + only label-driven test edits, every delta explained.
- **Scope fences — do not cross them:**
  - **No token-list / scheme / widget-SCSS / resolver change** — 297/300/301 are done and guarded. The token doc work is **wording/session-number consolidation only**, not a contract change.
  - **No schema change.** If an IA item seems to need one, stop and surface — it has drifted out of "naming/IA polish."
  - **No new feature or behaviour.** The page-vs-content "split" is presentation/IA (nav + label clarity), not a data-model or capability change.
  - **No Fleet / `/api/health` / response-schema change** — non-boundary, v2.3.0.
- **Verify objectively, but proportionately.** This is low-risk. Run the fast suite at the end; spot-check the relabeled/moved admin surfaces in a real browser (the moved Stripe section still saves; the renamed sections still show the same fields). No multi-surface scheme captures — that was 301. Do not manufacture judgment gates (`memory/feedback_dont_gate_on_taste_defaults`).

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close.** Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

The arc is **complete** at 302 close (this is its final session). The next session returns to the **`release-plan.md` execution order** — the Public Website Complete milestone continues; D2 is now unblocked (the arc was its Rule-8 prerequisite). Draft the next session's documents per the base-prompt template **only when the user names it** — do not auto-pipeline, and do not assume which release-plan item is next; reflect the actual execution-order state.

### Phase 2 — Close

When the user says to close: session log per `template-session-log.md`; update `release-plan.md` (**§ E6 fully retired — the arc is complete; 302 closed**); update the "Theme & Template Re-Taxonomy" stub in `session-outlines.md` (**mark the arc complete — all four points resolved**, carry nothing forward); append to `completed-sessions.md`; archive the immediately-preceding session's files into `sessions/archived/` (`sessions/301. *`); commit on the final `session-302/N` branch; notify; do not push; do not begin the next session until the user starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Re-Taxonomy IA tail: CMS-Settings relabel + Stripe-branding → Finance + page/content template IA split + token-doc consolidation + VERSION 0.302.1".
- **PM-level tone for status reports** per `memory/feedback_pm_level_tone.md`. Keep it short — this is a small session; report it as one.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Relabel, do not reinvent.** Move/rename within the existing Filament pages/resources and the existing `FinanceSettingsPage`. No new abstractions, no new settings storage, no redesign.
