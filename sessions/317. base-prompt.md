# Base Prompt — Session 317 (Accessibility Pass — Aria + Usability + Lighthouse)

**Start now.** Execute the reading list below, open the session prompt at `sessions/317. Accessibility Pass — Aria + Usability + Lighthouse.md`, then stop at the Open Gate before beginning task work.

> Note: the first housekeeping batch (session 314) spilled into a second batch (session 315), so this prompt was bumped to session 317. Execution-order position is unchanged — the accessibility pass is still position 45 (the E19 plan entry); only the session number moved.

---

We are about to begin a new session: **317. Accessibility Pass — Aria + Usability + Lighthouse.**

This is **execution-order position 45** in `sessions/release-plan.md` — § E19 Accessibility pass. Sequenced inside the Public Website Complete milestone block (Rule 12), promoted from the housekeeping inbox at session 313 close. Combines three related sweeps into one consolidated session — Aria attributes, usability of operator-facing controls, and a Lighthouse audit using the locally-installed Lighthouse app.

This session is **NOT boundary-touching** — no Fleet Manager surface in scope. CRM contract stays at v2.3.0. No schema changes expected.

**Session shape:** audit-style with bundled remediation. Run the three sweeps in order (Aria → usability → Lighthouse), surface findings inclusively, then walk them with the user to triage which findings land in this session vs. carve out. Default leaning at audit-finding triage: fix in this session unless the finding has grown past one-iteration size, in which case carve out per the cleanup-track Open Flag pattern. Six-iteration ceiling on the remediation half.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). They are the canonical format reference — do not infer format from previous session logs.
2. Read `sessions/session-outlines.md` for active-tracks and any stub or open gap referencing accessibility (the public-website gap report at `sessions/public website/gap-report.md` may carry related rows).
3. Read the `sessions/release-plan.md` entry at execution-order position 45 (§ E19) — canonical for scope.
4. Read `docs/app-reference.md` for the surface inventory. The public-render surface and the operator-facing admin surface are both in scope; the portal is in scope to the extent the portal is part of the public-website demo.
5. Open `sessions/317. Accessibility Pass — Aria + Usability + Lighthouse.md` and read the session prompt.
6. Note any drift between the session prompt and the actual code in a brief work-log entry.

---

## Starting state inherited from session 316

- **CRM contract at v2.3.0** — unchanged through 313 / 314 / 315 / 316.
- **Schema baseline:** no schema changes expected; accessibility work touches markup + ARIA attributes + tab order, not data shape.
- **Fast Pest at 316 close:** baseline depends on the record-duplicate session's outcomes. Confirm at session start.
- **Playwright suite** running in CI; isolated stack via `npm run test:e2e`. Accessibility-relevant assertions may be added (e.g. axe-core via a Playwright integration if the team picks that path).
- **Planning state:** E19 promoted at s313 close; placement between the record-duplicate work (E18, session 316) and the media-finder work (E20, session 318).
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — the text-toolbar `title` attributes item is folded into this session's Aria sweep per the release-plan note.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Audit findings default to fix** per `feedback_audit_findings_default_to_fix`. Surface inclusively; "won't-fix" comes from the apply walkthrough, not from audit-side filtering.
- **Three sweeps in order** — Aria first (mechanical, well-defined), then usability (judgment-heavy, surfaces UX-level findings that may bleed into the inbox), then Lighthouse (tool-driven, fills any gaps the prior two missed).
- **Lighthouse tooling.** The user has the Lighthouse app installed locally. Use it via the agent's available mechanisms; if direct invocation isn't possible, run Lighthouse via the Chrome DevTools CLI against the dev server and capture the JSON report.
- **Operator-facing affordances first, public-render second.** Operators run the admin every day; public-render accessibility shows up on the demo site. Both matter; prioritize the surface that has the higher per-issue user impact.
- **Aria findings often pair with `title` attribute work.** The inbox item for text-toolbar `title` attributes is part of the Aria sweep — don't ship it separately.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **Questions are not instructions** per `feedback_questions_are_not_instructions`.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list above and the Starting state are read, surface a one-paragraph orientation — which surfaces are in scope (public-render, admin, portal), how the three sweeps will be sequenced, what the Lighthouse-invocation approach is — and wait for the user to confirm before starting. Do not pipeline straight from reading into implementation.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- Copy `sessions/template-base-prompt.md` to `sessions/318. base-prompt.md`.
- Draft `sessions/318. Media Duplicate + Unused Finder.md` from `sessions/template-session-prompt.md`.

Then stop. The user reviews the work and the next session's drafts, and initiates close when ready.

### Phase 2 — Close

When the user says to close (after the next session's prompts are agreed):

- Write `sessions/317. Accessibility Pass — Log.md` from `sessions/template-session-log.md`. Per sweep: findings inventory, what landed, what carved out, Lighthouse score deltas if captured.
- Update `sessions/completed-sessions.md` with the 317 row.
- Update `sessions/release-plan.md` § E19 marking the entry closed.
- Update `sessions/housekeeping-inbox.md` — remove the text-toolbar `title` attributes item (absorbed into the Aria sweep) and any other inbox items resolved by this pass.
- **Archive the previous session's files** per the archive rule.
- Commit on `session-317/N`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages name the sweep (Aria / Usability / Lighthouse) + the surface touched; no docstrings/comments on code you did not write; follow existing markup conventions (don't restructure templates to add Aria — add the attributes in place).
