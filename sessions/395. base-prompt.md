# Base Prompt — Session 395 (Member Portal Packaged & Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/395. Member Portal Packaged & Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **395. Member Portal Packaged & Extracted**.

This is **arc position D5, second and final session of the block** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical). The carve landed at 394 (`plugins/MemberPortal/` — the six widgets, six controllers + 17 routes, the auth stack, the `member-portal` capability, the Events v0.1.2 rider; contract 0.18.0). This session **closes the block in one combined session** (the D4/393 compression precedent, decided at the 394 close): package (`nonprofitcrm/member-portal` as a path-repo package, P6 mechanics) + **the fourth squash-boundary redraw** (the two portal tables leave core's dump — a sequence-carrying redraw, the donations mechanics) + the **portal-absent composition-safety hunt** (richer than D4's — the contact admin screen and the page-topology seeders, surveyed below) + **the sixth extraction-runbook run** to `crm-plugin--member-portal`, emptying `plugins/` a fourth time.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.18.0 → 0.19.0 at close (Package shape sixth consumer; surface 5 fourth squash-redraw consumer — the second sequence-carrying redraw, the seeder-topology note; External repo sixth consumer; changelog).

**Session shape:** migration-squash + extraction, behavior-preserving on the full composition. Routes 272/272 byte-identical; no visual change; the schema move is representational (identity check (a) proves byte-fidelity). **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D4 ✅, D5 open — carve done 394) and § 6.7 (models stay core — `PortalAccount` and its two mailables stay core; the 394 disposition).
3. Read the 394 session log (`sessions/394. Member Portal Carved — Log.md`) — especially its **block handoff** section: the redraw input, the composition-safety survey, and the gates already landed at the carve (the five `auth('portal')` reads are done — do not re-gate them).
4. Read `docs/plugin-contract.md` at 0.18.0 — surface 5 in full (the squash-redraw procedure, both identity checks, the sequences-travel and seeder-gate notes), § Package shape (P6 mechanics — explicit version, bound require, path repository, per-plugin Dockerfile COPY lines), § External repo (the extraction posture — version field dropped, public repo, lock pinned to the tag), § The distribution manifest (the `folder` → `path` → `vcs` source-kind walk and the guard interlock).
5. Read `docs/plugin-extraction-runbook.md` — the sixth run; the 393 worked example carries the same-session compression nuance (vendor-relative reaches valid from the package step — fixtures and in-plugin path assertions land in final form directly).
6. Read the 393 session log (`sessions/archived/393. Memberships Packaged & Extracted — Log.md`) — the one-combined-session playbook this session repeats, and the 390 log (`sessions/archived/390. Donations Packaged — Log.md`) for the sequence-carrying redraw mechanics (`donation_receipts_id_seq` — the precedent for `portal_accounts_id_seq`).
7. Open `sessions/395. Member Portal Packaged & Extracted.md` — the redraw input and front-loaded decisions; note the Open-Gate items (the owner-created `crm-plugin--member-portal` repo; sibling checkout freshness).
8. Skim the surfaces: `plugins/MemberPortal/` as carved at 394 (provider, routes, widgets, tests, the twin fixtures at `tests/Fixtures/Plugins/MemberPortal*TestCase.php` with their expiring no-migrator-path caveat); the two tables' DDL in `database/schema/pgsql-schema.sql` (~lines 1039–1090, constraints ~2062–2082, index ~2612, FK ~3049); `database/seeders/DatabaseSeeder.php` (the `PortalPageSeeder`/`SystemPageSeeder` calls and the 393 `MembershipTierSeeder` gate precedent at ~line 179); `app/Filament/Resources/ContactResource.php` (the Portal Access section) + `app/Filament/Resources/ContactResource/Pages/EditContact.php` (the four portal actions — the hunt's likeliest trips); `database/seeders/DemoPortalMemberSeeder.php`.
9. Note any drift between the session prompt's captured survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 394

- **CRM contract at v2.7.0** — 394 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D4 complete; D5 carve done. **Five extracted plugins** (`crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` v0.3.0, `crm-plugin--events` **v0.1.2** — the 394 capability rider, `crm-plugin--donations` v0.1.0, `crm-plugin--memberships` v0.1.0) plus **`plugins/MemberPortal/` in-repo** (manifest source `folder`, sixth entry). Plugin contract **v0.18.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** nine plugin-owned tables in their packages' migrations; `portal_accounts` + `portal_password_reset_tokens` are the last vertical-owned tables in core's dump — **this session moves them**. `portal_accounts` carries `portal_accounts_id_seq` (bigint PK — the second sequence-carrying redraw) and one outbound FK (`contact_id → contacts ON DELETE SET NULL` — no RESTRICT behavior this time); **zero inbound FKs from core tables** (verify at session). `portal_password_reset_tokens` is three columns, PK on email, no FKs, no sequence.
- **Fast Pest suite green at 3384 / 0 parallel** (394 close: 3355 baseline + 29 additive). The `MemberPortal` testsuite (4 files, 37 tests) runs from `plugins/MemberPortal/tests` — it re-points to `vendor/nonprofitcrm/member-portal/tests` at extraction. Expected delta this session: **additive** — the schema twin pair (the 393 shape: tables-exist + removal-path coverage); existing assertions unchanged beyond the fixture migrator paths.
- **Route baseline: 272 routes** (dev/test), byte-identical to main at 394 modulo the moved middleware FQCN (the 381 action-FQCN exception) — unchanged this session.
- **Composition-safety state (what 394 already gated — do not redo):** the five core `auth('portal')` reads gate on the `member-portal` capability (`PageContext`, `PageController`'s member branch → 404, `ActivityLogger`, `FormSubmissionController`'s collision block, the `portal_member` resolver arm); the removal mirror exercises all five. **What this session's hunt owns:** the portal-*table* reads and the seeded topology (the session prompt's survey).
- **The twin fixtures carry the expiring caveat:** `MemberPortalRemovedTestCase` / `MemberPortalDisabledViaFlagTestCase` register no migrator path — they **must gain it this session** the moment schema moves (vendor-relative directly — the 393 same-session compression nuance).
- **Playwright suite** — CI signal, non-gating; the parked chrome-parity spec remains the known failure. No local Playwright.
- **Housekeeping inbox** — the two 389-folded items stay; the `portal_form_collision` defaults item was checked at the 394 close: the carve kept the collision surface core, so the item stands unchanged. The capture buffer was empty at 394 close. This shape absorbs no items.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **The package is P6 mechanics verbatim** (the 393 run): `plugins/MemberPortal/composer.json` (`nonprofitcrm/member-portal`, explicit `version: 0.1.0`, PSR-4 `Plugins\MemberPortal\` → `""`, PHP `^8.2`, no third-party dependencies); root path repository + bound `^0.1` require; manifest `folder` → `path` same-commit with the lock; one Dockerfile manifest-COPY line per composer stage; `plugins:manifest-sync` expected a provider-list no-op; `composer validate --strict` clean.
- **The redraw is the surface-5 procedure** (390's sequence mechanics): the two tables leave `database/schema/pgsql-schema.sql` for raw-DDL create-table migrations under `plugins/MemberPortal/database/migrations/` (creation order `portal_accounts` → `portal_password_reset_tokens`); the sequence travels complete (`CREATE SEQUENCE` + `OWNED BY` + `SET DEFAULT nextval`); the FK, unique constraint, and index travel byte-faithfully; `loadMigrationsFrom` in `boot()`. Core dump regenerated against a scratch database with the portal tables dropped first (recreate the database, not the schema). **Both identity checks run and must pass:** (a) full composition byte-identical modulo the two migrations rows + the sequence + pg_dump's per-run token, with seeded-data spot counts; (b) portal-absent composition (`PLUGINS_DISABLED=member-portal`) boots, seeds, and smokes clean with zero portal tables.
- **The composition-safety hunt fixes what check (b) trips** (the 383/390/393 discipline). Surveyed candidates, front-loaded in the session prompt: the `DatabaseSeeder` page-topology calls, the ContactResource/EditContact portal-table reads, `DemoPortalMemberSeeder`. The capability (`present('member-portal')`) is the gate signal — the 394 precedent, not route presence. Untripped candidates go to recorded debt, not speculative gates.
- **The extraction is the runbook, sixth run:** owner-created public `crm-plugin--member-portal`; copy, drop the `version` field, asset declarations vendor-relative, in-plugin path reads re-pointed (grep the traveling tests for `plugins/MemberPortal` paths), README on the established model; tag `v0.1.0`; core swap `path` → `vcs` + `git rm`, `composer update nonprofitcrm/member-portal` (composer before artisan), Dockerfile COPY lines removed; phpunit `MemberPortal` suite → the vendor path; docs re-points. `plugins/` empties a fourth time — expect zero new guard relaxations.
- **Gate 4 re-runs at extraction** on the two `script.js` declarations (vendor-relative): `widgets:sync` + `build:public` + bundle-content proof (the `portalSignup` component + the change-password script).
- **The manifest is edited, never the generated file.** Scope is binding: no portal-vertical feature work, no per-plugin CI (D11), no model/factory moves (§ 6.7), no FM surface, no D6 work. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.18.0 → 0.19.0 (Package shape sixth consumer; surface 5 fourth redraw consumer — second sequence-carrying, the topology-seeder note; External repo sixth consumer; changelog); plan doc § Stage D D5 line closed ✅; the D6 kickoff survey captured at close (Blog/Posts — the next block) and the 396 prompts drafted.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the 394 block handoff still matches the code, state the package → redraw → hunt → extract plan — and resolve the open items: **(1)** the owner has created the public `crm-plugin--member-portal` repository (the extraction target — its absence blocks only the extraction phase, so if it is missing the session can open anyway and the owner creates it before phase C); **(2)** the sibling checkouts are current (`git pull` each of the five; no sibling edit is expected this session). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
