# Base Prompt — Session 370 (Security S1 — Perimeter Headers, CSP & Editor Self-Hosting)

**Start now.** Execute the reading list below, open the session prompt at `sessions/370. Security S1 — Perimeter Headers, CSP & Editor Self-Hosting.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **370. Security S1 — Perimeter Headers, CSP & Editor Self-Hosting**.

This is **launch-schedule position 1** in `sessions/release-plan.md` — **S1, the first session of the Security Hardening track** (`sessions/tracks/security-hardening.md`), which is Gate 3 of the launch plan ratified at session 369. S1 runs first in the whole schedule deliberately: every later Gate-2 rehearsal must exercise the *hardened* surface, so the perimeter lands before anything is rehearsed against it. The track doc § S1 is canonical for scope; the release-plan § S1 entry is the working-set pointer.

This session is **NOT boundary-touching** — no change to the Fleet Manager contract surface (stays **v2.6.0**). **Caution:** the work edits `docker/nginx/prod.conf` (and `default.conf` parity), which also carry the per-location mTLS gates for the five FM `/api/*` endpoints. Adding response headers does not change the contract's HTTP shape, but verify at close that the mTLS `if ($ssl_client_verify != "SUCCESS")` blocks and the `/api/*` response envelopes are byte-untouched. (The track's boundary-touching session is S2, not this one.)

**Session shape:** feature/hardening-style, 1 session (CSP staging may split per Rule 11). Ships the security-headers layer (CSP among them), self-hosts the admin editor assets, enforces prod-hardening defaults that today live in per-node `.env` convention, and folds in two small guards: the Stripe test-mode gate on the Random Data Generator and the demo-role seeding gate.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/tracks/security-hardening.md` — § Charter, § S1 (canonical scope), and Appendix A's top-5 risks #2 / #3 / #5 (the evidence this session acts on). Appendix A was verified against code at 368; re-verify only what you directly touch.
3. Read `sessions/release-plan.md` § Launch schedule + the § S1 working-set entry (prerequisites, success criterion, artifact).
4. Read `docs/app-reference.md` for environment names, container names, and key dependencies. No schema files needed — this session adds no migration.
5. Read the code you will modify before writing anything (process rule, but load-bearing here): `app/Providers/Filament/AdminPanelProvider.php` (the Quill CDN render hooks), `bootstrap/app.php` (middleware registration), `docker/nginx/prod.conf` + `docker/nginx/default.conf` (headers land here; mTLS blocks must not move), `config/session.php` posture, `database/seeders/PermissionSeeder.php` (the unconditional `demo` role block), the Random Data Generator widget (super-admin dashboard widget from session 245), and `.env.example`.
6. Open `sessions/370. Security S1 — Perimeter Headers, CSP & Editor Self-Hosting.md` and read the session prompt carefully.
7. Note any drift between the session prompt and the actual code in a brief work-log entry; proceed per the drift and decision-threshold rules.

---

## Starting state inherited from session 369

- **CRM contract at v2.6.0** — 369 was planning-only (launch replan: three gates, the Security Hardening track created, the release plan's remaining section rewritten and owner-ratified). No boundary contact.
- **Schema baseline:** no migration since 359 (the 2FA columns). This session adds none.
- **Fast Pest baseline: 3045 / 0** (parallel, verified at 368; 369 was a no-op pass). **New tests expected this session** — headers middleware, the generator test-mode guard, the seeder gate.
- **Playwright:** baseline preserved; not expected to run unless a CSP regression on a real page needs a browser check (the `design`-group Pest cluster plus a manual page load is the cheaper first signal).
- **VERSION** is `0.369.01` (369's close). This session's close bumps to `0.370.01`.
- **Planning state:** `release-plan.md` § Launch schedule is new and canonical (positions 1–16 + the floating Gate-1 session); `sessions/tracks/security-hardening.md` is new; `sessions/launch-plan-summary.md` is the owner's steering copy. Old positions 47–67 are superseded.
- **Housekeeping:** feature shape — no general inbox absorption. Two inbox/stub items are **in scope by design** (not absorbed extras): the Stripe test-mode detection guard (the 245-era release-blocker stub) and the demo-role seeding gate (`[0.367.02 · 2026-07-09]` inbox item, premise corrected at 368: the *role* is unconditional in `PermissionSeeder`; the *account* is already gated).

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md`). Session-specific:

- **Security over usability** (standing project rule): when the two directly conflict — e.g. a strict CSP breaks an admin convenience — choose security and move the friction to the UX layer. Surface the conflict rather than silently weakening the policy.
- **Front-end builds:** self-hosting the editor assets and any admin CSS/JS change follow the standard build rules (`npm run build` for Vite-compiled surfaces). If a widget-visible surface is touched, `build:public` per the decision rule.
- **nginx config edits on the dev machine:** the three single-file bind-mounts (`docker/nginx/*.conf`, `docker/php/local.ini`) break their snapshot path when edited — recover with `docker compose up -d --force-recreate <service>`, never `restart` (standing WSL2 fragility note).
- **Verify headers empirically**, not by config reading: curl the running dev stack for each header on a public page, an admin page, and an FM `/api/health` request (the last also proves the mTLS block untouched).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation and **wait for the owner to confirm before starting.** Bring: (1) the proposed CSP rollout shape (public vs admin policy split, report-only staging vs direct enforcement, and what the inline-style reality of the widget renderer forces); (2) the Stripe test-mode guard shape recommendation (hard refuse-to-render vs acknowledgment gate — the stub's default lean is refuse-to-render); (3) anything the code read contradicts in the track doc's § S1 scope. One confirmation, then proceed.
