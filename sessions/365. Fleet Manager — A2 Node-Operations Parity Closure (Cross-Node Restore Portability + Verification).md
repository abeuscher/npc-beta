# 365. Fleet Manager — A2 Node-Operations Parity Closure (Cross-Node Restore Portability + Verification)

Close release-plan entry A2: fix the one live defect blocking it (cross-node restores abort on the source node's role grants), verify the four node-operations capabilities against real nodes with the owner driving, hand off the operator-runbook artifact to the FM repo, and reconcile the stale cross-repo planning state.

---

## Plan reference

This session executes `sessions/release-plan.md` § A2 (execution-order position 46). That entry is canonical for the **success criterion** — from the FM admin UI an operator can (a) provision a new CRM node end-to-end, (b) trigger and verify a backup, (c) restore a node from a backup blob, (d) fetch and surface application logs without SSH — and for the **artifact** (an FM operator runbook covering all four). Its FM-side *status* notes are stale; the base prompt carries the corrected picture (FM at session 048; all four capabilities code-complete on both sides).

---

## Session-specific deltas

What the plan entry doesn't know:

- **A2 is a closure, not a build.** The "2 sessions likely (install + backup + restore; log-reading separately)" estimate described work FM has since shipped (FM 013, 018, 020, 021/022, plus 038/039/040/047 beyond scope). The remaining CRM-side work is one bounded fix; the rest is verification, the runbook handoff, and doc reconciliation.
- **The cross-node role-grant restore bug (FM 042, 2026-06-05, never absorbed CRM-side).** CRM backups are `pg_dump`s that carry `GRANT … TO <role>` (and ownership) statements naming the **source node's** read-only DB role (per-node role names since FM 037). Restoring such a blob on any other node aborts on the missing role. This blocks: the FM 042 demo-baseline live-restore verification, and any real cross-node restore under A2(c). Recorded fix options from the FM 042 log: dump-side `pg_dump --no-privileges --no-owner`, or restore-side strip/pre-create. **Lean: dump-side** — spatie/db-dumper supports per-connection `dump` options on the `pgsql` connection (`config/database.php`), it fixes every future blob at the source, and the restore path stays dumb. Restore-side rescue of *old* blobs is explicitly not pursued: old baselines get re-recorded (cheap, and the demo baseline needs re-recording anyway).
- **Consequence to verify while in there:** the restored DB must still work with the node's *own* read-only role afterward — check how/where the read-only role's grants are (re)applied on a node (that's FM-037 machinery, FM-side `.env`/provision path; the CRM fix must not assume grants survive a restore). If the answer is "FM re-grants on provision only," the handoff notes that a restored node may need a re-grant step — an FM-side runbook line, not CRM code.
- **Verification is owner-driven.** The four capabilities get exercised through the FM UI against nodes the owner designates. (b) has been "pending manual-test sign-off" since FM 020 — this session is that sign-off. (c) is the interesting one: after the fix deploys and a fresh backup exists, a cross-node restore (or the demo-baseline loop) should complete end-to-end for the first time.
- **The runbook artifact is FM-side.** Draft the handoff at `/home/al/fleetmanager/sessions/a2-runbook-handoff-from-crm.md`: the four capabilities, where each lives in the FM UI, the verification results from Phase 3, the old-blobs caveat, and pointers to the FM session logs that shipped each piece (013/018/020/021/022/038). An FM session finalizes it as the actual operator runbook; A2 closes CRM-side with the handoff + verification recorded, per the owner's ruling at the gate.
- **Planning reconciliation (Phase 1):** correct the A2 entry's status notes, the execution-order line 46 note, the Cross-Repo block's FM-side absorption statuses (v2.2.0/v2.3.0 shipped FM 020–022; v2.4.0 = FM 050 drafted; v2.5.0 pending), and `sessions/tracks/fleet-manager-agent.md`'s snapshot. Do not renarrate FM history — one-line status corrections with pointers to FM's own docs.

---

## Open questions to resolve at session start

1. Fix shape: dump-side (`--no-privileges --no-owner`, recommended) vs restore-side stripping — and whether `--no-owner` is wanted alongside `--no-privileges` (lean yes: ownership statements have the same cross-node failure mode).
2. Does the contract doc get a no-bump documentation revision noting privilege-free blobs (253/291/335 precedent)?
3. Phase 3 scope: which capabilities get live exercise this session, against which node(s), and is the demo-baseline re-record + restore loop (FM 042's blocked verification) included?
4. Where A2 "closes": does the CRM-side entry close on handoff + verification, with the runbook finalization tracked FM-side — or does it stay open until the FM runbook lands?

---

## Phases

### Phase 1 — Reconcile the planning state

Correct the stale FM-side status in `sessions/release-plan.md` § A2, the Cross-Repo block in `sessions/session-outlines.md`, and the `fleet-manager-agent` track-doc snapshot, against `/home/al/fleetmanager/sessions/completed-sessions.md` ground truth. Small, surgical edits with pointers.

### Phase 2 — Cross-node restore portability fix

Add the dump options to the `pgsql` connection so `spatie/laravel-backup` produces role-portable dumps. Verify objectively on a local `backup:run`: the dump inside the zip contains no `GRANT`/`REVOKE`/`ALTER … OWNER` statements referencing node roles. Confirm `demo:restore`'s psql path accepts the new dump shape unchanged. Tests: a Pest case asserting the configured dump options resolve onto the db-dumper (unit-level, no live pg needed), plus whatever the existing `Feature/Backup/` conventions suggest. Run the fast suite.

### Phase 3 — Live verification (owner driving)

Walk capabilities (a)–(d) per the gate's agreed scope. Owner drives the FM UI; agent watches CRM-side outcomes and records per-capability results (pass / issue found) in the work log. If (c)'s full loop needs the fix deployed first: bump VERSION, owner merges + deploys, fresh backup, then restore — sequence it inside the session if the owner wants, else record the post-deploy steps in the handoff.

### Phase 4 — Runbook handoff

Write `/home/al/fleetmanager/sessions/a2-runbook-handoff-from-crm.md` per the deltas above. Update the A2 entry with verification results and the agreed closure state.

---

## Out of scope

- **FM application code** — anything FM-side beyond the sessions-dir handoff doc.
- **Restore-side rescue of old blobs** — old baselines are re-recorded, not code-accommodated.
- **v2.4.0 / v2.5.0 FM-side absorptions** (FM 050 and successor — FM sessions, noted in reconciliation only).
- **A3 (multi-node readiness)** — next in Phase A once A2 closes; don't start it.
- **Contract surface changes** — stays v2.5.0.

---

## Testing

- **Slow test groups to run this session:** none beyond the fast suite, unless the dump-option work touches a file carrying slow backup tests (`Feature/Backup/` is fast).
- **New tests expected:** yes, small — the dump-option resolution test; possibly a `demo:restore` dump-shape acceptance case if cheap.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file:** `sessions/365. Fleet Manager — A2 Node-Operations Parity Closure (Cross-Node Restore Portability + Verification) — Log.md`
- **Branch:** `session-365/N` (final iteration)
- **Boundary:** NOT boundary-touching — contract stays **v2.5.0**; if the gate approves the documentation revision, it rides with no version bump and a CHANGELOG "revision" entry per precedent.
- **Track doc:** update `sessions/tracks/fleet-manager-agent.md` (snapshot + A2 status; if A2 closes, note what Phase A has left — A3 onward).
