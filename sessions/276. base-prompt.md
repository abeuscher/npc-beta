# Base Prompt — Session 276

We are about to begin a new session: **276. Notes Permissions (feature half)**.

This session is **C1** in `sessions/release-plan.md` — the carved-out feature half of the original Notes Permissions & Permissions Audit stub from session 244. The audit half feeds C3 later; 276 is the feature work only. C1 closes before C3 runs because C3 audits the surface 276 builds.

This session is **NOT boundary-touching** — no Fleet Manager contract change. CRM contract stays at v2.3.0 (per session 268). The 273 W8 #3 trait + parity test continues to gate code-vs-spec drift this session as it does post-275.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference; do not infer format from previous logs.
2. Read `sessions/session-outlines.md` § "Notes Permissions (feature half)" — the canonical scope stub.
3. Read `sessions/release-plan.md` § C1 — confirms positioning between item #27 (session 275, Rich-Text Surface Sanitization Hardening) and item #29 (E9, Widget Help Authoring), and locks the C1-precedes-C3 ordering.
4. Read `sessions/archived/275. Rich-Text Surface Sanitization Hardening — Log.md` end-to-end — sets the just-prior baseline (`Note.body` boundary now sanitised on save; observer pattern still in place; trait pattern for cross-cutting model behaviour established).
5. Read the current Notes surface to ground implementation:
   - `app/Models/Note.php` — fillable, casts, observer registration via `#[ObservedBy(NoteObserver::class)]`, `body` mutator from session 275.
   - `app/Filament/Resources/NoteResource.php` — `coreFormSchema()` is the canonical Note form (used by both `NoteResource` and the Contact / Organization Timeline relation managers); `TYPE_OPTIONS` and `STATUS_OPTIONS` enums.
   - `app/Policies/NotePolicy.php` — current `update_note` / `delete_note` / `view_note` gates. C1 extends this policy.
   - `app/Observers/NoteObserver.php` — landed at session 273/3 with CUD activity logging only. C1's permission gates layer above it.
   - `database/seeders/PermissionSeeder.php` — the `note` resource gets `view_any/view/create/update/delete` via the standard `$resources` array (line 26). C1 adds new permissions next to the standalone block (around line 75-95).
6. Read `docs/schema/notes.md` — the structured-interactions surface. Key fields C1 may gate: `type` (subtype, free-text-tolerant), `outcome`, `meta` (jsonb — participants and source-specific fields land here today, not as separate columns). Confirm against the live model: `direction` and `is_internal` are NOT in the schema today; the original session 244 stub anticipated them but they didn't ship. Decide at session start whether C1's scope adds them or stays within the existing surface.
7. Read `docs/app-reference.md` for environment names, container names, and view-to-file mappings. Read `docs/schema/README.md` for the table index — focus on `notes`, `permissions`, `model_has_permissions`, `roles`, `model_has_roles`, `site_settings`.
8. Open `sessions/276. Notes Permissions (feature half).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from 275

- **CRM contract at v2.3.0** (per session 268) — unchanged.
- **Schema baseline:** session-274 squash + the single 275 migration (`2026_05_10_120000_convert_collection_items_trix_to_quill.php`). 276 starts from this baseline; any new migration absorbs into the next squash (T1 terminal session).
- **Fast test suite green** at the **275 close baseline** (~2277 tests; confirm at session start, count delta logged at 275 close).
- **Slow test suite green** at **16 passed** (carried).
- **Playwright suite** at **44 / 0** post-275.
- **All rich-text save boundaries sanitised** (per 275: Note.body, Event.description/meeting_details, EmailTemplate.body, SiteSetting rich-text keys, PageWidget.config, Collection.data rich_text fields, all 8 custom_fields-bearing models). C1's permission gates compose with these — sanitisation already runs before any save touches the DB.
- **Memos editor is Quill** (per 275/3); no remaining Trix surface in the app.
- **`SanitisesRichTextCustomFields` trait** at `app/Models/Concerns/SanitisesRichTextCustomFields.php` — uses `Str::snake(class_basename($model::class))` for `model_type` lookup matching the `'contact'` / `'event'` / `'page'` codebase convention.
- **FM contract version-stamp parity** gated by `tests/Feature/Infrastructure/FmContractVersionParityTest.php` (273/4); the test continues to fire at every commit.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **Pre-beta license applies.** No backward-compat shims for permission renames or policy changes.
- **No agent contract change.** No `docs/fleet-manager-agent-contract.md` body edit, no version bump.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. If the `edit-only-by-creator` setting needs a new column on `site_settings` (it almost certainly doesn't — `SiteSetting::set('notes_edit_only_by_creator', '1')` is the existing pattern), no migration; otherwise add one.
- **Update `docs/schema/`** after writing any new migration. C1 likely adds permission rows + a SiteSetting key (no migration); update `docs/schema/notes.md` only if a column is added.
- **Tests for new behaviour.** Policy gate changes need feature tests asserting both the allowed and denied paths for each role. SiteSetting toggle needs both-states coverage. The manager-override permission needs a test asserting it bypasses the edit-only-by-creator gate cleanly.
- **When implementation is complete on each iteration**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: **275 close baseline + the new permission/policy tests** (count delta logged in the close).
- **Front-end builds.** Likely no Vite work — this session is mostly model/policy/seeder. If a NoteResource form change touches `resources/js/` or `resources/scss/`, run `npm run build`.
- **Playwright run** required on the iteration that ships the policy gates if the gates affect the admin UI's edit/delete affordances. Likely yes — Filament's row-action visibility is policy-gated by `EditAction::make()` etc. Verify operator-A-cannot-edit-operator-B's-note in the UI.
- **Pest + Playwright must NOT run in parallel** per the recovery-ladder discipline established at 273/5. Run sequentially.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

Standard close gate per `sessions/template-base-prompt.md`. Session-specific notes:

- **Session log:** write `sessions/276. Notes Permissions (feature half) — Log.md`. Document per-iteration what was built, the full permission matrix added (which permissions, which roles get them by default, which roles override the edit-only-by-creator gate), the SiteSetting key added (if any) with its default and admin-form location, and the test count delta vs the 275 baseline.
- **Update `sessions/completed-sessions.md`:** append session 276's row.
- **Update `sessions/session-outlines.md`** § "Notes Permissions (feature half)" — mark the stub as ✅ closed (session 276); preserve the forward reference into C3 (audit half).
- **Update `sessions/release-plan.md`:** mark the C1 execution-order entry as ✅ closed.
- **No `docs/fleet-manager-agent-contract.md` updates** — non-boundary-touching.
- **Help-doc updates** — if any new admin-facing setting lands (e.g. the SiteSetting toggle on `GeneralSettingsPage`), update the matching help doc under `resources/docs/`.
- **Archive the previous session:** move all files matching `sessions/275. *.md` into `sessions/archived/`.
- **Commit:** stage all changed files (close-gate doc bundle + any iteration commits not already merged) on the final `session-276/N` branch. Notify the user. Do not push.
- **No FM-side handoff** — non-boundary-touching.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

The style rules in `sessions/template-base-prompt.md` apply. Session-specific:

- **Iteration commit messages describe the gate or seam delivered** — e.g. "Iteration /1 — `update_others_note` permission + extended NotePolicy::update gate" or "Iteration /3 — Manager override permission + edit-only-by-creator SiteSetting toggle".
- **Each policy / permission addition lands with its companion test** — gate addition without a regression-guard is incomplete; the test surface is the security gate.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Simple, correct, well-considered solutions over fast or clever ones.**
- **Security posture is the defining constraint.** When the spec stub and a usability concern conflict, choose security per the standing `feedback_security_over_usability.md` memory rule. Move friction to the UX layer (e.g. greying out the Edit button with a tooltip "Only the author can edit this note"), not to the protection layer (the policy still denies, regardless of UX).
