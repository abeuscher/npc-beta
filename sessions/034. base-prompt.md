We are about to begin a new session: **Transactional Email — Resend Integration**.

Please open `sessions/034. Transactional Email — Resend Integration.md` and read the session prompt. Summarize what you understand the session to involve and ask any clarifying questions before we proceed. Once we are aligned, execute the work.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run `php artisan test` and fix any failures before proceeding.
- **Pause for manual testing.** Do not proceed past this point without explicit instruction.
- **Ask what the next session topic should be.** The user will name a topic. Reference `sessions/session-outlines.md` for context — it is a fluid working document, not a strict queue. Sessions are picked from it in whatever order makes sense at the time, and the user may choose something not yet on the list.
- **Discuss the next session prompt.** Talk through scope, decisions, and anything unclear before writing. Do not write the prompt until the user confirms the discussion is complete.
- **Write the next session prompt** as a standalone file at `sessions/[NNN]. [Title].md`, following the structure and conventions of existing session prompts. Pause for the user to review it.
- **Write the next session base prompt** as `sessions/[NNN]. base-prompt.md` by copying `sessions/base-prompt.md` and filling in the session number, title, and log filename. This is what the user will paste to start the next session.
- **Ask explicitly whether to close the session** before writing the log or committing.
- **Session log**: write a log file at `sessions/034. Transactional Email — Resend Integration — Log.md` summarising what was built, what changed, and any deferred decisions.
- **Update session-outlines.md**: move this session's title into the Completed Sessions table. If the next session prompt was written, add or update its stub in the appropriate group.
- **Commit**: stage all changed files (including the log, the next session prompt, and the updated outlines), commit to a feature branch named `session-034`, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
