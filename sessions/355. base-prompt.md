# Base Prompt — Session 355 (Editor↔Public Parity — Homogenize Widget Hydration)

**Start now.** Execute the reading list below, open the session prompt at `sessions/355. Editor↔Public Parity — Homogenize Widget Hydration.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **355. Editor↔Public Parity — Homogenize Widget Hydration.**

This is **not a `release-plan.md` entry** — it is the **first of the two tracks** of the standing **Editor↔Public parity** architectural stub (Post-Beta backlog in `sessions/session-outlines.md`, surfaced session 333). The stub's root cause: the page-builder canvas renders public widget HTML/CSS/JS inside a Filament admin document, so two cascades and two JS lifecycles share one `window`, and the editor re-implements widget **hydration** as a bespoke second copy of the public path — the copies drift. Session 333 fixed symptoms + stood up a parity harness; **session 354** closed the **chrome-render** instance (header/footer wrapper parity) **and** substantially executed the stub's *second* track — **"layer the admin reset"** — via the `layer(filament)` one-liner in `resources/css/filament/admin/theme.css` (Filament's whole admin CSS, including Tailwind preflight, now in `@layer filament` below `@layer widgets`, so layered widget rules win). This session does the stub's **first** track: consolidate the **widget-owned** JS hydration into one shared routine.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract **stays at v2.4.0** (inherited from 353's count-only `data_hygiene` `/api/health` subcheck; 354 was non-boundary). No `docs/fleet-manager-agent-contract.md` or Cross-Repo-block changes.

**Session shape:** architectural refactor, **Playwright-guarded** (the 333 parity harness + the 354 chrome-parity guard are the regression net — this is collision-*detection*, the only proof the refactor preserved parity). One-sentence summary: extract the widget-owned hydration (libs loaded + correctly-layered CSS + Alpine/Swiper/Chart init) into one shared `hydrate(rootEl)` that both the public site (once over `document`) and the editor (over each freshly-injected canvas subtree) call, retiring the bespoke editor copy.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is a **stub-driven/architectural** session, so use the fuller session-prompt shape (explicit phases, open questions, out-of-scope, drift signals).
2. Read `sessions/session-outlines.md` — the active-tracks block and the **Editor↔Public parity — homogenize widget hydration + layer the admin reset** stub (the two-track framing, the widget-owned vs surface-owned split, and the **iframe-ruled-out** note are all load-bearing context; don't re-derive them).
3. **Read the session prompt's scoping section carefully** (`sessions/355. Editor↔Public Parity — Homogenize Widget Hydration.md`) — it carries the two hydration copies side by side and the three open questions. Build from it; verify against current code.
4. **Read the two hydration copies + the third scattered piece:**
   - **Public auto-init (source path A):** `resources/js/public.js` — static lib imports, `window.*` global assignment, `alpine:init` registrations (customSelect + theme store), `Alpine.start()` once over `document`. Libs **bundled**, init whole-document + one-shot.
   - **Editor re-implementation (source path B):** `resources/js/page-builder-vue/composables/useLibraryLoader.ts` — `loadLibs(libs)` (lazy per-lib CSS fetched + wrapped in `@layer reset`, per-lib JS from `window.__widgetLibs`) and `reinitAlpine(scopeEl)` (destroy + re-init Swiper/Chart/Alpine over a subtree, with `x-ignore` lift/restore + post-init `swiper.update()`). Libs **lazy/manifest-driven**, init per-subtree + repeatable. Note how `PreviewCanvas.vue` calls `reinitAlpine` (the `requestAnimationFrame` double-rAF after `v-html` injection).
   - **The admin lib emission (the third piece):** `app/Providers/Filament/AdminPanelProvider.php` — the `panels::head.end` render hooks emitting `widgetEditorCss()`, `widgetJs()`, the per-lib bundles, and `window.__widgetLibs` (all libs on every admin page today).
5. **Read the asset-resolution seam + the single-sourced model/view:** `app/Services/WidgetAssetResolver.php` (`widgetEditorCss` / `widgetJs` / `allLibs` / `libsForWidgets`) and `app/Services/WidgetRenderer.php` (the one HTML producer both surfaces already share — the proof that model+view are single-sourced and only hydration diverges).
6. **Read the regression net:** `tests/e2e/page-builder/full-width-matrix.spec.ts` (the 333 editor↔public parity harness) and `tests/e2e/page-builder/chrome-parity.spec.ts` (added at 354). Read `docs/app-reference.md` for the **front-end build command** (use the WSL node PATH per the build-command note), the **isolated Playwright e2e stack** (`./dev e2e:up` / `e2e:down`), and the note that the e2e stack serves an **image-baked `public/build`** named volume — a local `npm run build` must be `docker cp`'d into the e2e app container to be exercised (learned at 354).

---

## Starting state inherited from session 354

- **CRM contract at v2.4.0** — unchanged (354 non-boundary; the chrome fix + the `layer(filament)` fix touched no Fleet surface). **This session is non-boundary; the contract is unchanged.**
- **Schema baseline:** no migrations; `database/migrations/` empty (post-348-squash). None expected — this is client-side hydration.
- **Fast Pest green at 2930 / 0** (354 close). Expected delta: **none** — hydration is Vue/TS/JS (Pest doesn't see it). New Pest only if a fold changes server-side behaviour (e.g. `AdminPanelProvider` lib emission gains logic).
- **Playwright** — `chrome-parity.spec.ts` added at 354 (standing guard: chrome wrapper structure + link-colour parity + no page errors). `full-width-matrix.spec.ts` is the 333 parity harness. **This session uses both as the regression net + dev loop.** Run Pest and Playwright **sequentially**.
- **Planning state:** 354 closed the chrome-render instance of the parity stub **and** executed the stub's Track-2 "layer the admin reset" via the `layer(filament)` one-liner (`resources/css/filament/admin/theme.css`) — the unlayered-preflight leak class is handled (interim; the clean Tailwind-v4 path remains open debt). So 355 inherits a **half-done stub**: Track 2 substantially landed (interim), Track 1 (hydration) is this session. No release-plan change.
- **Housekeeping inbox** (`sessions/housekeeping-inbox.md`) — **not a housekeeping session;** the inbox is **not** drained here (it is overdue for a dedicated batch — ~25 live items — but that's a separate pick). Capture mid-session finds via `npm run logbug`; digest the buffer at close per the standard gate.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Single-source the widget-owned half only.** The widget-owned hydration (HTML/CSS/libs/init) **must** be unified; the surface-owned editor code (selection, drag/drop, inline editing, host chrome) is legitimately per-surface — **do not touch it.** If a widget's behaviour straddles the line, surface the specific case rather than guessing the boundary.
- **The public path is load-bearing — refactor toward it carefully.** Single-sourcing must not change how the live public site hydrates widgets. If unifying forces a behavioural change to `public.js`'s path, stop and surface it before committing.
- **Verify parity objectively with Playwright; pull the owner in for the visual call.** The 333 harness + the 354 chrome-parity guard are the green net — confirm no init-race / swiper-default-pager / uneven-grid / Alpine-double-init regression on **either** surface. Close the objective loop yourself (no console errors, libs present, designed pager renders); surface any visual judgement for the owner.
- **Build after front-end changes.** Any `resources/js/**` change → `npm run build` before announcing completion (Pest won't catch a Vue/TS/JS break). For the e2e stack, `docker cp` the fresh `public/build` into `nonprofitcrm_e2e_app` (the e2e named volume is image-baked — a host build alone isn't seen by the stack; learned at 354).
- **Surface the module-seam choice at the gate.** Where the shared `hydrate()` lives and how both build graphs import it without duplicating Alpine/Swiper is the one architectural decision to settle before going deep — bring it to the Open Gate.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the **scoped cause** (two divergent copies of widget-owned hydration — `public.js` bundled-libs/whole-document one-shot vs `useLibraryLoader` lazy-libs/per-subtree re-init — plus the `AdminPanelProvider` emission) and the **`hydrate(rootEl)` plan** (extract the widget-owned half → both surfaces call it → verify against the parity net); (2) your **answer to the three open questions** — the shared-module home + import seam (Q1), the bundled-vs-lazy lib-presence contract (Q2), and the first-init-vs-re-init split (Q3) — with a recommendation on each; (3) the **iteration shape** — whether the extract + both-surface wiring + verification land together or split (Rule 11) if the seam proves load-bearing. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/355. Editor↔Public Parity — Homogenize Widget Hydration — Log.md` — the `hydrate()` contract that landed, the widget-owned/surface-owned boundary, how the three pieces consolidated, the Q2/Q3 decisions, the parity-harness verification (and which specs were kept as standing guards vs torn down), the build run, and Pest results.
- **Bump `VERSION`** to `0.355.<iteration>`.
- **Non-boundary:** no contract-doc or Cross-Repo-block change (CRM stays v2.4.0). Confirm `FmContractVersionParityTest` still green if any Fleet file is incidentally touched (it shouldn't be).
- **Update the stub** in `sessions/session-outlines.md`: mark **Track 1 (homogenize widget hydration) resolved**; leave **Track 2 (layer the admin reset)** open with the note that 354's `layer(filament)` is the interim fix and the clean Tailwind-v4 path remains gated on Filament v4.
- **Housekeeping inbox:** not drained this session; digest the capture buffer (`housekeeping-incoming.md`) per the standard gate.
- **Archive** `sessions/354. *.md` → `sessions/archived/`.
- **Commit on `session-355/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: **extract toward the public path, don't invent a third mechanism** — the goal is one routine that subsumes both copies, not a new abstraction layered over them. Keep the surface-owned editor code untouched. Don't expand into Track 2 (the Tailwind-v4 upgrade) or the surface-owned interaction layer.
