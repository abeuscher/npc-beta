# Base Prompt — Session 399 (QuickBooks Carved, Packaged & Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/399. QuickBooks Carved, Packaged & Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **399. QuickBooks Carved, Packaged & Extracted**.

This is **arc position D9a** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical). D8 closed at 398 (`crm-plugin--products` v0.1.0; contract 0.22.0; `plugins/` empty a seventh time; the third single-session carve → package → extract; the § 6.8 own-vertical disposition shipped). **D9 resolves at this prompt-draft as a two-session split (decision 1 — ratify at the gate):** this session takes QuickBooks alone — carve (`plugins/QuickBooks/`: `QuickBooksAuth` + `QuickBooksClient` + the OAuth callback controller + the three in-panel routes via the admin socket's **`routesFile`** axis — its first shipping consumer, the `SyncTransactionToQuickBooks` job, the Finance-page QuickBooks settings sections to a plugin-owned settings page, **the `quickbooks` capability** — surface 13's third, **the `TransactionRecorded` in-repo inversion** — core `Transaction::recordStripe`'s hard-wired job dispatch becomes a core event the plugin listens to) → package (`nonprofitcrm/quickbooks`, P6 mechanics, **`quickbooks/v3-php-sdk` package-owned** — the 387 dependency posture; **no schema, no redraw** — the qb columns stay on core tables, the Blog empty-category wording) → the **tenth extraction-runbook run** to `crm-plugin--quickbooks`. Mailchimp + Resend + the email-transport seam are **D9b (session 400)**, surveyed at this session's close.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.22.0 → 0.23.0 at close (Package shape + External repo tenth consumer; surface 13 third capability — the integration flavor; surface 3 `routesFile` shipping-consumer promotion; surface 10 the `TransactionRecorded` core event; changelog).

**Session shape:** carve + capability + in-repo inversion + package + extraction, behavior-preserving on the full composition. Routes 272/272 byte-identical (the three QB routes keep names/URIs/middleware through the socket pull; moved-action FQCNs the 381 exception); the settings sections move page but behave identically; no schema events, no visual public-site change, gate 4 n/a (no widgets, no assets). **Expect one iteration; the honest-stop fallback (after carve, or after carve + package) is a planned outcome, not a failure.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D8 ✅, D9 open) — note the D9 stub's ~1–2 estimate that decision 1 resolves as the D9a/D9b split.
3. Read the 398 session log (`sessions/398. Products Carved, Packaged & Extracted — Log.md`) — especially its **D9 kickoff input** section.
4. Read `docs/plugin-contract.md` at 0.22.0 — § Package shape (the third-party-dependency posture — this session's SDK move follows it), § External repo, § The distribution manifest, surface 3 in full (the `routesFile` axis this session promotes — core wraps every pull in `Authenticate`; the SocketProbe fixture is the only current consumer), surface 13 in full (the capability mechanics + the payments credential-dimension precedent this capability's resolver mirrors), surface 10 (events/hooks — the new core event slots here), surface 1 (the provider-injects-its-own-config posture, the Payments `services.stripe` precedent).
5. Read `docs/plugin-extraction-runbook.md` — the tenth run; the 387 Payments worked example is the closest precedent (foundation-flavored, no assets, dependency-carrying, no schema).
6. Read the 387 log (`sessions/archived/387. Payments Packaged & Extracted — Log.md`) for the dependency-carrying package walk, and the 380 log (`sessions/archived/380. Payments Foundation Module — Log.md`) for the capability mechanics this session's `quickbooks` capability mirrors.
7. Open `sessions/399. QuickBooks Carved, Packaged & Extracted.md` — the survey and front-loaded decisions; note the Open-Gate items (the D9a/D9b split as the headline ratification; the owner-created `crm-plugin--quickbooks` repo; sibling freshness with **no sibling edits expected** — the inversion is in-repo).
8. Skim the surfaces: `app/Services/QuickBooks/{QuickBooksAuth,QuickBooksClient}.php`; `app/Http/Controllers/QuickBooksCallbackController.php`; `routes/admin/quickbooks.php` + the `AdminPanelProvider` pull; `app/Jobs/SyncTransactionToQuickBooks.php` + `app/Models/Transaction.php` ~85 (`recordStripe` — the dispatch to invert); `app/Filament/Pages/Settings/FinanceSettingsPage.php` ~387–500 (the QB sections); `app/Filament/Resources/TransactionResource.php` (the `isConnected()` read, `qb_status` column, QB action, `quickbooks_id` field); `app/Filament/Resources/ContactResource.php` ~240 (column-only read); `app/Filament/Widgets/DashboardIntegrationStatusWidget.php`; `app/Services/Setup/SetupChecklist.php`; `app/Providers/AppServiceProvider.php` (the `services.quickbooks.key` injection); `config/services.php`; the four QB test files; `app/Services/ImportExport/SiteSettingsBundlePolicy.php` + `app/Services/Import/FakeCsvComposer.php` (settings-key lists).
9. Note any drift between the session prompt's captured survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 398

- **CRM contract at v2.7.0** — 398 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D8 complete. **Nine extracted plugins** (`crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` **v0.4.0**, `crm-plugin--events` v0.1.3, `crm-plugin--donations` v0.1.0, `crm-plugin--memberships` v0.1.0, `crm-plugin--member-portal` v0.1.1, `crm-plugin--blog` v0.1.0, `crm-plugin--forms` v0.1.0, `crm-plugin--products` v0.1.0); **`plugins/` is empty** until this session's carve refills it. Plugin contract **v0.22.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** seventeen plugin-owned tables in their packages' migrations; the core dump is core-only (guard-enforced, owner map at 17). Two live core migrations (the 396 posts drop, the 398 allocations drop). **This session adds no schema events** — no redraw, no drop, no mark-as-applied.
- **Fast Pest suite green at 3480 / 0 parallel** (398 close: 3441 baseline + 39 additive). The `Products` testsuite (10 files, 54 tests) runs from `vendor/nonprofitcrm/products/tests`. Expected delta this session: **additive** — the QuickBooks twin/mirror/flag/boundary set + the inversion coverage; the four existing QB test files unchanged beyond path/namespace re-points.
- **Route baseline: 272 routes** (dev/test), byte-identical to main at 398 — unchanged this session (the three QB in-panel routes move to the socket's `routesFile` pull but keep names/URIs/middleware).
- **Composition-safety state:** the member-portal capability gates the portal reaches (394/395), forms reads gate on `Route::has('forms.submit')` (397), products reads on `Route::has('products.checkout')` (398) — all do-not-touch. The payments capability is the resolver-shape precedent for `quickbooks`.
- **The twin fixtures this session carry NO migrator paths, permanently** (the plugin owns no schema — the Blog wording, not the expiring caveat).
- **Playwright suite** — CI signal, non-gating; the parked chrome-parity spec remains the known failure. No local Playwright.
- **Housekeeping inbox** — no absorptions expected this shape. Standing items: `admin_invitation`, CSP-font, chrome-parity.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **The carve is the 389→398 playbook** with three new-design overlays, each landing behind an existing surface: the **capability** (decision 3 — declared at `register()`, resolver mirrors `isConnected()`; the five surveyed core reaches gate on it; never a core reference to the plugin namespace), the **inversion** (decision 4 — `Transaction::recordStripe` dispatches the new core event; the plugin listener dispatches the traveling job; QuickBooks-absent = no listener, no job, no error), and the **`routesFile` promotion** (decision 6 — the three routes byte-identical through the socket pull). The settings sections move whole (decision 5) with their activity-log lines intact.
- **The SDK goes package-owned at the package step** (the 387 posture): the plugin's `composer.json` requires `quickbooks/v3-php-sdk`, root drops its direct line same-commit, lock resolution identical (now transitive).
- **No redraw, no drop** — record the empty schema category explicitly (the 387/393/396 wording discipline); the twin fixtures' docblocks say permanently-no-migrator-path.
- **The fallback is planned, not shameful** (decision 2): if the capability or inversion runs long, stop after carve or after carve + package, log the stop point, and D9a closes next session.
- **Factory-email discipline:** any new arrange feeding a NOT NULL column pins its factory values (the 395 flake lesson).
- **The manifest is edited, never the generated file.** Scope is binding: no Mailchimp/Resend work (D9b), no transport seam, no schema moves, no QB feature work, no per-plugin CI (D11), no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.22.0 → 0.23.0 (tenth Package-shape + External-repo consumer; surface 13 third capability; surface 3 `routesFile` shipping wording; surface 10 the `TransactionRecorded` event; changelog); plan doc § Stage D D9 line annotated (D9a closed, D9b open); the D9b kickoff survey (Mailchimp + Resend + the transport seam) captured at close and the 400 prompts drafted.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the survey still matches the code, state the carve + capability + inversion → package → extract plan — and resolve the open items: **(1)** the owner ratifies the five front-loaded calls (**the D9a/D9b split — the headline**; the one-session shape with the loud fallback; the `quickbooks` capability; the `TransactionRecorded` inversion; the settings-page move); **(2)** the owner has created the public `crm-plugin--quickbooks` repository (blocks only the extraction phase); **(3)** the sibling checkouts are current (`git pull` each of the nine — **no sibling edits expected**: the inversion is in-repo). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish or its honest stop point.
