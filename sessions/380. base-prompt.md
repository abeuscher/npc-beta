# Base Prompt — Session 380 (Payments Foundation Module)

**Start now.** Execute the reading list below, open the session prompt at `sessions/380. Payments Foundation Module.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **380. Payments Foundation Module**.

This is **arc position P4** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary). P4 carves the Stripe rails — the checkout-session service, the webhook controller, the Stripe price observer, the live-mode sniffer — into `plugins/Payments/`, the first **foundation plugin**, and lands the **capability-detection API** (`docs/plugin-contract.md` surface 13) as core mechanics: verticals ask core "is payments enabled?" instead of reading Stripe config directly, and resolve a core-owned checkout contract instead of a concrete Stripe class. This is the first *shipping* plugin extraction beyond the widget pilot, so surface 13 is expected to go PROVEN; the contract doc is the arbiter. P4's close also produces the owner's decision input for P5 (Donations vs Events as the first domain vertical).

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**. (The client-billing surface — `app/Services/Billing/*`, `EnforceSuspensionState`, `AccountPage` — is explicitly out of scope and guard-protected; see the session prompt's boundary section.)

**Session shape:** feature-style / behavior-preserving extraction plus new core capability. The public payment behavior (all four checkout flows, the webhook, the $0/free paths) must be identical before and after; the capability API is new core mechanics consumed by real vertical call sites. **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` §§ 2–4, § 6 (disposition 2 — payments-as-foundation, dependency rules) and § 7, and `docs/plugin-contract.md` v0.3.0 (surfaces 13, 1, and 10; surface 2's entry as the worked "make a surface real" precedent; the normative rules).
3. Open `sessions/380. Payments Foundation Module.md` — the front-loaded decisions there are settled; do not reopen them.
4. Read the code being carved and its seams: `app/Services/StripeCheckoutService.php`, `app/Http/Controllers/StripeWebhookController.php`, `app/Observers/ProductPriceObserver.php`, `app/Support/StripeMode.php`, the five vertical call sites (`app/Http/Controllers/DonationCheckoutController.php`, `ProductCheckoutController.php`, `MembershipCheckoutController.php`, `EventController.php`, `Portal/EventRegistrationController.php`), `app/Services/EventRegistrationQuantities.php`, `app/Providers/AppServiceProvider.php` (the SiteSetting→config injection at boot), `app/Providers/PluginServiceProvider.php` + `app/Plugins/*` (the 379 registry pattern to mirror), `config/plugins.php`, `routes/web.php` (the webhook + checkout routes), `bootstrap/app.php` (the `/webhooks/*` CSRF exemption), and `tests/Feature/Infrastructure/ConventionDriftTest.php` (the two-Stripes boundary guards, including the positive control that names the file being moved).
5. Read the behavioral net you must keep green: `tests/Feature/DonationCheckoutTest.php`, `PaidCheckoutTest.php`, `StripeCheckoutBrandingTest.php`, `StripePaymentMethodsTest.php`, `MultiQuantityRegistrationTest.php`, `RegistrationConfirmationTest.php`, `DonationAcknowledgmentTest.php`, `ProductPurchaseTest.php`, `StripeGeneratorGuardTest.php`, `FinancialSourceWritePathTest.php`.
6. Skim `sessions/session-outlines.md` (Plugin Architecture track entry), `docs/app-reference.md`, `app/Http/Middleware/EnforceSuspensionState.php` (the suspension asymmetry that must not change), and `app/Filament/Pages/Settings/FinanceSettingsPage.php` (stays core; know its shape).
7. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 379

- **CRM contract at v2.7.0** — 379 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** arc P3 complete (379) — `AdminPanelProvider`'s inline `->routes()` closure decomposed into eleven per-feature files under `routes/admin/` with exact route-list parity (274 routes, byte-identical JSON); plugin admin socket live (`App\Plugins\AdminContribution` + `App\Plugins\PluginAdminRegistry`, populated at plugin `register()`, consumed by the panel provider); plugin permissions swept idempotently by `PermissionSeeder`, granted to no shipped role; plugin in-panel route pulls always wrapped in `Authenticate` by core; socket proven by the `SocketProbe` fixture (`tests/Fixtures/Plugins/SocketProbe/`, enabled + removal test mirrors); standing guard `PluginAdminCssGuardTest` bans plugin admin CSS. `docs/plugin-contract.md` at **v0.3.0** (surfaces 1, 2, 11, 12 PROVEN; 3 and 4 DECLARED with mechanics built and fixture-tested; 5–10, 13 DECLARED).
- **Schema baseline:** no schema changes at 375–379. None expected this session either — `stripe_*` columns stay on their core tables (surface 5 lands at P5/P6).
- **Fast Pest suite green at 3156 / 0** (379 close; +12 over the 378 baseline: 6 socket, 3 removal-mirror, 3 CSS-guard). Expected delta this session: moderate — capability-API tests, remove-the-line vanish tests for the payments plugin, the payments boundary guard, the ProductPriceObserver backfill; existing payment tests re-pointed mechanically where they mock the moved class.
- **Playwright suite** — baseline preserved; does not run this session, CI is the signal. (No e2e coverage exists for any checkout flow — a known gap, not this session's to fill.)
- **Planning state:** no planning-doc changes at 379 beyond the arc entry marking P3 complete (done at 379 close).
- **Housekeeping inbox** — the 379 capture buffer was empty at close; nothing folds in. **This session absorbs one inbox sub-item** (Rule 2): from the `[test-integrity]` `[0.363.03]` coverage-gap list, item (1) — ProductPriceObserver's Stripe archive-and-recreate flow has no real coverage (the deleted tests passed with the observer unregistered). P4 moves that file; the one-test backfill (Stripe boundary mock) lands with the move. Items (2)–(4) of that entry stay in the inbox.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavioral parity is the bar for the carve.** Capture `php artisan route:list --json` before touching anything; after the move the diff must be empty — the webhook route (`POST /webhooks/stripe`, name `webhooks.stripe`) and every checkout route keep identical names, URIs, methods, and middleware. The existing payment test files (reading-list item 5) are the behavioral net; their assertions do not change — only mechanical re-points where they mock the moved class by name.
- **The two-Stripes boundary is guard-enforced in both directions.** `ConventionDriftTest` already bans Stripe reaches from the client-billing files and carries a positive control naming `app/Services/StripeCheckoutService.php`; that control must be updated to the new plugin path **in the same commit** as the move, or the suite fails. Nothing from `app/Services/Billing/`, `EnforceSuspensionState`, or `AccountPage` moves or changes meaning.
- **The free paths are the degradation proof.** The $0/comp event path, the no-tier event path, and the comp-membership path never touch Stripe today and must be byte-identical in behavior after the carve — they are what "Events without Payments" degrades to, and their existing tests must pass untouched.
- **The front-loaded decisions in the session prompt are settled.** Implementation details within them are yours to decide and log.
- **Scope is binding.** The inbound inversion (webhook → typed events, surface 10) is P5's, per-vertical. `FinanceSettingsPage` stays core. No schema changes. Receipt layer stays core. Capability API serves exactly one capability this session (`payments`). Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** surface 13 gets its mechanics and (if genuinely exercised by shipping call sites, per the honesty rule) promotion to PROVEN; surface 1's entry notes the second shipping consumer; surface 10 gains the P5 inversion note. Bump the doc's version, changelog entry.
- **P5 input at close:** record in the session log a short Donations-vs-Events recommendation for the first vertical extraction, informed by what the carve revealed (coupling found, webhook fan-out shape, test posture). The owner decides at close per the plan doc.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code (the five call sites, the config-injection seam, the guard line numbers), state the carve order and any drift found — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
