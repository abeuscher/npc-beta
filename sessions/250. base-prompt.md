# Base Prompt — Session 250

We are about to begin a new session: **250. Rich Text Custom Fields**.

This session lands E3 from `sessions/release-plan.md` — a new `rich_text` custom-field type alongside the existing `text` / `number` / `date` / `boolean` / `select` set. Operators get a Quill-backed admin editor for capturing formatted content (bios, descriptions, multi-paragraph notes) on Contact / Event / Page records. The importer treats rich-text cells as plain strings (no HTML parsing) — same shape as the existing text fields. Closes one of two B2 prerequisites (the other is E2 Importer Mapping Page UX, queued for 251).

CRM-side parallel work to FM 012/013+ (which is absorbing v2.0.0 mTLS and resuming A2 affordance work in the FM repo). Not boundary-touching — the agent contract is unchanged this session.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md` — the 11 discipline rules + § E3.
3. Read `sessions/session-outlines.md`:
   - Active tracks block.
   - Cross-Repo block (Fleet Manager contract is at v2.0.0 as of session 248; this session is not boundary-touching).
   - Rich Text Custom Fields stub.
4. Read `docs/app-reference.md` — environments, container names, view-to-file mappings, key dependencies. Read `docs/schema/README.md` for the table index, then read `docs/schema/custom_field_defs.md`. (No schema migration expected — `field_type` is already a string column; `rich_text` is a new value, not a column.)
5. **For the existing custom-field surface** — the `CustomFieldDef` model + admin resource + form-component bridge:
   - `app/Models/CustomFieldDef.php` — `toFilamentFormComponent()` is the one-liner-per-type bridge to add `rich_text` to.
   - `app/Filament/Resources/CustomFieldDefResource.php` — the admin form's `field_type` Select (line ~70) and the table's `field_type` BadgeColumn (line ~137) both list the type-key → label mapping. Update both.
   - `app/Filament/Resources/ContactResource.php` line ~98 — `toFilamentFormComponent()` is invoked here; a representative rendering surface to verify.
6. **For the Quill editor primitive** — `app/Forms/Components/QuillEditor.php` is the existing Filament form component used elsewhere in the admin (CmsSettingsPage system-page-content fields, GeneralSettingsPage dashboard-welcome, MailSettingsPage modal forms). View partial at `resources/views/forms/components/quill-editor.blade.php`. The Quill v2 lib loads on every admin page via `AdminPanelProvider`'s `panels::head.end` hook — no per-form bootstrap needed.
7. **For the rendering surface** — find where custom-field values are displayed on detail views (Contact / Event / Page) and decide whether `rich_text` values render with `{!! !!}` (raw) or via a sanitizer. Quill v2 strips scripts by default, but accepting third-party HTML through the importer means trusting upstream sanitization isn't safe. Surface architectural choice at session start: **do we sanitize on save, sanitize on render, or trust Quill output?** The Design decisions section of `sessions/250. Rich Text Custom Fields.md` calls this out for resolution at Phase 1.
8. **For the importer pass-through** — `app/Importers/ContactFieldRegistry.php` declares `custom_fields` as a flat passthrough. Verify that rich-text-shaped CSV cells (HTML from a competitor export) round-trip without escaping or stripping. The session prompt's importer-shape says "treat as plain string, no HTML parsing." If verification surfaces extra coercion, surface and decide.
9. **For the Random Data Generator** — `app/Services/RandomDataGenerator.php::fakeCustomFields()` already handles `rich_text` (added at session 245 in anticipation; see line ~376–384). No generator change expected.
10. Open `sessions/250. Rich Text Custom Fields.md` and read the session prompt carefully.
11. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration**, ask the user what existing system handles it. The session leans on the existing QuillEditor primitive — do not introduce a new rich-text editor library, do not pull in HTMLPurifier without surfacing first.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** The HTML sanitization shape is the load-bearing decision this session — settle it in Design decisions before writing the rendering layer.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migration expected.** `field_type` is a string column; `rich_text` is a new value, not a column. If verification surfaces a need for a migration (e.g., the column has a CHECK constraint listing allowed values), surface and decide.
- **Update `docs/schema/custom_field_defs.md`** — extend the `field_type` allowed-values list with `rich_text`.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline at 249 close: 1778 fast tests / 9318 assertions. Expect modest growth — new tests for the rich_text branch in `toFilamentFormComponent`, the resource form/table updates, and a render/sanitization test.
- **Front-end builds — when to run which:**
  - No SCSS or page-builder JS expected — this session is admin-side PHP + Blade only.
  - The Quill v2 lib is already loaded on every admin page via the panel provider; no admin-bundle build required.
  - If the rendering surface for rich-text on the public site touches `resources/scss/**`, run `npm run build`.
- **Cross-repo coordination — Fleet Manager agent contract.** This session is **not** boundary-touching. The standing cross-repo flag is dormant: this session does not modify any file in `app/Http/Controllers/Api/Fleet/*`, `docker/nginx/{default,prod}.conf`'s mTLS sections, the response schema, or the VERSION pipeline. If during the session a change **would** touch any of those, stop and ask — the contract surface is locked at v2.0.0 until the next deliberately boundary-touching session.
- **Verify objective outcomes yourself; pull the user in for judgment.** When you can observe the result directly, do — don't ask the user to run it and report back. Close the loop yourself for CLI output, file contents, exit codes, logs, and anything Playwright can verify. **Playwright is a verification mechanism, not just an existing test suite** — the rich-text editor renders in the browser; a quick spec written for the moment to verify "QuillEditor mounts on a Contact's rich-text custom field, accepts bold/italic, persists across save+reload" is a real verification path. Reach for it without being told. Pull the user in for visual judgment on the editor's chrome and the rendered HTML's typography on detail views.
- **Pause for manual testing only when human judgment is required.** The HTML rendering on detail views and widget output benefits from a human eye for typography polish. After implementation, announce that visual testing is ready, then stop and wait. Do not suggest closing the session.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The next session after 250 is the partner B2 prerequisite:

- **E2 — Importer Mapping Page UX.** UX session across the five mapping pages (`ImportContactsPage` and friends): completed-row visual indicator, reduced vertical sprawl, friendlier dropdowns, optional grouping. UX feel + design judgment heavier than E3; the session lands as a shared pattern across all five pages, not a per-page reskin. Stub at `sessions/session-outlines.md` § Importer Mapping Page UX.

If FM 012/013+ surfaces CRM-side fix-ups during its absorption, those land first and E2 slides to 252.

Decide at 250 close based on FM-side state. Draft the 251 base prompt + session prompt against the agreed shape.

### Phase 2 — Close

When the user says to close (after 251's prompt is agreed):

- **Session log**: write `sessions/250. Rich Text Custom Fields — Log.md` against `template-session-log.md`.
- **Update `sessions/completed-sessions.md`**: append session 250's row.
- **Update `sessions/release-plan.md`**: tick the checkmark on E3.
- **Update `sessions/session-outlines.md`**: collapse the Rich Text Custom Fields stub to a closed-at-250 reference, mirroring how A1 was collapsed at 245 close and E1 at 249 close.
- **Schema docs**: confirm `docs/schema/custom_field_defs.md` reflects the new `rich_text` allowed value.
- **Archive the previous session**: move all files matching `sessions/249. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-250/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array. (No new admin pages expected this session — the change extends the existing CustomFieldDef admin surface.)
- Every new Filament page must include `getBreadcrumbs()`. (Same caveat — likely not triggered this session.)
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. The portal renders custom-field values today; verify the rich-text rendering path on portal surfaces (if any) doesn't introduce an XSS hole.
