# Base Prompt — Session 381 (Events Vertical Module)

**Start now.** Execute the reading list below, open the session prompt at `sessions/381. Events Vertical Module.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **381. Events Vertical Module**.

This is **arc position P5** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary) — the first **domain vertical** extraction, decided at P4 close (owner picked Events over Donations, per the recommendation in the 380 log). P5 carves the Events behavioral surface — registration controllers and routes, the quantities service, both observers, three mailables, the reminders command and schedule, the policy, the admin resource, seven widgets, and the events importer — into `plugins/Events/`, and lands the two remaining big contract mechanics as *real*: the admin socket (surfaces 3/4, fixture-proven at 379, promoted to PROVEN by this shipping resource) and the inbound payments inversion (surface 10 — the Payments webhook's events branch becomes a core-owned typed event the Events plugin listens to). The Events↔Payments pair becomes the living proof of the dependency rules: a vertical consuming a foundation capability optionally, degrading to free events without it.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**. The plugin contract (`docs/plugin-contract.md`) bumps 0.4.0 → 0.5.0 at close. (The client-billing surface remains out of scope and guard-protected as always.)

**Session shape:** feature-style / behavior-preserving extraction plus new core mechanics (importer socket, typed payment-settled event). Public event behavior — free and paid registration, both controllers, reminders, cancellation sends, landing-page rendering — must be identical before and after. **Expect multiple iterations**; each phase below is independently committable. **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` §§ 2–4, § 6 (dispositions 2 — dependency rules, 3 — per-plugin tests, 7 — migrations-vs-squash) and § 7, and `docs/plugin-contract.md` v0.4.0 — surfaces 2, 3, 4, 8, 10, 13 and the normative rules; surface 13's entry is the worked P4 precedent for "make a surface real", and surfaces 3/4 carry the fixture-proven mechanics this session promotes.
3. Open `sessions/381. Events Vertical Module.md` — the survey and front-loaded decisions there are settled; do not reopen them.
4. Read the code being carved and its seams: `app/Http/Controllers/EventController.php`, `app/Http/Controllers/Portal/EventRegistrationController.php`, `app/Services/EventRegistrationQuantities.php`, `app/Observers/EventObserver.php`, `app/Observers/EventRegistrationObserver.php`, `app/Mail/RegistrationConfirmation.php` + `EventCancellation.php` + `EventReminder.php`, `app/Console/Commands/SendEventReminders.php` + the `events:send-reminders` schedule line in `routes/console.php`, `app/Policies/EventPolicy.php` + its `AuthServiceProvider` map line, `app/Filament/Resources/EventResource.php` + its four pages (note `EditEvent`'s cancellation wizard and `ViewRegistrations`' registrant export), the seven widget folders (`app/Widgets/{EventDescription,EventImage,EventMiniCalendar,EventRegistration,EventsListing,PortalEventRegistrations,ThisWeeksEvents}/`), the importer trio (`app/Importers/EventFieldRegistry.php`, `EventImportFieldRegistry.php`, `RegistrationFieldRegistry.php`) + `app/Importers/Concerns/AggregatingRegistry.php` + `app/Filament/Pages/ImportEventsPage.php` / `ImportEventsProgressPage.php` and their core consumers (`app/Services/Import/CsvTemplateService.php`, `ImportSessionActions.php`, `ImportSessionPreview.php`, `app/Console/Commands/SeedFakeImportSourcesCommand.php`), the events routes in `routes/web.php` (`events.register`, `portal.events.register`, `api.events.json`), `plugins/Payments/Http/Controllers/StripeWebhookController.php` (`handleEventRegistrationCheckout` — the branch that inverts), `plugins/Payments/PaymentsServiceProvider.php` + `app/Plugins/*` (the registry patterns to mirror), and `database/seeders/PermissionSeeder.php` (the role matrix carrying the `event` resource permissions).
5. Read the behavioral net you must keep green: `tests/Feature/MultiQuantityRegistrationTest.php`, `RegistrationConfirmationTest.php`, `PaidCheckoutTest.php`, `RegistrantExportTest.php`, `EventCancellationTest.php`, `FinancialSourceWritePathTest.php`, `DonationAcknowledgmentTest.php`, `StripeCheckoutBrandingTest.php`, `PaymentsPluginSession380Test.php` + `PaymentsPluginRemovalSession380Test.php`, `PluginAdminSocketSession379Test.php` (+ removal mirror), `WidgetTemplateBoundaryTest.php`, and the widget manifest/sync tests. Then grep tests for any other file naming a moved class and re-point mechanically.
6. Skim `sessions/session-outlines.md` (Plugin Architecture track entry), `docs/app-reference.md`, `app/Providers/Filament/AdminPanelProvider.php` (how it consumes the admin registry), and the "stays core" list in the session prompt's survey — the engine seams you must NOT move.
7. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 380

- **CRM contract at v2.7.0** — 380 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** arc P4 complete (380) — Stripe rails live in `plugins/Payments/` (checkout service, webhook controller, price observer, live-mode sniffer); core `App\Plugins\CapabilityRegistry` (`provide`/`present`/`enabled`, lazy resolvers) bound by `PluginServiceProvider`; core-owned contracts `App\Payments\Contracts\CheckoutProvider` + `PaymentModeProvider` (static entry `App\Payments\PaymentMode::isLive()`, false when unbound); five vertical checkout call sites + `DashboardIntegrationStatusWidget` consume `enabled('payments')`; standing guard `PaymentModuleBoundaryTest` bans the Stripe SDK, `Plugins\Payments\`, and `services.stripe.*` config reads in `app/` with zero allowlist; `laravel/cashier` removed (two dead vendor routes dropped, final route count 272), `stripe/stripe-php` direct. `docs/plugin-contract.md` at **v0.4.0** (surfaces 1, 2, 11, 12, 13 PROVEN; 3/4 DECLARED with fixture-tested mechanics; 5–10 DECLARED; surface 10 carries the named P5 inversion plan).
- **Schema baseline:** no schema changes at 375–380. None expected this session — the events tables stay on core (front-loaded decision 2).
- **Fast Pest suite green at 3189 / 0** (380 close; +33 over the 379 baseline: capability semantics, payments twin + removal mirror, ProductPriceObserver backfill, payments boundary guard). Expected delta this session: substantial — vanish twin/mirror for Events, the Events×Payments presence-matrix tests, the importer-socket tests, the inversion tests, the EventCancellation wizard-send backfill, `EventModuleBoundaryTest`; existing tests re-pointed mechanically where they name moved classes.
- **Playwright suite** — baseline preserved; does not run this session, CI is the signal.
- **Planning state:** 380 close marks P4 complete in the plan doc + track entry; owner decided **Events** as the first vertical at P4 close.
- **Housekeeping inbox** — the 380 capture buffer carried one item (Filament admin font → Google Fonts provider, CSP report-only warning), folded at 380 close; it is **not** this session's work. **This session absorbs one inbox sub-item** (Rule 2): from the `[test-integrity]` `[0.363.03]` coverage-gap list, item (4) — the live `EventCancellation` wizard send in `EditEvent` has no test anywhere (`EventCancellationTest` guards only the retired auto-send). P5 moves `EditEvent`; the one-test backfill (Mail-faked wizard-action exercise) lands with the move. Items (2)–(3) of that entry stay in the inbox.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavioral parity is the bar for the carve.** Capture `php artisan route:list --json` before touching anything; after each phase the diff must be empty apart from action-FQCN changes on moved controllers (the 380 precedent) — `events.register`, `portal.events.register`, and `api.events.json` keep identical names, URIs, methods, and middleware. Existing test assertions do not change; only mechanical re-points where they name a moved class.
- **The boundary rule is one-directional and guard-enforced from day one:** nothing in `app/` may reference the `Plugins\Events\` namespace (`EventModuleBoundaryTest`, zero allowlist). The plugin reaching core — models, `Transaction::recordStripe`, the capability API — is the allowed hard dependency. The models staying core is what makes the remaining engine seams (resolver arms, page-builder sources, content import/export) legitimate core code, not violations.
- **The Events×Payments matrix is the point of the session.** Both present = full behavior (existing suite). Events present + Payments removed = free/comp events work, paid degrades to not-configured (extend the 380 `PaymentsRemovedTestCase` reuse). Events removed = the whole events surface vanishes, data kept. All three states get explicit tests.
- **Permissions are behavior-preserving:** the `event` resource permissions stay in core's `PermissionSeeder` role matrix (shipped roles keep their grants — see front-loaded decision 6). No grant changes of any kind.
- **The front-loaded decisions in the session prompt are settled.** Implementation details within them are yours to decide and log.
- **Scope is binding.** Models and schema stay core (P7 moves them with the squash redraw). Donations/memberships/products webhook branches stay inline in Payments. Resolver arms, page-builder data sources, content import/export, the random-data generator, SEO, custom-field plumbing stay core. No per-install activation (P8), no packaging (Stage B), no schema changes, no FM surface, no visual changes. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Front-end build:** `build:public` is REQUIRED after the widget move (three widgets carry SCSS/JS whose repo paths change) — run `widgets:sync` then `build:public` and verify the manifest + bundle carry the `plugins/Events/` assets (the surface-11 caveat: stale paths drop silently). `npm run build` is not expected (no Vite-compiled source changes).
- **Contract doc update at close:** surfaces 3 and 4 promoted to PROVEN (first shipping admin-page plugin), surface 8 recorded with the importer-socket mechanics and promoted if genuinely exercised end-to-end, surface 10 recorded with the typed-event mechanics and promoted scoped to the events slice, surface 2 notes the at-scale widget contributor, surface 13 notes the first in-plugin vertical consumer, plus the surface-3 permission nuance (decision 6). Bump to 0.5.0, changelog entry.
- **P6 input at close:** record in the session log a short recommendation on Stage B entry (P6 scope: composer path-repo mechanics + squash-boundary redraw) informed by what this carve revealed — especially anything that made the in-repo boundary awkward and would be cheaper to fix before packaging.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code (the owned-file list, the stays-core list, the permission matrix, the webhook branch), state the carve order and any drift found — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
