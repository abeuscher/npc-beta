# Plugin Architecture — Plan

**Status:** living document, written during session 376 as the discussion happens. Owner-corrected as it goes. This doc is the canonical output of the session and the spine of the sessions that follow it.

**Arc progress (last update: session 395, 2026-08-20):** **P1–P10 + D1–D5 ✅ complete (sessions 377–395) — Stages A, B, C-lite closed; Stage D running.** D2 (388): Events extracted to `github.com/abeuscher/crm-plugin--events` (tag `v0.1.0`, manifest `path` → `vcs`) — the known-edges runbook run: first migration-owning extraction (the disabled-≠-uninstalled fixtures re-pointed to the vendor'd migrations, assertions unchanged), first testsuite-carrying extraction (the 29-file `Events` suite travels with the package; phpunit re-points to `vendor/nonprofitcrm/events/tests`, identical counts in core CI), gate 4 live at scale (five vendor-relative asset paths, bundle content + the BlogPager cross-input proven), and `plugins/` emptied until D3's carve (scoped guard relaxations; root PSR-4 stays until D12); contract doc **v0.12.0**; fast Pest 3263/0; route parity 272/272. **D3 ✅ closed (389 carve → 390 package → 391 extract):** the Donations vertical is `nonprofitcrm/donations` at `github.com/abeuscher/crm-plugin--donations` (public, tag `v0.1.0`, manifest `path` → `vcs`, version field dropped) — the fourth runbook run, every edge D2-proven at smaller scale: the two fixture migrator reaches + four in-plugin test path reads re-pointed vendor-relative, the 14-file `Donations` suite runs from `vendor/nonprofitcrm/donations/tests` with identical counts, gate 4 proven on the one DonationForm asset, `plugins/` emptied again with zero new guard relaxations; surfaces 7 + 10-donations PROVEN (389), second squash-boundary redraw clean (390); contract **0.15.0**; fast Pest 3314/0; routes 272/272; Payments v0.2.0 + Events v0.1.1 released along the way. **D4 ✅ closed (392 carve → 393 package + extract, one combined session — the D1 compression precedent):** the Memberships vertical is `nonprofitcrm/memberships` at `github.com/abeuscher/crm-plugin--memberships` (public, tag `v0.1.0`, manifest `path` → `vcs`, version field dropped) — carve 392 (the second cross-repo inversion, Payments v0.3.0); package + third squash-boundary redraw + fifth runbook run 393 (smallest redraw yet — zero inbound core FKs, no sequences; both identity checks clean; composition-safety route-presence gates incl. the check's one find, `DatabaseSeeder`'s unconditional tier-seeder call; third migration-owning + testsuite-carrying extraction, no runbook deltas, zero new guard relaxations, `plugins/` empty again until D5's carve); contract **0.17.0**; fast Pest 3355/0; routes 272/272. **D5 ✅ closed (394 carve → 395 package + extract, one combined session — the D4/393 compression precedent):** the Member Portal behavioral surface is `plugins/MemberPortal/` — six widgets, six controllers + the 17 portal routes plugin-registered byte-identically, the `PortalEmailChange` mailable, and the first plugin-owned **auth stack** (guard/provider/broker config-injected at `register()`, the `portal.auth` alias at `boot()` — the first plugin-owned middleware alias); the **`member-portal` capability** is surface 13's second shipping capability (presence-is-enablement), consumed by core's five gated `auth('portal')` reads and by the `crm-plugin--events` **v0.1.2** rider (`portal.events.register` registers only when present — vanishes instead of 500ing portal-absent); the verification/password-reset mailables stayed core as model-adjacent (logged drift from the survey's "three travel" — their senders are `PortalAccount`'s framework hooks, § 6.7); contract **0.18.0**; routes 272/272; fast Pest 3355 → 3384/0. **The 395 closer:** `nonprofitcrm/member-portal` packaged (sixth path-repo package, P6 mechanics) + the **fourth squash-boundary redraw** — the two portal tables to plugin-owned byte-faithful migrations (`portal_accounts_id_seq` travels complete — the second sequence-carrying redraw; the SET-NULL contact FK travels as plain behavior), both identity checks clean; the portal-absent composition-safety hunt gated on the capability (the contact-admin Portal Access section + the four EditContact portal actions, the page-topology seeder calls — `PortalPageSeeder` + `SystemPageSeeder`'s three auth pages, chrome stays; `DemoPortalMemberSeeder` self-skips); then the sixth runbook run to `github.com/abeuscher/crm-plugin--member-portal` (public, tag `v0.1.0`, manifest `path` → `vcs`, version field dropped) — the fourth migration-owning + testsuite-carrying extraction, no runbook deltas, zero new guard relaxations, `plugins/` empty a fourth time until D6's carve; contract **0.19.0**; routes 272/272 byte-identical; fast Pest 3384 → 3391/0. **Next: D6 — the Blog/Posts block opener.** **The 385 reframe (owner ruling):** no launch — the finish line is the end-state architecture via **§ Stage D — full decomposition** below (~15–21 sessions, positions D1–D12), then **§ Stage E** (niche distributions) as the follow-on arc. Per-stage detail lives in § 7; per-session detail in the session logs (the pre-385 session-by-session narrative formerly held here is compressed into the § 7 stage ladder — the logs are canonical).

---

## 1. Motivation (settled)

The owner's founding idea: make plugins out of everything possible within the app — separate repos with a central build over a minimal core. It is the one architectural conviction carried through the whole project, and it has never been fully implemented; this plan is the thinking-through it has always been owed. (The inspiration is the mod architecture of moddable games; the design here targets a very different use case and diverges deliberately — the analogy informs the shape, not the contract.)

Three motivations surfaced in discussion, in increasing order of consequence:

1. **The architecture itself** — minimal core, everything else a plugin, assembled by a central build.
2. **The LLM context-window problem.** A bounded piece plus a written contract lets an agent work without loading the whole application. This project already proves the pattern in-house: the CRM↔Fleet Manager relationship (two repos, a versioned contract doc at v2.7.0, ~50 FM sessions run without ever loading the CRM codebase). Important correction settled in discussion: **the context savings come from the boundary and the contract, not from the repo split.** Repo separation adds coordination cost and pays only where a piece changes independently at its own rhythm.
3. **The business premise** — vertical SaaS on a shared platform: one engine, several niche configurations, each marketed to a small audience in its own vocabulary. What differs per niche is mostly vocabulary, defaults, seed content, feature selection, and the demo site — so **a niche is a *manifest* (core + chosen plugin set + config/seed pack), a modpack, not new engineering.** This rests on the cheapest modularity property (per-install composition), not the expensive one (plugins patching plugins).

**Retrofit over greenfield (settled).** The goal is something worth showing — "this architecture survives contact with a real 375-session application" is the rarer and more persuasive claim than a greenfield toy, there is no specific greenfield product in mind (scope-creep risk), and this codebase is a scoped, built product with known quantities.

---

## 2. Principles (settled in discussion)

1. **The contract is the load-bearing artifact.** A module boundary only saves context if an agent can substitute a short document for the code on the other side. Missing, stale, or leaky contracts mean the modularity tax is paid for nothing.
2. **Thin core buys a small shared surface; modularity buys a small working set.** The core's size sets the floor on what every agent must understand regardless of which module it works in. If the core is fat, most changes are core changes and the modularity never pays.
3. **Litmus test for plugin-hood: a piece is a plugin only if some plausible install would exclude or replace it.** Every boundary is a contract maintained forever; pay for swappability only where variation actually occurs. Pieces that every install needs are core — boundary-izing them can still buy context benefits, but not composition benefits.
4. **Sockets vs. parts.** Composition points every install needs (admin nav, the page canvas, the settings surface) are core *sockets*; plugins contribute into them. The nav is not a plugin; nav entries are.
5. **Plugins never patch plugins.** Plugins extend core through published surfaces; they do not modify each other. Mature plugin ecosystems concentrate their compatibility pain exactly where two extensions meet with no controlled contract to resolve against — it is the property whose cost explodes. Explicitly out.

## 3. Modularity properties — which are the point (working ranking)

| Property | Verdict for this project |
|---|---|
| 1. Minimal core, maximal periphery | **The point.** Drives everything below. |
| 2. Declarative definitions (data over code) | Already rhymes with the widget system (`config_schema`, definition classes). Adopted where it falls out naturally; not pursued as an end in itself. |
| 3. Separate distribution (own repos, own versions) | **The stated vision** — end state, reached last (Stage C), only where change-rhythm justifies it. |
| 4. Load order + plugins patching plugins | **Out** (Principle 5). Composer resolves acyclic dependencies; that is the ceiling. |
| 5. Per-install composition | **The business payoff** — niche = manifest. Note: the session prompt assumed `widget_types.is_active` exists; it does not (no per-install activation mechanism anywhere today). This property is *less* built than assumed. |
| 6. Community authorship | Aspirational; recorded as a later-stage line item only. |

---

## 4. The full-vision end state (proposed, under discussion)

### 4a. Core (the engine)

| Piece | Today | Notes |
|---|---|---|
| **Identity & access** — `User`, `Contact`, auth, permissions (Spatie), 2FA | `app/Models/Contact.php` is the hub every feature writes to | Contacts are core: the identity layer. How plugins *extend* a contact is the #1 open contract question (§6). |
| **Admin shell** — Filament panel, nav groups as sockets, settings framework | `app/Providers/Filament/AdminPanelProvider.php` — path-based discovery, 5 hard-coded nav groups, plus a large inline route closure | The inline route closure is a known obstacle; Filament supports per-package discovery paths, so the socket model is feasible. |
| **Page/widget engine** — pages, layouts, widget primitive, `WidgetDefinition` contract, registry, `ContractResolver`, public renderer, theme/tokens | `app/Widgets/Contracts/WidgetDefinition.php`, `app/Services/WidgetRegistry.php`, `app/WidgetPrimitive/ContractResolver.php` | The product's actual engine per the owner ("a niche-specific page builder"). The data-contract path is the proven miniature of the whole architecture. |
| **Plumbing** — media library, email transport + template mechanism, queues/scheduler, activity log, backups | Spatie medialibrary; `EmailTemplate::forHandle()` (defaults hard-coded on the model — needs a registry to be plugin-usable) | |
| **Fleet/ops surface** — `/api/health` + FM contract, deploy spine, VERSION | `app/Http/Controllers/Api/Fleet/*`, `deploy.yml`, GHCR immutable tags | Already a working central build to dock into. |

### 4b. Plugins (candidate decomposition)

| Family | Candidates | Litmus-test read |
|---|---|---|
| **Domain verticals** | Donations (funds, checkout, receipts), Events (registration, ticketing, calendars), Memberships (tiers), Member Portal, Forms, Blog/Posts | Each individually excludable — a hobby-club niche has no donations; an internal CRM has no portal. "The CRM section" is not one plugin; it dissolves into identity-core + these. Analog (owner's): first-party DLC — modules we author and ship, distinct from any future third-party surface. |
| **Foundation plugins** | Payments (Stripe rails: customer records, checkout, webhooks, receipt plumbing) | A plugin other plugins may *optionally* use. Passes the litmus test (a calendar-only install excludes it). Verticals degrade gracefully without it — Events without Payments = free events only; the existing $0/comp registration path proves the shape works. |
| **Widgets** | The 41 widget folders; each or in packs | The most plugin-shaped code in the app (own folder, contract class, assets, demo seeder). Blockers: hardcoded 41-class registration in `WidgetServiceProvider`, DB-mediated asset paths, and model-reaching templates (Nav → `NavigationMenu::find()`, DonationForm → `Fund::where()`, Portal* → tiers/settings/auth guard). Clean-today pilots: LogoGarden, BarChart, TextBlock (pure render over `dataContract()` data). |
| **Integrations** | QuickBooks, Mailchimp, Resend, (Stripe — see §6) | The classic, easiest plugin family: already fairly isolated, no install needs all of them. |
| **Importers** | Per-domain importers ship *with their domain plugin* | Registries exist (`app/Importers/*FieldRegistry.php`) but wiring is static (~14 hard-coded Filament page pairs). |

### 4c. The plugin API — the contract core publishes

Each surface below is something a plugin needs and core must guarantee. Current-code grounding in parentheses.

1. **Registration** — one service provider per plugin; Laravel package auto-discovery. (Precedent: `WidgetServiceProvider` already registers 41 definitions + Blade namespace + slots — the proto-plugin registry.)
2. **Widget contribution** — register `WidgetDefinition`s into the registry; `widgets:sync` stays the DB path. (Exists; needs to accept external packages.)
3. **Admin contribution** — Filament resources/pages via per-package discovery paths; nav-group self-assignment. (Filament supports this; the panel provider's inline route closure must be broken up.)
4. **Routes** — per-plugin route files. (Precedent: the three admin-builder API route files pulled in by the panel provider.)
5. **Migrations** — `loadMigrationsFrom` per plugin; interaction with the squash discipline is an open hard problem (§6).
6. **Seed/config packs** — per-plugin seeders + the niche configuration layer.
7. **Email templates** — handle registration replaces the hard-coded defaults array on the model.
8. **Importer registration** — registry entries + the Filament import-page pairing.
9. **Contact extension** — how a plugin adds data to the identity core without owning it (candidates: relations + the already-installed `spatie/laravel-schemaless-attributes`; see §6).
10. **Events/hooks** — Laravel events for cross-plugin reaction (e.g. Donations fires, Mailchimp listens).
11. **Front-end assets** — per-plugin widget CSS/JS through the existing out-of-band build server (`build:public` already reads asset paths from `widget_types` and builds outside Vite/Docker — a central asset build already exists).
12. **Versioning** — the contract doc itself, versioned like the FM contract, with the same discipline.
13. **Capability detection / soft dependencies** — a plugin can ask "is capability X present and enabled?" and degrade gracefully when it isn't. Dependency rules (owner guideline, settled): verticals never depend on each other; hard dependencies point only at core; optional dependencies point at foundation plugins (composer `suggests`, not `require`).

### 4d. Distribution — the niche/manifest layer (Stage C end state)

- **Plugin repos**: each plugin a composer package (+ its widget assets), own version, own tests.
- **Slim core repo**: the engine + the published plugin API contract doc.
- **Distribution repo per niche** ("the modpack"): a `composer.json` requiring core + the plugin set, a config/seed pack (vocabulary, defaults, theme, demo content), and the manifest the central build resolves into a deployable image.
- **Central build**: the existing `deploy.yml` → GHCR → immutable VERSION pipeline, generalized to build a distribution instead of one repo. FM polls `/api/health` per install exactly as today; a plugin manifest in the health payload is a future FM-contract item (Two-Repo Coordination Protocol — not this session).

**Changeability — the three-tier model (settled in discussion).** When a client needs something their niche configuration excluded, the fix depends on which tier the feature sits at: (1) *in the image, switched off* → flip a per-install activation flag (minutes; FM's existing config-push channel is the delivery mechanism); (2) *exists but not in the niche image* → add it to the distribution manifest, version-bump, existing CI rebuilds and ships (an hour, no code); (3) *doesn't exist* → build it, as a self-contained package. Recommendation: **niche images are supersets** — include every plugin a client in the vertical might plausibly want, let per-install activation decide visibility — so the common case stays at tier 1. Consequences: the per-install activation layer becomes **load-bearing for the business model** (and is today the least-built part of the vision — no activation mechanism exists anywhere); and enable/disable are asymmetric — enabling later is cheap, disabling must mean "hidden and inert, writes stopped," never "data deleted" (a rule the plugin contract must state).

---

## 5. What already exists vs. what must be built (inventory summary, session 376)

**Exists:** the widget contract + registry + deploy-integrated sync; the data-contract/ContractResolver path (proven plugin-safe boundary); an out-of-band central asset build server; an immutable-versioned central image build; a working two-repo contract discipline (FM, v2.7.0); consistent registry patterns (importers, email handles, definitions-in-provider).

**Missing:** any stated definition of "core"; runtime pluggability anywhere (every seam is a hardcoded list); per-install activation of anything; composer path-repos / npm workspaces (single monolithic package); a migrations-per-plugin slot (squash + monolithic seeder); a plugin API contract doc.

---

## 6. Question dispositions (running list)

**Resolved in session:**

1. **Contact extension: relations on id, only** *(owner ruling)*. Plugins attach data via plugin-owned tables keyed by `contact_id` — no columns added to core tables, no schemaless-blob co-mingling. This is also the compliance-friendly direction: plugin-owned tables can be independently encrypted (per-column casts), access-scoped, audited, and even moved to their own database connection later — exactly the doors a future HIPAA-class requirement needs open. Corollary: core `Contact` stays minimal identity (name/contact info). UI half: the contact admin screen exposes a socket where plugins contribute their own panels.
2. **Payments is a foundation plugin; verticals never depend on each other** *(owner guideline)*. Hard dependencies point only at core; foundation plugins are optional capabilities a vertical detects and degrades without (Events without Payments = free events/calendar only — a common real use case). See §4b and §4c-13.
3. **Tests: per-plugin suites; the monolith is dismantled** *(owner ruling)*. Each plugin carries its own suite; core keeps engine tests plus a thin composition smoke suite; the central build runs core + the distribution's plugin suites. Extraction doubles as the audit moment — reduce and de-duplicate while pulling apart. Add-a-test discipline becomes a stated rule in the plugin contract.
4. **Composition mechanics: both layers, superset images** *(from the three-tier changeability model, §4d)*. The niche manifest decides what is *in* the image at build time; per-install activation decides what is *on* at runtime — the fast, common path. Remaining detail: the contract's definition of "disabled" (inert — no routes registered, no nav, no scheduler jobs, writes stopped, data kept).

5. **Filament coherence dissolves into three contract conventions** *(owner-approved)*: (a) core owns the nav-group list; a plugin declares which group its pages join plus a sort weight; (b) a plugin declares its permissions, core seeds them at enable time, granted to no shipped role by default except super-admin (the existing `manage_account` precedent); (c) plugin admin pages inherit core's admin theme — plugins ship no admin CSS.

**Open:**

6. **The real niches** — deferred to owner research. Needed only ahead of distribution-layer work, not before; the core line drawn here does not wait on it.
7. **Migrations vs. the squash discipline** — mechanics understood; the work is (a) redrawing the squash boundary so core's schema dump excludes plugin tables while each plugin owns its own migration list, (b) deterministic install order: core schema → enabled plugins' migrations → seeders (enabling a plugin later runs its migrations then), (c) a per-composition fresh-install identity check replacing today's single fixed-schema assumption. Cost class: about a session, landing with the squash redraw (P6/P7). Discipline problem, not a danger problem.

8. **Products/store code** — product checkout branches and the two product widgets exist but were never on the § 4b candidate list (the store was deferred pre-pivot). Where they land — own vertical, Payments-adjacent, or core-until-a-store-era — resolves at Stage D position D8's prompt draft.

   **Events-owned schema annotation (recorded at session 381 — P5's deliverable in lieu of moving schema; the input for the P6/P7 squash redraw).** Tables that move out of core's dump when the boundary is redrawn: `events`, `event_registrations`, `ticket_tiers` (docs at `docs/schema/{events,event_registrations,ticket_tiers}.md`). All three live in the squashed dump `database/schema/pgsql-schema.sql` (the formerly-live `is_complimentary` migration was absorbed by the 382 squash; P7 authored the plugin's create-table migrations from the dump DDL and removed the three tables from it — executed session 383). Factories staying core (confirmed at P7 — no scheduled move): `EventFactory`, `EventRegistrationFactory`, `TicketTierFactory`. Core columns/relations that reference these tables and stay core: `pages.type = 'event'` landing pages (`events.landing_page_id`), `contacts`/`organizations` event relations, `transactions` polymorphic subject rows (`EventRegistration` subjects), import bookkeeping columns (`import_source_id` / `import_session_id` on both events tables). The models (`Event`, `EventRegistration`, `TicketTier`) and the engine seams that read them (resolver arms, page-builder sources, content import/export, random-data generator) are core code by decision — reaffirmed at P7 (383 prompt decision 2, owner-reviewed): the model move has **no scheduled arc position**; candidate slot alongside Stage C seam work. Consequence, binding on core: with the schema plugin-owned (P7), core code reading these tables must be composition-safe on a never-installed composition (contract surface 5's core-model caveat).

---

## 7. Scope ladder, session arc & feasibility (proposed — under discussion)

The ladder is the de-risking order: each stage is independently valuable and independently stoppable, and each is where a specific class of problem is cheapest to discover.

### Stage A — module boundaries in-repo (~4–6 sessions)

No repo split, no build change. Formalize the contract and prove the boundaries are real; this is where entanglements surface cheaply.

- **P1 (session 377) ✅ — Pilot widget as a self-registering module + plugin contract v0.1.** Done: LogoGarden (carried both JS and SCSS — asset seam exercised end to end) extracted to `plugins/LogoGarden/`, registered solely via its own provider through `config/plugins.php`; `docs/plugin-contract.md` v0.1.0 drafted from what the extraction proved. See the 377 log.
- **P2 (378) — Make the widget boundary real across all 41.** The data-contract path becomes mandatory; the model-reaching templates (Nav, DonationForm, Portal set) are routed through declared contracts; a standing guard test (the `DesignGroupIntegrityTest` pattern) bans model calls in widget templates from then on.
- **P3 (379) — Admin-shell sockets.** Decompose `AdminPanelProvider`'s inline route closure into per-feature route files; implement the three approved Filament conventions (nav groups + sort weight; permissions seeded at enable, super-admin-only by default; core-owned admin theme).
- **P4 (380) — Payments foundation module.** Carve the Stripe rails (customer records, checkout, webhooks, receipt plumbing) into an in-repo module behind the capability-detection API; verticals call "is payments enabled?" instead of reaching for Stripe directly.
- **P5 (381) ✅ — First domain vertical in-repo.** Done: Events (owner decision at 380 close). The behavioral surface into `plugins/Events/`; admin socket + importer socket + inbound `CheckoutSettled` inversion landed real (surfaces 3/4/8 PROVEN, 10 PROVEN events-slice); Events×Payments presence matrix executable; models/schema stay core with the § 6.7 annotation as the P7 redraw input. See the 381 log.

### Stage B — real packages, one workspace (~3–5 sessions)

Packaging mechanics proven while iteration stays cheap (everything still one repo, one CI).

- **P6 (382) ✅ — First composer package + the plain T1 squash.** Done: LogoGarden is the first true path-repo package (`nonprofitcrm/logo-garden`, own composer.json with explicit version, bound require, committed lock, strict validation clean; Dockerfile manifests-before-source fix proven locally + in CI); `config/plugins.php` confirmed sole activation + ordering authority — no auto-discovery, guard-enforced (`PluginPackageGuardTest`); the T1 squash landed with zero test fallout and a clean 348-procedure identity check (events tables kept in the dump per the scope split); contract doc v0.6.0 (Package shape (Stage B) section; surface 1 amended). See the 382 log — its P7 input section carries the mechanics learnings (explicit version, bound constraints, COPY-glob flattening, the in-image proof procedure).
- **P7 (383) ✅ — Events vertical as a package & squash boundary redraw.** Done: Events is the second composer package (`nonprofitcrm/events`, P6 mechanics exactly); **the squash boundary redrawn** — the three events tables left core's dump for plugin-owned byte-faithful create-table migrations (`loadMigrationsFrom`), deterministic install order core dump → enabled plugins' migrations → seeders, both per-composition identity checks clean (full composition byte-identical modulo migrations rows; the events-absent composition is the repo's first fresh-install-without-a-vertical proof — it surfaced exactly one unconditional core read of a plugin-owned table, `PageContextTokens`, fixed with the established page-type gate); vanish semantics settled as disabled ≠ uninstalled (fixtures keep schema via a direct migrator path); the test-monolith dismantling begun — 29 events-slice files moved to `plugins/Events/tests/` as the `Events` testsuite with a 3-deletion logged reduction pass; standing guard pins plugin tables out of the core dump. Contract doc v0.7.0 (surface 5 PROVEN). See the 383 log — its P8 input section carries what the events-absent composition revealed for the activation layer.
- **P8 (384) ✅ — Per-Install Activation & Workspace Assembly.** Done: the tier-1 flag-flip path is real — env-backed `PLUGINS_DISABLED` (comma-separated kebab handles, derived from the provider FQCN, no registry) surfaced through `config/plugin-activation.php` and subtracted by `PluginServiceProvider`'s filter at the exact strip-the-line seam the removal fixtures prove, so **runtime-disabled ≡ the proven mirror semantics by construction**; `config/plugins.php` untouched as the installed superset + sole ordering authority; unknown handles fail the bootstrap loudly. Workspace assembly proven as the superset image (the accepted Stage B reading, owner-confirmed at the Open Gate): one locally built image booted as the full composition (migrations 117, 41 widget rows, events surface live) and as `PLUGINS_DISABLED=events` (migrations 114 on a fresh DB, 34 widget rows, register POST 405 / JSON 404, schedule empty), then the **enable-later flip verified end to end** (flag off → restart → `migrate` applies the three plugin migrations, surface returns). No FM surface; plugin contract **v0.8.0** (surface 1 two-layer rewrite + tier-1 runbook). Build-side generalization (distribution manifest, central build over external repos) is P10. See the 384 log — its P9 input records what the flag layer assumes about in-repo plugins.

### Stage C — separate repos, central build (**C-lite**, ~2–4 sessions)

The full vision says *everything* eventually lives in its own repo; the demonstration needs only **one** plugin to make the round trip. C-lite proves the distribution story end to end without migrating the world.

- **P9 (385) ✅ — One plugin to its own repo & the extraction runbook.** Done: LogoGarden made the round trip — its eight files live at `github.com/abeuscher/crm-plugin--logo-garden` (public, tagged `v0.1.0`, README carrying the FM-style release discipline; naming pattern `crm-plugin--{kebab-handle}`), consumed through a root `vcs` repository + the unchanged bound `^0.1` require, lock pinned to the tag (git source at the exact commit + dist zip). The explicit `version` field dropped from the extracted manifest (the path-repo determinism device inverts under tags); the Dockerfile's two LogoGarden manifest COPY lines removed (composer fetches the package — public repo, credential-free in every build context); asset paths vendor-relative with the surface-11 bundle-content gate run and passed (CSS carries the `logo-garden__*` rules, JS registers both Alpine components, swiper in the manifest); guard coverage follows the code out of the repo via the shared `extractedPluginPackageDirs()` helper (six scanning guards + `PluginPackageGuardTest`'s new tagged-VCS pinning assertions incl. the no-version-field pin); activation proven location-independent (the 384 `PLUGINS_DISABLED=logo-garden` generalization test passes with assertions unchanged). In-image proof clean (package arrives from GitHub inside `docker build`, autoloads via the optimized `--no-dev` autoloader, `plugins/LogoGarden` absent). **`docs/plugin-extraction-runbook.md` written from the run** — the generic numbered procedure with LogoGarden as the worked example, incl. the visibility/auth decision point, the test-membership decision point (tests stay core this round — owner ruling, 384 close), and the Events-specific follow-ups (fixture migration paths, cross-plugin asset input, thumbnail regeneration). Contract doc **v0.9.0** (External repo section; surface 11 vendor-path note). Fast Pest 3249/0; route parity 272/272. See the 385 log.
- **P10 (386) ✅ — Distribution Manifest & Central Build.** Done: `distribution.json` at the repo root is the plugin-set + ordering authority — per-entry provider FQCN, source kind modeled honestly (`folder` for unpackaged Payments / `path` for in-repo Events / `vcs` for extracted LogoGarden), composer package + bound constraint where one exists; entry order is provider load order. `config/plugins.php` is now its **generated artifact** (`plugins:manifest-sync`; single reader `App\Plugins\DistributionManifest`; deterministic byte-stable output with a generated-file header; the runtime loader, activation filter, and every surface-1 guarantee untouched — same FQCNs, same order, remove-the-entry ≡ remove-the-line). `composer.json` and the Dockerfile stay hand-maintained, pinned by the standing guard `DistributionManifestGuardTest` (regeneration idempotence, verified to fail on a hand edit; per-source-kind lock pinning + repository/require alignment; no unmanifested plugin surfaces — lock packages, `plugins/` dirs, Dockerfile COPY lines, phpunit testsuites); `PluginPackageGuardTest`'s hand-maintained `EXTRACTED_PLUGIN_PACKAGES` const replaced by a manifest read. Composition-by-manifest proven by verified run (same one-image mechanics as 384/382): the image built from the committed manifest = the full composition; the image rebuilt after only a manifest edit (Events entry removed + regenerate) = the 384 events-disabled composition facts; restored after. The Stage E boundary recorded in the contract (a manifest edit governs what registers/migrates/syncs; per-distribution code absence = Stage E composer sets). `deploy.yml` untouched; contract doc **v0.10.0**; runbook gains the manifest step. See the 386 log.
- **P11** — the first niche distribution pack: manifest + config/seed/vocabulary pack. *(Repositioned at the 385 reframe: no longer an optional arc tail — it is the opening move of the follow-on arc, § Stage E below.)*

### Stage D — full decomposition (owner-committed, session 385 reframe)

**The reframe (owner ruling, session 385, 2026-08-19).** There is no launch. The project's finish line is the **end-state architecture**: thin core in this repo, every plugin a composer package in its own external repository, composed by the central build. The launch gates, Security Hardening S4/S5, and the Gate-1 demo-polish floater are **reframed out** (not parked-for-launch — removed from the roadmap's forward path; the findings register and track docs stand, and the freight resurrects only if a real client appears, at which point the project pivots to serving them). After Stage D, the follow-on arc (Stage E) is niche distributions: marketing sites + config packages per vertical, plugins added on demand.

**Settled decisions (owner, session 385):**

1. **Repo topology: individual repos, no umbrella.** One repo per plugin/pack under the established `crm-plugin--{kebab-handle}` naming convention. No git-submodule umbrella (a second pinning mechanism composer never reads, plus clone/CI friction), no plugins monorepo (shared tags or splitter tooling would surrender per-plugin version cadence). The composer vendor prefix + naming convention are the namespace; a GitHub org is the optional zero-mechanics grouping if ever wanted.
2. **Everything is a package and everything leaves the main repo** — grouped where grouping is natural. Plugin-hood cost is fixed overhead per package, not code size, so granularity is packs, not per-widget.
3. **Widget disposition:** vertical widgets travel with their verticals (the Events precedent — no separate cost). The generic remainder becomes **two packs**: `crm-plugin--content-widgets` (~13: Hero, TextBlock, Image, Logo, Carousel, VideoEmbed, MapEmbed, Table, ButtonRow, SocialSharing, BoardMembers, Memos, BarChart) and `crm-plugin--admin-widgets` (~4: QuickActions, RecentNotes, SetupChecklist, RandomDataGenerator — super-admin/dev-flavored, excludable as a unit). **Nav stays core** (site chrome; fails the litmus test — every install needs it). Pack membership judgment calls (BarChart's home, Memos' ProseMirror weight) finalize at carve time. A pack is activation-atomic (`PLUGINS_DISABLED` per plugin); per-widget picker curation is a config-pack concern (Stage E), and any widget that ever needs image-level exclusion graduates to its own plugin via the runbook then.
4. **Per-vertical blocks run carve → package → extract end-to-end** — each block finishes with the vertical in its own repo (the runbook is the per-block closer), rather than a separate extraction wave at the end.

**The ladder (positions D1+, session numbers assigned at session start; estimates from arc actuals — Events: carve 1 + package/schema 1):**

- **D1 (387) ✅ — Payments packaged + extracted.** Done in one session: the manifest source-kind walk `folder` → `path` (Stage B / P6 mechanics; the same-commit guard interlock held) → `vcs` (`github.com/abeuscher/crm-plugin--payments`, public, tag `v0.1.0`) via the runbook's second run — first foundation-plugin extraction (nothing outside the plugin edited beyond three test file-read re-points; the surface-13 seam held) and the first dependency-carrying package (`stripe/stripe-php ^16.6` package-owned, root's direct line dropped, lock resolution identical — the recorded pattern in contract § Package shape). Contract **0.11.0**; runbook deltas folded (dependency posture, no-assets gate wording, COPY-line ordering). See the 387 log.
- **D2 (388) ✅ — Events extracted.** Done in one session: the third runbook run, every known edge exercised — `github.com/abeuscher/crm-plugin--events` (public, tag `v0.1.0`), manifest `path` → `vcs`, first migration-owning extraction (fixture migrator reaches re-pointed to the vendor path, disabled-≠-uninstalled semantics unchanged), first testsuite-carrying extraction (the `Events` suite runs from `vendor/nonprofitcrm/events/tests`, same 29 files/236 tests in core CI — the § 6.3 disposition made real; per-plugin CI stays D11), gate 4 at scale (five asset-path rewrites, bundle content + the BlogPager cross-input proven), `plugins/` emptied until D3's carve (scoped guard relaxations recorded in the runbook; root PSR-4 stays until D12). Contract **0.12.0**. See the 388 log.
- **D3 (389–391) ✅ — Donations vertical.** The block ran carve → package → extract in three sessions. **Carve (389):** the behavioral surface into `plugins/Donations/` (checkout controller + route, observers, policies, mailables, the four admin surfaces, the importer contribution, both widgets, three listeners); surface 7 PROVEN (core `EmailTemplateRegistry`, the two donation handles its first contribution); surface 10's donation slice PROVEN via the **first cross-repo inversion** — `crm-plugin--payments` v0.2.0 (all three donation-reaching webhook branches → `CheckoutSettled` / new core `InvoiceSettled` / `InvoicePaymentFailed` dispatches) plus a rider `crm-plugin--events` v0.1.1 (listener metadata guard + a removal-mirror assertion generalized for sibling importers); surface 9 not forced (no contact-screen socket needed — honesty rule); contract 0.13.0. **Package (390):** `nonprofitcrm/donations` as the fourth path-repo package and the second squash-redraw consumer — the four donation tables left core's dump for plugin-owned byte-faithful migrations, both per-composition identity checks verified clean, the setup checklist's fund row gated on the funds admin route (the one composition-safety find); contract 0.14.0. **Extract (391):** the fourth runbook run to `github.com/abeuscher/crm-plugin--donations` (public, tag `v0.1.0`, manifest `path` → `vcs`, version field dropped) — second migration-owning + testsuite-carrying extraction, every edge D2-proven, no runbook deltas, zero new guard relaxations, `plugins/` empty again until D4's carve; contract 0.15.0. See the three session logs.
- **D4 (392–393) ✅ — Memberships vertical.** The block ran carve (392) → package + redraw + extract (393, one combined session — the D1 compression precedent). **Carve (392):** the behavioral surface into `plugins/Memberships/` (checkout controller + route, observer, policy, the three admin resources incl. the Contact-model MemberResource, both import pages, the importer contribution, MembershipStatus + PricingChart); surface 10's memberships slice via the **second cross-repo inversion** (`crm-plugin--payments` v0.3.0 — checkout branch → dispatch, renewal branch deleted since `InvoiceSettled` was already dispatch-always; two plugin listeners; no `InvoicePaymentFailed` listener — failures were never handled); contract 0.16.0. **Package + extract (393):** `nonprofitcrm/memberships` as the fifth path-repo package (P6 mechanics verbatim) and the **third squash-boundary redraw** — the two membership tables to plugin-owned byte-faithful migrations (smallest redraw yet: zero inbound core FKs, no sequences — the first with none; `memberships.contact_id` RESTRICT-as-behavior covered by the existing `CascadeDeleteTest` net), both identity checks verified clean, composition-safety gated on route presence (resolver arm, PortalSignup template, SignupController tier handling, and the check's one find — `DatabaseSeeder`'s unconditional `MembershipTierSeeder` call); then the fifth runbook run to `github.com/abeuscher/crm-plugin--memberships` (public, tag `v0.1.0`, manifest `path` → `vcs`, version field dropped) — third migration-owning + testsuite-carrying extraction, no runbook deltas, zero new guard relaxations, `plugins/` empty again until D5's carve; contract 0.17.0; routes 272/272 byte-identical; fast Pest 3349 → 3355/0. See the two session logs.
- **D5 (394–395) ✅ — Member Portal vertical.** The block ran carve (394) → package + redraw + hunt + extract (395, one combined session — the D4/393 compression precedent). Six portal widgets, the portal auth guard, portal routes. **Carve (394):** the behavioral surface into `plugins/MemberPortal/` (six widgets, six controllers + 17 routes byte-identical, the email-change mailable, the auth stack — guard/provider/broker config injection + the first plugin-owned `portal.auth` middleware alias); the **`member-portal` capability** (surface 13's second) gates core's five `auth('portal')` reads and the Events portal registration route (`crm-plugin--events` v0.1.2 rider — vanish, not 500, on portal-absent); verification + password-reset mailables stayed core as model-adjacent (their senders are `PortalAccount`'s framework hooks — logged drift); contract 0.18.0. **Package + redraw + hunt + extract (395):** `nonprofitcrm/member-portal` as the sixth path-repo package (P6 mechanics verbatim) and the **fourth squash-boundary redraw** — the two portal tables to plugin-owned byte-faithful migrations (the second sequence-carrying redraw: `portal_accounts_id_seq` travels complete, the donations mechanics; the `contact_id` SET-NULL FK travels as plain referential behavior, no net required; zero inbound core FKs verified), both identity checks verified clean (portal-absent: public + admin 200, every portal slug a clean catch-all 404, zero portal tables); composition-safety gated on the **capability** (the 394 signal): the ContactResource Portal Access section + the four EditContact portal actions (the render-time table reads), the page-topology seeder calls (`PortalPageSeeder` whole; `SystemPageSeeder`'s three auth pages only — the seeder also owns the site chrome, which stays every-composition; adapted from the survey's gate-both-calls), `DemoPortalMemberSeeder` self-skips (reachable portal-absent via the Events demo declaration); then the sixth runbook run to `github.com/abeuscher/crm-plugin--member-portal` (public, tag `v0.1.0`, manifest `path` → `vcs`, version field dropped) — fourth migration-owning + testsuite-carrying extraction (the 6-file `MemberPortal` suite from the vendor path, identical counts), no runbook deltas, zero new guard relaxations, `plugins/` empty again until D6's carve; contract 0.19.0; routes 272/272 byte-identical; fast Pest 3384 → 3391/0. See the two session logs.
- **D6 — Blog/Posts vertical** (~1–2).
- **D7 — Forms vertical** (~1). WebForm + submissions.
- **D8 — Products/store disposition + carve** (~1–2). *Open question (§ 6.8): where product/store code lands — own vertical, Payments-adjacent, or stays core until a store era. Resolve at prompt-draft time for this block.*
- **D9 — Integrations** (~1–2). QuickBooks, Mailchimp, Resend as plugins (the classic easiest family); Resend forces the email-transport seam if D3 hasn't already.
- **D10 — The two widget packs** (~2–3). content-widgets + admin-widgets carved, packaged, extracted; the volume cost is assets/thumbnails/demo-seeders at scale, not mechanics.
- **D11 — Test redistribution + per-plugin CI** (~1–2). Resolves the accepted debt from P9 decision 8: plugin-subject tests move to their plugins (or the central build formally runs distribution suites per § 6 disposition 3); per-plugin CI stood up or explicitly declined.
- **D12 — Endgame** (~1–2). Model/factory moves (the § 6.7 carry-forward), root `"Plugins\\"` PSR-4 retirement (all in-repo plugins gone), seed/config-pack surface (6) settled, core-slimming audit + a standing "core = engine" guard, contract to 1.0.0.

**Stage D total: ~15–21 sessions.** With P10 (~1–2) in front, **~16–23 sessions from here to the end state.** Order within D3–D10 is flexible; D1/D2 go first (cheap, and they exercise the runbook while it's fresh), D11/D12 close.

### Stage E — the follow-on arc (niche distributions; not yet planned in detail)

The direction after the end state (owner, 385): multiple marketing sites and config packages targeted at specific verticals — P11's layer promoted from optional tail to the whole next arc. A niche = a distribution repo (manifest + config/seed/vocabulary pack + demo content) composed by the central build; new plugins added on demand per niche. Planning waits until Stage D is far enough along to make it concrete.

### Feasibility verdict (draft)

- **Total to a demonstrable full-vision-in-miniature: ~9–15 sessions** (A+B+C-lite). Against a few-weeks owner budget at the historical pace, that is comfortably reachable with headroom for splits.
- The claim it supports: *"a real, 375-session production application, decomposed into a thin core and plugins with a published contract; features arrive as packages; one plugin lives in a separate repo and is composed in by a central build; a niche product is a manifest."* Every clause is demonstrable in code, not slideware.
- **Stopping points are honest:** stopping after A leaves a better-factored monolith with a real contract doc; after B, an app assembled from packages; after C-lite, the separate-repos story proven. No stage strands the codebase mid-migration — un-extracted code simply stays core until its turn.
- ~~Full Stage C (every plugin its own repo) and the niche business layer remain open-ended future work, deliberately unscheduled~~ **Superseded at the 385 reframe:** full decomposition is now the committed Stage D ladder above, and the niche layer is the Stage E follow-on arc.
- **Owner commitment (session 376): the arc through C-lite is the plan.** Extended at session 385: **the arc runs through Stage D to the end state.** Sessions carry labels P1–P11 / D1–D12; session numbers are assigned at session start per house rule.

### Execution posture (owner directive, session 376)

This arc runs faster and more autonomously than the fine-tuning era, because its success criteria are binary:

- **Binary scoping.** Every session's success criterion is objectively checkable: fast suite green, standing guards green, fresh-install identity clean, build clean, app boots. Extractions are behavior-preserving by definition, so the existing test and parity nets are the oracle. **No UAT step** unless a session genuinely changes a visual surface (most don't); the owner confirms at merge points, not mid-session.
- **Decisions are front-loaded into prompts.** Design details get resolved when the session prompt is drafted, not discovered mid-session. Mid-session, the default is decide-per-plan-and-log; pause-and-ask is reserved for contract-surface changes that are expensive to reverse. The goal: near-zero between-session open questions that amount to the owner saying yes.
- **Scope creep is structurally blocked.** Each session's out-of-scope list is binding. Adjacent work discovered mid-extraction goes to the arc backlog or the housekeeping inbox — never absorbed into the running session.
- **The contract doc is the arbiter.** Once `docs/plugin-contract.md` exists (P1), "does this follow the contract?" replaces most judgment calls.

---

## 8. Non-goals

- Plugins patching plugins (Principle 5).
- Community/third-party authorship tooling — later-stage line item only.
- Any FM-boundary change this session; Stage-C touches to `/api/health` or `deploy.yml` are future coordination items under the Two-Repo Coordination Protocol.
