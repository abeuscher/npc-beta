# Session 369 — Launch Replan — Three Gates, the Security Track, and the July Cut — Log

**Date:** 2026-07-17
**Branch:** session-369/1

---

## What was built

No application code — planning documents only, by design. The session rewrote the release plan around three launch gates and produced the security-hardening track, with the owner ratifying the full sort at the mid-session checkpoint before any file was rewritten.

### Phase 1 — Gates ratified + the sort

- Open Gate settled the goalposts: **Gate 1** (professional to paid traffic) ruled **essentially passed** — one floating session remains (demo tighten-up from designer feedback + the owner's final page/component review, combined; floats on outside feedback). **Gate 2** (safe for one real client's money + PII) and **Gate 3** (owner believes it's secure, internal-review bar) confirmed as proposed. Edge cases settled: the QuickBooks leg of the donation loop is deferrable; demo-server hardening lives in Gate 3.
- Full disposition of the 20 remaining items presented as a table at the hard checkpoint and ratified: **16 scheduled** (10 Gate-2, 5 security, 1 terminal), **7 deferred until first customer** (C3a page-action accountability, #32b concurrent-editing indicator, D1 scale rehearsal, the D2 compatibility remainder, G2 fixture extensions, D4 test-suite review, C4's QuickBooks leg), **nothing cut**, register unchanged plus a start-now urgency flag on Privacy/ToS (Stripe requires both URLs).
- One ordering question raised and resolved: the owner asked why security runs ahead of new work; the "earliest ads-on" half of the rationale was mooted by the owner's ruling that **nothing goes live until Gates 2 and 3 both pass**, but the "rehearse the final surface" half (hardening before rehearsals, Rule 9's logic applied at the front) stood, and the security-first order was kept.
- Drift note: the base prompt said the capture buffer carried the demo-role item; it had in fact already been folded to the inbox at the 368 close (buffer was header-only). No other drift — VERSION, contract version, and both input artifacts matched their described state.

### Phase 2 — The rewrite (five deliverables)

- **`sessions/release-plan.md` rewritten:** launch-replan preamble; the former positions 47–67 and the `── BETA 1 RELEASE ──` target replaced by the **§ Launch schedule** (16 positions + the floating Gate-1 session, budget math, FM statement); new working-set entries S1–S5 pointing at the track doc; `Deferred until first customer` + `Cut record` (empty, explicitly) sections; per-entry disposition notes on every re-gated item; Rule 8 marked dormant; the old post-Beta `── SECURITY AUDIT READY ──` milestone promoted into Gate 3 with `── SECURITY AUDIT PASSED ──` retained as a post-launch marker only. Stale markers swept: B1b + execution-position-20 + E16 checkmarks, the A001 "remains queued" correction, PMW2–PMW4 residue collapsed to a tombstone, the FieldMapper out-of-gate item marked absorbed (259).
- **`sessions/tracks/security-hardening.md` created:** charter + internal-review bar, the five session scopes (S1 perimeter/CSP/editor-self-hosting with the Stripe test-mode guard + demo-role seeding folds; S2 fleet-endpoint second lock — the track's one boundary-touching session; S3 XSS containment + exposure audit absorbing #32c without the C3a prereq; S4 public-write-surface adversarial pass absorbing F2 + the CAPTCHA inbox item; S5 permission-matrix pass absorbing F3 + demo hardening + the Gate-3 findings register), an absorption table so nothing double-schedules, the 368 surface map folded in verbatim as Appendix A, and the "genuinely well-built" list as Appendix B.
- **`sessions/session-outlines.md` updated:** the Beta One milestone bullet replaced by the Launch bullet; the Security Audit Ready bullet replaced by the Gate-3 track bullet; Security Hardening added to Active tracks; every re-sorted stub annotated with its new disposition (deferred / absorbed / gate-2 position); the stale A2 stub header corrected; "End of Roadmap — Beta 1" retitled.
- **Session 370 prompts drafted** (`370. base-prompt.md` + `370. Security S1 — Perimeter Headers, CSP & Editor Self-Hosting.md`) — owner reviewed and approved.
- **`sessions/launch-plan-summary.md`** — the one-page owner steering copy (gates, schedule, deferred list, budget/pace, the counsel dependency), written last from the ratified plan.

---

## Security considerations

The whole session is security-shaped: it promotes security from a post-Beta afterthought to launch-gating and turns the 368 surface map into five scheduled sessions. Nothing was fixed here by design — findings were scheduled, not patched. The standing posture change from 368 is now structural in the plan: internal enumerate-and-fix review is the release bar; the external audit marker is post-launch, owner-procured if ever.

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`. (No application change — planning files only.)

---

## Deferred decisions

- **CSP rollout shape + Stripe test-mode guard shape** — deliberately left to session 370's Open Gate (the code read informs the recommendation).
- **Floating Gate-1 session scheduling** — waits on designer feedback; slots wherever it lands.
- **Privacy/ToS** — external (counsel); flagged start-now in the register and the owner summary.
- **The deferred-until-customer list** — revisits when a real customer exists; content preserved in place.

---

## Test result

3045 passed, 0 failed. (Fast suite, parallel — no-op integrity pass; identical to the 368 baseline, no application code changed.)
