# Session 115 — Housekeeping & Consistency Audit — Log

**Date:** 2026-04-02
**Branch:** session-115/1 … /2

---

## What was built

### Phase 1 — Schema doc restructure

- Split monolithic `docs/schema.md` (46 tables) into 59 individual files under `docs/schema/`, one per table.
- Created `docs/schema/README.md` with alphabetical clickable index and migration squash note.
- Deleted original `docs/schema.md`.
- Updated references in `sessions/template-base-prompt.md` (read instruction + migration update rule) and `docs/app-reference.md`.

### Phase 2 — File inventory & orphan audit

- Produced full project file inventory at `sessions/115. File Inventory.md`.
- Deleted 3 confirmed orphans: empty placeholder directories `app/Services/Grants/`, `app/Services/Stripe/`, `app/Services/Mailchimp/`.
- Deleted 82 obsolete per-session base-prompt files (`sessions/034. base-prompt.md` through `sessions/115. base-prompt.md`). Only `sessions/template-base-prompt.md` remains.
- Flagged root-level business docs (`01-business-overview.md`, `02-technical-overview.md`, `03-whitepaper.md`, `demo-outline.md`) for user review — user removed them manually.

### Phase 3 — Admin field input audit

- Fixed 3x `RichEditor` → `QuillEditor` in `GeneralSettingsPage.php` (dashboard welcome, reset password page content, email verification page content).
- Fixed 1x `Checkbox` → `Toggle` for `all_day` in `EventResource.php`.
- Confirmed all other field types consistent: file uploads (SpatieMediaLibraryFileUpload for model media, FileUpload for settings/CSV), date pickers, colour pickers, selects (all contact selects searchable), toggle/checkbox usage.

### Additional — Services/Media restructure

- Moved `ChromeRenderer`, `InlineImageRenderer`, `SvgSanitizer`, `ImageSizeProfile` from `app/Services/` to `app/Services/Media/`.
- Updated namespace references in 11 files: 4 models, 2 controllers, 2 Blade views, 1 settings page, 1 service, 1 test.

### Additional — Force-delete/restore override rollback

- Removed redundant `canRestore()`/`canForceDelete()` static method overrides from 10 Filament resources.
- Removed manual `->visible()` permission overrides from `ForceDeleteAction` and `ForceDeleteBulkAction` across 15 files (10 resources + 5 relation managers).
- These overrides were displacing Filament's built-in soft-delete action visibility logic, which automatically combines policy checks with record trashed-state checks. The policies already had the correct rules.
- Preserved legitimate business logic: `PageResource` `->hidden()` for system pages, `CollectionResource` `->visible()` for system collections.

### Additional — Session 116 prompt

- Wrote session prompt and base prompt for session 116 (Code Review & Framework Alignment Audit).
- Updated session outlines with new title and description.

---

## Security checklist — verified

- [x] No functional changes to permission logic or data access — force-delete rollback restores framework-managed authorization
- [x] All existing tests pass (553 passed, 0 failed)
- [x] Schema files are complete — every table from the original `docs/schema.md` has its own file
- [x] No references to the old `docs/schema.md` path remain in live code

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`.

---

## Deferred decisions

- **ImportLog model** — schema doc labels it "legacy" but it is actively used by the current import workflow (ImportContactsJob, ImportContactsPage, ImportHistoryPage, ImportProgressPage). Cannot be removed until the importer is reworked.
- **Root-level business docs** — user removed manually during session.
- **CollectionResource vs ContentCollectionResource** — confirmed distinct purposes (schema editor vs content manager). Not redundant.

---

## Test result

553 passed, 0 failed.
