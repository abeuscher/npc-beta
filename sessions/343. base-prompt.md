# Base Prompt — Session 343 (Housekeeping — Media Hygiene + Widget/Tooling Polish)

**Start now.** Execute the reading list below, open the session prompt at `sessions/343. Housekeeping — Media Hygiene + Widget-Tooling Polish.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **343. Housekeeping — Media Hygiene + Widget/Tooling Polish.**

This is **not a `release-plan.md` entry** — it is a **housekeeping batch** (Rule 2) draining verified items from `sessions/housekeeping-inbox.md`, scheduled after session 342. It picks up the two stretch items deliberately held out of 342 (EventMiniCalendar internal margins; Random Data Generator → add Organizations) plus the design-independent items that fit alongside them. As with 342, the batch is scoped to items needing **zero outside input** and self-verifiable outcomes. Items are **pre-Public-Website-Complete** unless individually tagged `[post-milestone]`.

This session is **NOT boundary-touching** — no Fleet Manager / `/api/*` surface is in scope and no `CONTRACT_VERSION` change. **CRM contract stays at v2.3.0.**

**Session shape:** housekeeping batch. Drain ~3 pre-verified inbox items across two clusters — (1) **media hygiene** (the documented orphan-`cas/`-tree wipe gap → a `media:prune-orphans` command), and (2) **widget + admin-tool polish** (EventMiniCalendar internal-margin collapse fix; Random Data Generator gains Organizations). Each item is independently shippable; commit in cluster-sized changesets.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. Read `sessions/session-outlines.md` — the **active-tracks** block (to see what's gated/paused) and the **Housekeeping & Review — Beta 1 Scope** section. No Cross-Repo update is expected (non-boundary), but confirm.
3. **Read the inbox items this session executes** in `sessions/housekeeping-inbox.md` — each is listed by capture-stamp in the session prompt. **Re-verify each premise against current code before implementing** (the staleness guard); 342's close pre-verified these but re-confirm at the walk.
4. **Cluster 1 reading (media hygiene):** the orphan-sweep inbox item `[0.336.01]` in full; the existing media commands `app/Console/Commands/RelocateMediaToCasCommand.php` + `BackfillMediaHashesCommand.php` (mirror their shape — progress output, `--dry-run` if present); `app/Services/Media/MediaContentHasher.php` + `MediaRelocator.php` (how `content_hash` is computed + where files live: `storage/app/public/cas/{hash[0:2]}/{hash}/…`); the `Media` model / `media` table for `content_hash`; `config/backup.php` (the `cas/` tree is the backup source — the sweep must only touch genuine orphans); `bootstrap/app.php` `withSchedule()` (where `media-library:clean` is registered — decide schedule-vs-on-demand for the new command, noting the known scheduler-runner gap in `docs/app-reference.md`).
5. **Cluster 2 reading (widget + tooling):** `app/Widgets/EventMiniCalendar/styles.scss` (the fixed internal `margin`/`padding`/`gap` values that crowd as the widget caps at `max-width: 21rem` and centres — session 330 stopped it hiding, so it now renders narrow); `app/Services/RandomDataGenerator.php` + `app/Widgets/RandomDataGenerator/RandomDataGeneratorDefinition.php` + `template.blade.php` (the generate / count / clear entity lists — Organizations is absent; mirror the Contact/Product `source => Source::SCRUB_DATA` pattern, and respect the `OrganizationResource` form fields); `app/Models/Organization.php` (fillable + scrub-source policy — `ACCEPTED_SOURCES` already includes `SCRUB_DATA`).
6. Read `docs/app-reference.md` for container names + the media/backup sections (Cluster 1 is media-tree-adjacent; the "Orphan media sweep" + "Backups" sections are directly relevant).
7. Note any drift between this prompt and the actual code in a brief work-log entry; proceed unless something needs a decision per the drift / decision-threshold rules.

---

## Starting state inherited from session 342

- **CRM contract at v2.3.0** — unchanged. 342 was a non-boundary housekeeping batch (Debugbar removal, `.env.example` audit, branded error pages, page-builder safety/copy, two test-backfill Pests). This batch is also non-boundary.
- **Schema baseline:** no change since 328. 342 added none. **This batch expects no schema change** — it is a console command + widget CSS + admin-tool service work; no migration. Flag immediately if any item turns out to need one.
- **Fast Pest suite green at the 342 close baseline** (2794 passed, 0 failed). **New Pest expected: yes** — the `media:prune-orphans` command (orphan-detection + only-orphans-deleted is deterministic and Pest-testable), plus a Random Data Generator Organizations case where behaviour changes. The EventMiniCalendar margin fix is pure CSS — no test, verify visually.
- **Playwright:** baseline preserved; **no new specs this session.** The inbox's two e2e guard items stay out of scope (e2e-authoring pass, not this batch).
- **Front-end build:** Cluster 2 edits `app/Widgets/EventMiniCalendar/styles.scss` (widget SCSS) → **`docker compose exec app php artisan build:public`** is required before announcing completion (rebuilds the public widget bundle + manifest). The Random Data Generator change is a PHP service/Definition (no build). The media command is PHP/console (no build).
- **Planning state:** 342 drained 9 items from the inbox (folded out under "Recently dispositioned"); the two stretch items it held are this batch's carry-ins. No release-plan or outlines structural changes since.
- **Housekeeping inbox:** this session drains the specific items enumerated in the session prompt. All other inbox items (design-gated, post-milestone, or the e2e guards) stay for future batches.

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md`). Session-specific notes:

- **Verify each item's premise at the walk before touching it.** 342's close pre-verified these three; re-confirm against current code (already fixed? superseded? references gone?). Drop or re-scope dead items as found, and note the disposition.
- **The orphan sweep deletes files — prove it only touches orphans.** The command must delete only `cas/<hash>/` directories whose `content_hash` is referenced by **no live `media` row**. Build the live-hash set from the DB, diff against the tree, and have the test assert that a live file is never deleted while a planted orphan is. A `--dry-run` mode (report-only) is the safe default for a destructive sweep; mirror the existing media commands' shape. Do **not** wire it into any deploy/demo path (it is local-only growth; the demo droplet restores, never seeds).
- **`build:public` after the EventMiniCalendar SCSS edit.** Widget SCSS changes only reach the public bundle through `build:public` (it regenerates `public/build/widgets/manifest.json`). Run it once the SCSS lands; confirm the widget still renders square-celled and centred at narrow widths (the 330 anti-morph guarantee) after the spacing fix.
- **Random Data Generator parity, not new plumbing.** Add Organizations by mirroring the existing entity arms (generate / count / clear) with `source => Source::SCRUB_DATA`, so `demo:reset` / scrub-clear continue to round-trip. Keep the generated org fields realistic (name/type/industry/city) but do not invent new model surface.
- **Commit in cluster-sized changesets** — each cluster is independently deployable; one or more commits per branch is fine.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation each listed item still holds against current code (flag any that went stale); (2) the proposed cluster ordering and whether any of the optional stretch candidates (widget-thumbnail blank-render audit; toolbar linkpicker email→`mailto:`) are in or out. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 + Phase 2. Session-specific details:

- **Log file:** `sessions/343. Housekeeping — Media Hygiene + Widget-Tooling Polish — Log.md`. Record each item drained, its disposition (shipped / dropped-as-stale / deferred), and any item that turned out invalid at the walk.
- **Bump `VERSION`** to `0.343.<iteration>`.
- **Update** `sessions/completed-sessions.md` + `sessions/session-outlines.md`; **fold every drained item out of `sessions/housekeeping-inbox.md`** (record the drain under "Recently dispositioned").
- **Archive the previous session's files** — move `sessions/342. *.md` → `sessions/archived/`.
- **Next session:** owner confirms at close (another housekeeping batch is the fallback; non-roadmap pressure — designer feedback, external input — otherwise).
- **Commit on `session-343/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: the win is **a small clutch of verified, design-independent fixes shipped cleanly** — the documented media-orphan gap closed with a safe, dry-run-capable command; the mini-calendar breathing at narrow widths; the data generator reaching Organizations. Resist scope creep into the design-gated items (slide/popout repeater editor, Logo-widget rich-text, link-color consolidation, image-editing toolbar, BoardMembers→people-list, full-size text editing, alignment-preset defaults) and into the e2e guards. Commit messages name the deliverable ("343/N — {cluster}: {scope}").
