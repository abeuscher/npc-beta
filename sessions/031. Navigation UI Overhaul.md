# 031. Navigation UI Overhaul

Replace the current navigation items interface with per-item Edit and Delete buttons, drag-to-reorder (removing the sort order number field), and a redirect to the list after saving. Clean, opinionated UX for menu management.
