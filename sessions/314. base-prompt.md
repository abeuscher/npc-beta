# Base Prompt — Session 314 (Housekeeping Batch 2 — Batch 2A)

**Start now.** Execute the reading list below, open the session prompt at `sessions/314. Housekeeping Batch 2 — Batch 2A.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **314. Housekeeping Batch 2 — Batch 2A.**

This is **execution-order position 42** in `sessions/release-plan.md` — § E12 Housekeeping Batch 2, the first of two expected batches. Sequenced inside the Public Website Complete milestone block (Rule 12). Inbox size at session 313 close pushed E12 over the single-session threshold; this prompt covers the first batch and the close of 314 drafts a Batch 2B prompt for whatever doesn't land here.

This session is **NOT boundary-touching** — no Fleet Manager surface in scope. CRM contract stays at v2.3.0. No schema changes expected.

**Session shape:** housekeeping. The session walks `sessions/housekeeping-inbox.md` with the user at session start, picks the subset that fits one batch (skip items tagged `[post-milestone]`), and applies iteration-sliced. Six-iteration ceiling per the established discipline.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). They are the canonical format reference — do not infer format from previous session logs.
2. Read `sessions/session-outlines.md` for the active-tracks block and the housekeeping-inbox pointer.
3. Read the `sessions/release-plan.md` entry at execution-order position 42 (§ E12) — canonical for scope.
4. **Read `sessions/housekeeping-inbox.md` end-to-end** — this is the canonical input for the batch. Walk it with the user at session start to pick the Batch 2A subset.
5. Read `docs/app-reference.md` for environment + view-to-file mappings if any inbox item lands in unfamiliar territory.
6. Open `sessions/314. Housekeeping Batch 2 — Batch 2A.md` and read the session prompt.
7. Note any drift between the session prompt and the actual code in a brief work-log entry.

---

## Starting state inherited from session 313

- **CRM contract at v2.3.0** — unchanged by 313 (refactor-only).
- **Schema baseline:** no schema changes since session 308; 313 added none.
- **Fast Pest at 313 close:** 2586 passed (baseline carried unchanged through both iterations).
- **Playwright suite** running in CI on every push against the isolated stack. Local Playwright now defaults to the isolated stack via the renamed `npm run test:e2e` (s313 close); `test:e2e:local` is the explicit escape hatch for runs against the dev DB.
- **Planning state:** s313 close lifted three new own-session promotions into the milestone block (E18 Record-duplicate / E19 Accessibility / E20 Media-finder) and added a post-Beta backlog section near the top of `sessions/session-outlines.md`. The Code Shape & Fit track Cycle 1 closed at 313; next refactor cycle ≈ session 379.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — fresh items added at s313 close (Organizations importer helper, logo reset, backup-download cleanup, toolbar `title` attributes, ellipsis-menu port, columns alignment labels). Walk the full inbox at session start to pick the Batch 2A subset.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Walk the inbox first.** Open the inbox with the user, classify each item by size, and pick the Batch 2A subset that fits one session's iteration budget. Items the batch doesn't take stay in the inbox for Batch 2B.
- **Skip `[post-milestone]`-tagged items.** Those are deferred past the Public Website Complete milestone by inbox convention.
- **One iteration per item by default.** Bundle only when two items share a file or touch the same surface; otherwise keep iterations independent so any single one can be reverted without unwinding the others.
- **Six-iteration ceiling** — past 6 iterations the next iteration is the carve-out signal. Stop and let the leftover items move to Batch 2B.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **Questions are not instructions** per `feedback_questions_are_not_instructions`.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list above and the Starting state are read, surface a one-paragraph orientation — which items the proposed Batch 2A absorbs, in what order, and what (if anything) needs clarification — and wait for the user to confirm before starting. Do not pipeline straight from reading into implementation.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents. The next session is either Batch 2B (if items spilled over) or session 315 § E18 Record-duplicate affordance (if Batch 2A absorbed the inbox subset cleanly):

- Copy `sessions/template-base-prompt.md` to `sessions/315. base-prompt.md` (or `sessions/315. base-prompt.md` titled "Housekeeping Batch 2B" if a spillover is needed — push the E18 prompt to 316).
- Draft the matching `sessions/315. Session Title.md` from `sessions/template-session-prompt.md`.

Then stop. The user reviews the work and the next session's drafts, and initiates close when ready.

### Phase 2 — Close

When the user says to close (after the next session's prompts are agreed):

- Write `sessions/314. Housekeeping Batch 2 — Batch 2A — Log.md` from `sessions/template-session-log.md`.
- Update `sessions/completed-sessions.md` with the 314 row.
- Update `sessions/housekeeping-inbox.md` — remove items shipped this session; the items spilled into Batch 2B (if any) stay in the inbox until that session ships them.
- Update `sessions/session-outlines.md` and `sessions/release-plan.md` if this session resolves any forward stubs.
- **Archive `sessions/313. *` into `sessions/archived/`** per the archive rule.
- Commit on `session-314/N`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages name the inbox item being closed; no bundling across unrelated items in the same iteration; no docstrings/comments on code you did not write.
