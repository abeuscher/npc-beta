# Base Prompt — Session 322 (Header / Footer Defaults Overhaul — E16)

**Start now.** Execute the reading list below, open the session prompt at `sessions/322. Header-Footer Defaults Overhaul.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **322. Header / Footer Defaults Overhaul.**

This is **execution-order position 47** in `sessions/release-plan.md` — **E16. Header / footer defaults overhaul**, a pre-Public-Website-Complete milestone item (Rule 12; promoted from the housekeeping inbox at the session-282 audit). It sits just below E15 (Table widget) and just above the `── PUBLIC WEBSITE COMPLETE ──` divider. It pairs naturally with the demo-site work being designed in the cloud (A006): the chrome defaults this session sets are what the demo install — and every fresh install — renders.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at **v2.3.0**. No schema change is expected (site chrome is template + page-builder configuration, not new tables).

**Session shape:** feature / appearance-defaults work. Two concrete changes plus a review: (1) the default header chrome stops being full-width and becomes reasonable centered chrome; (2) the footer Nav gains a vertically-stacked layout option, and the seeded default footer ships with it; (3) a bounded review of site chrome defaults so a fresh install looks production-ready out of the box. Reconcile with — don't undo — the footer redesign the Public Marketing Website track already landed (session 284: centered nav + copyright, `#d4d4f2` band, the `public.blade.php` chrome-wrapper fix).

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. Read `sessions/session-outlines.md` — the active-tracks block and the Public Website Complete milestone framing. Note that A006 (demo-site page design) is in flight in the cloud and consumes this session's chrome defaults; don't design around the current defaults in a way that conflicts.
3. Read the `sessions/release-plan.md` E16 entry (≈ line 428) — it is canonical for scope, success criterion, and artifact. The session prompt is a delta against it.
4. **Read the chrome system before any edit** (the session prompt names the open questions these resolve):
   - `app/Services/Media/ChromeRenderer.php` — how header/footer chrome is rendered (and the duplicate header/footer `renderLayoutBlock` that session 294 deliberately left non-collapsing, tied to this entry).
   - The seeded chrome pages: templates reference chrome via `header_page_id` / `footer_page_id` pointing at the special `_header` / `_footer` page-builder pages (`database/seeders/TemplateSeeder.php`, and wherever the `_header` / `_footer` pages themselves are seeded — `database/seeders/BasePageSeeder.php`). The "default footer includes the stacking nav out of the box" deliverable lives in that seeded `_footer` page.
   - `app/Widgets/Nav/NavDefinition.php` + the Nav widget template/styles — the Nav widget's `layout` config is the likely home for the stacked-vs-horizontal option.
   - `resources/views/components/site-header.blade.php` + `site-footer.blade.php` — the fallback chrome partials.
   - `app/Filament/Resources/TemplateResource/Pages/EditPageTemplateChrome.php` — the operator-facing chrome editor (gated by `edit_site_chrome`); any new footer-nav option must surface here too if it's operator-configurable.
5. Read `docs/app-reference.md` for the chrome/template references, and skim the session-284 PMW log for what the footer redesign already changed (so this session sets defaults consistent with it).
6. Note any drift between this prompt and the actual code in a brief work-log entry.

---

## Starting state inherited from session 321

- **CRM contract at v2.3.0** — unchanged. Session 321 was non-boundary (demo-node lockdown + reset, CRM-side only).
- **Schema baseline:** no schema change at 321; none expected at 322 (chrome is config/templates, not tables).
- **Fast Pest green at 2677 / 0** (321 close: 2667 + 10 demo-node tests — the role-lockdown guard and `demo:reset` baseline). New tests expected this session for the Nav stacked-layout option and the seeded chrome defaults; pure-CSS portions may not warrant tests.
- **Playwright suite** — baseline preserved. Chrome is a visual surface; a small standing spec asserting the header renders centered (not full-width) and the footer nav renders stacked is a reasonable durable guard — decide at the open gate. Otherwise Playwright stays the CI signal, not a local close-gate.
- **Planning state:** A006 (Demo Site Page Design) is running in the cloud; it designs a demo public site that consumes this session's chrome defaults. `demo:reset` + the `demo` role shipped at 321. No release-plan re-sequencing since 321.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this is not a housekeeping batch session; do not absorb inbox items.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific notes:

- **Reconcile with session 284, don't regress it.** The PMW track already redesigned the footer (centered nav + copyright, `#d4d4f2` band) and fixed the `public.blade.php` chrome wrapper. That work was applied to the marketing site's content; E16's job is to make the sensible version the **seeded fresh-install default**, not to re-litigate the redesign. Read the 284 log before touching the footer.
- **Front-end builds:** chrome touches SCSS/Blade and possibly the Nav widget. Per the build decision rule — `resources/scss/**` or `resources/js/**` → `npm run build`; a change to the Nav widget's asset (`app/Widgets/Nav/**` styles, `widget_types` assets) → `docker compose exec app php artisan build:public`. Run the right one before announcing completion.
- **Keep the chrome-defaults review bounded.** The success criterion is header width + footer stacking nav + obvious production-readiness. Don't expand into a full chrome/theme redesign, the Design System Editor "buttons first" pass, or E17 (borders) — those are separate.
- **Verify objective outcomes yourself** (rendered DOM/CSS via Playwright or a render test); **pull the user in for the visual judgment** (does the centered chrome look right). `feedback_questions_are_not_instructions` and `feedback_pm_level_tone` apply.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the user to confirm before starting**. Cover the decisions worth settling once: (1) **footer stacking nav** — extend the Nav widget's `layout` config with a stacked/vertical option vs a separate footer-nav variant (lean: extend `layout`); (2) **header "centered" width** — which max-width/container token to standardize on (reuse the marketing-site content width rather than inventing one); (3) **how the seeded default ships the stacked footer nav** — edit the seeded `_footer` page-builder page vs the fallback partial vs both; (4) **standing visual guard** — whether to land a small Playwright spec for centered-header / stacked-footer render. Bring a recommendation for each. One confirmation, then go.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline. Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 (attenuate + prepare next session) and Phase 2 (close). Session-specific details:

- **Log file:** `sessions/322. Header-Footer Defaults Overhaul — Log.md`. Capture the header-width change, the footer stacking-nav option + how the default ships it, the bounded chrome-defaults review outcome, and reconciliation with the session-284 footer redesign.
- **Bump `VERSION`** to `0.322.<iteration>`.
- **Update** `sessions/completed-sessions.md`, `sessions/session-outlines.md` (E16 resolution), and `sessions/release-plan.md` (mark E16 closed).
- **Archive the previous session's files** (move `sessions/321. *.md` → `sessions/archived/`), and sweep any completed `A###` agentic set if A006 has landed by then.
- **Next session:** the natural next entry is E15 (Table widget — needs its prompt rewritten to the revised rich-text Insert-Table success criterion) or the A006-produced demo-site build session once that returns. Confirm with the user at the close walkthrough.
- Commit on `session-322/N`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages name the surface touched (header chrome, footer nav, chrome defaults); no docstrings/comments on code you did not write; mirror the existing chrome-template / Nav-widget / seeder conventions rather than designing fresh abstractions.
