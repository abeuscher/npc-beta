# Base Prompt — Session 275

We are about to begin a new session: **275. Rich-Text Surface Sanitization Hardening**.

This session is the **carved-out successor to Flag W4c/A** from the 271+272 audit. The 250-time stub for "Rich-Text Surface Sanitization Hardening" was confirmed still applicable at 272's deep walks; 273-close ratified the carve-out shape (mirroring how 206-close carved out 207); 274's squash absorbed any structural additions from 273's apply work. 275 lands after 274's squash so any new migrations introduced for the Trix→Quill data-migration step start from a clean baseline.

This session is **NOT boundary-touching** — no Fleet Manager contract change. CRM contract stays at v2.3.0 (per session 268). The 273 W8 #3 trait + parity test continues to gate code-vs-spec drift this session as it does post-274.

The 207 session is the canonical procedural precedent for a carve-out's shape — single focused architectural concern, scoped tightly, ships a coherent slice. Read 207's log alongside this prompt's session prompt for the iteration-pattern reference.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference; do not infer format from previous logs.
2. Read `sessions/session-outlines.md` § "Rich-Text Surface Sanitization Hardening" — the standing 250-time stub captures the canonical scope. 275 implements that stub.
3. Read `sessions/release-plan.md` execution-order list — confirm 275's positioning between item #26 (the 4-session housekeeping cluster) and item #27 (C1 — Notes Permissions feature half).
4. Read `sessions/archived/272. Code Review & Cleanup (Audit Part 2 — Subsystem Deep Walks) — Log.md` § W4c — the rich-text surface inventory. **This is the canonical input.** Names every model boundary that stores rich-text HTML and the editor each surfaces with (Quill vs Trix).
5. Read `sessions/archived/273. Code Review & Cleanup (Apply) — Log.md` end-to-end — the apply-half context plus the carve-out reasoning.
6. Read `sessions/archived/274. Migration Squash & Code Optimization — Log.md` end-to-end — confirms the squash baseline that 275 builds on. If any optimization-phase pick at 274 affected rich-text surfaces (unlikely but possible), 275's scope adjusts accordingly.
7. Read the squash-precedent shape for any new migrations 275 introduces:
   - `sessions/archived/207. Column Layout Inspector — Appearance Unification — Log.md` — the canonical carve-out precedent (a single-concern session emerging from an audit's apply half). Note how 207 introduced one new migration (`appearance_config` jsonb on `page_layouts`) that was then absorbed by 208's squash. 275 has the same shape: any new migration absorbs into the next squash (T1 terminal session).
8. Read the rich-text editor surface to ground the implementation:
   - `app/Forms/Components/QuillEditor.php` — the canonical Quill component used everywhere except Memos.
   - `app/Models/Collection.php` (specifically the `getFormSchema()` method around line 126) — the Trix surface that 275 converges to Quill.
   - `app/Services/ImportExport/ContentImporter.php` (`sanitizeWidgetConfig` method around line 419) — the import boundary that 275 extends with HTML sanitization.
   - `app/Models/CustomFieldDef.php` (around line 67) — where `rich_text`-typed custom fields render Quill.
9. Read `docs/app-reference.md` for environment names, container names, and view-to-file mappings. Read `docs/schema/README.md` for the table index — focus on `collections`, `notes`, `events`, `email_templates`, `site_settings`, `custom_field_defs`, `page_widgets`, and any model with a `custom_fields` jsonb column.
10. Open `sessions/275. Rich-Text Surface Sanitization Hardening.md` and read the session prompt carefully.
11. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from 274

- **CRM contract at v2.3.0** (per session 268) — unchanged.
- **Schema baseline:** session-274 squash. All migrations from 209 → 274 collapsed into the dump; 275 starts from a clean baseline.
- **Fast test suite green** at the **274 close baseline** (confirm at session start; expected ≈2169 ± any optimization-phase deltas from 274).
- **Slow test suite green** at **16 passed** (carried).
- **Playwright suite** at **42 / 0** post-274.
- **All 21 admin Resources gated with `canAccess()`** (W6/A closed at 271).
- **FM contract version-stamp parity** gated by `tests/Feature/Infrastructure/FmContractVersionParityTest.php` (273/4); the test continues to fire at every commit.
- **Flag W4c/A — Rich-Text Surface Sanitization Hardening** — pending; this is the session that resolves it.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **Pre-beta license applies.** No backward-compat shims. The Memos Trix→Quill data migration runs once and is absorbed by the next squash; the old Trix-shape disappears from history. Same posture as the 273 / 274 cluster.
- **No agent contract change.** No `docs/fleet-manager-agent-contract.md` body edit, no version bump.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first.
- **Update `docs/schema/`** after writing any new migration — particularly the `collections.md` doc if the Memos migration affects schema (the data-migration step is row-level, not schema-level, so likely no schema doc edit).
- **Tests for new behaviour.** This session is heavily test-coverage-driven — the security posture is "every rich-text surface is allow-list-sanitised." The allow-list test suite is load-bearing and must cover every Quill default-toolbar shape round-tripping clean PLUS every disallowed tag/attribute being stripped PLUS representative XSS payloads being neutralised.
- **When implementation is complete on each iteration**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: **274 close baseline + the new sanitiser tests** (count delta logged in the close).
- **Front-end builds.** `npm run build` if the Memos editor swap affects `resources/js/**` (Filament's RichEditor is server-rendered Trix; QuillEditor is also server-rendered but the Quill JS bundle is loaded via Filament's Alpine `x-load`). Check empirically — likely no new Vite work needed.
- **Playwright run** required on the iteration that ships the Memos editor swap (verifies the Trix→Quill UI transition + the data-migration round-trip). Plus any iteration that touches an importer's rich-text-typed custom-field path (covers the import-boundary sanitisation work).
- **Pest + Playwright must NOT run in parallel** per the recovery-ladder discipline established at 273/5. Run sequentially.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

Standard close gate per `sessions/template-base-prompt.md`. Session-specific notes:

- **Session log:** write `sessions/275. Rich-Text Surface Sanitization Hardening — Log.md`. Document per-iteration what was built, the full surface-by-surface sanitisation coverage table (every model boundary + the apply site + the test that gates it), the Memos data-migration row count, and the allow-list test suite's coverage matrix (Quill default-toolbar shapes × disallowed tags × XSS payloads).
- **Update `sessions/completed-sessions.md`:** append session 275's row.
- **Update `sessions/session-outlines.md`** § "Rich-Text Surface Sanitization Hardening" — mark the stub as ✅ closed (session 275); preserve any forward references to follow-on work that didn't fit in this session.
- **Update `sessions/release-plan.md`:** mark the 275 execution-order entry as ✅ closed.
- **No `docs/fleet-manager-agent-contract.md` updates** — non-boundary-touching.
- **Help-doc updates** — none expected.
- **Archive the previous session:** move all files matching `sessions/274. *.md` into `sessions/archived/`.
- **Commit:** stage all changed files (close-gate doc bundle + any iteration commits not already merged) on the final `session-275/N` branch. Notify the user. Do not push.
- **No FM-side handoff** — non-boundary-touching.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

The style rules in `sessions/template-base-prompt.md` apply. Session-specific:

- **Iteration commit messages describe the surface or seam delivered** — e.g. "Iteration /1 — `App\Support\HtmlSanitizer` utility + allow-list test suite" or "Iteration /3 — Memos Trix→Quill convergence + data migration".
- **Each model-boundary apply site lands as part of an iteration with its companion test** — sanitiser application without a regression-guard test is incomplete; the test surface is the security gate.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Simple, correct, well-considered solutions over fast or clever ones.**
- **Security posture is the defining constraint.** When the spec stub and a usability concern conflict, choose security per the standing `feedback_security_over_usability.md` memory rule. Move friction to the UX layer (e.g. a clear "this content was sanitised on save; refresh to see the canonical render" flash message), not to the protection layer.
