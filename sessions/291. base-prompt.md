# Base Prompt — Session 291

**Start now.** Execute the reading list below, open the session prompt at `sessions/291. Public Marketing Website — Form Submission Notifications.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **291. Public Marketing Website — Form Submission Notifications**.

This is a **lifted-gap session** resolving **G23** from `sessions/public website/gap-report.md`, surfaced during session 290's Contact page rebuild. It runs **outside the Public Marketing Website track's phase count** — same posture as session 285 (CMS Fixes) and session 288 (Pricing Chart Widget): the track surfaces a forcing function, the user approves the lift, a standalone session resolves it. In the Public Website Complete milestone execution order it slots **before the Demo page build** — the Demo page's submit/conversion flow is built on top of the notification surface this session ships, so it must land first. Canonical scope lives in `sessions/scoping-form-notifications.md` (authored at 290 mid-session, user-reviewed). After 291: Demo Page Build (now session 292), then the page-capture harness + track close (now session 293) — both shifted down one by this lift.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. Form-submission notifications are CRM-internal: a `Form.settings` JSON extension, one Mailable, one observer, one SiteSetting key, one Filament UI extension. Mail *delivery* rides the existing `config/mail.php` mailer (infra, not the Fleet Manager agent contract). CRM contract stays at **v2.3.0**.

**Session shape:** feature-style. Wire form submissions to dispatch configurable site-owner email notifications, leveraging the existing mail infrastructure — a declarative `Form.settings.notifications` surface plus the dispatch path, then populate the seeded `contact-page` form so the Contact page (290's first consumer) actually notifies on submit.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. **Read `sessions/scoping-form-notifications.md` in full — it is canonical for this session's scope, leverage inventory, proposed shape, and the five decisions to resolve at session start.** This session has no `release-plan.md` entry yet (the lift is recorded at 290 close); the scoping doc plays the role the plan entry normally would.
4. Read `sessions/public website/gap-report.md` — the **G23** row (what was attempted / blocked / workaround / recommended action) and the G21 row's 290 status update (the form-label-on-dark path, adjacent but not in scope here).
5. Read `sessions/tracks/public-marketing-website.md` for the track status snapshot and the gap-resolution discipline (this session is the discipline in action).
6. Read the leverage surfaces before touching them: `app/Models/Form.php`, `app/Filament/Resources/FormResource.php`, `app/Http/Controllers/FormSubmissionController.php`, `app/Models/SiteSetting.php`, `config/mail.php`, one existing Mailable as the pattern reference (`app/Mail/PortalFormCollision.php` — it is the existing form-submission-adjacent mail), and `app/Filament/Pages/Settings/MailSettingsPage.php` (where the new SiteSetting key surfaces).
7. Read `database/seeders/FormSeeder.php` — the `contact-page` form (added at 290) is this session's first notification consumer.
8. Read `docs/app-reference.md` for environment/container names and `docs/schema/README.md` for the table index. **No migration is expected** — `Form.settings` and `form_submissions` already exist; the new SiteSetting key is a row, not a schema change.
9. Open `sessions/291. Public Marketing Website — Form Submission Notifications.md` and read the session prompt carefully.
10. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules.

---

## Starting state inherited from session 290

- **CRM contract at v2.3.0** — unchanged. Session 290 (Contact rebuild) touched no boundary surface. This session also touches none.
- **Schema baseline:** no schema changes since 280-close. Session 290 added no migration. This session adds none — `Form.settings` (JSON) and `form_submissions` already exist; the notification recipient default is a `SiteSetting` row.
- **Fast test suite green at 2421 / 0 sequential** (290 close baseline; 2416 + 4 `AppearanceStyleComposer` link-color cases + 1 `ContentImportExport` round-trip case). New tests **expected** this session — feature-style work (notification schema validation, observer dispatch with mocked mail, template render, multi-recipient correctness, recipient-token sanitisation).
- **Playwright suite** — baseline preserved; none added at 290. Playwright optional this session — the submission→mail path is objectively verifiable in Pest with `Mail::fake()`; a Playwright spec is only warranted if the FormResource notifications UI needs interaction coverage. Prefer Pest.
- **Planning state:** Session 290 expanded mid-session beyond content-only — it shipped the `text.link_color` appearance feature (composer `--np-link-color` + `.np-site a` var-indirection + inspector field + Quill `color`/`background` toolbar removal + `button.btn` carve-out so form submit buttons render), extended `FormSeeder` with the `contact-page` form, and surfaced G21 / G22 (resolved at 290) / G23 (this session). Demo Page Build and the page-capture harness each shifted down one (now 292 / 293) by this lift. The release-plan entry + execution-order renumber for this lifted-gap session land at 290 close (or are recorded as part of this session's planning updates if 290 is already closed when 291 starts).
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — feature-style sessions don't absorb housekeeping items unless a finding directly ties to the notification surface. The s290 "link-color mechanism consolidation" and "Contact hero readability overrides" items are out of scope here; carry forward. Touch only if a finding surfaces inside this session's scope.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Before implementing any integration, ask what existing system handles it.** *Session-291 note: this homework is already done — `sessions/scoping-form-notifications.md` is the leverage inventory. Mail goes through the existing `config/mail.php` mailer + the established `Mailable` + `Mail::to()->send()` pattern (nine existing Mailables as precedent). Do not build a new mail abstraction; extend the existing one. Do not configure a real SMTP/Resend transport — that is an env decision outside this session (the default `log` mailer is sufficient for in-session verification via `Mail::fake()` and log inspection).*
- **Surface architectural choices before going deep.** The five decisions in the scoping doc's "Decisions the user needs to make before lift" are the architectural surface. Recommended defaults are carried into the session prompt's "Open questions" section — confirm them at session start; only pause if the user's answer would change the data shape (decisions 1 and 2 are the load-bearing ones).
- **No migration expected.** If the work appears to need one, stop and surface — it would mean the design drifted from the JSON-on-`Form.settings` shape the scoping doc fixed.
- **Run the fast suite when implementation is complete:** `docker compose exec app php artisan test --exclude-group=slow`. Baseline 2421 / 0; expect a positive delta from the new notification tests.
- **Front-end builds:** the FormResource notifications UI is Filament (server-rendered PHP, no Vite build). If no `resources/js/**` or `resources/scss/**` is touched, no build. The Filament form-builder repeater extension does not need an asset build.
- **Verify objective outcomes yourself.** "Does a submission dispatch the configured notification(s) to the right recipient(s) with the right content" is objective — close it with `Mail::fake()` assertions and a log-mailer inspection of a real end-to-end submit, not a user round-trip. Pull the user in only for the judgment surface: whether the default email template reads acceptably and whether the FormResource notifications UI is operator-legible.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that form notifications are wired, the `contact-page` form notifies the site owner on submit, and surface the default-template wording + FormResource UI for the user's review. Then stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.** Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/292. base-prompt.md`) — copy `template-base-prompt.md`, set session number/title to **292. Public Marketing Website — Demo Page Build**. 292 is the shape-change session deferred from the original 291 slot: a short visual page (one band, two cells, one button per `sessions/public website/demo-page-spec.md`) plus the conversion-action wiring (single-button auto-login into the shared demo install, per-IP rate-limit, shared "Demo User" account, demo page hosted on the demo server). Reference project memory `[[demo-page-architecture]]` for the decisions locked at 289 close. The 292 prompt scopes both halves and notes the Demo submit flow now builds on **this** session's notification surface as its primitive (one decision in the scoping doc resolved by sequencing — notifications land first).
- **Next session prompt** — draft `sessions/292. Public Marketing Website — Demo Page Build.md` against both halves.

**Carry-forward reminder for the close handoff:** *Before the Public Website Complete milestone can close — (a) Privacy Policy + Terms of Use starter pages must land (Stripe Dashboard requires both URLs; pairs with the E16 header/footer overhaul); (b) real photography must replace every sample-image placeholder across all five pages (the FDR / Schliessmann / Del Hierro / Brun portraits are stand-ins, not launch assets). Neither is a 292 deliverable; both are milestone-close gates the user is tracking.*

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/291. Public Marketing Website — Form Submission Notifications — Log.md` using `template-session-log.md` as the format reference. Document: the five decisions as resolved, the final `Form.settings.notifications` shape, the dispatch mechanism (observer vs inline), the default template, the FormResource UI shape, the `contact-page` form's notification config, Pest delta, and any drift.
- **Add the release-plan entry:** insert a `§ 291` entry in `sessions/release-plan.md` for this lifted-gap session and renumber the execution-order list — Form Notifications at the pre-Demo slot, Demo Page Build → 292, Page-capture harness + close-out → 293. Mark 291 ✅ closed.
- **Update `sessions/public website/gap-report.md`:** mark **G23 ✅ resolved at 291** with the as-shipped resolution; cross-reference from the G23 row and the 290 shape-change note.
- **Update `sessions/scoping-form-notifications.md`:** flip its status header to "lifted and resolved at session 291" with a one-line as-built delta vs the proposed shape.
- **Update `sessions/completed-sessions.md`:** append session 291's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — note the G23 lift resolved at 291 and the Demo/​harness renumber.
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (last update date; G23 resolved via lifted-gap session 291; Demo queued at 292, harness/close at 293).
- **Archive the previous session:** move all `sessions/290. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-291/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Form.settings.notifications + FormSubmissionMailable + observer dispatch + contact-page wiring + Pest".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.** New code follows the surrounding conventions (existing Mailables, existing observers if any, FormResource's existing repeater patterns).
- **Extend, don't reinvent.** The mail send path mirrors the existing nine Mailables. The notifications config mirrors how `Form.settings` already carries `submit_label` / `success_message` / `honeypot` / `form_type`. The FormResource UI mirrors its existing field-builder repeater. New abstractions are a smell — surface before introducing one.
