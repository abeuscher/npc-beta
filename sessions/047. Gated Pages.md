# 047. Gated Pages

Restrict individual pages or path prefixes to logged-in members. Decide at session start between a per-page toggle and a reserved path prefix (e.g. `/members`). Depends on Member Portal (046).
