# 067. Installer

Browser-based setup wizard and `php artisan install` CLI for fresh installs. Toggleable content packs (blog, events, donations). Depends on deployment sessions being complete so installer assumptions can be validated against a real environment.
