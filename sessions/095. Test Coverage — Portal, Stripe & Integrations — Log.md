# Session 095 — Test Coverage — Portal, Stripe & Integrations — Log

**Date:** 2026-03-30
**Branch:** session-095

---

## What was built

### Phase 1 — Portal authentication

- 11 tests covering login (valid/invalid/inactive), signup (new contact + existing contact linking), password reset (token creation and consumption), route protection (redirect to login), and contact scoping (no cross-contact data leakage on address and password updates).

### Phase 2 — Donations & Stripe checkout

- 10 tests covering donation model creation (one-off and recurring types), amount validation ($1 min, $10,000 max), webhook signature rejection (invalid and missing), transaction record creation, donation status lifecycle, and fund association.

### Phase 3 — Products & purchases

- 9 tests covering capacity enforcement (blocked at capacity, active-only counting), purchase record creation, ProductPriceObserver registration (skips when no Stripe key), zero-amount pricing, and waitlist entry lifecycle (waiting → notified → converted, and cancellation).

### Phase 4 — Tax receipts

- 5 tests covering correct total calculation for a contact/year, multi-fund breakdown, re-send as new row (not overwrite), active-only donation filtering, and year isolation.

### Phase 5 — Mailing list filter resolution

- 13 tests covering equals, contains, is_empty, is_not_empty operators, AND/OR conjunction logic, raw_where SQL injection blocking (DROP, DELETE, UPDATE, comment markers), empty filter fallback returning all opt-in contacts, and exclusion of do_not_contact and non-opted-in contacts.

### Phase 6 — Import review workflow

- 6 tests covering staged attribute update application on approval, tag syncing on approval, rollback discarding staged changes without modifying contacts, rollback deleting newly imported contacts, and status transitions (pending → reviewing → approved, rolled_back).

### Phase 7 — Activity log

- 8 tests covering ContactObserver firing on create/update/delete, admin/portal/system actor type detection, custom event logging with description and meta payload, and silent-fail behaviour on exceptions.

---

## Security checklist — verified

- [x] Portal tests verify that queries are scoped to `contact_id` — no cross-contact data leakage.
- [x] Stripe webhook tests verify signature validation (invalid and missing signatures rejected with 400).
- [x] Mailing list tests verify `raw_where` is safe from SQL injection (DROP, DELETE, UPDATE, and comment markers all blocked).

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`.

---

## Deferred decisions

- Stripe checkout controller tests use model-layer verification rather than full HTTP integration (Stripe SDK cannot be mocked via overload when already autoloaded). Full integration tests would require a test-mode Stripe key or a wrapper service around StripeClient.

---

## Test result

294 passed, 0 failed.
