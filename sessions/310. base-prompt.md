# Base Prompt — Session 310 (Content Import/Export — Unified Import Site / Export Site UI)

> **Scheduled as session 310.** The rollup. Builds on session 309's opt-flag + analyzer infrastructure to add a single "Import Site" / "Export Site" pair of actions under Tools. Most calls in this session are architectural — backup-grade vs button-grade, diff preview yes/no, what counts as an "entity collision" — and are flagged for upfront resolution rather than apply-time discovery.

**Start now.** Execute the reading list below, open the session prompt at `sessions/310. Content Import-Export — Unified Import Site Export Site UI.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **310. Content Import/Export — Unified Import Site / Export Site UI.**

This is a **UI session with architectural calls bundled at the front.** The four calls (recommended answers in the session prompt) are: backup-grade vs button-grade (recommend button-grade v1), diff preview yes/no (recommend no), entity collision keys (slug/handle/id-map per entity), `format_version` policy (no bump, additive only). Resolve all four at session start in the log; do not relitigate at apply time.

This session is **NOT boundary-touching** — no Fleet Manager surface, no cross-instance migration, no backup-automation. **CRM contract stays at v2.3.0.** **No DB schema change.** Per-entity import/export forms stay untouched; this session is purely an additive rollup surface.

**Session shape:** small-ish UI + the `exportSite()` wrapper. Inherits the analyzer + opt-flag plumbing from session 309. If A001 (agentic entity coverage) has merged before this session runs, the rollup covers all entities; if not, the rollup covers whatever subset is shipped, with the others added later as A001 lands.

Before doing anything else:

1. Re-read session templates only if uncertain about a structural detail.
2. Read `sessions/session-outlines.md` and `sessions/release-plan.md` — confirm 310's position and whether A001 has landed yet.
3. **Read the full session prompt** at `sessions/310. Content Import-Export — Unified Import Site Export Site UI.md` — the four architectural calls + phases.
4. **Read session 309's close-log** — what 310 inherits: the opt-flag pattern, the analyzer's manifest shape, the two-step import UI pattern. 310 reuses all three.
5. Read `app/Services/ImportExport/ContentExporter.php` + `ContentImporter.php` post-309 — confirm the analyzer's current shape and which entities are covered.
6. If A001 has merged: read its log for any per-entity quirks (e.g., navigation's storage shape, collections' custom `data` JSON handling) that the rollup UI needs to surface in the manifest.
7. Skim `app/Filament/Pages/ImporterPage.php` + 309's two-step import page — UI shape to mirror.
8. Note drift in a brief work-log entry.

---

## Starting state inherited from session 309 (and possibly A001)

- **CRM contract at v2.3.0** — unchanged.
- **Schema baseline:** none added by 309 or A001.
- **Fast Pest at 309 close** — see `sessions/309. … — Log.md`. If A001 has merged, see its log for the four round-trip cases it added.
- **Playwright suite** running on every CI push. 309 added one import-UI spec. This session adds one more for the rollup UI.
- **`ContentImporter::analyze()` + opt flags** are the foundation; this session calls them, doesn't change them.
- **`ContentExporter::exportSite()`** is the only new exporter method this session adds — a wrapper over the per-entity exporters that 309 + A001 have established.
- **No widget changes.** No SCSS changes. No theme/typography touches.
- **Build/version discipline:** bump `VERSION` to `0.310.<iteration>` each iteration.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Resolve the four architectural calls at session start, in the log.** Don't relitigate at apply time. Specifically:
  1. Backup-grade vs button-grade — commit to button-grade v1.
  2. Diff preview — commit to no for v1.
  3. Entity collision keys — spell out per entity in the analyzer's manifest documentation.
  4. `format_version` — no bump.
- **Default-to-fix on findings** per `feedback_audit_findings_default_to_fix`.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **No multiple-choice questions for the four calls** — recommendations are in the session prompt; pick or override with prose, not a checkbox list.
- **Pest fast suite + Playwright run sequentially when complete.** Green, every count delta explained.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

310 is **arc-terminal for the user-driven content-import/export work**, but A001 may still be queued or in-flight. If A001 hasn't landed yet, the close-log should flag what's missing from the rollup and what minor follow-up will be needed once A001 merges (likely: extend the manifest summary copy to mention the new entities; the analyzer + import code paths handle them automatically).

### Phase 2 — Close

When the user says to close: write `sessions/310. Content Import-Export — Unified Import Site Export Site UI — Log.md`. Update `completed-sessions.md`. **Archive `sessions/309. *` into `sessions/archived/`.** Commit on the final `session-310/N` branch, notify, do not push. Never push to main / force-push / merge to main. **Update `release-plan.md` and `session-outlines.md`** — content-import/export arc closes here on the user-driven side; A001's agentic completion is a separate roll-up note.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages describe the deliverable; PM-level status tone; no docstrings/comments on code you did not write. **Extend, don't reinvent** — `exportSite()` is the only new exporter wrapper; the Filament page reuses session 309's two-step shape; the analyzer and opt-flag plumbing are reused as-is.
