# Base Prompt — Session 398 (Products Carved, Packaged & Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/398. Products Carved, Packaged & Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **398. Products Carved, Packaged & Extracted**.

This is **arc position D8** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical). D7 closed at 397 (`crm-plugin--forms` v0.1.0; contract 0.21.0; `plugins/` empty a sixth time; the second single-session carve → package → extract). This session opens — and aims to close — the Products block the same way, **with the fallback loud** (this block carries a cross-repo inversion, which alone filled session 392): carve (`plugins/Products/`: both checkout/waitlist controllers + routes, the ProductDisplay + ProductCarousel widgets, the Product admin resource + two relation managers, both attribute-wired observers, `ProductPolicy`, **the third cross-repo inversion** — the Payments webhook's product branch → `CheckoutSettled` dispatch, `crm-plugin--payments` v0.4.0) → package (`nonprofitcrm/products`, P6 mechanics) **+ the sixth squash-boundary redraw** (the four product tables leave the core dump — uuid PKs, no sequences; the RESTRICT purchase FKs travel as behavior) **+ the dead `allocations` table drop** (the 396 posts precedent) → the **ninth extraction-runbook run** to `crm-plugin--products`. The block's headline ratification: **the § 6.8 disposition** — resolved at prompt-draft as **own vertical** (the store passes the litmus test; Payments-adjacent would put a vertical inside a foundation plugin; core-forever contradicts the 385 reframe).

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.21.0 → 0.22.0 at close (Package shape + External repo ninth consumer; surface 5 sixth redraw consumer — the second no-sequences case, the third RESTRICT-as-behavior case, the allocations-drop note; surface 10 products slice — the third inversion with the branch-ordering note; changelog).

**Session shape:** carve + inversion + package/redraw + extraction, behavior-preserving on the full composition. Routes 272/272 byte-identical (moved-action FQCNs the 381 exception); no visual change; the schema events are boundary moves plus one dead-table drop (identity checks prove both). **Expect one iteration; the honest-stop fallback (after carve, or after carve + package) is a planned outcome, not a failure.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D7 ✅, D8 open) and § 6.8 (the disposition this prompt resolves — the plan-doc entry closes at this session's close) and § 6.7 (models stay core — all four product models stay, which is why both observer attributes must invert).
3. Read the 397 session log (`sessions/397. Forms Carved, Packaged & Extracted — Log.md`) — especially its **D8 kickoff input** section and the single-session pacing with the fallback.
4. Read `docs/plugin-contract.md` at 0.21.0 — § Package shape, § External repo, § The distribution manifest, surface 5 in full (the redraw mechanics; the no-sequences category; RESTRICT-as-behavior; the identity-check procedure; the seeder-gate note), surface 10 in full (the 389/392 inversion release pattern this session runs a third time — contract wording first, plugin release, tagged, lock bump + constraint widening same-commit with the listener; listener self-filter rules), surface 13 (the payments capability the moving checkout controller already consumes), surface 4 (front-of-house route ordering).
5. Read `docs/plugin-extraction-runbook.md` — the ninth run; the 393/395/396/397 worked examples carry the compression nuances (fixtures vendor-relative from birth; the in-plugin schema test's `__DIR__`-symlink nuance).
6. Read the 392 log (`sessions/archived/392. Memberships Carved — Log.md`) for the inversion-carrying carve playbook (the Payments v0.3.0 release mechanics — this session's v0.4.0 is the same walk), and the 396 log's dead-table drop (Phase A′ — the allocations drop follows it exactly).
7. Open `sessions/398. Products Carved, Packaged & Extracted.md` — the survey and front-loaded decisions; note the Open-Gate items (five ratifications with the § 6.8 disposition as the headline; the owner-created `crm-plugin--products` repo; sibling freshness with **one expected sibling edit** — `crm-plugin--payments` v0.4.0).
8. Skim the surfaces: `app/Http/Controllers/ProductCheckoutController.php` (the payments-capability seam) + `ProductWaitlistController.php`; `routes/web.php` ~42–48; `app/Widgets/ProductDisplay/` (+ its `DemoSeeder`) + `app/Widgets/ProductCarousel/` (the asset declarations); `app/Filament/Resources/ProductResource.php` + pages + both relation managers; `app/Models/{Product,Purchase}.php` (the two `#[ObservedBy]` attributes) + `app/Observers/{ProductObserver,PurchaseObserver}.php`; `app/Providers/AuthServiceProvider.php` (the one policy line); `vendor/nonprofitcrm/payments/Http/Controllers/StripeWebhookController.php` (`handleProductPurchase` ~94–200 + `findOrCreateContact` — the inversion's subject and its branch ordering ahead of the generic fallback); the four tables' DDL + the dead `allocations` DDL in `database/schema/pgsql-schema.sql` (~970–1300 + constraints/FKs); `app/Services/PageBuilderDataSources.php` (the products picker), `app/Filament/Resources/TransactionResource.php` ~270 (the product filter), `app/Services/ImportExport/ContentExporter.php` ~110 (the manifest pluck), `app/WidgetPrimitive/ContractResolver.php` ~100/111/519+ (the product arms), `database/seeders/DatabaseSeeder.php` ~233 (the products system-Collection row).
9. Note any drift between the session prompt's captured survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 397

- **CRM contract at v2.7.0** — 397 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D7 complete. **Eight extracted plugins** (`crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` **v0.3.0** — this session releases v0.4.0, `crm-plugin--events` v0.1.3, `crm-plugin--donations` v0.1.0, `crm-plugin--memberships` v0.1.0, `crm-plugin--member-portal` v0.1.1, `crm-plugin--blog` v0.1.0, `crm-plugin--forms` v0.1.0); **`plugins/` is empty** until this session's carve refills it. Plugin contract **v0.21.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** thirteen plugin-owned tables in their packages' migrations; the core dump is core-only (guard-enforced, owner map at 13). The four product tables + the dead `allocations` table sit in the core dump — their redraw/drop is this session's Phase B. Two live core migrations exist (the 396 posts drop, the two 397 forms rows are plugin-side).
- **Fast Pest suite green at 3441 / 0 parallel** (397 close: 3411 baseline + 30 additive). The `Forms` testsuite (8 files, 50 tests) runs from `vendor/nonprofitcrm/forms/tests`. Expected delta this session: **additive** — the Products twin/mirror/flag/boundary/schema/inversion set; existing product assertions unchanged beyond path/namespace re-points.
- **Route baseline: 272 routes** (dev/test), byte-identical to main at 397 — unchanged this session (the two product routes move but keep names/URIs/middleware).
- **Composition-safety state:** the member-portal capability gates every core portal reach (394/395 — do not touch); the forms reads gate on `Route::has('forms.submit')` (397 — do not touch); `ProductCheckoutController` already consumes the payments capability — the seam travels verbatim. **The shared pager stylesheet is core-owned** (ADR 0007) — not a products concern.
- **The twin fixtures this session DO carry migrator paths** (the plugin owns schema); they land **vendor-relative from birth** (the matured 393–397 compression nuance — zero re-points at any later step).
- **Playwright suite** — CI signal, non-gating; the parked chrome-parity spec remains the known failure. No local Playwright.
- **Housekeeping inbox** — no absorptions expected this shape. Standing items: the `admin_invitation` seeder/array half (restated at the 397 close), CSP-font, chrome-parity.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **The carve is the 389/392/394/396/397 playbook**, with the 392 inversion overlay: provider + capability-free registration, both routes registered at `boot()` ahead of the catch-all byte-identically, both widgets nested under `Widgets/`, the double observer inversion per decision 5 (attributes off the core models, `observe()` ×2 in plugin `boot()` — never a core-model reference to a plugin class), the policy via `Gate::policy()`, **the inversion per decision 3** (contract wording first; the webhook edit in the sibling checkout; tag v0.4.0; core lock bump + `^0.3` → `^0.4` in composer.json AND distribution.json same-commit with the consuming listener; the listener self-filters on `product_price_id` before any read; a product-tagged session must never reach the generic fallback), the rule-5 testsuite pass, twins/mirrors/flag/boundary. `widgets:sync` + `build:public` at the carve; **gate 4 runs live** (ProductCarousel declares scss + js).
- **The redraw is the sixth** (decision 6): byte-faithful raw-DDL create-table migrations in FK order (`products` → `product_prices` → `purchases` → `waitlist_entries`), **no sequences** (uuid PKs — record the empty category), the RESTRICT FKs travel as behavior (the `CascadeDeleteTest` net rides unchanged), the CASCADE FKs with their tables; **the allocations drop precedes the redraw** (decision 4 — its own commit, the 396 Phase A′ shape, re-verify zero readers/zero rows first); dump regenerated against a scratch database (recreate the database, not the schema); both identity checks clean; fixtures vendor-relative from birth.
- **The fallback is planned, not shameful** (decision 2): if the carve or inversion runs long, stop after carve or after carve + package, log the stop point, and the block closes next session — the D3/D4/D5 two-session shape.
- **Factory-email discipline:** any new arrange feeding a NOT NULL column pins its factory values (the 395 flake lesson).
- **The manifest is edited, never the generated file.** Scope is binding: no model moves, no products capability, no store-era features, no per-plugin CI (D11), no FM surface, no D9 work. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.21.0 → 0.22.0 (ninth Package-shape + External-repo consumer; surface 5 sixth redraw + the allocations-drop note; surface 10 products slice; changelog); plan doc § Stage D D8 line closed ✅ + § 6.8 marked resolved; the D9 kickoff survey (Integrations) captured at close and the 399 prompts drafted.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the survey still matches the code, state the carve + inversion → package/redraw/drop → extract plan — and resolve the open items: **(1)** the owner ratifies the five front-loaded calls (**the § 6.8 own-vertical disposition — the headline**; the one-session shape with the loud fallback; the Payments v0.4.0 inversion; the allocations drop; the double observer inversion); **(2)** the owner has created the public `crm-plugin--products` repository (blocks only the extraction phase); **(3)** the sibling checkouts are current (`git pull` each of the eight — **one sibling edit is expected**: `crm-plugin--payments` v0.4.0). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish or its honest stop point.
