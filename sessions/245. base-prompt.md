# Base Prompt — Session 245

We are about to begin a new session: **245. Random Data Generator as Dashboard Widget**.

This is the first session in the pre-Beta-1 release plan execution order — entry **A1** in `sessions/release-plan.md`. The plan's discipline rules govern this session; in particular **Rule 5** (plan doc is source of truth) and **Rule 11** (session count is always flexible — split rather than overload).

## Success criterion (from `release-plan.md` § A1 — do not relitigate)

A super-admin-gated Filament dashboard widget that generates contacts / donations / events / registrations / memberships in configurable counts. Generated rows tag `source = 'generated'` so teardown queries are clean. Custom fields on contacts respect declared types when seeded. Per-action confirmation step prevents accidental clicks. The existing `APP_DEBUG_TOOLS`-flagged debug generator is retired.

The session closes when the success criterion is met. Iterate the criterion only with explicit re-sign-off (Rule 3).

## Forcing function

Every downstream rehearsal in the plan (B2 Onboarding cluster, C3 Permission audit, C4 Donation-to-acknowledgment, C5 Event with everything, C6 Membership renewal, D1 Scale rehearsal) needs the ability to seed realistic synthetic data quickly. The current debug generator's seams showed during 242 backup-restore testing; this session promotes it to a first-class operational tool.

---

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/release-plan.md` in full — the 11 discipline rules at the top govern every session in the plan, and § A1 is this session's entry. Pay particular attention to Rules 5, 6, 11.
3. Read `sessions/session-outlines.md` for the active-tracks block, the Active tracks (Fleet Manager Agent re-opens for Beta-1-blocking node ops parity per A2), and the Random Data Generator stub which carries the original scope detail beyond what § A1 captures.
4. Read `docs/app-reference.md` for environment + container names + the dashboard view-to-file mappings. Read `docs/schema/README.md` then read the schema docs for the tables this session writes to: `docs/schema/contacts.md`, `docs/schema/donations.md`, `docs/schema/events.md`, `docs/schema/event_registrations.md`, `docs/schema/memberships.md`, `docs/schema/transactions.md`. Also read `docs/schema/custom_fields.md` (or wherever custom-field types are documented) — the session needs to respect declared field types when seeding contact records.
5. Read the existing debug data generator code (gated by `APP_DEBUG_TOOLS`). It currently lives on the dashboard via flag-conditional rendering — locate it and read both its UI and its generation logic. The session retires this surface; understanding what it does today is required to ensure the new widget covers the same ground.
6. Read session 233's work on `source` columns and `HasSourcePolicy`: `sessions/archived/233. *.md` (or wherever 233 lives now). The new generator tags `source = 'generated'` on every record it produces, leveraging the existing column + policy infrastructure.
7. Read at least one existing Filament dashboard widget in the codebase as a pattern reference for the new widget's shape (where dashboard widgets live, how they register, how they handle permission gates).
8. Open `sessions/245. Random Data Generator as Dashboard Widget.md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. (No migration is expected for this session — `source` columns already exist.)
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Fix any failures before proceeding. Baseline at 244 close: 1689.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures. The Random Data Generator widget is a Filament dashboard widget — likely no front-end build needed (Filament admin uses its own asset pipeline). If any Vue/SCSS source touches: `npm run build`. If any widget-asset surface touches: `docker compose exec app php artisan build:public`.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. The new widget's permission gating, generator counts, and `source = 'generated'` tagging all want test coverage. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Cross-repo coordination — Fleet Manager agent contract.** This session does not touch the FM agent contract surface. The CRM contract stays at v1.2.0; FM-side cache is at v1.1.0 awaiting refresh on FM's next boundary-touching session.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

The next session after 245 is **A2 — Fleet Manager — node operations parity** per `release-plan.md` § A2. Per Rule 11, A2 may run as one session or two (install + backup + restore in one; log-reading separately). The 245 close-gate drafts session 246's prompt against whichever shape is agreed.

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/246. base-prompt.md`, customizing the read list for A2 and naming the success criterion up front. Note that A2 is FM-side capability work; the read list should include the Fleet Manager planning spec and the agent contract doc.
- **Next session prompt**: draft `sessions/246. Fleet Manager — Node Operations Parity.md` (or split title if the work is splitting into 246 + 247) using `sessions/template-session-prompt.md` as the format reference. Base it on `release-plan.md` § A2.

### Phase 2 — Close

When the user says to close (after 246's prompt is agreed):

- **Session log**: write a log file at `sessions/245. Random Data Generator as Dashboard Widget — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly.
- **Update `sessions/completed-sessions.md`**: append session 245's row.
- **Update `sessions/release-plan.md`**: land a checkmark on the A1 entry. Per Rule 6, this is the only mid-plan edit allowed (record findings or surface a needed prerequisite if any surfaced; otherwise just the checkmark).
- **Update `sessions/session-outlines.md`**: the existing Random Data Generator stub gets folded into a "complete — see release-plan.md § A1" pointer, or removed entirely if its content is fully captured by § A1 + the session log.
- **No track to update** — A1 doesn't belong to a track.
- **Production deployment notes in the log**: list any operator action needed (likely: enable the new widget for super admins; retire the `APP_DEBUG_TOOLS` env-var path).
- **Archive the previous session**: move all files matching `sessions/244. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (the log, the updated `release-plan.md` checkmark, the updated `session-outlines.md`, the completed-sessions index, the 246 base prompt + session prompt, and any files moved into `sessions/archived/`), commit on the current `session-245/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`. (The Random Data Generator is a dashboard widget, not a page — breadcrumbs may not apply, but the principle stands for any new pages this session creates.)
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. (Out of scope for this session — no portal code touched. The new widget is admin-side and super-admin-gated.)
