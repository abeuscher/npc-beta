We are about to begin a new session: **189. CRM Importer — Events, Registrations & Transaction Linkings**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, then read the individual table files under `docs/schema/` relevant to this session's scope — at minimum `contacts.md`, `custom_field_defs.md`, `notes.md`, `import_sessions.md`, `import_sources.md`, `import_id_maps.md`, `import_logs.md`, `import_staged_updates.md`.
3. Open `sessions/188. CRM Importer — Contacts: Presets, Dry-Run & Match Keys.md` and read the session prompt carefully.
4. Confirm the WCG working data is present in `local/import-fixtures/wcg/` (or wherever the user tells you to look) and verify the path is `.gitignored` before touching any real data. Never stage real exports, never paste real rows into committed docs or fixtures. Anonymised fixtures for tests live under `tests/Fixtures/import/contacts/`.
5. Summarise your understanding and confirm you are ready to proceed.
