# Base Prompt — Session 256

We are about to begin a new session: **256. Organizations Importer**.

This session lands **B1c** from `sessions/release-plan.md` — a new prerequisite stub for B2 (Onboarding rehearsal cluster), inserted between B1a and B2 per Rule 1 (stub-then-rehearsal discipline). The Organizations importer is the missing top-level admission path for the Org peer-record family that B1a delivered. Without it, B2 has no clean way to land real-shape Wild Apricot / Neon exports that carry rich Org metadata (type / website / email / address) — the Org-as-source sentinels added at B1a only auto-create org stubs by name.

This session is **not boundary-touching** — Fleet Manager contract stays at v2.1.0.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md` — the 11 discipline rules + the new § B1c entry slotted between B1a and B2 in the execution order.
3. Read `sessions/session-outlines.md`:
   - Active tracks block (informational — Fleet Manager track is active but this session is not boundary-touching).
   - Cross-Repo block — confirm v2.1.0 going in; this session does not change it.
   - The B1c stub.
4. Read `docs/app-reference.md` — environments, container names, view-to-file mappings, Tools group importer surfaces.
5. **Schema docs** — read each table file under `docs/schema/` that this session touches:
   - `docs/schema/README.md` (index)
   - `docs/schema/organizations.md` (current shape — name / type / website / phone / email / address only; this session adds source / custom_fields / import_source_id / import_session_id / external_id)
   - `docs/schema/notes.md` (polymorphic; Org importer's `__note_organization__` sentinel writes here)
   - `docs/schema/tags.md` and `docs/schema/taggables.md` (polymorphic; Org importer's `__tag_organization__` sentinel writes here)
6. **Current Organization model** — read [`app/Models/Organization.php`](../app/Models/Organization.php) end-to-end. Note especially:
   - The model does **not** currently use `EnforcesScrubInheritance` or `HasSourcePolicy` — Phase 2 adds both.
   - Existing relations (`contacts()`, `donations()`, `memberships()`, `eventRegistrations()`, `eventsSponsored()`, `transactions()`, `tags()`, `notes()`) stay untouched.
7. **Source-policy reference** — read `app/WidgetPrimitive/HasSourcePolicy.php` and `app/WidgetPrimitive/EnforcesScrubInheritance.php` end-to-end. Read [`app/Models/Donation.php`](../app/Models/Donation.php) for the `ACCEPTED_SOURCES` / `scrubInheritsFrom()` reference shape — Org's are simpler (no `scrubInheritsFrom`, since Org sits at the top of the source-policy graph).
8. **Existing namespaced importer pattern to mirror** — the Donations / Memberships / Events / Invoice Details / Notes importers all follow the same shape via [`app/Filament/Pages/Concerns/InteractsWithImportWizard.php`](../app/Filament/Pages/Concerns/InteractsWithImportWizard.php) + [`app/Filament/Pages/Concerns/InteractsWithImportProgress.php`](../app/Filament/Pages/Concerns/InteractsWithImportProgress.php). Read `ImportMembershipsPage` + `ImportMembershipsProgressPage` end-to-end as the closest reference (single-entity-with-contact-match shape — Orgs are similar minus the contact-match bucket).
9. **Field registry pattern** — read [`app/Importers/EventFieldRegistry.php`](../app/Importers/EventFieldRegistry.php) (DerivesFromFillable + $excluded) and [`app/Importers/MembershipImportFieldRegistry.php`](../app/Importers/MembershipImportFieldRegistry.php) (aggregating shape with otherOptions for sentinels) — the new `OrganizationFieldRegistry` and `OrganizationImportFieldRegistry` mirror these.
10. **CSV template service** — read [`app/Services/Import/CsvTemplateService.php`](../app/Services/Import/CsvTemplateService.php) end-to-end. The new `organizationHeaders()` method + `organizations` template arm add here.
11. **Importer index page** — read [`app/Filament/Pages/ImporterPage.php`](../app/Filament/Pages/ImporterPage.php) end-to-end. The Org importer card lands here.
12. **ImportSource saved-mapping shape** — read [`app/Models/ImportSource.php`](../app/Models/ImportSource.php) end-to-end. The model carries per-importer `*_field_map` / `*_custom_field_map` / `*_match_key` columns; this session adds the Organization triplet.
13. **Verify-at-start questions** that the session prompt names but the implementation should resolve before going deep:
    - **`ImportModelType` enum** — does it carry an `Organization` value already? Add it if not.
    - **Source-policy graph** — does any other model declare an FK to organizations that would cascade scrub-source? Brief grep before adding `EnforcesScrubInheritance`.
14. Open `sessions/256. Organizations Importer.md` and read the session prompt carefully.
15. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.1.0** (per session 253). No change expected this session.
- **Session 255 deliverables now on every install:** five new transactional FKs (`donations.organization_id`, `memberships.organization_id`, `event_registrations.organization_id`, `events.sponsor_organization_id`, `transactions.organization_id`), all `ON DELETE SET NULL`; four importer Org-as-source sentinels (`__org_donor__` / `__org_member__` / `__org_sponsor__` / `__org_invoice_party__`) reusing the `__org_contact__` strategy radio UX; new Organization admin form (single-section, with email field, type values nonprofit/for_profit/government/other); `EventsSponsoredRelationManager` on Org edit page; ellipsis menu with View Affiliated Contacts link; Timeline page (lifted from Contact's pattern via shared abstract `app/Filament/Concerns/RecordTimelinePage.php`); sponsor field on Event admin form; SelectFilter on Contacts list keyed to `organization_id`; block-with-counts deletion guard with force-delete branch. See `sessions/255. Organizations Model Overhaul (Min) — Log.md` for the full landing.
- **PHP `memory_limit` is 1G** ([docker/php/local.ini](../docker/php/local.ini)).
- **Fast test suite is green** at 1860 passed (255 close baseline). This session adds migration shape tests + importer round-trip tests + dedup tests + custom-field/tag/note round-trip tests. Modest growth expected.
- **Bind-mount edit gotcha** is documented; doesn't apply to this session.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration**, ask the user what existing system handles it. This session leans entirely on existing infrastructure (`InteractsWithImportWizard` / `InteractsWithImportProgress` traits, `ImportSource` saved-mapping shape, `ImportModelType` enum, `CsvTemplateService`, `ImporterPage` index, `EnforcesScrubInheritance` source policy). Do not introduce new patterns.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** This session's load-bearing questions — *"does Organization need `scrubInheritsFrom` declared?"*, *"do we add a contact-match bucket to OrganizationImportFieldRegistry, or is name-only dedup sufficient?"*, *"what's the dedup match-key default — name or email?"* — are settled in the session prompt's Design decisions. If implementation surfaces a reason to deviate, surface it before going deep.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Schema migrations** — write each migration, run via `docker compose exec app php artisan migrate`, then update the matching `docs/schema/*.md` file in the same change-set per the standard discipline.
- **One combined migration** for the Organization schema additions (source / custom_fields / import_source_id / import_session_id / external_id + the ImportSource saved-mapping triplet). Cleaner commit boundary than five small files.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline is 1860 passed (255 close).
- **Front-end builds — when to run which:**
  - No SCSS, no Vue, no widget asset changes — no `npm run build` or `build:public`.
  - No `public/css/admin.css` edits expected.
- **Cross-repo coordination — Fleet Manager agent contract.** This session is **not** boundary-touching. The standing cross-repo discipline applies as a passive check. No change expected.
- **Verify objective outcomes yourself; pull the user in for judgment.** Migration shape tests, importer round-trip tests, and dedup-strategy tests are objective — close the loop yourself. The visual feel of the importer wizard is a judgment call (announce that visual testing is ready, then stop and wait).
- **Pause for manual testing only when human judgment is required.** After implementation + fast suite pass, announce that visual testing is ready (the user opens the Importer index, confirms the Org importer card, runs a small fixture CSV through the wizard end-to-end). Stop and wait. Do not suggest closing the session.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The next session after 256 is **B2 — Onboarding rehearsal cluster** per the release-plan execution order. With B1c shipped, B2 has the full Org admission surface it needs to land real-shape exports.

If 256 surfaces gaps that B2 would trip over (e.g., a missing field in the Org importer that real-world data demands, an edge case in dedup behavior under skewed CSVs), those land first. Otherwise B2 is unblocked.

Decide at 256 close. Draft the 257 base prompt + session prompt against the agreed shape.

### Phase 2 — Close

When the user says to close (after 257's prompt is agreed):

- **Session log**: write `sessions/256. Organizations Importer — Log.md` against `template-session-log.md`.
- **Update `sessions/completed-sessions.md`**: append session 256's row.
- **Update `sessions/release-plan.md`**: check off **B1c** (✅) with a "closed at session 256" note plus a one-line summary of what landed (admin importer + 5 new schema columns on organizations + 3 new mapping-save columns on import_sources + tag / note / custom-field sentinels). B2 next.
- **Update `sessions/session-outlines.md`**: fold the B1c stub into a closure note. B2 stub stays open.
- **No track doc updates** — this session does not belong to an active track.
- **Spec doc** — no Fleet Manager contract change; no edits to `docs/fleet-manager-agent-contract.md`.
- **Archive the previous session**: move all files matching `sessions/255. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-256/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly — `ImportMembershipsPage` / `ImportMembershipsProgressPage` are the closest references; `MembershipImportFieldRegistry` + `MembershipFieldRegistry` are the registry references; `Donation`'s `EnforcesScrubInheritance` + `HasSourcePolicy` setup is the source-policy reference.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones — the new admin pages, registries, and migration are unavoidable additions; everything else is in-place edits.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- **Cross-importer mirror discipline** — every new sentinel that lands in the trait's `buildNamespacedMappingRow` lands with parallel handling in `serializeColumnMaps` and the matching `processImportRow`/progress-page logic in the same commit. Drift between them is a process failure. (Note: the four Org-as-source sentinels added at 255 already use the trait shape. The three new Organization-importer sentinels (`__custom_organization__`, `__tag_organization__`, `__note_organization__`) follow the same parallel-edit discipline.)
