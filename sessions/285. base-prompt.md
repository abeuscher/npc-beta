# Base Prompt — Session 285

**Start now.** Execute the reading list below, open the session prompt at `sessions/285. Public Marketing Website — CMS Fixes Before Home Rebuild.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **285. Public Marketing Website — CMS Fixes Before Home Rebuild**.

This session continues the **Public Marketing Website track** (track doc at `sessions/tracks/public-marketing-website.md`). PMW1 closed at session 284 and surfaced ten gap-report rows; four of those are blockers for the home-layout rebuild that follows in 286. This session lifts those four into landed fixes so the rebuild has the CMS surface area it needs. Sequenced inside the **Public Website Complete milestone**.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** bundled gap-fix. Five contained changes — layout `appearance_config` round-trip through the page exporter/importer (G1), Text widget gains a CTA capability (G2), one new ghost-on-dark button variant for dark-band CTAs (G3 interim), typography defaults tuned at the Theme page (G13/G14), and an Image-widget capability audit before any of the above. Together they unblock the home rebuild scheduled for 286.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. Read `sessions/release-plan.md` for the session's position inside Public Website Complete; the entry for this session may not yet be in the plan at session start (added during 284's close handoff) — if absent, treat the gap report as the canonical scope source and note the planning gap in the work log.
4. Read `sessions/tracks/public-marketing-website.md` for the track's status snapshot, gap-resolution discipline, and forward plan.
5. Read **`sessions/public website/gap-report.md`** — canonical input for this session. Rows G1, G2, G3 (interim path), G13, G14 are the scope. The remaining rows (G4–G12 minus G3) are out of scope and stay open.
6. Read `sessions/public website/audit-summary.md` for the system-inventory context — typography, button styles, appearance config schema, widget inventory, sample images, import mechanism.
7. Read `docs/app-reference.md` for the page-builder component map. Read `docs/schema/README.md` for the table index; no schema changes are expected this session.
8. Open `sessions/285. Public Marketing Website — CMS Fixes Before Home Rebuild.md` and read the session prompt carefully.
9. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

---

## Starting state inherited from 284

- **CRM contract at v2.3.0** — unchanged. PMW1 was content + audit; no boundary surface touched. Session 283 shipped Stripe Checkout branding; no further contract changes since.
- **Schema baseline:** no schema changes since 280-close.
- **Fast test suite green at 2390 / 0 sequential** (284 close baseline, unchanged from 283 close — content-only session). New tests expected this session for G1 (layout round-trip), G2 (Text widget CTA), G3 (button variant option). Estimated +5 to +10 fast tests.
- **Playwright suite** — baseline preserved. No Playwright in this session.
- **Planning state:** Public Marketing Website track is active; PMW1 closed at 284; cleaned home.json sits at `sessions/public website/home.json`; first gap-report rows landed at PMW1 close and are canonical input for this session.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — gap-fix sessions don't absorb housekeeping items unless directly tied to one of the gaps being lifted. Touch only if a finding surfaces inside the scope here; otherwise carry forward.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. *Session-285 note: no integrations in scope.*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** Standard rule applies. The most likely 285-specific candidate: if the G3 button-variant work surfaces an opportunity to do the long-term per-variant night-mode treatment cleanly inside this session (rather than just one new variant), flag the option to the user before going wide — the interim path was chosen explicitly to keep this session scoped; the user may or may not want to expand on the spot.
- **Adapt to drift; don't ask about it.** Standard rule. The cleaned home.json from PMW1 is current; the gap-report rows are recent; drift expected only at code-level fine detail.
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that propagate (variant naming, schema shape on TextBlock CTAs, exporter/importer test scope). Local choices — just decide and note.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. *Session-285 note: no migrations expected — the changes are schema-on-widget-definition (PHP) plus SiteSetting (`button_styles`, `typography`) tuning; no DB schema changes.*
- **Update `docs/schema/`** after writing any migration. (No-op this session.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 284-close baseline (2390 / 0) plus the new tests added for G1 / G2 / G3.
- **Front-end builds.** Changes to widget definitions (Text widget schema, button variant additions) may touch SCSS or JS surfaces. Rule: `resources/scss/**` or `resources/js/**` → `npm run build`; widget asset surfaces → `php artisan build:public`. The G3 button-variant work will require both — `npm run build` for the SCSS where button-variant CSS classes live, and `php artisan build:public` for the public widget CSS bundle that DesignSystemPage rebuilds on save.
- **Tests for new behaviour:** yes — every code change in scope (G1, G2, G3) should ship a fast Pest test. G13/G14 are settings-only and skip the tests-for-new-behaviour rule.
- **Fast vs slow classification:** all new tests fast. None expected to exceed 5s individually.
- **Playwright artifact hygiene:** N/A this session.
- **Cross-repo coordination — Fleet Manager.** N/A.
- **Verify objective outcomes yourself; pull the user in for judgment.** This session is largely objective: the exporter round-trips correctly or it doesn't; the Text widget renders CTAs or it doesn't; the button variant exists in the SiteSetting or it doesn't. Close those loops via Pest + tinker / live import-export round-trips. The typography tuning at the Theme page is the only visual surface — surface for the user's review at session-end.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that the four gap-row fixes are landed and tested, the Theme page typography tuning has been pushed, and the home page is now ready for the 286 rebuild against the layout spec. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/286. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to **Public Marketing Website — Home Layout Rebuild**.
- **Next session prompt** (`sessions/286. Public Marketing Website — Home Layout Rebuild.md`) — draft against `sessions/public website/homepage-layout-spec.md` (canonical for that session's structure) and the now-resolved gap-report state from this session's work.

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/285. Public Marketing Website — CMS Fixes Before Home Rebuild — Log.md` using `template-session-log.md` as the format reference. Document: per-gap-row what shipped, exporter/importer test coverage added, Text widget CTA schema shape, button variant name and color values, typography deltas, image-widget audit findings.
- **Update `sessions/public website/gap-report.md`:** mark G1 / G2 / G3 (interim) / G13 / G14 as ✅ resolved with a one-line note per row recording what shipped. The remaining rows stay open.
- **Update `sessions/completed-sessions.md`:** append session 285's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status to "active — PMW1 closed at 284; CMS fixes closed at 285; home rebuild next at 286."
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot.
- **Update `sessions/release-plan.md`:** if a 285 entry was added at 284 close, mark it ✅ closed; if no entry exists, add one retroactively in the appropriate position inside Public Website Complete.
- **Archive the previous session:** move all `sessions/284. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-285/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — G1 layout appearance_config round-trip + Pest test", "Iteration /2 — G2 Text widget CTAs + template + Pest test", "Iteration /3 — G3 ghost-on-dark button variant + bundle rebuild", "Iteration /4 — G13/G14 typography defaults tuned at Theme page".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Gap-report updates are operator-readable.** Mark resolved rows succinctly: row name, "✅ resolved at 285", one-line summary of what shipped.
