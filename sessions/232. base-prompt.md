# Base Prompt — Session 232

We are about to begin a new session: **232. Widget Primitive — Phase 5d-2 (Membership Status — Single-Row Record-Detail Widget)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top.
3. Read `sessions/tracks/widget-primitive.md` (the canonical Widget Primitive track doc — premise sits alongside at `widget-primitive-premise.md`). Specifically:
   - Status snapshot (top of doc)
   - Phase Retrospectives — at minimum 5d-1 (just landed) and **4g (223, EventDescription + EventRegistration)** — 5d-2 mirrors 4g's single-row DTO shape (`['item' => row | null]`, `cardinality: 'one'`) for a record-detail Slot consumer. Skim 5d-1 (231) for the per-arm permission-gate pattern and the sub-nav threshold cleanup that landed in iteration 2.
   - "Phase 5" entry under "The 2.0 transition — rough sequence," especially the 5d+ bullet
   - "Pending paradigm questions" section — Phase 5e (permissions in the contract layer) is still carved out. 5d-2 reuses the minimal per-arm `Gate::denies` pattern proven in 5d-1; full perm-in-contract architecture stays deferred. After 5d-2 lands, the per-arm-gate evidence will be at two widgets (Recent Notes + Membership Status); Recent Donations at 5d-3 makes three, which is the natural moment to reassess centralization.
   - "Design decisions locked in" — particularly **source-as-capability**, the **single-row DTO shape** locked in 4g, the **`a+` View framing**, and the **field-naming convention** (concept-named display fields per 226).
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, plus `docs/schema/memberships.md`, `docs/schema/membership_tiers.md`, `docs/schema/record_detail_views.md`, and `docs/schema/page_widgets.md`. **This session writes no migrations** — `memberships` and `membership_tiers` already exist; `record_detail_views` already exists from 5c.
5. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/archived/231. Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget) — Log.md` — establishes (a) the per-arm permission-gate pattern at the resolver (`auth()->user()?->can('view_note')` returns empty fail-closed); (b) `SlotContext` threaded through `resolveSystemModel()`; (c) the `EditContactView` concrete sub-page wiring on EditContact; (d) the **iteration-2 cleanup** — sub-nav threshold gates on Views + additional pages only (entry no longer counts), and `RecordDetailViewResource::canDelete()` returns false for seeded primary Views per the `primaryHandles()` helper. **5d-2 reuses every one of these primitives unchanged. The sub-nav stays suppressed because Membership Status is added to the existing `contact_overview` View, not a new one.**
   - `sessions/archived/223. Widget Primitive — Phase 4g (EventDescription + EventRegistration) — Log.md` — establishes the single-row contract DTO shape (`['item' => row | null]`, `cardinality: 'one' | 'many'`). 5d-2 is the second consumer of this shape and the first single-row contract on a record-detail Slot.
6. Read the full state of every file this session edits or creates (see the session prompt's Phase descriptions for the file list). At minimum: `app/WidgetPrimitive/ContractResolver.php`, `app/WidgetPrimitive/Projectors/SystemModelProjector.php`, `app/Models/Membership.php`, `app/Models/MembershipTier.php`, `app/Models/Contact.php` (specifically `activeMembership()`), `database/seeders/RecordDetailViewSeeder.php`, and `app/Filament/Resources/RecordDetailViewResource.php` (for `primaryHandles()` + the canDelete gate).
7. Read related existing tests to anchor patterns:
   - `tests/Unit/WidgetPrimitive/ContractResolverNoteTest.php` (just landed in 5d-1) — the per-arm permission-gate + ambient-record-scoping test template. 5d-2 mirrors the resolver-test shape with `view_membership` swapped in for `view_note` and single-row DTO assertions for list assertions.
   - `tests/Unit/WidgetPrimitive/Projectors/SystemModelProjectorNoteTest.php` (just landed) — the projector test template. 5d-2 mirrors the fail-closed-field-whitelist shape.
   - `tests/Feature/EventDescriptionContractRetrofitTest.php` — the canonical single-row test pattern from 4g, including the empty-key zero-query check (relevant: 5d-2's resolver early-returns when no ambient record is supplied, no Membership query fires).
   - `tests/Unit/Widgets/RecentNotesDefinitionTest.php` (just landed) — the widget-definition test template. 5d-2 mirrors with cardinality:'one' assertions.
8. Open `sessions/232. Widget Primitive — Phase 5d-2 (Membership Status — Single-Row Record-Detail Widget).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. **This session writes no migrations.**
- **Update `docs/schema/`** after writing any migration — none expected this session.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session adds a new widget at `app/Widgets/MembershipStatus/` with a Blade template and no declared assets — neither `build:public` nor `npm run build` should be required if the widget mirrors RecentNotes' shape (server-rendered Blade, no JS/SCSS). The session prompt confirms.**
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands a new widget, a new resolver arm, a new projector method, and a seeder change — every piece gets test-locked.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/233. base-prompt.md`. Expected next session: **Phase 5d-3 — Recent Donations**, the third concrete record-detail widget. Recent Donations was named at the start of the 5d arc as the forcing function for full Phase 5e (perm-in-contract architecture); after 5d-2 lands, the per-arm-gate pattern will have two widgets of evidence, and the choice at 5d-3 close is whether to ship Recent Donations on the same per-arm pattern (third evidence point) or to lift the gate machinery into a dedicated Phase 5e session. Pick at 232 close based on what 5d-2 surfaced.
- **Next session prompt**: draft `sessions/233. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/232. Widget Primitive — Phase 5d-2 (Membership Status — Single-Row Record-Detail Widget) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 232's row.
- **Update `sessions/session-outlines.md`** (the roadmap): the Active-Tracks block's Widget Primitive entry needs its remaining-session count bumped (one fewer; ~3–5 left).
- **Update `sessions/tracks/widget-primitive.md`**: bump the status snapshot (last update date, what's complete, what's active). **Phase 5d-2 closes the second sub-phase of the 5d arc** — extend the existing 5d retrospective entry (or write a fresh compressed retrospective entry under Phase Retrospectives if 5d-1 was logged as its own retrospective).
- **Archive the previous session**: move all files matching `sessions/231. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines/track docs, the completed-sessions index, and any files moved into `sessions/archived/`), commit on the current `session-232/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
