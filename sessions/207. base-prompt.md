# Base Prompt — Session 207

We are about to begin a new session: **207. Column Layout Inspector — Appearance Unification**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, then read `docs/schema/page_layouts.md` and `docs/schema/page_widgets.md` — this session touches both contracts.
3. Read the predecessor context — this session was carved out of session 206's Open Flag A:
   - `sessions/archived/206. Code Review & Cleanup (Apply) — Log.md` (or `sessions/206. Code Review & Cleanup (Apply) — Log.md` if not yet archived). Flag A's resolution describes the architectural intent; this prompt is the implementation spec.
4. Read the page builder files grouped below. Pull adjacent files only as the walkthrough directs — do not pre-read the entire `resources/js/page-builder-vue/` tree.
   - **Vue — inspectors and panels**
     - `resources/js/page-builder-vue/components/LayoutInspectorPanel.vue` — the current bespoke layout inspector being replaced.
     - `resources/js/page-builder-vue/components/InspectorPanel.vue` — the widget inspector whose chrome the new layout inspector mirrors.
     - `resources/js/page-builder-vue/components/appearance/BackgroundPanel.vue` and `SectionLayoutPanel.vue` — the two universal panels reused by layouts. They get genericized in this session.
     - `resources/js/page-builder-vue/components/appearance/TextPanel.vue` — reference only; layouts do **not** get a Text tab.
     - `resources/js/page-builder-vue/components/InspectorHeader.vue` and `InspectorTabs.vue` — chrome components reused unchanged.
   - **Vue — store & types**
     - `resources/js/page-builder-vue/stores/editor.ts` — widget appearance write methods; a layout-appearance equivalent is added here.
     - `resources/js/page-builder-vue/types.ts` — the `Layout` TypeScript type (adds `appearance_config`).
   - **PHP — model, API, renderer**
     - `app/Models/PageLayout.php` — `$casts` / `$fillable` and any layout-level accessor.
     - `app/Http/Controllers/Admin/PageBuilderApiController.php` — the layout update endpoint plus the widget schema-key filter as the reference pattern.
     - `app/Services/AppearanceStyleComposer.php` and the public page renderer (`app/Services/PageBlockRenderer.php`, `app/Http/Controllers/PageController.php`) — wherever layout CSS is composed today from `layout_config`.
   - **Docs**
     - `docs/schema/page_layouts.md` — update target after the migration lands.
5. Open `sessions/207. Column Layout Inspector — Appearance Unification.md` and read the session prompt carefully. Pay particular attention to the **architectural invariant** block — the column-layout-is-not-a-widget rule is load-bearing.
6. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Fix any failures before proceeding.
  - This session touches `resources/js/` extensively — run `npm run build` and confirm it succeeds.
  - This session touches the page builder Vue surface and the layout CRUD path — run `npm run test:e2e`.
  - Widget asset bundle rebuild (`php artisan build:public`) is **not** expected — no widget SCSS/CSS/JS changes. Skip unless widget assets incidentally drift.
- **Tests for new behaviour:** the migration round-trip and the API schema-key filter on layouts both need new Pest coverage. The panel genericization is a pure structural refactor with no behaviour change — note that explicitly in the commit message and rely on the existing suite for the widget path.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it.

Claude then drafts the next session's documents. The next session after 207 is **208 — Migration Squash & Code Optimization** per the `session-outlines.md` queue (the squash was originally queued as 207 and was displaced one slot by this session). 208 follows the squash procedure from sessions 062 / 082 / 108 / 142 / 181 (schema snapshot → collapse migrations → verify `migrate:fresh --seed` identity → archive old migrations) and absorbs the `page_layouts.appearance_config` migration from this session into its baseline. It also picks up any performance-optimization opportunities surfaced during 206 and 207's refactors.

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/208. base-prompt.md`, updating the session number, title, and session-specific read-list (include the prior squash logs as precedent: 062 / 082 / 108 / 142 / 181).
- **Next session prompt**: draft `sessions/208. Migration Squash & Code Optimization.md` using `sessions/template-session-prompt.md` as the format reference, with session 181's prompt as the structural model. Explicitly note that the squash baseline must fold in `page_layouts.appearance_config` (from 207) without resurrecting the pre-migration `layout_config.background_color` / padding / margin keys.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/207. Column Layout Inspector — Appearance Unification — Log.md`. Use `sessions/template-session-log.md` as the format reference. Include a per-phase summary, the schema changes (the new column + data migration), and any deferred decisions.
- **Update session-outlines.md**: move this session's title into the Completed Sessions table. Confirm the "Migration Squash & Code Optimization" stub now sits at session 208. Review adjacent stubs (Column Widget UI Improvements in the Post-Beta 1 block) and update the "column widget" phrasing if it drifts against this session's architectural invariant.
- **Archive the previous session**: move all files matching `sessions/206. *.md` into `sessions/archived/`. This includes the previous session's base prompt, session prompt, and log. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-207/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Architectural invariant for this session:** a column layout is not a widget. `page_layouts` and `page_widgets` are separate tables with separate contracts and separate rendering paths. This session shares two *panels* across the two *inspectors* via data-driven props; it does not unify the underlying constructs in any other way.
