# Session 294 — Column-Layout Mobile Collapse + Swiper Blanket — Playwright-Verified — Log

**Date:** 2026-05-16
**Branch:** session-294/1

---

## What was built

### Phase 1 — Audit oracle

- `scripts/audit-mobile.js` — host-run ESM Playwright harness mirroring `generate-page-screenshots.js` conventions (`--base-url`, global-playwright fallback, `--json-out`, `--screenshots`). Five public pages × {320,360,390,414,767} → structured violation list: page-overflow, element-overflow (root offenders only), columns-not-collapsed, text-overflow, aspect-distorted. Exits non-zero on any violation so it is the loop's stop condition.
- **Before baseline: 122 violations, all `text-overflow`** (crushed multi-column text — e.g. an `<h3>` at `clientWidth:40px`, four columns jammed into 320px). Zero page/element overflow; `columns-not-collapsed` correctly dormant (no attribute yet). 25 before screenshots captured.

### Phase 2 — Column-layout collapse (the core)

- `PageBlockRenderer::renderLayoutBlock()` — the column track is now emitted as the single `--layout-cols` custom property instead of an inline `grid-template-columns`; a `data-collapse-mobile="true|false"` attribute is emitted on `.layout-grid`, resolved as a concrete `($config['collapse_mobile'] ?? true) !== false` (default-on at read, never branches on "missing"). All other grid inline props unchanged.
- `resources/scss/_layout.scss` — `.page-layout { container-type: inline-size }`; `.layout-grid { grid-template-columns: var(--layout-cols, 1fr) }` (stylesheet now owns the property, so the collapse override needs no `!important`); `@container (max-width: $bp-md) { .layout-grid[data-collapse-mobile="true"] { grid-template-columns: 1fr; flex-direction: column } }`.
- `LayoutRegion.vue` — mirrors `--layout-cols`, binds `:data-collapse-mobile`, and carries a parallel scoped `@container` rule on `.layout-region__grid`; `PreviewCanvas.vue` — `.widget-preview-scope` gains `container-type: inline-size` so the simulated viewport drives the same container query the public side does.
- `collapse_mobile` toggle — "Collapse columns on mobile" checkbox on the Column Settings tab (`LayoutColumnSettingsTab.vue`), default checked via concrete-value read. Added `'collapse_mobile'` to the `layout_config` save allow-list in **both** `PageBuilderApiController` and `RecordDetailViewBuilderApiController` (the live G1/285-shape silent-drop seam — export/import already round-trips the whole blob).

### Phase 3 — Swiper blanket + overflow guards (audit-driven)

- Uniform Swiper guard `.swiper { max-width: 100%; min-width: 0 }` in `_custom.scss` — makes the vendored `.swiper{overflow:hidden}` containment ours and adds `min-width:0` against a carousel blowing out a flex/grid column. No JS rewrite (the widgets already ship mobile-aware configs).
- Mobile button-label wrap guard (`_buttons.scss`, `@media (max-width:$bp-md)` on `.btn`) — labels wrap instead of clipping; this also removed the min-content push that overflowed `.hero-inner`.
- Aggressive text-wrap guard (`_base.scss`, `overflow-wrap:break-word` on headings/paragraphs/`.widget-text-block__content` inside `.np-site`) — text wraps rather than overflows even where authored widget padding crushes the box.
- `.hero-inner` mobile cap (`_custom.scss`, `max-width:100%` at `≤$bp-md`) — the column flexbox + `hero--pos-*-left` align-items made `.hero-inner` size to its headline's max-content and exceed the container at phone widths.

### Phase 4 — Loop to zero + residue

- Autonomous loop: **122 → 9 → 3 → 0** across all five pages × five widths. Terminated on the empty oracle. 25 after screenshots + before/after JSON under `sessions/public website/mobile-audit/`.
- Pest: updated two stale assertions to the `--layout-cols` contract (`PageBlockRendererLayoutTest`, `PageTest`); added `--layout-cols`/default-on + `data-collapse-mobile=false` renderer cases, the `collapse_mobile` export/import round-trip (G1/285 guard), and the save-allow-list persistence guard.
- `VERSION` → `0.294.1`.

### Mid-session, user-directed (typography calibration → E5 prep)

- Removed the long-standing pre-existing `AppVersionStampTest` "seven-character git SHA" doc-collision test at the user's instruction — it policed the *absence* of a discarded feature's prose in a doc (brittle by construction). The four live infra guards in that file remain.
- Ran a calibration experiment for the next session (E5): a hardened, markup-agnostic Playwright type probe (font-size clustering, not tag names) of gong / attio / clay at our named breakpoints. Produced a per-class default ramp (display compresses hard, body essentially fixed — a uniform 25%/step is wrong). Refreshed the "Mobile Type Scaling" stub in `session-outlines.md` with the ramp table, line-height-as-unitless-multiplier, a heading bottom-margin em default, and the verification shape. Drafted `295. base-prompt.md` + `295. Mobile Type Scaling …md`. Probe relocated to `scripts/type-calibration.mjs`; data + 21 screenshots to `sessions/public website/type-calibration/`.

---

## Security considerations

Low surface. The only server-input change is adding `'collapse_mobile'` to two `layout_config` save allow-lists — it remains inside the existing `validate(['layout_config' => 'nullable|array'])` + `array_intersect_key` whitelist (the allow-list is the security boundary; a benign boolean key widens it by one well-formed entry, no new injection path). The renderer emits the attribute through the existing `e()`-escaped `style`/attribute path. Portal/auth surfaces untouched. No Fleet Manager / `/api/health` / contract surface touched — CRM contract stays v2.3.0. None beyond standard convention.

---

## Schema changes

None. `page_layouts.layout_config` is an existing `jsonb` column; `collapse_mobile` is a new key within it (no migration, default-on resolved at read). No `docs/schema/` change.

---

## Production deployment notes

None beyond standard. The SCSS + page-builder Vue changes ship via `npm run build`; no widget-asset (`build:public`) change was required. `VERSION` bumped to `0.294.1` as part of the iteration (the deploy pipeline enforces the bump).

---

## Deferred decisions

- **ChromeRenderer header/footer layouts do not collapse.** `ChromeRenderer` has its own private duplicate `renderLayoutBlock` not in the prompt's leverage surfaces; left unchanged deliberately (chrome logo+nav bars are the canonical "stay side-by-side" case; the audit covers chrome on all five pages and is at zero). Whether chrome should also gain the mechanism is a follow-on tied to E16 (Header/footer overhaul).
- **Residue confirmed by the user at handoff:** Demo 50/50 hero+form stacks on mobile (default-on = evident intent); Pricing à-la-carte `1fr 1fr` goes 1-up (default-on); ProductCarousel keeps its `slidesPerView:'auto'` + coverflow peek (surfaced carve-out, not forced 1-up); BlogPager is prev/next nav, not a Swiper widget (the prompt's "≤6" inventory was loose).
- **E5 open design call handed forward:** the 3-site calibration mean (display ~0.54 phone) leans slightly aggressive for NonprofitCRM's restrained register; the 295 prompt recommends biasing the display default toward ~0.6 (Attio-leaning) and surfacing the chosen ramp at that session's handoff. The section-heading calibration row is the soft data point (2/3 sites) — defaulted as the display/body midpoint in the stub.
- **Open question resolved in-session:** neither the public nor preview side had any `container-type` (the prompt's assumption that the preview already had it was drift) — resolved via the preferred path (a), generalized: `container-type` added to both `.page-layout` and `.widget-preview-scope` so one `@container` rule drives both. The five recorded drift items are in the session work log (folded here).

---

## Test result

2450 passed, 0 failed. (294 added +4 tests; the prior 2446 / 1 carry-forward — the pre-existing `AppVersionStampTest` doc-collision red — was resolved by removing that test at the user's instruction. Pest and Playwright run sequentially per `feedback_test_runs_not_parallel`.)
