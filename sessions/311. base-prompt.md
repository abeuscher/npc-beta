# Base Prompt — Session 311 (Table Widget)

> **Scheduled as session 311.** A new Table widget for the Page Builder. Standard widget shape — definition class, blade template, SCSS partial, help doc, demo-seeder entry, seeder registration. Picked for this slot specifically because it lands entirely outside A001's blast radius (which runs async in `app/Services/ImportExport/`); the two sessions can ship in parallel without merge interference.

**Start now.** Execute the reading list below, open the session prompt at `sessions/311. Table Widget.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **311. Table Widget.**

This is a **standard new-widget session.** No architectural calls. The widget pattern is well-established — `app/Widgets/<Name>/<Name>Definition.php` + `template.blade.php` + `styles.scss` + `thumbnails/` + a `WidgetTypeSeeder` entry + a help doc at `resources/docs/widget-table.md`. Mirror the closest existing example (PricingChart is the nearest shape — repeater of structured rows with rich-text cells).

This session is **NOT boundary-touching** — no Fleet Manager surface, no cross-instance migration, no import/export work. **CRM contract stays at v2.3.0.** **No DB schema change.** The Table widget is a brand-new widget definition; existing widgets are not modified.

**Parallel-safety note.** A001 (the async-agent extension of bundle coverage to events / products / navigation / collections) is scheduled to run concurrently. A001's blast radius is fenced to `app/Services/ImportExport/` + `tests/Feature/` + its own session log. This session writes only to `app/Widgets/Table/`, `database/seeders/WidgetTypeSeeder.php`, `resources/docs/widget-table.md`, possibly `resources/scss/` for a partial entry, and a `tests/Feature/TableWidgetTest.php` shaped to avoid file-name collision with A001's per-entity round-trip specs (which are named after entities, not widgets). No further coordination needed.

Before doing anything else:

1. Re-read session templates only if uncertain about a structural detail.
2. Read `sessions/session-outlines.md` and `sessions/release-plan.md` — confirm 311's position and the E15 success criterion.
3. **Read the full session prompt** at `sessions/311. Table Widget.md` — scope + phases + the inline-eligibility decision the session takes.
4. Read `app/Widgets/PricingChart/PricingChartDefinition.php` — the nearest existing shape (repeater of rows containing rich-text cells). Mirror it.
5. Read `app/Widgets/BoardMembers/BoardMembersDefinition.php` — a simpler repeater-shape example for reference.
6. Read `database/seeders/WidgetTypeSeeder.php` — find the registration pattern; the new entry is a single append.
7. Skim one existing widget help doc (`resources/docs/widget-pricing-chart.md`) for the format the help-sync pipeline expects.
8. Note drift in a brief work-log entry.

---

## Starting state inherited from session 310 (and possibly A001)

- **CRM contract at v2.3.0** — unchanged.
- **Schema baseline:** none added by 310. A001 also adds none.
- **Fast Pest at 310 close** — 2547 passed (see `sessions/310. … — Log.md`). If A001 lands before this session, expect roughly +4–8 round-trip tests on top of that baseline (one per new entity family). The new Table widget will add its own +1–3.
- **Playwright suite** running on every CI push. This session adds one Playwright spec for the new widget's render + admin-form happy path.
- **Inline-eligibility per-widget roster** is a Deliberately Kept carve-out (recorded at `sessions/refactor-track-deliberately-kept.md`). Whether the Table widget joins the roster is a decision this session makes — see the session prompt's "Decisions to make" section.
- **No widget changes elsewhere.** No SCSS changes outside the new widget's partial. No theme/typography touches.
- **Build/version discipline:** bump `VERSION` to `0.311.<iteration>` each iteration.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Default-to-fix on findings** per `feedback_audit_findings_default_to_fix`.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **No multiple-choice questions** for the inline-eligibility call — pick or override with prose, not a checkbox list.
- **Pest fast suite + Playwright run sequentially when complete.** Green, every count delta explained. Playwright is the CI signal; do not run locally as close-gate per the template-base-prompt rule.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

311 is **arc-terminal for the standalone Table widget work**. Successor sessions in the Public Website Complete milestone (Header/footer overhaul, Borders pass) are independent. The close-log should flag whether the inline-eligibility decision opens a follow-on (audit pass to add the widget to the roster + roster regression test update) and queue accordingly.

### Phase 2 — Close

When the user says to close: write `sessions/311. Table Widget — Log.md`. Update `completed-sessions.md`. **Archive `sessions/310. *` into `sessions/archived/`.** Commit on the final `session-311/N` branch, notify, do not push. Never push to main / force-push / merge to main. **Update `release-plan.md` and `session-outlines.md`** — E15 closes; the housekeeping inbox's `~~Table widget~~` strikethrough entry can stay (it's already marked promoted).

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages describe the deliverable; PM-level status tone; no docstrings/comments on code you did not write. **Mirror, don't invent** — the new widget is a copy of an existing pattern, not a new pattern.
