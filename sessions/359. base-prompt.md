# Base Prompt — Session 359 (Admin 2FA — TOTP Enrollment, Recovery Codes & the Demo-Mode Exemption)

**Start now.** Execute the reading list below, open the session prompt at `sessions/359. Admin 2FA — TOTP Enrollment, Recovery Codes & the Demo-Mode Exemption.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **359. Admin 2FA — TOTP Enrollment, Recovery Codes & the Demo-Mode Exemption.**

This executes **`sessions/release-plan.md` § A5 (2FA for admin accounts)** — a **Phase A operational-foundation**, the first pick off the Beta-1 backlog after the Sovereign Widget arc paused at 358. Per the outlines stub it **must close before C3 (Permission audit)**. It is **table-stakes Beta-1 security** — explicitly *not* the post-Beta **Security Audit Ready** milestone (named at 358 close), which is the later additional enumerate-every-vulnerability hardening pass. Mandatory TOTP second factor on the Filament admin panel: one-time enrollment, login challenge, recovery codes.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. The FM `/api/*` path is stateless (throttle-only, no session guard), so 2FA structurally cannot touch it; confirm with a guard test, change nothing there. CRM contract **stays at v2.4.0** (inherited; 358 was non-boundary). No `docs/fleet-manager-agent-contract.md` or Cross-Repo-block changes. The session **does** add a schema change (the `users` 2FA columns) — a migration + `docs/schema/` update, but no contract surface.

**Session shape:** **feature-style (security foundation), plan-backed (A5).** Build mandatory admin 2FA end to end against the green Pest net, with two non-negotiable carve-outs designed in from the start: (1) the **demo-mode exemption** — the demo server's single-button `/demo/enter` auto-login must stay frictionless, so the post-login enforcement gate short-circuits when `isDemoMode()` (`app()->environment('demo')`); (2) the **test-suite interaction** — a mandatory post-login gate would redirect every existing `actingAs($admin)` feature test to the enrollment screen, so the test-context posture is planned up front, not patched after. **Not** portal 2FA, **not** non-TOTP factors, **not** per-role granularity, **not** the FM API auth model, **not** the broader post-Beta security pass.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is **plan-backed but fuller-shaped** (it carries phases, open questions, out-of-scope, drift signals) because A5's plan entry is terse and the demo/test carve-outs need spelling out.
2. Read **`sessions/release-plan.md` § A5** (the `#### A5.` block — canonical scope/success/artifact) and the `session-outlines.md` § "2FA for Admin Accounts" stub. Also read the new **`── SECURITY AUDIT READY ──`** milestone in `release-plan.md` (just past `── BETA 1 RELEASE ──`) so you hold the distinction: A5 is Beta-1 table-stakes; that milestone is the later additional pass — do not pull its scope forward.
3. **Read the session prompt carefully** (`sessions/359. Admin 2FA — …md`) — it carries the seam map, the architectural open questions (the 2FA backend choice + the enforcement-gate mechanism — settle at the Open Gate, not mid-implementation), the **demo-mode exemption** section, and the **test-suite interaction** section.
4. **Read the admin-auth + demo seam** (mapped at 358 close prep — confirm against the code):
   - `app/Providers/Filament/AdminPanelProvider.php` — Filament **3.2** panel, stock `->login()`, `web` guard, the middleware stack (the enforcement gate joins it after `AuthenticateSession`), and the demo "Re-enter the demo" render hook (gated on `isDemoMode()`).
   - `app/Models/User.php` — `HasRoles`, `canAccessPanel()` gates only on `is_active`; the 2FA columns + helper methods attach here. **No** existing post-login enforcement gate to mirror (the invitation flow is token-based).
   - `app/Http/Controllers/DemoLoginController.php` + `app/helpers.php` (`isDemoMode()` = `app()->environment('demo')`) — the demo auto-login: `Auth::login('demo@demo.local', role demo)` → redirect to the admin panel. **This is the exemption integration point.**
   - `config/auth.php` (the `web` guard + `users` provider; note the separate `portal` guard is **out of scope**) and `database/seeders/PermissionSeeder.php` (the nine roles; `demo` is seeded only in demo mode).
   - `routes/api.php` — the FM `/api/*` group (throttle-only). Confirm no session guard; the FM-path guard test asserts 2FA never lands here.
   - `composer.json` — confirm no Fortify / google2fa present (the backend choice is greenfield).
5. Read `docs/app-reference.md` for environment/container names, the admin panel path (`ADMIN_PATH`), the test runner (`./dev test`), and the migration command. Read the `users` table file under `docs/schema/` for the current column set (the migration extends it).

---

## Starting state inherited from session 358

- **CRM contract at v2.4.0** — unchanged (358 was non-boundary: Sovereign Widget Stage 4.5 — `demoContext()` portal-widget thumbnails, the manual-thumbnail opt-out, strip-widget framing, the coverage guard). **This session is non-boundary; the contract is unchanged.**
- **Schema baseline:** no migrations since the **348 squash** (`database/migrations/` empty); 358 added none. **This session adds the first new migration since the squash** — the `users` 2FA columns. Write it, run it via Docker, update the `users` file under `docs/schema/`.
- **Fast Pest green at 2945 / 0** (358 close). Expected delta: **new Pest** for enrollment / challenge / recovery codes / the enforcement gate / the **demo-mode exemption** / the FM-path guard. **Plus** the test-suite interaction — a mandatory gate redirects existing `actingAs($admin)` tests, so the suite count and posture shift; the **full fast suite must return to green** at session end (treat the `actingAs` redirect wave as expected-and-handled per the session prompt's testing-context section).
- **Playwright** — the page-builder net is unaffected by an auth-flow change; no new spec expected unless the enrollment/challenge UI warrants one. Run Pest and Playwright **sequentially** if Playwright runs at all.
- **Planning state:** 358 closed the Sovereign Widget Stage 4.5 (static-thumbnail fidelity); the arc's remaining stages stay post-Beta-1 deferred. The **`── SECURITY AUDIT READY ──`** milestone was named in `release-plan.md` + `session-outlines.md` at 358 close prep — A5 (this session) is the Beta-1 table-stakes 2FA, *not* that milestone. **No other release-plan change**; A5 was already in the execution order (Phase A).
- **Housekeeping inbox** (`sessions/housekeeping-inbox.md`) — **not a housekeeping session;** the inbox is **not** drained (Rule 2 applies only to housekeeping sessions). The capture buffer `sessions/housekeeping-incoming.md` held only its header at 358 close.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Surface the architectural choices at the Open Gate, build after.** The 2FA backend (reuse Fortify primitives without its routes / a Filament 2FA package / hand-rolled `google2fa`) and the enforcement-gate mechanism (one post-login middleware vs per-page `canAccess()`) propagate — confirm the recommended leans (Fortify primitives; single middleware) with the owner before going deep, don't commit-and-unwind.
- **The demo-mode exemption is a hard requirement, not a nicety.** The enforcement gate short-circuits on `isDemoMode()` so `/demo/enter` stays one click; the demo user accumulates no 2FA state. Ship a test that proves it. (Owner-flagged at 358 close.)
- **Design the test-suite interaction up front.** A mandatory gate breaks every `actingAs($admin)` feature test unless the testing-context posture is decided first (recommended: exempt the gate in the `testing` env + targeted opt-in tests that exercise it explicitly). The full fast suite ends green.
- **Run the migration via Docker after writing it** (`docker compose exec app php artisan migrate`) and update `docs/schema/` for `users`. **Bump `VERSION` to `0.359.<iteration>` before any mid-session deploy-to-test** (deploy.yml rejects a duplicate VERSION); otherwise VERSION bumps at close.
- **Verify objectively; pull the owner in for the visual/UX call.** Close the objective loop yourself (enrollment confirms; challenge accepts valid / rejects invalid / consumes a recovery code; the gate redirects un-enrolled and exempts demo; the FM API stays reachable; the full suite is green). Surface the **enrollment + challenge UX** (the screens an admin actually walks on next login) for the owner's judgment.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the **scope** (mandatory admin TOTP — enrollment + challenge + recovery codes — with the demo exemption + FM-path-untouched); (2) the **two architectural decisions** with your recommended leans (2FA backend; enforcement-gate mechanism) for the owner to ratify, since they propagate; (3) the **test-context posture** you'll use so the existing suite stays green; (4) the **iteration shape** — recommend whether the backend+schema lands as iteration 1 with enrollment/challenge/gate batched, or a different split. One confirmation, then proceed.
