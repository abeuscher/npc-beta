We are about to begin a new session: **191. CRM Importer — Polish, Shared Code & Source Templates**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, then read the individual table files under `docs/schema/` relevant to this session's scope — at minimum `contacts.md`, `donations.md`, `memberships.md`, `membership_tiers.md`, `transactions.md`, `events.md`, `event_registrations.md`, `import_sessions.md`, `import_sources.md`, `import_id_maps.md`, `import_logs.md`, `import_staged_updates.md`.
3. Read the session 190 prompt (`sessions/190. CRM Importer — Donations, Memberships & Invoice Details.md`) for the design decisions that produced the current duplicate-then-diverge page structure.
4. Read all five wizard pages and all five progress pages under `app/Filament/Pages/Import*.php`. These are the files being refactored — you must read them in full before proposing any extraction. Also read `app/Services/Import/FieldMapper.php` for the `normalizeValue()` helper added in session 190.
5. Read the field registry files under `app/Importers/` — all nine files. These define the canonical fields per content type and are the source of truth for the CSV template feature.
6. Open `sessions/191. CRM Importer — Polish, Shared Code & Source Templates.md` and read the session prompt carefully.
7. Confirm the WCG working data is present in `local/import-fixtures/wcg/` and verify the path is `.gitignored` before touching any real data.
8. Summarise your understanding and confirm you are ready to proceed.
