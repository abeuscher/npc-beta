# 068. Audit Log

Surface the Spatie Activity Log already in the stack. Enable logging for key models (contacts, donations, pages, users, roles) and build an admin UI for browsing the log by record, user, and date range.
