# Base Prompt — Session 346 (Code Review & Cleanup — Carve-out A: Bundle Import/Export Split)

**Start now.** Execute the reading list below, open the session prompt at `sessions/346. Code Review & Cleanup (Carve-out — Bundle Import-Export Split).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **346. Code Review & Cleanup (Carve-out A — Bundle Import/Export Split).**

This is **carve-out A** of Cycle 3 — lifted at the 344 audit close because the two >1k-line file-splits would push the apply session (345) past its 6-iteration ceiling. Order in the cycle: audit 344 ✅ → apply 345 ✅ → **carve-out 346** → carve-out 347 (InlineFormatToolbar) → squash 348. Mirrors the 207/275 carve-out precedent. Cycle shape canonical in `sessions/tracks/code-review-and-cleanup.md`; the finding is W11/W7 in the 344 audit log.

This session is **NOT boundary-touching** — no Fleet Manager surface. CRM contract stays **v2.3.0**, no `CONTRACT_VERSION` change.

**Session shape:** track-phase (carve-out — legibility refactor). Split `app/Services/ImportExport/ContentImporter.php` (≈1958 LOC) and `app/Services/ImportExport/ContentExporter.php` (≈1168 LOC) — the site-bundle import/export subsystem, the pair that grew most since Cycle 2 — into a slimmer orchestrator + per-domain collaborators along their mirror seams. **Behaviour-preserving** — this is a monolith-legibility split, not a feature change; the existing round-trip test suite is the net.

Before doing anything else:

1. Re-read the session templates (`template-base-prompt.md`, `template-session-prompt.md`, `template-session-log.md`).
2. Read the **344 audit log** W11/W7 + the W11 advisory `sessions/code-review-large-files-partial-extraction.md` for the seam analysis: Exporter = per-type `serializeX` + a shared widget-tree serializer (used by pages + templates); Importer = a page/widget hydration engine + per-type import+media-rewire pairs (Products/Events/Navigation/Collections/Templates) + Design/SiteSettings/Media.
3. Read the track doc `sessions/tracks/code-review-and-cleanup.md` (carve-out boundary, won't-fixes).
4. Read `docs/app-reference.md`. Read `docs/schema/` for the entities these serialize (pages, templates, events, products, navigation, collections, media).
5. **Read both files end-to-end before touching either** — this is a behaviour-preserving extraction; you must hold the whole call graph. Re-verify the small 345/2 edit to `ContentImporter` (the EventRegistration `withoutEvents()` wrap) is present and carried through the split.
6. Note drift vs the audit's seam map; proceed unless a decision is needed.

---

## Starting state inherited from session 345

- **CRM contract at v2.3.0** — unchanged. 345 landed the apply backlog (security/correctness fixes, dead-code sweep, importer W7 dedup, convention-drift test, W12/W8 cleanup) on one branch. Non-boundary.
- **Schema baseline:** unchanged. This carve-out is a pure refactor — **no migration.**
- **Fast Pest baseline:** carried from the 345 close (was 2799 + 345's net additions). **No new behaviour → ideally no new tests**; the existing `ContentImportExportTest` (≈2407 LOC) + the import/export Playwright specs are the behaviour gate. Add a targeted test only if a seam genuinely needs one.
- **Playwright:** the site import/export specs are the round-trip net; run them if the extraction touches a path they cover.
- **Planning state:** Cycle 3 — audit + apply done; this is carve-out A; carve-out B (347) + squash (348) follow.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Behaviour-preserving only.** No feature change, no schema change, no contract change. If the split tempts a behaviour change, stop and surface it — that's a separate pick, not this session.
- **Surface the extraction shape before going deep** (open gate): per-domain trait mixins on the existing classes vs separate injected collaborator classes (e.g. `WidgetTreeSerializer` + per-entity serializers/importers). This is an architecture choice that propagates — decide it with the owner first, don't commit-and-unwind.
- **The round-trip test is the net.** Run the full `ContentImportExportTest` after each extraction step; a green round-trip is the success signal. Keep `migrate:fresh --seed` + a real export→import round-trip in the loop.
- **Don't relitigate** the standing won't-fixes (the CSV-importer concerns are a different subsystem — out of scope here).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation this is a behaviour-preserving legibility split of the two bundle files, non-boundary, no schema; (2) the **extraction shape** decision — trait mixins vs injected collaborator classes, and whether Importer and Exporter share a `WidgetTree*` unit (they both serialize/hydrate the widget tree for pages + templates); (3) the iteration plan (suggest: Exporter first — its per-type serializers are near-mechanical — then Importer, which carries more risk). One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/346. Code Review & Cleanup (Carve-out — Bundle Import-Export Split) — Log.md` — the before/after LOC of both files + the extracted collaborators, and the round-trip verification.
- **Bump `VERSION`** to `0.346.<iteration>`.
- **Update** the track doc + outlines + release-plan Cycle-3 status (carve-out A done).
- **Next:** carve-out 347 prompt already drafted (re-verify against the post-346 tree). Do **not** draft 348 squash yet.
- **Archive** `sessions/345. *.md` → `sessions/archived/`.
- **Commit on `session-346/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: follow existing conventions exactly; the win is two files dropping well under 1k with a clean orchestrator→collaborator shape and a still-green round-trip — no behaviour change, no new abstractions beyond the extraction the split requires.
