# 034. Inactive Field Visual Indicator

Verify and complete the CSS rule that visually grays out disabled Filament form fields and their labels. Audit coverage across all forms that use `->disabled()` and add any missing cases. Small, focused session.
