# 342. Housekeeping — Infra Hygiene and Page-Builder Polish

A design-independent housekeeping batch: drain ~8–9 verified inbox items across dependency/config hygiene, page-builder operator-safety + copy, and two test-backfill gaps — chosen because the roadmapped tracks are gated on external/designer input and this work needs none.

---

## Stub reference

Not a `release-plan.md` entry. Drains the following verified items from `sessions/housekeeping-inbox.md` (capture-stamps shown; re-verify each at the walk per the staleness guard — session 341 pre-verified the core set as still live).

**Cluster 1 — Dependency & config hygiene**
- `[0.315.01]` / re-flagged `[0.320.01]` — **Remove Laravel Debugbar.** Installed (`barryvdh/laravel-debugbar ^3.13`, confirmed in `composer.json`), never used. Owner's explicit ask: the toolbar must never render on the **public site, in any environment**. Pull the dependency + any published config; prove the public outcome.
- `[0.319.01]` — **`.env` / `.env.example` audit pass.** Both stale. Confirmed at 341: the named dead vars (`APP_DEBUG_TOOLS`, `FLEET_MANAGER_AGENT_KEY`, `PUBLIC_BUILD_SERVER_API_KEY`, `WEBHOOK_SECRET`, `DEPLOY_FLEET_API_KEY`) have **zero** code references. Prune the dead set, reconcile the rest against actual `env()` consumption, document the **FM-provisioned vs standalone/self-host** split. (Full concrete findings list is in the inbox item — re-verify each before removing.)
- `[0.316.01]` — **Branded error pages.** Confirmed no `resources/views/errors/` directory exists; the app serves the framework default. Add branded `404` (and `403`, `500`).

**Cluster 2 — Page-builder operator-safety + copy**
- `[0.331.01]` — **Confirm before deleting a populated column.** On a layout column-count *decrease*, if a column being removed still holds content, alert the operator before destroying it (too easy to lose content as a side effect of an unrelated change). Corroborated by session 332's bug "E".
- `[0.317.01]` — **Disabled-checkbox affordance is hover-only.** In `resources/js/page-builder-vue/components/LayoutColumnSettingsTab.vue` the "Background fills page width" checkbox fades to disabled when "Content fills page width" is on, but the *why* lives only in a hover tooltip. Add an inline visible hint / disabled affordance.
- `[0.317.01]` — **Page form terminology overlap.** "Page Type" / "Page Template" / "Content Template" on the page create/edit forms (`PageResource` + `EditPageDetails`) read as near-duplicates; the "Content Template" helper ("applied once at creation") is vague. Copy/labeling pass — **not** a mechanism change.
- `[0.317.01]` — **Duplicate-action consistency.** The Duplicate affordance exists on Pages/Posts/Events/Products (and is mirror-able from those) but not on Organizations / Templates / Navigation / Mailing Lists. **Owner decision (settled): add it to all four**, mirroring the existing `PostResource`/`ProductResource` replicate action, for a uniform operator expectation.

**Cluster 3 — Test backfill** (deterministic, service-layer, no UI)
- `[test-integrity]` A005 D1 forward-queue gap — **invoice-details first-row-wins conflict resolution.** Add a Pest case to `tests/Feature/ImportFinancialTest.php`: given two rows in one invoice group with conflicting parent fields (e.g. `payment_method` Card vs Cash), assert the **first** row's value wins on the resulting Transaction. Currently unverified after the `invoice-details-grouping.spec.ts` deletion.
- `[test-integrity]` A005 forward-queue gap — **donation checkout-initiation path.** Add a Pest case (mocking the Stripe boundary) for the real create-pending-then-delete-on-Stripe-failure controller path that the deleted `DonationCheckoutTest` factory-echo never exercised. Target `tests/Feature/DonationCheckoutTest.php` and the donation checkout controller.

**Optional stretch (design-light; in only if scope allows — confirm at Open Gate)**
- `[0.329.02]` — **EventMiniCalendar internal margins collapse** as the widget narrows. Self-verifiable CSS spacing fix. (Widget still present; confirmed.)
- `[post-milestone]` — **Random Data Generator: add Organizations** to the entity-generation list. Small admin-tool addition; `post-milestone`-tagged, so genuinely optional.

---

## Settled decisions

- **Duplicate action — all four.** Owner confirmed: add the Duplicate affordance to **Organizations, Templates, Navigation, and Mailing Lists** (mirroring the existing `PostResource`/`ProductResource` replicate action) for a uniform operator expectation. No open questions remain for this batch.

---

## Phases

Independently shippable; commit per cluster.

### Phase 1 — Dependency & config hygiene
Remove Laravel Debugbar (dependency + config + prove public-site absence); prune + reconcile `.env` / `.env.example` and document the FM-vs-standalone split; add branded `404`/`403`/`500` views mirroring an existing public Blade layout.

### Phase 2 — Page-builder operator-safety + copy
Column-delete-with-content confirm guard in the page-builder Vue column path; inline disabled hint on the "Background fills page width" checkbox; page-form terminology copy pass; extend the Duplicate action per the Open question. Run `npm run build` once the Vue changes land.

### Phase 3 — Test backfill
The invoice-details first-row-wins Pest and the donation checkout-initiation Pest. Service-layer, deterministic; run the fast suite to confirm green.

---

## Out of scope

- **Design-gated inbox items** — slide/popout repeater editor, Logo-widget rich-text + appearance, link-color mechanism consolidation (tied to the G12 hero↔nav decision), the image-editing toolbar (its own future release-plan entry), BoardMembers→people-list, full-size text editing, columns alignment-preset defaults. These need a design call or owner decision and are deliberately held.
- **The two Playwright e2e guards** (ordered/unordered list rendering; widget "knockout" controllability) — e2e-authoring work for a dedicated test-hardening pass, not this batch.
- **Any `/api/*` Fleet Manager contract change** — none; no `CONTRACT_VERSION` bump; CRM stays v2.3.0.
- **Schema changes** — none expected; flag immediately if any item turns out to need one.

---

## Testing

- **Slow groups:** none.
- **New Pest:** yes — the two Cluster 3 backfill cases, plus a test for the column-delete confirm guard and the new Duplicate actions where behaviour changes. Pure copy / config / Blade-view items skip tests.
- **Front-end build:** `npm run build` is required (Cluster 2 edits `resources/js/page-builder-vue/**`). No `build:public` expected (no widget asset/SCSS change) unless the EventMiniCalendar stretch item touches widget SCSS — then `build:public` per the build rule.
- **CI/deploy:** none of this is CI-pipeline-affecting (the deploy workflows are not touched). The `.env.example` change is documentation/config, not workflow.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file:** `sessions/342. Housekeeping — Infra Hygiene and Page-Builder Polish — Log.md`
- **Branch:** `session-342/N`
- **Inbox:** fold every drained item out (record under "Recently dispositioned"); leave the design-gated, post-milestone, and e2e-guard items in place.
- **Archive:** `sessions/341. *` → `sessions/archived/`.
- **Next session:** owner confirms at close.
