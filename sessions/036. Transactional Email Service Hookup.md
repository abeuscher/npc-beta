# 036. Transactional Email Service Hookup

Add admin settings for connecting an external mail provider (SendGrid, Postmark, or Mailgun). Replace the default SMTP config with a provider-backed connection configurable from the admin without touching `.env`.
