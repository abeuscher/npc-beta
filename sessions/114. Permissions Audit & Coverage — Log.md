# Session 114 — Permissions Audit & Coverage — Log

**Date:** 2026-04-02
**Branch:** session-114/1 … /2

---

## What was built

### Phase 1 — Missing policies

All five policies (Event, Form, Product, MailingList, FormSubmission) were already created and registered in AuthServiceProvider during iteration 1. Verified complete.

### Phase 2 — Custom action permission checks

Added `->hidden()` UI checks and `abort_unless()` server-side guards to 10 custom actions across 6 files:

- **ImporterPage** — `approve` and `rollback` actions gated behind `review_imports`
- **DonorsPage** — `sendReceipts()` gated behind `manage_donations`
- **EditMailingList** — `syncMailchimp` gated behind `update_mailing_list`
- **RoleResource** — delete action and bulk delete gated behind `isSuperAdmin()`
- **EventResource** — bulk delete gated behind `delete_event`
- **UserResource** — bulk delete gated behind `delete_user`
- **FundResource** — bulk delete gated behind `delete_fund`

### Phase 3 — Livewire method hardening

All three Livewire components (PageBuilder, PageBuilderBlock, PageBuilderInspector) already had `assertCanEdit()` with `update_page` permission on every write method. Verified complete from iteration 1.

### Phase 4 — canDelete permission coverage

Added permission checks alongside business logic in 4 `canDelete()` overrides:

- **TemplateResource** — `update_page`
- **MembershipTierResource** — `isSuperAdmin()`
- **WidgetTypeResource** — `delete_widget_type`
- **TransactionResource** — `delete_transaction`

### Additional fixes from manual testing

User-reported gaps found during read-only role testing:

- **ListContacts** — Import action gated behind `create_contact`, Export CSV gated behind `view_any_contact`
- **MemberResource** — Edit button gated behind `update_contact`, Delete/Restore behind `delete_contact`, ForceDelete behind `super_admin`
- **ProductPricesRelationManager** — Create, Edit, Delete all gated behind `update_product`
- **View Transactions buttons** (EditContact, EditProduct, EditEvent) — all gated behind `view_any_transaction`
- **NavigationMenuResource** — added `canCreate()`, `canEdit()`, `canDelete()` using `navigation_item` permissions
- **TemplateResource** — added `canCreate()` (`update_page`) and `canEdit()` (`update_page` or `edit_theme_scss` or `edit_site_chrome`)
- **TransactionResource** — added `canCreate()` (`create_transaction`), `canEdit()` (`update_transaction`), and permission visibility on table Edit/Delete actions

---

## Security checklist — verified

- [x] Every model exposed through a Filament resource has a registered policy
- [x] Every custom action that writes data checks permissions (UI + server-side)
- [x] Every Livewire write method checks permissions server-side
- [x] canDelete() overrides include permission checks alongside business logic
- [x] All existing tests pass

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`.

---

## Deferred decisions

- None

---

## Test result

553 passed, 0 failed.
