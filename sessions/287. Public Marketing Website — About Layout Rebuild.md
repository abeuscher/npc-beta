# 287. Public Marketing Website — About Layout Rebuild

Rebuilds the About page against `sessions/public website/about-layout-spec.md` using the CMS surface area resolved at 285 and the spec-driven execution pattern validated at 286. The layout spec is canonical for cell-by-cell structure, image placements, padding, and heading sizes; this session executes it.

---

## Stub reference

No release-plan entry exists at session start. The session is driven by `sessions/public website/about-layout-spec.md` (authored by an outside agent before session start, mirroring the pattern that worked for the home at 286). The existing About page is exported by the user before session start as `sessions/public website/existing-about-page-export.json`. A release-plan entry for this session lands at 286's close handoff or — if not — gets added retroactively at 287's close.

Track context: this session sits between session 286 (Home Layout Rebuild) and the next phase of the Public Marketing Website track. Track doc at `sessions/tracks/public-marketing-website.md` carries the broader plan. The layout-spec approach validated at 286 — clean import on first pass, eight bands in spec order, cross-section rhythm matched the spec's prediction. The same shape repeats here for About; if it continues to work, Pricing / Contact / Demo follow the same pattern.

---

## Pre-session inputs (user-supplied before session start)

- **`sessions/public website/about-layout-spec.md`** — band-by-band structural spec for the About page, authored by the outside agent in the same shape as `homepage-layout-spec.md`. Specifies columns, backgrounds, padding, heading sizes, image cells, and cross-section rhythm. **Required.** If absent at session start, stop and ask.
- **`sessions/public website/existing-about-page-export.json`** — the current About page exported from admin → CMS → Pages → Export. Treated as ground truth for the existing structure being superseded. **Required.** If absent at session start, stop and ask the user to export.

If either is absent, do not improvise — pause and ask. The session shape depends on both being present.

---

## Session-specific deltas

- **The layout spec is canonical for layout interpretation; `copy.md` is canonical for words; `brief.md` system conventions remain in force.** Authority order per the spec's "Authority" section, mirroring 286's pattern.
- **Five gap-report rows resolved at 285 + four surfaced at 286.** G1 / G2 / G3-interim / G13 / G14 stay ✅ resolved (preconditions for the spec's directives). G15 / G16 / G17 / G18 remain open — the About spec may exercise them again; if so, note continued usage rather than re-flagging as new gaps.
- **The current `about.json` (if it exists from 286 or earlier) gets superseded.** The original `sessions/public website/existing-about-page-export.json` stays untouched.
- **Image placeholders use the sample image library.** Per the spec's image-cell count and labels. Browse `resources/sample-images/` and pick fits that don't try to anticipate the eventual real-photo aesthetic. Tag each placeholder with the spec's label in the widget label or a custom field so the eventual photo swap is mechanical.
- **Hero-cell carrier defaults to Text widget + CTAs** (286's revision-pass outcome). Use Hero widget only if the About spec explicitly calls for it — at which point flag continued G15 / G16 manifestation in the work log.
- **Links to in-product demo LPs.** Per the brief, About is the page that points to `/my-nonprofit` and `/my-nonprofit-workshop`. Confirm those slugs resolve in the current install before serializing — if they don't, log a gap and use the brief's named slugs as link targets anyway (the eventual demo content's URL is the user's call).

---

## Open questions to resolve at session start

- **Image placeholder picks across the spec's image cells.** Sample image library has 11 portraits, 9 still-photos, 10 product-photos, 14 logos. The About page is the founder-narrative page — portraits and still-photos are the likely fits. Pick fits that don't try to anticipate the eventual real-photo aesthetic; the brief is explicit on this point.
- **Hero-band carrier choice.** Default to Text widget + CTAs per 286's revision-pass outcome. Confirm before serializing the hero band only if the About spec calls for Hero widget specifically.
- **Demo-LP link slugs.** Confirm `/my-nonprofit` and `/my-nonprofit-workshop` resolve in the local install. If not, surface to the user before serializing the relevant band.

---

## Out of scope

- **Pricing / Contact / Demo / Home work.** Track phases after this session (Home was 286).
- **Real photography.** Sample image library as placeholders. Eventual photos land post-track.
- **Real `mailto:` address.** Same placeholder `mailto:al@example.com` carries forward across all pages.
- **Gap-resolution implementation.** Same as 286: gaps surfaced for discussion, not unilaterally resolved. Each gap that warrants a code change becomes its own follow-on session if the user approves.
- **The Playwright page-capture harness.** Scheduled for the track's close-out phase. Informal screenshot suffices this session.
- **The two in-product demo LPs themselves.** This session adds links to them from About — it does not author the LPs.
- **New widgets, new button variants beyond G3 interim, typography re-tuning beyond G13/G14.** All in gap-report-and-lift territory.

---

## Testing

- **Slow test groups to run this session:** none.
- **New tests expected:** no — content-only session per the base-prompt convention.
- **Regression check:** fast Pest suite after the about re-import (target: 286 close baseline of 2407 / 1, where the 1 failure is the pre-existing `DashboardBuilderApiControllerTest > it rejects widg…` that pre-dates this session). Single run, not a watch loop.

---

## Closing steps

Standard close gate. Session-specific details:

- **Log file:** `sessions/287. Public Marketing Website — About Layout Rebuild — Log.md`.
- **Artifact:** new `sessions/public website/about.json` (rebuilt against the layout spec), updated screenshot at `sessions/public website/about-after-rebuild-screenshot.png` (or equivalent), updated `sessions/public website/gap-report.md` with any new rows surfaced.
- **Update `sessions/release-plan.md`:** mark session 287's entry ✅ closed at session 287.
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status.
- **Update `sessions/completed-sessions.md`:** append session 287's row.
- **Archive the previous session:** move all `sessions/286. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-287/N` branch. Notify the user. Do not push.
