# Base Prompt — Session 361 (Demo Product Tour — Membership-Step Framing Fix + Flow Review & Persistent Controls)

**Start now.** Execute the reading list below, open the session prompt at `sessions/361. Demo Product Tour — Membership-Step Framing Fix + Flow Review & Persistent Controls.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **361. Demo Product Tour — Membership-Step Framing Fix + Flow Review & Persistent Controls.**

This is **emergent / urgency-driven, not a `release-plan.md` entry.** It promotes the housekeeping-inbox item `[post-milestone] [0.346.01 · 2026-06-07]` — *"Demo tutorial tour — the membership highlight panel step is mispositioned (highlight off-target and sitting too low) in the driver.js guided tour (session 338) inside the demo"* — into a built fix, **and** widens it to a whole-tour usability pass. The forcing function is live: **prospects are evaluating the product (and the owner's business plan) on the demo node right now, and the guided tour is confusing/broken as-is**, so it is the priority. It sits downstream of the **Guided Product Tour** work (session 338, the driver.js framework + 11-step demo walkthrough) and the (paused) **Public Marketing Website / demo-site** arc. It is **distinct from** the post-Beta "in-app contextual tours via the help system" backlog item — that is a *different* feature (per-view help tours); this session is the existing **338 product tour**.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope (the tour is front-end JS + Blade anchors + Filament widget timing; the FM `/api/*` path is untouched). CRM contract stays at **v2.5.0** (set by session 360). No Cross-Repo block or `docs/fleet-manager-agent-contract.md` update.

**Session shape:** **feature-style / UX-fix, front-end-dominated, flag-driven (s304-style inbox item) + urgency-driven, Playwright-verified.** Fix the membership step's async framing race (the highlight lands before the panel's layout settles), then do a whole-tour flow review and add a **second, persistent set of tour controls** (always-visible Back / Next / Exit) so a mispositioned popover can never strand a user — reworking or adding steps where the flow is illogical.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is **emergent, fuller-shaped** (phases, open questions, out-of-scope, drift signals).
2. Read `sessions/session-outlines.md` — the **Active tracks** block (the demo-site / Public Marketing Website arc is **paused**; this tour fix is a live-urgency carve-out), the **post-Beta "in-app contextual tours"** backlog item (so you keep it **distinct** from this session's 338 product tour), and the **Housekeeping Inbox** section. Then read the membership item in `sessions/housekeeping-inbox.md` (`[0.346.01]`) — this session resolves it.
3. **No `release-plan.md` entry.** Read the **session 338 design intent**: `sessions/archived/338. Guided Product Tour — driver.js framework + demo CRM walkthrough.md` (+ its `— Log.md` if archived) — especially the **owned `data-tour` anchor convention** (never rely on Filament `.fi-*` classes), the **multi-page engine + localStorage resume**, the **Livewire-morph robustness** (live CSS-selector re-query for self-target anchors), the **role-gated URL map** (steps skip pages the viewer's role can't reach), and the **demo-safe showcases** (Roles / Import). Note any limitations flagged at 338 close.
4. Read `docs/app-reference.md` for the **front-end build command** (the tour JS is `resources/js/admin/tour/**`, compiled via Vite → `npm run build`), the **e2e Playwright runner** (`npm run test:e2e` against the isolated stack — see also agent memory `reference-e2e-playwright-runner` and `reference-frontend-build-command`), the **demo node** (`APP_ENV=demo`, `isDemoMode()`, container names), and the test runner.
5. **Read the tour system (confirm against code):**
   - `resources/js/admin/tour/index.js` (launch delegation + resume hooks), `controller.js` (the multi-page driver engine; `onHighlightStarted` / `onPopoverRender` / `onNextClick` / `onPrevClick`, the `buildStep()` button config, the live-selector re-query, stale-spotlight cleanup), `state.js` (localStorage `np-tour` resume), `anchors.js` (`resolveAnchor` / `waitForElement` / `ensureNavVisible` — the 8 s / 80 ms poll that resolves on element **existence**, the suspected root cause of the membership race), and `tours/product-tour.js` (the **11-step** definitions + copy).
   - The membership step's anchor in `resources/views/filament/widgets/record-detail-view.blade.php` (`data-tour="record.membership"` on the `membership_status` cell) and `app/Filament/Widgets/RecordDetailViewWidget.php` (`$isLazy = false`, eager render — present but evidently not sufficient to settle geometry before the highlight).
   - `app/Providers/Filament/AdminPanelProvider.php` — the render-hook `data-tour` injections (`page.content`, `resource.records`, `topbar.view-site`) and the `HEAD_END` **role-gated `window.__npTour` URL map**.
   - The launcher: `app/Filament/Widgets/DashboardWelcomeWidget.php` + `resources/views/filament/widgets/dashboard-welcome-widget.blade.php` (`data-np-tour-start`). The showcases `app/Filament/Pages/Tour{Roles,Import}ShowcasePage.php`.
   - The tests: `tests/e2e/tour/product-tour.spec.ts` (the **standing** regression spec that walks all 11 steps, interactivity, cross-page resume — you'll extend it) and `tests/Feature/TourShowcasePagesTest.php` (showcase access + lockdown).
6. **Confirm the e2e runner discipline** (agent memory `reference-e2e-playwright-runner`, `feedback-test-runs-not-parallel`): use `npm run test:e2e`; run **Pest and Playwright sequentially, never in parallel** (they collide on DB/FPM); `docker cp` / rebuild the build into the e2e `public/build` volume after a front-end change so the isolated stack serves the new bundle.

---

## Starting state inherited from session 360

- **CRM contract at v2.5.0** — session 360 bumped it **v2.4.0 → v2.5.0** (additive: the `POST /api/admin/recover` operator-mediated admin-recovery endpoint + break-glass `admin:recover` CLI + activity-log audit; fixed the vestigial `config/activitylog.php` `table_name` `activity_log` → `activity_logs` bug). **361 is non-boundary** — contract stays v2.5.0; no contract-doc or Cross-Repo touch.
- **Schema baseline:** 360 added **no migration** (the recovery audit rides the existing `activity_logs` table via `ActivityLogger`). The last migration remains 359's `users` 2FA columns. **361 expects no schema change** (front-end / Blade / widget-timing only).
- **Fast Pest green at 2977 / 0** (360 close, `--parallel --exclude-group=slow`). Expected delta this session is **small** — the work is front-end; verification is **Playwright** (the membership-framing repro/fix + the new persistent controls + any added/reworked steps update `tests/e2e/tour/product-tour.spec.ts`). A Pest case may land if a widget render-timing change needs a server-side guard, but none is expected.
- **Playwright suite** — `tests/e2e/tour/product-tour.spec.ts` is the **standing, non-quarantined** tour regression spec; this session **extends** it (new controls, any new/reworked steps) and adds the **membership-framing verification** (screenshot the highlighted element vs the popover under simulated load lag). **Local Playwright development is warranted here** — the owner explicitly wants the agent to use Playwright + screenshots to verify the membership framing and walk the whole flow; this is active-spec-development + objective-browser-verification, the sanctioned local-Playwright case (the close-gate is still `./dev test` / the fast Pest suite, run sequentially after any Playwright run).
- **Planning state:** 360 promoted the s304 admin-lockout flag (CRM half). **361 is not a release-plan entry** — it is the `[0.346.01]` membership-mispositioning inbox item promoted under a live-evaluator forcing function, widened to a whole-tour pass. The demo-site / Public Marketing Website arc stays **paused**; this is a targeted carve-out, not a resumption of that arc.
- **Housekeeping inbox** (`sessions/housekeeping-inbox.md`) — **not a housekeeping session;** the inbox is **not** drained (Rule 2). The one exception is the `[0.346.01]` **membership-panel-mispositioning** item, which is *this session's subject* — fold its resolution at close. (If the whole-tour pass also clears the other `[0.346.01]` tour item or surfaces new ones, note them at close, don't pre-drain.)

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Surface the architectural choices at the Open Gate, build after.** The **iteration split** (urgent membership fix first, deployable; flow + persistent controls second), the **membership-race fix mechanism**, the **persistent-controls shape** (a fixed bar — position/styling/whether it supplements or replaces the popover buttons), and **which steps get added/reworked** all propagate or need owner taste — confirm the recommended leans before going deep, don't commit-and-unwind. The step-copy / step-set changes are **content/UX the owner judges** — bring a Playwright storyboard (a screenshot per step) and the proposed changes, get approval, then edit.
- **Verify the membership framing objectively, with Playwright + screenshots, under simulated lag.** The owner's explicit ask: first reproduce the misposition (the panel's geometry isn't settled when driver highlights), accounting for loading lag / the async race — throttle the render (slow the network/CPU or exploit the lazy/Livewire delay) so the race surfaces reliably — capture before/after screenshots, fix, and re-verify. Don't pull the owner in to confirm "is it framed right" — Playwright sees the bounding box; close that loop yourself. Pull the owner in for the **flow/copy judgment** (does the tour make sense, is the wording right).
- **Front-end build is mandatory before announcing completion.** The tour JS lives in `resources/js/admin/tour/**` → `npm run build` (Vite admin bundle; use the WSL-path npm per agent memory `reference-frontend-build-command`). For Playwright against the isolated e2e stack, rebuild + push the bundle into the stack's `public/build` volume (agent memory `reference-e2e-playwright-runner`) or the spec runs against a stale tour.
- **Pest and Playwright run sequentially, never in parallel** (agent memory `feedback-test-runs-not-parallel`) — they collide on DB/FPM and take the local site down; `docker compose restart nginx` recovers stale upstreams.
- **Keep the 338 invariants.** Anchors stay **owned `data-tour` attributes** (never Filament `.fi-*` classes); the multi-page engine + localStorage resume + Livewire-morph live-selector re-query stay intact; steps still **skip pages the viewer's role can't reach** (the role-gated URL map); the demo-safe **showcases stay demo-safe** (no real data/secrets). A persistent control bar must not break cross-page resume or leak past tour-exit.
- **If the owner signals a deploy-to-test** at any point, **bump `VERSION` to `0.361.<iteration>` proactively** (agent memory `feedback-deploy-version-bump` — `deploy.yml` rejects a duplicate VERSION) and flag it; the demo node updates via `release/public-demo` / the demo workflow per `docs/app-reference.md`. (No need to ship the membership fix ahead of the rest — the session lands as one coherent piece unless the owner asks otherwise.)

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the **scope** (membership-step framing fix + whole-tour flow review + a second, persistent set of tour controls; reworking/adding steps as the review finds; the post-Beta in-app contextual-help tours stay out); (2) the **iteration split** with your recommended lean (a cohesive session — e.g. the membership async-framing fix + Playwright verification, then the persistent controls + flow review + step rework — landing together at the normal session cadence, **not** an urgent ship-the-fix-first split); (3) the **membership-race fix mechanism** (your read of the root cause from the code + the wait-for-settled-geometry-then-`driver.refresh()` direction, confirmed by a Playwright repro under lag); (4) the **persistent-controls shape** (recommended: a fixed bar with Back / Next / Exit + "Step X of Y" — confirm position/whether it supplements the popover buttons; this needs owner/designer taste); (5) the **flow-review method** (a Playwright walkthrough that screenshots each step into a storyboard, flags illogical transitions, and proposes step add/reword changes for owner approval before editing copy); (6) whether anything else is in/out (e.g., auto-start on demo arrival — a 338 deferred decision, recommend **out** unless asked). One confirmation, then proceed.
