# Base Prompt — Session 230

We are about to begin a new session: **230. Widget Primitive — Phase 5c.5 (Admin UI for Authoring per-Record-Type View Sets)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top and the Beta 1 stubs.
3. Read `sessions/tracks/widget-primitive.md` (the canonical Widget Primitive track doc — premise sits alongside at `widget-primitive-premise.md`). Specifically:
   - Status snapshot (top of doc)
   - Phase Retrospectives — at minimum 5c (just landed) and 3d (215, the Dashboard Config UI pattern this session mirrors most closely). Skim 3a (212) for the dashboard-track Filament-hosting pattern and 4f (222) for the SOURCE_SYSTEM_MODEL retrofit shape — the latter is forward context for the 5d+ concrete widgets that follow this session.
   - "Phase 5" entry under "The 2.0 transition — rough sequence," especially the 5d+ bullet
   - "Pending paradigm questions" section — Phase 5e (permissions) and the widget-help gap. **Both are explicitly out of scope for 5c.5**; named so you don't accidentally bake the wrong assumption in.
   - "Design decisions locked in" — particularly the **"a+" View framing** entry. The IsView interface, polymorphic owner discipline, source-as-capability, and concentrated enforcement at the resolver are all load-bearing for this session's authoring surface.
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, plus `docs/schema/record_detail_views.md`, `docs/schema/dashboard_configs.md`, and `docs/schema/page_widgets.md`. **This session writes no migrations** — it lands an authoring UI on top of the schema 5c shipped.
5. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/archived/229. Widget Primitive — Phase 5c (record_detail_views Table + IsView Registry + Sub-Nav Rendering Primitive) — Log.md` — establishes the `record_detail_views` table, the `RecordDetailView` Eloquent model, the `ViewRegistry` singleton, the sub-nav primitive trait, and the Templates/Themes refactor. **5c.5 lands the admin UI for adding / removing / reordering / configuring widgets inside a View — the page-builder Vue editor mounted in a new "record_detail" mode.**
   - `sessions/archived/215. Widget Primitive — Phase 3d (Dashboard Config UI & Storage) — Log.md` — establishes the closest precedent pattern. The session mirrors 215's shape: API controller (allowedSlots check on create + appearance_config keys filtered to slot's allowed fields + IDOR guard scoped to owner), routes file, Vue page-builder mode, Filament page that mounts the Livewire shell, Permission seeder. The deltas this session introduces over the 215 pattern are named in the session prompt's design-decisions block.
   - `sessions/archived/214. Widget Primitive — Phase 3c (Dashboard-Native Widgets) — Log.md` — establishes the slot-aware widget-picker pattern (`WidgetType::forPicker(?string $pageType, ?string $slotHandle = null)`), reused this session by passing `'record_detail_sidebar'` as the slot filter.
6. Read the full state of every file this session edits or creates (see the session prompt's Phase descriptions for the file list).
7. Read related existing tests to anchor patterns:
   - `tests/Feature/DashboardBuilderApiControllerTest.php` — the API controller test mirror; 5c.5's controller test mirrors this 1:1 with `record_detail_sidebar` and `manage_record_detail_views` swapped in.
   - `tests/Feature/DashboardSettingsPageTest.php` — the Filament page test mirror.
   - `tests/Feature/EditPageTemplateSubNavigationTest.php` — establishes Livewire-mounted page tests against record_detail_views; pattern reused for the new authoring page.
8. Open `sessions/230. Widget Primitive — Phase 5c.5 (Admin UI for Authoring per-Record-Type View Sets).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. **This session writes no migrations** — `record_detail_views` already exists from 5c.
- **Update `docs/schema/`** after writing any migration — none expected this session.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session touches `resources/js/page-builder-vue/` (new editor mode + a few component branches) — `npm run build` is required before announcing completion.** No widget asset pipeline files, so no `build:public`.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands a new API controller, a new Livewire shell, a new Filament page (or Resource), and new Vue branches — every piece gets test-locked.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/231. base-prompt.md`. Expected next session: **Phase 5d-1 — first concrete record-detail widget (Recent Notes for current contact)**. This is the "earn the primitive" milestone — contract design + projector + test-lock pair, mirror of Phase 4a. May force Phase 5e (permissions in the contract layer) decisions if Notes carry permission gates beyond resource-level.
- **Next session prompt**: draft `sessions/231. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/230. Widget Primitive — Phase 5c.5 (Admin UI for Authoring per-Record-Type View Sets) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 230's row.
- **Update `sessions/session-outlines.md`** (the roadmap): the Active-Tracks block's Widget Primitive entry needs its remaining-session count bumped (one fewer; ~5–7 left).
- **Update `sessions/tracks/widget-primitive.md`**: bump the status snapshot (last update date, what's complete, what's active). **Phase 5c.5 is a follow-up to 5c, not a phase terminus** — append the session detail to 5c's compressed retrospective entry rather than writing a new one. The next phase boundary is 5d-1 / 5d when concrete widgets start landing.
- **Archive the previous session**: move all files matching `sessions/229. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines/track docs, the completed-sessions index, and any files moved into `sessions/archived/`), commit on the current `session-230/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
