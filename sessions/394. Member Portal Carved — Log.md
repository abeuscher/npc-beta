# Session 394 — Member Portal Carved — Log

**Date:** 2026-08-20
**Branch:** session-394/1

---

## What was built

Arc D5 of the Plugin Architecture arc, first session of the carve → package → extract block — the Member Portal behavioral surface carved into `plugins/MemberPortal/`, refilling the `plugins/` directory D4's extraction emptied. The 389/392 carve playbook plus two edges no prior carve had: the portal owns **auth infrastructure** (guard, user provider, password broker, middleware alias — all moved plugin-side), and a **sibling extracted plugin depends on it** (`crm-plugin--events`' portal registration route — resolved with surface 13's second shipping capability and an Events v0.1.2 rider). Behavior-preserving throughout: routes 272/272 byte-identical against main (method+URI+name+middleware; the only diff is the moved middleware class FQCN behind the unchanged `portal.auth` alias — the 381 action-FQCN exception), fast suite 3355 → **3384/0** purely additive, no schema change, no visual change. One iteration, one core commit (`9b156687`) plus the rider commit in the sibling repo (`a72e171`, tagged `v0.1.2`).

### Phase 1 — The carve

- **Plugin shape:** `plugins/MemberPortal/` + `MemberPortalServiceProvider` (namespace `Plugins\MemberPortal\`, handle `member-portal`); `distribution.json` gains the sixth entry, source kind `folder` (+ `source.path`), appended last; `plugins:manifest-sync` regenerated `config/plugins.php` with the sixth provider line. The 388 tolerate-empty guard relaxations tolerated the refill by construction — zero guard edits.
- **Moved with namespace change, behavior identical:** the six controllers (`LoginController`, `SignupController`, `ForgotPasswordController`, `ResetPasswordController`, `EmailVerificationController`, `AccountController`) to `plugins/MemberPortal/Http/Controllers/`; all 17 portal routes to the plugin's route file (web group; GET pages under `system_prefix`, root-path POSTs, throttles, the `signed` verification link, the `portal.account` legacy-redirect closure — registered at provider `boot()` ahead of the page-slug catch-all, names/URIs/middleware byte-identical); `PortalAuthenticate` to `plugins/MemberPortal/Http/Middleware/`; the six widgets nested under `Widgets/` (`template()` overrides via `plugin-member-portal-widgets::`; both `script.js` asset paths re-pointed); the `PortalEmailChange` mailable + its blade; the three portal views (`verify-email`, `reset-password`, `forgot-password-sent`) to the `plugin-member-portal::` namespace.
- **The auth stack moved per the ratified disposition (decision 2):** the `portal` guard + `portal_accounts` provider + password broker injected into `config('auth.*')` from the provider's `register()` (static values — the Payments config-injection posture without the DB guard); the `portal.auth` alias registered in `boot()` via `Route::aliasMiddleware` — **the first plugin-owned middleware alias**. `config/auth.php` and `bootstrap/app.php` keep no portal entries. Open question 3 confirmed at the parity diff: the framework-owned `verified:portal.verification.notice` parameter survives the alias moving plugin-side.
- **The `member-portal` capability (surface 13's second):** declared at `register()`, `fn () => true` — presence-is-enablement (no credential dimension). Consumers: the five gated core reads (below) and the Events rider.
- **Five core `auth('portal')` reads capability-gated** — logged drift: the survey named two, but `PageContext` (constructed on every public page render), `PageController`'s member-page branch, and `ActivityLogger`'s actor chain also touch the guard, and an undefined guard **throws** — the removal mirror would have tripped all three immediately, so all five were gated at the carve (the fix-what-the-mirror-exercises discipline): `PageContext` → null user; `PageController` member branch → **404 on portal-absent** (the minimal vanish stub; full page-topology work is package-phase); `ActivityLogger` → the portal branch short-circuits (its try/catch would otherwise have silently swallowed ALL system-actor log rows portal-absent); `FormSubmissionController`'s collision block self-skips; the `portal_member` resolver arm returns `['item' => null]`.
- **The Events rider — `crm-plugin--events` v0.1.2** (the 389 rider mechanics: contract wording → sibling edit → tag → core lock bump same-commit, `^0.1` constraint unchanged): `portal.events.register` registers only when `present('member-portal')`. Laravel resolves middleware aliases at dispatch, so with the portal stripped the route would have 500'd on the unresolvable alias; now it vanishes to the catch-all (405/404 — the established shape). Events-without-Portal joins the presence matrix (core-side, in the removal mirror — the fixture lives core, so the assertion landed where the fixture is).
- **Rule-5 membership pass (the reviewed split):** new phpunit `MemberPortal` testsuite at `plugins/MemberPortal/tests` — **1 subject file moved** (`PortalAuthTest`, 15 tests — login/signup/reset/verification/scoping) **+ 1 block split** (the two free-signup tests lifted out of core's `PaidCheckoutTest` into a plugin `PortalSignupTest` — their subject, `SignupController`, moved this session; the 392 pass had kept them core precisely because it was then core). **Stayed core** (portal as fixture / core subjects): `PortalPagesSession337Test` (subject: the core page seeders), `MemberPageWidgetScopingTest` (subject: core PageController gating + resolver scoping), `FinancialSourceWritePathTest`, the widget-engine parity/arms/demo/picker files, `ActivityLogTest`, `FormSubmissionTest`. **Reduction pass: 0 deletions** — no strict duplicates found. No core test carried an `App\Widgets\Portal*` class import (all reach by handle) — zero import re-points needed.
- **New tests (+29 fast):** enabled twin `MemberPortalPluginSession394Test` (provider/capability/injected auth config/alias/all-17-routes/catch-all ordering/widgets/Events-route/view namespaces), removal mirror `MemberPortalPluginRemovalSession394Test` on `MemberPortalRemovedTestCase` (surface vanish incl. the Events route, POST paths 405, data kept, and **all five gated reads exercised** — public page renders, member page 404s, arm nulls, activity logs a system actor, a colliding form submission lands with no mail), flag-vanish `MemberPortalDisabledViaFlagSession394Test` on its fixture, boundary guard `MemberPortalModuleBoundaryTest` (zero `Plugins\MemberPortal\` references in `app/`, zero allowlist, positive control). Twin fixtures carry the expiring no-migrator-path caveat (schema is core until the package phase — the 392 wording).
- **Docs:** plugin contract **0.18.0** (surface 1 plugin-owned-guard note; surface 4 first plugin-owned alias + the sibling-alias-gating rule; surface 13 second capability + the dependency-rule amendment — optional dependencies point at *capability providers*, not only foundation plugins, mediation by the core registry being what keeps verticals decoupled; surface 7 `portal_password_reset` wording amended "permanently" → model-adjacent; changelog); plan doc arc header + D5 line annotated (block open, carve done); `docs/app-reference.md` six portal rows + the auth-flow paragraph re-pointed; `docs/runbooks/permission-matrix.md` portal-controllers line re-pointed.
- Count/list updates (additive): `PluginActivationTest` ×2 (disable-all list + the unknown-handle message's valid set), `LogoGardenPaymentsDisabledViaFlagSession384Test` (superset count 5 → 6).
- 395 prompts drafted post-verification (owner-approved): Member Portal Packaged & Extracted — the one-session block closer per decision 8 (below), redraw input and composition-safety survey front-loaded from live code.

### Verification

- Route parity: **272/272** on method+URI+name+middleware (stash-capture diff against main; sole diff: `App\Http\Middleware\PortalAuthenticate` → `Plugins\MemberPortal\Http\Middleware\PortalAuthenticate` behind the unchanged alias — the 381 action-FQCN exception).
- Portal-absent boot proven live: `PLUGINS_DISABLED=member-portal` → 254 routes (272 − 17 portal − 1 Events rider), no guard config, capability absent, clean boot.
- Gate 4: `widgets:sync` (41 rows; both portal rows carry the plugin paths) → `build:public` → bundle proven to carry the `portalSignup` component and the change-password script (`portal-pw-form` / `NPPasswordMismatch`).
- Dev-stack smoke: home + `/system/{login,signup,forgot-password}` + `/admin/login` 200 (signup renders the tier picker — the traveling seam gates intact); `/members` 302; POST `/login` 419 (CSRF — the plugin route live); a portal widget thumbnail serves from the plugin folder.
- Composer: `validate --strict` clean; lock pins `nonprofitcrm/events` v0.1.2 at the exact tag commit, dist zip.
- CI: **`Tests` aggregate green** on the pushed branch (fast + slow — the authoritative signal; Playwright non-gating).

### Notable drift from the survey (adapted, logged)

- **Two of the three mailables stayed core.** The survey said the three portal mailables travel, but `PortalEmailVerification` and `PortalPasswordReset` are sent by core: `PortalAccount`'s framework hook methods (`sendEmailVerificationNotification` / `sendPasswordResetNotification` — the model stays core, § 6.7) and `EditContact`'s grant-access invite. Moving them would put plugin-namespace references in `app/` — exactly what the zero-allowlist boundary guard bans. They stayed core as model-adjacent (the `PortalFormCollision` logic: core sender ⇒ core mailable); only `PortalEmailChange` traveled (sole sender: the traveling `AccountController`). Cheap to revisit if the model ever moves (D12); recorded in contract surface 7.
- **Five gated guard reads, not two** (detail above) — `PageContext`, `PageController`, `ActivityLogger` were the survey's misses.
- The route count is 17 named portal routes, not "~18".
- Two `.gitkeep` files (the emptied `app/Http/Controllers/Portal/` and `resources/views/portal/` dirs) removed with their directories.

---

## D5 block handoff (what the package + extract session — 395 — inherits)

1. **Block shape decided (decision 8): one combined session** (the D4/393 compression precedent) — justified by: a two-table redraw with one sequence (the donations mechanics, twice proven), extraction edges five-times proven with zero runbook deltas in the last two runs, and the composition-safety hunt pre-surveyed. Fallback recorded in the 395 prompt: if the hunt exceeds its survey, stop honestly after package + redraw.
2. **The redraw input is front-loaded into the 395 prompt** (captured from the dump at this close): creation order `portal_accounts` → `portal_password_reset_tokens`; **`portal_accounts_id_seq` travels complete** (CREATE + OWNED BY + SET DEFAULT nextval — the second sequence-carrying redraw, the `donation_receipts_id_seq` mechanics); one outbound FK `contact_id → contacts ON DELETE SET NULL` (no RESTRICT-as-behavior case); unique-on-email + the contact_id index; **zero inbound FKs from core tables** (verify with a dump grep at session).
3. **Composition-safety survey front-loaded:** the hunt is the portal-***table*** reads (the guard reads are done — this session): `ContactResource`'s Portal Access section + `EditContact`'s four portal actions (queries fire at contact-edit render — the likeliest identity-check trips), the `DatabaseSeeder` page-topology calls (`PortalPageSeeder`/`SystemPageSeeder` — a topology question, not a crash; `seedWidget` already skips missing types), `DemoPortalMemberSeeder`. Gate signal: the capability (`present('member-portal')`), not route presence.
4. **The twin fixtures carry the expiring caveat:** no migrator path — they gain it the moment schema moves, vendor-relative directly (the 393 same-session compression nuance).
5. **The owner creates `crm-plugin--member-portal`** (public) before 395's extraction phase — an open-gate item there (blocks Phase C only).
6. No sibling edit expected at 395 (the Events rider shipped this session).

---

## Security considerations

No new endpoint or input path; the carve moved files verbatim (every throttle, the honeypot + form-timing checks, the `signed` verification link, the silent-redirect enumeration guard, and the portal-security contact-scoping all travel unchanged — `PortalAuthTest`'s scoping assertions pass from the plugin suite). The auth stack's move is representational: the injected guard/provider/broker config is byte-equivalent to the removed `config/auth.php` entries, and the `portal.auth` alias maps to the same class at its new path (route parity proves the middleware chain). The new capability gates only ever narrow behavior on portal-absent compositions (member pages 404, the collision check self-skips, the arm nulls) — the full composition is unchanged, proven by parity + the suite. The Events v0.1.2 rider is lock-pinned to the exact tag commit (the 385 supply-chain posture). `deploy.yml` untouched; FM surfaces untouched; CRM contract stays v2.7.0.

---

## Schema changes

None. `portal_accounts` and `portal_password_reset_tokens` stay in the core dump; the redraw is session 395 (input recorded in the 395 prompt and the block handoff above).

---

## Production deployment notes

None beyond standard `migrate`. Deploy already runs `widgets:sync` + `build:public`, which picks up the two moved `script.js` paths (verified locally: the rebuilt bundle carries both portal scripts; all `widget_types` handles unchanged, so no content changes). `composer install` fetches Events v0.1.2 credential-free from its public repo (lock-pinned). VERSION bumped 0.393.01 → **0.394.01**.

---

## Deferred decisions

- **The two-table squash redraw + packaging + extraction** — session 395, one combined session (decision 8 resolved at this close; prompts drafted, survey front-loaded).
- **The portal-table composition-safety gates** (ContactResource/EditContact, the topology seeders, DemoPortalMemberSeeder) — 395; the identity check arbitrates.
- **`portal_password_reset` handle migration** — follows the model if it ever moves (D12 territory; contract surface 7 wording amended this session).
- **The two core portal mailables** (`PortalEmailVerification`, `PortalPasswordReset`) — stay core as model-adjacent; revisit only with a § 6.7 model move.
- **Model/factory moves** (§ 6.7 stands); **per-plugin CI** (D11); **root PSR-4 retirement** (D12); **events-handle registry migration** (a future `crm-plugin--events` release); **`baseDir()`-derived asset declarations** (standing backlog); **Playwright chrome-parity park** — all unchanged.
- **Housekeeping inbox:** the `portal_form_collision` defaults item checked at this close — the carve kept the collision surface core (mailable, handle, controller check), so the item stands unchanged in the inbox.

---

## Test result

- **Fast Pest:** 3384 passed, 0 failed (parallel) — 3355 baseline + 29 additive; every pre-existing portal assertion unchanged beyond path/namespace re-points and the two additive count/list updates.
- **`MemberPortal` testsuite:** 4 files, 37 tests at `plugins/MemberPortal/tests` — all green (runs inside the fast suite).
- **Route parity:** 272/272 (method+URI+name+middleware; sole diff the moved middleware FQCN — the 381 exception).
- **Portal-absent composition:** boots clean at 254 routes, capability + guard absent, Events portal route vanished.
- **Composer:** `validate --strict` clean; lock pins `nonprofitcrm/events` v0.1.2 at the exact tag commit.
- **Asset gate:** `widgets:sync` (41 rows) + `build:public` — bundle content verified (both portal scripts present from the plugin paths).
- **CI:** `Tests` aggregate **green** on the pushed branch (fast + slow; Playwright non-gating, the parked chrome-parity spec remains the known failure).
