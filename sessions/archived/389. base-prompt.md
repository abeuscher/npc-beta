# Base Prompt — Session 389 (Donations Carved)

**Start now.** Execute the reading list below, open the session prompt at `sessions/389. Donations Carved.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **389. Donations Carved**.

This is **arc position D3, first session of the block** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary). D2 (388) closed the known-edges extraction: Events lives at `crm-plugin--events`, `plugins/` is empty, and all three shipped plugins are external packages. D3 is the **hardest remaining entanglement** — the Donations vertical (funds, checkout, receipts, acknowledgments, soft credits, the donors/year-end surface, the importer, two widgets) — run per settled decision 4 as a carve → package → extract block (~2–3 sessions). This session is the **carve**: the behavioral surface moves into `plugins/Donations/` (refilling `plugins/`), on the 381 Events-carve playbook but with three sessions' more contract maturity. Two surfaces get forced: **surface 7** (the email-template handle registry — built this session, with the donation handles as its first plugin contribution) and **surface 10's donation slice** (the webhook inversion — **cross-repo for the first time**, since the branches to invert live in the extracted `crm-plugin--payments`; the block's drafted answer is a Payments `v0.2.0` release, decision 5).

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.12.0 → 0.13.0 at close: surface 7 PROVEN, surface 10's donation slice PROVEN, the post-extraction release pattern recorded.

**Session shape:** infrastructure, behavior-preserving, single-phase (carve only — schema and extraction are the block's later sessions). Routes 272/272 byte-identical; every donation behavior net passes with assertions unchanged; no schema work. **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse — decision 4's one-release-or-two split is the flagged one.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D2 ✅; settled decisions 1–4) and § 6 dispositions 1 (contact extension), 3 (tests), 7 (squash mechanics — the *next* session's work, read for the boundary).
3. Read the 388 session log (`sessions/388. Events Extracted — Log.md`), especially its **D3 kickoff input** section.
4. Read `docs/plugin-contract.md` at 0.12.0 — surfaces 3/4/8 (the carve sockets, all PROVEN — the mechanics this session reuses), surface 7 (DECLARED — built this session), surface 10 (the per-vertical rollout paragraph — the donation slice inverts now), surface 13 (the checkout controller already consumes it), surface 5's core-model caveat, and the External repo section (the release discipline decision 5 exercises).
5. Read the **381 Events-carve log** (`sessions/archived/381. Events Vertical Module — Log.md`) — the carve playbook this session repeats: socket usage, route byte-parity discipline, boundary guard, twin/mirror tests.
6. Open `sessions/389. Donations Carved.md` — the survey (captured at 388 close), front-loaded decisions 1–9, and the two Open-Gate questions (the webhook-inversion split; the Payments sibling checkout being current).
7. Read the donation surface named in the survey: `app/Models/{Donation,DonationCredit,DonationReceipt,Fund}.php`, `app/Http/Controllers/DonationCheckoutController.php` (+ its `routes/web.php` block), `app/Filament/Pages/DonorsPage.php`, `app/Filament/Resources/{DonationResource,FundResource}.php` (skim their pages), `app/Filament/Pages/ImportDonations{,Progress}Page.php`, `app/Mail/{DonationAcknowledgment,DonationReceipt}.php`, `app/Models/EmailTemplate.php` (the defaults array surface 7 replaces the donation half of), `app/Widgets/{DonationForm,RecentDonations}/`, `app/Observers/{DonationObserver,DonationCreditObserver}.php`, the three policies.
8. Read the **webhook to be inverted** in the sibling checkout: `../crm-plugin--payments/Http/Controllers/StripeWebhookController.php` — the `donation_id` branch, `sendDonationAcknowledgment`, and every `Donation::` reach (including the recurring/subscription branches) — plus core's `app/Payments/Events/CheckoutSettled.php` and the Events listener precedent in `vendor/nonprofitcrm/events/Listeners/PromotePaidRegistrations.php`.
9. Skim the registry patterns this session mirrors: `app/Plugins/{ImporterRegistry,ImporterContribution,CapabilityRegistry,AdminContribution}.php`, and `vendor/nonprofitcrm/events/EventsServiceProvider.php` (the at-scale provider shape a vertical carve produces).
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry) and note the housekeeping-inbox posture for this shape.
11. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 388

- **CRM contract at v2.7.0** — 388 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D2 complete. All three shipped plugins are external: `crm-plugin--logo-garden`, `crm-plugin--payments`, `crm-plugin--events` (each public, tag `v0.1.0`, vcs-sourced in `distribution.json`, lock-pinned). **`plugins/` is empty** — this session's carve refills it; the 388 guard relaxations tolerate both states, no revert needed. Plugin contract **v0.12.0**; the runbook carries the 388 deltas (two-posture test membership, empty-`plugins/` guard note, cross-input gate-4 grep).
- **The `Events` phpunit testsuite runs from `vendor/nonprofitcrm/events/tests`** (29 files, 236 tests) — the precedent decision 8 of the session prompt applies to the new `Donations` suite (in-repo at `plugins/Donations/tests/` until that plugin extracts).
- **Payments plugin:** `stripe/stripe-php` package-owned; the webhook still carries the donation branches inline (surface 10's rollout list) — this session's inversion target, via the first post-extraction plugin release (drafted `v0.2.0`, decision 5).
- **Planning state:** the 385 reframe stands — the finish line is the Stage D end state; after D3 the ladder continues D4 (Memberships) → D5 (Member Portal) → D6–D12.
- **Schema baseline:** unchanged from 383; the four donation tables (`donations`, `funds`, `donation_credits`, `donation_receipts`) are in the core dump and **stay there this session** — the redraw is the block's package session.
- **Fast Pest suite green at 3263 / 0** (388 close — the 387 baseline held exactly). Expected delta this session: **additive** — new registry/listener/fixture/guard tests; every existing donation assertion unchanged.
- **Route baseline: 272 routes** (dev/test environment) — byte-identical this session. (Production images list 270 — the two `dev.widgets.*` routes are dev-only.)
- **Playwright suite** — CI signal; the known parked chrome-parity failure stands (pre-existing, non-gating). No local Playwright.
- **Housekeeping inbox** — posture per the 388 close (infrastructure shape absorbs no items; the open `[test-integrity]` items stay).

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Checkout (paid, and the not-configured degradation), acknowledgment + `acknowledged_at` idempotency, year-end receipts, recurring donations, soft credits, the importer, admin pages, both widgets — all pass with assertions unchanged. Routes 272/272. No schema, no visual change.
- **The cross-repo inversion is contract-first** (decision 5): contract wording → `crm-plugin--payments` change, commit, tag `v0.2.0`, push → core lock bump (`composer update nonprofitcrm/payments`) **in the same commit** as the require/constraint widening (`^0.2` in both `composer.json` and `distribution.json` — the guard enforces agreement) and the listener that consumes the new dispatch. Never edit `vendor/` — the sibling checkout is the source.
- **The manifest is edited, never the generated file.** The new Donations `folder` entry goes in `distribution.json`; `plugins:manifest-sync` regenerates the config list (this time it is NOT a no-op — a provider line is added).
- **Surface honesty:** promote surface 7 on the shipped registry + contributor; promote surface 10's donation slice on the shipped inversion; surface 9 is promoted **only if** the contact-screen socket is genuinely built and exercised (decision 6 says it likely isn't this session).
- **Widget moves re-run the asset pipeline:** after the two widget folders move, `widgets:sync` + `build:public`, and confirm DonationForm's js in the bundle (its path changes; `collectSources()` skips missing paths silently).
- **Rule-5 membership pass is a reviewed pass:** subject tests move to `plugins/Donations/tests/` (new phpunit testsuite + `DistributionManifestGuardTest` traces it to the manifest entry); tests where donations are fixture data stay core; log the split. The 383 reduction discipline applies (delete strict duplicates, log deletions).
- **Lockfile changes ship in the same commit as their composer.json changes** (the 385 discipline).
- **Scope is binding.** No schema move, no extraction, no events-handle migration, no D4+ work, no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.12.0 → 0.13.0 per the session prompt's decision 9; plan doc § Stage D D3 line annotated (block opened, carve done); block-handoff notes in the log for the package session.
- **Block handoff at close:** record in the session log what the package session inherits — the four-table redraw input (owner map, FK order, any check constraints), the composition-safety reaches found (`ContactResource`, random-data generator, `DonorsPage`), and anything the inversion surfaced.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code, state the plan, note any drift found — and resolve the two Open-Gate items: **(1) decision 4's split** (one Payments `v0.2.0` release inverting **all** donation-reaching webhook branches — checkout-settled and the recurring/invoice branches — vs. checkout-settled only this session; the prompt drafts one release), and **(2) the `crm-plugin--payments` sibling checkout is present and current**. Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
