# Base Prompt — Session 315 (Housekeeping Batch 2 — Batch 2B)

**Start now.** Execute the reading list below, open the session prompt at `sessions/315. Housekeeping Batch 2 — Batch 2B.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **315. Housekeeping Batch 2 — Batch 2B.**

This is **execution-order position 42** in `sessions/release-plan.md` — § E12 Housekeeping Batch 2, the second of the two batches that entry spans. The first batch (session 314) shipped three inbox items and dropped three as no-longer-valid; this batch works through the remainder. Sequenced inside the Public Website Complete milestone block (Rule 12). Closing this session closes the E12 plan entry.

This session is **NOT boundary-touching** — no Fleet Manager surface in scope. CRM contract stays at v2.3.0. No schema changes expected.

**Session shape:** housekeeping. Walk `sessions/housekeeping-inbox.md` with the user at session start, pick the subset that fits one batch (skip items tagged `[post-milestone]`), and apply iteration-sliced. Six-iteration ceiling per the established discipline. Whatever doesn't land here either spills to a third batch or gets re-dispositioned at close.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). They are the canonical format reference — do not infer format from previous session logs.
2. Read `sessions/session-outlines.md` for the active-tracks block and the housekeeping-inbox pointer.
3. Read the `sessions/release-plan.md` entry at execution-order position 42 (§ E12) — canonical for scope.
4. **Read `sessions/housekeeping-inbox.md` end-to-end** — this is the canonical input for the batch. Walk it with the user at session start to pick the Batch 2B subset.
5. Read `docs/app-reference.md` for environment + view-to-file mappings if any inbox item lands in unfamiliar territory.
6. Open `sessions/315. Housekeeping Batch 2 — Batch 2B.md` and read the session prompt.
7. Note any drift between the session prompt and the actual code in a brief work-log entry.

---

## Starting state inherited from session 314

- **CRM contract at v2.3.0** — unchanged by 314 (housekeeping-only).
- **Schema baseline:** no schema changes since session 308; 314 added none.
- **Fast Pest at 314 close:** 2594 passed (313 baseline 2586 + 8 from the three shipped housekeeping items). Confirm at session start.
- **Playwright suite** running in CI on every push against the isolated stack. Local Playwright defaults to the isolated stack via `npm run test:e2e`; `test:e2e:local` is the explicit escape hatch for runs against the dev DB.
- **Planning state:** session 314 shipped three inbox items (the Hero widget's button-alignment + text-block-max-width controls; a fresh-install placeholder logo image; the logo wordmark responding to a full-bleed hero's text colour) and dropped three as no-longer-valid (the paragraph/list-item padding default — the typography system already owns that rhythm; the button-style refresh — as far as it goes before a designer; the editor text-colour legibility item — obsoleted by the inline editor giving operators direct control). The downstream feature sessions were renumbered down one slot: record-duplicate is now session 316, the accessibility pass 317, the media finder 318.
- **New capture flow (shipped at 314):** items can now be captured mid-session with `npm run logbug -- "…"`, which stamps them with the VERSION marker + date into `sessions/housekeeping-incoming.md`; this session's close digests that buffer into the inbox. The inbox now also carries a **verify-the-premise-before-scheduling** rule — re-check each candidate against current code at the walk before committing it to an iteration, weighted by how old its capture stamp is. Three of 314's six proposed items were dead on arrival precisely because this check wasn't there; apply it rigorously this session.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — carries the items 314 didn't take. Walk the full inbox at session start to pick the Batch 2B subset.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Walk the inbox first, and verify each candidate against current code before scheduling it.** Open the inbox with the user, classify each item by size, confirm its premise still holds against the current app (the staleness guard documented in the inbox), and pick the Batch 2B subset that fits one session's iteration budget. Items the batch doesn't take stay in the inbox.
- **Skip `[post-milestone]`-tagged items.** Those are deferred past the Public Website Complete milestone by inbox convention.
- **One iteration per item by default.** Bundle only when two items share a file or touch the same surface; otherwise keep iterations independent so any single one can be reverted without unwinding the others.
- **Six-iteration ceiling** — past 6 iterations the next iteration is the carve-out signal. Stop and let the leftover items move to a later batch.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **Questions are not instructions** per `feedback_questions_are_not_instructions`.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list above and the Starting state are read, surface a one-paragraph orientation — which items the proposed Batch 2B absorbs, in what order, which (if any) failed the premise re-check, and what needs clarification — and wait for the user to confirm before starting. Do not pipeline straight from reading into implementation.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents. The next session is **316. Record-Duplicate Affordance** (its prompts are already drafted at `sessions/316. base-prompt.md` + `sessions/316. Record-Duplicate Affordance.md` — review and refresh them against what this session learned rather than drafting from scratch). If Batch 2B itself spills over, draft a third housekeeping batch at 316 instead and bump record-duplicate to 317.

Also digest the housekeeping capture buffer (`sessions/housekeeping-incoming.md`) per the close-gate step in `sessions/template-base-prompt.md` and surface it to the user before close.

Then stop. The user reviews the work and the next session's drafts, and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- Write `sessions/315. Housekeeping Batch 2 — Batch 2B — Log.md` from `sessions/template-session-log.md`.
- Bump the `VERSION` file (format `0.<session>.<iteration>`).
- Update `sessions/completed-sessions.md` with the 315 row.
- Update `sessions/housekeeping-inbox.md` — remove items shipped this session; fold the capture buffer in and clear it.
- Update `sessions/release-plan.md` § E12 — mark the entry closed (both batches done).
- **Archive `sessions/314. *` into `sessions/archived/`** per the archive rule.
- Commit on `session-315/N`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages name the inbox item being closed; no bundling across unrelated items in the same iteration; no docstrings/comments on code you did not write.
