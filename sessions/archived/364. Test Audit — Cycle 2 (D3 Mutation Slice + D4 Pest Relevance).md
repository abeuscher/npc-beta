# 364. Test Audit — Cycle 2 (D3 Mutation Slice + D4 Pest Relevance)

Run the Test Audit track's second cycle: one mutation-testing slice (D3) plus a bounded Pest file-relevance and fast/slow-classification refresh (D4), draining the in-scope `[test-integrity]` inbox items along the way.

---

## Stub reference

No `release-plan.md` entry — a cadence cycle of the **Test Audit track**; `sessions/tracks/test-audit.md` is canonical for the cycle shape, the dimension procedures, and the cadence rule. Trigger was ≈ session 365; pulled to 364 at the 363 close with housekeeping drained and the guided-tour track paused on external feedback.

---

## Session-specific deltas

Context the track doc doesn't carry:

- **The 363 flake sighting.** One fast-suite run at 363 showed a single failure among four otherwise-green runs (2977/0 captured twice after; the failing run's output was not captured). Nothing in 363's change surface is implicated. Cycle 1 left the `inline-editing-foundation:79` cold-load flake on passive watch — this may or may not be related. A bounded CI-history check (the `Tests` workflow runs on the pushed 363 branches) is the cheap first move; decide disposition only after identification.
- **D3 queue drift.** The s241 forward queue predates v2.2.0–v2.5.0; the Fleet Manager controller surface it lists has since more than doubled (backup trigger, backup blob, admin recovery). If the gate picks that slice, the slice boundary is the controllers + their support services (`AdminAccountRecovery` etc.), not the nginx/mTLS layer (config, not PHP — Infection can't reach it).
- **D4 has never run at current scale.** s094 walked 33 test files; the suite is now ~200+ Feature files / ~2,990 tests. The gate's bounding proposal should pick an examinable subset with a rationale (e.g., files untouched the longest, files over a size threshold, files whose subject code was refactored since — the 346/347 carve-out subjects are natural candidates) and explicitly queue the remainder for Cycle 3.
- **[test-integrity] drainage candidates:** the `[0.326.01]` localization-readiness scoping sweep (scope-only: confirm the hardcoded-string footprint before committing to any pass), and optionally the `[0.324.01]` widget-knockout Playwright guard (a guard *build*, D1-adjacent — take only if the owner wants it this cycle; it is a standing-spec addition, so `docs/testing/playwright-discipline.md` governs).

---

## Open questions to resolve at session start

1. Which D3 slice (lean: Fleet Manager controllers; alternate: `AppearanceStyleComposer`).
2. The D4 bound (file subset + what defers to Cycle 3).
3. Whether the knockout-guard item is in or out this cycle.
4. Timebox for identifying the 363 flake, yes/no.

---

## Out of scope

- **Application code.** Audit sessions change tests and docs only; production bugs surfaced get captured to the inbox.
- **Net-new coverage for surviving D3 mutants** (per the s241 lean — the D3 deliverable is evidence-based test sharpening, not coverage expansion).
- **D1 Playwright sweep** — ran at Cycle 1; nothing has accumulated (the tour-spec rebuild at 362 was already disciplined). Individual Playwright dispositions still allowed where D4/D2 findings demand.
- **Any Fleet Manager surface** — non-boundary; contract stays **v2.5.0**.

---

## Testing

- **Slow test groups:** run any group a D4 verdict reclassifies, to prove the move (a fast test crossing 5s → `->group('slow')` and vice versa).
- **New tests expected:** generally no — this session deletes, renames, rescopes, and marks. Exceptions: a D4 "update" verdict may rewrite a test through the real production path (the A005 TaxReceipt precedent), and the knockout guard (if taken) is a new standing spec authored local-first.
- **Baselines:** verify the post-merge fast baseline (≈ 2993/0) before any disposition; run the full fast suite after each dimension's applies. **Pest and Playwright sequentially, never in parallel.** `tests/Feature/DesignGroupIntegrityTest.php` pins the `design` group — any D4 change to that cluster edits the reviewed list in the same pass.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file:** `sessions/364. Test Audit — Cycle 2 (D3 Mutation Slice + D4 Pest Relevance) — Log.md`
- **Branch:** `session-364/N` (final iteration)
- **Boundary:** NOT boundary-touching — contract stays **v2.5.0**.
- **Track doc:** this session closes Cycle 2 → write the Cycle 2 retrospective into `sessions/tracks/test-audit.md`, refresh the status snapshot (next trigger ≈ session 414 or forcing function), and update the one-liner in `sessions/session-outlines.md`.
- **Inbox bookkeeping:** drained `[test-integrity]` items leave via the disposition rules; every deletion/rename lands its paper-trail line.
