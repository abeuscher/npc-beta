# Base Prompt — Session 388 (Events Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/388. Events Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **388. Events Extracted**.

This is **arc position D2** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary) — the second Stage D session. D1 (387) proved the manifest-era runbook on the easy case: Payments, a six-file foundation plugin with no assets, no migrations, no tests, extracted to `crm-plugin--payments` with the stripe dependency moving out of the root manifest. D2 is the **known-edges run**: Events — already a Stage B path package (`nonprofitcrm/events`) since 383 — leaves the repo for `crm-plugin--events` via `docs/plugin-extraction-runbook.md`, exercising every edge Payments didn't have: three plugin-owned migrations reached by the removal/flag fixtures, the 29-file `Events` phpunit testsuite, seven widgets with real assets (gate 4 is live for the first time since LogoGarden) including the EventsListing→BlogPager cross-plugin asset input, and shipped thumbnails. Its structural novelty: extraction empties `plugins/` until D3's carve refills it, forcing a small standing-guard relaxation.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change is expected (the 385/387 precedent: a public VCS-resolved package needs nothing from the deploy pipeline). The plugin contract (`docs/plugin-contract.md`) bumps 0.11.0 → 0.12.0 at close: third extracted reference implementation (first migration-owning, first testsuite-carrying), the testsuite posture recorded (the suite travels with the package and core CI runs it from the vendor path), runbook deltas folded.

**Session shape:** infrastructure, behavior-preserving, single-phase (extract only — Events is already packaged). Runtime behavior is byte-identical throughout: routes 272/272 (dev/test), suite at the 387 baseline with assertions unchanged (path re-points, one baseline-key re-point, one guard relaxation), same test count, no schema work. **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1 ✅, D2 is next; settled decisions 1–4) and the § 7 P7/P9 lines (Events' packaging history and the extraction mechanics this session composes).
3. Read the 387 session log (`sessions/387. Payments Packaged & Extracted — Log.md`), especially its **D2 kickoff input** section.
4. Read `docs/plugin-contract.md` at 0.11.0 — External repo (Stage C-lite) incl. the 387 dependency posture, The distribution manifest, surface 5 (migrations — the fixture semantics that must survive the move), surface 2's at-scale contributor notes, surface 11 (assets + the vendor-path caveat and bundle-content gate).
5. Read `docs/plugin-extraction-runbook.md` end to end — third run; the worked examples are LogoGarden and Payments; Events diverges at the migration-owning/testsuite follow-ups the runbook already lists.
6. Open `sessions/388. Events Extracted.md` — the survey (captured at 387 close) and front-loaded decisions; the Open-Gate items are the owner-created repo and the testsuite decision (decision 2).
7. Skim the plugin being moved: `plugins/Events/` top-level shape (89 files — don't read them all; read `EventsServiceProvider.php` fully, confirm `__DIR__`-relative loading, and read the three widget definitions declaring assets: EventMiniCalendar, EventRegistration, EventsListing).
8. Read the fixture + guard surfaces the survey names: `tests/Fixtures/Plugins/EventsRemovedTestCase.php` + `EventsDisabledViaFlagTestCase.php` (the migrator `basePath` reaches), `tests/Feature/PluginPackageGuardTest.php` (the schema-dump owner map + the in-repo glob that needs the empty-`plugins/` relaxation), `tests/Feature/DistributionManifestGuardTest.php`, `tests/Feature/WidgetColorTokenConsumptionTest.php` (the line-keyed baseline entry), and skim the re-point targets (`ContentListingWidgetsTest`, `AppearanceConfigRenameTest`).
9. Read the build surface: root `composer.json` (path repo + require), `Dockerfile` (the Events COPY lines ×2 that go), `distribution.json` (the Events `path` entry), `phpunit.xml` (the `Events` testsuite line).
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry) and note the housekeeping-inbox posture for this shape.
11. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 387

- **CRM contract at v2.7.0** — 387 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1 complete; Stage D open. Payments extracted to `github.com/abeuscher/crm-plugin--payments` (tag `v0.1.0`, vcs entry in the manifest, lock-pinned; first dependency-carrying package — `stripe/stripe-php` package-owned, root's direct line gone, resolution identical at v16.6.0 transitive). Both standing guards derive their sets from `distribution.json`. Plugin contract **v0.11.0**; runbook carries the 387 deltas (dependency posture, no-assets gate wording, Dockerfile COPY-line ordering per manifest path-entry order).
- **Events today:** the only in-repo plugin — a `path` entry in the manifest, Stage B package `nonprofitcrm/events` 0.1.0 with explicit version field, own migrations (three create-table), own `tests/` (the `Events` phpunit testsuite, 29 files), seven widgets with assets + thumbnails, Dockerfile manifest COPY ×2 stages. LogoGarden and Payments are the two vcs precedents.
- **Planning state:** the 385 reframe stands — the finish line is the Stage D end state; D3 (Donations, ~2–3, the hardest entanglement) follows D2.
- **Schema baseline:** unchanged from 383. No schema work this session (the three events tables are plugin-owned and travel with the package — that's the point of the fixture re-point).
- **Fast Pest suite green at 3263 / 0** (387 close — unchanged from 386; D1 added no tests). Expected delta this session: zero — path re-points, one baseline key, one guard relaxation; same counts.
- **Route baseline: 272 routes** (dev/test environment) — byte-identical this session. (Production images list 270 — the two `dev.widgets.*` routes are dev-only; not composition drift.)
- **Playwright suite** — CI signal; the known parked chrome-parity failure stands (pre-existing, non-gating). No local Playwright.
- **Housekeeping inbox** — posture per the 387 close (infrastructure shape absorbs no items; the open `[test-integrity]` items stay).

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Every Events behavior net — enabled twin, removal mirror, flag-vanish, presence matrix, importer, admin socket, routes — passes with assertions unchanged; the two fixtures prove disabled-≠-uninstalled against the **vendor'd** migrations. Routes 272/272 (dev/test). No schema, no visual change.
- **The manifest is edited, never the generated file.** The `path` → `vcs` walk happens in `distribution.json`; `plugins:manifest-sync` is a provider-list no-op; the guards verify the rest.
- **Gate 4 is live this session** — the one failure a green suite won't catch: after the asset-path rewrite, run `widgets:sync` then `build:public` and prove the built bundle carries the three asset-declaring widgets' SCSS/JS **and** the BlogPager pager styles EventsListing pulls in as a core cross-input. A green suite alone is not the proof.
- **The testsuite move is decision 2:** phpunit's `Events` suite re-points to `vendor/nonprofitcrm/events/tests` — same 29 files, same counts, running in core CI unchanged. If the vendor-path suite misbehaves under paratest/CI in any way, stop and surface it — don't improvise a different test topology mid-session.
- **The empty-`plugins/` guard relaxation is scoped** (survey item 6 / decision 4): the in-repo path-package assertion tolerates an empty set; nothing else in either guard changes.
- **Lockfile changes ship in the same commit as their composer.json changes** (the 385 discipline). Runbook step-5 ordering note from 387: with the source dir deleted, artisan cannot boot until `composer update` re-resolves — composer first, then `plugins:manifest-sync`.
- **Scope is binding.** No Donations work (D3); no model/factory moves; no per-plugin CI (D11); no root-PSR-4 retirement (D12); no FM surface; client billing stays guard-protected. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.11.0 → 0.12.0 per the session prompt's decision 6; runbook deltas folded; plan doc § Stage D D2 line marked; arc header advanced to D3.
- **D3 kickoff input at close:** record in the session log what D3 (Donations) inherits — the webhook donation-branch inversion (surface 10's per-vertical rollout), the funds/receipts/transactions entanglement, the expected pressure on surface 7 (email-template registry) and surface 9 (contact extension) — plus anything this run adds.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code, state the plan, note any drift found — and confirm the two owner-facing items: **(1) the owner has created the empty public `crm-plugin--events` repo** (owner-created, public for credential-free composer, checked out as a sibling of this repo), and **(2) the testsuite decision** (decision 2: the 29-file `Events` suite travels with the package; phpunit re-points to the vendor path; core CI keeps running it). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
