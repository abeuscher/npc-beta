# Base Prompt — Session 286

**Start now.** Execute the reading list below, open the session prompt at `sessions/286. Public Marketing Website — Home Layout Rebuild.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **286. Public Marketing Website — Home Layout Rebuild**.

This session continues the **Public Marketing Website track** (track doc at `sessions/tracks/public-marketing-website.md`). PMW1 closed at 284 with the home structurally cleaned but conservative — the cleanup adhered too closely to "preserve existing structure" and didn't surface enough visible change. Session 285 then lifted the four CMS blockers that the conservative pass exposed. This session rebuilds the home against the new layout spec at `sessions/public website/homepage-layout-spec.md`, using the CMS surface area that landed at 285. Sequenced inside the **Public Website Complete milestone**.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** content + visual-build. The deliverables are an updated `home.json` rebuilt against the eight bands the layout spec defines, a re-import into the live local DB, a screenshot for the user's review, and gap-report appends for anything the rebuild surfaces.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. Read `sessions/release-plan.md` for the session's position inside Public Website Complete. If a 286 entry is missing at session start, treat the layout spec and gap-resolved-state as the canonical scope source.
4. Read `sessions/tracks/public-marketing-website.md` for the track's status snapshot, gap-resolution discipline, and forward plan.
5. Read **`sessions/public website/homepage-layout-spec.md`** — canonical for cell-by-cell structure, image placements, padding, heading sizes, and the cross-section rhythm. The spec's authority section explicitly overrides `brief.md` and `copy.md` layout interpretations for the home page.
6. Read `sessions/public website/copy.md` for the words (which the layout spec does not override). Read `sessions/public website/brief.md` for the system conventions and gap-surfacing protocol that remain in force.
7. Read `sessions/public website/audit-summary.md` for the system inventory and `sessions/public website/gap-report.md` for the current state of open gaps. Confirm G1, G2, G3 (interim), G13, G14 are marked ✅ resolved at 285 close before relying on the layout spec's assumptions.
8. Read the four reference images (`reference-restrained-crm-page.png`, `reference-kitchen-sink-saas-page.png`, `reference-section-band-cycling.png`, `reference-feature-grid-pattern.png`) and the `references.md` index — structural inspiration only, not surface decoration.
9. Read `sessions/public website/existing-home-page-export.json` for the format-1.1.0 JSON shape and `sessions/public website/home.json` for the PMW1 cleaned state being superseded.
10. Read `docs/app-reference.md` for the page-builder component map and `docs/schema/README.md` for the table index. No schema changes are expected this session.
11. Open `sessions/286. Public Marketing Website — Home Layout Rebuild.md` and read the session prompt carefully.
12. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

---

## Starting state inherited from 285

- **CRM contract at v2.3.0** — unchanged. Session 285 lifted CMS-side gaps; no boundary surface touched.
- **Schema baseline:** no schema changes since 280-close.
- **Fast test suite green at 2390 + N / 0 sequential** where N is the new tests landed at 285 close for G1 / G2 / G3 (read the 285 log for the exact baseline). No new tests expected this session — content-only work. Single regression-check run after the home re-import suffices.
- **Playwright suite** — baseline preserved. No Playwright in this session (page-capture harness lands at PMW4 / track close).
- **Planning state:** Public Marketing Website track is active; PMW1 closed at 284; CMS fixes closed at 285; this session executes the home rebuild against `sessions/public website/homepage-layout-spec.md`.
- **CMS surface area available (resolved at 285):**
  - Layout-level `appearance_config` now round-trips through export/import (G1). Backgrounds and padding can live on layout containers as the spec directs.
  - Text widget carries CTAs (G2). Bands 4 / 5 / 6 / 8's "Text widget with centered CTA" pattern is now expressible without falling back to Hero-as-text-band workaround.
  - One ghost-on-dark button variant exists (G3 interim). Use it for the dark-band CTA in Band 5.
  - Typography defaults tuned (G13 / G14). H1, heading margin-bottoms, paragraph margin-bottom now match reference scale. The hero headline's "use H1" directive in the spec no longer needs the override-mechanism fallback path.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — content sessions don't absorb housekeeping items unless a finding directly ties to home-page rendering. Touch only if a finding surfaces inside this session's scope; otherwise carry forward.

---

## Process rules for every session

- **Before writing any code or JSON**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. *Session-286 note: no integrations in scope.*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** Standard rule applies. The most likely 286-specific candidate: if a band specified by the layout spec turns out to be expressible in two materially different ways — e.g. the Band 1 hero choice between "single Hero widget plus Image widget sibling" vs "Text widget plus Image widget sibling" — surface the choice with a one-line recommendation before committing.
- **Adapt to drift; don't ask about it.** The layout spec is recent and the CMS surface state changed at 285. Drift expected at code-level fine detail (helper names, registry rows, specific test paths). Default to silent adaptation + a one-line work-log note. Flag only when a drift changes intent.
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that propagate across bands or set a precedent for About / Pricing / Contact / Demo (image-placeholder picks across all five pages, the visual treatment of CTAs sitting next to inline body text, etc.). Local edits to a single band — just decide and note.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them. *Session-286 note: no migrations expected.*
- **Update `docs/schema/`** after writing any migration. (No-op this session.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 285 close baseline. No new tests expected; this is a regression check.
- **Front-end builds.** No SCSS / JS expected this session. The rebuild is content-only (JSON + re-import). Don't run builds if not needed.
- **Tests for new behaviour:** no new tests expected. Content-only sessions skip the tests-for-new-behaviour rule per the base-prompt convention.
- **Fast vs slow classification:** N/A.
- **Playwright artifact hygiene:** N/A this session.
- **Cross-repo coordination — Fleet Manager.** N/A.
- **Verify objective outcomes yourself; pull the user in for judgment.** Objective surfaces: the JSON imports without errors; the page renders; the eight bands appear in spec order; the cross-section rhythm matches the spec's table. Close those loops via re-import + curl. The visual surface — whether the rebuild reads like the references — is the user-judgment surface. Surface for review at session-end.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that the home is rebuilt against the layout spec, re-imported, and ready for visual review at the home URL. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/287. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to the next phase of the Public Marketing Website track. Most natural next phase: **Public Marketing Website — Pricing build-out + About extend** (was PMW2; sequence carries forward unchanged after the home-rebuild insertion). Alternatively, if E5 / E6 / E7 / E8 (Mobile Type Scaling, Theme Colors Refactor, Column-Layout Mobile Collapse) are the higher priority given typography work landed at 285, lift one of those next — confirm with the user before drafting.
- **Next session prompt** — draft against the relevant release-plan entry + the layout-spec-approach pattern this session validates. The layout spec's authority section notes: *"Once this page is built and reviewed, an equivalent layout spec will be written for About, Pricing, Contact, and Demo. The home page is the pattern test for the spec approach."* The next session's prompt should reflect whether the spec-approach worked as intended at 286 close.

**Pre-next-session ask in the close handoff.** If the next phase is Pricing + About, surface a reminder: *"Before that session starts, please export the current About and Pricing pages from the admin and place them in `sessions/public website/` as `existing-about-page-export.json` and `existing-pricing-page-export.json`."* The user runs the export action; the agent treats those files as ground truth at session start.

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/286. Public Marketing Website — Home Layout Rebuild — Log.md` using `template-session-log.md` as the format reference. Document: per-band decisions (image placeholder picks, alignment calls, fallback paths taken), gap-report appends, manual-testing observations from the user's review, validation of whether the layout-spec approach delivered the bigger / cleaner change the spec was authored to produce.
- **Update `sessions/public website/gap-report.md`:** append any new rows surfaced during the rebuild. Mark the rebuild as the visible-change validation of the layout-spec approach.
- **Update `sessions/completed-sessions.md`:** append session 286's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status to reflect 286 closure and the next phase queued.
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (last update date; home rebuild complete; next phase queued).
- **Update `sessions/release-plan.md`:** mark session 286's entry ✅ closed at session 286.
- **Archive the previous session:** move all `sessions/285. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-286/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Home rebuilt against layout spec (bands 1–8) + screenshot + gap-report appends".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **The rebuild is operator-readable.** Image placeholder labels in widget labels or custom fields are how the eventual photo swap stays mechanical; honor the spec's image-naming convention.
