# Session 387 — Payments Packaged & Extracted — Log

**Date:** 2026-08-19
**Branch:** session-387/1

---

## What was built

Arc D1 of the Plugin Architecture arc — the opening Stage D session. Payments walked its `distribution.json` source kind `folder` → `path` → `vcs` and left the repo: the first extracted foundation plugin, the first dependency-carrying package, the second extraction-runbook run and the first under the manifest layer. Behavior-preserving throughout: routes 272/272 (dev/test), fast suite at the 386 baseline with assertions unchanged, no visual surface, no schema. Two phases as two commits on one iteration branch (the 386 shape — the base prompt's "expect two iterations" ran as independently-green commits since merge-to-main between iterations is the owner's step and the Open Gate directed a run to the binary finish).

### Phase 1 — Payments as a path package (`d55bd531`)

- `plugins/Payments/composer.json`: `nonprofitcrm/payments`, type library, MIT, explicit `version: 0.1.0` (the path-repo determinism device), PSR-4 `Plugins\Payments\` → `""`, requiring `php ^8.2` **and `stripe/stripe-php ^16.6`** — the dependency move (decision 4): root `composer.json` dropped its direct stripe line in the same commit; the lock resolved the identical v16.6.0, now transitive only.
- Root `composer.json`: path repository at `plugins/Payments` + bound `^0.1` require; lock committed same-commit; `composer validate --strict` clean.
- `distribution.json`: the Payments entry `folder` → `path` (gains `package` + `constraint`) in the same commit as the plugin composer.json — the 386 guard interlock held as designed. `plugins:manifest-sync` a provider-list no-op.
- Dockerfile: manifest COPY line ×2 composer stages. **Learning:** `DistributionManifestGuardTest` compares the COPY lines against the manifest's path entries as an *ordered* map — the lines must appear in manifest path-entry order (Payments before Events). Fixed in-session, folded into the runbook.

### Phase 2 — Payments extracted (`085052c9`)

- The runbook, top to bottom. Repo `github.com/abeuscher/crm-plugin--payments` (owner-created, public, verified unauthenticated); six files copied to the repo root; `version` field dropped (tags are the metadata); README written FM-style (contract 0.11.0, tags-are-releases, tests-stay-core, the dependency-ownership note); committed, tagged **`v0.1.0`**, pushed.
- Core swap: manifest entry `path` → `vcs` (sync no-op again); composer repository swapped to the vcs URL, require unchanged; `plugins/Payments/` removed; `composer update nonprofitcrm/payments` re-pinned the lock to the tag (git source at the exact tag commit + dist zip, v0.1.0); Dockerfile COPY lines removed. **Ordering learning:** with the source dir deleted the vendor symlink dangles and artisan cannot boot — run `composer update` *before* any artisan command (incl. `plugins:manifest-sync`). Folded into the runbook.
- Re-points: three test file reads (`PaymentModuleBoundaryTest` ×2, `ConventionDriftTest` ×1) to `vendor/nonprofitcrm/payments/…`, assertion content unchanged; two comment-only path mentions updated (`ProductPrice`, `AppServiceProvider`). Nothing else outside the plugin needed an edit — the surface-13 capability seam held, the foundation-plugin extraction property this run set out to prove.
- Guards: zero manual edits — `PluginPackageGuardTest`'s extracted set and `DistributionManifestGuardTest`'s alignment checks derived everything from the manifest walk, exactly as the 386 design intended.
- Docs: plugin contract **0.10.0 → 0.11.0** (Package shape gains the package-owned dependency posture; External repo gains the second consumer; reference implementations updated; changelog); runbook deltas folded (Stage B interlock + COPY ordering notes, dependency-carrying preconditions line, no-assets wording at step 3.2 and gate 4, the Payments worked-example survey lines); plan doc D1 line ✅, arc header advanced to D2.
- 388 prompts drafted mid-wait (owner-approved) from a fresh Events step-0 survey — see D2 kickoff input below.

---

## D2 kickoff input (what D2 — Events extracted — inherits)

1. **The survey ran at 387 close (full results in the 388 session prompt):** 89 files; five asset-path strings in three widget definitions (EventMiniCalendar scss+js, EventRegistration js, EventsListing scss+js — EventsListing's *second* scss entry is the core BlogPager cross-input and stays as-is); nine direct test file reads (`ContentListingWidgetsTest` ×6, `AppearanceConfigRenameTest` ×1, plus the two fixture migrator reaches `EventsRemovedTestCase:41` / `EventsDisabledViaFlagTestCase:37`); one line-keyed baseline entry (`WidgetColorTokenConsumptionTest:98` allowlists `plugins/Events/Widgets/EventsListing/styles.scss:128` — re-key to the vendor path); `PluginPackageGuardTest`'s schema-dump owner map names `plugins/Events`; the `Events` phpunit testsuite (29 files); Dockerfile COPY ×2; seven `thumbnails/static.png` that travel with the copy.
2. **Extraction empties `plugins/`** until D3's carve refills it: `PluginPackageGuardTest`'s in-repo test asserts `glob('plugins/*/composer.json')` non-empty — needs a scoped relaxation (tolerate an empty in-repo set). Root `"Plugins\"` PSR-4 stays (retirement is D12; D3 carves into `plugins/` again).
3. **The testsuite decision (drafted, confirm at 388's gate):** the `Events` suite travels with the package; phpunit re-points `plugins/Events/tests` → `vendor/nonprofitcrm/events/tests` so core CI runs the same 29 files unchanged — the plan § 6.3 "central build runs the distribution's suites" disposition made real; per-plugin CI stays D11.
4. **Gate 4 is live for the first time since LogoGarden:** after the asset rewrite, `widgets:sync` + `build:public` + bundle-content proof for the three asset-carrying widgets *and* the BlogPager pager styles.
5. **This run's mechanics learnings apply directly:** COPY lines in manifest path-entry order (moot at D2 — the set empties), composer-update-before-artisan at step 5, `plugins:manifest-sync` is a no-op on both walks.
6. **No dependency move at D2** — Events requires `php` only.

---

## Security considerations

No new endpoint, gate, or input path; no code changed inside the plugin — its files moved verbatim (webhook signature verification, capability gating, and the boundary guards all pass with assertions unchanged, now scanning the vendor path). Supply-chain posture: the plugin now arrives from a public GitHub repo pinned in the lock to an exact tag commit (git source + dist zip) — same trust model as LogoGarden, established at 385; core bumps deliberately via `composer update`. The stripe SDK's resolution is unchanged (same version, now transitive). `deploy.yml` untouched; FM surfaces untouched; CRM contract stays v2.7.0; client-billing guards unaffected.

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`. Images build exactly as before from the deploy pipeline's perspective (`deploy.yml` zero-diff); `composer install` fetches the public package credential-free — proven in-image locally and by CI's image-building job. VERSION bumped 0.386.01 → **0.387.01**.

---

## Deferred decisions

- **Per-plugin CI + plugin-subject test moves** — accepted debt, unchanged; resolves at D11 (Payments' subject tests stay core; the D2 testsuite decision is about files already inside the plugin).
- **Per-distribution composer sets / code absence** — Stage E, recorded boundary.
- **`baseDir()`-derived asset declarations** — still backlog.
- **Products/store code's home** — plan doc § 6.8; resolves at D8's prompt draft.
- **Playwright chrome-parity failure** — still the pre-existing parked item, non-gating.
- **Stale local `.env.production`** — still the owner's to delete (384 rider).

---

## Test result

- **Fast Pest:** 3263 passed, 0 failed (parallel) — the 386 baseline exactly, both phases; assertions unchanged (three file-read re-points only).
- **Route parity:** 272/272 (dev/test environment), both phases.
- **Composer:** `validate --strict` clean, both phases; lock pins the tag (git source at the exact commit, dist zip, v0.1.0); stripe v16.6.0 identical, transitive.
- **Runbook gates:** 1–3 pass; gate 4 **n/a — plugin declares no assets** (recorded, not skipped); gate 5 in-image proof clean (package fetched from GitHub inside `docker build`, provider + Stripe SDK autoload via the optimized `--no-dev` autoloader, `plugins/Payments` absent); gate 6 **CI `Tests` aggregate green** on the pushed branch (fast + slow; Playwright = the parked non-gating item).
