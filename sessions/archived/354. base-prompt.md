# Base Prompt — Session 354 (Editor Chrome Fidelity — header/footer parity with the front end)

**Start now.** Execute the reading list below, open the session prompt at `sessions/354. Editor Chrome Fidelity — header-footer parity.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **354. Editor Chrome Fidelity — header/footer parity with the front end (+ a small page-builder housekeeping fold).**

This is **not a `release-plan.md` entry** — it is an emergent, owner-reported fidelity bug plus a small housekeeping batch. The back-end page-builder editor now renders the site header/footer into the preview canvas, but they are **not styled correctly**; the **front end (public render) is the source of truth.** This is a concrete, bounded instance of the standing **editor↔public architecture stub** (Post-Beta backlog, surfaced session 333) — the editor and public site are separate render paths that can drift. Session 340 (**Public Chrome Parity**) brought the *public* `ChromeRenderer` path to parity; this session does the **editor side** of the same chrome, header/footer only.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract **stays at v2.4.0** (inherited from session 353, which added the count-only `data_hygiene` `/api/health` subcheck). No `docs/fleet-manager-agent-contract.md` or Cross-Repo-block changes.

**Session shape:** design/fidelity, **Playwright-driven** (the dev loop is visual diff editor-vs-front-end at the preset viewports), with a small **housekeeping fold** of two page-builder-editor-appearance items. The scoping is already done (carried in the session prompt) — the editor reuses the same server-rendered chrome HTML as the front end but **omits the public layout's wrapper structure** and renders it in a `zoom` + container-query scope, so chrome-specific styles don't attach. The fix has a bounded floor (reproduce the public wrappers in the editor bands) and a Playwright-verified tail (overlay-nav vars, the preset-viewport contexts, the dropped chrome `styles` blob).

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is a **stub-driven/emergent** session, so use the fuller session-prompt shape (explicit phases, out-of-scope).
2. Read `sessions/session-outlines.md` — the active-tracks block, the **Public Chrome Parity (340)** history, and the **editor↔public architecture stub** in the Post-Beta backlog (the parent of this bug).
3. **Read the session prompt's scoping section carefully** (`sessions/354. Editor Chrome Fidelity — header-footer parity.md`) — it carries the two render paths side by side and the named cause. Don't re-derive it; verify it against current code and build from it.
4. **Read the two render paths:**
   - **Front end (source of truth):** `resources/views/layouts/public.blade.php` (the chrome block, ~lines 226–250) — chrome HTML is wrapped in `div.site-nav-wrapper[.site-nav-wrapper--overlay]` (carrying the `--overlay-*` nav color vars) → `header.site-header`, and `footer.site-footer`, as direct children of `body.np-site`. Note the inline `<style>@layer host { … }</style>` blocks (nav font-family, content scheme vars) the public layout emits.
   - **Editor:** `resources/js/page-builder-vue/components/PreviewCanvas.vue` (~lines 262–345) — the same server HTML is injected via `v-html="store.chromeHeader.html"` but wrapped in `div.preview-chrome > div.preview-chrome__html` (no `.site-nav-wrapper`, no `.site-header`/`.site-footer`), inside `div.np-site.widget-preview-scope` which carries `zoom` + `container-type: inline-size` at a fixed preset width.
   - **The editor's chrome payload:** `app/Livewire/PageBuilder.php` → `chromeBands()` (~line 443) — what it threads to the Vue store (`html`, `styles`, `edit_url`) and, crucially, **what it does not** (the overlay-nav state + `--overlay-*` vars the public layout computes). `resources/js/page-builder-vue/types.ts` (`ChromeBand`).
   - **The shared renderer:** `app/Services/Media/ChromeRenderer.php` — returns `{html, styles, scripts}`; note `styles` (the editor injects only `html`, and the public layout also emits only `['html']` — verify whether `styles` is load-bearing before treating it as a cause).
   - **The chrome SCSS:** `resources/scss/_base.scss`, `resources/scss/_layout.scss` (the `.np-site` / `.site-header` / `.site-footer` / `.site-nav-wrapper` rules that the editor's wrapper omission breaks).
5. Read `docs/app-reference.md` for environment/container names, the **front-end build command** (the editor is `resources/js/page-builder-vue/**` → `npm run build`; use the WSL node PATH, see the build-command note), and the **isolated Playwright e2e stack** (`./dev e2e:up` / `e2e:down`).
6. Read the two housekeeping items being folded (in `sessions/housekeeping-inbox.md`) — **re-verify each against current code at the walk** (the staleness guard; both carry older capture stamps):
   - `[0.344.01]` Page-builder **column drag-handle z-index** conflict with the admin header (handles render *over* it as they scroll offscreen).
   - `[0.319.01]` **Column resize-handle text scaling vs page zoom** (handles oversized to offset zoom; text should scale down toward actual size — best-effort ramp, not an exact fix).
   - *(Optional stretch, decide at the Open Gate)* `[0.324.01]` the **widget "knockout" controllability Playwright guard** — a reusable per-widget legibility/no-element-left-behind e2e gate. Thematically aligned but a test-infra build, not an appearance fix; fold only if the owner wants the durable gate this session.

---

## Starting state inherited from session 353

- **CRM contract at v2.4.0** — session 353 added the count-only `data_hygiene` `/api/health` subcheck (additive v2.3.0 → v2.4.0). **This session is non-boundary; the contract is unchanged.**
- **Schema baseline:** no migrations; `database/migrations/` is empty (post-348-squash). No schema expected this session — chrome fidelity is a render/markup/SCSS concern.
- **Fast Pest green at 2930 / 0** (353 close). Expected delta: small. Chrome fidelity is largely Vue/SCSS (Pest doesn't see it); the housekeeping items are page-builder UX. New Pest only if a fold adds server-side behaviour.
- **Playwright** — baseline preserved. **This session uses Playwright as the development loop** (visual diff: editor canvas vs the public render of the same page, at the preset viewports). Per the base-prompt rule, dev-only specs are torn down at close; a spec that becomes a standing regression guard (e.g. the optional knockout guard, or a chrome-parity guard if worth keeping) is committed and documented in the log. Run Pest and Playwright **sequentially** if both run.
- **Planning state:** no release-plan change. Parent is the **editor↔public architecture stub** (Post-Beta backlog); this session is a bounded header/footer instance of it. Public side already done at 340.
- **Housekeeping inbox** (`sessions/housekeeping-inbox.md`) — **this session folds a small batch** (the two editor-appearance items above; the knockout guard optional). Re-verify each at the walk; fold survivors out at close per the standard digest. Not a full inbox drain.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Front end is the source of truth — only the editor moves.** Never change the public `ChromeRenderer` / `public.blade.php` render to make the editor match. If the public render itself looks wrong, stop and surface it — that's a different bug. The editor's job is to reproduce the public chrome faithfully.
- **Preserve the editor's chrome affordance.** The `.preview-chrome` hover outline + "Edit header/footer ↗" deep-link into the template chrome editor must survive the fidelity fix — fold the public wrapper structure *under* (or alongside) the affordance layer, don't delete the affordance.
- **Verify objectively with Playwright; pull the owner in for the visual call.** Editor-vs-front-end parity is a visual judgment — drive the comparison with Playwright (screenshots at the preset viewports) and close the objective loop yourself (wrappers present, vars threaded, no console errors), then surface the visual result for the owner's eye.
- **Build after Vue/SCSS changes.** `resources/js/page-builder-vue/**` and `resources/scss/**` → `npm run build` before announcing completion (Pest won't catch a Vue/SCSS break).
- **Housekeeping staleness guard.** Re-verify each folded item against current code before implementing it; drop or re-scope dead items at the walk, not mid-implementation.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the **scoped cause** (editor reuses the public chrome HTML but omits the `.site-nav-wrapper`/`.site-header`/`.site-footer` wrappers + the `--overlay-*` vars and renders in a `zoom`/container-query scope) and the **floor-vs-tail** plan (reproduce the public wrappers in the editor bands → Playwright-verify the overlay-nav vars, the preset-viewport contexts, and the `styles`-blob question); (2) the **housekeeping pairing** — `[0.344.01]` drag-handle z-index + `[0.319.01]` resize-handle text scaling, with the `[0.324.01]` knockout Playwright guard as an explicit **optional stretch** (your recommendation on whether to fold it); (3) the **iteration shape** — whether the chrome fix and the housekeeping fold split across iterations (Rule 11) or land together. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/354. Editor Chrome Fidelity — header-footer parity — Log.md` — the cause + the wrapper-parity fix, what was threaded through `chromeBands()`, the `styles`-blob disposition, the Playwright dev loop (and which specs were kept as standing guards vs torn down), the housekeeping items folded (with the staleness-walk dispositions), the build run, and Pest results.
- **Bump `VERSION`** to `0.354.<iteration>`.
- **Non-boundary:** no contract-doc or Cross-Repo-block change (CRM stays v2.4.0). Confirm `FmContractVersionParityTest` still green if any Fleet file is incidentally touched (it shouldn't be).
- **Housekeeping inbox:** fold the surviving items out per the standard digest; note any dropped-at-walk items under "Recently dispositioned." Digest the capture buffer (`housekeeping-incoming.md`).
- **Archive** `sessions/353. *.md` → `sessions/archived/`.
- **Commit on `session-354/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: **reproduce the public wrapper structure exactly** (`.site-nav-wrapper` + overlay class/vars where applicable, `.site-header`/`.site-footer`) rather than re-styling the editor's own band classes to approximate it — match the source of truth, don't reinvent it. Keep the change editor-side; don't touch the public render. Don't expand the housekeeping fold beyond the agreed items.
