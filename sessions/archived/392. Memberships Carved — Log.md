# Session 392 — Memberships Carved — Log

**Date:** 2026-08-19
**Branch:** session-392/1

---

## What was built

Arc D4 of the Plugin Architecture arc, first session of the carve → package → extract block — the Memberships behavioral surface carved into `plugins/Memberships/`, refilling the `plugins/` directory D3's extraction emptied. The 389 Donations-carve playbook at the smallest scale yet (no mailables, no email-template handles, no scheduled commands, one widget asset): surface 10 gained its memberships slice via the **second cross-repo inversion** — a sibling `crm-plugin--payments` **v0.3.0** release taking the webhook's last two inline membership branches. Behavior-preserving throughout: routes 272/272 **byte-identical** (method+URI+name+middleware diffed against main via stash-capture), fast suite 3314 → **3349/0** purely additive, no schema work, no visual surface. One iteration, one core commit (`f67c408e`) plus the plugin release commit in the sibling repo (`c4c9a0d`, tagged `v0.3.0`).

### Phase 1 — The carve

- **Plugin shape:** `plugins/Memberships/` + `MembershipsServiceProvider` (namespace `Plugins\Memberships\`, handle `memberships`); `distribution.json` gains the fifth entry, source kind `folder` (+ `source.path`); `plugins:manifest-sync` regenerated `config/plugins.php` with the fifth provider line. The 388 empty-`plugins/` guard relaxations tolerated the refill by construction — zero guard edits.
- **Moved with namespace change, behavior identical:** `MembershipCheckoutController` + the `membership.checkout` POST (plugin route file, web group + `throttle:10,1`, registered ahead of the catch-all — the route name stays byte-identical for the core PortalSignup widget's render, decision 5); `MembershipObserver` (`#[ObservedBy]` stripped from the model, `observe()` in provider `boot()`); `MembershipPolicy` (`AuthServiceProvider` line → `Gate::policy()` in `boot()`); `MembershipResource` (+3 pages), `MembershipTierResource` (+3 pages), `MemberResource` (+1 page — the Contact-model resource rode with the plugin per decision 2), `ImportMembershipsPage` + progress page via `AdminContribution` discovery (no permission names declared — the extracted-vertical nuance); the importer (both field registries + `MembershipFieldMapper` → `plugins/Memberships/Importers/`) as an `ImporterContribution` (slug `memberships`, headers/field-map byte-identical to the retired core methods); both widgets nested under `Widgets/` (`template()` overrides via `plugin-memberships-widgets::`; PricingChart's scss path re-pointed); the two import blade views to the `plugin-memberships::` namespace.
- **Core strips, registry-resolved:** `CsvTemplateService::membershipHeaders()` + its match arm, `ImporterPage::downloadMembershipsTemplate()`, the hub blade's hard-wired memberships card + template button (the plugin loop renders both), `FixtureRunner`'s page-map entry, `SeedFakeImportSourcesCommand` + `GenerateFakeImportCsvsCommand` memberships blocks (registry-gated, the donations pattern), `WidgetServiceProvider`'s two registrations. The admin tour map's memberships entry moved from the class-named `$resources` array to the route-name + permission lookup (the events/donations precedent).
- **Surface 10 — the memberships slice, the second cross-repo inversion (contract-first, decision 4):** contract wording landed first, then `crm-plugin--payments` **v0.3.0** (tagged, pushed): the checkout `membership_id` branch became the third `CheckoutSettled` dispatcher (dispatch-add + branch-delete); the `invoice.payment_succeeded` membership-renewal branch was a **pure branch-delete** — `InvoiceSettled` was already dispatch-always since v0.2.0 (the open-gate question 2 answer). Payments retains zero `Membership` reads. Core same-commit: require + manifest constraint widened `^0.2` → `^0.3`, lock pinned to the tag (exact commit, dist zip), and two plugin listeners owning the former branch bodies verbatim — `PromotePaidMemberships` (promotion by `membership_id`, period from the tier's billing interval, one Transaction; idempotent via the pending-status guard carried over) and `RecordMembershipRenewal` (period extension + one Transaction per invoice id). **No `InvoicePaymentFailed` listener** — membership invoice failures were never handled and remain unhandled (preservation cuts both ways), proven by a behavioral test.
- **Rule-5 membership pass (the reviewed split):** new phpunit `Memberships` testsuite at `plugins/Memberships/tests` — **3 subject files moved** (MembershipImportFieldMapper, PricingChartDefinition, MembershipStatusDefinition) **+ 1 block split** (six membership-checkout/promotion tests lifted out of core's `PaidCheckoutTest` into a plugin `MembershipCheckoutTest`; the events/portal/free-signup tests stay core — the free-signup subject is `Portal\SignupController`). **Stayed core with re-pointed imports** (memberships as fixture data / core subjects): DeletionGuard, FieldRegistryExclusion, ImportFinancial ×2, ImportOrgSentinels, ImportSession191/194, ListExportActions, TrashManagement. `MembershipTest` (pure model tests — models are core) and the resolver/projector unit tests (core engine) stayed untouched. **Reduction pass: 0 deletions** — no strict duplicates found.
- **New tests (+35 fast):** enabled twin `MembershipsPluginSession392Test` (provider/route/observer/policy/widgets/admin discovery/importer socket + both listeners' promotion/replay/self-filter/renewal coverage + the failures-stay-unhandled proof + the webhook-dispatch proof — the first behavioral coverage the membership renewal path has ever had), removal mirror `MembershipsPluginRemovalSession392Test` on `MembershipsRemovedTestCase` (no migrator path — schema is core this session; the caveat expires at 393), flag-vanish `MembershipsDisabledViaFlagSession392Test` on its fixture, boundary guard `MembershipModuleBoundaryTest` (zero `Plugins\Memberships\` references in `app/`, zero allowlist, positive control).
- **Docs:** plugin contract **0.16.0** (surface 10 memberships slice — the second cross-repo inversion, now a pattern; the self-filter key list gains `membership_id`; changelog); plan doc arc header + D4 line annotated (block open, carve done); `docs/app-reference.md` six rows re-pointed; `docs/runbooks/permission-matrix.md` three resource links re-pointed.
- 393 prompts drafted post-verification (owner-approved): Memberships Packaged & Extracted — the one-session block closer per decision 8 (below), redraw input and composition-safety dispositions front-loaded.

### Verification

- Route parity: 272/272 **byte-identical** on method+URI+name+middleware (stash-capture diff against main).
- Gate 4 on the moved asset: `widgets:sync` (41 rows, handles unchanged; the `pricing_chart` row carries the plugin path) → `build:public` → the built bundle proven to carry the `pricing-chart__column--emphasized` / `pricing-chart__attribute-row` rules.
- Dev-stack smoke: home + admin login 200; checkout POST 419 (CSRF — the plugin-registered route is live); 5 plugins registered, provider loaded.
- Composer: `validate --strict` clean; lock pins `nonprofitcrm/payments` v0.3.0 at the exact tag commit, dist zip.

### Notable drift from the survey (adapted, logged)

- **Open-gate question 2 answered from live code:** `CheckoutSettled` did NOT fire for membership sessions (donation + event metadata only) — the checkout side was dispatch-add + branch-delete; the invoice side branch-delete only.
- **The portal seam writes ACTIVE complimentary memberships, not pending** — `Portal/SignupController` creates `status => 'active'` rows for free tiers (the survey and the 391 log said "pending"). No consequence this session (the seam is untouched); the corrected fact is in the 393 composition-safety survey.
- **`MembershipsFixtureBuilder` stayed core** — the survey grouped it with the plugin move, but the shipped donations/events pattern keeps fixture builders core (self-contained, no plugin reads); followed the live precedent.
- Four core tests hard-coded retired reaches and were updated additively: three files calling the retired `membershipHeaders()` (→ `headersFor('memberships')`) and `InlineEditingFoundationSession304Test`'s direct read of the old PricingChart template path.
- Count/list updates: `PluginActivationTest` ×2 (disable-all list + the unknown-handle message's valid set), `LogoGardenPaymentsDisabledViaFlagSession384Test` (superset count 4 → 5).

---

## D4 block handoff (what the package + extract session — 393 — inherits)

1. **Block shape decided (decision 8): one combined session.** Package + two-table squash redraw + extraction compress into 393 (the D1 Payments precedent) — justified by the carve's findings: the smallest redraw yet, all extraction edges four-times proven, no runbook deltas in the last run.
2. **The two-table redraw input is front-loaded into the 393 prompt** (captured from the dump at this close): creation order `membership_tiers` → `memberships`; five outbound FKs from `memberships` incl. `contact_id` **ON DELETE RESTRICT** (the FK-as-behavior candidate — verify net coverage); **zero inbound FKs from core tables**; **no sequences** (uuid PKs — the first redraw with none); the `billing_interval` check constraint wants raw-DDL fidelity.
3. **Composition-safety survey front-loaded:** the portal seam is the likeliest trip — `PortalSignup`'s `route('membership.checkout')` render throws on a memberships-absent composition, and `Portal/SignupController`'s `exists:membership_tiers,id` rule + complimentary-tier write reach plugin-owned schema post-redraw. Route-presence gates proposed; the memberships-absent identity check arbitrates per the 383/390 discipline. Recorded-debt candidates listed in the 393 prompt.
4. **The twin fixtures carry the expiring caveat:** `MembershipsRemovedTestCase` / `MembershipsDisabledViaFlagTestCase` register no migrator path — they must gain it the moment schema moves.
5. **One in-plugin path read re-points in the extracted copy** (the 391 step-3 nuance): `PricingChartDefinitionTest`'s asset assertion. The asset declaration itself goes vendor-relative at extraction (gate 4 runs again).
6. **The owner creates `crm-plugin--memberships`** (public) before 393's extraction phase — an open-gate item there.
7. **Sibling exclusivity re-check:** the 392 removal mirror already asserts alongside four loaded plugins; 393's open gate re-greps the extracted suites against the new `memberships` importer contribution (the 389 find — expected clean).

---

## Security considerations

No new endpoint, gate, or input path; the carve moved files verbatim (checkout keeps its throttle, honeypot, form-timing, and silent-redirect protections; admin pages remain inside the panel auth stack via Filament discovery; policy gating and the permission matrix untouched — no permission names declared, the role matrix stays whole). The webhook inversion preserves Stripe signature verification in Payments and moves membership fulfillment behind core-owned events with the idempotency guards carried verbatim (the pending-status guard on promotion; the invoice-id transaction check on renewal); with Memberships absent, a late webhook writes nothing (proven by the removal mirror). Membership invoice failures gain no new handling — no new code path. Payments v0.3.0 is lock-pinned to the exact tag commit (the 385 supply-chain posture). `deploy.yml` untouched; FM surfaces untouched; CRM contract stays v2.7.0.

---

## Schema changes

None. The `memberships` and `membership_tiers` tables stay in the core dump; the redraw is session 393 (input recorded in the 393 prompt and the block handoff above).

---

## Production deployment notes

None beyond standard `migrate`. Deploy already runs `widgets:sync` + `build:public`, which picks up the moved PricingChart scss path (verified locally: the rebuilt bundle carries the pricing-chart rules; both `widget_types` rows keep their handles, so no content changes). `composer install` fetches Payments v0.3.0 credential-free from its public repo (lock-pinned). VERSION bumped 0.391.01 → **0.392.01**.

---

## Deferred decisions

- **The two-table squash redraw + packaging + extraction** — session 393, one combined session (decision 8 resolved at this close; prompts drafted).
- **Portal-seam composition-safety gating** — 393 (survey + proposed dispositions front-loaded; the identity check arbitrates).
- **D5 Member Portal survey** — captured at 393's close per its decision 9.
- **Model/factory moves** (§ 6.7 stands); **per-plugin CI** (D11); **root PSR-4 retirement** (D12); **events-handle registry migration** (a future `crm-plugin--events` release); **`baseDir()`-derived asset declarations** (standing backlog); **Playwright chrome-parity park** — all unchanged.

---

## Test result

- **Fast Pest:** 3349 passed, 0 failed (parallel) — 3314 baseline + 35 additive; every pre-existing membership assertion unchanged beyond path/namespace re-points and the five additive count/list updates.
- **`Memberships` testsuite:** 6 files at `plugins/Memberships/tests` — 65 tests, all green (runs inside the fast suite).
- **Route parity:** 272/272, byte-identical on method+URI+name+middleware (diffed against main via stash-capture).
- **Composer:** `validate --strict` clean; lock pins `nonprofitcrm/payments` v0.3.0 at the exact tag commit.
- **Asset gate:** `widgets:sync` (41 rows, handles unchanged) + `build:public` — bundle content verified (pricing-chart selectors present from the plugin path).
- **CI:** `Tests` aggregate **green** on the pushed branch (fast + slow); Playwright 63/1 — the one failure is the pre-existing parked chrome-parity spec (non-gating).
