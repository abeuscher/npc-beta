# Session 389 — Donations Carved — Log

**Date:** 2026-08-19
**Branch:** session-389/1

---

## What was built

Arc D3 of the Plugin Architecture arc, first session of the carve → package → extract block — the Donations behavioral surface carved into `plugins/Donations/`, refilling the `plugins/` directory D2 emptied. The 381 Events-carve playbook with the 388-era contract: two surfaces promoted to PROVEN — surface 7 (the email-template handle registry, built this session) and surface 10's donation slice (the webhook inversion, **cross-repo for the first time** — two sibling plugin releases shipped: `crm-plugin--payments` v0.2.0 and a `crm-plugin--events` v0.1.1 rider). Behavior-preserving throughout: routes 272/272 byte-identical (method+URI+name+middleware diffed against main), fast suite 3263 → **3309/0** purely additive, no schema, no visual surface. One iteration, one core commit (`7d2dc087`).

### Phase 1 — The carve

- **Plugin shape:** `plugins/Donations/` + `DonationsServiceProvider` (namespace `Plugins\Donations\`, handle `donations`); `distribution.json` gains the fourth entry, source kind `folder` (+ required `source.path` — a validation detail the prompt's shape sketch omitted); `plugins:manifest-sync` regenerated `config/plugins.php` with the fourth provider line (NOT a no-op, as predicted). The 388 empty-`plugins/` guard relaxations tolerated the refill with no revert, as designed.
- **Moved with namespace change, behavior identical:** `DonationCheckoutController` + the `donations.checkout` POST (plugin route file, web group, registered ahead of the catch-all); both observers (`#[ObservedBy]` attributes stripped from the models, `observe()` in provider `boot()`); all three policies (`AuthServiceProvider` lines → `Gate::policy()` in `boot()`); both mailables (`Plugins\Donations\Mail\`); `DonationResource` (+4 pages, 2 relation managers), `FundResource` (+3 pages), `DonorsPage`, both import pages via `AdminContribution` discovery (route names/URIs/middleware identical; no permission names declared — the extracted-vertical nuance); the importer (both field registries + `DonationFieldMapper` → `plugins/Donations/Importers/`) as an `ImporterContribution` (slug `donations`, headers/field-map byte-identical to the retired core methods); both widgets nested under `Widgets/` (`template()` overrides via `plugin-donations-widgets::`, DonationForm's js asset path re-pointed); three page views to the `plugin-donations::` namespace.
- **Core strips, registry-resolved:** `CsvTemplateService::donationHeaders()` + its match arm, `ImporterPage::downloadDonationsTemplate()`, the hub blade's hard-wired donations card + template button (the plugin loop renders both in the same visual position), `FixtureRunner`'s page-map entry, `SeedFakeImportSourcesCommand` + `GenerateFakeImportCsvsCommand` donations blocks (registry-gated, the events pattern). Core reaches into plugin admin classes replaced with route-name lookups that degrade gracefully: `SetupChecklist::checkDefaultFund` (funds URL), the admin tour map (donations entry, the events precedent).
- **Surface 7 — the email-template registry:** core `App\Plugins\EmailTemplateRegistry` (`contribute`/`defaultsFor`/`all`, the ImporterRegistry pattern), bound in `PluginServiceProvider` before plugins register. `EmailTemplate::defaults()` resolves core array → registry → empty; the base seeder's two inline donation entries replaced with a loop over registry contributions. The plugin contributes `donation_receipt` + `donation_acknowledgment` byte-identically at `register()`. Semantics tested core-side (`EmailTemplateRegistrySession389Test`: precedence, fallback, admin-edit wins, seeder exposure).
- **Surface 10 — the donation slice, the first cross-repo inversion (contract-first, decision 5):** contract wording landed, then `crm-plugin--payments` **v0.2.0** (tagged, pushed): the webhook's **three** donation-reaching branches invert — `donation_id` checkout → `CheckoutSettled` dispatch; `invoice.payment_succeeded` → new core `InvoiceSettled` dispatched for *every* subscription invoice (the membership branch stays inline until D4; disjoint subscription ids make dispatch-always safe); `invoice.payment_failed` → new core `InvoicePaymentFailed`. Payments retains zero `Donation` reads (imports, both handler methods, and the acknowledgment path all gone). Core same-commit: require + manifest constraint widened `^0.1` → `^0.2`, lock pinned to the tag, and three plugin listeners owning the former branch bodies verbatim — `PromotePaidDonations` (promotion, one Transaction, queued acknowledgment behind the `acknowledged_at` marker), `RecordRecurringDonationPayment` (renewal Transaction + `donation_received` activity on true renewals), `MarkRecurringDonationPastDue` (past_due + failed Transaction). Each listener self-filters on its own key before any read.
- **The v0.1.1 Events rider (decided per plan and logged, not in the prompt):** two sibling-plugin facts surfaced. (1) `EventsPluginRemovalSession381Test` asserted `getPluginImporters()` is *empty* with Events stripped — false once Donations contributes; vendor is never edited, so the fix is a release. (2) The Events `CheckoutSettled` listener predated a second dispatcher and would have logged a spurious no-pending warning on **every** donation checkout — a behavior delta this carve would otherwise introduce. One minimal `crm-plugin--events` **v0.1.1** (tagged, pushed, lock bumped within `^0.1`): the removal-mirror assertion generalized (`not->toContain('events')`) and the listener's metadata self-filter guard added. The listener-self-filter rule is now normative in contract surface 10.
- **Rule-5 membership pass (the reviewed split):** new phpunit `Donations` testsuite at `plugins/Donations/tests` — **9 subject files moved** (DonationCheckout, DonationAcknowledgment, TaxReceipt, SoftCreditsRelationManager, RecentDonationsWidgetImportedRows, DonationImportFieldMapper, DonationCreditsImporterUntouched; Unit: DonationStatusMapper, RecentDonationsDefinition) **+ 1 block split** (the `DonationCreditObserver` test lifted out of core's `OrgClusterObserversTest` into a plugin file — the observer is plugin-owned; the org-cluster file keeps its core observers). **Stayed core with re-pointed imports** (donations as one type among many / core subjects): DeletionGuard, FieldRegistryExclusion, ImporterMappingRow, ImportOrgSentinels, ImportSession191/194, ListExportActions/Service, ImportFinancial + ImportFinancialSourceAndLedger, FakeImportCsvGenerator, SeedFakeImportSources, DonationCreditModel (pure model relations — models are core), FundCampaign, FinancialSourceWritePath, OrgClusterObservers (minus the split block), StripeCheckoutBranding, the WidgetPrimitive unit files. **Reduction pass: 0 deletions** — no strict duplicates found.
- **New tests (+46 fast, +1 slow):** enabled twin `DonationsPluginSession389Test` (provider/route/observers/policies/widgets/admin discovery/importer socket/email registry + the three listeners' dispatch/replay/no-op-on-other-vertical coverage + the three webhook-dispatch proofs — the first behavioral coverage the recurring/failed-invoice donation paths have ever had), removal mirror `DonationsPluginRemovalSession389Test` on `DonationsRemovedTestCase` (no migrator path — schema is core this session; the caveat expires at 390), flag-vanish `DonationsDisabledViaFlagSession389Test` on its fixture, boundary guard `DonationModuleBoundaryTest` (zero `Plugins\Donations\` references in `app/`, zero allowlist, positive control), and the registry-semantics file.
- **Docs:** plugin contract **0.13.0** (surface 7 PROVEN; surface 10 rewritten — three core events, the self-filter rule, the post-extraction release pattern, the rollout list trimmed; changelog); plan doc arc header + D3 line annotated (block open, carve done); `docs/app-reference.md` rows re-pointed; widget-development doc's DemoSeeder row re-pointed.
- 390 prompts drafted post-verification (owner-approved): Donations Packaged — the 383 package + squash-redraw playbook, redraw input and composition-safety dispositions front-loaded.

### Notable drift from the survey (adapted, logged)

- The webhook had **three** donation-reaching branches, not two groups as worded — `invoice.payment_failed` (past_due + failed Transaction) alongside checkout-settled and the renewal; the one-release decision covered all three, with a second new core event (`InvoicePaymentFailed`).
- Decision 3 listed `portal_form_collision` among core handles in the defaults array — it has no array entry (only the seeder carries its copy); captured to the housekeeping buffer, no decision forced.
- `DonationImportFieldRegistry` referenced `TransactionFieldRegistry`/`ContactFieldRegistry` unqualified (same-namespace pre-move) — imports added; the sibling-exclusivity assertion in the events removal mirror (above) was the same class of find at test level.
- Five core tests hard-coded counts/handle-lists that grew by one plugin (`PluginActivationTest` ×2, `LogoGardenPaymentsDisabledViaFlagSession384Test`, plus three files calling the retired `donationHeaders()`) — assertions updated additively.
- The importer hub's donations card now renders the generic plugin blocked-state text (adds the review-queue sentence the hard-wired card lacked) — cosmetic, the events precedent.

---

## D3 block handoff (what the package session — 390 — inherits)

1. **The four-table redraw input is front-loaded into the 390 prompt** (captured from the dump this session): FK map and creation order `funds` → `donations` → `donation_credits` → `donation_receipts`; no core table carries an FK into any of the four; `donation_receipts.contact_id` is **ON DELETE RESTRICT** — that FK *is* the blocks-contact-force-delete behavior and travels with the table.
2. **Composition-safety reaches surveyed** (the donations-absent identity check's hunting ground): `SetupChecklist::checkDefaultFund` (`Fund::query()->exists()` — the likeliest `PageContextTokens` analogue; its URL half is already route-gated from this session), `Contact` donor scopes/`hasDonations()`, `ContactResource`/`OrganizationResource` delete-path reads, `RandomDataGenerator`, the `fund`/`donation` resolver arms, model-type-gated import-session reads. Proposed dispositions in 390's decision 6.
3. **The fixtures carry an expiring caveat:** `DonationsRemovedTestCase`/`DonationsDisabledViaFlagTestCase` register no migrator path — they must gain `plugins/Donations/database/migrations` the moment schema moves.
4. **What the inversion surfaced for future extractions:** sibling-plugin tests may encode single-plugin exclusivity assumptions (empty-list assertions) — grep the extracted suites for them before a carve adds a sibling; and every `CheckoutSettled`-family listener must self-filter on its own routing key (now normative, surface 10).
5. **Extraction (391) notes:** DonationForm's js asset path goes vendor-relative then (gate 4 live, one asset); the `Donations` testsuite re-points to the vendor path; repo `crm-plugin--donations` to be owner-created.

---

## Security considerations

No new endpoint, gate, or input path; the carve moved files verbatim (checkout keeps its throttle + validation, admin pages remain inside the panel auth stack via Filament discovery, policy gating and the permission matrix are untouched — no permission names declared, the role matrix stays whole). The webhook inversion preserves Stripe signature verification in Payments and moves donation fulfillment behind core-owned events with idempotency guards carried verbatim (payment-intent/invoice-id transaction checks + the `acknowledged_at` per-gift marker); with Donations absent, a late webhook writes nothing (proven by the removal mirror). The two sibling releases are lock-pinned to exact tag commits (the 385 supply-chain posture, fourth and fifth consumers). `deploy.yml` untouched; FM surfaces untouched; CRM contract stays v2.7.0.

---

## Schema changes

None. The four donation tables stay in the core dump; the redraw is session 390 (input recorded in the 390 prompt and the block handoff above).

---

## Production deployment notes

None beyond standard `migrate`. Deploy already runs `widgets:sync` + `build:public`, which picks up the moved DonationForm asset path (verified locally: the rebuilt bundle carries the `donationForm` Alpine component; both `widget_types` rows keep their handles, so no content changes). `composer install` fetches Payments v0.2.0 and Events v0.1.1 credential-free from their public repos (lock-pinned). VERSION bumped 0.388.01 → **0.389.01**.

---

## Deferred decisions

- **The four-table squash redraw + packaging** — session 390 (prompts drafted, redraw input front-loaded).
- **Extraction to `crm-plugin--donations`** — session 391 (the block closer; prompts drafted at 390's close).
- **Events adopts the surface-7 registry for its three handles** — still a future `crm-plugin--events` release (v0.1.1 deliberately did not touch them).
- **Surface 9 (contact-screen socket)** — not forced; the donations data shape satisfies the normative rule, no admin socket was stranded. Promotion stays on exercise.
- **EmailTemplateResource token hints + `portal_form_collision` defaults inconsistency** — two capture-buffer items folded to the inbox at this close.
- **Per-plugin CI (D11), model/factory moves (§ 6.7), root PSR-4 (D12), Playwright chrome-parity park** — all unchanged.

---

## Test result

- **Fast Pest:** 3309 passed, 0 failed (parallel) — 3263 baseline + 46 additive; every pre-existing donation assertion unchanged (path/namespace re-points and five additive count/list updates only). The one new slow-group test (seeder → registry rows) passes.
- **`Donations` testsuite:** 18 files at `plugins/Donations/tests` — 99 fast + 1 slow, all green.
- **Route parity:** 272/272, byte-identical on method+URI+name+middleware (diffed against main via stash-capture).
- **Composer:** `validate --strict` clean; lock pins `nonprofitcrm/payments` v0.2.0 and `nonprofitcrm/events` v0.1.1 at exact tag commits.
- **Asset gate:** `widgets:sync` (41 rows, handles unchanged) + `build:public` — bundle content verified (`donationForm` component present from the plugin path).
- **CI:** `Tests` aggregate **green** on the pushed branch (fast + slow; Playwright = the parked non-gating chrome-parity item).
