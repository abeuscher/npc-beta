# Base Prompt — Session 365 (Fleet Manager — A2 Node-Operations Parity Closure)

**Start now.** Execute the reading list below, open the session prompt at `sessions/365. Fleet Manager — A2 Node-Operations Parity Closure (Cross-Node Restore Portability + Verification).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **365. Fleet Manager — A2 Node-Operations Parity Closure (Cross-Node Restore Portability + Verification)**.

This is **execution-order position 46** in `sessions/release-plan.md` — **A2. Fleet Manager — node operations parity**, the release-gated Phase A entry (moved post-Public-Website-Complete at the 282 audit). **Critical orientation: the A2 entry's FM-side framing is severely stale.** It says "FM-side resumes at FM 013+ after FM 012 absorbs v2.0.0 + v2.1.0"; the FM repo is actually at **session 048 complete / 050 drafted**, and per the entry's own per-capability status notes all four A2 capabilities are code-complete on both sides: (a) provisioning shipped FM 018 (extended by 039 demo profile, 040 adopt-existing, 047 public-website); (b) backup trigger CRM 263 + FM 020 (pending manual-test sign-off); (c) restore CRM 268 + FM 021/022 (plus same-node restore at FM 038, beyond original scope); (d) logs CRM 251 + FM 013. What A2 still needs is **one live CRM-side defect fixed, live verification, the runbook artifact, and planning-doc reconciliation** — not a build. The defect: **the cross-node restore role-grant bug** (FM 042 log, Deferred decisions) — a CRM backup's `pg_dump` carries `GRANT … TO <source node's read-only role>` (the node-specific role FM 037 introduced), so restoring that blob on a *different* node aborts on the missing role. FM 042's live demo-baseline-restore verification has been blocked on it since 2026-06-05; the CRM side never absorbed the register item.

This session is **NOT boundary-touching** in the contract sense — no `/api/*` surface or response schema changes; CRM contract stays at **v2.5.0**. The fix lands in the backup *dump configuration* (`config/database.php` pgsql `dump` options / `config/backup.php`), which is not contract surface. Two bookkeeping notes ride along without a version bump: (1) a possible documentation revision in `docs/fleet-manager-agent-contract.md` noting that blobs dumped after this fix carry no role privileges (the 253/291/335 no-bump revision precedent — decide at the gate whether it's warranted); (2) the Cross-Repo block in `sessions/session-outlines.md` gets its stale FM-side status corrected as part of Phase 1 reconciliation (FM is at 048, not 012; v2.2.0/v2.3.0 absorptions long shipped at FM 020/021/022; v2.4.0 absorption is FM 050, drafted; v2.5.0 absorption pending).

**Session shape:** hybrid — one bounded fix + verification + reconciliation. Phase 1 reconciles the stale A2/Cross-Repo planning state against FM reality; Phase 2 fixes cross-node backup-restore portability (dump-side `--no-privileges --no-owner`); Phase 3 is owner-driven live verification of the four A2 capabilities (the owner drives the FM UI; the agent verifies CRM-side outcomes); Phase 4 drafts the A2 operator-runbook handoff into `/home/al/fleetmanager/sessions/` (the established handoff-doc pattern) for FM-side finalization.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference.
2. Read the **A2 entry** in `sessions/release-plan.md` (execution-order position 46; entry under "#### A2") — canonical for the success criterion (four capabilities + runbook artifact), but treat its FM-side status notes as stale per above.
3. Read `sessions/tracks/fleet-manager-agent.md` — status snapshot and carry-forwards (also stale on FM-side state; it compresses at Phase 1).
4. Read the **FM-side ground truth**: `/home/al/fleetmanager/sessions/completed-sessions.md` (the real FM session history), the **FM 042 log** (`/home/al/fleetmanager/sessions/archived/042. Demo-node reset via baseline restore — Spaces baseline picker + demo-restore cron — Log.md`) for the role-grant bug's exact shape and the blocked verification, and the **FM 037 log** (the read-only DB role that causes it). Skim `/home/al/fleetmanager/sessions/session-outlines.md` Active tracks.
5. Read `config/backup.php` and the `pgsql` connection in `config/database.php` (spatie/db-dumper takes per-connection `dump` options — `add_extra_option` / `addExtraOption`), plus `app/Console/Commands/` for the `demo:restore` command and `docs/app-reference.md` §§ backup/restore (the manual-restore procedure and the demo-node exception).
6. Read `docs/fleet-manager-agent-contract.md` § backup/blob + § Demo-node reset coordination — context for whether the fix warrants a documentation revision.
7. Note any drift between this prompt and the code in a brief work-log entry; proceed per the drift rules.

---

## Starting state inherited from session 364

- **CRM contract at v2.5.0** — unchanged; 364 was a test-audit cycle (test-code + docs only, non-boundary). 365 is also non-boundary (config + command surface, no contract change).
- **Schema baseline:** no migration since 359's `users` 2FA columns. 365 expects **no schema change**.
- **Fast Pest baseline: 2988 / 0** (parallel, verified at 364 close). The drop from 363's 2993 is deliberate audit reshaping: −13 false-confidence tests deleted, +8 real tests reclassified in from the slow group. Expected delta this session: a few new tests for the dump-option fix and any restore-path hardening.
- **Slow group reshaped at 364:** 8 tests moved to fast (three Fleet 429 throttle tests among them); the >5s boundary verified clean on both sides. The `design`-group pinned list verified intact.
- **Playwright suite:** untouched by 364; the three standing per-tour specs + the `@on-demand` storyboard regenerator. Not expected to run this session.
- **Test-suite state notes from 364:** the s363 intermittent is identified and fixed (`DashboardBuilderApiControllerTest`, ordered pick + delta assertion). `infection.json5` now points at the Fleet slice with `timeout: 120` (the durable per-slice config, per `docs/testing/mutation-audits.md`).
- **Deploy state:** `0.363.03` merged to main by the owner ahead of 364 (CI `Tests` green on the merge; the "failure" conclusions on main-branch runs are the non-gating Playwright job). Demo node last known at `0.362.01` awaiting external tour feedback — **feedback still interrupts** when the owner says so. VERSION bumps to `0.364.01` at the 364 close gate; if 365 needs a mid-session deploy for live restore verification, bump VERSION first per the standing rule.
- **Housekeeping:** 364 landed three `[test-integrity]` paper-trail entries in the inbox (D3 findings, D4 dispositions, boundary refresh) and re-scoped the localization item (footprint NOT small; parked pending an owner decision). Two fresh captures sit in `sessions/housekeeping-incoming.md`: the demo-dashboard seeding seam (recent_donations/recent_notes vs allowedSlots) and the four D4-exposed coverage-gap backfills. Neither is 365 scope.
- **Planning state:** guided-tour track still paused on external feedback; template-conversion tool still in promotion candidates; Code Shape & Fit ≈ 379; Code Review & Cleanup ≈ 398; Test Audit Cycle 3 ≈ 414. **A2's stale FM-side framing is this session's Phase 1 target** — reconcile, don't rebuild.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **FM repo access is read + sessions-dir-write only.** Ground truth about FM state comes from `/home/al/fleetmanager/sessions/`; the handoff doc (Phase 4) is written there per the established pattern (`data-hygiene-handoff-from-crm.md`, `demo-node-handoff-from-crm.md`). Do not modify FM application code — FM-side work belongs to FM sessions.
- **The owner drives all live-node operations.** Phase 3's verification runs against real droplets through the FM UI; the agent's role is to say what to exercise, watch the CRM side (endpoint behavior, restored state), and record outcomes. Nothing destructive happens without the owner at the wheel; the restore drill targets a node the owner designates, never a production customer node (none exist yet, but say so out loud before acting).
- **Old blobs stay broken by design.** The dump-side fix makes *future* backups portable; existing baselines (including the demo baseline from FM 042) need re-recording after the fix deploys. Phase 4's handoff must say this explicitly — it's the difference between "fixed" and "fixed after the next baseline push."
- **Verify objective outcomes yourself** where they're CRM-side (the dump no longer contains `GRANT`/`OWNER` statements — assert it from the actual dump output of a local `backup:run`); pull the owner in for the FM-UI and live-node halves.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) the **fix-shape lean** — dump-side `--no-privileges --no-owner` via the pgsql connection's `dump` config (clean, spatie-supported, fixes all future blobs) vs restore-side role stripping/pre-creation (also rescues old blobs) — with a recommendation; (2) whether the contract doc gets a **no-bump documentation revision** noting privilege-free blobs; (3) the **Phase 3 verification plan** — which of the four capabilities get live exercise this session, against which node(s), and what the owner needs ready (FM access, a scratch droplet or the demo node, ~how much of their time); (4) the **runbook handoff shape** — skeleton-with-checklists in `/home/al/fleetmanager/sessions/` for an FM session to finalize, vs a fuller draft. One confirmation, then proceed.
