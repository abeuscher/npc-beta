# Base Prompt — Session 224

We are about to begin a new session: **224. Widget Primitive — Phase 4h (Product Pair Retrofit + `WidgetDataResolver` Shell Retirement) — Phase 4 Close**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session writes no migrations and no schema changes — `products`, `product_prices`, and `purchases` are the surfaces involved; read `docs/schema/products.md`, `docs/schema/product_prices.md`, and `docs/schema/purchases.md` to confirm column shape, the `product_prices` HasMany shape, and the `purchases.status` enum used by `Product::isAtCapacity()` (the aggregate gate).
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise.
   - `sessions/widget-primitive-migration.md` — the migration sketch. Phase 4 retrofit is in its closing beat; this session lands the **last two genuine retrofit candidates** (the Product pair) and retires the entire legacy `WidgetDataResolver` shell. The new architectural beats this session introduces (the `'product'` arm in both `ContractResolver` and `SystemModelProjector`, the first nested DTO projection for `prices`, and a single resolver containing both list-shaped and single-row dispatch for the same model) propagate forward into every Phase 5 record-detail-sidebar widget.
4. Read the most recent Phase 4 session logs to understand the trail of decisions this session lands on top of:
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — established the `_label` projector-key naming convention and the `eager-load-skips-when-all-FKs-null` Test B caveat.
   - `sessions/archived/221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer) — Log.md` — the query-settings architecture and the `QuerySettings` value object on `App\WidgetPrimitive`. ProductCarousel may declare `querySettings` (it is list-shaped); ProductDisplay does not (single-row, no panel).
   - `sessions/archived/222. Widget Primitive — Phase 4f (BlogPager Retrofit + PageContext::posts Retirement) — Log.md` — established the `users where "id" in` batch-select assertion pattern for eager-loaded belongsTo relations, and the convention that `PageContext` helper methods retire in the same session as their last consumers.
   - `sessions/223. Widget Primitive — Phase 4g (Event Single-Row Pair: EventDescription + EventRegistration + PageContext::event Retirement) — Log.md` (or `sessions/archived/` if 224 close has moved it) — established the **single-row DTO shape** (`['item' => row | null]`), the `cardinality: 'one' | 'many'` field on `DataContract` with `'many'` default, the **aggregate-derived field via `withCount` baked into the resolver** (`is_at_capacity`), and the per-request slug-keyed cache key shape (`'event:one:' . $slug`). Also established the **template-side `$errors` caveat**: `Blade::render()` does not inject `$errors`, so any test rendering a widget template whose form branch references `$errors` must either bind a `ViewErrorBag` or pick a config that short-circuits before the form renders. ProductDisplay's at-capacity branch references `$errors->has('waitlist')`, so this caveat is directly relevant.
5. Read the full state of every file this session edits. Specifically:
   - `app/Widgets/ProductCarousel/ProductCarouselDefinition.php` — 56 lines. No `dataContract()` today. Add `dataContract()` returning `SOURCE_SYSTEM_MODEL`/`'product'`/`cardinality: CARDINALITY_MANY` with the field whitelist used by the template's render. Optionally add `querySettings()` if the panel is wanted (decision: scoped in the prompt — tentatively **no panel** for v1; the `limit` config field stays as the per-instance cap and propagates into the contract's filters at `dataContract()` time).
   - `app/Widgets/ProductCarousel/template.blade.php` — 110 lines. Line 2 calls `\App\Services\WidgetDataResolver::resolveProducts(...)` directly. Rewrite to read from `$widgetData['items']`. Each item carries the same shape it does today (`name`, `description`, `image_url`, `prices[]` with `id`/`label`/`amount`).
   - `app/Widgets/ProductDisplay/ProductDisplayDefinition.php` — 30 lines. No `dataContract()` today. Add `dataContract()` returning `SOURCE_SYSTEM_MODEL`/`'product'`/`cardinality: CARDINALITY_ONE` with a wider field whitelist (the template reads `name`, `description`, `id`, `is_at_capacity`, `prices[]`).
   - `app/Widgets/ProductDisplay/template.blade.php` — 99 lines. Walks `$pageContext->product(...)` then reads many model fields plus calls `$product->isAtCapacity()` (an aggregate that runs `COUNT(*)` on `purchases` filtered by `status='active'`) and iterates `$product->prices` (Eloquent collection). Rewrite to read from `$widgetData['item']`. The auth/session/request access (`session('waitlist_success')`, `request()->query('checkout')`, `$errors->has('waitlist')`, `$errors->has('checkout')`, `old(...)`) stays unchanged.
   - `app/WidgetPrimitive/Projectors/SystemModelProjector.php` — 130-ish lines after 223. Add a private `projectProduct(Product)` method emitting the field superset both Product widgets need: `id`, `name`, `slug`, `description`, `image_url`, `capacity`, `is_at_capacity`, `prices` (the **first nested DTO projection** — an array of `['id', 'label', 'amount']` sub-rows). Wire `projectProduct` into the `projectorFor()` dispatch added in 223.
   - `app/WidgetPrimitive/ContractResolver.php` — extended in 223. Add a new `'product'` arm to both cardinality dispatch branches: `resolveProduct()` (list-shaped — runs `Product::published-equivalent->where('is_archived', false)->withCount(['purchases as active_purchases_count' => ...])->with(['media', 'prices'])->orderBy('sort_order')->get()`) and `resolveProductOne()` (single-row by slug — same shape but `where('slug', $slug)->first()`, slug-keyed cache `'product:one:' . $slug`). Both arms eager-load `prices` for the nested DTO projection. Note: `Product` model has no `published()` scope today; the inline filter is `where('status', 'published')->where('is_archived', false)`.
   - `app/Services/WidgetDataResolver.php` — 110 lines. **Delete the entire file** at end of session, after retiring its last callers (ProductCarousel template + `PageContext::collection()`).
   - `app/Services/PageContext.php` — `collection(string $handle, ?int $limit)` (lines ~26–35) and the `array $collectionCache = []` property become unused after `WidgetDataResolver` retires (the only caller is internal). `product(?string $slug)` (lines ~38–49) and the `array $productCache = []` property become unused after ProductDisplay retrofit. Delete all four. `form(string $handle)` and `formCache` STAY — Forms widget has not retrofitted yet and has no scheduled session.
   - `tests/Feature/ProductCarouselTest.php` — 184 lines, 11 tests. Three tests exercise `WidgetDataResolver::resolveProducts(...)` directly (lines 26–80). One test (line 154) renders the template via `view('widgets::ProductCarousel.template', [...])` passing the legacy `$collectionData` shape. Both groups need either deletion or migration to the contract path. Two controller tests (`success_page` validation) are unrelated and stay. Two seeder tests (`product_carousel` config + total widget count) stay.
   - `tests/Feature/WidgetDataResolverTest.php` — 70 lines, 3 tests. **Delete the file entirely** when `WidgetDataResolver` retires.
   - `resources/docs/widget-types.md` — `collection(...)` row in Stream Methods table (line 109), `product(...)` row in Record Methods table (line 118), the `$pageContext->product(...)` usage example (line 124), the entire **System Collections** section (lines 137–148, since `WidgetDataResolver` is gone — `blog_posts`/`events`/`products` system handles no longer exist as a public surface). The `$pageContext->collection('blog_posts', 5)` example in the usage block (line 131) needs to update or remove.
   - `resources/docs/widget-development.md` — `app/Services/WidgetDataResolver.php` row in the directory table (line 27), the **Direct data access** bullets at lines 102–105 (Product display, Product carousel, Form widget — only Form survives), the `WidgetDataResolver` reference in **Collection item images** (line 269), and the source types table at lines 332–337 (the `custom`/`blog_posts`/`events`/`products` source-type catalog — which is now stale since `WidgetDataResolver::resolve()` is the only thing that consumed `Collection.source_type` for system handles).
6. Read the two contract retrofit test files most relevant to the product arm:
   - `tests/Feature/EventDescriptionContractRetrofitTest.php` (sibling pattern from 223) — the single-row DTO test shape, the empty-slug zero-query early-return assertion, and the slug-not-found `['item' => null]` assertion.
   - `tests/Feature/EventRegistrationContractRetrofitTest.php` (sibling pattern from 223) — the `withCount` subquery-shape pin (events SELECT carries the COUNT subquery; zero standalone child-table selects). Mirror this for products: the products SELECT carries `(select count(*) from "purchases" where "status" = 'active') as "active_purchases_count"` and zero standalone `purchases` selects.
   - `tests/Feature/EventsListingContractRetrofitTest.php` — the list-shaped Test B's three-select pin (events / media / pages-for-landingPage). Spot-check after adding the `'product'` arm to ContractResolver — it should remain byte-equivalent (different model arm, no shared state).
7. Read `tests/Feature/PageBuilderApiTest.php` — confirm whether `product_carousel` or `product_display` show up in any `apiWidget(..., 'product_*')` test. If so, those tests' `widget_type_collections` / `query_settings` assertions may need bumps mirroring the BlogListing/EventsListing pattern from 217 + 221.
8. Confirm `app/Widgets/ProductCarousel/script.js` and `app/Widgets/ProductDisplay/script.js` do not exist. ProductCarousel declares `assets()` with an `scss` file (`styles.scss`) plus `libs: ['swiper']` — no JS asset file. ProductDisplay declares no assets. **A `build:public` run is expected this session if any widget asset declaration changes** (e.g. SCSS additions). Default to running `build:public` once at end-of-session as a safety check.
9. Open `sessions/224. Widget Primitive — Phase 4h (Product Pair Retrofit + WidgetDataResolver Shell Retirement) — Phase 4 Close.md` and read the session prompt carefully.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216–223. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. **Not expected this session.**
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. **Likely required this session — ProductCarousel declares `app/Widgets/ProductCarousel/styles.scss` in `assets()`. If the SCSS file is unchanged, the build will be a no-op; either way, run it once as a safety check.**
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/225. base-prompt.md`. Expected next session: **Date Handling — Centralized Vocabulary + Schema Audit** (the post-Phase-4 stub already drafted in `session-outlines.md` lines ~272–301). Phase 1 lands a `DateFormat` value object and migrates the widget surface to it; Phase 2 (optional same-session) adds `published_at` to events and products with backfill + observer wiring. If Phase 2 turns out thorny, split it off — the format-vocabulary concern is the more time-sensitive one for Phase 5 sidebar widgets.
- **Next session prompt**: draft `sessions/225. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/224. Widget Primitive — Phase 4h (Product Pair Retrofit + WidgetDataResolver Shell Retirement) — Phase 4 Close — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: mark Phase 4h complete under the Widget Primitive track stub. **Mark Phase 4 of the Widget Primitive track as complete overall** — every `app/Widgets/*` retrofit candidate now declares a contract; `WidgetDataResolver` retired; `PageContext` retains only `currentPage`, `currentUser`, and `form()`. Confirm the Date Handling stub is the next scheduled session and that Phase 5 (record-detail slot + ambient context refactor) follows it.
- **Archive the previous session**: move all files matching `sessions/223. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-224/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles. (Captured in `feedback_no_collapse_ui` memory.)
