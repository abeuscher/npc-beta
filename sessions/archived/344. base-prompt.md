# Base Prompt — Session 344 (Code Review & Cleanup — Cycle 3 Audit)

**Start now.** Execute the reading list below, open the session prompt at `sessions/344. Code Review & Cleanup (Cycle 3 — Audit).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **344. Code Review & Cleanup (Cycle 3 — Audit).**

This session opens **Cycle 3 of the Code Review & Cleanup track** (`sessions/tracks/code-review-and-cleanup.md`) — the standing maintenance arc. It is the **audit session**, first of the compressed **3-session cycle (audit → apply → squash)**. Cycle 2 closed at session 274; the ~50-session cadence puts the trigger near 324 and we are at 344, ~70 sessions of accumulated drift — the cycle is due and lifted now by cadence. This is the recurring mid-cycle cleanup, **distinct from the terminal pre-Beta-1 review + squash (T1)**, which still runs later per `release-plan.md` Rule 10. It is **not yet a `release-plan.md` entry** — the track doc's Forward plan is canonical for the cycle shape; the three formal release-plan rows (audit/apply/squash) are added when the cycle is queued (a planning step for this session's close).

This session is **analysis-only — it produces a findings document, not code changes** — so it is **non-boundary by construction and ships no behaviour change; CRM contract stays at v2.3.0**, no `CONTRACT_VERSION` change. The one caveat: a cleanup audit can *surface* drift in the Fleet Manager contract surface (Cycle 2 consolidated three divergent contract-version sources here — the W8 #3 / Flag W4/A work). Surface any boundary-relevant finding into the Open Flags block for the apply session; do **not** touch boundary code in the audit itself.

**Session shape:** audit (track-phase). One session that folds Cycle 2's two audit halves into one — the **horizontal sweeps** (dead code/imports, naming + framework-convention drift, permission-gate uniformity, file-length quantitative, inline-code-in-markup) **and** the **subsystem deep walks** (CRM, importer, page-builder Vue, rich-text), plus **three Cycle-3 standing additions** (a static-reflection scan of resource method signatures vs the Filament v3 base; a boolean-settings / config-shape consistency sweep; an integration-seam sweep). Output: the merged duplication + framework-alignment tables, the Open Flags block, the file-length quantitative output, the inline-code candidate table, and the **carve-out decisions** (any workstream exceeding ~6 apply-iterations auto-flags a carve-out, decided at *this* session's close, not at apply-close).

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. **Read the track doc in full — it is canonical for this cycle:** `sessions/tracks/code-review-and-cleanup.md`. The **Forward plan — Cycle 3 shape** section defines the compressed audit shape, the cadence trigger, the standing improvements (convention-drift gate test, static-reflection scan, boolean-settings sweep, integration-seam sweep, the W11 auto-skip-with-note rule, the 6-iteration carve-out heuristic), and the permanent inter-cycle artifacts. The **Cycle 1 + Cycle 2 retrospectives** carry the workstream taxonomy (W1–W12) and the won't-fixes already reaffirmed (do not relitigate them).
3. Read `sessions/session-outlines.md` — the active-tracks one-liner for this track (~line 59) and the closed Cycle-2 detail block ("Code Review & Cleanup — 4-session housekeeping cycle", ~line 143) for the prior cycle's per-session shape and the W-numbering.
4. **Read the Cycle-2 audit logs for output shape** (mirror their structure — the W7/W8 tables, Open Flags block, quantitative output): `sessions/archived/271. … (Audit Part 1 — Foundations + Quantitative) — Log.md` and `sessions/archived/272. … (Audit Part 2 — Subsystem Deep Walks) — Log.md`. The apply + squash logs (`273. … (Apply) — Log.md`, `274. Migration Squash & Code Optimization — Log.md`) show what the audit's output must feed.
5. **Standing-agenda targets — read before the relevant pass:**
   - The inter-cycle gate to extend: `tests/Feature/Infrastructure/FmContractVersionParityTest.php`. The convention-drift Pest test is a Cycle-3 *apply* deliverable; the audit scopes what it must cover (Filament resource method names vs the v3 base class, `CustomFieldDef` `model_type` lowercase-short-name call sites, trait-constant access patterns).
   - Boolean-settings sweep precedents: the `'true'`/`'false'` `SiteSetting` convention (`horizon_enabled`, `notes_edit_only_by_creator`) vs native `bool` casts vs `filter_var(..., FILTER_VALIDATE_BOOLEAN)` — sweep every boolean-shaped tenant/setting read+write for shape drift.
   - Integration-seam pattern: the `feedback_audit_integration_seams.md` memory and the session-308 typography incident (two writers into `SiteSetting::typography`; the importer silently won) — sweep every `SiteSetting` row written from >1 call site, every observer on a model also written by an importer, every shared-default constant consumed by >1 writer.
6. Read `docs/app-reference.md` for the subsystem map + container names (the subsystem deep walks are organised by it).
7. **Drift note:** the track doc names a permanent artifact `sessions/code-review-large-files-partial-extraction.md` (the W11 large-file advisory) — it is **not present at that path** (verified missing at 343 close). For the file-length pass, regenerate the LOC grep counts fresh (the track doc already says to refresh them at audit time) and reconcile the dangling reference (recreate the artifact or correct the track doc) as part of this session.
8. Note any other drift between this prompt and the actual code in a brief work-log entry; proceed unless something needs a decision per the drift / decision-threshold rules.

---

## Starting state inherited from session 343

- **CRM contract at v2.3.0** — unchanged. 343 was a non-boundary housekeeping batch (the `media:prune-orphans` orphan-cas sweep, the EventMiniCalendar narrow-rail overflow fix, Random Data Generator → Organizations, plus one flaky-test fix). This session is also non-boundary (analysis-only).
- **Schema baseline:** no change since 328. 343 added none. **No migration expected** — the audit produces a findings doc; any schema-touching pick is deferred to the apply/squash sessions. (The squash session is where migration history collapses into the schema-dump baseline.)
- **Fast Pest suite green at the 343 close baseline (2799 passed, 0 failed).** 343 added 5 tests (3 `media:prune-orphans`, 2 Random Data Generator Organizations) and made `DashboardBuilderApiControllerTest`'s dashboard-grid widget selection deterministic (derive the allowed set from the registry, not a hardcoded list). **No new Pest expected this session** — analysis-only, no behaviour change.
- **Playwright:** baseline preserved; no specs added at 343; none expected this session.
- **Planning state:** Cycle 3 of Code Review & Cleanup is **lifted now by cadence** (343 is past the ~324 trigger). The track doc's Forward plan is canonical. The three formal release-plan rows for the cycle are not yet present — add them as part of queuing the cycle (planning edit at close, per the track-vs-release-plan split in the track doc).
- **Housekeeping inbox:** an audit session is not a housekeeping-batch drain — its output is the Open Flags / carve-out decisions in the audit log, not inbox folds (though an incidental small find may still be logged via `logbug`). The capture buffer (`housekeeping-incoming.md`) was empty at 343 close.

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md`). Session-specific notes:

- **The audit produces a findings document, not fixes.** Resist applying picks inline — the compressed 3-session shape keeps audit and apply separate so the apply session owns one clean `session-345/1` branch. The only things that may land in the audit session are read-only analysis artifacts (e.g. the static-reflection scan script) and the dangling-artifact reconciliation; everything actionable goes into the tables/flags for the apply session.
- **Don't relitigate reaffirmed won't-fixes.** The track doc lists the Cycle 1 + Cycle 2 won't-fixes (the three large PHP importer files, the Filament-idiomatic importer action closures, the chunked-tick → queued-job migration, the Contacts-importer namespaced-pattern divergence). Note them as standing; apply the W11 auto-skip-with-note rule for two-cycle-old outliers in no active work.
- **Carve-out decision at audit close, not apply close.** If a workstream's apply scope exceeds ~6 iterations, auto-flag a carve-out candidate now (the 207 / 275 precedent), so the apply session isn't relitigating its own scope at the end.
- **Boundary findings are flagged, not fixed.** Anything touching the Fleet Manager contract surface goes into the Open Flags block with the cross-repo note; the audit does not edit boundary code or the contract version.
- **Surface, don't fix, integration seams.** The seam sweep names the seams (multiple writers, disagreeing defaults); the fix (a flag + a pre-flight check) is an apply pick. Won't-fix is fine where overlap is intentional and conflict-free.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation the cycle is lifted by cadence and the compressed 3-session shape (audit → apply → squash) is the plan; (2) the proposed workstream ordering for the single audit session (horizontal sweeps → subsystem deep walks → Cycle-3 standing additions), and whether the boolean-settings + integration-seam sweeps are in this audit or deferred; (3) any early carve-out you can already foresee given the ~70-session growth window. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 + Phase 2. Session-specific details:

- **Log file:** `sessions/344. Code Review & Cleanup (Cycle 3 — Audit) — Log.md` — carry the merged duplication + framework-alignment tables, the Open Flags block, the file-length quantitative output, the inline-code candidate table, and the carve-out decisions. **This log IS the audit's deliverable** — the apply session reads it.
- **Bump `VERSION`** to `0.344.<iteration>` (audit sessions still bump — VERSION marks the close, not the surface touched).
- **Update** `sessions/tracks/code-review-and-cleanup.md` status snapshot (Cycle 3 in progress: audit done) and the `sessions/session-outlines.md` active-tracks line. Add the three Cycle-3 release-plan rows if not already added.
- **Draft the apply session (345)** prompt + base prompt from the audit's findings.
- **Archive the previous session's files** — move `sessions/343. *.md` → `sessions/archived/`.
- **Commit on `session-344/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: the win is **a complete, honest findings document the apply session can execute against without re-discovery** — the merged tables, the flags, the quantitative output, and clean carve-out decisions. Resist fixing as you find (the apply session owns that); resist relitigating settled won't-fixes; name integration seams and boundary drift without touching them.
