# Base Prompt — Session 303 (Theme + Media Portability — Self-Contained Bundles, ID-Preserving Media Seed, Theme Export/Import)

**Start now.** Execute the reading list below, open the session prompt at `sessions/303. Theme + Media Portability — Self-Contained Bundles, ID-Preserving Media Seed, Theme Export-Import.md`, then begin the work. No confirmation needed.

---

We are about to begin: **303. Theme + Media Portability — Self-Contained Content Bundles + ID-Preserving Media Seed + Theme Export/Import**.

This session is **not** a numbered `release-plan.md` execution-order position — it is a **user-elected lift of the "Theme + Media Portability" stub** in `sessions/session-outlines.md`, slotted ahead of the default next item (E8, UI/UX Sprint, execution-order position 38). It was sequenced **post-arc on purpose**: the Theme & Template Re-Taxonomy arc (`release-plan.md` § E6) fully retired at session 302 close, so a "theme" is now a *complete* thing — colours + typography + buttons + the session-301 scheme/chrome model — and a theme can export whole, not partial. The Public Website Complete milestone continues; this is pre-Public-Website-Complete, pre-Beta 1.

This session is **NOT boundary-touching** — no Fleet Manager surface. **CRM contract stays at v2.3.0.** The work is Filament actions + import/export services + a queued job + a gated download route only. If anything appears to need a Fleet / `/api/health` / response-schema touch, stop and surface — scope drifted.

**Session shape:** feature-style, two-phase with an explicit Rule-11 cut-line. It makes the *working → demo* round-trip whole: (1) page/template content bundles that carry their media **bytes** (cross-system portable zip), plus the theme/design export-import folded into the same envelope primitive; (2) a standalone ID-preserving media seed + Media Library export/import UI. Phase 1 is a coherent shippable milestone on its own; Phase 2 is a documented continuation if the session overflows.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — stable since 276.
2. Read `sessions/session-outlines.md` — the Active-tracks block and **the "Theme + Media Portability" stub** (the combined-session framing, the (A)/(B) split, the Rule-11 note, the "not to be confused with" disambiguations). Note the arc stub immediately above it is now **complete** (302 retired § E6) — that completion is this session's sequencing prerequisite, not active scope.
3. **`sessions/media-portability-prompt-draft.md` is the canonical, binding design for the media-portability (B) half** — decisions #1–#8 and Phases 1–2. Read it in full first. The executing agent makes **no** (B) architectural choices; all (B) forks are resolved there. The 303 session prompt is the *combined-session* delta: how the theme/design export-import (A) folds onto that primitive, the interleave, and the Rule-11 cut-line.
4. This session does **not** belong to an active track. The stub + the media-portability draft + the 303 session prompt are jointly canonical for scope.
5. Read the actual surfaces before touching them (the session prompt grounds each to its file):
   - `app/Services/ImportExport/ContentExporter.php` — `FORMAT_VERSION = '1.1.0'`; `envelope(array $payload)`; `exportPages` / `exportTemplates` / `exportBundle`; `serializeTemplate` (already carries the 301 `scheme`/`no_header`/`no_footer` columns — confirm, do not rebuild). The (A) half adds an additive `exportDesign()` → `payload.design`; the (B) half adds `exportMedia()` / `exportAllMedia()` → `payload.media`.
   - `app/Services/ImportExport/ContentImporter.php` — `import(array $bundle, ImportLog $log)`, `validateEnvelope()` (major-version match — **untouched** per (B) decision #1). New `payload.design` pass (A) and `payload.media` pass (B) both run **before** pages/templates.
   - `app/Filament/Actions/ImportBundleAction.php` + the six export call sites named in the media-portability draft ("The established pattern this extends") — each keeps its existing JSON action and gains a sibling zip action.
   - `app/Filament/Pages/MediaLibraryPage.php` — read-only today; Phase 2 adds gated export/import actions.
   - `app/Filament/Pages/DesignSystemPage.php` — the Theme page; `manage_cms_settings` gate; already calls `app(AssetBuildService::class)->build()` on save (`saveColors` / `save`). The (A) theme export/import action pair home.
   - `app/Services/ColorTokenResolver.php` (`theme_colors` SiteSetting, `defaults()`), `app/Services/TypographyResolver.php` (`typography` SiteSetting), and the `button_styles` design SiteSetting row — the three design-group JSON rows (A) exports/imports with **deep-merge over defaults, never sweep** (the 295 lesson / concrete-values rule).
   - `docker-compose.e2e.yml` — the isolated e2e stack now has the `app_vendor_e2e` named volume (landed 302). Note the lifecycle caveat below before running `npm run test:e2e:isolated`.
6. Read `docs/app-reference.md` (container/env names, view-to-file mappings) and `docs/schema/README.md` + the `media`, `site_settings`, `templates`, `pages` table files under `docs/schema/`.
7. Note any drift between the session prompt / the media-portability draft and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules. The (B) draft was written against the code and is precise; expect only minor surface drift.

---

## Starting state inherited from session 302

*(Session 302 — Naming & IA Polish — Re-Taxonomy Tail — closed immediately before this session. Re-confirm against 302's actual log at start; adapt silently to drift.)*

- **CRM contract at v2.3.0** — unchanged; 302 was a non-boundary IA relabel session (admin label/regroup + one settings-section move + doc consolidation). 303 is also non-boundary.
- **Schema baseline:** no schema changes since the session-301 migration `2026_05_18_120000_add_scheme_and_chrome_suppression_to_templates.php` (302 added none). 303 adds **no new tables/columns** — the posture-B media seed does raw explicit-id `media`-row inserts + a `media_id_seq` reset (data movement, not schema); the theme/design export-import touches only existing `design`-group `SiteSetting` rows + the existing 301 `templates` columns.
- **Fast suite green at 302 close: 2476 / 0** (2474 at 301 close + 2 net-new `StripeBrandingFinanceRelocationSession302Test`). Parallel (`./dev test`). `DesignGroupIntegrityTest` pinned-list count **19**, unchanged (the 302 relocation test was deliberately *not* in the `design` group). New tests expected this session — see the session prompt's Testing block.
- **Playwright** — isolated stack via `npm run test:e2e:isolated`; baseline includes `tests/e2e/template-scheme.spec.ts` (5, session 301) + `tests/e2e/widget-color-tokens.spec.ts` (session 300) + earlier specs. `memory/feedback_test_runs_not_parallel` applies — **Pest and Playwright never run together**; `docker compose restart nginx` recovers stale upstreams. Whether Playwright runs this session: the success criteria here are objective (zip round-trips, id/uuid/path preservation, rows-exist) — verify via Pest + `Queue::fake()` per the draft; a browser pass is not the primary signal.
- **e2e stack note (carry, do not re-fix):** 302 landed the `app_vendor_e2e` named volume in `docker-compose.e2e.yml` (mirrors `app_node_modules_e2e`; protects the image-baked `vendor/` from the repo bind-mount so the CI Playwright job resolves `vendor/autoload.php`). Lifecycle caveat: the named volume persists — after a local PHP-dependency change, `npm run e2e:down` (uses `down -v`) then `e2e:up` to re-seed `vendor/`. The companion Postgres-host CI fix is already on `main` (`bca2914`). None of this is 303's concern unless it blocks the suite.
- **Planning state:** § E6 / the Theme & Template Re-Taxonomy arc **fully retired** at 302 close (all four arc points resolved; `session-outlines.md` arc stub marked complete). D2 (Compatibility cluster, post-milestone position 58) is now Rule-8-unblocked. This session is a **user-elected lift of the "Theme + Media Portability" stub ahead of E8** (the default next execution-order item, position 38) — not a re-sequencing of the plan; E8 and the rest of the order are unchanged behind it.
- **Housekeeping inbox** — walk `sessions/housekeeping-inbox.md` at start/close per Rule 2. 303 is a **feature session, not a batch** — it does **not** absorb the inbox. It *adds* two paper-trail entries on close (per the media-portability draft's closing steps): a UUID-path-generator out-of-scope stub to `session-outlines.md`, and an export-artifact TTL-sweeper note to `housekeeping-inbox.md`. Only fold an existing inbox item if it is literally in scope (none is).

---

## Process rules for every session

Base-prompt process rules (`sessions/template-base-prompt.md`) apply in full. Session-specific:

- **Existing infrastructure first.** Queues/Horizon/Redis, Spatie Media Library, `ContentExporter`/`ContentImporter`, `ImportBundleAction`, `AssetBuildService`, persistent database notifications — all already exist and are the load-bearing substrate. Build **on** them; do not re-implement. The media-portability draft names every seam.
- **The (B) design is canonical and binding — do not re-derive it.** `sessions/media-portability-prompt-draft.md` decisions #1–#8 resolve every (B) fork (container-not-format-bump, archive-first resolution, posture-B id preservation + collision/orphan policy, conversions regenerated, queued both directions, standalone media envelope, zip-slip/zip-bomb security, size ceiling). If the code contradicts a decision, that is drift to surface — not a licence to redesign.
- **The (A) theme/design export-import is the few-hours rider.** It rides Phase 1's envelope/zip primitive: a new additive `payload.design` (the three design-group `SiteSetting` rows), import **deep-merges over resolver defaults and never sweeps stored config** (the session-295 em-rhythm-revert lesson; `memory/feedback_concrete_values`), then triggers the existing `AssetBuildService::build()` rebuild. The session-301 per-template `scheme`/`no_header`/`no_footer` carry-through already ships inside `ContentExporter`/`ContentImporter` — **confirm it, do not rebuild it** (that was 301's mandatory correctness obligation, not this session's feature).
- **Scope fences — do not cross them:**
  - **No UUID-path-generator migration.** Posture B is delivered via explicit-id preservation only (draft decision #3 + Out-of-scope). The cleaner UUID-path shape is a one-line stub written on close, not built.
  - **No format-version bump.** `ContentExporter::FORMAT_VERSION` stays `1.1.0`; `validateEnvelope()` untouched. Zip vs JSON is detected by file type, not a version field (draft decision #1).
  - **No schema change.** If a step seems to need a migration, stop and surface — it has drifted out of scope.
  - **No Fleet / `/api/health` / response-schema change** — non-boundary, v2.3.0.
  - **Not a backup mechanism.** Full-system backup already exists (`spatie/laravel-backup`, FM `/api/backup/blob`). This is page-scoped, operator-driven content/theme portability — keep that framing.
- **Verify objectively (this is objective work).** Success is "the zip round-trips," "the right `media` rows/files/ids exist," "the theme deep-merges without sweeping," "the gate rejects the wrong role," "the job dispatched and the notification carries the right payload." Close those loops yourself in Pest + `Queue::fake()` per the draft's Testing block. Pull the user in only for a genuine judgment call (e.g. the 512 MB ceiling / artifact-location disk-budget confirmation, and the notification-vs-email UX — both pre-listed as session-start open questions). No manufactured judgment gates (`memory/feedback_dont_gate_on_taste_defaults`).

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close.** Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

The "Theme + Media Portability" stub is **resolved** at 303 close (both halves shipped) — or, if a Rule-11 split is taken at the documented cut-line, the stub carries forward **only** the Phase-2 continuation and the next session is its completion. Either way, the session after this returns to the `release-plan.md` execution order — **E8 (UI/UX Sprint, position 38) is the default next item** unless the user elects another lift. Draft the next session's documents per the base-prompt template **only when the user names it** — do not auto-pipeline, and do not assume; reflect the actual state (full-resolve vs. Phase-2-continuation).

### Phase 2 — Close

When the user says to close: session log per `template-session-log.md`; update `session-outlines.md` (**mark the "Theme + Media Portability" stub resolved**, or rewrite it to the Phase-2-only continuation if split); add the **UUID-path-generator out-of-scope stub** to `session-outlines.md` and the **export-artifact TTL-sweeper note** to `housekeeping-inbox.md` (per the draft's closing steps); add a **help-doc stub** for the new operator-facing export/import surface per the style rules; append to `completed-sessions.md`; archive the immediately-preceding session's files into `sessions/archived/` (`sessions/302. *`); commit on the final `session-303/N` branch; notify; do not push; do not begin the next session until the user starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Self-contained content+media zip bundle (BundleArchive + archive-first import) + theme/design export-import (payload.design, deep-merge, build trigger) + VERSION 0.303.1".
- **PM-level tone for status reports** per `memory/feedback_pm_level_tone.md`. The user is the project owner — plain language, no framework-primitive jargon, short.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Build on the existing pattern; do not reinvent.** New `BundleArchive` service + additive `payload.design` / `payload.media` passes + sibling Filament actions — no new abstraction beyond what the draft names. Every new admin-facing surface gets a help-doc stub and (for any new page) `getBreadcrumbs()`.
- This introduces an operator-facing export/import surface — a help-doc stub on close is **required**, not optional.
