# Session 125 — Build Server Settings & CI Pipeline — Log

**Date:** 2026-04-04
**Branch:** session-125/1 … /2

---

## What was built

### Phase 1 — Build server settings in CMS Settings page

- Added "Build Server" section to `CmsSettingsPage` between "General" and "Header & Footer Code Snippets"
- Build Server URL — text input, nullable, with placeholder
- Build Server API Key — password input (masked), stored encrypted in `site_settings` with type `encrypted`
- Blank API key field preserves existing stored key on save
- "Test Connection" button pings `{url}/health` with Bearer auth and shows success/failure notification
- Updated `AssetBuildService` to read URL and API key from `SiteSetting::get()` first, falling back to `config('services.build_server.*')` env vars

### Phase 2 — Dashboard integration health check

- Added `getBuildServerStatus()` to `DashboardIntegrationStatusWidget` with tri-state return: `connected` (green), `unreachable` (amber), `not_configured` (grey)
- Updated widget blade template to render the Build Server row with colour-coded status

### Phase 3 — CI build pipeline

- Updated `.github/workflows/deploy.yml`: added PHP setup, composer install, and `build:public` step (conditional on `BUILD_SERVER_URL` var, `continue-on-error`). Added post-deploy `build:public` on the droplet as fallback.
- Restructured `Dockerfile`: moved full Vite build (public + Filament admin theme) into the `node-builder` stage with PHP/Composer for Tailwind content scanning. Removed Node.js from the production `app` stage. Vite manifest and assets are copied from `node-builder`; `public/build/widgets/` is left intact for CI-built widget bundles.
- Fixed two build issues: added `--ignore-platform-reqs` for composer in node-builder (extensions not needed for Tailwind scanning), and added missing `tailwind.config.filament.js` and `app/Filament/` copies.

### Session 126 prompt

- Wrote session prompt and base prompt for "126. Logo Garden Widget & Demo Collection Seeding"

---

## Security checklist — verified

- [x] Build server API key is stored in `site_settings` with type `encrypted` (not exposed in client-side code)
- [x] API key field uses password input (masked in the admin UI)
- [x] Build server API key is not logged or included in error output
- [x] All existing tests pass

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`. Build server URL and API key should be configured in CMS Settings after deploy. The `BUILD_SERVER_URL` GitHub Actions variable and `BUILD_SERVER_API_KEY` secret should be set for CI widget builds.

---

## Deferred decisions

- Template custom SCSS migration to build server (deferred post-beta)
- Vite build server replacement for admin panel (out of scope)

---

## Test result

609 passed, 0 failed.
