# 219. Widget Primitive — Phase 4d (BarChart Retrofit)

Retrofit BarChart onto a `SOURCE_WIDGET_CONTENT_TYPE` data contract: add `dataContract()` (per-instance field whitelist driven by user-configured `x_field` and `y_field`), rewire the template to read `$widgetData['items']`, drop the legacy `collections()` slot declaration, swap the two sibling slot-strip tests off BarChart, and land the Phase 4 test-lock pair. First true `SOURCE_WIDGET_CONTENT_TYPE` retrofit (Carousel was an audit), and the first widget where the contract's `fields` list is dynamic rather than fixed.

---

## Design decisions (resolved before starting)

- **Contract shape: per-instance, dynamic fields.** Unlike Carousel's static `['title', 'description']`, BarChart's contract `fields` list is built from `$config['x_field']` and `$config['y_field']` at `dataContract()` call time. The contract whitelists exactly the two Collection fields the admin mapped — no more, no less. When either field is empty (unconfigured widget), the corresponding entry is omitted from the contract's `fields` array, and the projector returns rows with whatever subset is set. The widget's existing `requiredConfig` already gates rendering on both fields being populated, so the empty-fields case is the unmounted-state path.
- **Inline ContentType.** Same pattern as Carousel: declare an inline `ContentType` in `dataContract()` with two `text`-typed fields keyed by `$xField` and `$yField`. No image fields (BarChart has no media), so per `WidgetContentTypeProjector::projectRow()`'s `if ($imageKeys !== [])` guard, projected rows will have **no `_media` key at all** — keys will be exactly `[$xField, $yField]`. The template never reads `_media`. The ContentType handle is `'bar_chart.data'`.
- **Source: `SOURCE_WIDGET_CONTENT_TYPE`.** `resourceHandle` = `$config['collection_handle']`. Same source as Carousel; the resolver already handles this case (`ContractResolver::resolveWidgetContentType()`).
- **Template change.** Line 9 `$collectionData['data'] ?? []` becomes `$widgetData['items'] ?? []`. The downstream `foreach ($items as $item) { $item[$xField], $item[$yField] }` access pattern is unchanged — the projected DTO row still has those keys (because we declared them on the contract). No other template edits.
- **`collections()` removal.** Per the system rule established in 218: contract-locked widgets do not declare slots. `BarChartDefinition::collections()` is removed. Admin preview shows the unmounted state for empty/unconfigured BarCharts (correct UX). Demo data flows through `DemoSeeder` for dev/seeded paths.
- **Slot-strip tests are retired, not swapped.** Two tests in `tests/Feature/PageBuilderApiTest.php` exercise `PageBuilderApiController::filterQueryConfig()` (the legacy `widget_type->collections` slot-strip mechanism). 218 swapped them from Carousel to BarChart; the pattern would continue (BarChart → BoardMembers → LogoGarden → …) until every collection-using widget is retrofit-locked, at which point the strip mechanism is dead code. We stop the whack-a-mole here:
  - **Test 1** ("strips query_config slots that are not declared on the widget type") — **delete**. Directly tests the doomed slot-strip mechanism; no value carrying it forward.
  - **Test 2** ("update endpoint contract that the JS selective merge depends on") — **rewrite to not depend on a slot-declaring widget**. The test's purpose is to lock the JS selective-merge response shape (label, config, appearance_config, query_config echoed exactly; preview_html omitted). The slot-declaration is incidental setup. Use a contract-locked widget (e.g. `'carousel'`) and assert the same selective-merge contract; `query_config` is exercised as an empty payload or a payload whose keys all strip (the exact strip behavior is no longer load-bearing for this test).
- **Sibling-test fallout: `AppearanceConfigRenameTest::it bar_chart widget renders bar_fill_color into the chart config`.** This Blade-render test passes `collectionData['data']` directly. After the template rewires to `$widgetData['items']`, that input path no longer feeds the items loop and the empty-items guard fires (no `widget-bar-chart` class rendered). Update the test to pass `widgetData => ['items' => [...]]` instead of `collectionData => ['data' => [...]]`. Same key-rename pattern that the prior retrofit sessions used for their template-rewire fallout.
- **Test-lock fields.** Test A asserts the projector emits exactly two declared keys (`$xField` and `$yField`) — **no `_media` key**, since BarChart's content type declares no image fields and `WidgetContentTypeProjector::projectRow()` only emits `_media` when `imageFieldKeys() !== []`. Sentinels in undeclared Collection-item `data` keys do not leak. Sentinels go in keys that have no derived whitelisted counterpart — for BarChart, this is anything other than the user-mapped X/Y fields. Use sentinel keys `internal_notes` and `legacy_id` (same pattern as Carousel). Test B asserts the contract path emits exactly one collections, one collection_items, and one media select — `WidgetDataResolver` loop no longer fires for BarChart after `collections()` removal.
- **Public-surface gate.** `ContractResolver::resolveWidgetContentType()` filters by `is_public = true` when `$context->publicSurface` is true (canvas-slot default). Test fixture's Collection must be `is_public => true, is_active => true`.
- **Demo seeder unchanged.** `BarChart/DemoSeeder.php` already creates a real `chart-demo` Collection — no fallback path involved. The dev demo route renders against live data via the contract. No seeder edits.
- **No new schema.** No migration, no `docs/schema/` updates.
- **Front-end builds.** `script.js` and `assets()` declarations are untouched. Default expectation: no `npm run build`, no `build:public`. Run only if cleanup unexpectedly touches widget asset files.
- **Out of scope: changing how `x_field` / `y_field` are validated or persisted.** They remain free-text-from-Collection-fields-list as today.

---

## Phase 1 — Audit

1. Re-read `app/Widgets/BarChart/BarChartDefinition.php`. Confirm: no `dataContract()`, declares `collections(): ['data']`, `requiredConfig` keys `['collection_handle', 'x_field', 'y_field']`.
2. Re-read `app/Widgets/BarChart/template.blade.php`. Confirm: line 9 reads `$collectionData['data'] ?? []`. Confirm zero direct Eloquent access. Note that `$item[$xField]` and `$item[$yField]` are the only data-row reads — these are what the projector needs to emit.
3. Re-read `app/Widgets/BarChart/DemoSeeder.php`. Confirm it creates a real Collection (`chart-demo`) with two text-typed fields (`label`, `value`) and ten data points. Independent of any slot/fallback path.
4. Trace one BarChart render through `WidgetRenderer::render()` AS IT IS TODAY:
   - Line 46 `foreach ($widgetType->collections ?? [])` iterates once (slot `'data'`).
   - Line 49 `WidgetDataResolver::resolve($collHandle, $queryConfig)` returns the rows from the `chart-demo` Collection (or whatever the admin pointed at).
   - Line 50 stores `$collectionData['data'] = $resolved`.
   - Lines 71–87 the contract block: `dataContract($config)` returns `null` today (no method), so `$widgetData = null` and `$contract = null`.
   - Line 117 Blade renders the template. `collectionData['data']` populates `$items`. Template renders.
5. Grep `App\\Models\\|::find\(|::where\(|->data\(|getFirstMedia` inside `app/Widgets/BarChart/` — expect zero matches except in `DemoSeeder.php` (allowed).
6. Grep `BarChart|use App\\Widgets\\BarChart` across the whole repo — log every callsite to confirm nothing outside the widget folder reaches into BarChart-specific helpers.
7. Record findings in the session log under "Audit results."

## Phase 2 — Contract + projector

`WidgetContentTypeProjector` already handles SWCT projection generically — no projector edits needed. The projection map is the contract's `fields` list (which the widget controls per-instance).

Add `dataContract(array $config): ?DataContract` to `BarChartDefinition.php`:

```php
public function dataContract(array $config): ?DataContract
{
    $xField = (string) ($config['x_field'] ?? '');
    $yField = (string) ($config['y_field'] ?? '');
    $fields = array_values(array_filter([$xField, $yField], fn ($f) => $f !== ''));

    $contentTypeFields = array_map(
        fn ($f) => ['key' => $f, 'type' => 'text'],
        $fields,
    );

    $contentType = new ContentType(
        handle: 'bar_chart.data',
        fields: $contentTypeFields,
    );

    return new DataContract(
        version: '1.0.0',
        source: DataContract::SOURCE_WIDGET_CONTENT_TYPE,
        fields: $fields,
        resourceHandle: $config['collection_handle'] ?? null,
        contentType: $contentType,
    );
}
```

Add the `use App\WidgetPrimitive\ContentType;` and `use App\WidgetPrimitive\DataContract;` imports.

## Phase 3 — Template rewire

`app/Widgets/BarChart/template.blade.php` line 9: replace
```php
$items = $collectionData['data'] ?? [];
```
with
```php
$items = $widgetData['items'] ?? [];
```

No other template edits. Confirm via end-to-end render that an empty `$items` still hits the unmounted-state guard at line 26.

## Phase 4 — Cleanup

Remove `BarChartDefinition::collections()` (the `return ['data'];` method). Per the 218 system rule.

## Phase 5 — Sibling-test cleanup

Two flavors of fallout:

### 5a — `tests/Feature/PageBuilderApiTest.php` (slot-strip tests are retired)

- Delete the test "strips query_config slots that are not declared on the widget type" (around line 245). It directly tests the slot-strip mechanism, which is being retired across the board as Phase 4 progresses; carrying it forward via successive widget swaps is whack-a-mole on a feature we are systematically removing.
- Rewrite the test "update endpoint contract that the JS selective merge depends on" (around line 262) to not depend on a slot-declaring widget. Switch to a contract-locked widget (e.g. `'carousel'`) and lock the JS selective-merge contract (label, config, appearance_config, query_config echoed exactly; preview_html omitted; server-authoritative metadata fields present). The exercise of `query_config` no longer needs to use a "real" slot key — the assertion is that whatever the client sent in `query_config` is what the response carries (after the strip-pass, which for a contract-locked widget reduces to `[]`).

### 5b — `tests/Feature/AppearanceConfigRenameTest.php`

- The `it bar_chart widget renders bar_fill_color into the chart config` Blade-render test (around line 143) passes `collectionData => ['data' => [...]]` to feed the template. After the template rewires to `$widgetData['items']`, that path is dead — the items array is empty and the unmounted-state guard returns early before the `widget-bar-chart` class renders. Update the test to pass `widgetData => ['items' => [...]]` with the same row shape the projector would emit (keys `label` and `value`, no `_media`).

Verify both files in isolation before re-running the full suite.

## Phase 6 — Test-lock

Create `tests/Feature/BarChartContractRetrofitTest.php` mirroring the Carousel/EventsListing/BlogListing shape. Two tests:

### Test A — Fail-closed field whitelist + key-level invariant

Setup:
- Create a `Collection` row with `is_public: true, is_active: true, source_type: 'custom'`, handle `'audit-bars'`.
- Declare `fields`: `[['key' => 'label', 'type' => 'text'], ['key' => 'value', 'type' => 'text'], ['key' => 'internal_notes', 'type' => 'text'], ['key' => 'legacy_id', 'type' => 'text']]`.
- Create three `CollectionItem` rows with `data` containing all four keys: `label` ('A', 'B', 'C'), `value` ('10', '20', '30'), `internal_notes` ('NOTES_NOTLEAKED_SENTINEL'), `legacy_id` ('LEGACY_ID_SENTINEL').
- Mount BarChart widget with config `['collection_handle' => 'audit-bars', 'x_field' => 'label', 'y_field' => 'value', ...]`.

Assertions:
- Direct `ContractResolver::resolve()` call: `$dto['items']` has 3 rows, each with keys canonically `['label', 'value']` — exactly the two contract-declared fields, **no `_media` key** (BarChart's content type declares no image fields, so the projector's `_media` block is skipped). No `internal_notes`, no `legacy_id`.
- Rendered HTML contains the labels ('A', 'B', 'C') and the encoded values (10, 20, 30) inside the chart-config JSON payload.
- Rendered HTML does NOT contain `'NOTES_NOTLEAKED_SENTINEL'` or `'LEGACY_ID_SENTINEL'`.

### Test B — Resolver is the only path, with one collections + one collection_items + one media select

Setup:
- Same fixture as Test A but with 3 published items + 1 unpublished item.
- Mount widget; render through `WidgetRenderer::render()`.
- `DB::enableQueryLog()`.

Assertions:
- HTML's chart-config JSON has exactly 3 entries in `labels` and 3 in `values`.
- HTML does not contain the unpublished item's label.
- Exactly one `select` against `"collections"` carrying `"handle"`, `"is_active"`, `"is_public"` filters.
- Exactly one `select` against `"collection_items"` carrying `"is_published"` and `"sort_order"`.
- Exactly one `select` against `"media"`.

Both tests go in the fast suite (under 5s each, mirror Carousel's timing).

## Phase 7 — Verification

- **Fast Pest suite:** `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Two new tests in `BarChartContractRetrofitTest`. Regression-critical sibling tests to watch: the rewritten `PageBuilderApiTest::it update endpoint contract that the JS selective merge depends on`, the updated `AppearanceConfigRenameTest::it bar_chart widget renders bar_fill_color into the chart config`, `AppearanceLayerRegressionTest` (covers BarChart), and `WidgetTypesSession098Test` (asserts BarChart renders a canvas with collection data — verify it still does after the template rewire).
- **Front-end builds:** none expected. `script.js` untouched. Run `build:public` only if `script.js` ends up edited.
- **Playwright:** no new spec, no re-run.
- **Grep audit at end:** zero direct Eloquent in `app/Widgets/BarChart/` outside `DemoSeeder.php`. Zero `$collectionData[` references in `app/Widgets/BarChart/template.blade.php`.
- **Manual smoke:** load a page hosting a BarChart widget pointed at the `chart-demo` Collection (or a real custom Collection). Confirm the chart renders with bars labeled and sized correctly. Also load the admin page builder and preview a BarChart against demo data — confirm: configured BarChart shows the rendered chart, unconfigured BarChart shows the unmounted state. Run the thumbnail script for `bar_chart` to confirm the dev demo route still produces a populated chart.

---

## Testing

- **Slow test groups to run this session:** none.
- **New tests expected:** yes — `tests/Feature/BarChartContractRetrofitTest.php` with two tests (A: fail-closed whitelist + key-level invariant via direct resolver call + HTML sentinel assertion; B: resolver-is-only-path + query-count pin).
- **Test deletions / rewrites expected:** delete `PageBuilderApiTest::it strips query_config slots that are not declared on the widget type` (slot-strip mechanism is being retired). Rewrite `PageBuilderApiTest::it update endpoint contract that the JS selective merge depends on` to use a contract-locked widget instead of a slot-declaring one. Update `AppearanceConfigRenameTest::it bar_chart widget renders bar_fill_color into the chart config` to feed `widgetData` instead of `collectionData`.

---

## Security checklist

Before closing:

- [ ] BarChart's contract declares exactly `[$xField, $yField]` (filtered for non-empty); the projector's row shape is a strict subset of this. Asserted by Test A's resolver-level key assertion.
- [ ] BarChart's render path performs zero direct-Collection / direct-CollectionItem / direct-Media queries outside `ContractResolver::resolveWidgetContentType()`. Asserted by Test B.
- [ ] Resolver eager-loads `media` on the `CollectionItem` query — no N+1 per row. Asserted by Test B's exactly-one-media-select.
- [ ] `BarChart/template.blade.php` does not access Eloquent models directly. Verified by grep at end of session.
- [ ] Public-surface gating preserved — canvas-slot path filters collections by `is_public = true`. Asserted indirectly by Test B's collections-select query shape.
- [ ] Portal `contact_id` scoping preserved — no portal code touched. Verified by git diff.
- [ ] Fast Pest suite passes.
- [ ] No front-end build required unless cleanup unexpectedly touched widget assets.

---

## Out of scope

- **Adding new fields to BarChart's content type or rethinking how X/Y fields are picked.** The current free-text-from-Collection-fields shape is the contract; widening it is a feature session.
- **Per-field demo-data hints.** The "widget-owns-demo-data" track discussed during 218 is a separate session; BarChart's `DemoSeeder` continues seeding a real Collection as today.
- **Migrating BoardMembers or LogoGarden in this session.** Each is its own Phase 4 session — they share BarChart's SWCT shape but have static field lists.
- **Changing the legacy `WidgetDataResolver` loop in `WidgetRenderer::render()`** — the cleanup is widget-local, same posture as 216–218. The legacy loop's eventual removal is a separate session, conditional on every collection-using widget being retrofit-locked first.
- **Memos composer / write-side dogfooding.** Different track.
- **Phase 6 page-builder convergence.** Premature with only four widgets retrofit-locked.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file**: `sessions/219. Widget Primitive — Phase 4d (BarChart Retrofit) — Log.md`
- **Branch**: `session-219/N` (final iteration)
