# Base Prompt — Session 348 (Code Review & Cleanup — Cycle 3 — Migration Squash)

**Start now.** Execute the reading list below, open the session prompt at `sessions/348. Code Review & Cleanup (Cycle 3 — Migration Squash).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **348. Code Review & Cleanup (Cycle 3 — Migration Squash).**

This is the **terminal session of Cycle 3** of the recurring Code Review & Cleanup track — order: 344 audit ✅ → 345 apply ✅ → 346 carve-out A ✅ → 347 carve-out B ✅ → **348 squash**. It is the **mid-cycle squash**, the eighth in the lineage **062 → 082 → 108 → 142 → 181 → 208 → 274 → 348**, and is **distinct from the absolute-final T1 terminal session** in `sessions/release-plan.md` (which runs after every other release-plan entry closes, before Beta 1). Cycle 3 fully closes at this session; the next cycle is cadence-triggered at ~session 398.

This session is **NOT boundary-touching** — no Fleet Manager surface. CRM contract stays **v2.3.0**, no `CONTRACT_VERSION` change. A squash is schema-bootstrap mechanics; it does not change the contract.

**Session shape:** migration-squash (terminal Cycle-3 session). Collapse the **13 migrations** accumulated since the 274 baseline into a fresh `database/schema/pgsql-schema.sql`, absorbing any small leftover schema picks from 345–347. **Cycle 3 is the compressed 3-session shape, so this squash is lighter than 274** — the audit (344) + apply (345) already did the code-optimization / help-docs / Playwright-teardown findings work; do **not** re-run those inventories. This is the squash + a short leftover-pick scan + a Phase-5 buffer for fallout.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference; do not infer format from previous logs.
2. Read the **274 squash** session prompt + log (`sessions/archived/274. Migration Squash & Code Optimization.md` + its `— Log.md`) — the recipe this session follows verbatim (Phase 1 data-phase audit → Phase 4 `schema:dump`/delete/`migrate:fresh` identity → squash-note update). Also skim the **208** squash log for the production-replay close-gate shape.
3. Read the Cycle-3 cluster in `sessions/tracks/code-review-and-cleanup.md` (the squash session's brief: light/mechanical, Phase-5 buffer, "stays standalone") and the release-plan Cycle-3 entry.
4. Read `docs/schema/README.md` (the current squash-note block — last updated 274 / 2026-05-09) and `docs/app-reference.md` for container names + the `schema:dump` / `migrate:fresh --seed` commands.
5. **List `database/migrations/` and the 344/345/346/347 close logs** before touching anything — the 13 migrations are the squash input; the logs carry any deferred leftover pick (e.g. Flag 344-G). Note which migrations have a **data phase** (Trix→Quill, url-token decode, ticket-tier backfill, template-colour relocate, media content-addressed relocation) — those need the seeders verified to produce the end shape before deletion.
6. Note drift vs this prompt; proceed unless a decision is needed.

---

## Starting state inherited from session 347

- **CRM contract at v2.3.0** — unchanged. 347 was an admin-only, behaviour-preserving legibility split of `InlineFormatToolbar.vue` (1768 → 1071 LOC) into composables + popover sub-components; Vue-only, non-boundary. **347 closed at VERSION `0.347.NN`** (so 348 closes at `0.348.<iter>`).
- **Schema baseline:** `database/schema/pgsql-schema.sql` from the **274 squash (2026-05-09)** + **13 migrations** accumulated since (May-10 → May-30). This session collapses those 13 into a fresh dump. No new schema is expected beyond any small Phase-3 leftover pick the user approves.
- **Fast Pest baseline:** **2816 / 0** carried from the 346 close (347 added no Pest — Vue-only; its behaviour net was Playwright + the design-group parity guard). The squash validates by construction via `migrate:fresh --seed`; the only expected Pest delta is a **downward** one if an obsolete migration **test** file has to be deleted as squash fallout (Phase 5 buffer; log the delta).
- **Playwright:** the inline-editing specs + the **347 standing `tests/e2e/page-builder/inline-formatting-toolbar.spec.ts` (7 cases)** + the `design`-group parity guard are the net; each spec's `beforeAll` `migrate:fresh --seed` exercises the fresh dump end-to-end. Run Pest and Playwright **sequentially**, never in parallel.
- **Planning state:** Cycle 3 audit + apply + both carve-outs done; this is the terminal squash. At close, Cycle 3 is fully closed and gets its **Phase Retrospective** (phase-expiry compression) in the track doc; next trigger ~398. The hanging **311 Table widget** spec was superseded by the new **349 Table widget** prompt during 347 — archive `sessions/311. Table Widget.md` at this close if not already swept.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — the squash absorbs only **squash-adjacent** leftovers (an un-landed dropped-column migration, an orphaned migration test/config), not general inbox items; those stay for a feature session.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The squash is mechanical and exact — follow the 274 Phase-4 steps in order.** `schema:dump` → delete every migration file → `migrate:fresh --seed` identity check → squash-note update. Do not edit or reorder historical migrations (no-ops under squash).
- **Verify every data-phase migration is redundant on the fresh-install path before deleting it** — the dump captures schema only; the seeders must already produce the migrated-in shape (Quill content, decoded URLs, ticket tiers, relocated theme colours, content-addressed media). If a seeder does **not** produce the end shape, stop and surface — that's a real gap, not a squash mechanic.
- **Keep Phase 5 as a separate buffer commit.** Squash fallout (an obsolete migration test, a seeder assuming a deleted migration) lands as its own commit, not inside the squash commit.
- **Pre-beta license:** no compat shims; a leftover pick that drops a column/table cuts it as a new migration the squash absorbs.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation this is the light/mechanical mid-cycle squash (not T1), non-boundary, no contract change; (2) the **migration inventory** result — the 13 files, which carry a data phase, and your read on whether the seeders reproduce each data-phase shape (the only thing that can block the squash); (3) the **leftover-pick list** from the 344–347 logs (likely small or empty — Flag 344-G is the known candidate) for the user to pick or defer. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/348. Code Review & Cleanup (Cycle 3 — Migration Squash) — Log.md` — migration count collapsed (13 → 0 + fresh dump), data-phase verification result, leftover picks applied vs deferred, Phase-5 squash-fallout (any test deletions + the test-count delta), and the production re-baseline note (`migrate:fresh --seed` if disposable, else `psql < database/schema/pgsql-schema.sql`).
- **Bump `VERSION`** to `0.348.<iteration>`.
- **Update** the track doc (status snapshot + **Cycle-3 Phase Retrospective**, phase-expiry compression — this is the terminal session of the Cycle-3 phase) + outlines + the release-plan Cycle-3 entry; mark Cycle 3 fully closed; confirm next-trigger ~398.
- **Update** `docs/schema/README.md` squash note → session 348 / today.
- **Archive** `sessions/347. *.md` → `sessions/archived/`; sweep the superseded `sessions/311. Table Widget.md` if still at top level.
- **Commit on `session-348/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: a squash adds no code surface — the win is the migration history collapsed to a clean baseline with `migrate:fresh --seed` identity proven and the full suite green, nothing else changed. Resist scope creep into the 349 table widget or any forward feature.
