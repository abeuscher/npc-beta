# Base Prompt — Session 321 (Demo Node — Lockdown Verify + Baseline Reset + Local Dress-Rehearsal)

**Start now.** Execute the reading list below, open the session prompt at `sessions/321. Demo Node — Lockdown Verify + Baseline Reset + Local Dress-Rehearsal.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **321. Demo Node — Lockdown Verify + Baseline Reset + Local Dress-Rehearsal.**

This session **closes the loop on the demo work** — the standing milestone-close gate (c)/(d) from the Public Marketing Website track (see `session-outlines.md`). It is **not** a `release-plan.md` execution-order entry; it is the carry-forward that lets the user provision a live demo droplet. The bulk of the demo *application* code already shipped (session 289 architecture + session 292 page): `isDemoMode()` keyed on `APP_ENV=demo`, the `/demo/enter` auto-login (`DemoLoginController`, shared `demo@demo.local`, `throttle:10,1`, 404 off-demo), the `demo` role (a seeded **allow-list** with a binding deny-list, session 292), mail forced to `log` in demo mode, and the public `/demo` CTA already wired to `/demo/enter`. So this session is **verify + tune + the reset mechanism + a local dress-rehearsal**, not a greenfield build.

This session is **NOT boundary-touching** — it does not change the `/api/health` agent contract. **CRM contract stays at v2.3.0.** The demo node's identity, version-pinning, egress firewall, and any FM-driven reset are **Fleet Manager's job** and are handed off separately (`sessions/321. base-prompt.md` companion note dropped into the FM repo — see the Cross-Repo note below). It **may add a console command** (`demo:reset`) and a **demo baseline seeder**; no schema change is expected.

**Session shape:** a little dev work, then a scenario. Two halves: (1) **local dev** — verify the `demo` role boundary actually holds in practice, tune its width (the user wants it *as wide as safely possible*, now that an egress firewall will backstop the blast radius), build a `demo:reset` baseline-reseed command + curate the baseline content a prospect should see; (2) **local dress-rehearsal** — run the app under `APP_ENV=demo` locally and walk the full visitor path (public `/demo` → `/demo/enter` → locked-down panel → reset). The live droplet deploy is the user-driven scenario that follows; expect issues to surface there too.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. Read `sessions/session-outlines.md` — the active-tracks block, the **Cross-Repo: Fleet Manager / CRM** block, and the Public Marketing Website close-gate items (c) the Demo copy/`/demo/enter` hookup and (d) the FM shared-contract arc.
3. Read the project memory `project_demo_page_architecture.md` (single-button auto-login, no email, per-IP rate-limit, 24h wipe, demo on its own server) — the settled architecture this session executes against.
4. Read the demo surfaces as they stand: `app/Http/Controllers/DemoLoginController.php`, `app/helpers.php` (`isDemoMode`), the `demo` role block in `database/seeders/PermissionSeeder.php` (the allow-list + the binding deny-list comment), the demo-mode branches in `app/Providers/AppServiceProvider.php` (mail→log) and `app/Filament/Pages/Settings/GeneralSettingsPage.php`, and `sessions/public website/demo.json` (the public `/demo` page + CTA).
5. Read `docs/app-reference.md` (env names, container names, the backup/restore runbook) and the demo-node handoff note that was dropped into the FM repo (`/home/al/fleetmanager/sessions/demo-node-handoff-from-crm.md`) — it states exactly which surfaces are CRM-side (this session) vs FM-side (separate FM session), so scope doesn't bleed.
6. Note any drift between this prompt and the actual code in a brief work-log entry.

---

## Starting state inherited from session 320

- **CRM contract at v2.3.0** — unchanged. 320 was non-boundary (storage-layer infra).
- **Schema baseline:** 320 added **no** column (refcounted delete is computed over `media.content_hash`). The CAS relocation migration (`2026_05_22_130000`) ran; media is content-addressed on disk. No demo-specific schema is expected this session.
- **Fast Pest green at 2667 / 0** (320 close: 2655 + 12 content-addressed-storage tests). New tests expected for the `demo:reset` command and any demo-role boundary assertions.
- **Demo application code already in place** (s289/s292): `isDemoMode()`, `/demo/enter` auto-login, the `demo` allow-list role + deny-list, mail→log in demo, public `/demo` CTA wired. This session verifies and extends rather than builds.
- **Security model already settled this planning round:** the interior surface is the `demo` role (allow-list, deny-by-default — privilege escalation already closed); the infra surface is an **egress firewall** on the droplet (the single control that matters — kills the spam-relay / mining / exfil class; nginx hardening is *not* the spam control and is at most coarse body-size/rate caps in the shared image). Both confirmed with the user. The egress firewall + version-pin + node identity are **FM/ops-side** (handoff note).
- **Reset mechanism leaning:** a **local `demo:reset` reseed on a cron** (no egress, simple) is preferred over FM `RestoreFromBlob` for the single demo node — confirm at the open gate.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this is not a housekeeping batch session; do not absorb inbox items. (Note: the `.env`/`.env.example` audit item was added this round — it belongs to a future housekeeping batch, not here.)

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Cross-repo discipline — this is the lesson from the s320 round.** This work spans both repos. Do **all** CRM-side edits on the `session-321/N` branch; the FM-side work (demo node identity, version pin, egress firewall provisioning profile, any FM-driven reset) is a **separate FM session** driven from the handoff note. Do **not** interleave uncommitted edits across both working trees at once — a CRM edit bled into an FM commit last round precisely because both trees were live together.
- **Testing demo mode locally** means running under `APP_ENV=demo` (which flips `isDemoMode()`: mail→log, settings locked, `/demo/enter` live). Plan a clean way to exercise this without wrecking the normal `local` dev env (a scoped env toggle / separate run), and restore `local` afterward.
- **The demo-role boundary is the headline deliverable — and it's a public, internet-facing auto-login.** Verify the deny-list actually holds in practice (settings, user/role management, API keys, imports, exports, real-data exfiltration, Stripe), not just on paper. Prefer a Playwright or Pest assertion that a `demo`-role user is walled off from each named surface, as a standing guard.
- **Reset must be idempotent and leave a known-good baseline.** Whatever the mechanism, re-running it returns the demo to the curated prospect-facing state cleanly.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`. **Questions are not instructions** per `feedback_questions_are_not_instructions`.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the user to confirm before starting**. Cover the decisions worth settling once: (1) **reset mechanism** — local `demo:reset` reseed-on-cron (lean) vs FM `RestoreFromBlob`; (2) **demo-role width** — keep events/donations view-only, or widen now that the egress firewall backstops the blast radius (the user wants it as wide as safely possible); (3) **baseline content** — what a prospect should see on first entry (public site + how much sample CRM data), and where that lives (a `demo` seeder); (4) **lockdown verification shape** — Pest vs Playwright for the standing demo-role guard. Bring a recommendation for each. One confirmation, then go.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

After implementation and the local dress-rehearsal are complete, draft the next session's documents. The natural next entry is the **live-deploy scenario follow-up** (whatever the droplet deploy surfaces) or a return to the Public Website Complete execution order (**E15 Table widget** / **E16 header-footer**). Confirm with the user at this session's close walkthrough.

- Copy `sessions/template-base-prompt.md` to `sessions/322. base-prompt.md`.
- Draft the matching session prompt from `sessions/template-session-prompt.md`.

Then stop. The user reviews the work and the next session's drafts, and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- Write `sessions/321. Demo Node — Lockdown Verify + Baseline Reset + Local Dress-Rehearsal — Log.md` from `sessions/template-session-log.md`. Capture: the demo-role boundary verification (what was confirmed walled-off, what was widened), the reset mechanism + baseline content decision, the local dress-rehearsal result, and a pointer to the FM-side handoff for the deploy half.
- Bump the `VERSION` file.
- Update `sessions/completed-sessions.md`, `sessions/session-outlines.md` (resolve close-gate items (c)/(d) as far as the CRM side carries them), and the **Cross-Repo block** if anything changed.
- **Archive the previous session's files** (move `sessions/320. *.md` → `sessions/archived/`).
- Fold the capture buffer into the inbox per the standard step.
- Commit on `session-321/N`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages name the surface touched (demo role, `demo:reset`, demo seeder); no docstrings/comments on code you did not write; mirror the existing seeder / console-command / role conventions rather than designing fresh abstractions.
