# Base Prompt — Session 229

We are about to begin a new session: **229. Widget Primitive — Phase 5c (`record_detail_views` table + IsView registry + sub-nav rendering primitive + Templates/Themes refactor)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top and the Beta 1 stubs.
3. Read `sessions/tracks/widget-primitive.md` (the canonical Widget Primitive track doc — premise sits alongside at `widget-primitive-premise.md`). Specifically:
   - Status snapshot (top of doc)
   - Phase Retrospectives — at minimum 5a and 5b, plus skim Phase 3a (212) and Phase 3d (215) since 5c's Filament hosting and admin-eventual-UI both mirror dashboard-track patterns
   - "Phase 5" entry under "The 2.0 transition — rough sequence," especially the 5c bullet
   - "Pending paradigm questions" section — Phase 5e (permissions) and the widget-help gap. **Both are explicitly out of scope for 5c**; they're named so you don't accidentally bake the wrong assumption in.
   - "Design decisions locked in" — particularly the **"a+" View framing** entry. The IsView interface, polymorphic owner discipline, source-as-capability, and concentrated enforcement at the resolver are all load-bearing for this session.
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. **This session writes one schema migration** (`record_detail_views` table); update `docs/schema/` per the standard process rule when the migration lands.
5. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/archived/228. Widget Primitive — Phase 5b (Record-Detail Filament Mount + Ambient Wiring + IsView Primitive) — Log.md` — establishes `IsView` interface, `RecordDetailView` plain-PHP class (hardcoded array), `SOURCE_RECORD_CONTEXT` source/projector/tokens trio, the `RecordDetailViewWidget` Filament hosting on `EditContact`, and the placeholder widget. **5c lifts the hardcoded array into `record_detail_views` table rows and ships the registry that resolves Views by record type.**
   - `sessions/archived/215. Widget Primitive — Phase 3d (Dashboard Config UI & Storage) — Log.md` — establishes the `dashboard_configs` table as polymorphic owner of `page_widgets` (the discipline pattern this session mirrors for Views). Also the pattern for a Filament admin page that owns page-builder Vue mounting — relevant if/when admin UI for Views lands in 5c.5+ (deferred this session).
   - `sessions/archived/214. Widget Primitive — Phase 3c (Dashboard-Native Widgets) — Log.md` — establishes the hardcoded-array pattern for widgets in a Filament-hosted slot, which 5b mirrored and 5c lifts to table-driven.
6. Read the full state of every file this session edits or creates (see the session prompt's Phase descriptions for the file list).
7. Read related existing tests to anchor patterns:
   - `tests/Unit/WidgetPrimitive/Views/RecordDetailViewTest.php` — established 5b shape; the test gets adapted as `RecordDetailView` shifts from in-memory class to Eloquent model.
   - `tests/Feature/EditContactRecordDetailViewTest.php` — Filament hosting smoke test; needs adapting to read the View from the table.
   - `tests/Feature/DashboardSlotGridTest.php` — pattern for Livewire-mounted Filament-widget tests with table-driven content.
8. Open `sessions/229. Widget Primitive — Phase 5c (record_detail_views Table + IsView Registry + Sub-Nav Rendering Primitive).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first. **This session writes one migration** (`record_detail_views` table).
- **Update `docs/schema/`** after writing the migration — add `docs/schema/record_detail_views.md` and update the table index in `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session is PHP + Filament page edits + Blade views** — no widget asset pipeline files, no Vue, no SCSS that the build server compiles. No build expected.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands the `record_detail_views` table + Eloquent model, the `ViewRegistry` (or equivalent) singleton, the sub-nav rendering primitive, and the Templates/Themes consumer retrofit — every piece gets test-locked.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/230. base-prompt.md`. Expected next session: **Phase 5c.5 — admin UI for authoring per-record-type View sets**, OR the **first concrete record-detail widget in the 5d+ arc** (Recent Notes for current contact is the natural starting point per the migration doc). Which lands first depends on the user's priorities at close-gate time — the admin UI is more mechanical, the first concrete widget is the "earn the primitive" milestone.
- **Next session prompt**: draft `sessions/230. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/229. Widget Primitive — Phase 5c (record_detail_views Table + IsView Registry + Sub-Nav Rendering Primitive) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 229's row.
- **Update `sessions/session-outlines.md`** (the roadmap): the Active-Tracks block's Widget Primitive entry needs its remaining-session count bumped (one fewer; ~5–8 left).
- **Update `sessions/tracks/widget-primitive.md`**: bump the status snapshot (last update date, what's complete, what's active). **Phase 5c closes a phase** — apply the **phase-expiry compression** per the close-gate rule: lift session 229's session-by-session detail out of the status snapshot and write a compressed retrospective entry in the "Phase Retrospectives" section.
- **Archive the previous session**: move all files matching `sessions/228. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines/track docs, the completed-sessions index, the schema migration, the schema docs, and any files moved into `sessions/archived/`), commit on the current `session-229/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
