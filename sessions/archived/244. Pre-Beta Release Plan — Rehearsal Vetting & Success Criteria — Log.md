# Session 244 — Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria — Log

**Date:** 2026-04-29
**Branch:** session-244/1

---

## What was built

A planning session that produced four artifacts, no code:

- `sessions/release-plan.md` — replaces the retired `sessions/release-plan-outline.md`. Canonical pre-Beta-1 release gate doc carrying 11 discipline rules, pre-release requirements register, working set across Tracks A–E + Terminal (32 numbered positions in execution order), out-of-gate register, and deferred-decisions section.
- `sessions/session-outlines.md` — updated with FM Active Track flag for Beta-1-blocking node-ops resumption; new pre-Beta-1 stubs for A2 / A3 / A5; new ### Rehearsal Sessions subsection inside Beta-1 Scope (10 pointer-stubs into release-plan.md); Notes Permissions stub split (feature half stays as C1; audit half consumed by C3); Code Review stub renamed to Code Review + Migration Squash (T1 terminal); Random Data Generator stub annotated A1; Privacy/ToS + master-runbook deferred decision added to Infrastructure & Ops; Event Description Widget Removal, Text Color Hierarchy Rules, and Financial Data Origin Phase B/C all moved to post-Beta-1; the 244 stub itself removed.
- `sessions/245. base-prompt.md` — drafted against A1 (Random Data Generator as Dashboard Widget). Names success criterion up front per the 244 prompt's instruction; customizes read list to include `release-plan.md`, the existing `APP_DEBUG_TOOLS` debug generator, session 233's `source` column work, schema docs for the touched tables, and a Filament dashboard widget pattern reference. Phase 1 close-gate notes the 246 session targets A2 with shape (one or two sessions) decided at draft time per Rule 11.
- `sessions/245. Random Data Generator as Dashboard Widget.md` — design decisions resolved (super-admin gate, `source = 'generated'` tag, confirmation modal, custom-field type respect, retire `APP_DEBUG_TOOLS` surface). Three phases. Out-of-scope explicit on adding `source` columns to `contacts`/`events` (surface as finding rather than silent migration).

### Phase 1 — Per-candidate vetting (verdicts)

The four vetting criteria from the outline (confidence delivered / failure prevented / time cost / artifact produced) ran against every candidate. Results below. Per-candidate success criteria, prerequisites, artifacts, and time costs all live in `release-plan.md`; the log carries only the verdict register.

**Onboarding category — all IN, folded into B2 cluster:**

| Candidate | Verdict | Notes |
|-----------|---------|-------|
| Custom fields + public collection ingestion | **IN — B2** | Prerequisite: B1 Org Model Overhaul (kept as separate stub session) |
| Migration in from messy CSV | **IN — B2** | Folded into shared messy-data fixture |
| Migration out | **IN — B2** | Folded into B2; lower priority sub-scenario |
| Custom-field-with-lookup edge case *(named in 244 prompt)* | **IN — B2 sub-criterion** | Either works against B2 fixture or surfaces as a documented finding |

**Capsize:**

| Candidate | Verdict | Notes |
|-----------|---------|-------|
| DB wipe + backup recovery | **IN — A4 (runbook polish)** | Procedure verified twice end-to-end at session 242 against local infrastructure; A4 validates against production-shape infra and produces operator runbook |

**Scale:**

| Candidate | Verdict | Notes |
|-----------|---------|-------|
| Very large dataset performance | **IN — D1** | Sizing-doc artifact carries strong sales credibility |

**Workflow — most IN, several folded into C3:**

| Candidate | Verdict | Notes |
|-----------|---------|-------|
| Membership renewal cycle | **IN — C6** | No clean stub overlap; feature exercise of existing membership system |
| Donation-to-acknowledgment loop | **IN — C4** | Strongest narrative arc for sales; prerequisite E4 (Stripe Checkout Branding) |
| Event with everything | **IN — C5** | Prerequisite C2 (Event Ticket Tiers) — kept as separate stub session |
| Email at volume | **IN — C7** | Prerequisite A3 (production-infra send is the meaningful test) |
| Concurrent admin editing | **IN — folded into C3** | Audit-style; small scope; folds with Permission audit + Accidental public exposure |
| Permission audit | **IN — C3** | Half-split with existing Notes Permissions stub: feature half stays as C1 (own session); audit half consumed by C3 |
| Accidental public exposure | **IN — folded into C3** | Distinct from Permission audit (content classification vs role permissions) but shares the audit-style discipline; folded for one session |
| Integration retest *(243-added)* | **IN — D3** | Audit-style; runs absolutely last per Rule 9 |

**Compatibility — all IN, folded into D2:**

| Candidate | Verdict | Notes |
|-----------|---------|-------|
| Browser bingo | **IN — folded into D2** | Folded with Accessibility + Flaky connection; may split per Rule 11 |
| Accessibility pass | **IN — folded into D2** | WCAG AA scope agreed (axe-core + manual screen-reader pass on key flows) |
| Flaky connection field test | **IN — folded into D2** | Folded; may split out if scope inflates |

**Pre-release requirements (non-rehearsal) — all IN as Track A:**

| Candidate | Verdict | Notes |
|-----------|---------|-------|
| Fleet Manager — node operations parity | **IN — A2** | Re-opens FM Active Track for Beta-1-blocking specific scope (install / backup / restore / log-reading); CRM contract stays at v1.2.0 |
| Multi-node operational readiness | **IN — A3** | Operational provisioning, mostly not code |

### Phase 1 — Per-candidate vetting (out / merged / deferred)

| Candidate | Verdict | Reason |
|-----------|---------|--------|
| Event Description Widget Removal → PageContext | **OUT — post-1.0** | Refactor; no rehearsal forces it. Roadmap entry moved to post-Beta-1 section. |
| Text Color Hierarchy Rules | **OUT — post-1.0** | Design discussion that doesn't surface during any rehearsal as currently scoped. Defer until forced. |
| Financial Data Origin Phase B/C | **OUT — post-release** | User direction at 244: "definitely out of scope / post release." Phase A complete at 233; B and C deferred. Roadmap entries moved to post-Beta-1 section. |
| Test Suite Audit | **OUT — conditional** | Recent partial run did not show significant cruft per user. Becomes a priority only if test suite timeout starts to feel like a blocker. Stub stays in roadmap (not moved). |

### Phase 1 — Surfaced during 244 (not in original outline)

| Item | Verdict | Track / position |
|------|---------|------------------|
| **2FA for admin accounts** | **IN** | A5 — added after surfacing as a single-person-support gap during the conversation. Foundational security; must close before C3 |
| **Random Data Generator promotion to Track A** | **IN** | A1 — originally a Track E item in the existing roadmap; promoted to A1 (first session) because it's fixture infrastructure for B2/C3/C4/C5/C6/D1 |
| **Privacy policy + ToS** | **IN — pre-release requirements register** | Non-session checklist gate items in `release-plan.md`. User has drafts in process with counsel |
| **Operator master runbook / SOPs** | **DEFERRED DECISION** | Captured in pre-release requirements register and in `release-plan.md` Deferred decisions. TBD whether this work lives in this project or a separate non-technical project; revisit before Beta-1 release |
| **Code Review + Migration Squash bundling** | **IN — T1 terminal** | User direction: code review pairs with migration squash and runs immediately pre-release as a single terminal session. Existing Code Review & Cleanup stub renamed |
| **Stub-then-rehearsal discipline (rule)** | **Adopted as Rule 1 + Rule 2** | When a rehearsal has a prerequisite stub, the stub ships first as its own session; rehearsals don't do feature work. Audit-style rehearsals (C3, D3) may absorb small in-session fixes |
| **Session count flexibility (rule)** | **Adopted as Rule 11** | A session that surfaces unforeseen work splits rather than overloading a single context window. Plan tracks the work, not the count |

### Phase 2 — Sequencing

Flat execution order produced (32 positions). Track A foundations run first (Rule 7). Track B onboarding cluster requires B1 prerequisite + E2/E3 Track-E items first. Track C interleaves with Track E items that affect each rehearsal's surface (E4 before C4; E9 between C1 and C2). Track D mobile/typography prereqs (E5/E6/E7) close before D2 starts (Rule 8). D3 absolute-last among rehearsals (Rule 9). T1 terminal (Rule 10). Full execution order in `release-plan.md` § Execution order.

### Phase 3 — Roadmap update

`session-outlines.md` updated with all changes summarized in the artifact list above. The new ### Rehearsal Sessions subsection collects 10 pointer-stubs (A4 + B2 + C3 + C4 + C5 + C6 + C7 + D1 + D2 + D3); the Track A non-rehearsal sessions (A1 / A2 / A3 / A5) and Track C prerequisite sessions (C1 feature half, C2 Event Ticket Tiers) live as full stubs alongside the existing Track E items.

### Phase 4 — Replace the outline with the plan

`git mv sessions/release-plan-outline.md sessions/release-plan.md` followed by full rewrite of contents. The outline doc is retired; the plan doc is canonical.

---

## Stub-pairing decisions register

| Existing stub | Rehearsal candidate | Shape | Outcome |
|---------------|---------------------|-------|---------|
| Notes Permissions & Permissions Audit | Workflow `Permission audit` | **Half-split** | Feature half stays as C1 (separate session); audit half consumed by C3 (folded with Concurrent admin editing + Accidental public exposure). Existing stub edited in roadmap to scope to feature half only |
| Organizations Model Overhaul | Onboarding `Custom fields + public collection ingestion` | **Split** | Prerequisite stub (B1) ships first as own session; B2 rehearsal cluster runs after with B1 in place |
| Event Ticket Tiers | Workflow `Event with everything` | **Split** | Prerequisite stub (C2) ships first as own session; C5 rehearsal runs after with tier feature in place |

The principle that drops out: **rehearsals are verification, not implementation.** Audit-style rehearsals (C3, D3) may absorb small fixes; feature-style rehearsals must not roll feature work in. Codified as Rules 1 + 2 in `release-plan.md`.

---

## Investment-gate framing

User stance at 244: skip the subset selection. The full plan as-shown is sufficient to seek investment against. All working-set entries in `release-plan.md` carry `gate: release` only. The structural slot to flip individual entries to `gate: release, investment` exists per Rule 6 if a future conversation wants it, but no markup change happens in this session.

---

## Security checklist — verified

- [x] No code edits, migrations, or Fleet Manager contract changes. Confirmed by `git status`: only doc-shaped changes (release-plan.md rename + rewrite, session-outlines.md edits, two new 245 prompt files).
- [x] No portal data path is touched.
- [x] `release-plan.md` does not name internal credentials, env-var values, customer data, or anything that would be sensitive if the plan doc were shared with a prospect. Reviewed before close.
- [x] Each "In" rehearsal has a written success criterion in `release-plan.md` before close.

---

## Schema changes

None.

---

## Production deployment notes

None — 244 produced planning artifacts only; no code, no migrations, no operator action.

---

## Deferred decisions

- **Investment-gate subset selection.** Skipped at user direction; full plan is sufficient for investment seeking. Structural slot exists per Rule 6 if a future conversation wants it.
- **Operator master runbook / SOPs scope.** TBD whether this work lives in this project or a separate non-technical project. Revisit before Beta-1 release.
- **A2 split shape.** Fleet Manager node operations parity may run as one session or two (install + backup + restore in one; log-reading separately). Decide at A2 session start based on FM-side codebase state. Captured in 245 close-gate Phase 1 instruction.
- **C3 split shape.** Permission audit + concurrent + exposure folded; may split if findings exceed in-session-fix capacity (Rule 2).
- **D2 split shape.** Compatibility cluster folded; may split into Browser+Accessibility / Flaky-connection halves if scope inflates.
- **E12 (Housekeeping Batch 2) shape.** May split per Rule 11 if any item turns out to need design-level conversation; the existing stub flags `app:reset` as the largest item.
- **Track E ordering positions 5–14.** User accepted my best-guess interleaving on the explicit understanding that "all of this stuff is blocking" and the order is for sense-making, not unending logic. Future sessions may reorder Track E items as scheduling allows, provided dependencies on rehearsals (Rule 8 for D2 prereqs; E4 before C4) are honored.

---

## Test result

N/A — 244 produced no code change. Per the base prompt's "no fast-test run expected" rule, the fast Pest baseline stays at 1689 from session 243 close.
