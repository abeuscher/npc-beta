# Base Prompt — Session 221

We are about to begin a new session: **221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session writes one data-shape migration to flatten existing `page_widgets.query_config` rows from slot-keyed to top-level; no column changes, but read `docs/schema/page_widgets.md`, `docs/schema/widget_types.md`, `docs/schema/tags.md`, and `docs/schema/taggables.md` to understand the surfaces involved.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise.
   - `sessions/widget-primitive-migration.md` — the migration sketch. Phase 4 retrofit is mid-campaign; this is the **Phase 4 follower** queued in `session-outlines.md` lines 255–262 and made formally landable by session 220's retirement of the last slot-declaring widgets. Read the full follower stub there before starting — it captures the surface area, the precedence question, and the keying-convention question.
4. Read the four most recent Phase 4 session logs to understand the trail of decisions this session lands on top of:
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — `SOURCE_SYSTEM_MODEL` retrofit pattern; the `date_range: from now` default that this session must keep locked from user override.
   - `sessions/archived/218. Widget Primitive — Phase 4c (Carousel Retrofit — Audit, Test-Lock, Cleanup) — Log.md` — established the system rule that contract-locked widgets do not declare `collections()`. Set up the slot-strip retirement.
   - `sessions/archived/219. Widget Primitive — Phase 4d (BarChart Retrofit) — Log.md` — surfaced query settings as a real gap (panel persists, user-supplied `limit:4` silently ignored at render time); retired one slot-strip test, rewrote the other against a contract-locked widget; established the seeder-discipline rule.
   - `sessions/220. Widget Primitive — Phase 4e (BoardMembers + LogoGarden Retrofit) — Log.md` (or `sessions/archived/` if 221 close has moved it) — last two slot-declaring widgets retired; the `WidgetRenderer::render()` line 46 legacy loop now iterates 0 times for every widget in `app/Widgets/*`; `WidgetDataResolver::resolveCustom` is unreferenced from the renderer.
5. Read the full state of every file this session edits. Specifically:
   - `app/Services/WidgetRenderer.php` — the legacy `foreach ($widgetType->collections ?? [])` loop at lines 44–51 (now dead code), and the `ContractResolver::resolve()` call at line 84 (where user `query_config` will merge into `$contract->filters`).
   - `app/Services/WidgetDataResolver.php::resolveCustom()` — the dead path. Note lines 70 and 74 reference `cmsTags`, a relation deleted in session 040 — `include_tags`/`exclude_tags` on the legacy path has been silently broken for ~14 months. Goes away when `resolveCustom` is deleted; not a separate fix.
   - `app/Http/Controllers/Admin/PageBuilderApiController.php::filterQueryConfig()` (around line 662) — slot-allowlist strip mechanism, now a no-op (every widget's `collections` column is `[]`). To be deleted.
   - `app/WidgetPrimitive/ContractResolver.php` — where `order_by`, `direction`, `include_tags`, `exclude_tags` get implemented per source-arm (`resolveWidgetContentType`, `resolvePost`, `resolveEvent`).
   - `app/WidgetPrimitive/DataContract.php` — the contract's `filters` array shape; how user knobs merge.
   - `app/Widgets/Contracts/WidgetDefinition.php` — the parent class; this session adds a `querySettings()` declaration method (default empty) that widgets override per-source-arm if they want a panel.
   - `resources/js/page-builder-vue/components/QuerySettings.vue` — the chevron toggle (lines 39–58), slot-keyed iteration (line 62), hardcoded `order_by` options (lines 100–104) — all rewritten this session.
   - `resources/js/page-builder-vue/components/InspectorFieldGroup.vue` line 99 — the misnamed `"Carousel Settings"` accordion heading on the `advanced: true` chevron group; the chevron disclosure is removed and the heading is renamed (or the group is flattened entirely — design choice in this session).
   - `resources/js/page-builder-vue/components/InspectorPanel.vue` line 176 — where `<QuerySettings>` is mounted.
   - `resources/js/page-builder-vue/stores/editor.ts` `updateLocalQueryConfig` (around line 809) — signature changes from `(widgetId, collHandle, key, value)` to `(widgetId, key, value)` after flattening.
   - `resources/js/page-builder-vue/types.ts` — `Widget.widget_type_collections: string[]` (line 29) becomes vestigial; `Widget.query_config: Record<string, any>` shape semantics change from slot-keyed to flat.
6. Read the tag relations to confirm the uniformity finding:
   - `app/Models/Page.php`, `app/Models/Event.php`, `app/Models/CollectionItem.php`, `app/Models/Organization.php`, `app/Models/Contact.php` — all five declare `tags(): MorphToMany` with the identical `morphToMany(Tag::class, 'taggable')` shape. One implementation works for every source-arm: `whereHas('tags', fn ($q) => $q->whereIn('slug', $tagSlugs))`. There is **no** legacy `cmsTags` relation on `CollectionItem` (deleted in session 040, despite `WidgetDataResolver::resolveCustom`'s stale reference).
7. Read the four contract retrofit test files to understand the Test B query-count pin pattern this session must preserve:
   - `tests/Feature/CarouselContractRetrofitTest.php`
   - `tests/Feature/BarChartContractRetrofitTest.php`
   - `tests/Feature/BoardMembersContractRetrofitTest.php`
   - `tests/Feature/LogoGardenContractRetrofitTest.php`
   None of these pass `query_config` today, so the existing query-count pins should still hold byte-equivalent unchanged — no new joins or `whereHas` subqueries when no filters are supplied. Verify mid-session.
8. Read `tests/Feature/PageBuilderApiTest.php` end-to-end. The slot-strip pair was retired/rewritten in 219; the remaining query_config round-trip assertions need updating to the flat shape this session lands.
9. Read `tests/Feature/AppearanceConfigRenameTest.php` lines 187–215 — the two Blade-render tests that exercise widgets with `widgetData`/`collectionData` directly. These pass today; verify they still pass after the renderer's data-shape change.
10. Read `resources/docs/widget-development.md` lines 77–96 and 320–325 — these prose blocks describe `$collectionData` and `WidgetDataResolver::resolve()` flowing into the template. Edit conservatively to reflect the new path: `$widgetData` is the only data path; `query_config` knobs flow through the contract resolver; the slot keying is gone.
11. Open `sessions/221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer).md` and read the session prompt carefully.
12. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216–220. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. The admin bundle, the public site CSS, and the page-builder Vue app all compile through Vite, so this one command covers all three surfaces. **This session edits `resources/js/page-builder-vue/**` — `npm run build` is required.**
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. Not expected this session — no widget asset files touched.
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/222. base-prompt.md`. Expected next session: continued Phase 4 retrofit on the remaining 5 system-model-shaped candidates — BlogPager (cheapest; post-shaped, projector exists), the Event trio (EventDescription, EventRegistration, EventCalendar handled separately given its client-side render path), or the Product pair (ProductCarousel + ProductDisplay; require a new `'product'` arm in `ContractResolver` and `SystemModelProjector`). Scope to be agreed with the user.
- **Next session prompt**: draft `sessions/222. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: under the Widget Primitive track stub, mark the Phase 4 follower complete (query settings flow through the contract layer; the legacy `WidgetRenderer::render()` line 46 loop, `WidgetDataResolver::resolveCustom` and its private helpers, `PageBuilderApiController::filterQueryConfig`, the `widget_type_collections` Vue field, and the two chevron toggles in `QuerySettings.vue` and `InspectorFieldGroup.vue` are all retired). Note that Phase 4 retrofit continues for the 5 remaining system-model-shaped candidates and for the product pair (which require a new `'product'` arm).
- **Archive the previous session**: move all files matching `sessions/220. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-221/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles. (Captured in `feedback_no_collapse_ui` memory.)
