# Base Prompt — Session 397 (Forms Carved, Packaged & Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/397. Forms Carved, Packaged & Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **397. Forms Carved, Packaged & Extracted**.

This is **arc position D7, the whole block in one session** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical). D6 closed at 396 (`crm-plugin--blog` v0.1.0; contract 0.20.0; `plugins/` empty a fifth time; the first single-session carve → package → extract). This session runs the Forms block the same way — carve (`plugins/Forms/`: `FormSubmissionController` + the `forms.submit` route, the WebForm widget + the `<x-public-form>` component, the Form admin resource, the submission observer + mailable, both form email handles) → package (`nonprofitcrm/forms`, P6 mechanics) **+ the fifth squash-boundary redraw** (the `forms` + `form_submissions` tables leave the core dump — the first **two-sequence** redraw; SET-NULL contact FK as plain behavior; zero inbound core FKs) → the **eighth extraction-runbook run** to `crm-plugin--forms`. The block's design freight: **the observer detach** (the `#[ObservedBy]` attribute on the core `FormSubmission` model inverts to a plugin-side `observe()` registration — the Memberships precedent) and **the first plugin-owned Blade component** (`<x-public-form>`, the WebForm widget's real render surface).

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.20.0 → 0.21.0 at close (Package shape + External repo eighth consumer; surface 5 fifth redraw consumer with the two-sequence note; surface 7 amended — both form handles migrate to the plugin registry, resolving the "permanently" wording per the 394 precedent; the plugin-owned Blade component note; changelog).

**Session shape:** carve + package/redraw + extraction, behavior-preserving on the full composition. Routes 272/272 byte-identical (the moved controller FQCN is the 381 exception); no visual change; the schema events are boundary moves only (identity check (a) proves byte-identity). **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D6 ✅, D7 open) and § 6.7 (models stay core — `Form`/`FormSubmission` stay, which is exactly why the observer attribute must invert).
3. Read the 396 session log (`sessions/396. Blog Carved, Packaged & Extracted — Log.md`) — especially its **D7 kickoff input** section and the single-session block pacing.
4. Read `docs/plugin-contract.md` at 0.20.0 — § Package shape (P6 mechanics), § External repo (the extraction posture), § The distribution manifest (the `folder` → `path` → `vcs` walk and the guard interlock), surface 5 in full (the redraw mechanics — sequences travel, the identity-check procedure, the seeder-gate note), surface 7 (the registry mechanics this session's handle migration uses), surface 4 (front-of-house route ordering).
5. Read `docs/plugin-extraction-runbook.md` — the eighth run; the 393/395/396 worked examples carry the same-session compression nuances.
6. Read the 393 log (`sessions/archived/393. Memberships Packaged & Extracted — Log.md`) for the redraw + one-session playbook, and skim the 395 log for the sequence-carrying redraw mechanics (`portal_accounts_id_seq` — this session carries two).
7. Open `sessions/397. Forms Carved, Packaged & Extracted.md` — the survey and front-loaded decisions; note the Open-Gate items (five ratifications; the owner-created `crm-plugin--forms` repo; sibling freshness with **no sibling edit expected**).
8. Skim the surfaces: `app/Http/Controllers/FormSubmissionController.php` (the collision seam ~mid-file); `routes/web.php` ~52 (the route) and ~108 (the catch-all); `app/Widgets/WebForm/` + `resources/views/components/public-form.blade.php`; `app/Filament/Resources/FormResource.php` + pages + the relation manager; `app/Models/FormSubmission.php` (the `#[ObservedBy]` attribute) + `app/Observers/FormSubmissionObserver.php` + `app/Mail/FormSubmissionMailable.php`; `app/Providers/AuthServiceProvider.php` (the two policy lines); `database/seeders/FormSeeder.php` + `DatabaseSeeder.php` ~31 (the call) and ~144–151 (the two email rows); the two tables' DDL in `database/schema/pgsql-schema.sql` (~343–410 + constraints/FKs); `app/Services/PageBuilderDataSources.php` ~51 and `app/Services/PageContext.php` ~26 (the core reads); `app/Widgets/QuickActions/QuickActionsDefinition.php` (the `new_form` entry).
9. Note any drift between the session prompt's captured survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 396

- **CRM contract at v2.7.0** — 396 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D6 complete. **Seven extracted plugins** (`crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` v0.3.0, `crm-plugin--events` **v0.1.3** — the 396 pager-styles rider, `crm-plugin--donations` v0.1.0, `crm-plugin--memberships` v0.1.0, `crm-plugin--member-portal` v0.1.1, `crm-plugin--blog` v0.1.0); **`plugins/` is empty** until this session's carve refills it. Plugin contract **v0.20.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** eleven plugin-owned tables in their packages' migrations; the core dump is core-only (guard-enforced). The dead `posts` table dropped at 396 (a live core drop migration remains in `database/migrations/`). The two form tables sit in the core dump — their redraw is this session's Phase B.
- **Fast Pest suite green at 3411 / 0 parallel** (396 close: 3391 baseline + 20 additive). The `Blog` testsuite (6 files, 23 tests) runs from `vendor/nonprofitcrm/blog/tests`. Expected delta this session: **additive** — the Forms twin/mirror/flag/boundary/schema set; existing form assertions unchanged beyond path/namespace re-points.
- **Route baseline: 272 routes** (dev/test), byte-identical to main at 396 — unchanged this session (the one forms route moves but keeps name/URI/middleware; the controller FQCN is the 381 exception).
- **Composition-safety state:** the member-portal capability gates every core portal reach (394/395 — do not touch); the blog needs no capability (396); the forms collision seam already consumes the member-portal capability from inside the moving controller — it travels verbatim. **The shared pager stylesheet is core-owned** (`resources/scss/widgets/_shared-pager.scss`, ADR 0007) — not a forms concern, listed so nobody "cleans it up".
- **The twin fixtures this session DO carry migrator paths** (unlike Blog — this plugin owns schema); they land vendor-relative directly (the 393/395/396 compression nuance).
- **Playwright suite** — CI signal, non-gating; the parked chrome-parity spec remains the known failure. No local Playwright.
- **Housekeeping inbox** — this shape **absorbs half of one item**: the `portal_form_collision` defaults item resolves via the handle's registry migration (front-loaded decision 3); its `admin_invitation` half stays. The CSP-font and chrome-parity items stand.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **The carve is the 389/392/394/396 playbook**: provider + capability-free registration, the route registered at `boot()` ahead of the catch-all byte-identically, the widget nested under `Widgets/` + the component per decision 4, the observer inversion per decision 2 (attribute off the core model, `observe()` in plugin `boot()` — never a core-model reference to a plugin class), policies via `Gate::policy()`, the handle contributions at `register()`, the rule-5 testsuite pass, twins/mirrors/flag/boundary guard. `widgets:sync` + `build:public` at the carve; **gate 4 records n/a — WebForm declares no assets** (the 387 wording; the sync-row proof stands in).
- **The redraw is the fifth** (decision 7's mechanics): byte-faithful raw-DDL create-table migrations in FK order (`forms` → `form_submissions`), **both sequences travel complete** (the first two-sequence redraw — record the count), the SET-NULL contact FK as plain behavior, the internal CASCADE FK with its tables; dump regenerated against a scratch database (recreate the database, not the schema); both identity checks clean; fixtures vendor-relative directly.
- **The handle migration is decision 3's mechanics**: both handles contributed at `register()` with defaults byte-identical to the seeder copy; the two `DatabaseSeeder` email rows removed (the registry-driven seeder path covers them); the inbox item split at close per the session prompt's closing steps.
- **Factory-email discipline:** any new arrange feeding a NOT NULL column pins its factory values (the 395 flake lesson).
- **The manifest is edited, never the generated file.** Scope is binding: no `Form`/`FormSubmission` model move, no forms capability, no per-plugin CI (D11), no FM surface, no D8 work. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.20.0 → 0.21.0 (eighth Package-shape + External-repo consumer; surface 5 fifth redraw + two-sequence note; surface 7 amendment; the Blade-component note; changelog); plan doc § Stage D D7 line closed ✅; the D8 kickoff survey captured at close (Products/store — the § 6.8 disposition resolves at D8's prompt draft) and the 398 prompts drafted.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the survey still matches the code, state the carve → package/redraw → extract plan — and resolve the open items: **(1)** the owner ratifies the five front-loaded calls (one-session block shape; the observer inversion; the handle migration + inbox absorb; the `<x-public-form>` component travel; the `FormSeeder` route-presence gate); **(2)** the owner has created the public `crm-plugin--forms` repository (blocks only the extraction phase); **(3)** the sibling checkouts are current (`git pull` each of the seven — **no sibling edit is expected** this session). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
