# Base Prompt — Session 345 (Code Review & Cleanup — Cycle 3 Apply)

**Start now.** Execute the reading list below, open the session prompt at `sessions/345. Code Review & Cleanup (Apply).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **345. Code Review & Cleanup (Cycle 3 — Apply).**

This is the **apply session** of Code Review & Cleanup Cycle 3 — the second of the compressed 3-session cycle (audit 344 ✅ → **apply 345** → squash 348), expanded by two carve-outs (346/347) lifted at the 344 close. It consumes the W7/W8/W11/W12/Open-Flags backlog from the **344 audit log, which is canonical for scope** — every pick, its file:line, and its disposition is there. It is **not** a `release-plan.md`-numbered entry on its own; the Cycle-3 cluster is recorded in `release-plan.md`'s post-milestone section and the cycle shape is canonical in `sessions/tracks/code-review-and-cleanup.md`.

This session is **NOT boundary-touching** — no Fleet Manager contract surface is in scope. CRM contract stays at **v2.3.0**, no `CONTRACT_VERSION` change. (Flag 344-A touches the media-upload pipeline and Flag 344-C touches the importer/observer seam — both CRM-internal, neither is the FM surface.)

**Session shape:** track-phase (apply). Six iterations on a single `session-345/1` branch, each landing one fix-or-cleanup group from the audit; the **6-iteration ceiling is a hard heuristic** — anything that would push past it is a carve-out smell (and the two big file-splits are already carved out to 346/347, so they are explicitly **out of scope here**).

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference.
2. **Read the 344 audit log in full — it is the canonical findings document:** `sessions/344. Code Review & Cleanup (Cycle 3 — Audit) — Log.md`. The merged W7/W8 tables, W11 quantitative, W12 candidate table, the Open Flags block (344-A…344-G), and the agreed 6-iteration plan all live there.
3. Read the track doc `sessions/tracks/code-review-and-cleanup.md` — the Cycle-3 shape, the carve-out boundary, the standing improvements (the convention-drift Pest test is **this session's** deliverable), and the reaffirmed won't-fixes (do not relitigate).
4. Read `docs/app-reference.md` for the subsystem map + container names. Read the W11 advisory `sessions/code-review-large-files-partial-extraction.md` for the file-length dispositions.
5. **Before editing any file, read it** — the audit's line numbers are advisory and may have drifted; re-verify each pick against the live code at the open gate (the standing adapt-to-drift rule). In particular re-confirm the 6 dead-import occurrence counts and the `getRelationManagers` site before deleting/renaming.
6. **Before wiring the SVG sanitizer (Flag 344-A), ask the existing-infrastructure question:** the media pipeline already has `SvgSanitizer` (built, unused) + Spatie media events + `registerMediaConversions` SVG-skip — confirm the right hook (a media-added listener vs an upload-controller pass) before building anything new.
7. Note any drift between this prompt / the audit log and the actual code in a brief work-log entry; proceed unless something needs a decision per the drift / decision-threshold rules.

---

## Starting state inherited from session 344

- **CRM contract at v2.3.0** — unchanged. 344 was analysis-only (the Cycle-3 audit); no code shipped. This session is non-boundary.
- **Schema baseline:** no change since 328 (squash baseline at 274; 13 migrations on disk since). **No migration expected** unless the boolean-settings `use_pico` fix (Flag/Pick) is done as a seeder edit (not a migration). Any schema touch updates `docs/schema/`.
- **Fast Pest baseline 2799 / 0** (343 close; re-confirmed unchanged at 344 close — analysis only). **New tests expected this session** — the convention-drift Pest test (iteration /5), characterization tests for the importer W7 dedup (/4), and regression tests for the security/correctness fixes (/1, /2).
- **Playwright:** baseline preserved; none added at 344. Add a spec only if a fix needs browser-level verification (e.g. the EventRegistration-import-suppression path), torn down at close unless it's a standing guard.
- **Planning state:** Cycle 3 is in progress (audit done). The cycle is recorded in `release-plan.md`'s post-milestone section; carve-outs 346/347 are drafted and queued before the squash (348). The convention-drift Pest test is a Cycle-3 standing deliverable owned by this session.
- **Housekeeping inbox:** an apply session is not a housekeeping-batch drain. The capture buffer was empty at 344 close.

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md`). Session-specific notes:

- **Per-iteration commits on one branch.** Each of the 6 iterations is a self-contained commit on `session-345/1`. Run the fast suite (`./dev test`) before announcing each iteration ready to deploy/test.
- **Carve-outs are out of scope.** The `ContentImporter`/`ContentExporter` split (346) and the `InlineFormatToolbar` extraction (347) do **not** happen here. If an iteration starts growing into one of those, stop — that's the carve-out boundary working.
- **Don't relitigate reaffirmed won't-fixes** (three large importer PHP files, ImporterPage action closures, chunked-tick→queued-job, Flag B, `_custom.scss`, PostResource rename, DashboardConfig→DashboardView rename). Note as standing.
- **Boundary stays untouched.** No FM contract edits, no `CONTRACT_VERSION` change.
- **Verify objective outcomes yourself** (CLI/file/test/Playwright) — especially the security + correctness fixes (/1, /2). A quick Playwright spec for the EventRegistration-import-no-email path is the model.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the 6-iteration plan from the 344 log and that the two carve-outs stay out of scope; (2) the two genuine decisions the audit deferred into apply — for Flag 344-B, rename `getRelationManagers`→`getRelations` (activates the Collection items relation manager) **vs** delete it + `CollectionItemsRelationManager` (if items are managed via `ContentCollectionResource`); and for Flag 344-F, add explicit `canAccess()` to the 6 Resources **vs** bless the `canViewAny` delegation pattern in the convention test; (3) the SVG-sanitizer wiring approach (media-added listener vs upload-pass) and how to correct the 329 record. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 + Phase 2. Session-specific details:

- **Log file:** `sessions/345. Code Review & Cleanup (Apply) — Log.md` — carry the per-iteration breakdown, the W7/W8/Open-Flags outcome tables (applied/skipped/where), and the test deltas.
- **Bump `VERSION`** to `0.345.<iteration>`.
- **Update** `sessions/tracks/code-review-and-cleanup.md` status snapshot (Cycle 3: audit + apply done; carve-outs 346/347 next, squash 348 last) and the `sessions/session-outlines.md` active-tracks line + the release-plan Cycle-3 entry.
- **Next:** carve-out 346 + 347 prompts already exist (drafted at 344 close) — re-verify their line numbers against the post-345 tree; do **not** draft the 348 squash prompt yet (drafted at 347-close).
- **Archive** `sessions/344. *.md` → `sessions/archived/`.
- **Commit on `session-345/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: this is a cleanup apply — **follow existing conventions exactly, change only what the audit picked, add tests for every behaviour change.** The win is a clean branch that lands the security/correctness fixes + the mechanical dedup + the standing convention-drift gate, without scope-creeping into the carve-outs.
