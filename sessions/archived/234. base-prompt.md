# Base Prompt — Session 234

We are about to begin a new session: **234. Widget Primitive — Phase 5d-3 (Recent Donations — Third Concrete Record-Detail Widget)**.

This session resumes the Widget Primitive track after the 233 detour into Financial Data Origin Phase A. With the `source` column now live on `donations` and the import/Stripe/manual write paths all source-aware, Recent Donations can ship with origin-aware projector affordances clean from day one.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top. The Financial Data Origin & Lifecycle Discipline stub was compressed at 233 close and Phase A is no longer active work.
3. Read `sessions/tracks/widget-primitive.md` — this is the active track for this session. The status snapshot, the `Phase 5` sequence, the `Phase Retrospectives` for 5a / 5b / 5c / 5c.5 / 5d-1 / 5d-2, and the `Pending paradigm questions` section (specifically "Permissions in the contract layer") are all directly load-bearing.
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, plus the schema files this session reads against:
   - `docs/schema/donations.md` (post-233: now carries `source`)
   - `docs/schema/contacts.md` (the ambient-record type)
   - `docs/schema/funds.md` (for the `donation_fund_name` projection)
5. Read the most recent session logs that established adjacent infrastructure:
   - `sessions/archived/231. Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget) — Log.md` — **the closest mirror.** List-shaped record-detail widget; per-arm `Gate::denies` resolver pattern; concept-named projector field map; ambient-record-scoped query; SOURCE_SYSTEM_MODEL retrofit shape; widget definition + Blade template + seeder integration. 5d-3 is structurally near-identical at the resolver and projector layer.
   - `sessions/archived/232. Widget Primitive — Phase 5d-2 (Membership Status — Single-Row Record-Detail Widget) — Log.md` — second per-arm gate evidence point. 5d-2 is single-row (cardinality 'one'); 5d-3 is list-shaped (cardinality 'many'), so 5d-1 is the closer mirror at the contract layer, but 5d-2 is the closer mirror for the per-arm-gate pattern that becomes the Phase 5e decision input at this session's close.
   - `sessions/233. Financial Data Origin & Lifecycle Discipline — Phase A — Log.md` — establishes the `source` column on donations (and the other three financial tables) that 234's projector consumes via the `donation_origin` field per Decision 1. Read the Phase 1 backfill predicates and Phase 6 manual-admin-paths section so you understand which Donation rows now carry which source values.
6. Read the full state of every file this session edits or creates. Substantial because this session lands a new widget end-to-end:
   - **Resolver + projector (extend, don't replace):** `app/WidgetPrimitive/ContractResolver.php`, `app/WidgetPrimitive/Projectors/SystemModelProjector.php`. The `donation` arm is added to `resolveSystemModel()`'s cardinality-many match (sibling to `note`); the projector gains `projectDonation()` and a `'donation' => fn ($d) => $this->projectDonation($d)` entry in `projectorFor()`'s match.
   - **Widget files (new):** `app/Widgets/RecentDonations/RecentDonationsDefinition.php` (handle, label, allowedSlots, acceptedSources, schema/defaults, dataContract); `app/Widgets/RecentDonations/template.blade.php` (server-rendered, no JS/SCSS — mirror of RecentNotes / MembershipStatus).
   - **Registration:** `app/Providers/WidgetServiceProvider.php` (register the new definition).
   - **Seeders:** `database/seeders/WidgetTypeSeeder.php` (verify the auto-discovery path picks up the new widget — same as 5d-1's RecentNotes seeding); `database/seeders/RecordDetailViewSeeder.php` (extend `seedContactOverview()` to attach RecentDonations at sort_order 2 via `attachWidgetIfMissing`).
   - **Source primitive (already in place from 213/233, reference only):** `app/WidgetPrimitive/Source.php`, `app/Models/Donation.php` (now has `source` in fillable plus the original `HasSourcePolicy` trait declaration with `ACCEPTED_SOURCES = [IMPORT, STRIPE_WEBHOOK]`).
7. Read related existing tests to anchor patterns:
   - `tests/Unit/WidgetPrimitive/ContractResolverNoteTest.php` (8 cases — direct mirror at the resolver layer).
   - `tests/Unit/WidgetPrimitive/ContractResolverMembershipTest.php` (8 cases — single-row equivalent; useful for the per-arm-gate test shape).
   - `tests/Unit/WidgetPrimitive/Projectors/SystemModelProjectorNoteTest.php` (projector test pattern for list-shaped contracts).
   - `tests/Unit/Widgets/RecentNotesDefinitionTest.php` (widget definition test pattern).
   - `tests/Feature/RecordDetailViewSeederTest.php` (seeder integration — will need a new case for `recent_donations` attached at sort_order 2).
8. Open `sessions/234. Widget Primitive — Phase 5d-3 (Recent Donations — Third Concrete Record-Detail Widget).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed. **Pause specifically on the four design decisions named in the session prompt's "Design decisions to confirm before starting" section.**

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. **No migrations are expected this session — pure widget addition consuming the existing schema.**
- **Update `docs/schema/`** after writing any migration — N/A this session (no migrations expected).
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session touches no front-end source — RecentDonations is server-rendered Blade with no JS / SCSS / declared assets, mirror of RecentNotes and MembershipStatus. Neither `npm run build` nor `build:public` should be required.**
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands a new resolver arm, a new projector method, a new widget definition + Blade, and a seeder extension — every layer gets a test.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Phase 5e decision artifact.** **Specific to this session.** Compare the three resolver arms side by side (`note`, `membership`, `donation`) at their `Gate::denies` lines and the empty-shape returns. Two outcomes:
  - *Clean centralization possible:* all three arms read as flat single-permission, single-deny patterns. Draft `sessions/235. Widget Primitive — Phase 5e (Permissions in the Contract Layer).md` proposing a `requiredPermission` field on `DataContract`, the resolver enforcement point, the migration plan to lift the three existing inline gates, and the test shape. The 235 base prompt + session prompt are this session's deliverables.
  - *Wrinkle surfaces:* one or more arms have logic the flat declaration can't express (combinatorial, callback-driven, ambient-aware). Document the wrinkle in the 234 log's Deferred Decisions section; defer 5e until the wrinkle's shape is clearer or until enough additional arms accumulate to inform the grammar. Next session 235 becomes whatever else is queued (Phase 5d-4 if pursued, or DashboardConfig→DashboardView retrofit, or one of the deferred carry-forwards).
- **Next session base prompt** (whatever 235 ends up being): copy `sessions/template-base-prompt.md` to `sessions/235. base-prompt.md`.
- **Next session prompt**: draft `sessions/235. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/234. Widget Primitive — Phase 5d-3 (Recent Donations — Third Concrete Record-Detail Widget) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 234's row.
- **Update `sessions/session-outlines.md`** (the roadmap): collapse the Widget Primitive track entry's "Phase 5d-3 queued" wording to reflect 5d-3 as complete. Adjust the active-tracks block accordingly.
- **Update the Widget Primitive track doc** at `sessions/tracks/widget-primitive.md`: bump the status snapshot (last update date, what's complete, what's active). Per the close-gate phase-expiry rule, **if Phase 5d closed at this session** (i.e., no further 5d-N widgets are queued), lift the per-session detail of 5d-1/5d-2/5d-3 out of the snapshot into a compressed Phase 5d retrospective. If 5d-4 (Recent Activity) remains queued, keep 5d open and don't compress yet.
- **Archive the previous session**: move all files matching `sessions/233. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the widget files, resolver/projector edits, seeder updates, the log, the updated outlines, the track doc, the completed-sessions index, the 235 base prompt + session prompt, and any files moved into `sessions/archived/`), commit on the current `session-234/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
