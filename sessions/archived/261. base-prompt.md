# Base Prompt — Session 261

We are about to begin a new session: **261. List Resource Exports — CSV and JSON**.

This session lifts B2b from the working set with a JSON format added on top of the originally-CSV-only spec. Operator-facing **Export CSV** and **Export JSON** actions land on every list resource that holds migration-relevant data. The existing Contacts exporter pattern is the template — it gets a parallel JSON action this session for parity, and the same shape spreads across nine non-Contact list resources.

This session is **not boundary-touching** — Fleet Manager contract stays at v2.1.0.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference; do not infer format from previous logs.
2. Read `sessions/release-plan.md`:
   - All 11 discipline rules.
   - Track B § B2b — note that the working-set entry's success criterion was originally CSV-only; the user-agreed expansion at 260 close folds JSON into the same session and lifts XLSX to a follow-on.
   - Out-of-Gate register — confirm the new XLSX-export stub entry that landed at 260 close.
3. Read `sessions/session-outlines.md` Cross-Repo block — confirm v2.1.0 going in; this session does not change it.
4. Read `sessions/260. First-Deploy Migrate Race Fix — Log.md` — context only; the migrate-race fix is in production as of 260 close.
5. Read [`app/Filament/Resources/ContactResource/Pages/ListContacts.php`](../app/Filament/Resources/ContactResource/Pages/ListContacts.php) end-to-end — the existing `exportContacts` header action is the pattern template every other list resource lifts in this session.
6. Read each importer's field registry under `app/Importers/` (the `*ImportFieldRegistry` pair, not the older single-name `*FieldRegistry`):
   - [`OrganizationImportFieldRegistry.php`](../app/Importers/OrganizationImportFieldRegistry.php)
   - [`DonationImportFieldRegistry.php`](../app/Importers/DonationImportFieldRegistry.php)
   - [`EventImportFieldRegistry.php`](../app/Importers/EventImportFieldRegistry.php)
   - [`MembershipImportFieldRegistry.php`](../app/Importers/MembershipImportFieldRegistry.php)
   - [`InvoiceImportFieldRegistry.php`](../app/Importers/InvoiceImportFieldRegistry.php)
   - [`NoteImportFieldRegistry.php`](../app/Importers/NoteImportFieldRegistry.php)
   The `flatFields()` output of each defines the canonical column shape. Exporter standard columns mirror this so `export → edit → re-import` round-trips predictably and the 259 auto-mapper recognizes headers without effort.
7. Read [`app/Models/CustomFieldDef.php`](../app/Models/CustomFieldDef.php) — the `model_type`-keyed lookup that drives CF column flattening on resources with `custom_fields` jsonb.
8. Read all 10 target list pages (inventory existing ActionGroup state; some already have ellipsis menus, others get one added):
   - [`ContactResource/Pages/ListContacts.php`](../app/Filament/Resources/ContactResource/Pages/ListContacts.php) — already has ActionGroup
   - [`OrganizationResource/Pages/ListOrganizations.php`](../app/Filament/Resources/OrganizationResource/Pages/ListOrganizations.php)
   - [`DonationResource/Pages/ListDonations.php`](../app/Filament/Resources/DonationResource/Pages/ListDonations.php)
   - [`EventResource/Pages/ListEvents.php`](../app/Filament/Resources/EventResource/Pages/ListEvents.php)
   - [`EventResource/Pages/ViewRegistrations.php`](../app/Filament/Resources/EventResource/Pages/ViewRegistrations.php) — note: not a top-level resource; export scoped to one event
   - [`MembershipResource/Pages/ListMemberships.php`](../app/Filament/Resources/MembershipResource/Pages/ListMemberships.php)
   - [`TransactionResource/Pages/ListTransactions.php`](../app/Filament/Resources/TransactionResource/Pages/ListTransactions.php)
   - [`FundResource/Pages/ListFunds.php`](../app/Filament/Resources/FundResource/Pages/ListFunds.php)
   - [`CampaignResource/Pages/ListCampaigns.php`](../app/Filament/Resources/CampaignResource/Pages/ListCampaigns.php)
   - [`NoteResource/Pages/ListNotes.php`](../app/Filament/Resources/NoteResource/Pages/ListNotes.php)
9. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
   - **Custom-field model-key strings.** The 6 CF-having models (Organization / Donation / Event / Membership / EventRegistration / Transaction) need confirmed `CustomFieldDef::forModel('<key>')` keys. Three are establishable today (`contact`, `event` confirmed); the other four need verification — grep for existing `forModel` call sites, and if a key isn't established yet, decide whether to define it this session (likely yes — small lift, supports the export round-trip) or scope down.
   - **Architectural shape.** Per-page inline exporter logic (mirroring `ListContacts` exactly 10x) vs a shared `ListExportService` (or trait) handling CF flattening + format-switching. Lean: shared service — CF nesting in JSON, CF flattening in CSV, and the streamed response are 80% the same logic per resource. Confirm before going deep.
   - **EventRegistration placement.** Confirm the export action lives on `ViewRegistrations.php` and is scoped to the currently-viewed event's registrations only (not a global all-registrations export).
   - **ActionGroup inventory.** Per-page: which already have one, which need one added. Decide consistently — "all 10 pages have an ellipsis ActionGroup containing both export actions" is the target.
   - **Help-doc note.** Decide whether to land a small additive note in [`resources/docs/onboarding-migration.md`](../resources/docs/onboarding-migration.md) § Migration-out about the new per-resource exports as an alternative to the top-level Tools flow. Lean: small, absorb in-session.
10. Open `sessions/261. List Resource Exports — CSV and JSON.md` and read the session prompt carefully.
11. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.1.0** (per session 253). No change expected this session.
- **Fast test suite green** at **2018 passed** (260 close baseline; 260 added no tests). This session adds tests across 10 list resources — estimate **+20 new tests** (one CSV + one JSON per resource).
- **Slow test suite green** at **15 passed**.
- **PHP `memory_limit` is 1G** ([docker/php/local.ini](../docker/php/local.ini)) — fast suite runs at `2G` per the standard rehearsal command.
- **B2b stub canonical** in [`release-plan.md`](release-plan.md) § Track B § B2b. The working-set entry's success criterion was originally CSV-only; the user-agreed expansion at 260 close folds JSON into the same session and lifts XLSX to a follow-on.
- **Existing ListContacts CSV exporter** at [`app/Filament/Resources/ContactResource/Pages/ListContacts.php:177-225`](../app/Filament/Resources/ContactResource/Pages/ListContacts.php#L177) is the canonical pattern. It uses `getFilteredSortedTableQuery()`, streams via `streamDownload`, flattens `CustomFieldDef::forModel('contact')` into one column per CF, and gates on `view_any_contact`.
- **The migrate-race fix (session 260)** is in main as of session 260 close. Production deploys behave correctly under `--wait` + postgres-healthcheck pairing.
- **OpenSpout (XLSX writer)** is already a transitive dependency via `filament/actions v3.3.49`. Available for the future XLSX follow-on; **not used this session.**

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **Single-file bind-mount caution.** This session shouldn't touch `docker/nginx/prod.conf`, `docker/php/local.ini`, or `docker/nginx/default.conf`. If it does for any reason, plan on `docker compose up -d --force-recreate <svc>` rather than `restart`.
- **No CRM-side schema changes.** This session is admin-PHP-only; no migrations.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **Tests required for new behavior.** Each of the 20 new exporter actions (CSV + JSON × 10 resources) gets at least one Pest test. Minimum assertions per test: header / first-row shape correct; CF column(s) flatten where applicable; permission gate enforced (anonymous / wrong-role users get denied); filter+sort state reflected in the export.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline is 2018 passed (260 close). Slow group baseline: 15 passed. **Target after this session: ~2038 passed fast** (+20).
- **Front-end builds — when to run which:** This session is admin-PHP-only. **No `npm run build` or `build:public`.** If anything in `resources/js/**` or `resources/scss/**` changes unexpectedly, flag it before continuing.
- **Cross-repo coordination — Fleet Manager agent contract.** This session is **not** boundary-touching. Standing cross-repo discipline applies as a passive check.
- **Verify objective outcomes yourself; pull the user in for judgment.**
  - **Objective:** each export action emits the right CSV/JSON shape on click against a synthetic fixture; tests pass; `migrate:status` clean (no schema drift); `php artisan help:sync` (if the help-doc additive note lands) reports the doc registered.
  - **Judgment-required:** action label / placement aesthetics, ellipsis-menu shape consistency across resources, whether the additive help-doc note reads cleanly to a non-technical audience.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and verification are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The natural next session after 261 depends on close-time judgment:

- If XLSX demand surfaces post-261 (e.g. operator-test feedback in flight), **B2b' — XLSX format add** is the natural next session, building on the writer abstraction this session lays down.
- Otherwise, **B1b — Affiliations Junction & Soft-Credit Layer** per the release-plan execution order (release-plan position 17, post-B2 follow-up to B1a).

Confirm at 261 close.

### Phase 2 — Close

When the user says to close (after 262's prompt is agreed):

- **Session log**: write `sessions/261. List Resource Exports — CSV and JSON — Log.md` against `template-session-log.md`. Document the writer abstraction shape, the per-resource standard-column lists (or where they're now declared centrally), the CF model-key resolution outcome, and any in-session absorptions.
- **Update `sessions/completed-sessions.md`**: append session 261's row.
- **Update `sessions/release-plan.md`**:
  - Check off B2b (✅) with a "closed at session 261" note. Update its success criterion in place to reflect the CSV+JSON expansion that landed (the original criterion was CSV-only).
  - Remove the XLSX stub from the Out-of-Gate register if XLSX follow-on is being scheduled as B2b' for the next session. Otherwise leave the stub.
- **Update `sessions/session-outlines.md`**: no roadmap change expected unless XLSX follow-on gets formalized at close.
- **Spec doc** — no Fleet Manager contract change.
- **Findings register** — flag any surprises (CF model-keys that needed defining; resources where the importer flatFields() didn't cleanly map to exporter columns).
- **Help-doc update** — if the additive note in `resources/docs/onboarding-migration.md` landed, run `php artisan help:sync` on close to register.
- **Archive the previous session**: move all files matching `sessions/260. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-261/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly. The Contacts exporter at [`ListContacts.php:177-225`](../app/Filament/Resources/ContactResource/Pages/ListContacts.php#L177) is the canonical shape; deviations from it need justification.
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** introduce XLSX in this session (lifted to Out-of-Gate); do **not** widen scope to non-list resources or to additional formats (`.tsv`, `.xml`, `.parquet`); do **not** refactor the existing Contacts CSV exporter beyond what's needed to share code with the new abstraction.
- Prefer editing existing files to creating new ones — except for the new shared `ListExportService` (or trait) the architectural-shape decision warrants.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
