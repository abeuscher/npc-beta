# Base Prompt — Session 308 (Page Builder ↔ Public View — Visual Parity, Content Inside Widgets)

> **Scheduled as session 308.** **Critical-path, Beta-1-blocking polish.** Sits immediately after the now-closed inline-editing arc (304 / 305 / 306 / 307). Forcing function: rich-text editing in the builder is producing visual divergences (double bullets on `<ul><li>` being the named example) that the HTML-layer parity guard from session 307 will not catch — this session is the visual-layer complement to that guard, at the rendered-DOM / computed-style layer.

**Start now.** Execute the reading list below, open the session prompt at `sessions/308. Page Builder ↔ Public View — Visual Parity (Content Inside Widgets).md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **308. Page Builder ↔ Public View — Visual Parity (Content Inside Widgets).**

This is the **visual-layer complement** to the HTML-layer parity guard landed at session 307. The work is one Playwright spec (a standing harness) plus a sweep of whatever it surfaces — fix the CSS-scope and Quill-injected leaks, flag the intentional divergences (drag handle, edit affordance, inline toolbar, caret, zoom transform) and any genuinely unfixable Quill-contenteditable artifact. Position in the execution order: a polish session between the closed inline-editing arc and the next pre-Beta-1 lift; not itself part of any active track.

This session is **NOT boundary-touching** — no Fleet Manager surface. **CRM contract stays at v2.3.0.** **No DB schema change.** If the work appears to need a Fleet / `/api/health` / schema touch, stop and surface — scope drifted.

**Session shape:** audit-then-fix with a durable Playwright artifact. Two distinct deliverables: (a) the standing visual-parity harness (`tests/e2e/page-builder/widget-visual-parity.spec.ts`) exercising per-element computed-style diff between the builder canvas and the public render of the same widget; (b) the sweep of resulting diffs — fix the CSS-scope and Quill-injected leaks in scope, flag the rest. The double-bullet bug on `<ul><li>` is the named root-cause-first deliverable in Phase 1 — its mechanism (one shared cause vs many distinct causes) determines whether Phase 3 is a verification pass or a real audit.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — stable since 276.
2. Read `sessions/session-outlines.md` and `sessions/release-plan.md` — confirm the 308 entry slotted at session-307 close and look for any related stubs (alignment-tool regression, admin-lockout recovery — both housekeeping-inbox items, not 308's scope).
3. Not part of an active track.
4. **Read session 307's close-log** (`sessions/307. … — Log.md`) — what 308 inherits: the HTML-layer parity guard at `tests/Feature/BuilderPublicRenderParityTest.php`, the eligibility-gate widening to 13 widgets, the documented expected-diff set at the HTML layer (`data-config-placeholder` attribute, empty editable wrappers, `EventCalendar` random id).
5. **Read `tests/Feature/BuilderPublicRenderParityTest.php`** in full — 308's harness is the rendered-DOM-layer complement. Same widget set, same builder-vs-public framing, different layer. The HTML guard's documented diffs do not carry computed style; the visual harness's documented diffs are at the styling layer.
6. Read the session 306 close-log (archived) for the Quill seed-fix (`clipboard.convert` + `setContents` + `history.clear`) and the snow-theme chrome strip from session 304 — both are the foreground context for diagnosing the double-bullet bug class. The toolbar spec at `docs/inline-formatting-toolbar-spec.md` is the rule-of-record for what formats Quill emits.
7. Read `docs/app-reference.md` — the single render path (`WidgetRenderer::render()`); the public namespace contract; the page-builder canvas's preview-scope wrapper.
8. Read `resources/scss/_base.scss` + `resources/scss/_buttons.scss` + `resources/scss/_forms.scss` for the `.np-site` / `.widget-preview-scope` namespace rules — the first place to look when the harness surfaces a CSS-scope leak.
9. Confirm the leverage surfaces before touching them:
   - `tests/Feature/BuilderPublicRenderParityTest.php` — HTML layer (Pest, `design` group).
   - `tests/Feature/AssetBundleDriftGuardTest.php` — served-bundle layer (Pest, `design` group; session 296).
   - `tests/e2e/page-builder/widget-visual-parity.spec.ts` — new, this session, rendered-DOM layer (Playwright).
10. Note drift in a brief work-log entry; proceed unless something needs a decision per the drift/decision-threshold rules.

---

## Starting state inherited from session 307

- **CRM contract at v2.3.0** — unchanged.
- **Schema baseline:** none added by 307; no schema change anticipated for 308.
- **Fast Pest 2530/0** at the 307 close (`./dev test`), plus one pre-existing documented parallel-order flake on `RecordDetailViewSeederTest` (passes 5/5 in isolation; called out in 304/305/306/307 logs). Net delta this session: zero expected — no new Pest unless the sweep surfaces a fix that warrants a Pest case.
- **Playwright suite UN-PARKED as of 2026-05-19** (PRs #7 + #8). Runs on every CI push against the isolated stack; intentionally NOT in the `tests` merge-gate `needs` (non-gating by design). The new `widget-visual-parity.spec.ts` joins that suite — runs and reports on every push, non-gating.
- **HTML-layer parity guard active** — `tests/Feature/BuilderPublicRenderParityTest.php` in the `design` Pest group, 16 cases, asserts builder-canvas `WidgetRenderer::render(..., inlineEditing: true)` is appearance-equivalent to public `WidgetRenderer::render(..., inlineEditing: false)` modulo three documented diffs (placeholder attr, empty editable wrappers, EventCalendar random id). The visual harness this session adds is the complementary layer.
- **Inline-eligible widget set at 13 widgets** (per 307's widening pass): TextBlock, Hero, ThreeBuckets, PricingChart, BarChart, BlogListing, BoardMembers, DonationForm, EventCalendar, EventsListing, MapEmbed, ProductCarousel, SocialSharing. Image + Nav remain explicitly excluded (Tier-B couplings). Re-derive the list from `WidgetDefinition::inlineEditable()` at session start in case it shifts.
- **Toolbar shipped at 306** — built to `docs/inline-formatting-toolbar-spec.md`; conformant. The Quill snow-theme chrome was stripped at 304 (WYSIWYG fix); the bullet-list seed-fix landed at 306 (`clipboard.convert` + `setContents` + `history.clear`). Both fixes are the foreground for diagnosing the double-bullet bug class.
- **Pest is the merge-gate; the Playwright suite is the un-parked CI signal** (non-gating). `feedback_test_runs_not_parallel` applies — Pest and Playwright run sequentially locally and (via `playwright` job `needs: slow`) in CI.
- **Build/version discipline:** bump `VERSION` to `0.308.<iteration>` each iteration.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **The visual-parity harness is a standing deliverable, not a check-and-discard.** Once the sweep is done, the harness's exclusion set is locked and the spec runs on every CI push as the visual-layer regression guard. Treat it the way session 307 treated the HTML-layer guard: durable, documented, complementary to the upstream guards.
- **Default-to-fix on the sweep findings** per `feedback_audit_findings_default_to_fix`. Surface every diff inclusively; the user dispositions the "won't-fix" set during the apply walkthrough at session close.
- **The double-bullet bug is the canonical example, not the whole job.** Root-cause it in Phase 1 to choose the Phase 3 shape (verify-shared-cause vs sweep-N-bugs), but don't anchor on it — the harness's value is catching the next regression, not just this one.
- **Subtract the zoom transform; exclude the tooling overlays.** Both are documented expected differences. The harness compares only inside the widget subtree (`[data-widget-id]` or equivalent — confirm the selector at session start against the current builder DOM); overlays sit outside that subtree and are naturally excluded. The zoom is handled by either setting it to 100% for the duration of the spec or by dividing measured dimensions through the read-back transform — whichever is cleaner against the canvas plumbing.
- **Pest fast suite + Playwright run sequentially when complete.** Green, every count delta explained.
- **No mid-session UX pause expected** for the sweep portion. Pause at session end for the user to review the residual fix list + the unfixable / intentional-divergence dispositions.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

308 is **not arc-terminal** — it's a polish session positioned between the closed inline-editing arc and the next pre-Beta-1 lift. Do not auto-draft a successor unless the sweep surfaces a residual fix list the user explicitly wants picked up as its own session; in that case, draft prompts only when the user names it.

### Phase 2 — Close

When the user says to close: write `sessions/308. Page Builder ↔ Public View — Visual Parity — Log.md` per `template-session-log.md` (harness shape + selector contract + exclusion set + how it coordinates with the HTML guard + the served-bundle guard; the double-bullet root cause + fix; the sweep's resolved-vs-flagged-vs-unfixable breakdown; new Playwright; `VERSION` bump; drift; any carry-forward). Update `completed-sessions.md`. **Archive `sessions/307. *` into `sessions/archived/`.** Commit on the final `session-308/N` branch, notify, do not push. Never push to main / force-push / merge to main. **Update `release-plan.md` and `session-outlines.md`** to record the visual-parity harness as the third coverage layer (served-bundle freshness 296, HTML-render parity 307, visual / computed-style parity 308); the entries can be one-line each, pointing at this session's log.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages describe the deliverable; PM-level status tone per `memory/feedback_pm_level_tone.md`; no docstrings/comments/type-annotations on code you did not write. **Extend, don't reinvent** — the visual harness sits alongside the existing parity test (Pest, `design` group) and the served-bundle drift guard (Pest, `design` group), in the un-parked Playwright suite. No new editor, no second render path, no new sanitiser, no new namespace; the fix path for any surfaced CSS leak is either tightening an existing selector (`.np-site` ↔ `.widget-preview-scope`) or extending the existing Quill chrome strip.
