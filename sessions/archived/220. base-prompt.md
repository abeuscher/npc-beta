# Base Prompt — Session 220

We are about to begin a new session: **220. Widget Primitive — Phase 4e (BoardMembers + LogoGarden Retrofit)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session adds no new tables and modifies no existing ones; no `docs/schema/` updates are expected.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise. Pay attention to the "Widgets do not receive raw Eloquent models" section. BoardMembers and LogoGarden are the **last two** widgets routing through the legacy `WidgetRenderer::render()` line 46 loop → `WidgetDataResolver::resolveCustom`. After this session lands, that path has no callers in `app/Widgets/*` and the slot-strip mechanism in `PageBuilderApiController::filterQueryConfig` becomes formally retirable.
   - `sessions/widget-primitive-migration.md` — the migration sketch. Phase 4 ("Retrofit existing widgets onto contracts") is the active phase. Both widgets are `SOURCE_WIDGET_CONTENT_TYPE` retrofits with **per-instance dynamic `fields` lists** (same shape as BarChart in 219) — admins map Collection fields to widget concepts (e.g. `name_field`, `image_field`) and the contract's whitelist is the union of those non-empty mappings.
4. Read the four prior Phase 4 session logs to internalise the audit + retrofit + test-lock + cleanup pattern, especially 219's per-instance-dynamic-fields shape (which 220 repeats with multiple text fields and one image field):
   - `sessions/archived/216. Widget Primitive — Phase 4a (BlogListing Retrofit — Audit, Test-Lock, Cleanup) — Log.md` — original audit/test-lock/cleanup playbook.
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — true retrofit with template rewrite.
   - `sessions/archived/218. Widget Primitive — Phase 4c (Carousel Retrofit — Audit, Test-Lock, Cleanup) — Log.md` — first SWCT lock; established the system rule that contract-locked widgets do not declare `collections()`.
   - `sessions/219. Widget Primitive — Phase 4d (BarChart Retrofit) — Log.md` (or `sessions/archived/` after 220 close moves it) — first **per-instance dynamic-fields** SWCT retrofit; surfaced the seeder-discipline gap (re-run `WidgetTypeSeeder` after removing `collections()`) and retired two slot-strip tests in `PageBuilderApiTest` rather than continuing the swap pattern.
5. Read both widgets as they stand today — both are **true retrofits**, not audits. Both walk the legacy `$collectionData` path; both have multiple Collection-field mappings the admin configures:
   - `app/Widgets/BoardMembers/BoardMembersDefinition.php` — declares `collections(): ['members']`. Schema includes `image_field`, `name_field`, `title_field`, `department_field`, `description_field`, `linkedin_field`, `github_field`, `extra_url_field`, `extra_url_label_field` — eight text-mapped fields plus one image-mapped field, all user-configurable per instance.
   - `app/Widgets/BoardMembers/template.blade.php` — line 2 reads `$collectionData['members'] ?? []`. Line 51 reads `$member['_media'] ?? []`. Lines 53–60 access `$member[$nameField]`, `$member[$titleField]`, etc.
   - `app/Widgets/BoardMembers/DemoSeeder.php` — creates real `team-demo` Collection with seeded items; same pattern as Carousel/BarChart.
   - `app/Widgets/LogoGarden/LogoGardenDefinition.php` — declares `collections(): ['logos']`. Schema includes `image_field` (required) and `name_field` (optional, gated on `show_name`).
   - `app/Widgets/LogoGarden/template.blade.php` — line 2 reads `$collectionData['logos'] ?? []`. Lines 14–22 walk `$logo['_media']` to resolve images. Line 23 reads `$logo[$nameField]`.
   - `app/Widgets/LogoGarden/DemoSeeder.php` — same pattern.
6. Read the contract resolution path both widgets will route through after the retrofit:
   - `app/WidgetPrimitive/ContractResolver.php::resolveWidgetContentType()` — same path Carousel and BarChart use. Looks up Collection by `resourceHandle`, pulls items with `media` eager-loaded, projects through `WidgetContentTypeProjector::project()`.
   - `app/WidgetPrimitive/Projectors/WidgetContentTypeProjector.php::projectRow()` — strips undeclared fields. Per the projector's `if ($imageKeys !== [])` guard ([line 81](app/WidgetPrimitive/Projectors/WidgetContentTypeProjector.php#L81)): `_media` only appears on rows when the contract's content type declares image fields. Both BoardMembers (always has `image_field`) and LogoGarden (always has `image_field` — required) will emit `_media`.
7. Read the legacy data path both widgets use today and that the retrofit retires:
   - `app/Services/WidgetRenderer.php` line 46 — the legacy `foreach ($widgetType->collections ?? [])` loop. **After 220, this loop iterates 0 times for every widget in `app/Widgets/*`.** Confirm via grep that no widget definition outside the retrofit-locked set declares `collections()`.
   - `app/Services/WidgetDataResolver.php::resolveCustom()` — the path BoardMembers and LogoGarden currently route through. After 220, `resolveCustom` should be unreferenced from `WidgetRenderer`. Confirm via grep that no other call sites exist (`ProductCarousel/template.blade.php` calls `WidgetDataResolver::resolveProducts` directly — different method, different concern; not in scope to retire here).
8. Read existing BoardMembers- and LogoGarden-adjacent test coverage:
   - Grep `tests/**/*` for `board_members`, `BoardMembers`, `logo_garden`, `LogoGarden` — list every test that references either widget. Establish baseline.
   - `tests/Feature/AppearanceLayerRegressionTest.php` — covers both widgets' appearance rendering. Tests call `renderOnPage()` without `collection_handle` config, so the post-retrofit unmounted-state path should keep them passing (`assertOk`-only assertions).
   - `tests/Feature/AppearanceConfigRenameTest.php` — has config-schema tests for `board_members` and `logo_garden` (key-rename assertions). Should pass unchanged. Confirm there is no Blade-render test for either widget that passes `collectionData` directly (BarChart's [line 143-166](tests/Feature/AppearanceConfigRenameTest.php#L143-L166) test was the only such case, fixed in 219).
   - `tests/Feature/CarouselContractRetrofitTest.php`, `tests/Feature/BarChartContractRetrofitTest.php` — the shapes to mirror. Two test files this session: `BoardMembersContractRetrofitTest` and `LogoGardenContractRetrofitTest`, each with the standard Test A (fail-closed whitelist + sentinel) + Test B (one-collections + one-collection_items + one-media query pin).
9. Read `resources/docs/widget-types.md` and `resources/docs/widget-development.md` — confirm whether any prose still references either widget's `collections()` slot or the `$collectionData` template path. Edit conservatively if so.
10. **Seeder discipline (carry-forward from 219).** After removing `collections()` from either definition, run `docker compose exec app php artisan db:seed --class=WidgetTypeSeeder --force` to sync `widget_types.collections` for both widgets. Pest's `RefreshDatabase` re-seeds in `beforeEach`, but the dev DB does not — surfacing as a "panel still renders even though I removed `collections()`" bug if skipped (caught and fixed in 219; do not repeat).
11. Open `sessions/220. Widget Primitive — Phase 4e (BoardMembers + LogoGarden Retrofit).md` and read the session prompt carefully.
12. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216–219. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. The admin bundle, the public site CSS, and the page-builder Vue app all compile through Vite, so this one command covers all three surfaces.
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. This rebuilds the public widget asset bundle on the build server and regenerates `public/build/widgets/manifest.json` (which the admin panel and public layout both read).
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/221. base-prompt.md`. Expected next session: either (a) the **"Phase 4 follower" — Query Settings in the contract layer** (now formally landable since 220 retires the last slot-declaring widgets and the slot-strip mechanism becomes retirable); or (b) continued Phase 4 retrofit on a system-model-shaped widget — BlogPager (cheapest; post-shaped, projector already exists), or one of EventCalendar / EventDescription / EventRegistration. Scope to be agreed with the user. The query-settings follower is queued in `session-outlines.md` under "Phase 4 follower — Query settings in the contract layer."
- **Next session prompt**: draft `sessions/221. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/220. Widget Primitive — Phase 4e (BoardMembers + LogoGarden Retrofit) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: under the Widget Primitive track stub, mark Phase 4e complete (BoardMembers and LogoGarden retrofit-locked: contracts added, templates rewired to `$widgetData['items']`, legacy `collections()` removed for both, test-lock pairs landed for each, `WidgetDataResolver::resolveCustom` now unreferenced from `WidgetRenderer`, slot-strip mechanism formally retirable). Note that Phase 4 retrofit continues for the system-model-shaped candidates (BlogPager, EventCalendar/Description/Registration, ProductCarousel) and that the "Phase 4 follower — Query settings in the contract layer" stub is now actionable.
- **Archive the previous session**: move all files matching `sessions/219. *.md` into `sessions/archived/`. This includes the previous session's base prompt, session prompt, and log. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-220/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
