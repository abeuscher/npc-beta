# Base Prompt — Session 266

We are about to begin a new session: **266. Backup notification hardening — FM 020 finding**.

This session lifts working-set entry **A1d'** — the CRM-side companion to a structural fragility surfaced during FM session 020 manual testing on 2026-05-05. With A1d's checkmark already at the top (closed at 263) and A1d' closing here, the Fleet Manager track's currently-active entries are settled; the next session in the execution order is **E10 — Full-Width Architecture Enforcement**.

This session is **NOT boundary-touching** — no Fleet Manager contract change. CRM contract stays at v2.2.0 (per session 263). No FM-side absorption needed; the fix is fully internal to the CRM.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference.
2. Read `sessions/release-plan.md`:
   - Track A § A1d (closed at 263) — what shipped that this session hardens (BackupController + integrity guard + `/api/backup/trigger` envelope).
   - Track A § A1d' — the working-set entry that closes here. Success criterion was drafted in full at 264 close from the FM 020 finding; this session ships against that criterion.
   - Execution-order list — confirm A1d' at position 21 (already inserted at 264 close), next entry is E10 at position 22.
3. Read `sessions/263. Fleet Manager Contract v2.2.0 — Backup Trigger Endpoint — Log.md` end-to-end (or at minimum the integrity-guard / envelope-shape sections) — context for what 263 shipped that A1d' hardens around.
4. Read `sessions/264. Affiliations Junction — Schema, Migration, Admin UI — Log.md` § findings register and any A1d'-relevant lift notes — A1d' was lifted at 264 close from the FM 020 manual-testing finding.
5. Read `sessions/265. Donation Credits — Soft-Credit Layer — Log.md` (the immediate predecessor) — fast-suite baseline to compare against this session's growth target.
6. Read `docs/fleet-manager-agent-contract.md` (or at least its TOC + the `/api/backup/trigger` section) — confirms this session is non-boundary-touching: no contract surface changes, no version bump, no CHANGELOG entry.
7. Read `config/backup.php` — particularly the `notifications` block at lines 195–218 (the six spatie notification classes at lines 197–202, the `'to' => 'your@example.com'` placeholder at line 212, the `env('MAIL_FROM_ADDRESS', ...)` at line 215). This is Phase 1's edit target.
8. Read `app/Providers/AppServiceProvider.php` end-to-end. Particularly the SiteSetting → config bridge block in `boot()` around lines 60–90 (the `try { ... } catch` block). This is Phase 2's edit target.
9. Read `app/Listeners/RecordBackupSuccess.php` — the listener whose throw-suppression is the bug's load-bearing mechanism. Tiny file; one method.
10. Read `app/Http/Controllers/Api/Fleet/BackupController.php` end-to-end — the integrity-guard caller. Pay attention to the `trigger()` method's flow (Artisan::call → readSuccessRecord → integrity cross-check → success/failed envelope). This is the contract surface A1d' hardens *around* without modifying.
11. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
    - **Listener-chain ordering.** The diagnosis depends on spatie's mail-channel notification listener registering *ahead* of `RecordBackupSuccess` in `Illuminate\Events\Dispatcher`'s listener chain for `Spatie\Backup\Events\BackupWasSuccessful`. Verify via `docker compose exec app php artisan event:list` (or grep for the event name) — confirm the actual listener registration order. If `RecordBackupSuccess` actually runs *first*, the throw-kneecap diagnosis is wrong and the dropping-mail-on-success-path fix doesn't fully address the symptom (would still need the SiteSetting bridge but not the channel removal).
    - **Config-tree path verification.** Confirm `config('backup.notifications.mail.from.address')` is actually the path spatie's notification stack reads at send time (vs e.g. some plugin override or a different nesting). The runtime override only works if spatie reads the key we override. Easiest check: read `vendor/spatie/laravel-backup/src/Notifications/Notifiable.php` (or wherever `routeNotificationForMail` resolves) and confirm it calls `config('backup.notifications.mail.to')` / `config('backup.notifications.mail.from.*')`.
    - **SiteSetting key presence.** Confirm the dev-env `site_settings` table actually has rows for `mail_from_address`, `mail_from_name`, `contact_email`. If empty in dev, the bridge falls through to the hardcoded defaults — important for test expectations to match runtime behavior.
    - **Demo-mode ride-along.** Confirm spatie reads its mail driver via Laravel's default mailer (no separately-configured driver) — if so, the existing `if (isDemoMode())` short-circuit at AppServiceProvider line 93 already covers spatie's mail. Quick check: read one of the spatie notification classes (e.g. `vendor/spatie/laravel-backup/src/Notifications/Notifications/BackupHasFailedNotification.php`) and confirm its `via()` returns the `['mail']` channel without a custom mailer override.
12. Open `sessions/266. Backup notification hardening — FM 020 finding.md` and read the session prompt carefully. The prompt is well-structured but came from the FM-side bug report, so the format has two non-template sections worth keeping (Background, Production deployment notes) — they're load-bearing for a fix-shaped session.
13. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.2.0** (per session 263) — unchanged.
- **Fast test suite green** at the **265 close baseline of 2136** (confirm at session start). Target growth this session: **~+4 net new tests** (config-routing assertions + listener-write assertion). Modest session.
- **Slow test suite green** at **15 passed** (assumed unchanged at 265 close).
- **A1d ✅ closed at session 263** — BackupController, integrity guard, `/api/backup/trigger` envelope. This session does not modify any of that surface.
- **B1b ✅ closed at session 265** — donation_credits + soft-credit attribution layer. Unrelated to this session except as the immediate predecessor for baseline comparison.
- **A1d' was lifted at 264 close** from the FM 020 manual-testing finding; success criterion already drafted in `release-plan.md:79–86`. This session ships against that criterion.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **CRM-side schema changes — none.** `SiteSetting` keys (`mail_from_address`, `mail_from_name`, `contact_email`) already exist; no migration. No `docs/schema/` updates needed.
- **No Composer changes.** No new dependencies.
- **No `npm run build` or `build:public`.** Config-only and provider-bridge edits; no frontend touches.
- **No agent contract change.** This is internal to the CRM. The `/api/backup/trigger` envelope shape, the integrity-guard semantics, the `last_backup_at` mtime cross-check — all unchanged. No `docs/fleet-manager-agent-contract.md` edit, no version bump, no CHANGELOG entry, no FM-side coordination.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. Particularly: if any of the four verify-at-start questions in step 11 contradicts the prompt's diagnosis, surface the finding before proceeding — the fix shape may need to change.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **Tests required for new behaviour.** ~+4 new tests across two files: success-class notifications resolve to `[]`, failure-class to `['mail']`, the SiteSetting → spatie config bridge maps the three keys correctly, and `RecordBackupSuccess` writes the timestamp file when invoked.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. **Target after this session: 265-close-baseline (2136) + ~4 new tests.**
- **Verify objective outcomes yourself; pull the user in for judgment.**
  - **Objective:** config keys resolve to expected values post-bridge; success-class notifications block resolves to `[]`; failure-class stays at `['mail']`; tests green. All four are objectively self-checkable via `php artisan config:show backup.notifications` or test assertions.
  - **Operator-verified at deploy:** the actual "did the bug go away" check requires the deployed env (re-trigger from FM 020 against beuscher.net post-deploy). The success criterion is `client.backup.succeeded` audit row instead of `client.backup.failed` integrity-guard envelope. This is the user's manual test post-merge, not yours.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and verification are complete, Claude stops and waits.

The natural next session after 266 is **E10 — Full-Width Architecture Enforcement** per the post-265 execution order (position 22). E10 is a UI-architecture session — distinct shape from the A1d' hardening work. Plan to draft 267's prompt fresh against `sessions/session-outlines.md` § "Full-Width Architecture Enforcement" and `release-plan.md` § E10 at 266 close.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/266. Backup notification hardening — FM 020 finding — Log.md`. Document the success-channel notifications change, the SiteSetting → spatie config bridge, the four verify-at-start findings (especially the listener-chain ordering check), the test surface, and any in-session absorptions.
- **Update `sessions/completed-sessions.md`:** append session 266's row.
- **Update `sessions/release-plan.md`:**
  - **A1d' ✅** flipped in place at line 79 (and the matching execution-order entry at line 466 — position 21). No insertion or renumbering needed; the entry is already correctly positioned.
  - Confirm the success criterion as written matches what shipped; tighten any wording if the implementation ended up shaped differently.
  - Confirm the next entry in the execution order is E10 at position 22.
- **No `docs/schema/` updates** — schema unchanged.
- **No `docs/fleet-manager-agent-contract.md` updates** — non-boundary-touching, contract surface unchanged.
- **Findings register** — flag any surprises (listener-ordering reality vs diagnosis, spatie config-path quirks, demo-mode ride-along edge cases, test-pattern friction around re-invoking `boot()`).
- **Help-doc update** — none expected; this is operator-invisible internal hardening.
- **Archive the previous session:** move all files matching `sessions/265. *.md` into `sessions/archived/`.
- **Commit:** stage all changed files and commit on `session-266/N` branch. Notify the user. Do not push.
- **No FM-side handoff** — non-boundary-touching. The deployed CRM update will surface to FM 020 organically on the next "Trigger backup now" attempt; no FM-side code or doc changes needed.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly. The existing SiteSetting → config override block in `AppServiceProvider::boot()` (lines ~62–87) is the canonical reference for the three new sibling overrides — match its dot-path-key + `?->value ?: 'default'` shape exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** edit the `'to' => 'your@example.com'` literal in `config/backup.php` (the runtime bridge overrides it; editing the file constant adds vendor-diff noise without effect); do **not** introduce a new `app:reload-config` artisan command or similar; do **not** add Slack / Discord channel hardening (out of scope); do **not** touch the BackupController integrity-guard semantics or the `/api/backup/trigger` envelope shape.
- Prefer editing existing files to creating new ones — except for the two new test files (`tests/Feature/Backup/SpatieBackupNotificationConfigTest.php` and `tests/Feature/Backup/RecordBackupSuccessListenerTest.php`).
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
