# Session 171 — Defaults Binding & Sovereign Rendering — Log

**Date:** 2026-04-12
**Branch:** session-171/1

---

## What was built

### Phase 1 — Audit

- Audited `WidgetRenderer`, `WidgetType`, `PageWidget`, `WidgetRegistry`, `WidgetDefinition`, `NavDefinition`, `nav.blade.php`, `App\Livewire\PageBuilder`, `App\Http\Controllers\Admin\PageBuilderApiController`, `InspectorField.vue` + sibling inspector components, `resources/js/page-builder-vue/stores/editor.ts`, `resources/js/page-builder-vue/types.ts`, `DemoDataService`, `WidgetServiceProvider`, and the three existing widget/nav test files.
- Surfaced a scope mismatch: the session-171 prompt named `PageBuilderBlock.php`, `PageBuilderInspector.php`, and `resources/views/livewire/page-builder-inspector.blade.php` — all three were deleted in the Livewire → Vue migration (sessions 146–152). The stale vocabulary traced to `docs/editor-migration-plan.md`.
- Rewrote the session 171 prompt to name the correct files (API controller + Vue inspector) and deleted `docs/editor-migration-plan.md` entirely. All pre-migration references to the plan doc live only in `sessions/archived/`.

### Phase 2 — `WidgetConfigResolver`

- Added `App\Services\WidgetConfigResolver` as a stateless service with public methods `resolve(PageWidget)`, `resolvedDefaults(PageWidget)`, `hasOverride(PageWidget, string)`, `defaultFor(PageWidget, string)`.
- Internally composes `defaults → themeOverrides → instance` in that order. `themeOverrides()` is a private stub returning `[]` (Stage-N extension point).
- `defaults` source: `WidgetDefinition::defaults()` if the widget handle is registered, else `WidgetType::getDefaultConfig()` (coexistence branch for seeder-only widgets).
- Registered as a singleton in `App\Providers\WidgetServiceProvider::register()`.

### Phase 3 — Renderer integration

- `App\Services\WidgetRenderer::render()` now resolves config via `app(WidgetConfigResolver::class)->resolve($pw)` before building `$templateVars`. All downstream logic (configMedia, collection data, richtext inline images) operates on the resolved config.

### Phase 4 — Sparse save + `resolved_defaults` wire contract

- **Sparse creation.** Both `PageBuilderApiController::store()` and `App\Livewire\PageBuilder::createBlock()` now seed `config => []` on new widgets. Previously both wrote `$widgetType->getDefaultConfig()` (fat).
- **Sparse save.** New `PageBuilderApiController::stripDefaults(PageWidget, array)` helper strips keys whose values equal the resolved default (strict `===`). Wired into `update()`, `uploadImage()`, and `removeImage()`.
- **`resolved_defaults` in API payload.** `PageBuilderApiController::formatWidget()` and `formatWidgetWithPreview()` now include a `resolved_defaults` key from `WidgetConfigResolver::resolvedDefaults($pw)`. `App\Livewire\PageBuilder::getBootstrapData()` ships the same key for both root widgets and column children.

### Phase 5 — Nav template simplification

- `resources/views/widgets/nav.blade.php` drops every `?? 'fallback'` chain for keys the resolver now guarantees (20+ defensive null-coalesces removed). Conditional rendering based on semantic values (e.g. `branding_type === 'none'`) is preserved.

### Phase 6 — Inspector UI (Vue)

Initially implemented per-field indicator (blue dot + ↺ reset button per field) as specified in the prompt. User review requested a change: remove the per-field visual indicator entirely, and replace the per-field reset with a single widget-level "reset all settings to defaults" control in the inspector header (between the rename and delete icons). Revised implementation:

- `resources/js/page-builder-vue/components/InspectorField.vue` — `fieldValue` now reads `widget.config[key] ?? widget.resolved_defaults?.[key]`; `field.default` from the schema is no longer consulted at display time. The earlier per-field dot/reset button and its CSS were removed.
- `resources/js/page-builder-vue/stores/editor.ts` — `clearAllOverrides(widgetId)` action replaces the earlier per-field `clearOverride`. Sets the local config to `{}` and queues the same debounced save.
- `resources/js/page-builder-vue/components/InspectorHeader.vue` — new refresh button (Heroicons `arrow-path` outline SVG) between the rename and delete icons. Disabled when `widget.config` is empty (nothing to reset). Clicking opens a confirmation modal.
- `resources/js/page-builder-vue/components/ConfirmResetModal.vue` — new modal following the same pattern as `ConfirmDeleteModal` (teleport + fade transition + overlay-click / escape dismiss). Confirm button uses the primary colour, not red.
- `resources/js/page-builder-vue/types.ts` — `Widget.resolved_defaults: Record<string, any>` added.

Also removed (requested during review): the `.color-picker__trigger--open` rule and its `isOpen` class binding in `resources/js/page-builder-vue/components/primitives/ColorPicker.vue`. The open dropdown is sufficient active-state indication on its own.

### Phase 7 — Documentation

- `docs/widget-system.md` — new "Config resolution" section describing the resolver's composition order, the `resolved_defaults` wire contract, sparse-save rules, the theme-override extension seam, and the invariant that inspector display cannot diverge from rendered output.
- `resources/docs/widget-development.md` — updated the "State and persistence" and "Bootstrap data" discussions to describe the widget-level reset control and the `resolved_defaults` payload field.
- `docs/editor-migration-plan.md` — deleted (completed 2026-04-07, migration done in sessions 146–152, no longer a reference).

---

## Security checklist — verified

- [x] Resolver has no user-input code paths — all composition is from trusted sources (`WidgetDefinition::defaults()`, the empty-stub theme overrides, and already-validated `page_widgets.config`).
- [x] Sparse save does not drop keys the user intentionally set to empty strings that happen to also be the default — confirmed by design: if equal to default, the resolver recomposes it on read, so the DB state and the rendered state agree.
- [x] Widget-level "reset all settings" is authorisation-scoped identically to existing widget edits — it goes through the same `PUT /admin/api/page-builder/widgets/{id}` path which already checks `update_page`.
- [x] No new public routes introduced.
- [x] `resolved_defaults` in the API payload contains only values sourced from the resolver (definition / theme stub) — no echoed user input.

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`. The resolver is a code-only service. Pre-existing fat `page_widgets.config` rows continue rendering correctly via the resolver and will be slimmed naturally the next time a user saves each widget — no backfill required.

---

## Deferred decisions

- Theme-level overrides remain stubbed (`themeOverrides()` returns `[]`). They are wired in a later session as part of the Sovereign Widget arc.
- Retroactive slimming of existing fat `page_widgets.config` rows — intentionally skipped. No-op data migration not required for correctness.
- Non-nav widgets continue sourcing defaults from `WidgetType::getDefaultConfig()` via the resolver's coexistence branch. They move to definition classes opportunistically — en masse in session 172 as part of per-widget file colocation.

---

## Test result

951 passed, 0 failed.
