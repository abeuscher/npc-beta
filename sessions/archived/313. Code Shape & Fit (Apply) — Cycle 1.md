# 313. Code Shape & Fit (Apply) — Cycle 1

Apply the refactors agreed from session 312's audit. This is the **fix half** of the Code Shape & Fit track's first cycle. 312 produced the W-R1 through W-R5 backlogs, the intent-unrecoverable findings section, the Open Refactor Flags, and the (now-populated) Deliberately Kept register. 313 walks them with the user, ratifies pre-resolutions, and applies iteration-sliced.

The 273 session is the closest procedural precedent (cleanup track's apply session). This prompt mirrors its shape — Open Flags walked first, then per-workstream backlog walkthroughs, then iteration-sliced application — adapted to the refactor track's discipline: instance-fix default on class findings, prose diff narration at close-gate (no LOC target), Intent-citation discipline preserved through the iteration commits.

---

## Design decisions (resolved before starting)

- **Every row is a candidate, not a mandate.** The user will pre-resolve the Open Refactor Flags at 312-close. Walk through them at session start to ratify each pre-resolution and confirm iteration ordering. Items not chosen stay on the backlog as deferrals (with a note in the next cycle's pre-loaded findings) or are marked won't-fix in the Deliberately Kept register with citation.
- **Refactors that change public API shape are out of scope.** No DB column meaning changes, no JSON response shape changes, no migration writes. Anything that needs a schema change goes to the next cleanup cycle's squash.
- **Instance-fix default on class findings.** When a class-finding row offers pattern-fix vs instance-fix, the default is instance-fix unless the user explicitly opts into pattern-fix at walkthrough. Pattern fixes on N instances are N times the failure-mode cost; the anti-performative-change discipline is uncomfortable with bundling.
- **Six-iteration ceiling.** Past 6 iterations on a single apply branch is the carve-out smell — at that point, the next iteration's content is the carve-out. Hard heuristic.
- **Tests for behaviour-changing extractions.** Any iteration that introduces a new seam (a service split, a moved boundary) gets test coverage. Pure structural extractions with byte-equivalent semantics rely on the existing suite; the agent notes that explicitly in the commit message.
- **Close-gate diff narration.** At session close, the agent names what was consolidated in prose, citing specific abstractions / files / call sites removed and any new ones added. LOC delta and file count delta are reported as context — not as targets. Goodhart on LOC is a real failure mode here.
- **Iteration strategy:** each refactor move (or tightly-grouped set within the same workstream and risk tier) is its own iteration branch `session-313/1`, `session-313/2`, …. Each ships deploy-testable independently. **Do not bundle across workstreams or across risk tiers.** Bundling several W-R3 single-caller cleanups into one low-risk iteration is fine; bundling a W-R3 cleanup with a W-R5 seam move is not.
- **Intent-citation preserved through commits.** The commit message for each iteration carries the *Intent* citation from the backlog row. This propagates the discipline from the audit log into git history.

---

## Open Refactor Flags from 312

Three flags carried forward from 312's audit. Pre-resolutions are 312's default leanings (per audit log § Open Refactor Flags); ratify or revise at walkthrough start. Items the user shifts off the docket get noted in the Deliberately Kept register (with citation pointing at this session) or in the next cycle's pre-loaded findings.

### Flag W-R1/A — `buildTree()` returns a triple-key superset; each endpoint destructures a different slice

**312 finding:** `PageBuilderApiController::buildTree()` (line 510+) returns `['widgets' => ..., 'items' => ..., 'required_libs' => ...]`. Seven endpoints (`index`, `store`, `update`, `destroy`, `copy`, `storeLayout`, `destroyLayout`) each call `buildTree()` and destructure a different subset. Inside `buildTree()` itself, `WidgetType::requiredForPage()` is computed at line 527 and again at line 605 (sibling private method); `WidgetPreviewRenderer::collectLibs()` is called in two parallel loops (root widgets at line 553, layout-nested widgets at line 567) accumulating into the same `$allLibs` array.

**Risk + scope:** Medium. Touches the page-builder API surface that the Vue editor depends on; the Playwright page-builder spec set will need to confirm green. A clean fix is W-R5-shape (seam move) wearing W-R1 clothing — too big for a single iteration.

**Proposed paths from 312:**
- **(a) Instance fix.** Pick the most-wasteful caller (`reorder` or `destroy`) and introduce a `buildTreeMinimal()` variant. ~20 LOC, 1 file, low risk.
- **(b) Pattern fix.** Lift a `PageTreeBuilder` service exposing `tree()` / `items()` / `requiredLibs()` as separate memoized properties. ~80 LOC across 2 files, medium risk. Better long-term seam.

**Pre-resolution:** (a) instance fix per default leaning. Ratify or revise at walkthrough.

**Lands in iteration:** `session-313/N` (cardinality tier — typically `/3` per the suggested shape).

### Flag W-R2/A — `ImportContactsPage` 1014 LOC standing won't-fix (Flag B successor)

**312 finding:** `ImportContactsPage.php` at 1014 LOC remains the importer cluster's single largest file, 7.9× cluster average. Standing won't-fix since cleanup Cycle 1 (session 206 Flag B), re-affirmed at cleanup Cycle 2 (session 273). The Locality-lens walk in 312 affirms: the page's responsibilities (custom-field collision detection, duplicate-contact matching, contact-specific override of trait hook points) are contact-specific and not extractable to a per-type config object without losing semantic cohesion. Locality holds.

**Risk + scope:** Convergence would be 2–3 iterations, high risk (100+ Pest tests + 4 Playwright specs would need synchronization). 11 sessions of stable namespaced-importer experience since 273 may have lowered the cost — but no forcing function has emerged.

**Proposed paths from 312:**
- **(a) Promote to Deliberately Kept register.** Move the won't-fix into the register with a session-313 citation. The pattern is intentional per the per-type-UX principle (`feedback_uniform_ux_over_per_type_plumbing`).
- **(b) Convergence iteration.** 2–3 apply iterations to translate ImportContactsPage's hook-point overrides into the namespaced pattern.
- **(c) Lightweight converter pattern.** Let ImportContactsPage stay separate but share configuration shape with the namespaced pages.

**Pre-resolution:** (a) promote to Deliberately Kept register per default leaning. Flag B has held for two cleanup cycles; absence of a forcing function + presence of the user's `feedback_uniform_ux_over_per_type_plumbing` preference both point to ratifying the standing call. Ratify or revise at walkthrough.

**Lands in iteration:** none (register edit at session-close, not an iteration).

### Flag W-R4/A — Pattern-fix scale of the source-context finding

**312 finding:** The context-gathering pattern in W-R4-1 (source-name / source-id / session-label / source-display re-derivation across `approve()` and `rollback()`) also appears in three description-generator methods on the same `ImportSessionActions` service: `approveDescription`, `rollbackDescription`, `deleteDescription`. Each re-derives at least `$sourceName` and `$sessionLabel`.

**Risk + scope:** ~35 LOC across one file. Medium risk — the per-type dispatch has subtle differences (contact-only audit notes; per-type description language); careful test coverage required.

**Proposed paths from 312:**
- **(a) Instance fix only.** Ship W-R4-1 as a single low-risk iteration; defer the description-method consolidation. Default per track discipline.
- **(b) Pattern fix.** Consolidate the helper across all five sites in one iteration. ~−35 LOC. Medium risk; requires reviewing all description-method outputs against existing user-facing copy.

**Pre-resolution:** (a) instance fix only per default leaning. User can opt into (b) at walkthrough.

**Lands in iteration:** the instance fix (W-R4-1) lands in `session-313/N` (cause-fix tier — typically `/2`). The pattern-fix (b) only lands if explicitly opted into.

---

## Deliberately Kept register walkthrough (from 312)

312 proposed register additions and retirements without unilateral edits. Walk both lists with the user before the workstream backlogs.

### Proposed additions (from 312 audit log)

One addition proposed; one Open Flag (W-R2/A) defaults to promotion if ratified.

#### `PageBuilderApiController` endpoint shape — per-action persistence, not bulk save

- **Pattern:** A W-R5-looking finding — 22 HTTP endpoints, most per-action CRUD-shape (widgets store/update/destroy/copy/reorder/preview; layouts store/update/destroy; image upload/remove; appearance image upload/remove; updateColorSwatches; data lookups). 1:1 mapping with Vue store actions.
- **Why it's intentional:** Supports optimistic UI, per-action revert / retry, per-action undo, aligns with the rest of Filament's admin patterns. Collapsing to one bulk endpoint per save transaction would be a regression in those dimensions.
- **Citation:** 312 audit log § W-R5; 273 apply log § /6 editor.ts composable extraction (establishes the Pinia store's per-action dispatch pattern); `resources/js/page-builder-vue/api.ts:82-90` documents the owner-scoped baseUrl pattern for per-action calls.
- **Proposed workstream coverage:** W-R5 (Seam quality), W-R1 (Cardinality — the surface pattern that triggers the consideration).

#### `ImportContactsPage` 1014 LOC — promotion candidate via Flag W-R2/A

- See Flag W-R2/A above. Default leaning ratifies the won't-fix as a register entry rather than relitigating Flag B.

For each:
- **Ratify** — pattern lands in the register at session close.
- **Reject** — pattern returns to the next cycle's audit as a candidate finding.
- **Modify** — adjust the pattern description / workstream coverage and ratify.

### Proposed register revisions (existing entries)

#### FieldMapper carve-out — accuracy fix

- **Current entry says:** "Six near-identical per-importer mapper services (`ContactFieldMapper`, `EventFieldMapper`, `DonationFieldMapper`, `MembershipFieldMapper`, `InvoiceFieldMapper`, `NoteFieldMapper`)."
- **Actual inventory:** `app/Services/Import/FieldMapper.php` (the Contacts mapper — uses the unprefixed legacy name, mirroring the `ImportProgressPage` naming-asymmetry) + 5 per-type siblings (`MembershipFieldMapper`, `DonationFieldMapper`, `NoteFieldMapper`, `InvoiceFieldMapper`, `EventFieldMapper`). **No `OrganizationFieldMapper` exists** despite the Organizations importer landing at session 256.
- **Proposed edits:**
  - Update the file inventory to list the actual six file names (with the unprefixed `FieldMapper.php` for contacts).
  - Add a note explaining the Organizations importer omission from the auto-mapping lift — either (a) it was deliberately excluded at 259 because Organizations doesn't have the same alias-richness, in which case state that, or (b) an Organization mapper is a follow-on candidate, in which case lift to the housekeeping inbox. Walkthrough decides which.

### Proposed retirements (from 312 audit log)

None. All three existing register entries (FieldMapper services, Page vs View distinction, inline-eligibility roster) had their citations spot-checked and resolved cleanly — sessions referenced exist, memory entries exist, code referenced exists. The FieldMapper entry has an accuracy fix (above) but is not a retirement candidate.

For each:
- **Retire** — entry removed from the register at session close; the underlying pattern becomes a fair audit target for the next cycle.
- **Affirm** — entry stays; the staleness was apparent but not actual.
- **Refine** — update the entry's justification or workstream coverage so it correctly reflects current intent; keep the carve-out.

Register file edits land at session close, in the same commit as the retrospective updates. The walkthrough decision goes in the apply log.

---

## W-R1 — Cardinality (apply)

One backlog row from 312. Open Flag W-R1/A (separate, above) carries the structural-cardinality concern around `buildTree()`.

| # | Intent (with citation) | Current shape | Deviation | Proposed move |
|---|------------------------|---------------|-----------|---------------|
| W-R1-1 | Populate sibling typography keys (`theme_heading_family`, `theme_body_family`) from the authoritative TypographyResolver source for the inline toolbar's text-style menu — cited from `app/Livewire/PageBuilder.php:400-405` comment block: *"Session 305 §6.3: the inline toolbar's text-style menu renders Paragraph + H1–H6 rows in the theme's actual typography. The TypographyResolver buckets are the authoritative source; fall back to the resolved per-element family, then to the resolver's DEFAULT_FAMILY so the field is always concretely populated."* | Two immediately-invoked closures at `app/Livewire/PageBuilder.php:407` and `:413` each call `\App\Services\TypographyResolver::resolve()` and then extract a different sibling key from the returned array. | The resolver call runs twice within one page-builder bootstrap, returning the same shared result; downstream callers (both closures) extract sibling keys from the same dict. | Hoist a single `$typography = \App\Services\TypographyResolver::resolve()` above the two closures; both keys read from it. ~−6 LOC, 1 file, low risk. |

Walkthrough rule: for each row, the user picks **apply this iteration / defer to next cycle / won't-fix (add to Deliberately Kept register)**. Default is apply unless the user signals otherwise.

---

## W-R2 — Locality (apply)

No inline backlog rows from 312. Both `Import*Page` cohorts hold under the Locality lens. The two remaining W11 outliers (`InteractsWithImportWizard.php` 1192 LOC, `InteractsWithImportProgress.php` 950 LOC) are cohesive template-method traits with pluggable per-type hooks; the smearing-test does not fire.

The `ImportContactsPage` 1014-LOC standing won't-fix is captured as Flag W-R2/A (above) rather than as a backlog row — its default leaning is promotion to the Deliberately Kept register, not an apply iteration.

| # | Intent (with citation) | Current shape | Deviation | Proposed move |
|---|------------------------|---------------|-----------|---------------|
| *(no inline W-R2 findings landed at 312 audit)* | | | |

**Class-finding walkthrough discipline:** for each class-finding row, the user picks pattern-fix or instance-fix at walkthrough. Default is instance-fix; the user must explicitly opt into pattern-fix. Pattern-fix iterations carry an automatic risk-tier-bump in the testing requirements section.

---

## W-R3 — Directness (apply)

No backlog rows from 312. The W-R3 walk scanned ~200+ public methods across `app/Services/`, `app/Http/Controllers/` (especially `Admin/`), and `app/Filament/Concerns/`; no abstractions met the bar of "single-caller AND no plausible second caller AND no semantic weight." See 312 log § W-R3 for the disposition table of single-caller candidates considered and skipped.

| # | Intent (with citation) | Current shape (with expected second caller note where applicable) | Deviation | Proposed move |
|---|------------------------|---|---|---|
| *(no inline W-R3 findings landed at 312 audit)* | | | |

**Single-caller findings have an extra walkthrough step.** Before applying, confirm the agent's expected-second-caller analysis is still accurate (the codebase may have changed between 312 and 313). If a second caller is now coming or pending in another session's work, the finding becomes a "wait" — the abstraction is justified by the imminent caller, not by current usage.

---

## W-R4 — Necessary vs. accidental work (apply)

One backlog row from 312. Open Flag W-R4/A (above) carries the pattern-fix scale of the same finding.

| # | Intent (with citation) | Current shape | Deviation (cites the upstream disagreement, not the symptom) | Proposed move |
|---|------------------------|---------------|---|---|
| W-R4-1 | Approve / rollback an `ImportSession` and apply staged updates with audit-note context — cited from `app/Services/Import/ImportSessionActions.php:20-32` class docstring: *"Performs approve / rollback / delete on ImportSession records with the per-model_type cascade logic. Factored out of ImporterPage so the three Filament action closures can stay thin and the logic can be tested without mounting a Livewire component."* | `approve()` (lines 35-79) and `rollback()` (lines 81-113, Contact branch) each derive `$sourceName`, `$sourceId`, `$sessionLabel`, `$sourceDisplay` from the same `ImportSession` argument before composing the audit-note body. Identical four-statement block, duplicated. | Same context-gathering work happens twice across two methods that already share an `ImportSession` argument and the same downstream audit-note shape. The disagreement is between "service composes context per method" and "service composes context once per session and passes it forward." | Extract `sessionContext(ImportSession $session): array{name: ?string, id: ?string, display: string, label: string}` helper; both `approve()` and `rollback()` consume it. ~−10 LOC net, 1 file, low risk. Existing `ImportSessionActions` Pest tests cover both methods' note-creation paths; byte-equivalent extraction. |

**Walkthrough discipline:** verify each row's *Deviation* points at the upstream cause, not the local symptom. Apply moves that fix the cause; reject moves that just clean up the symptom — those are next-cleanup-cycle W7 / W8 work, not refactor work.

---

## W-R5 — Seam quality (apply)

No backlog rows from 312. Each class pair walked (`ImportSessionActions` + `InteractsWithImportProgress`, `PageBuilderApiController` + Vue store, `HealthController` + `BackupController`) turned out to be a correctly-placed seam. Observer-granularity scan came back consistent (all observers fire on model-level lifecycle hooks). See 312 log § W-R5 for the full per-pair analysis.

| # | Intent (with citation) | Current shape | Deviation | Proposed move | What breaks if we move it |
|---|------------------------|---------------|-----------|---------------|---------------------------|
| *(no inline W-R5 findings landed at 312 audit)* | | | | | |

**Candidate sub-section:** no candidates without breakage analysis were filed at 312 — the `PageBuilderApiController` + Vue store candidate that surfaced briefly during the walk was promoted to a Deliberately Kept register addition (above) rather than left as an unresolved candidate.

**Seam moves are the highest-risk class in this session.** Default iteration slotting: late in the session order, after lower-risk extractions have shipped and the suite has confirmed green from those.

---

## Intent-unrecoverable findings (review at walkthrough; not for apply)

Two findings from 312. Walk through with the user. Possible outcomes per entry:

- **User recalls the intent on the spot** — finding shifts to its proper workstream backlog with citation pointing at this walkthrough.
- **User does not recall** — finding stays intent-unrecoverable, carries forward to the next cycle's W-R5 documentation-gap walk.
- **User declares the code intentionally undocumented** — entry goes into the Deliberately Kept register.

### `app/Http/Controllers/Admin/PageBuilderApiController.php` — `WidgetType::requiredForPage()` re-derivation across `destroy()` + `buildTree()`

- **Surface pattern:** W-R1 (Cardinality). Same call appears at line 156 (inside `destroy()` for permission check) and line 527 (inside `buildTree()` which `destroy()` calls immediately after at line 164). Same value derived twice within a single request lifecycle.
- **What's missing:** No docstring on `WidgetType::requiredForPage()`; no docstring on `PageBuilderApiController::destroy()`; no test names pin the cardinality; no session-log reference for the required-widget feature's intent at this controller seam; no memory entry.
- **Why it matters:** Inlining the second derivation looks like a safe four-line change, but without recoverable Intent the apply session can't confirm whether the second derivation is a slip or whether some not-yet-discovered code path expects fresh evaluation per controller method.
- **Suggested next step:** Walkthrough discussion — does the user recall whether the original design called for fresh derivation per endpoint?

### `app/Http/Controllers/Admin/PageBuilderApiController.php` — `WidgetPreviewRenderer::collectLibs()` parallel loops at lines 553 + 567

- **Surface pattern:** W-R1 (Cardinality). Two foreach loops (root widgets, then layout-nested widgets) each call `app(WidgetPreviewRenderer::class)->collectLibs($widget, $allLibs)` accumulating into the same `$allLibs` array.
- **What's missing:** No docstring at the loop site; no docstring on `WidgetPreviewRenderer::collectLibs()`; no test names pinning the dual-loop accumulation pattern; no session log explaining why the iteration is two passes instead of one over a merged collection.
- **Why it matters:** The straightforward consolidation (one loop over a merged collection) looks safe, but the dual-loop shape may reflect ordering invariants the lib accumulator depends on, or a downstream rendering invariant tied to the order widgets appear in the response items list.
- **Suggested next step:** Walkthrough discussion — does the user recall whether the original design depended on root-before-layout ordering of accumulated libs?

**Do not refactor intent-unrecoverable code this session.** The refactor would itself become an undocumented decision.

---

## Suggested iteration shape

A recommendation — the user reorders, bundles, or splits after the walkthrough. Six-iteration ceiling. The 312 audit produced a small backlog (two inline findings + three Open Flags + two intent-unrecoverable), so the iteration shape collapses significantly from the generic template.

- **`session-313/1` — W-R4 cause-fix + W-R1 cardinality fix bundled (low-risk).** Both inline findings are low-risk byte-equivalent or near-byte-equivalent extractions.
  - W-R4-1: extract `sessionContext()` helper in `ImportSessionActions`; consume from `approve()` and `rollback()`. Byte-equivalent; existing test coverage carries.
  - W-R1-1: hoist `$typography = \App\Services\TypographyResolver::resolve()` above the two closures in `PageBuilder.php`; both closures consume from the hoisted variable. Byte-equivalent for closure semantics; existing typography Pest group carries.
  - Bundle rationale: both are single-file, sub-10-LOC, low-risk extractions in the same risk tier. Iteration discipline ("don't bundle across workstreams or across risk tiers") permits bundling *within* a tier; W-R1 and W-R4 sit in the same low-risk tier when both are byte-equivalent.
  - Fast Pest at close: 2586 baseline; no new tests expected (byte-equivalent moves carry the discipline note in the commit message).
- **`session-313/2` — Flag W-R1/A instance fix (medium-risk).** Default pre-resolution: pick the most-wasteful `buildTree()` caller (likely `reorder` or `destroy`) and introduce `buildTreeMinimal()` variant. ~20 LOC, 1 file. Adds a query-count regression-guard test for the new endpoint (mirroring the `tests/Feature/PageBuilderApiTest.php` pattern from cleanup Cycle 2). **Full fast suite + Playwright page-builder spec set after.**
- **`session-313/3` — Buffer / register edits / close.** Reserved for:
  - Flag W-R2/A → register promotion of `ImportContactsPage` won't-fix (register file edit, not an iteration of code change).
  - Register addition: `PageBuilderApiController` per-action endpoint shape (register file edit).
  - Register revision: FieldMapper carve-out accuracy fix (register file edit; the Organizations branch decided at walkthrough).
  - Intent-unrecoverable walkthrough outcomes (if user recalls intent, lift to backlog; if not, leave for next cycle).
  - The Cycle 1 retrospective in the track doc.
  - The track doc's Status snapshot update (mark Cycle 1 complete; set Next trigger ≈ session 363).

**Pattern-fix opt-ins** (only if user opts in at walkthrough):
- Flag W-R4/A pattern fix: consolidate the three description-generator methods on `ImportSessionActions` to use the shared helper. ~−35 LOC, medium risk. Would land as a fourth iteration `session-313/4` if opted into.
- Flag W-R1/A pattern fix: lift a `PageTreeBuilder` service. ~80 LOC across 2 files, medium risk. Would replace `/2` if opted into.

Bundle or split as the user prefers. Each iteration ships with its own test run and manual-test pause where applicable.

---

## Testing

- **Slow test groups to run this session:** non-optional after `session-313/5` (W-R5 seam moves) regardless of which seams are touched. Additionally, if any other iteration touches importer flows, run the importer slow group in that iteration. Otherwise none by default.
- **New tests expected:** yes, scoped per-iteration:
  - W-R1 iterations: query-count regression-guard tests (one per cardinality fix).
  - W-R2 / W-R5 behaviour-changing extractions: feature tests covering the new seam.
  - Byte-equivalent structural extractions: rely on existing suite; note in commit message.
- **Fast suite target after the session:** existing 274-baseline + sum of new tests per iteration. Log the final delta.
- **Playwright suite:** required after any iteration touching frontend code; required after `/5` regardless.
- **`npm run build`:** required for any iteration touching front-end source.
- **`php artisan build:public`:** required only if a widget asset surface was touched (Laravel artisan command, not an npm script).

---

## Security checklist

Before closing:

- [ ] Permission gates preserved on every endpoint touched
- [ ] Portal `contact_id` scoping preserved on any portal-adjacent refactor
- [ ] Schema-key filtering still enforced on `PageBuilderApiController::update()` after any Vue / API surface touched
- [ ] FM contract version parity test still passes (the cleanup-track artifact at `tests/Feature/Infrastructure/FmContractVersionParityTest.php`)
- [ ] No new untrusted-input → filesystem paths introduced
- [ ] No behavior changes smuggled in alongside structural extractions (the commit messages must distinguish behavior-changing from byte-equivalent moves explicitly)
- [ ] Fast Pest suite passes
- [ ] `npm run build` passes on every iteration that touched front-end source
- [ ] `npm run test:e2e` passes after the iterations requiring it
- [ ] **Close-gate diff narration filed in the log** (see *Closing steps* below)
- [ ] **No findings landed without recoverable Intent citations.** Every applied refactor's commit message carries the citation from the backlog row.

---

## Out of scope

- New features of any kind
- Migration squash (next cleanup cycle's squash session)
- Schema changes
- Refactors not present on the W-R1 through W-R5 backlog or Open Refactor Flags from 312 — add to the next refactor cycle's pre-loaded findings rather than expanding mid-session
- Bug fixes beyond incidental one-line cases surfaced during a refactor
- Cleanup-track work (convention drift, dead code, doc coverage)
- Pattern fixes on class findings unless the user explicitly opts in at walkthrough
- Refactoring intent-unrecoverable code
- Performance optimization as a primary goal — refactors that happen to improve perf as a side effect are fine

---

## Closing steps

Follow the close gate in `sessions/313. base-prompt.md`. Session-specific details:

- **Log file:** `sessions/313. Code Shape & Fit (Apply) — Cycle 1 — Log.md`. Include:
  - Per-iteration summary (what landed, what tests added, deferred items)
  - Final disposition of each Open Refactor Flag (Applied in /N | Carved out | Won't-fix → Deliberately Kept register)
  - W-R1 through W-R5 outcome tables (applied vs deferred vs won't-fix)
  - Intent-unrecoverable walkthrough outcomes
  - **Close-gate diff narration:** prose summary of what was consolidated, citing specific abstractions / files / call sites removed and any new ones added. LOC delta and file count delta as context. The agent's narration is the artifact; raw counts are footnotes.
- **Branch:** `session-313/N` (final iteration).
- **Update the track doc:** `sessions/tracks/code-shape-and-fit.md` § *Cycle Retrospectives* gets the full Cycle 1 retrospective (window covered, sessions list, quantitative outcomes table, load-bearing decisions, blind spots, process incidents, won't-fixes reaffirmed). Mirrors the cleanup track's Cycle 2 retrospective shape.
- **Update the track doc's Status snapshot:** mark Cycle 1 complete; set Next trigger session ≈ 379 (≈ +50 sessions from this close per the cadence rule).
- **Update the Deliberately Kept register at `sessions/tracks/code-shape-and-fit-deliberately-kept.md`:** entries added during the walkthrough (won't-fix items with citation pointing at this session) land in the register file. Stale entries flagged by 312 and ratified-for-retirement at walkthrough get removed with a brief log note.
- **Stub closeout:** `sessions/session-outlines.md` § "Code Shape & Fit — 2-session cycle" — mark the 313 child stub ✅ closed; collapse the cluster's release-plan position to a one-liner pointing at the track doc.
- **Cycle 1 carve-outs:** any carve-out lifted from the apply session per the 6-iteration ceiling gets its dedicated successor session drafted at close (mirroring how 273-close carved out 275 for rich-text sanitization). Insert into `sessions/release-plan.md` at the agreed position.
