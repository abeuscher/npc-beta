# Base Prompt — Session 287

**Start now.** Execute the reading list below, open the session prompt at `sessions/287. Public Marketing Website — About Layout Rebuild.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **287. Public Marketing Website — About Layout Rebuild**.

This session continues the **Public Marketing Website track** (track doc at `sessions/tracks/public-marketing-website.md`). Session 286 closed with the home rebuilt against `sessions/public website/homepage-layout-spec.md` plus a small revision pass — the layout-spec-driven approach worked: the home re-imported cleanly on first pass, the eight bands appeared in spec order, and the cross-section rhythm read as the spec predicted. This session repeats the pattern for the About page using a parallel layout spec authored before the session starts. Sequenced inside the **Public Website Complete milestone**.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** content + visual-build. The deliverables are an `about.json` rebuilt against the About layout spec, a re-import into the live local DB, a screenshot for the user's review, and gap-report appends for anything the rebuild surfaces.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. Read `sessions/release-plan.md` for the session's position inside Public Website Complete.
4. Read `sessions/tracks/public-marketing-website.md` for the track's status snapshot, gap-resolution discipline, and forward plan. Note the 286-close retrospective.
5. Read **`sessions/public website/about-layout-spec.md`** — canonical for cell-by-cell structure, image placements, padding, heading sizes, and the cross-section rhythm. The spec's authority section overrides `brief.md` and `copy.md` layout interpretations for the About page. *(If the spec is not present at session start, stop and ask the user — the layout-spec pre-pass is a precondition.)*
6. Read `sessions/public website/copy.md` for any About copy carried there. Read `sessions/public website/brief.md` for the system conventions and gap-surfacing protocol that remain in force.
7. Read `sessions/public website/audit-summary.md` for the system inventory and `sessions/public website/gap-report.md` for the current state of open gaps. Note that G15 / G16 / G17 / G18 surfaced at 286 are open — the About spec may exercise them again. G1 / G2 / G3 (interim) / G13 / G14 stay ✅ resolved.
8. Read the four reference images and the `references.md` index — structural inspiration only, not surface decoration.
9. Read `sessions/public website/existing-about-page-export.json` for the current About page's structure and the format-1.1.0 JSON shape. *(If the export is not present at session start, stop and ask the user to produce it from admin → CMS → Pages → Export.)*
10. Read `sessions/public website/home.json` and `sessions/public website/homepage-layout-spec.md` as the worked example of the spec-driven approach — the 286 deliverable is the precedent for how a spec maps to JSON.
11. Read `docs/app-reference.md` for the page-builder component map and `docs/schema/README.md` for the table index. No schema changes are expected this session.
12. Open `sessions/287. Public Marketing Website — About Layout Rebuild.md` and read the session prompt carefully.
13. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

---

## Starting state inherited from 286

- **CRM contract at v2.3.0** — unchanged. Session 286 was content-only; no boundary surface touched.
- **Schema baseline:** no schema changes since 280-close.
- **Fast test suite at 2407 / 1 sequential.** The 1 failure (`DashboardBuilderApiControllerTest > it rejects widg…`) pre-dates this session — not caused by 286, surfaced as drift during 286's regression check. No new tests expected for 287 — content-only work. Regression-check run after the about re-import suffices.
- **Playwright suite** — baseline preserved. No Playwright in this session (page-capture harness lands at PMW4 / track close).
- **Planning state:** Public Marketing Website track is active; PMW1 closed at 284; CMS fixes closed at 285; home rebuild closed at 286 with the layout-spec approach validated. About rebuild lands here using the same pattern. After About, expected sequence is Pricing (288), Contact (289), Demo (290), Page-capture harness + track close (291) — each its own session, one page per session, following the spec-then-execute pattern.
- **CMS surface area carried forward (resolved at 285, exercised at 286):**
  - Layout-level `appearance_config` round-trips through export/import (G1).
  - TextBlock carries `ctas` + `cta_alignment` (G2). The 286 home used this on bands 4 / 5 / 6 / 8.
  - `secondary-dark` button variant for dark/gradient backgrounds (G3 interim).
  - Typography defaults tuned (G13 / G14). H1 at 3.5rem, heading margin-bottoms calibrated.
  - Image widget carries `aspect_ratio` + `max_width` (from 285's audit-driven addition).
- **New gap-report rows from 286** (open):
  - **G15** — Hero widget renders its own `.site-container` inside a layout cell (nested containers). If the About spec places a Hero inside a layout cell, the same pattern will surface; flag as continued G15, do not rebuild.
  - **G16** — Hero `fullscreen: true` inside a layout cell stretches the cell to 100vh. Same: flag continued usage, do not resolve.
  - **G17** — TextBlock requires placeholder content to render as a CTA-only band. Same workaround (`<p><br></p>` placeholder) applies if the About spec calls for CTA-only Text widgets.
  - **G18** — Image widget cell-placement controls deferred. Apply same full-cell-fill default unless the spec specifies otherwise.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — content sessions don't absorb housekeeping items unless a finding directly ties to about-page rendering. Touch only if a finding surfaces inside this session's scope; otherwise carry forward.

---

## Process rules for every session

- **Before writing any code or JSON**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. *Session-287 note: no integrations in scope. The About page links to the two in-product demo LPs (`/my-nonprofit`, `/my-nonprofit-workshop`) per the brief — confirm those slugs resolve before serializing the JSON; if not, log as a gap and use the brief's named slugs as the link targets.*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** Standard rule applies. The most likely 287-specific candidates: image placeholder picks across the About spec's image cells (set the precedent for the remaining pages); hero-band carrier choice (Text widget + CTAs is now the default per 286's revision pass — only deviate if the spec explicitly calls for Hero); G15 / G16 manifestations (decide whether to repeat 286's pattern or lift to a follow-on session before proceeding).
- **Adapt to drift; don't ask about it.** The layout spec is recent and the home is the worked precedent. Drift expected at code-level fine detail (helper names, registry rows, specific test paths). Default to silent adaptation + a one-line work-log note. Flag only when a drift changes intent.
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that propagate across bands or set a precedent for Pricing / Contact / Demo. Local edits to a single band — just decide and note.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them. *Session-287 note: no migrations expected.*
- **Update `docs/schema/`** after writing any migration. (No-op this session.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 286 close baseline (2407 / 1 — the pre-existing dashboard-builder failure). No new tests expected; this is a regression check.
- **Front-end builds.** No SCSS / JS expected this session. The rebuild is content-only (JSON + re-import + small DB tweaks if the spec touches template colors per 286's pattern). Don't run builds if not needed.
- **Tests for new behaviour:** no new tests expected. Content-only sessions skip the tests-for-new-behaviour rule per the base-prompt convention.
- **Fast vs slow classification:** N/A.
- **Playwright artifact hygiene:** N/A this session.
- **Cross-repo coordination — Fleet Manager.** N/A.
- **Verify objective outcomes yourself; pull the user in for judgment.** Objective surfaces: the JSON imports without errors; the page renders; the bands appear in spec order; the cross-section rhythm matches the spec's table. Close those loops via re-import + curl + Playwright screenshot. The visual surface — whether the rebuild reads like the references — is the user-judgment surface. Surface for review at session-end.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that About is rebuilt against the layout spec, re-imported, and ready for visual review at `/about`. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/288. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to the next phase of the Public Marketing Website track. Most natural next phase: **Public Marketing Website — Pricing Layout Rebuild**, following the same spec-then-execute pattern. The track's "equivalent layout spec will be written for About, Pricing, Contact, and Demo" expectation continues — confirm with the user whether Pricing's layout spec will be authored before 288 starts.
- **Next session prompt** — draft against the layout-spec-approach pattern that 286 and 287 validate.

**Pre-next-session ask in the close handoff.** If the next phase is Pricing, surface a reminder: *"Before that session starts, please export the current Pricing page from the admin and place it in `sessions/public website/` as `existing-pricing-page-export.json`. If you'd like to do an outside-agent pre-pass on the Pricing layout spec, place that as `sessions/public website/pricing-layout-spec.md` before session start."*

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/287. Public Marketing Website — About Layout Rebuild — Log.md` using `template-session-log.md` as the format reference. Document: per-band decisions (image placeholder picks, alignment calls, fallback paths taken), gap-report appends, manual-testing observations from the user's review, validation that the layout-spec approach continued to deliver predictable results.
- **Update `sessions/public website/gap-report.md`:** append any new rows surfaced during the rebuild. Note continued G15 / G16 / G17 / G18 manifestations under their existing rows.
- **Update `sessions/completed-sessions.md`:** append session 287's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status to reflect 287 closure and the next phase queued.
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (last update date; about rebuild complete; next phase queued).
- **Update `sessions/release-plan.md`:** mark session 287's entry ✅ closed at session 287.
- **Archive the previous session:** move all `sessions/286. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-287/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — About rebuilt against layout spec + screenshot + gap-report appends".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **The rebuild is operator-readable.** Image placeholder labels in widget labels or custom fields are how the eventual photo swap stays mechanical; honor the spec's image-naming convention.
- **The 286 revision-pass approach is the default for Band 1 hero composition.** Unless the About spec explicitly calls for the Hero widget, use a Text widget + CTAs in the hero band's text cell. This side-steps G15 / G16 and produces left-aligned CTAs natively (Text widget's `cta_alignment` follows Quill's `ql-align-*` classes without the G11 substring bug).
