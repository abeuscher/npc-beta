# 082. Codebase Audit & Migration Squash

Follows the pattern established in sessions 042, 049, and 062. Full audit of fields, schema, permissions, and help coverage against the current state of the codebase, covering everything added or modified in sessions 063–081. Includes a migration squash following the same procedure as session 062.

---

## Before writing any code

Read the following files:

- Every file in `app/Filament/Resources/` — all resource edit forms
- Every file in `app/Filament/Pages/` — all custom admin pages
- Every file in `app/Models/` — all models, their `$fillable`, `$casts`, and soft delete status
- Every migration in `database/migrations/` — sort by date, focus on anything added since session 062
- Every file in `resources/docs/` — all help articles and their registered routes
- `docs/schema.md` — the running schema document

Take notes as you read. Cross-reference against the session 062 log to avoid re-auditing what was already resolved.

---

## Workstream 1 — Edit page field audit

Same methodology as sessions 042, 049, and 062. For every Filament resource and admin page that has an edit form, compare the fields shown against the database columns and the model's `$fillable`.

Focus on what is new since session 062:

- **Household** (session 071) — `household_id` on `contacts`. Confirm it is in `$fillable`, has a correct nullable FK, and is exposed correctly in the contact edit form.
- **Activity Log** (session 072) — `activity_logs` table. Confirm model `$fillable` and `$casts` (particularly `properties` JSON column and any datetime fields).
- **Stripe / Finance models** (sessions 073–076) — `transactions`, `donations`, `donation_receipts`, `funds`, `products`, `product_prices`, `purchases`. For each: confirm `$fillable`, `$casts` (amounts as decimal, dates as datetime, booleans). Confirm `contact_id` denormalisation on `transactions` (added session 078).
- **InvitationToken** (session 070) — confirm model `$fillable`, `$casts`, and that expired tokens are handled (no orphan rows).
- **PortalFormCollision** and any other portal-related models added in sessions 063–068 — confirm `$fillable` and `$casts` are complete.
- **SampleLibrary / debug generator** (sessions 064, 079) — confirm these are gated behind a non-production guard and do not expose data manipulation routes in production.
- **EmailTemplate** — `custom_template_path` and branding fields added across sessions 034–080. Confirm all columns are in `$fillable`.

For each discrepancy: deliberate omission (document with a comment) or gap to fix.

---

## Workstream 2 — DB schema doc accuracy

`docs/schema.md` is the running schema reference. Verify it is accurate for all tables added or modified since session 062:

- `contacts` — `household_id` FK (session 071)
- `activity_logs` — full table (session 072)
- `transactions` — full table including `contact_id` denorm (sessions 073, 078)
- `donations` — full table (session 075)
- `donation_receipts` — full table (session 076)
- `funds` — full table including `restriction_type` (session 076)
- `products` and `product_prices` — full tables (session 074)
- `purchases` — full table (session 074)
- `invitation_tokens` — full table (session 070)
- Any other tables added or modified in sessions 063–081

For any table that is missing or out of date, update `docs/schema.md` to match.

---

## Workstream 3 — Permission gate audit

Repeat the gate audit from session 062 for everything added since then. Produce the same table format:

| Resource / Page | Gate method | Permission checked |
|---|---|---|

Specifically check:

- `TransactionResource` — `canViewAny`, `canEdit`, `canDelete` (Stripe-gated; session 073 + 081)
- `DonationResource` — `canViewAny`, `canCreate`, `canEdit`, `canDelete` (session 075)
- `DonorsPage` (Giving Summary) — `canAccess()` (session 075 + 081)
- `ProductResource` — `canViewAny` and create/edit/delete gates (session 074)
- `FundResource` — `canViewAny` and mutation gates (session 076)
- `FinanceSettingsPage` — `canAccess()` with `manage_financial_settings` (session 073)
- Any admin pages or resources added in sessions 063–081 that may have been built without a gate

If any permission is missing from `PermissionSeeder`, add it and document the fix.

---

## Workstream 4 — Soft deletes consistency

Produce the same model table as session 062, updated to include new models from sessions 063–081. Specifically check:

- `Donation` — financial record; should probably not be soft-deleteable (or hard-guarded). Document the decision.
- `Transaction` — same reasoning; Stripe-sourced transactions are immutable. Document.
- `DonationReceipt` — audit trail record; confirm deletion is blocked or guarded.
- `Fund` — should block deletion if donations reference it (agreed session 081 — confirm implementation status).
- `Product` / `ProductPrice` — block deletion if purchases reference it (agreed session 081 — confirm implementation status).
- `InvitationToken` — confirm expired tokens are cleaned up without soft-delete complexity.

---

## Workstream 5 — Fillable vs. casts consistency

Scan all models added or modified since session 062. Every column in `$fillable` that is a date, boolean, decimal, or JSON/array type must appear in `$casts`.

Focus on:

- `Donation` — `amount` decimal, `started_at` / `ended_at` datetime, `type` and `status` strings
- `Transaction` — `amount` decimal, `occurred_at` datetime, `direction` and `status` strings
- `DonationReceipt` — `total_amount` decimal, `sent_at` datetime, `breakdown` JSON array
- `Fund` — `restriction_type` string/enum
- `Purchase` — `amount` decimal, relevant datetimes
- `ActivityLog` — `properties` JSON, `created_at` datetime

---

## Workstream 6 — Foreign key index audit

Review all migrations added since session 062. For every `foreignId` / `foreignUuid` / `foreign()` call, confirm there is an explicit index on the column (PostgreSQL does not auto-index FK columns).

Pay particular attention to:

- `transactions.contact_id` (denormalised in session 078 — may have been added without an index)
- `donations.contact_id`, `donations.fund_id`
- `donation_receipts.contact_id`
- `purchases.contact_id`, `purchases.product_id`
- `contacts.household_id`

Write a migration for any missing indexes.

---

## Workstream 7 — Orphaned documents and help coverage

**Orphaned documents**: check every `routes:` entry in `resources/docs/*.md` against the current route list (`php artisan route:list`). Flag and fix any route name that no longer exists.

**Missing help articles**: verify that any admin page or resource added since session 062 has a help article (or a documented stub decision). New surfaces to check:

- `TransactionResource` — Finance › Transactions
- `DonationResource` — Finance › Donations
- `DonorsPage` (Giving Summary) — Finance › Giving Summary
- `ProductResource` — Finance › Products
- `FundResource` — Finance › Funds
- `FinanceSettingsPage` — Settings › Finance
- `MailSettingsPage` — Settings › Mail (restructured session 081 — confirm help article is still accurate)
- Contact Notes sub-page (session 067)
- Admin User Invitations flow (session 070)
- Household field on contact edit form (session 071)
- Activity Log (session 072 — viewer not yet built, but settings/behaviour worth documenting)
- System Email Preview Wizard (session 080)

Create stubs for any admin surface that lacks one.

---

## Workstream 8 — Factory coverage

List every Eloquent model in `app/Models/`. Note whether a factory exists. Models added since session 062 that may lack factories:

- `InvitationToken`
- `ActivityLog`
- `Fund`
- `DonationReceipt`

Factories for `Donation`, `Product`, `ProductPrice`, `Purchase`, and `Transaction` were added in sessions 075 and 079 — confirm they are complete and cover all required fields.

Create minimal factories for any model that lacks one and could reasonably appear in tests.

---

## Workstream 9 — Migration squash

Same procedure as session 062.

1. Run `php artisan schema:dump` inside the Docker container. This writes a `database/schema/pgsql-schema.sql` file that replaces all existing migrations with a single authoritative schema snapshot.

2. Delete all migration files in `database/migrations/` that predate the squash dump. Confirm the dump file alone is sufficient to restore the schema from scratch.

3. Run `php artisan migrate:fresh --seed` inside the Docker container and confirm it completes cleanly with no errors or warnings.

4. SSH into the production dev server and repeat `migrate:fresh --seed` there. Confirm the same clean result.

5. Update the squash date note at the top of `docs/schema.md`.

**Important**: take a database backup before running `migrate:fresh` on production. Coordinate with the user on timing.

---

## Workstream 10 — Route list audit

Run `php artisan route:list` and review the output for:

- Any Finance routes (Stripe checkout, webhook receiver, donation widget POST) that may conflict with portal or CMS routes.
- Portal routes — confirm they still resolve correctly following any prefix changes since session 062.
- Debug/generator routes (sessions 064, 079) — confirm they are absent in production (`APP_ENV=production`).
- The admin invitation routes registered in `AdminPanelProvider` — confirm they are correctly named and not duplicated.

---

## Out of scope

- New features of any kind
- Refactoring code that works correctly
- Updating help article body copy (stubs are sufficient)
- Archive/expiry pattern for Product, Fund, Membership Tier, Form (separate session scheduled — see Content Type Deletability Audit stub)
- Content type deletability enforcement (separate session scheduled)
- Mailing list targeting engine (separate session scheduled)

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file**: `sessions/082. Codebase Audit & Migration Squash — Log.md`
- **Branch**: `session-082`
