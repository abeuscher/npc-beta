# Base Prompt — Session 307 (Page Builder Inline Editing — Session D: Arc Close — Builder↔Public Parity + Gate Widening)

> **Scheduled as session 307.** **Critical-path, Beta-1-blocking.** **Session D of the four-session inline-editing arc** (A=304 shipped / B=305 partial / C=306 / D=307). **Session C (`sessions/306. *`) is a hard prerequisite** — D cannot run until the toolbar has shipped to the design contract, the §A4 active-editor handle and §A14 MutationObserver are in place, and the §4.15 mockup-state walkthrough has the user's sign-off. Recorded in the roadmap: `sessions/release-plan.md` (the inline-editing arc at the retired § E8 slot, position 38) + the `session-outlines.md` "Page Builder Inline Editing" arc stub.

**Start now.** Execute the reading list below, open the session prompt at `sessions/307. Page Builder — Inline-Editing Arc Close (Builder↔Public Parity + Gate Widening).md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **307. Page Builder — Inline-Editing Arc Close (Builder↔Public Parity + Gate Widening).**

This session lands the two deliverables originally on session 305 that 305 didn't run: the **standing automated builder-preview vs public-render appearance parity tests**, and **widening the code-declared inline-eligibility gate** to the remaining safe display-prose nodes. It is arc-closing: at session-end, the inline-editing arc — A=304 / B=305 / C=306 / D=307 — is fully retired from the active execution order.

This session is **NOT boundary-touching** — no Fleet Manager surface. **CRM contract stays at v2.3.0.** **No DB schema change.** If the work appears to need a Fleet/`/api/health`/schema touch, stop and surface — scope drifted.

**Session shape:** infrastructure + a small annotation pass. Two distinct deliverables: (a) a standing parity-test harness asserting builder-preview render ≡ public-render appearance across a representative widget set; (b) widening the per-node inline-eligibility annotations on the remaining safe widgets, with the Tier-B exempt-set guard held green. Neither deliverable has a hard UX-judgment surface. No mid-session pause expected.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — stable since 276.
2. Read `sessions/session-outlines.md` — the **"Page Builder Inline Editing" arc stub** (final entry in the four-session arc).
3. Read `sessions/release-plan.md` — the inline-editing arc at the **retired § E8** slot (position 38).
4. Not part of an active track.
5. **Read session C's log** (`sessions/306. Page Builder — Inline Formatting Toolbar Rebuild — Log.md`) first — authoritative record of what 307 inherits: the toolbar shipped to spec, the §A4 handle + §A14 MutationObserver in `useInlineEdit`, the §6.1 ColorPicker keyboard nav, the cleaned public-vs-builder render conflation (already split at 305), and the eligible widget set as of 306 close.
6. **Read sessions 304 + 305 logs (archived)** — the inline-editability gate's design, the Tier-B exempt-set guard, the 40-widget safety pass + the canonical exempt list, the public-render conflation fix at 305, the spec at `docs/inline-formatting-toolbar-spec.md`. The widening pass is opt-in annotation, never schema-derived — re-confirm that contract before annotating.
7. Read `docs/app-reference.md` — the single render path (`WidgetRenderer::render()`); the parity test exercises both branches (`inlineEditing=true` vs `false`) of that one function.
8. Read `sessions/archived/296. *` and `tests/Feature/AssetBundleDriftGuardTest.php` — the stale-stylesheet drift guard at the served-bundle layer. The parity tests added here are **complementary** (render-layer equivalence, request-time inline composition); coordinate by cross-reference, do not duplicate. Both stay in the `design` Pest group; update the pinned list in `DesignGroupIntegrityTest.php` in the same reviewed pass.
9. Read the leverage surfaces before touching them:
   - `app/Services/WidgetRenderer.php` — the single render path; `inlineEditing=true` for the builder preview, `false` for public (split at 305).
   - `app/Widgets/*Definition.php` (the inline-eligible set as of 306 close: TextBlock, Hero, ThreeBuckets, PricingChart) and the candidate widening set (e.g., DonationForm heading, EventsListing static heading, anything Tier-A-clear from the 40-widget pass).
   - The inline-prose partial `resources/views/widget-shared/inline-prose.blade.php` and its current annotation contract.
   - The Tier-B guard test (added in session 304) — re-run it after every widening edit.
10. Note drift in a brief work-log entry; proceed unless something needs a decision per the drift/decision-threshold rules.

---

## Starting state inherited from session 306

- **CRM contract at v2.3.0** — unchanged.
- **Schema baseline:** none added by 307.
- **Toolbar:** built to `docs/inline-formatting-toolbar-spec.md`; conformant per §5 / §4.15 walkthrough verified by the user at 306 close. The toolbar is operational on every inline-eligible widget.
- **Public-vs-builder render conflation:** split at 305 (`WidgetRenderer::render()` has explicit `bool $inlineEditing = false`; only `WidgetPreviewRenderer` passes true). Public output is clean of editing markers and empty editable wrappers; visible content unchanged. **The parity test added here is crisper as a result** — fewer expected-differences to ignore between builder canvas and public.
- **Active-editor handle (§A4) + MutationObserver (§A14):** in place from 306; the corner-ghost bug is structurally impossible.
- **HtmlSanitizer:** permits `target`/`rel` per spec §6.2; link "Open in new tab" persists correctly.
- **`refreshPreview()` centralised suppression:** in place; every refresh path automatically honours the active-widget guard.
- **Currently inline-eligible widget set:** TextBlock (full), Hero (full), ThreeBuckets (heading + body across 3 buckets), PricingChart (eyebrow_label + heading + subheading + footnote + nested per-column eyebrow/title/price/lead_content/attribute_rows).
- **Tier-B exempt-set guard:** Pest assertion live since 304; must stay green across every widening edit.
- **Pest is the gate; the e2e suite is PARKED.** Fast suite green at 306's close. The `playwright` CI job remains parked. Gate on Pest. `feedback_test_runs_not_parallel` applies.
- **Build/version discipline:** bump `VERSION` to `0.307.<iteration>` each iteration.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **The parity-test harness is a standing deliverable, not a check-and-discard.** It lives in the `design` Pest group, alongside the stale-stylesheet drift guard from session 296. Update `DesignGroupIntegrityTest.php`'s pinned list in the same reviewed pass.
- **Widening is opt-in annotation only.** Never derive eligibility from schema `type`; never breach the Tier-B exempt set. Each widget added: annotate only genuine display-prose nodes; re-run the Tier-B guard + the parity assertion for that widget.
- **No new judgment surfaces.** The parity assertion is objective (HTML / composed-style equivalence with documented expected diffs); the widening list is the safe-set residue from the 40-widget pass + any new display-prose nodes the inline-eligible widgets gained since 304. Confirm with the user at session start whether the widening list is the canonical "Tier-A-clear residue" or whether a fresh per-widget review is wanted.
- **Pest fast suite + Playwright run sequentially when complete.** Green, every count delta explained.
- **No mid-session UX pause expected.** Pause only at session end for the user to review the widening list + parity coverage.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

307 is **arc-terminal**. Do not auto-draft a successor. If widening did not cover every safe candidate (a real possibility if the 40-widget pass surfaced edge cases), the remainder is a carry-forward note, not an auto-drafted session — the arc closes regardless.

### Phase 2 — Close

When the user says to close: write `sessions/307. Page Builder — Inline-Editing Arc Close — Log.md` per `template-session-log.md` (parity harness shape + what it asserts + how it coordinates with the drift guard; the widened widget set; new Pest/Playwright; `VERSION` bump; drift; any carry-forward). Update `completed-sessions.md`. **Archive sessions/306. \* into sessions/archived/.** Commit on the final `session-307/N` branch, notify, do not push. Never push to main / force-push / merge to main. **Update `release-plan.md`'s § E8 entry to mark the entire inline-editing arc COMPLETE / RETIRED** (or whatever the canonical close phrasing is for an arc-terminal session). **Update `session-outlines.md`'s "Page Builder Inline Editing" arc stub similarly** — the arc is done; the entry can be moved to an archive-style summary line or kept for reference, but it's no longer active.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages describe the deliverable; PM-level status tone per `memory/feedback_pm_level_tone.md`; no docstrings/comments/type-annotations on code you did not write. **Extend, don't reinvent** — the parity harness leverages the existing single `WidgetRenderer::render()` (now cleanly split between public and builder); the widening extends the existing code-declared annotation contract. No new editor, no second render path, no new sanitiser.
