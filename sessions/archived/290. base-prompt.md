# Base Prompt — Session 290

**Start now.** Execute the reading list below, open the session prompt at `sessions/290. Public Marketing Website — Contact Layout Rebuild.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **290. Public Marketing Website — Contact Layout Rebuild**.

This session continues the **Public Marketing Website track** (track doc at `sessions/tracks/public-marketing-website.md`). Session 289 closed with `pricing.json` shipped end-to-end against the pricing-page spec — five bands rendered, Band 2 carried by the new `pricing_chart` widget, zero importer warnings, screenshot reviewed and approved. This session rebuilds the Contact page using the same layout-spec-driven pattern that 286 / 287 / 289 all validated. Sequenced inside the **Public Website Complete milestone** at execution-order position 36 (one slot after Pricing).

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** content + visual-build (matches 286 / 287 / 289). The deliverables are a `contact.json` rebuilt against the Contact page spec, a re-import into the live local DB, a screenshot for the user's review, and gap-report appends for anything the rebuild surfaces. Contact is **greenfield** — there is no prior Contact page in the system to preserve. Per `copy.md`, the page is deliberately simple — likely 2–3 bands (hero / email / optional "what I respond to fastest"). The optional band is the designer's-call decision per copy.md.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. Read `sessions/release-plan.md` § 290 (canonical for scope, success criterion, prerequisites, and artifact).
4. Read `sessions/tracks/public-marketing-website.md` for the track's status snapshot, gap-resolution discipline, and the 289-close retrospective. Confirm Contact is queued at position 36 and Demo at 37.
5. Read **`sessions/public website/contact-page-spec.md`** if present — canonical for cell-by-cell structure, image placements, padding, heading sizes, and cross-section rhythm. The spec's authority section overrides `brief.md` and `copy.md` layout interpretations for the Contact page. *If the spec is not present at session start, the session shifts shape — see "Pre-flight check" below.*
6. Read `sessions/public website/copy.md` for the Contact copy (the "Contact (`/contact`, slug: `contact`)" section). The copy carries three bands: Hero / Email / What I respond to fastest (optional). The mailto address remains the placeholder `mailto:al@example.com` per the brief's gap G4.
7. Read `sessions/public website/brief.md` for the system conventions and gap-surfacing protocol that remain in force.
8. Read `sessions/public website/audit-summary.md` for the system inventory and `sessions/public website/gap-report.md` for the current state of open gaps. G15 / G16 / G17 / G18 / G19 / G20 / G-pricing-1 are all open from prior sessions; the Contact rebuild may exercise them.
9. Read `sessions/public website/about.json` and `sessions/public website/about-layout-spec.md` as the most recent worked example of the spec-driven approach — closer in shape to Contact than to Pricing, since Contact is two or three short bands rather than five. The four-sibling-layout pattern (G20 workaround) may or may not be needed depending on the Contact spec's band shape.
10. Read `docs/app-reference.md` for the page-builder component map and `docs/schema/README.md` for the table index. No schema changes are expected this session.
11. Open `sessions/290. Public Marketing Website — Contact Layout Rebuild.md` and read the session prompt carefully.
12. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

**Pre-flight check — is the Contact layout spec present?** Per the pre-pass discipline 286 / 287 / 289 all followed, the Contact rebuild expects a `contact-page-spec.md` to land in `sessions/public website/` before the session starts. If the file is present, treat it as canonical for layout. If it's not present at session start, **pause and ask the user** whether to (a) wait for an outside-agent pre-pass, (b) author a brief in-session layout spec from `copy.md` directly (the page is simple enough that a 30-line layout sketch may be sufficient), or (c) build directly from `copy.md` with the understanding that Contact's structural decisions get made at session time. Default recommendation: option (b) — Contact is short enough that a same-session spec sketch is cheap and keeps the build-pattern uniform with the other pages.

**Note on the existing contact page:** Contact is greenfield — no prior page exists at the `contact` slug in the system. The importer creates a new page on import (per its slug-matching behavior). No export to preserve, no scratch page to overwrite.

---

## Starting state inherited from 289

- **CRM contract at v2.3.0** — unchanged. Session 289 was content-only with no boundary surface touched. This session is content-only — also no boundary surface.
- **Schema baseline:** no schema changes since 280-close. Session 289 added no schema, no widget types, no migrations.
- **Fast test suite at 2416 / 0 sequential** (289 close baseline, exact match with 288 close). No new tests expected this session — content-only work; regression-check after the re-import suffices.
- **Playwright suite** — baseline preserved. No Playwright in this session; the page-capture harness still lands at the track's close (session 292).
- **Planning state:** Public Marketing Website track active; PMW1 closed at 284; CMS fixes closed at 285; home rebuild closed at 286; About rebuild closed at 287; pricing_chart widget closed at 288; pricing layout closed at 289. The Contact layout lands here. After Contact, expected sequence is Demo (291), Page-capture harness + track close (292).
- **CMS surface area carried forward (resolved at 285, exercised through 286 / 287 / 289):**
  - Layout-level `appearance_config` round-trips through export/import (G1 ✅).
  - TextBlock carries `ctas` + `cta_alignment` (G2 ✅).
  - `secondary-dark` button variant for dark/gradient backgrounds (G3 interim ✅). Available for any dark / gradient band Contact may use.
  - Typography defaults tuned (G13 / G14 ✅).
  - Image widget carries `aspect_ratio` + `max_width` (from 285).
- **No new surface from 289.** Content-only session, no system extensions.
- **Open from earlier sessions:**
  - **G15 / G16** (Hero-in-cell rendering quirks). Contact's hero is likely gradient + text + (maybe) image — default to the About / Pricing precedent (Text widget + Image widget, not Hero widget) unless the spec forces otherwise.
  - **G17** (TextBlock CTA-only band placeholder). If Contact's hero or email band has a CTA-only shape, G17 may manifest — apply the `<p class="ql-align-center"><br></p>` placeholder pattern per 286.
  - **G18** (Image cell-placement controls). If Contact uses any image placeholders, full-cell-fill default applies.
  - **G19 / G-pricing-1** (no `appearance_config` border knob). Unlikely to manifest on Contact; no pull-quote or card-emphasis pattern in the copy.
  - **G20** (`.widget--text_block { height: 100% }` collides with stacked siblings in single-column layouts). May or may not manifest depending on the Contact spec's band shape. If the spec calls for stacked text widgets in a single-column band, apply the four-sibling-layout workaround validated on About Band 2 / Pricing Band 5.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — content sessions don't absorb housekeeping items unless a finding directly ties to contact-page rendering. Touch only if a finding surfaces inside this session's scope; otherwise carry forward.

---

## Process rules for every session

- **Before writing any code or JSON**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. *Session-290 note: no integrations in scope. Contact's email band is a `mailto:` link, not a form. No Form widget on this page (the Form widget is reserved for the Demo page at session 291).*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** Most likely 290-specific candidates: whether to include the optional "What I respond to fastest" band (copy.md marks it optional, designer's call) — surface for user input rather than deciding silently; image-placement decisions if the spec calls for an image on the hero; the same-session-vs-pre-pass spec question if no Contact spec is present at session start.
- **Adapt to drift; don't ask about it.** Contact is greenfield and short; drift is mostly going to be at the level of "should this be a separate band or part of the hero band." Default to silent adaptation + a one-line work-log note.
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that propagate across bands or set a precedent for Demo (291) — the most relevant precedent decision here would be the inclusion / shape of the optional "What I respond to fastest" band. Local edits — just decide and note.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them. *Session-290 note: no migrations expected.*
- **Update `docs/schema/`** after writing any migration. (No-op this session.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 289 close baseline of **2416 / 0**. No new tests expected; this is a regression check.
- **Front-end builds.** No SCSS / JS expected this session. Content-only work. Don't run builds if not needed.
- **Tests for new behaviour:** no new tests expected. Content-only sessions skip the tests-for-new-behaviour rule per the base-prompt convention.
- **Fast vs slow classification:** N/A.
- **Playwright artifact hygiene:** N/A this session.
- **Cross-repo coordination — Fleet Manager.** N/A.
- **Verify objective outcomes yourself; pull the user in for judgment.** Objective surfaces: the JSON imports without errors; the page renders at `/contact`; the bands appear in spec order; the email band shows the placeholder `mailto:al@example.com` per the copy. Close those loops via re-import + curl + screenshot. The visual surface — whether the page reads as appropriately simple, whether the optional band is included and reads well — is the user-judgment surface. Surface for review at session-end.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that Contact is rebuilt against the layout spec (or against `copy.md` if no spec landed), re-imported, and ready for visual review at `/contact`. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/291. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to **291. Public Marketing Website — Demo Page Build**. Session 291 is a **shape change** from the layout-spec-driven content sessions — the Demo page itself is short and visual (one band, two cells, one button per `sessions/public website/demo-page-spec.md`), but the *conversion-action wiring* (single-button auto-login into the shared demo install, per-IP rate-limiting, shared "Demo User" account, demo page hosted on the demo server) is a code-architecture deliverable, not a content one. The 291 prompts need to scope both halves.
- **Next session prompt** — draft against both halves. Reference the project-memory entry `[[demo-page-architecture]]` for the architectural decisions resolved at 289 close (single button, no email, rate-limit per IP, 24h wipe via Fleet Manager, demo page lives on demo server, Fleet Manager shared-contract updates lift to their own cross-session arc).

**Pre-next-session ask in the close handoff.** The Demo page spec is already present at `sessions/public website/demo-page-spec.md` (reviewed and accepted at 289 close — button label "Click Me" stays per user, lorem ipsum on the dark side gets replaced with real copy along the lines of "No signup. Shared sandbox. Resets daily. Spend as long as you want."). No new outside-agent pre-pass needed. Surface a reminder: *"The Demo page spec is in place. Session 291 will scope the conversion-action wiring (auto-login endpoint, rate-limit, shared Demo User) as a code-architecture deliverable alongside the page JSON build."*

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/290. Public Marketing Website — Contact Layout Rebuild — Log.md` using `template-session-log.md` as the format reference. Document: per-band decisions, whether the optional "What I respond to fastest" band was included and why, any spec-on-the-fly authoring that happened, gap-report appends, manual-testing observations, validation that the layout-spec approach continued to deliver predictable results on a greenfield page.
- **Update `sessions/public website/gap-report.md`:** append any new rows surfaced. Note continued G15 / G16 / G17 / G18 / G19 / G20 / G-pricing-1 manifestations under their existing rows.
- **Update `sessions/completed-sessions.md`:** append session 290's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status to reflect 290 closure and the next phase queued at 291.
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (last update date; Contact rebuild complete; Demo queued at 291).
- **Update `sessions/release-plan.md`:** mark session 290's entry ✅ closed at session 290.
- **Archive the previous session:** move all `sessions/289. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-290/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Contact rebuilt against layout spec + screenshot + gap-report appends".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **The rebuild is operator-readable.** Image placeholder labels (if any) follow the spec's image-naming convention (e.g. `contact-hero-portrait`).
- **Hero-cell carrier defaults to Text widget + Image widget** if the hero is gradient + text + image. Same pattern as About Band 1, Pricing Band 1. Do not use the Hero widget unless the spec forces it.
- **Email band's `mailto:` stays at `mailto:al@example.com`** per copy.md and gap G4. The user is handling the real address before launch — not a session 290 fix.
- **Optional "What I respond to fastest" band — surface for user input.** Copy.md marks it as a designer's-call inclusion. Don't decide silently; surface as the architectural-choice question at session start (small enough to be a same-session decision, big enough to affect the page's shape).
