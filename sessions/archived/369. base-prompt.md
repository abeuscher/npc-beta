# Base Prompt — Session 369 (Launch Replan — Three Gates, the Security Track, and the July Cut)

**Start now.** Execute the reading list below, open the session prompt at `sessions/369. Launch Replan — Three Gates, the Security Track, and the July Cut.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **369. Launch Replan — Three Gates, the Security Track, and the July Cut**.

This session **does not execute a release-plan entry — it rewrites the release plan.** It is a full planning session (the numbered-stream analogue of agentic session A012, which planned the billing track) triggered by an owner-initiated re-prioritization at session 368: funding was not secured, the owner is bringing the product to market alone with no champion customer, and the target date is **end of July 2026** with a working budget of **~30–40 sessions**. The owner's assessment, which this session takes as ground truth: the roadmap's "Beta 1 complete" ordering no longer matches the actual constraint, which is *"can I safely expose the public site + demo to paid ad traffic, and safely hold one real client's donations and donor PII"* — and security confidence, previously deferred post-Beta, is now **launch-gating** (bar: internal enumerate-and-fix review; a paid external audit is unaffordable and explicitly not the bar).

This session is **NOT boundary-touching** — planning documents only, no application code, no contract change (stays v2.6.0), no migration, no tests expected. *(The plan it produces will schedule future sessions, some of which may touch the boundary — that's their business, not this session's.)*

**Session shape:** planning/audit-style, **1 session**. Re-sorts the 20 remaining release-plan items against three launch gates, produces the security-hardening track doc, and rewrites the release plan's remaining section into an executable July schedule the owner ratifies mid-session.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human. **This session's output is planning prose the owner reads cold — the PM-register rules apply doubly.**
2. Read the **two input artifacts captured at 368** — they are the evidence base and are already verified against code; do not re-derive them:
   - `sessions/368. roadmap-remaining-inventory.md` — the complete inventory of the 20 remaining items + the pre-release register + the deferred/post-Beta reservoir + stale-marker fixes owed to the plan doc.
   - `sessions/368. security-surface-map.md` — the surface-by-surface security map with the top-5 risk ranking that seeds the hardening track.
3. Read `sessions/release-plan.md` — the remaining-items section (positions 47–67), the pre-release requirements register, and the `── SECURITY AUDIT READY ──` milestone description (post-Beta, unsized — this session promotes and sizes it). Skim the rules block at the top; some rules (e.g. "D3 runs last", the D2 prerequisites) are load-bearing for re-ordering.
4. Read `sessions/session-outlines.md` — the active-tracks block and the Beta-1 stubs, to know what the roadmap currently promises.
5. Read the **368 log** (`sessions/368. Client Billing — Demo Conversion Cleanup Command — Log.md`) — CB3's withdrawal and the two carry-forward findings (user-delete FK restriction; demo role seeded everywhere), plus the owner-decision record this replan rests on.
6. Note any drift between the input artifacts and the current code/docs in a brief work-log entry; proceed per the drift rules.

---

## Starting state inherited from session 368

- **CRM contract at v2.6.0** — 368 was documentation-only (CB3 withdrawn; track doc corrected; one contract-doc scope note, no surface change, no bump).
- **The Client Billing track's CRM lane is complete** (CB1 + CB2 shipped; CB3 withdrawn on a false premise). Everything remaining in that track is FM-side (FM-B1…B5, separate repo) and is **not** this replan's to schedule — but the replan should say where FM work fits around the launch, since FM-B2 is what lights up the shipped Account page.
- **Schema baseline:** no migration since 359. This session adds none.
- **Fast Pest baseline: 3045 / 0** (parallel, verified at 368). No delta expected — planning-only.
- **Playwright:** not run — no browser surface.
- **VERSION** is `0.367.02`; 368's close bumps it to `0.368.01`. This session's close bumps to `0.369.01`.
- **Planning state:** release plan + outlines + billing track doc all updated at 368 for CB3's withdrawal. The stale markers listed at the end of the inventory artifact are owed a sweep — fold that into this session's rewrite rather than a separate pass.
- **Owner decisions on record (from the 368 conversation) — these are settled inputs, not open questions:**
  - Donations + real donor PII from day one; test transactions during setup, but donations must work at launch.
  - The public site and the demo are paid-traffic conversion surfaces (AdWords against competitors); first-impression quality is launch-gating. Likeliest first customer is personally found and driven to the site with context.
  - **Security bar = self-driven internal review**; owner may get external help as a favor; release is beholden to internal review only.
  - **Store/products deferrable. Events are core — ticketing must be present.** Donations are the must-work money path.
  - Budget ~30–40 sessions, end-of-July target, negotiable at the edges.
- **Housekeeping:** planning shape — no inbox absorption. The capture buffer carries one item (demo-role seeding, owner-logged at 368; premise correction recorded in the 368 log: it's the *role* that's unconditionally seeded, the *account* is already gated).

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **This session edits planning documents only.** No application code, no tests, no build. The full close-gate test run still applies (it should be a no-op pass).
- **The owner ratifies the sort before it's written.** The disposition of all 20 items is presented as a proposal at a mid-session checkpoint (see the session prompt) *before* `sessions/release-plan.md` is rewritten. Do not rewrite first and present after.
- **Don't manufacture planning distinctions.** Pick descriptive names, slot things where they obviously go, and spend the session's judgment budget on the genuinely contested calls (what's cut vs deferred, how many security sessions, what D2 shrinks to).
- **Words, not labels.** Every item ID gets its plain-English meaning restated on first use in anything the owner reads.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation and **wait for the owner to confirm before starting.** Bring:

1. **The proposed gate definitions** — one sentence each for Gate 1 (professional to paid traffic), Gate 2 (safe for one real client's money + PII), Gate 3 (owner believes it's secure) — so the sort has agreed goalposts before any item is sorted.
2. **The disposition vocabulary** — confirm the four buckets: *gate-1/2/3 (scheduled)*, *deferred-until-customer* (real work, wrong time), *cut* (not coming back), and *register* (non-session items like Privacy/ToS).
3. **Any gate-definition edge cases worth settling up front** — e.g. whether the QuickBooks leg of the donation loop is Gate-2 or deferrable, and whether the demo server's own hardening (it invites anonymous users in by design) belongs to Gate 1 or Gate 3.

One confirmation, then proceed.
