# Base Prompt — Session 218

We are about to begin a new session: **218. Widget Primitive — Phase 4c (Carousel Retrofit — Audit, Test-Lock, Cleanup)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session adds no new tables and modifies no existing ones; no `docs/schema/` updates are expected.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise. Pay attention to the "Widgets do not receive raw Eloquent models" section — Carousel is the third widget being brought under that invariant, and the first one whose contract source is `SOURCE_WIDGET_CONTENT_TYPE` (collection-backed, content-type-shaped) rather than `SOURCE_SYSTEM_MODEL`.
   - `sessions/widget-primitive-migration.md` — the migration sketch. **Phase 4** ("Retrofit existing widgets onto contracts") is the active phase; Carousel is the third widget in that campaign and the first SWCT retrofit-lock. Pay specific attention to the design-decisions-locked-in bullet that says *"Capability grain varies by source. Fail-closed per-field discipline applies where the data is sensitive and the relationships are walkable. SOURCE_SYSTEM_MODEL **and SOURCE_WIDGET_CONTENT_TYPE** keep per-field declaration"*.
4. Read the two prior Phase 4 session logs to understand the audit + test-lock + cleanup pattern this session replicates:
   - `sessions/216. Widget Primitive — Phase 4a (BlogListing Retrofit — Audit, Test-Lock, Cleanup) — Log.md` — the original audit/test-lock/cleanup playbook. Carousel is closer to BlogListing's "substantially-already-landed" shape than to EventsListing's true-retrofit shape.
   - `sessions/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — locked the second widget end-to-end and surfaced two carry-forward lessons: (a) leak-test sentinels must live in columns that have no derived whitelisted counterpart (the projector legitimately derives whitelisted fields from non-whitelisted source columns); (b) the projector field-name convention is `<source_column>` for the canonical shape (e.g. ISO timestamp) and `<source_column>_label` for any pre-rendered presentation companion. Carousel's `SOURCE_WIDGET_CONTENT_TYPE` source bypasses that naming question (no derivation — fields are user-authored content type fields), but the sentinel-choice lesson applies.
5. Read Carousel as it stands today — this is an **audit-style** session, not a true retrofit. Carousel already declares its contract; the work is verification, test-lock, and cleanup of the legacy data path:
   - `app/Widgets/Carousel/CarouselDefinition.php` — has `dataContract()` returning `SOURCE_WIDGET_CONTENT_TYPE` with `resourceHandle: $config['collection_handle']`, an inline `ContentType` declaring `title`, `description`, `image` fields, and `fields: ['title', 'description']`. Also still declares the legacy `collections(): array { return ['slides']; }` method — investigate whether that method is still load-bearing for the demo-fallback path or is now vestigial.
   - `app/Widgets/Carousel/template.blade.php` — line 2 reads `$slides = $widgetData['items'] ?? [];`. Lines 65–82 walk each slide's `$slide['_media']` map (populated by `WidgetContentTypeProjector::projectRow()` for declared image fields) and run token replacement against the caption template. No direct Eloquent access. The contract path is the live path.
   - `app/Widgets/Carousel/DemoSeeder.php` — Carousel's demo-data seeder. May still seed the legacy `slides` slot for the renderer's `$fallbackCollectionData` path, which `ContractResolver::resolveWidgetContentType()` then re-projects through `projectFallback()` when live items are empty. Read this carefully; the cleanup decision pivots on whether the demo fallback still flows through `collections()` or has been migrated.
6. Read the contract resolution path that backs Carousel today:
   - `app/WidgetPrimitive/ContractResolver.php::resolveWidgetContentType()` — looks up the collection by `resourceHandle` (publishable + active; public-surface filter when `$context->publicSurface`), pulls items with `media` eager-loaded, projects through `WidgetContentTypeProjector::project()`, then falls back to `projectFallback($contract, $fallbackRows)` when live items are empty. The fallback rows are picked by `fallbackRowsFor()` — first by `resourceHandle`, then by any non-empty entry in `$fallback` for back-compat with the widget-type slot-name keying (i.e. the `'slides'` key from `WidgetRenderer`'s `collections` loop).
   - `app/WidgetPrimitive/Projectors/WidgetContentTypeProjector.php` — `project()` handles live `CollectionItem` rows; `projectFallback()` handles plain associative-array fallback rows. Both go through `projectRow()` which strips undeclared fields and attaches a `_media` map keyed by content-type-declared image field keys.
   - `app/Services/WidgetRenderer.php` line 46 — the legacy `foreach ($widgetType->collections ?? [])` loop. This iterates Carousel's `'slides'` slot and calls `WidgetDataResolver::resolve($collHandle, ...)` to populate `$collectionData` *and* `$fallbackCollectionData`. The fallback flows into `ContractResolver::resolve(..., $fallbackCollectionData)` at line 84. Confirm whether the `$collectionData` half is dead under the contract path (the template reads `$widgetData['items']`, not `$collectionData`) and whether the `$fallbackCollectionData` half still serves a purpose.
7. Read the existing Carousel-adjacent test coverage to anchor the new test-lock pair:
   - `tests/Feature/CarouselContractTest.php` (if it exists) and any other `tests/**/*Carousel*` files — establish what's already covered. Grep before assuming.
   - `tests/Feature/BlogListingContractRetrofitTest.php` and `tests/Feature/EventsListingContractRetrofitTest.php` — the shapes to mirror for the new `CarouselContractRetrofitTest`. Test A's JSON-key assertion will need adjustment because Carousel does not embed a `<script x-ref="listingData">` JSON blob the way the listing widgets do — assert the projection shape directly via `ContractResolver::resolve()` instead, or via a DOM-shape assertion against the rendered slide markup.
8. Read `resources/docs/widget-types.md` — confirm whether any prose still references Carousel's `collections()` slot in a way that becomes misleading once the cleanup lands. Edit conservatively if so.
9. Open `sessions/218. Widget Primitive — Phase 4c (Carousel Retrofit — Audit, Test-Lock, Cleanup).md` and read the session prompt carefully. Same shape as 216: audit, then test-lock, then cleanup driven by what the audit found.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216 and 217. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. The admin bundle, the public site CSS, and the page-builder Vue app all compile through Vite, so this one command covers all three surfaces.
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. This rebuilds the public widget asset bundle on the build server and regenerates `public/build/widgets/manifest.json` (which the admin panel and public layout both read).
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/219. base-prompt.md`. Expected next session: continue Phase 4 retrofit. Plausible candidates after Carousel: (a) one of the genuine `SOURCE_WIDGET_CONTENT_TYPE` retrofits — BarChart, BoardMembers, or LogoGarden (each has a `collections()` declaration today and no `dataContract()`); (b) a system-model retrofit — BlogPager (post-shaped, projector-locked already) is the cheapest pick, EventCalendar / EventDescription / EventRegistration share the Event projector landed in 217. Scope to be agreed with the user.
- **Next session prompt**: draft `sessions/219. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/218. Widget Primitive — Phase 4c (Carousel Retrofit — Audit, Test-Lock, Cleanup) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: under the Widget Primitive track stub, mark Phase 4c complete (Carousel retrofit-locked: contract verified, test-lock pair landed, legacy `collections()` cleanup applied per the audit's findings). Note that Phase 4 retrofit continues, with ~17 page-builder widgets remaining (refined estimate from session 217's count: ~12 are genuine retrofit candidates; the rest are pure-config widgets that need no contract).
- **Archive the previous session**: move all files matching `sessions/217. *.md` into `sessions/archived/`. This includes the previous session's base prompt, session prompt, and log. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-218/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
