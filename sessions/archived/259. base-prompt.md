# Base Prompt — Session 259

We are about to begin a new session: **259. Importer Auto-Mapping Pattern Lift (B2a)**.

This session lands **B2a** from `sessions/release-plan.md` — lift the Contacts importer's `FieldMapper`-shaped service-level alias dictionary to the five namespaced importers (Events / Donations / Memberships / Invoice Details / Notes), so non-preset-supported CSVs auto-map at parity with the Contacts surface. It also audits the existing Contacts `FieldMapper` for missing common header aliases (`Postal Code` and likely siblings) surfaced during the B2 rehearsal.

This session is **not boundary-touching** — Fleet Manager contract stays at v2.1.0.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md`:
   - All 11 discipline rules.
   - § B2a entry — the working-set entry that scopes this session.
   - § B2 entry — the rehearsal that surfaced the architectural finding (closed at session 258).
   - The execution order showing B2a between B2 and B2b.
   - Out-of-Gate register — note the **FieldMapper missing common header aliases** entry (folds into this session's audit pass).
3. Read `sessions/session-outlines.md`:
   - Active tracks block (Fleet Manager dormant for this session).
   - Cross-Repo block — confirm v2.1.0 going in; this session does not change it.
   - § Organizations Model Overhaul stub — B2a is the architectural follow-on after B2 closed.
4. Read `sessions/258. Onboarding Rehearsal Cluster — Log.md` end-to-end. The B2a finding is the central architectural takeaway — this session implements its remediation. Specifically note Phase 2's `guessDestination` audit findings and the Phase 4 postal_code observation.
5. Read `resources/docs/onboarding-migration.md` (the help doc shipped at 258) — understand the operator's mental model the auto-mapping feeds into.
6. Read `docs/app-reference.md` — environments, container names, importer entry points.
7. Read `docs/schema/README.md` for the table index. Schema-touching is not expected this session — the lift is service-layer code only — but read enough to know which model fields the importers map to.
8. **The gold standard — read fully:**
   - [`app/Services/Import/FieldMapper.php`](../app/Services/Import/FieldMapper.php) — the rich service-level alias dictionary the lift is patterned on. Note: `presetMap()` shape, `isSkipped()` predicate, `presetFromSourceName()` heuristic, `presets()` list (`generic` / `wild_apricot` / `bloomerang`), `SOURCE_SKIPPED_HEADERS` per-source skip-list pattern.
   - Existing tests at `tests/Unit/Services/Import/FieldMapperTest.php` (or wherever the FieldMapper tests live — confirm at session start) for the test pattern to mirror.
9. **The five importers being lifted — read each `guessDestination()` + the corresponding `*ImportFieldRegistry`:**
   - [`app/Filament/Pages/ImportEventsPage.php`](../app/Filament/Pages/ImportEventsPage.php) `guessDestination()` + [`app/Importers/EventImportFieldRegistry.php`](../app/Importers/EventImportFieldRegistry.php)
   - [`app/Filament/Pages/ImportDonationsPage.php`](../app/Filament/Pages/ImportDonationsPage.php) `guessDestination()` + [`app/Importers/DonationImportFieldRegistry.php`](../app/Importers/DonationImportFieldRegistry.php)
   - [`app/Filament/Pages/ImportMembershipsPage.php`](../app/Filament/Pages/ImportMembershipsPage.php) `guessDestination()` + [`app/Importers/MembershipImportFieldRegistry.php`](../app/Importers/MembershipImportFieldRegistry.php)
   - [`app/Filament/Pages/ImportInvoiceDetailsPage.php`](../app/Filament/Pages/ImportInvoiceDetailsPage.php) `guessDestination()` + [`app/Importers/InvoiceImportFieldRegistry.php`](../app/Importers/InvoiceImportFieldRegistry.php)
   - [`app/Filament/Pages/ImportNotesPage.php`](../app/Filament/Pages/ImportNotesPage.php) `guessDestination()` + [`app/Importers/NoteImportFieldRegistry.php`](../app/Importers/NoteImportFieldRegistry.php)
   - Catalog every header→destination mapping the existing match statements cover. This is the floor; the lift only ever expands coverage, never shrinks.
10. **The integration point — read fully:**
    - [`app/Filament/Pages/Concerns/InteractsWithImportWizard.php`](../app/Filament/Pages/Concerns/InteractsWithImportWizard.php) `processUploadedFile()` and surrounding helpers. The trait calls `$this->guessDestination($normalized)` in the mapping pass. This is the seam the lift replaces — keep the seam, change the implementation.
    - [`app/Importers/Concerns/AggregatingRegistry.php`](../app/Importers/Concerns/AggregatingRegistry.php) — the base class for the `*ImportFieldRegistry` namespaced shape. Understand how `groupedOptions()`, `flatFields()`, `split()` work; the lift may extend this base, may not.
11. **The help doc + the rehearsal artifact:** [`resources/docs/onboarding-migration.md`](../resources/docs/onboarding-migration.md) frames operator expectations the auto-mapping satisfies. The "Edge cases to watch for" section's "Some column names won't auto-recognize" entry is the operator-facing version of this session's finding.
12. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
    - **Per-importer mapper service vs unified service.** Three options surface from the read-through: (a) extend each `*ImportFieldRegistry` with mapping methods (registries gain a richer responsibility); (b) create per-importer mapper services parallel to the existing `FieldMapper` (e.g., `EventFieldMapper`); (c) unified service with importer-keyed dispatch (`FieldMapper::map($header, $importerType, $preset)`). Decide before going deep — the answer shapes everything else.
    - **Preset coverage scope.** Required (`generic` only across all five importers) vs full (`generic` / `wild_apricot` / `bloomerang` per importer where the source system actually exports that record type). Some importers may not have a meaningful Wild Apricot variant (e.g., does Wild Apricot export invoice-detail data in the same shape? probably not). Audit before committing.
    - **`SOURCE_SKIPPED_HEADERS` parity.** The Contacts FieldMapper has per-preset skip-header arrays (e.g., Wild Apricot's `password`, `archived`, `subscribed to emails` etc.). Should the lifted importers gain analogous per-preset skip lists? Surface based on what's actually in source-system exports for the lifted record types.
    - **Sentinel-pattern parity.** Each importer has slightly different sentinel coverage (`__custom_*__`, `__note_*__`, `__tag_*__`, `__org_*__`). Standardizing sentinel coverage is *not* in B2a's scope — flag it as out-of-scope and a candidate follow-on, but do not absorb.
13. Open `sessions/259. Importer Auto-Mapping Pattern Lift.md` and read the session prompt carefully.
14. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.1.0** (per session 253). No change expected this session.
- **Onboarding playbook help doc** shipped at 258 (`resources/docs/onboarding-migration.md`). The "Some column names won't auto-recognize" edge-case entry is the customer-facing surface this session shrinks.
- **B2 closed at 258** with the architectural finding lifted as B2a (this session). The B2 rehearsal's fixture matrix exercised the auto-mapping coverage gap directly — Phase 2 measurements are in the 258 log and motivate scope.
- **G1 fixture generator (session 257)** — the messy-shape fixtures under `storage/app/import-test-fixtures/` are usable for end-to-end verification of the lift. Specifically, regenerating with `--shape=messy --seed=42` and re-running the rehearsal flow against the lifted importers should auto-map substantially more columns than at 258.
- **Fast test suite green** at **1921 passed** (258 close baseline — same as 257 because B2 didn't add tests). This session's expected test growth is meaningful — per-importer header-recognition test files mirroring `FieldMapperTest.php`. Estimate: 50–150 new test cases across 5 importers × 3 presets.
- **Slow test suite green** at **15 passed** (slow group). No expected change.
- **PHP `memory_limit` is 1G** ([docker/php/local.ini](../docker/php/local.ini)) — fast suite runs at `2G` per the standard rehearsal command.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **Architectural-decision discipline.** The "where does mapping live" decision is a Verify-at-Start item — surface the recommendation to the user before implementing, not after committing one branch and unwinding. (See user's standing rule on this in memory: *Pull user in for judgment; close objective loops yourself*.)
- **Floor-vs-ceiling discipline.** Every header→destination pair the existing `guessDestination()` match statements recognize must continue to map after the lift. Test coverage *for the existing pairs* is the floor; new aliases are the ceiling. Do not silently drop existing coverage.
- **No silent semantic changes.** If a column header that previously mapped to one destination now maps to a different one (e.g., because preset detection swings), that's a behavior change that needs explicit user sign-off, not a silent improvement.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline is 1921 passed (258 close). Slow group baseline: 15 passed.
- **Front-end builds — when to run which:** This session is service-layer PHP only. No SCSS / Vue / widget asset changes expected. **No `npm run build` or `build:public`.** If anything in `resources/js/**` or `resources/scss/**` changes unexpectedly, flag it before continuing.
- **Cross-repo coordination — Fleet Manager agent contract.** This session is **not** boundary-touching. Standing cross-repo discipline applies as a passive check.
- **Verify objective outcomes yourself; pull the user in for judgment.**
  - **Objective**: header-recognition unit tests pass; the rehearsal flow on the messy fixtures auto-maps substantially more columns than at 258; existing import e2e specs still pass; FieldMapperTest still passes after audit additions.
  - **Judgment-required**: the architectural shape (per-importer service vs unified service) — surface the recommendation before going deep; preset-coverage scope decisions where source-system shape is genuinely unknown.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and manual verification are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The next session after 259 is **B2b — Export CSV actions for non-Contact list resources** per the release-plan execution order, **unless B2a's scope split per Rule 11.**

If B2a split (likely if all five importers' preset coverage takes more than one context window), then 260 is "B2a part 2" before B2b. Decide at 259 close.

### Phase 2 — Close

When the user says to close (after 260's prompt is agreed):

- **Session log**: write `sessions/259. Importer Auto-Mapping Pattern Lift — Log.md` against `template-session-log.md`. Each importer's lift gets a section; the FieldMapper audit gets its own section.
- **Update `sessions/completed-sessions.md`**: append session 259's row.
- **Update `sessions/release-plan.md`**: check off **B2a** (✅) with a "closed at session 259" note. If B2a split, only check the half that closed.
- **Update `sessions/session-outlines.md`**: fold or update any stub references.
- **Spec doc** — no Fleet Manager contract change.
- **Findings register** — any new findings the audit surfaces land per the standard discipline (working-set / Out-of-Gate / playbook).
- **Help-doc update**: the playbook's "Some column names won't auto-recognize" entry should be revised post-lift to reflect what auto-recognizes now and what still won't. Light edit; not a rewrite.
- **Archive the previous session**: move all files matching `sessions/258. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-259/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly. The lift is structural; new files mirror existing service-shape conventions (`FieldMapper.php` is the template).
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** absorb sentinel-parity work, do **not** absorb the value-normalization findings (events status, email whitespace), do **not** touch the export-action gap.
- Prefer editing existing files to creating new ones, except where the architectural shape requires new service files.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
