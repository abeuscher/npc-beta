# Base Prompt — Session 217

We are about to begin a new session: **217. Widget Primitive — Phase 4b (EventsListing Retrofit)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session adds no new tables and modifies no existing ones; no `docs/schema/` updates are expected.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise. Pay attention to the "Widgets do not receive raw Eloquent models" section — that is the invariant this retrofit brings EventsListing under, the same way 216 did for BlogListing.
   - `sessions/widget-primitive-migration.md` — the migration sketch. **Phase 4** ("Retrofit existing widgets onto contracts") is the active phase; EventsListing is the second widget in that campaign. Pay specific attention to the design-decisions-locked-in bullet that says *"Fail-closed per-field discipline applies where the data is sensitive and the relationships are walkable. SOURCE_SYSTEM_MODEL… keep per-field declaration"*.
4. Read session 216's log. It locked in BlogListing's retrofit with two load-bearing tests (fail-closed whitelist + resolver-is-only-path with query-count pin), confirmed the audit + test + cleanup pattern is reproducible, and surfaced one important subtlety — the projector legitimately derives some contract-whitelisted fields from non-whitelisted source columns (e.g. `excerpt` derived from `meta_description`). The fail-closed invariant is about **contract keys**, not source-column content.
   - `sessions/216. Widget Primitive — Phase 4a (BlogListing Retrofit — Audit, Test-Lock, Cleanup) — Log.md`
5. Read EventsListing as it stands today — this is a **true retrofit**, not an audit. Unlike BlogListing, EventsListing has no `dataContract()` declaration yet; its template walks Eloquent relationships directly:
   - `app/Widgets/EventsListing/EventsListingDefinition.php` — no `dataContract()` method. Declares config schema, defaults, and assets only. The session adds the contract.
   - `app/Widgets/EventsListing/template.blade.php` — calls `$pageContext->upcomingEvents()` at line 3, then composes `url` (via `$event->landingPage`), `location` (via address parts), `price_badge` (HTML from `$event->is_free`), `date` / `date_iso` (format calls on `$event->starts_at`), and `image` (via `$event->getFirstMediaUrl('event_thumbnail', 'webp')`). This composition logic moves into the projector.
   - `app/Widgets/EventsListing/styles.scss` and `app/Widgets/EventsListing/script.js` — front-end concerns, unchanged by this session.
6. Read the existing contract-resolution path that this session extends:
   - `app/WidgetPrimitive/ContractResolver.php` — `resolveEvent()` already exists (added in the Phase 1/2 prototype work) with `date_range` and `order_by` filter support. Currently eager-loads `media` only. This session adds `landingPage` to the `with()` call because the projector's new `url` field needs it.
   - `app/WidgetPrimitive/Projectors/SystemModelProjector.php` — `projectEvent()` currently exposes `id, title, slug, starts_at (ISO), ends_at (ISO), address_line_1, city, state, meeting_label`. This session extends the map to include the derived fields EventsListing needs: `url`, `date` (human-formatted), `date_iso`, `image`, `is_free` (bool), `location` (composed string).
   - `app/Services/WidgetRenderer.php` — unchanged; already the integration point where EventsListing's new contract will resolve.
7. Read the existing EventsListing-adjacent test coverage:
   - `tests/Feature/EventTest.php` around line 297 — "events_listing widget renders upcoming events on a page". Currently passes through the pagecontext path; must continue passing through the contract path after retrofit.
   - `tests/Feature/ThisWeeksEventsDashboardRenderTest.php` — exercises `resolveEvent` via the ThisWeeksEvents widget. It uses a narrower field set; extending the projector's field map **must not break** this test's existing "resolver returns only whitelisted fields" assertions.
   - `tests/Feature/BlogListingContractRetrofitTest.php` — the shape to mirror for the new `EventsListingContractRetrofitTest`.
8. Read the `PageContext::upcomingEvents()` helper at `app/Services/PageContext.php` — **the only caller after this retrofit is the EventsListing template we're rewriting**. Grep confirms no other widget or controller uses it. The session deletes it alongside the template rewrite. Also read `resources/docs/widget-types.md` — the `upcomingEvents()` row in the context-methods table gets removed.
9. Open `sessions/217. Widget Primitive — Phase 4b (EventsListing Retrofit).md` and read the session prompt carefully. This is meatier than 216 — the retrofit is not already landed; the session lands the contract, extends the projector, rewrites the template, deletes the dead pagecontext helper, and adds the test-lock pair.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. The admin bundle, the public site CSS, and the page-builder Vue app all compile through Vite, so this one command covers all three surfaces.
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. This rebuilds the public widget asset bundle on the build server and regenerates `public/build/widgets/manifest.json` (which the admin panel and public layout both read).
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/218. base-prompt.md`. Expected next session: TBD — plausible candidates include (a) continuing Phase 4 retrofit with the next widget (Carousel, Faq, and MemberListing are natural candidates — Carousel exercises `SOURCE_WIDGET_CONTENT_TYPE`, which is different territory from the system-model retrofits), (b) a Memos composer widget exercising write-side dogfooding via `DataSink::write`, or (c) beginning Phase 6 public-page-builder convergence now that two page-builder widgets are retrofit-locked. Scope to be agreed with the user.
- **Next session prompt**: draft `sessions/218. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: under the Widget Primitive track stub, mark Phase 4b complete (EventsListing retrofit landed end-to-end: contract declared, projector extended, template rewritten, `PageContext::upcomingEvents()` removed). Note that Phase 4 retrofit continues, with ~18 page-builder widgets remaining.
- **Archive the previous session**: move all files matching `sessions/216. *.md` into `sessions/archived/`. This includes the previous session's base prompt, session prompt, and log. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-217/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
