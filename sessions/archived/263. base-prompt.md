# Base Prompt — Session 263

We are about to begin a new session: **263. Fleet Manager Contract v2.2.0 — Backup Trigger Endpoint**.

This session lifts a new working-set entry **A1d** into Track A (parallel framing to A1b/A1c — each contract bump is its own session entry). It adds a single mTLS-gated HTTP endpoint (`POST /api/backup/trigger`) that synchronously runs the existing `backup:run` artisan command and returns a JSON envelope reporting the new `last_backup_at` timestamp. The endpoint unblocks FM session 020 (operator-facing "Trigger backup now" affordance in the FM admin UI).

This session is **boundary-touching** — Fleet Manager contract bumps v2.1.0 → v2.2.0 (additive only; v2.1.0 consumers continue working unchanged).

The execution-order deviation is deliberate: position 18 (B1b — Affiliations Junction & Soft-Credit Layer) moves down the queue to land after this session, agreed at 262 close on the grounds that FM session 020 is blocked CRM-side and cross-repo coordination cost outweighs the cost of reordering B1b.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference; do not infer format from previous logs.
2. Read `sessions/release-plan.md`:
   - All 11 discipline rules.
   - Track A § A1d (added at 262 close — confirm the entry is canonical).
   - Track A § A2 — confirm A1d is referenced as the CRM-side prerequisite for A2(b).
   - Execution-order list — confirm position 18 (B1b) moved down to make room for A1d.
3. Read `sessions/session-outlines.md` Cross-Repo block — confirm contract version 2.1.0 going in; this session bumps it to 2.2.0.
4. Read [`docs/fleet-manager-agent-contract.md`](../docs/fleet-manager-agent-contract.md) end-to-end. Pay attention to the `/api/health` and `/api/logs` sections (the patterns this session extends), the CHANGELOG (newest-first convention), and the Security Posture sub-sections.
5. Read [`app/Http/Controllers/Api/Fleet/HealthController.php`](../app/Http/Controllers/Api/Fleet/HealthController.php) and [`app/Http/Controllers/Api/Fleet/LogsController.php`](../app/Http/Controllers/Api/Fleet/LogsController.php) end-to-end — the canonical controller shape this session mirrors. Pay attention to the response envelope conventions, the throttle decoration, and the test-side mTLS-gate handling.
6. Read [`docker/nginx/prod.conf`](../docker/nginx/prod.conf) — focus on the existing `/api/health` and `/api/logs` location blocks. The new `/api/backup/trigger` location follows the same shape with one additive: per-location `fastcgi_read_timeout 600;` to allow 10-minute backups.
7. Resolve where `request_terminate_timeout` lives in the PHP config (`docker/php/local.ini`, the Dockerfile, or default behavior). If it's lower than 600s and lives in a single-file bind-mount, plan on `docker compose up -d --force-recreate <svc>` per the standing rule.
8. Read [`config/backup.php`](../config/backup.php) and [`app/Listeners/RecordBackupSuccess.php`](../app/Listeners/RecordBackupSuccess.php) — the existing infrastructure this session leans on (no rework needed).
9. Read the existing fleet endpoint test files (`tests/Feature/Api/Fleet/HealthEndpointTest.php` and `LogsEndpointTest.php` if present) — the canonical test pattern for mTLS-gated endpoints. Specifically: how the mTLS gate is exercised (or whether it's deferred to deployment-time verification), the throttle test pattern, the response-envelope assertion shape.
10. Read `sessions/253. Fleet Manager Pivot Planning Session — Log.md` — context for the v2.1.0 documentation revision and the recovery-posture framing this session inherits.
11. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
    - **`request_terminate_timeout` location and value.** Where does it currently live? If it needs bumping, is the file a single-file bind-mount? Plan accordingly.
    - **Existing test pattern for mTLS gate.** Does `HealthEndpointTest` exercise the gate at unit level (mocking `$_SERVER['SSL_CLIENT_VERIFY']`) or defer it to deployment-time verification? Match the pattern.
    - **`Artisan::fake()` vs `Artisan::shouldReceive()` convention.** Which does the codebase favor? The new tests should match.
    - **Error-message source on non-zero exit.** `Artisan::call()` returns a code, not a message. The error context comes from `Artisan::output()`. Confirm the sanitisation pipeline handles `Artisan::output()` (potentially multi-line / verbose) the same way it handles `\Throwable::getMessage()`.
12. Open `sessions/263. Fleet Manager Contract v2.2.0 — Backup Trigger Endpoint.md` and read the session prompt carefully.
13. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.1.0** (per session 253). This session bumps to v2.2.0 (additive: new `/api/backup/trigger` endpoint).
- **Fast test suite green** at **2078 passed** (262 close baseline — +16 from XLSX format add). This session adds tests covering the new endpoint — estimate **~+8 to ~+12 new tests** (success path, non-zero-exit failure, exception failure, mtime-cross-check failure, mTLS gate, throttle, method enforcement, response sanitisation).
- **Slow test suite green** at **15 passed**.
- **PHP `memory_limit` is 1G** ([docker/php/local.ini](../docker/php/local.ini)) — fast suite runs at `2G` per the standard rehearsal command.
- **A1c ✅ closed at session 253** — v2.1.0 documentation revision (Recovery posture + FM-side trust assumptions). This session is the next contract-bump session.
- **A2(d) — log read** already shipped at session 251 (additive `/api/logs` endpoint, v2.0.0 → v2.1.0).
- **FM-side context (from FM session 018 close, 2026-05-05):** A2(a) provisioning shipped FM-side. A2(b) backup trigger is blocked on this CRM-side authoring + the matching FM session 020 affordance UI. A2(c) restore is FM-side, future. A2(d) log read shipped at FM 013.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **Single-file bind-mount caution — load-bearing this session.** This session **will** touch `docker/nginx/prod.conf` (adding the new location block) and **may** touch `docker/php/local.ini` (if `request_terminate_timeout` needs bumping). Both are single-file bind-mounts; `docker compose restart <svc>` will fail with an OCI error after `Edit`. Use `docker compose up -d --force-recreate <svc>` instead.
- **No CRM-side schema changes.** This session is application-PHP + nginx + spec-doc; no migrations.
- **No Composer changes.** All new code uses existing dependencies.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **Tests required for new behavior.** New endpoint requires controller-level test coverage per Phase 3.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline is 2078 passed (post-262). Slow group baseline: 15. **Target after this session: ~2086 to ~2090 passed fast** (+8 to +12).
- **Front-end builds — when to run which:** This session is application-PHP + nginx + docs only. **No `npm run build` or `build:public`.** If anything in `resources/js/**` or `resources/scss/**` changes unexpectedly, flag it before continuing.
- **Cross-repo coordination — Fleet Manager agent contract.** This session **is** boundary-touching. The standing two-repo coordination protocol applies: bump `Contract Version` in `docs/fleet-manager-agent-contract.md`, add a CHANGELOG entry, update the Cross-Repo block in `sessions/session-outlines.md` ("Last boundary-touching session in this repo" + bump version field). FM-side absorbs at FM session 020 via the standard WebFetch refresh.
- **Verify objective outcomes yourself; pull the user in for judgment.**
  - **Objective:** controller behaves per the test cases in Phase 3; spec-doc shape matches the actual controller output; nginx + PHP-FPM hold the request open through a 600s timeout; throttle returns 429 on the 7th request; mTLS rejection returns 403; `migrate:status` clean.
  - **Judgment-required:** error-message phrasing aesthetics (does the sanitised error read cleanly to an operator?), the response-envelope field naming (mirror existing `/api/health` shape, but flag any drift), whether the 600s ceiling feels right or wants to be 300s / 900s.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and verification are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The natural next session after 263 is **B1b — Affiliations Junction & Soft-Credit Layer** per the post-263 execution order (B1b moves into the slot vacated by this insertion). B1b is structural (two new junction tables, one-time data migration of `Contact.organization_id` into `affiliations`, importer sentinel rerouting, deletion-guard updates, possibly admin form touches) — already flagged in the plan as 1–2 sessions, may split per Rule 11. Confirm at 263 close.

### Phase 2 — Close

When the user says to close (after 264's prompt is agreed):

- **Session log**: write `sessions/263. Fleet Manager Contract v2.2.0 — Backup Trigger Endpoint — Log.md` against `template-session-log.md`. Document the controller shape, the success-record mtime cross-check decision, the path-prefix-strip approach (relative paths kept, absolute prefix stripped per 263 sign-off), the actual `request_terminate_timeout` resolution, the FM-side handoff status, and any in-session absorptions.
- **Update `sessions/completed-sessions.md`**: append session 263's row.
- **Update `sessions/release-plan.md`**:
  - Check off A1d (✅) with a "closed at session 263" note. Update its success criterion in place to reflect what shipped (specifically the mtime cross-check — that's the integrity-guard half that distinguishes this from a thin proxy).
  - Confirm A2's success criterion still names "(b) trigger and verify a backup against a node" with the CRM-side half marked as A1d-shipped.
- **Update `sessions/session-outlines.md`** Cross-Repo block:
  - Bump agent contract version `2.1.0 → 2.2.0`.
  - Bump "Last boundary-touching session in this repo" to 263.
  - Update "Pending boundary changes" to remove the v2.2.0 entry (if it was preemptively added) or note the FM-side absorption pending at FM session 020.
- **Spec doc** — `docs/fleet-manager-agent-contract.md` at v2.2.0; CHANGELOG entry above v2.1.0.
- **Findings register** — flag any surprises (e.g. PHP-FPM `request_terminate_timeout` interactions, Spatie backup error-message shapes, throttle-test setup gotchas, `Artisan::output()` quirks).
- **Help-doc update** — none expected; this is API surface, not admin UI.
- **Archive the previous session**: move all files matching `sessions/262. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-263/N` branch. Notify the user. Do not push.
- **FM-side handoff**: signal that v2.2.0 is shipped + the `/api/backup/trigger` endpoint is live. FM-side absorbs at FM session 020.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly. The `HealthController` and `LogsController` shapes are the canonical reference; the new `BackupController` slots in alongside them without restructuring either.
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** add async / queued backup execution; do **not** add a backup blob download endpoint; do **not** add per-bucket targeting; do **not** add a backup-status streaming protocol. All explicitly out of scope.
- Prefer editing existing files to creating new ones — except for the new `BackupController.php` and the new test file.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
