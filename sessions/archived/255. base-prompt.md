# Base Prompt — Session 255

We are about to begin a new session: **255. Organizations Model Overhaul (Min)**.

This session lands **B1a** from `sessions/release-plan.md` — the *minimum* implementation of Organizations as a peer to Contact. Transactional Org peering only: nullable `organization_id` on donations / memberships / event_registrations / events, four new per-importer Org-as-source sentinels, four read-only related-records panels on the Organization edit page, and block-with-counts deletion. **No affiliation junction this session.** That layer (plus soft-credit, contact-info parity audit, deletion-policy revision, migration of `Contact.organization_id` → junction) is **B1b**, queued post-B2 per the release-plan execution order. **Not boundary-touching** — Fleet Manager contract stays at v2.1.0.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md` — the 11 discipline rules + § B1a. Note the new B1b entry slotted post-B2.
3. Read `sessions/session-outlines.md`:
   - Active tracks block (informational — Fleet Manager track is active but this session is not boundary-touching).
   - Cross-Repo block — confirm v2.1.0 going in; this session does not change it.
   - The "Organizations Model Overhaul" stub.
4. Read `docs/app-reference.md` — environments, container names, view-to-file mappings, key dependencies.
5. **Schema docs** — read each table file under `docs/schema/` that this session touches:
   - `docs/schema/README.md` (index)
   - `docs/schema/organizations.md` (current shape — already has phone/website/address fields)
   - `docs/schema/contacts.md` (the existing `Contact.organization_id` FK stays as-is)
   - `docs/schema/donations.md` (will add `organization_id` here)
   - `docs/schema/memberships.md` (will add `organization_id` here)
   - `docs/schema/events.md` (will add `sponsor_organization_id` here)
   - `docs/schema/event_registrations.md` (will add `organization_id` here)
6. **Current Organization shape** — read [`app/Models/Organization.php`](../app/Models/Organization.php) end-to-end. Note especially:
   - `Organization::donations()` HasMany is currently **broken** — it expects a `donations.organization_id` column that does not exist. This session adds the column and the relation starts working correctly.
   - Existing `contacts()`, `tags()`, `notes()` relations stay untouched.
7. **Transactional models that get the new FK** — read [`app/Models/Donation.php`](../app/Models/Donation.php), [`app/Models/Membership.php`](../app/Models/Membership.php), [`app/Models/Event.php`](../app/Models/Event.php), [`app/Models/EventRegistration.php`](../app/Models/EventRegistration.php) end-to-end. Note their existing `contact_id` shape (some are nullable, some aren't — the new `organization_id` mirrors that nullability where possible).
8. **Existing Org-importer pattern to mirror** — read [`app/Filament/Pages/ImportContactsPage.php`](../app/Filament/Pages/ImportContactsPage.php) lines 580–610 (the `org_strategy_{$n}` Radio sub-form) and [`app/Filament/Pages/Concerns/InteractsWithImportProgress.php`](../app/Filament/Pages/Concerns/InteractsWithImportProgress.php) lines 780–820 (the existing org auto-create-by-name logic). The new sentinels (`__org_donor__`, `__org_member__`, `__org_sponsor__`, `__org_invoice_party__`) reuse the same shape, applied to *transactional* models rather than `Contact.organization_id`.
9. **Admin resource where the four panels land** — read [`app/Filament/Resources/OrganizationResource.php`](../app/Filament/Resources/OrganizationResource.php) end-to-end.
10. **Verify-at-start questions** that the session prompt names but the implementation should resolve before going deep:
    - **Invoices**: is there a top-level `Invoice` model in `app/Models/`, or are "invoices" grouped Transactions? The Invoice Details importer's behavior decides where `__org_invoice_party__` writes the FK.
    - **EventRegistration org-as-registrant**: column added to schema, but the importer's `__org_registrant__` sentinel is **out of scope** unless the verify-at-start reveals a real CSV shape that needs it. Default: skip the sentinel; admin UI can set the FK manually.
11. Open `sessions/255. Organizations Model Overhaul (Min).md` and read the session prompt carefully.
12. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.1.0** (per session 253). Fleet Manager `/api/logs` shipped at session 251. No change expected this session — schema additions to internal transactional tables don't touch the FM agent surface.
- **Session 254 deliverables now on every install:** the importer Map Columns step UX overhaul (status indicators, searchable destination dropdown, custom-field row completeness, duplicate-column notification, no-default duplicate-strategy radio); the wizard's back-button bug fix; the help slide-over routing fix (resolves through render-hook scope, survives Livewire roundtrips); minor `import-contacts.md` content updates. See `sessions/254. Importer Mapping Page UX — Log.md` for the full landing.
- **PHP `memory_limit` is 1G** ([docker/php/local.ini](../docker/php/local.ini)).
- **Fast test suite is green** at 1819 passed (252 close baseline + 254 additions). This session adds importer round-trip tests and admin-panel render tests for the four new Org panels — modest growth expected.
- **Bind-mount edit gotcha is documented** at [feedback_bind_mount_edit_breaks_container.md](../.claude/projects/-home-al-nonprofitcrm/memory/feedback_bind_mount_edit_breaks_container.md). Doesn't apply to this session — no `docker/nginx/*.conf` or `docker/php/local.ini` edits expected.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration**, ask the user what existing system handles it. This session leans entirely on existing infrastructure (Filament resources, importer trait, schema docs convention, `EnforcesScrubInheritance` source policy on transactional models). Do not introduce new patterns.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** This session's load-bearing questions — *"can a donation have both contact_id AND organization_id set, or is it XOR?"*, *"how does the existing `__org_contact__` strategy radio's UX get reused for the four new sentinels?"*, *"where does `__org_invoice_party__` write its FK if invoices are grouped Transactions?"* — are settled in the session prompt's Design decisions. If implementation surfaces a reason to deviate, surface it before going deep.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Schema migrations** — write each migration, run via `docker compose exec app php artisan migrate`, then update the matching `docs/schema/*.md` file in the same change-set per the standard discipline.
- **The four new transactional-model FKs are independent migrations.** Either land them as one combined migration (cleaner commit boundary) or four small ones (cleaner rollback). Either is fine; pick at session start.
- **The two row builders edited at 254** (`InteractsWithImportWizard::buildNamespacedMappingRow` in the trait and `ImportContactsPage::columnMappingRowSchema`) gain new sentinel handling for the four namespaced importers' Org-as-source destinations. Contacts already has its `__org_contact__` (employer link); the trait gains the new transactional sentinels. Edits are concentrated in the trait.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline is 1819 passed (254 close).
- **Front-end builds — when to run which:**
  - No SCSS, no Vue, no widget asset changes — no `npm run build` or `build:public`.
  - No `public/css/admin.css` edits expected unless the four-panel admin UI surfaces a need.
- **Cross-repo coordination — Fleet Manager agent contract.** This session is **not** boundary-touching. The standing cross-repo discipline applies as a passive check — if the implementation surfaces any change to `app/Http/Controllers/Api/Fleet/*`, `routes/api.php`, `docker/nginx/*.conf`, or the contract spec doc, pause and surface. Otherwise, no change to the Cross-Repo block.
- **Verify objective outcomes yourself; pull the user in for judgment.** Schema migrations, importer round-trip tests, deletion-block tests, and admin-panel render tests are all objective — close the loop yourself. The visual feel of the four new related-records panels is a judgment call (announce that visual testing is ready, then stop and wait).
- **Pause for manual testing only when human judgment is required.** After implementation + fast suite pass, announce that visual testing is ready (the user opens an Organization edit page, confirms the four panels render correctly, runs through one importer with the new Org-as-source sentinel). Stop and wait. Do not suggest closing the session.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The next session after 255 depends on what 255 surfaces. Default forward path per `sessions/release-plan.md` execution order:

- **B2 — Onboarding rehearsal cluster** (next in sequence; B1a unblocks it). B2's first sub-scenario (Migration in) will exercise the new Org-as-source importer destinations against a real-shape Wild Apricot or Neon export.
- If 255 surfaces gaps that B2 would trip over (e.g., a missing sentinel for a real-world data shape, a deletion-block edge case), those land first.

Decide at 255 close. Draft the 256 base prompt + session prompt against the agreed shape.

### Phase 2 — Close

When the user says to close (after 256's prompt is agreed):

- **Session log**: write `sessions/255. Organizations Model Overhaul (Min) — Log.md` against `template-session-log.md`.
- **Update `sessions/completed-sessions.md`**: append session 255's row.
- **Update `sessions/release-plan.md`**: check off **B1a** (✅) with a "closed at session 255" note plus a one-line summary of what landed (the four new FKs + four importer sentinels + four admin panels + deletion-block). **B1b stays open** as the post-B2 follow-up.
- **Update `sessions/session-outlines.md`**:
  - Fold the "Organizations Model Overhaul" stub into a B1a-closure note + a forward pointer to B1b.
  - Confirm the B1b stub captures: affiliation junction, soft-credit, org contact-info parity (industry + EIN gaps only — the rest is already on the model), deletion-policy revision, migration of `Contact.organization_id` → junction.
- **No track doc updates** — this session does not belong to an active track.
- **Spec doc** — no Fleet Manager contract change; no edits to `docs/fleet-manager-agent-contract.md`.
- **Archive the previous session**: move all files matching `sessions/254. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-255/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly — `Donation`, `Membership`, `Event`, `EventRegistration` model shapes are the references; `OrganizationResource` is the panel-rendering reference; `InteractsWithImportProgress::*` org-auto-create logic is the importer-side reference.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones — the four new migrations and four new importer-progress branches are unavoidable additions; everything else is in-place edits.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- No new admin pages, resources, or unrelated migrations expected this session — pure schema + importer + admin-panel additions inside existing surfaces.
- **Cross-importer mirror discipline** — every new sentinel that lands in the trait's `buildNamespacedMappingRow` lands with parallel handling in `serializeColumnMaps` and the matching `processImportRow`/progress-page logic in the same commit. Drift between them is a process failure.
