# Base Prompt — Session 353 (Fleet Data Hygiene — Phase 2: FM Visibility)

**Start now.** Execute the reading list below, open the session prompt at `sessions/353. Fleet Data Hygiene — FM Visibility.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **353. Fleet Data Hygiene — Phase 2: FM Visibility (count-only `data_hygiene` health subcheck).**

This is the **second phase of the Fleet Data Hygiene track** (`sessions/tracks/fleet-data-hygiene.md`) and the owner's continuing **pre-release priority**. Phase 1 (session 352) shipped the in-repo prevention + detection half — the CI keystone scrub-residue test and the node-local `app:data-hygiene` audit, built on a reusable count-only core (`DataHygieneAudit::counts()`). **This session is Phase 2: surface that count-only signal to Fleet Manager** as a new `data_hygiene` subcheck in `/api/health`, so FM can see at a glance what cruft has piled up on each node — **counts only, never raw rows.**

**This session IS boundary-touching** — unlike 352. It adds a subcheck to the FM↔node contract and so requires an **additive contract bump v2.3.0 → v2.4.0** and the full Two-Repo Coordination Protocol discipline. **Note the title-convention nuance:** the protocol's mechanical enforcement keys on CRM session titles starting `Fleet Manager Agent — *`; this session is titled under the Fleet Data Hygiene track instead, so the boundary discipline is **baked into this prompt directly** (read the contract spec before any work; update it + bump the version + update the Cross-Repo block in both repos at close). Treat it as a first-class boundary-touching session regardless of the title.

**The privacy boundary now GOVERNS — this is the phase it was written for.** Fleet Manager must never be *built* to read raw node data. The `data_hygiene` subcheck emits **aggregate counts only** (e.g. `{orphan_event_pages: 12, scrub_records: 0, orphan_media_dirs: 3, dead_owner_media: 0}`) — non-PII, safe on every node, ungated. **Raw rows/records never traverse the FM API.** The deep mode (`app:data-hygiene --deep`) stays node-local + consent-gated and is **not** exposed here. Design the surface so it *can't* read data, not so it's policied not to.

**The FM-side per-node "maintenance/auditable" toggle is FM-repo work — fenced out of this CRM session.** It gates any data-touching operation (deep audit, cleanup, remediation) and lives in Fleet Manager. The CRM half is the count-only subcheck; the toggle is handed off to the FM repo via the cross-repo block + a handoff note. (The optional defense-in-depth node-side flag pairs with Phase 3 remediation, not this session — counts are always safe to emit, so the subcheck needs no gate.)

**Session shape:** track-phase, boundary-touching (cross-repo). The win: FM polls `/api/health` on every install already; after this, the same poll surfaces a count-only data-hygiene signal per node — the visibility half of the track, built on Phase 1's seam. **~1 CRM session + FM-side absorption.** No migration expected — the subcheck reads `DataHygieneAudit::counts()`, which computes from existing columns + media ownership.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is a **boundary-touching, stub-driven** session, so use the fuller session-prompt shape (explicit phases, out-of-scope) and the boundary discipline.
2. **Read the track doc `sessions/tracks/fleet-data-hygiene.md` carefully** — it is canonical: the **privacy boundary** (now load-bearing — counts only over the wire), the **prevent-vs-detect split**, the **component design** (this session is **component C** — FM visibility), and the **sequenced forward plan** (Phase 2 = the count-only subcheck + the FM toggle). Read the **Fleet Data Hygiene** stub in `sessions/session-outlines.md` for the roadmap roll-up.
3. **Build on the 352 seam — read it, don't redo it:** `app/Services/DataHygieneAudit.php` (the shared core; `counts()` returns the four-category aggregate the subcheck consumes — already shaped for exactly this). Read `app/Console/Commands/DataHygieneAuditCommand.php` for the CLI consumer shape and the count-only-vs-deep split the boundary rests on. The 352 log (`sessions/352. *— Log.md`, in `sessions/archived/` if rotated) carries what `counts()` does + the dead-owner detection nuance (varchar `model_id` vs uuid PKs).
4. **The subcheck you'll add:** read `app/Http/Controllers/Api/Fleet/HealthController.php` — the six existing subchecks, the uniform four-key subcheck shape (`status` / `value` / `threshold` / `message`), and the **worst-of overall-status rule** (`overallStatus()`). The `data_hygiene` subcheck slots in alongside `app` / `database` / `redis` / `disk` / `last_backup_at` / `version`. **A key design call: does `data_hygiene` roll into the worst-of overall status, or is it informational?** Cruft accumulation is not a health emergency — lean toward informational (status reflects a generous soft threshold at most, never `red`), but bring the call to the gate.
5. **The contract bump:** read `app/Http/Controllers/Api/Fleet/Concerns/HasContractVersion.php` (the single `CONTRACT_VERSION` constant — bump `2.3.0` → `2.4.0`, additive). Read `docs/fleet-manager-agent-contract.md` (the canonical spec — `/api/health` response shape, the subcheck shape, the "Subchecks (v2.3.0 — six stable keys)" section, the worst-of rule, the CHANGELOG) — **this is the boundary-touching pre-read; you update it at close** (new `data_hygiene` subcheck section + CHANGELOG entry + version).
6. **The cross-repo discipline:** read the **Two-Repo Coordination Protocol** in `sessions/fleet-manager-planning-spec.md` and the **Cross-Repo block** in `sessions/session-outlines.md`. At close you bump the contract version, update the spec doc, and update the Cross-Repo block in **both** repos (FM-side absorption pending). The FM-side toggle + subcheck consumption is the handoff named there.
7. **Performance — a real design question:** `DataHygieneAudit::counts()` does a filesystem walk (`orphanMediaDirectories()`) **and** a full media scan (`deadOwnerMedia()`). `/api/health` is polled frequently. Decide how the subcheck stays cheap: cache the audit result with a short TTL, compute on a schedule and read the cached value, split the cheap DB-only counts from the expensive filesystem walk, or accept the cost. Note the standing **scheduler-runner gap** (no `schedule:work` on the worker — see `docs/app-reference.md`) when weighing a scheduled-compute option. Surface the plan at the gate.

---

## Starting state inherited from session 352

- **CRM contract at v2.3.0** — unchanged by 352 (which was non-boundary). **This session bumps it to v2.4.0** (additive — the new `data_hygiene` subcheck; v2.3.0 consumers ignore the unknown key and keep working).
- **352 shipped (Fleet Data Hygiene Phase 1):** the `wipe()` media-leak fix (scrub events/products now delete one model at a time so Spatie's teardown fires) + the **CI keystone scrub-residue test** (in `RandomDataGeneratorServiceTest`, fast suite, revert-checked); the **`app:data-hygiene`** audit (`DataHygieneAudit` service: `counts()` count-only core + `--deep` records mode); the two prune commands refactored onto the shared detection; and a new **`media:prune-dead-owner`** command (Spatie's `media-library:clean --delete-orphaned` crashes on this schema — varchar `model_id` vs uuid PKs — see `docs/app-reference.md` "Data hygiene audit"). The audit was validated live on nphelper (surfaced 252 orphan pages; the box was cleaned).
- **`DataHygieneAudit::counts()` is the Phase-2 seam** — returns `['orphan_event_pages', 'scrub_records', 'orphan_media_dirs', 'dead_owner_media']`, all non-PII aggregate ints. Shaped in 352 specifically for this subcheck. Reuse it; do not re-derive.
- **Schema baseline:** no migration at 352; `database/migrations/` empty post-348-squash. **Phase 2 expects no migration** — the subcheck reads `counts()` over existing data.
- **Fast Pest baseline:** the **352 close** number (**~2908 / 0** — confirm at session start). **New Pest expected:** the `data_hygiene` subcheck (its shape, its status semantics, that it emits counts only and never raw records, the worst-of interaction) + the contract-version bump assertion. Mirror the existing `HealthController` test conventions.
- **Playwright:** none expected — this is API/service-layer work, verified by Pest. Run Pest and Playwright **sequentially** if you run Playwright at all.
- **Planning state:** Fleet Data Hygiene is the active pre-release priority; Phase 1 closed at 352. This session opens Phase 2 (the boundary half). Phase 3 (bounded remediation) stays future. The **housekeeping inbox** (`sessions/housekeeping-inbox.md`, ~53 items, over threshold) and the **collection-item-media export gap** (found in 352 — `CollectionItem` media doesn't travel in a bundle) are noted alternatives for a later session — **not this one.**

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The privacy boundary governs this phase — counts only over the wire.** The `data_hygiene` subcheck emits aggregate counts (the `value` is non-PII). **Never** expose raw records, slugs, titles, ids, or file paths through the FM API. The `--deep` records mode stays node-local. If a design wants to put anything but counts on the wire, stop and surface it — the boundary is non-negotiable.
- **Boundary-touching discipline (baked in — the title doesn't auto-trigger it).** Read `docs/fleet-manager-agent-contract.md` before any implementation. The bump is **additive** (v2.3.0 → v2.4.0): existing consumers must keep working unchanged. At close: bump `HasContractVersion::CONTRACT_VERSION`, update the spec doc (new subcheck section + status-code/shape notes + CHANGELOG), and update the **Cross-Repo block in both repos' `sessions/session-outlines.md`**. Name the version bump explicitly in the log.
- **The FM-side toggle is FM-repo work — do not build it here.** The CRM half is the count-only subcheck. Hand off the per-node maintenance/auditable toggle + the FM-side subcheck consumption via the cross-repo block + a handoff note (mirror the demo-node handoff pattern). No CRM-side gating is needed — counts are always safe to emit.
- **No migration, no raw-data exposure.** The subcheck computes from `counts()`. If the work appears to need a schema column or to surface a record, stop and surface it at the gate.
- **Decide the subcheck's status semantics + cost at session start** (informational vs worst-of; the `value`/`threshold` shape — total vs the four-category breakdown; the caching/compute strategy for a polled endpoint) — log them.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation this is **Phase 2** of the Fleet Data Hygiene track — the **boundary-touching** count-only `data_hygiene` `/api/health` subcheck, additive **v2.3.0 → v2.4.0**, built on 352's `DataHygieneAudit::counts()` seam, with the FM-side toggle fenced out (cross-repo handoff); (2) the **subcheck plan** — the `value` shape (single total vs the four-category breakdown object), the **status semantics** (informational/always-green vs a soft `yellow` threshold vs rolling into the worst-of overall status — with your recommendation, since cruft is not a `red`-class failure), and the `threshold`/`message` fields; (3) the **performance plan** — how the subcheck stays cheap on a frequently-polled endpoint given `counts()` walks the filesystem + scans media (cache w/ TTL, scheduled compute + cached read, cheap-DB-counts-only, or accept the cost), accounting for the scheduler-runner gap; (4) the **cross-repo plan** — the contract-doc + CHANGELOG + Cross-Repo-block updates and the FM-side handoff note (toggle + subcheck consumption). One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/353. Fleet Data Hygiene — FM Visibility — Log.md` — the `data_hygiene` subcheck (its shape, status semantics, the worst-of interaction, the count-only guarantee), the caching/compute strategy chosen, the contract bump (v2.3.0 → v2.4.0, additive), the cross-repo updates, the FM-side handoff, the Pest added, and Pest results.
- **Bump `VERSION`** to `0.353.<iteration>`.
- **Bump the contract version** `HasContractVersion::CONTRACT_VERSION` `2.3.0` → `2.4.0`, update `docs/fleet-manager-agent-contract.md` (new `data_hygiene` subcheck section + CHANGELOG), and update the **Cross-Repo block in both repos' `sessions/session-outlines.md`** (current version + last boundary-touching session). Drop the FM-side handoff note where the demo-node handoff lives.
- **Track doc:** update `sessions/tracks/fleet-data-hygiene.md` status snapshot (last-update date, Phase 2 progress). If 353 *completes* the CRM half of Phase 2, note the FM-side absorption (toggle + subcheck consumption) as the remaining Phase-2 work; the **phase-expiry compression** for Phase 2 waits until the FM side lands.
- **Inbox:** not a housekeeping batch — **does not drain the inbox**. (Note any data-hygiene-adjacent inbox/outline items touched, but don't fold.)
- **Archive** `sessions/352. *.md` → `sessions/archived/`.
- **Commit on `session-353/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: **reuse `DataHygieneAudit::counts()`** as the subcheck's source — the track's build-once core; don't re-derive the audit logic in the controller. Keep the bump **additive** and the subcheck **count-only** (the boundary). Match the existing `HealthController` subcheck shape exactly (uniform four keys) so FM's existing parser ingests it for free. Don't build the FM-side toggle; don't expose raw records; don't add a migration.
