# Base Prompt — Session 367 (Client Billing — My Account Page + Manage-Account Permission)

**Start now.** Execute the reading list below, open the session prompt at `sessions/367. Client Billing — My Account Page + Manage-Account Permission.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **367. Client Billing — My Account Page + Manage-Account Permission**.

This is the **second CRM-side build session of the Client Billing & Account track** (`sessions/tracks/client-billing-and-account.md`) — draft entry **CB2** in that doc's Appendix A, folded into `release-plan.md` under the `first-customer` gate at the 366 close. It runs directly after **366 (CB1)**, which shipped the node's suspension gate + billing-state reader + contract v2.6.0. CB2 depends **only on CB1** (the pushed-document schema it renders) — not on any Fleet Manager session — so it runs in the CRM lane in parallel with FM's own chain (FM-B1 → FM-B2 → …). The whole point of front-loading the contract at CB1 was to let the two repos proceed independently from here; this session is that parallelism in action.

This session is **NOT boundary-touching** — it **consumes** contract v2.6.0 (the billing-state document CB1 authored) and does not change it. No new `/api/*` surface, no response-schema change, no `CONTRACT_VERSION` bump. **CRM contract stays at v2.6.0.** The Cross-Repo block and `docs/fleet-manager-agent-contract.md` do not change this session.

**Session shape:** feature-style, non-boundary. Builds the node's **read-only "My Account" Filament page** (Settings group), rendering exclusively from the billing-state document CB1's `BillingStateReader` exposes; the seeded `manage_account` permission (granted to **no shipped role** by default — deliberately super-admin-only); the on-page past-due/grace banner plus a slim panel-wide warning banner; the mechanical convention-drift guard cases (banning vendor-billing identifiers CRM-wide + Stripe SDK/config references in the billing/account code); the help doc; tests. **Still no vendor-Stripe anything CRM-side, by design** — the page renders a pushed document and links out to Stripe's hosted portal; that link is the only place the word "Stripe" appears in the new code.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read **`sessions/tracks/client-billing-and-account.md`** — canonical for this session's design: **§ Design decision 2** (the "My Account" page — what it shows, the staleness footer, the grace/lock banners, the permission), **§ Design decision 6** (the two convention-guard cases CB2 lands, and — critically — the *mapped donation surface* the guard must NOT ban), **Appendix A § CB2** (the success criterion), and **open questions 5 and 7** (they gate this session's edit-affordance posture and the permission's role grant).
3. Read **what CB1 (366) shipped — the surface you consume, do not rebuild:** `app/Services/Billing/BillingStateReader.php` (reads `storage/app/private/fleet/billing-state.json`, returns a null-object when absent/malformed, per-request singleton), `app/Services/Billing/BillingState.php` (the typed accessors the page renders: `asOf()`, `status()`, `plan()`, `nextInvoice()`, `billingContactEmail()`, `portalUrl()`, `reason()`, `suspensionState()`, `graceEndsAt()`, `trial()`, `isPresent()`), and `app/Services/Billing/SuspensionState.php`. Then read the contract doc's **§ Client billing — suspension flag + billing-state document** (the document schema the page renders) and the **366 log** (`sessions/366. … — Log.md`) for what was built and any carry-forwards.
4. Read the **settings-page pattern this page mirrors:** `app/Filament/Pages/Settings/FinanceSettingsPage.php` (the exact shape — `canAccess()` returning `auth()->user()?->can('<ability>')`, `navigationGroup = 'Settings'`, `getBreadcrumbs()`), and skim `CmsSettingsPage.php` in the same dir. Note how a page renders **non-Eloquent** content (the account page has no backing record — it renders from the pushed document; use an infolist fed from array state or a custom Blade view, whichever the surrounding pages favor).
5. Read the **permission plumbing:** `database/seeders/PermissionSeeder.php` (where `manage_account` is seeded — like `manage_cms_settings`, a seeded row, **no migration**), `docs/runbooks/permission-matrix.md` (the runbook that gets the matching row), and `tests/Feature/PermissionMatrixTest.php` (the probe test that pins seeded abilities — add the `manage_account` row here).
6. Read `tests/Feature/Infrastructure/ConventionDriftTest.php` (the standing conventions guard — the two new billing guard cases land here) and the **donation-Stripe surface the guard must leave alone:** `app/Services/StripeCheckoutService.php`, `app/Http/Controllers/StripeWebhookController.php`, and the `services.stripe.*` config block in `app/Providers/AppServiceProvider.php@boot`. The guard bans vendor-billing identifiers CRM-wide and Stripe references in the *billing/account* code — never the legitimate donation surface.
7. Read the help-doc convention: an existing stub under `resources/docs/` (e.g. `cms-pages.md`) for the frontmatter + route-registration shape (per `CLAUDE.md` § Style rules — a new admin page gets a help-doc stub).
8. Note any drift between this prompt / the track doc and the code in a brief work-log entry; proceed per the drift rules.

---

## Starting state inherited from session 366

- **CRM contract at v2.6.0** — 366 (CB1) shipped it (additive, node half of client billing). **This session is non-boundary — it consumes v2.6.0 and does not bump it.**
- **CB1 surface exists and is tested — consume it, don't rebuild:** `App\Services\Billing\BillingStateReader` (+ the `BillingState` DTO and `SuspensionState` enum), the `EnforceSuspensionState` middleware, and the `suspension` `/api/health` subcheck. The Account page renders through `BillingStateReader` — do **not** add a second read path or cache.
- **Schema baseline:** no migration since 359's `users` 2FA columns. **This session adds no migration** — `manage_account` is a seeded permission row (mirror `manage_cms_settings` in `PermissionSeeder`).
- **Fast Pest baseline: 3025 / 0** (parallel, 366 close). Expected delta: **new tests** for the page render + status-badge variants + self-hide, the banners, the permission gating + matrix probe row, and the convention-drift guard cases.
- **Playwright suite:** not expected to run — the account page renders from a fixture document and its gate is `canAccess()`, both Pest-verifiable; a standing spec is justified only if manual testing surfaces a browser-level rendering gap.
- **Planning state:** CB1–CB3 folded into `release-plan.md` at the 366 close under the `first-customer` gate (after position 48); the track doc status snapshot marks CB1 complete. CB2 is the next CRM entry. FM's lane (FM-B1 → FM-B2 → …) runs independently; nothing here waits on it.
- **Housekeeping:** feature shape — no inbox absorption. The parked A012 finding (`DashboardIntegrationStatusWidget` reads the never-set `services.stripe.key`) stays parked — it is donation-Stripe surface, out of scope here.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The track doc's locked decisions govern.** Design questions resolve against `sessions/tracks/client-billing-and-account.md` (§ Design decision 2 for the page, § Design decision 6 for the guard); genuine conflicts go to the owner at the gate.
- **Render from the pushed document alone.** The page reads through CB1's `BillingStateReader` and displays what the document carries. It holds **no vendor-Stripe credential, config key, or SDK call**; the word "Stripe" appears in the new code only as the label on the hosted-portal link (`portal_url`). This is the discipline the convention guard then makes mechanical.
- **Display-only — no local billing forms.** Per open question 5, the billing contact and every "editable" part are **read-only node-side**; each edit affordance is a hand-off to the Stripe-hosted portal, never a local form (there is no node→FM write channel and there shouldn't be).
- **Self-hide when there's no document.** With no pushed billing-state document (internal nodes, fresh installs), the page hides from navigation and `canAccess()` is false — additive by construction, exactly as CB1 is inert until FM pushes.
- **No migration.** `manage_account` is a seeded row; the permission-matrix runbook + `PermissionMatrixTest` get the matching entry.
- **Non-boundary checklist:** confirm no contract file (`docs/fleet-manager-agent-contract.md`, `HealthController::CONTRACT_VERSION`, the Cross-Repo block) is touched. If the work somehow reaches for the boundary, stop — that's a scope signal, not a step.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation (per the CLAUDE.md reporting rules) and **wait for the owner to confirm before starting.** Bring: (1) confirmation of track-doc **open question 7** — `manage_account` seeded but granted to **no shipped role** (super-admin-only by default, documented as intentional); (2) confirmation of **open question 5** — the account page's "editable" parts are all read-only hand-offs to the Stripe portal, no local forms; (3) the **CB3 fold decision** — the demo-conversion cleanup command (node half) is a ≤0.5-session fold candidate that pairs with FM-B4; keep it as a separate CB3 (working lean, since CB2 is already a full page + permission + banners + guards) or fold it in here; (4) the naming leans (page class + Settings-group label "Account"; ability `manage_account`) — cheap to confirm. One confirmation, then proceed.
