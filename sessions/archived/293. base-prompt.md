# Base Prompt — Session 293

**Start now.** Execute the reading list below, open the session prompt at `sessions/293. Public Marketing Website — Page-Capture Harness & Track Close.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **293. Public Marketing Website — Page-Capture Harness & Track Close**.

This is the **terminal session of the Public Marketing Website track** — Phase 4 (Page-capture harness + screenshots + close-out) per `sessions/tracks/public-marketing-website.md`. In the Public Website Complete milestone execution order it sits **after 292 (Demo Page Build, ✅ closed)** and is the **last track phase**; closing it collapses the track's release-plan position to a one-liner and writes the track-closure retrospective. There is no dedicated `release-plan.md` § entry — Phase 4 is a track phase, same posture as 289/290/292.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. No `/api/health` / `app/Http/Controllers/Api/Fleet/*` / response-schema / `VERSION`-at-deploy-semantics change of the *contract*. **CRM contract stays at v2.3.0.** (292 produced a CRM-side answers artifact for FM's client-upgrade hand-off — `sessions/fm-client-upgrade-handoff-answers.md`; that is a coordination Q&A document, explicitly **not** a contract edit, and 293 does not touch it or the contract.)

**Session shape:** track-phase / tooling + close-out. Build a standalone Playwright page-capture script that renders the five published public pages and commits `sessions/public website/screenshots/page-{slug}.png`, write `sessions/public website/build-summary.md`, then close the track (Phase 4 retrospective + full track closure).

Before doing anything else:

1. Re-read the session templates (`template-base-prompt.md`, `template-session-prompt.md`, `template-session-log.md`) only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it (note the post-292-close state: Demo built at 292, Phase 3 retrospective compressed, harness/close = 293).
3. **Read `sessions/tracks/public-marketing-website.md` in full** — Phase 4 sketch, the "Working folder" inventory, the gap-resolution discipline, and the **Stance** (the deliverable is content/tooling, not production code; gaps are surfaced, not resolved here). Phase 4's session log doubles as the track-closure entry.
4. **Read `sessions/public website/brief.md`** — the Phase 4 success criteria (lines ~110–111: a Playwright capture of each rendered page committed as `screenshots/page-{slug}.png`; a short `build-summary.md` listing completed / deferred / needs-user-input). The brief is canonical for the track.
5. Read `scripts/generate-thumbnails.js` end-to-end — it is the **reference for how Playwright is invoked in this codebase** (host-run ESM Node script, global Playwright, `--base-url` arg, dev environment). 293 writes a **parallel** script, not an extension: different mount point (full public pages at their published URLs, `fullPage: true`), different output path. Do not modify `generate-thumbnails.js`.
6. Read the leverage surfaces before touching them: `routes/web.php` (home is served at `/`, not `/home`; about/pricing/contact/demo at `/{slug}`), and confirm the five pages exist as published pages on the local instance (the four prior pages + `demo` from 292, which is live in the CMS at `/demo` as of 292).
7. Read `docs/app-reference.md` for environment/container names and the local base URL. No schema work is expected — 293 is a script + docs + close session; if the design appears to need a migration or a contract change, stop and surface.
8. Open `sessions/293. Public Marketing Website — Page-Capture Harness & Track Close.md` and read the session prompt carefully — it carries the harness spec, the five target URLs, and the close/retrospective shape.
9. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules.

**Carry-forward open item (read this).** The fast suite baseline going into 293 is **2446 passed / 1 failed**, not 0-failing. The single failure — `tests/Feature/Infrastructure/AppVersionStampTest.php` "documents the version field in the FM contract…" — is a **pre-existing self-collision introduced at the session-291 close commit** (that commit added both the invariant test forbidding the literal phrase "seven-character git SHA" in `docs/fleet-manager-agent-contract.md` *and* a 291 CHANGELOG sentence that quotes that exact phrase while explaining the correction). It was **not** introduced by 292 — both files are untouched on the 292 branch — and it was surfaced to the user at 292, decision deferred to them. **Consequence for 293:** do not treat "fast suite green" as the bar until this is resolved; expect 2446+ passing with this one known red unless the user has since fixed it. The recommended one-line fix (user-owned decision — confirm at session start whether they want it folded into 293 or left flagged) is to reword that 291 CHANGELOG sentence so it stops reproducing the forbidden literal (e.g. "wrongly described it as a git commit SHA"). It is CRM doc-hygiene with **no contract-surface and no runtime effect** — `HealthController::CONTRACT_VERSION` and the contract stay v2.3.0. If folded in, it is a one-line doc edit only; if not, it goes in `build-summary.md` as a known carry-forward.

---

## Starting state inherited from session 292

- **CRM contract at v2.3.0** — unchanged. 292 (Demo Page Build) was CRM-internal and not contract-touching. It shipped: `sessions/public website/demo.json` (single-band 50/50 split page — built into the running CMS, zero importer warnings, user-reviewed and approved with one visual revision: +100px on the dark cell's bottom padding as the viewport-height-gap workaround, synced into the committed JSON); the **form-less `/demo/enter` auto-login** (a dedicated tightly-scoped `demo` role in `PermissionSeeder` — never `super_admin`, deny-list on settings/secrets/integration/user-management/destructive; a shared "Demo User" seeded **demo-server-only** in `DatabaseSeeder` under `isDemoMode()`; per-IP `throttle:10,1`; `abort_unless(isDemoMode(), 404)` demo-server guard; `DemoLoginController` reuses the existing `Auth::login()` → admin-panel-redirect path); the threat model pre-registered in `docs/security-demo.md`; and `sessions/fm-client-upgrade-handoff-answers.md` (CRM-side answers to FM's client-upgrade hand-off — a coordination doc, **not** a contract change). `VERSION` bumped to `0.292.1`.
- **Schema baseline:** no schema changes since 280-close. 291 and 292 added no migration. 293 is **not expected to add one** — it is a script + docs + close session.
- **Fast test suite at 2446 / 1** (292 close baseline; 2440 prior + 6 new `DemoLoginTest` cases, all green). The **1 failure is the pre-existing 291 contract-doc self-collision** described in the carry-forward note above — not a 292 regression. **No new Pest expected this session:** the page-capture script is run-on-demand tooling (per the brief: "run on demand, not in CI"), a standalone Node script, not a Pest/Playwright-suite spec; its check is "the five PNGs exist and render the right pages" + the user's visual review of the images. The page-JSON deliverables are content and were verified at their own sessions.
- **Build/version discipline (carry-forward — load-bearing):** the pre-Beta pseudo-versioning shipped at 291 stands. The repo-root `VERSION` file holds `0.<session>.<iteration>` (currently `0.292.1`); `deploy.yml` reads + format-validates it, bakes it as `APP_VERSION`, tags the GHCR image immutably, and hard-fails the build if the version tag already exists. **Consequence for 293: bump `VERSION` to `0.293.<iteration>` as part of the iteration** — a forgotten bump fails the deploy build. `AppVersionStampTest` enforces the format invariant (and currently also carries the unrelated pre-existing red above).
- **Playwright suite** — baseline preserved; none added through 292. 293 *builds* a page-capture script but, per the track doc and brief, it is a **standalone host-run script committed to the working folder, run on demand, not added to CI or the Pest/Playwright suite**. The Playwright-suite baseline is therefore unchanged. (The `feedback_test_runs_not_parallel` discipline still applies if any in-suite spec is ever touched — but 293 adds none.)
- **Planning state:** at 292 close the Demo execution-order slot was marked ✅; **Phase 3 (Contact + Demo) terminated → phase-expiry compression was applied to the track doc at 292 close**; the gap-report was appended with 292's findings (G-demo-1 large-button size variant; viewport-height-aware layout sizing — the 100vh white-gap, asymmetric-bottom-padding workaround; full-bleed cells via both `*_full_width` toggles); completed-sessions / session-outlines / track doc updated; session 291 archived. 293 inherits that post-close state and is the track's **final** session — Phase 4 + full track closure.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — 293 is a track close-out session, **not** a housekeeping batch; do not absorb inbox items. The s290 "link-color mechanism consolidation" / "Contact hero readability overrides" items and the s291 post-Beta "compliant system-email footer" roadmap stub remain out of scope here; carry forward.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Parallel script, not an extension.** `scripts/generate-page-screenshots.js` is new and standalone. Mirror `generate-thumbnails.js`'s invocation conventions (host-run ESM Node, global Playwright, `--base-url` defaulting to `http://localhost`, the `NODE_PATH` note) but capture **full public pages at their published URLs** with `fullPage: true`, not widget previews. Do not modify `generate-thumbnails.js`.
- **The five published URLs.** `home → /` (root — served by `PageController@home`, **not** `/home`), `about → /about`, `pricing → /pricing`, `contact → /contact`, `demo → /demo`. Output filenames stay slug-keyed: `page-home.png`, `page-about.png`, `page-pricing.png`, `page-contact.png`, `page-demo.png` in `sessions/public website/screenshots/` (create the directory).
- **The demo page renders fine for capture.** `/demo` is a static split page on the dev instance; only the *button click* 404s off-demo, and the harness does not click. The dark side shows lorem placeholder copy (flagged `pending-copy`) — that is **expected**, a milestone-close gate (real copy is user-provided later), not a 293 defect. Capture it as-is.
- **Settle before capture.** Wait for fonts + the public asset bundle to settle (manifest-load idle / network-idle) before the screenshot, so bands render with final CSS — the thumbnail script's wait pattern is the reference.
- **Verify the objective outcome yourself.** "Do the five PNGs exist, are non-trivial in size, and show the right page" is objective — run the script, confirm the five files, and report. Pull the user in only for the visual-judgment surface: whether the captured pages look right as the committed record of the track.
- **`VERSION` must be bumped this session** to `0.293.<iteration>` (see Starting state). Mechanically enforced by the deploy pipeline — do it as part of the iteration, not only at close.
- **No gap resolution.** Per the track's gap-resolution discipline, 293 does not resolve any open gap (no new widget/button/config). Open gaps are recorded in `build-summary.md` with their status; resolution is the user's per-gap call on separate sessions.
- **No contract change.** If the build appears to need a Fleet Manager contract change or a `/api/health` touch, stop and surface — it would mean scope drifted. The pre-existing `AppVersionStampTest` red is a doc-prose carry-forward, not a contract change (see the carry-forward note).
- **Run the fast suite when implementation is complete:** `docker compose exec app php artisan test --exclude-group=slow`. Expect **2446 / 1** (the known pre-existing red) unless the user directed the one-line doc fix be folded in, in which case expect green. No positive delta is expected from 293 itself (no new Pest).
- **Pause for manual testing only when human judgment is required.** End-of-session: announce the harness is built and the five screenshots are captured + committed, surface the images for the user's visual review, and note the `build-summary.md` contents (what shipped, what's deferred to milestone-close, what needs user input). Then stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.** Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

293 is the **terminal session of the Public Marketing Website track**. There is no automatic "next session" within this track. After implementation and manual testing are complete:

- Do **not** auto-draft a `294. base-prompt.md` / session prompt. The next session is the user's choice — it returns to the Public Website Complete milestone execution order (the next milestone entry after the track), or to whatever the user lifts next. Surface the milestone-close gate list (below) so the user can decide what 294 should be, and draft 294's documents **only when the user names the next session**.

**Carry-forward reminder for the close handoff** (the user is tracking these; **none are 293 deliverables** — they are Public Website Complete milestone-close gates that remain open after the track closes): *(a) Privacy Policy + Terms of Use starter pages must land (Stripe Dashboard requires both URLs; pairs with the E16 header/footer overhaul); (b) real photography must replace every sample-image placeholder across all five pages; (c) the Demo dark-side real copy replaces the lorem placeholders; (d) the Fleet-Manager shared-contract arc (demo-server identity, abuse alerting, wipe/reset coordination — the `sessions/fm-client-upgrade-handoff-answers.md` answers feed this arc) lands across its own sessions; (e) the pre-existing `AppVersionStampTest` 291-doc-prose red, if not folded into 293.* `build-summary.md` is the durable record of this list.

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/293. Public Marketing Website — Page-Capture Harness & Track Close — Log.md` using `template-session-log.md` as the format reference. Document: the harness script (invocation, the five URLs, settle strategy, output path), the captured screenshots, the `build-summary.md` contents, the `VERSION` bump, Pest result (incl. the carry-forward red's disposition), and any drift. **This log doubles as the track-closure entry.**
- **Update `sessions/release-plan.md`:** mark the Page-Capture Harness & Track Close execution-order slot ✅ closed and collapse the **entire Public Marketing Website track** position to a one-liner (the track is now fully closed; per-phase detail lives in the track doc's Phase Retrospectives and the session logs).
- **Update `sessions/public website/gap-report.md`:** final pass — confirm every gap row's status; note that the track is closed and unresolved gaps are now the user's per-gap lift decisions (no longer track-owned).
- **Update `sessions/completed-sessions.md`:** append session 293's row.
- **Update `sessions/session-outlines.md`:** move the Public Marketing Website track out of the Active-tracks block (track complete); leave the milestone-close-gate carry-forwards visible where the user tracks them.
- **Update `sessions/tracks/public-marketing-website.md`:** bump the status snapshot to **track closed** (last update date; all five pages shipped + screenshotted; build-summary written). Apply the **Phase 4 phase-expiry compression** AND the track-level closure: Phase 4's retrospective lands in Phase Retrospectives; the forward plan collapses; nothing inflates.
- **Archive the previous session:** move all `sessions/292. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files (incl. `scripts/generate-page-screenshots.js`, the five `sessions/public website/screenshots/page-*.png`, `build-summary.md`, the bumped `VERSION`, the log, doc updates, archived 292 files) on the final `session-293/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Public-page screenshot harness (generate-page-screenshots.js) + five committed page captures + build-summary + VERSION 0.293.1".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.** The new script follows `generate-thumbnails.js`'s conventions (ESM, host-run, the same arg/`NODE_PATH` ergonomics).
- **Extend, don't reinvent.** The harness rides the existing Playwright invocation pattern; the screenshots land in the existing `sessions/public website/` working folder; the close rides the existing track-doc Phase-Retrospective compression. A new abstraction is a smell — surface before introducing one.
