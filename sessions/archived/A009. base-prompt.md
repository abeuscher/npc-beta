# Base Prompt — Session A009 (Public Docs Site — /docs + Agent-Readable Markdown + AEO Wiring)

> **Scheduled as session A009 — agentic.** Runs autonomously in a cloud session alongside regular session work; the `A###/N` branch prefix and the `0.909.x` VERSION band distinguish it from the user-driven `NNN/N` sequence. Additive public read surface over the existing help corpus: `/docs` HTML + sibling `.md` URLs + `llms.txt`/`sitemap.xml`/`llms-full.txt` AEO wiring, gated to the marketing instance by a new `PUBLIC_WEBSITE` env flag, plus the `help:sync`-into-deploy-path fix. Pest feature tests on the gated routes and rendered output are the binary acceptance signal.

**Start now.** Execute the reading list below, open the session prompt at `sessions/A009. Public Docs Site.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **A009. Public Docs Site — /docs + Agent-Readable Markdown + AEO Wiring.**

This is **execution-order position: emergent**, not tied to any `release-plan.md` entry. The forcing function is the SEO/SEM planning track for the marketing site: the audience that matters increasingly asks an AI before it asks Google, so the docs corpus must be crawlable and agent-fetchable — HTML for humans, sibling raw-markdown URLs + `llms.txt` indexing for agents. The corpus already exists (the in-app help system's 62 repo-canonical markdown articles); this session adds the public read surface and its instance gate.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0; no `CONTRACT_VERSION` change. **No DB schema change, no migration** — the session reads the `help_articles` / `help_article_routes` rows that `help:sync` already writes. (One forward-looking handoff, not a contract change: FM's provisioning template eventually sets `PUBLIC_WEBSITE=true` on the marketing node — log it as an FM-side note.)

**Session shape:** feature-style (new public surface), agent-friendly because the content system already exists and acceptance is fully self-verifiable via Pest. Five phases: stack bring-up → flag/helper → routes/controller/views → AEO wiring → deploy-path fix + corpus link hygiene → tests. Single iteration expected; split per Rule 11 if scope surfaces.

Before doing anything else:

1. **Read the full session prompt** at `sessions/A009. Public Docs Site.md` — settled design decisions (locked at the planning session; do not relitigate), phases, hard-no list, acceptance criteria. It is the load-bearing document.
2. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference for the log at close.
3. **Read these files in full** before any edit:
   - `app/Console/Commands/HelpSync.php` — the sync command (frontmatter parsing, validation, upsert/orphan-delete behavior).
   - `app/Services/HelpArticleService.php` + `app/Models/HelpArticle.php` + `app/Models/HelpArticleRoute.php` — resolution + model shape (`parent()` accessor for breadcrumbs).
   - `app/Filament/Pages/HelpIndexPage.php` + `HelpCategoryPage.php` + `HelpArticlePage.php` — the admin read surfaces this session's public surface mirrors (category labels, related-articles tag-intersection query, breadcrumb chain).
   - `app/Http/Controllers/LlmsTxtController.php` + `app/Http/Controllers/SitemapController.php` — the two AEO controllers this session extends (note the `SiteSetting::get('base_url', config('app.url'))` pattern and the 1-hour caches).
   - `routes/web.php` — route ordering: named/utility routes first, page-slug catchall last. The new `/docs` routes register with the utility block.
   - `app/Http/Controllers/PostController.php` — **the chrome precedent**: how non-Page content renders inside the public layout + `ChromeRenderer` header/footer. Mirror its shape for the docs views.
   - `resources/views/layouts/public.blade.php` — what the layout expects; the global-`noindex` read is `$page`-independent, verify nothing else assumes `$page`.
   - `config/site.php` + `app/helpers.php` — where the new config key + `isPublicWebsite()` helper land (mirror `isDemoMode()`).
   - `.github/workflows/deploy.yml` + `.github/workflows/deploy-demo.yml` — the `widgets:sync` lines the `help:sync` step lands after.
   - A representative sample of `resources/docs/*.md` (e.g. `contacts.md`, `taxonomy.md`) + every file the Phase-4 link grep flags.
4. Read `docs/app-reference.md` for environment/container names and the build decision rule. No `docs/schema/` reads needed — no schema change.
5. Note drift vs the session prompt; adapt silently per the drift rule and note adaptations in the log.

---

## Starting state inherited from the planning session (post-348 close)

- **CRM contract at v2.3.0** — unchanged; this session does not touch the boundary.
- **Schema baseline:** the fresh 348 squash dump (`database/schema/pgsql-schema.sql`); `database/migrations/` is empty post-squash. **This session adds no migration.** Fresh clone: `migrate:fresh --seed`, then `help:sync` to populate the corpus.
- **Fast Pest baseline: 2812 / 0** (the 348 close). Expected delta: roughly +10–15 (gating, article shape, `.md` sibling, llms/sitemap/llms-full, catchall non-regression).
- **Playwright baseline:** preserved; **no new specs** — every acceptance signal is server-rendered output Pest sees directly (Playwright-discipline heuristic: a future session derives zero value from a browser spec here).
- **Planning state:** session 349 (Table Widget) is drafted and may run in parallel on the numeric track; its surfaces (page-builder Vue, widget shell, `HtmlSanitizer`) do not overlap this session's. `LlmsTxtController` already exists and serves `/llms.txt` — this session extends it, does not create it.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — not absorbed; this is an emergent feature session, and the deploy-path `help:sync` fix is in-scope by design, not an inbox pull.
- **Cloud-session caveat:** `vendor/` and `node_modules/` clone empty — Phase 0 stack bring-up (`docker compose up -d`, `composer install`, `npm install`, `migrate:fresh --seed`, `help:sync`) before any task work.
- **Build/version discipline:** bump `VERSION` to `0.909.<iteration>` each iteration (agentic band — A001 used `0.901.x` … A009 uses `0.909.x`). Branch `A009/N`.

---

## Process rules for every session

Base-prompt process rules (`sessions/template-base-prompt.md`) apply in full. Session-specific:

- **The gate is load-bearing on every new surface.** Every new route — `/docs`, `/docs/{slug}`, `/docs/{slug}.md`, `/llms-full.txt` — and every conditional AEO extension checks `isPublicWebsite()`. A flag-off instance must be byte-identical in behavior to today. The Pest gating tests are the proof; write them first if convenient.
- **Read through config, never raw `env()` outside `config/site.php`** — the flag must survive `config:cache`.
- **Do not relitigate the settled decisions** (hardcoded `/docs`; env flag not SiteSetting; all articles public in v1; no schema). They were locked with the owner at the planning session.
- **Builds:** docs typography SCSS (if any) rides the public SCSS surface → `npm run build`. No widget assets touched → `build:public` should not need to run.
- **Tests green before close:** `./dev test` (fast suite). Run `help:sync` after the Phase-4 link edits and confirm 62 synced / 0 skipped.

---

## ── SESSION CLOSE ───────────────────────────────────────────────────────────

Per the A-session convention (no user-gated close; the session closes itself when acceptance criteria are met):

- **Log file:** `sessions/A009. Public Docs Site — Log.md` — template-session-log format. Must include: what was built per phase; the FM-side handoff note (provision `PUBLIC_WEBSITE=true` on the marketing node's `.env`); the link-normalization change list; deferred items (per-article public/private flag, JSON-LD/TechArticle, docs search, marketing→docs cross-link content pass); test results.
- **Bump `VERSION`** to `0.909.<final-iteration>`.
- **Update `sessions/completed-sessions.md`** with the A009 row.
- **Do not** edit `sessions/release-plan.md` (no entry exists) — add a one-line note to the SEO/docs context in `sessions/session-outlines.md` only if a natural home exists; otherwise the log carries the record.
- **Commit on `A009/N`, push the branch** (`git push -u origin A009/N`). Never push to main; the owner merges.

---

## Style rules

Base-prompt style rules apply. Session-specific: mirror `PostController` / the public layout conventions exactly for the docs views; mirror `LlmsTxtController`'s build-and-cache shape for `llms-full.txt`; keep the docs templates lean (site chrome + readable typography — this is a docs library, not a designed marketing surface; a visual polish pass can follow in a numeric session if the owner wants one).
