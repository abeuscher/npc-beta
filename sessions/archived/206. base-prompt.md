# Base Prompt — Session 206

We are about to begin a new session: **206. Code Review & Cleanup (Apply)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index; read the individual `docs/schema/*.md` files as each iteration's work directs.
3. Read the 205 audit output — this is the direct input for the current session:
   - `sessions/205. Code Review & Cleanup (Audit).md` — the audit spec with the pre-loaded findings.
   - `sessions/archived/205. Code Review & Cleanup (Audit) — Log.md` — the full W6 / W7 tables and open flags. The 206 session prompt reproduces these for convenience but the log is authoritative if any entry is ambiguous.
4. Read the prior apply-session precedent: `sessions/archived/179. Code Review & Cleanup (Apply).md` and `sessions/archived/179. Code Review & Cleanup (Apply) — Log.md`. 179 is the structural model for this session — walk-the-flags-first pattern, iteration slicing, test-per-extraction discipline.
5. Read the files grouped by iteration in the session prompt's W6 / W7 tables. Pull each file only when the iteration that touches it begins — do not pre-read everything up front. The walkthrough will confirm which extractions are in scope before any file is opened.
6. Open `sessions/206. Code Review & Cleanup (Apply).md` and read the session prompt carefully. Pay particular attention to the Open Flags block — those are decision points that must be resolved with the user before any iteration begins.
7. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
  - If the session touched any code under `resources/js/`, also run `npm run build` and confirm it succeeds. Vue/JS build failures are silent to the Pest suite.
  - If the session touched widget SCSS/CSS/JS (anything under `resources/scss/widgets/`, the `css` or `js` columns of any `widget_types` row, or any file referenced in a `WidgetType.assets` array), also run `docker compose exec app php artisan build:public` so the public widget asset bundle is rebuilt.
  - If the iteration touched Playwright fixtures or any importer wizard code path, run `npm run test:e2e` in that iteration before pausing.
- **Tests for new behaviour:** refactor iterations that introduce a new seam (services, enums, observers, base classes) get test coverage added or updated. Pure structural extractions with no behaviour change rely on the existing suite — note that explicitly in the iteration's PR/commit message.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it.

Claude then drafts the next session's documents. The next session after 206 is **207 — Column Layout Inspector — Appearance Unification**, carved out of Open Flag A mid-session because the scope proved too large for 206. The migration squash (originally queued as 207) has been displaced one slot to **208 — Migration Squash & Code Optimization**. Both 207's base prompt and session prompt were drafted mid-session during the Flag A walkthrough; check `sessions/207. base-prompt.md` and `sessions/207. Column Layout Inspector — Appearance Unification.md` — if they already exist, skip the draft step.

208 will still follow the squash procedure from sessions 062 / 082 / 108 / 142 / 181 (schema snapshot → collapse migrations → verify `migrate:fresh --seed` identity → archive old migrations) and pick up any performance-optimization opportunities surfaced during 206 and 207's refactors. The squash baseline must fold in the `page_layouts.appearance_config` migration from session 207.

- **Next session base prompt**: `sessions/207. base-prompt.md` was drafted mid-session. If absent, copy `sessions/template-base-prompt.md` and recreate using the read-list already agreed during the 206 walkthrough.
- **Next session prompt**: `sessions/207. Column Layout Inspector — Appearance Unification.md` was drafted mid-session. If absent, recreate using `sessions/template-session-prompt.md` as the format reference and the 206 Flag A resolution as the source spec.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/206. Code Review & Cleanup (Apply) — Log.md`. Use `sessions/template-session-log.md` as the format reference. Include a per-iteration what-was-built summary, the final set of applied W6 / W7 items, the outcome of each open flag, and any deferred items with reasons.
- **Update session-outlines.md**: move this session's title into the Completed Sessions table. Update the "Code Review & Cleanup" stub to reflect that 205/206 completed. Add a stub (or Completed-Sessions row, depending on state at close) for session 207 — Column Layout Inspector — Appearance Unification. Renumber the "Migration Squash & Code Optimization" stub to session 208. Review the "Column Widget UI Improvements" Post-Beta 1 stub for phrasing — "column widget" is incorrect terminology; columns are layouts, not widgets.
- **Archive the previous session**: move all files matching `sessions/205. *.md` into `sessions/archived/`. This includes the previous session's base prompt, session prompt, and log. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-206/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
