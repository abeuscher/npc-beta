# Base Prompt — Session 273

We are about to begin a new session: **273. Code Review & Cleanup (Apply)**.

This session is the third of four in the housekeeping cluster opened in `session-outlines.md` § "Code Review & Cleanup — 4-session housekeeping cycle":

- **271 — Audit Part 1 (Foundations + Quantitative)** ✅ closed.
- **272 — Audit Part 2 (Subsystem Deep Walks)** ✅ closed — produced the merged W7 / W8 / Open Flags inputs this session consumes.
- **273 — Code Review & Cleanup (Apply)** *(this session)* — open-flag walkthrough + W7 / W8 picks, iteration-sliced commits.
- **274 — Migration Squash & Code Optimization** — 208-shape; absorbs the squash baseline plus opportunistic cleanups.

This session is **NOT boundary-touching** — no Fleet Manager contract change. CRM contract stays at v2.3.0 (per session 268). One W8 row (FM contract version-stamp consolidation) does add a CI-enforced parity test against the spec doc, but the spec doc itself is read-only this session.

The 206 session is the canonical procedural precedent for the apply half. This prompt follows the 206 shape exactly: Open Flags block at the top, W7 table, W8 table, suggested iteration shape, testing and security checklists.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference; do not infer format from previous logs.
2. Read `sessions/session-outlines.md` § "Code Review & Cleanup — 4-session housekeeping cycle" — confirm 273's position (271 + 272 ✅ closed; 274 still queued).
3. Read `sessions/release-plan.md` execution-order list — confirm the cluster's positioning between A1e' (closed at 270) and the next queued entry (C1 — Notes Permissions feature half).
4. Read `sessions/272. Code Review & Cleanup (Audit Part 2 — Subsystem Deep Walks) — Log.md` end-to-end. **This is the canonical input.** The merged W7 / W8 tables, Open Flags block, per-subsystem findings, and W11 / W12 outputs all live here (271's broad-pass output merged in at 272's close).
5. Read `sessions/archived/206. Code Review & Cleanup (Apply).md` end-to-end — the canonical procedural precedent for the apply half. Note the iteration-sliced commit pattern, the per-flag walkthrough, and the carve-out shape (Flag A → 207).
6. Read `sessions/archived/206. Code Review & Cleanup (Apply) — Log.md` end-to-end — how the 206 walkthrough produced per-iteration deliverables, the W6 / W7 outcome tables, and how a flag was carved out mid-session into a dedicated successor (Flag A → 207 with prompts drafted live).
7. Read `docs/fleet-manager-agent-contract.md` end-to-end — read-only, but W8 row #3 (the FM contract version-stamp consolidation) wants a Pest test asserting the controller constant matches the spec doc's `Contract Version:` line.
8. Read `docs/app-reference.md` for environment names, container names, and view-to-file mappings. Read `docs/schema/README.md` for the table index — no schema changes this session.
9. Open `sessions/273. Code Review & Cleanup (Apply).md` and read the session prompt carefully. Walk through the Open Flags block first, ratify or refine each flag's resolution with the user, then walk W7 / W8 in the iteration order suggested.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from 272

- **CRM contract at v2.3.0** (per session 268) — unchanged.
- **Fast test suite green** at the **272 close baseline** (confirm at session start; expected ≈2164 ± small delta).
- **Slow test suite green** at **16 passed** (carried from 268; not re-run at 271 or 272).
- **Playwright suite** post-271/1 fixes: 40 passed / 2 failed flaky (FilePond upload-completion polling at `tests/e2e/helpers/wizard.ts:61` — Flag W10/A, scoped to land in 273).
- **All 21 admin Resources gated with canAccess()** (W6/A closed in-session at 271).
- **Stale `full_width` references** removed at 271 close.
- **Dead-import sweep** total: 24 imports removed across 19 files (22 at 271 close + 2 at 272/1 close — `Str` from ImportNotesProgressPage, `Contact` from ImportInvoiceDetailsProgressPage).
- **Orphan blade `posts/show.blade.php`** deleted at 271 close.
- **Help-doc / route gaps** closed at 271 (B1c importer routes, Notes Resource routes, retired tax-receipts doc deletion).
- **W11 outlier list + W12 inline-code-in-markup table** — finalised in 271's log; 273 walks them as candidate extraction targets.
- **W7 / W8 tables and Open Flags** — finalised in 272's log; 273 walks them as the primary deliverable.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **No schema changes; no Composer changes; no migrations.** Same rule as 271 / 272 — the squash absorbs migration changes at 274.
- **No agent contract change.** No `docs/fleet-manager-agent-contract.md` body edit, no version bump. The W8 row #3 work *adds a CI-enforced parity test* against the spec doc; the spec doc itself stays read-only.
- **Pause and ask** any time a walkthrough decision deviates from this prompt's pre-resolved guidance. The user's standing direction at 272 close was "accept all recommendations" — that resolved every flag's path before this session opens, but specific iteration ordering, test coverage shape, or scope tightening can still surface at the walkthrough.
- **Bug fixes are out of scope.** Catching a real bug during apply triggers an Open Flag, not a fix — unless the fix is one line and obviously correct.
- **Long runtime expected.** Six suggested iterations plus a walkthrough opening. Split as needed (`session-273/1`, `session-273/2`, …); each iteration ships a self-contained slice that the user can deploy + test before the next iteration starts.
- **Tests for new behaviour.** W8 #2 (observers) + W8 #3 (FM contract parity) introduce new seams and need test coverage. W7 extractions that produce byte-equivalent output rely on the existing suite — note that explicitly in the commit message.
- **When implementation is complete on each iteration**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: **272 close baseline ± a small delta** (W8 #2 adds 4 new observer tests, W8 #3 adds 1 parity test, so expected fast count rises to ≈2169).
- **Front-end builds — only if a safe-fix touches front-end source.** `npm run build` if any `resources/js/**` or `resources/scss/**` change lands; `build:public` if any widget asset surface changes. W12 candidate extractions and the `LayoutColumnSettingsTab` extraction (W7 #5) and the `editor.ts` composable extraction (W7 #4) all touch front-end source.
- **Playwright run** required on the iteration that lands Flag W10/A (FilePond polling fix) and on any iteration that touches an importer wizard, importer progress page, or `ImporterPage` action. W7 #3 (ImportSessionActions cascade-helper) is the high-risk iteration — run Playwright after it ships.
- **Manual verification not expected** for most iterations (extractions, observers, contract parity test). The W7 #5 (LayoutColumnSettingsTab) and W7 #4 (editor.ts composables) iterations *may* warrant a manual page-builder smoke test — announce when ready, then stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

Standard close gate per `sessions/template-base-prompt.md`. Session-specific notes:

- **Session log:** write `sessions/273. Code Review & Cleanup (Apply) — Log.md`. Document per-iteration what was built, the final disposition of each Open Flag (applied / carved out / won't-fix), the list of W7 / W8 items that landed vs stayed on the table, the W11 / W12 candidates that landed vs stayed on the table.
- **Update `sessions/completed-sessions.md`:** append session 273's row.
- **Update `sessions/session-outlines.md`** § "Code Review & Cleanup — 4-session housekeeping cycle" — mark the 273 child stub as ✅ closed; preserve the 274 child stub untouched.
- **Update `sessions/release-plan.md`:** if a release-plan execution-order entry needs adjusting (likely yes — see Rich-Text Sanitization Hardening carve-out below), tighten it. Otherwise no change.
- **Carve-out for Flag W4c/A (Rich-Text Sanitization Hardening).** The 272 audit confirmed the 250-time stub still applies and is too large to absorb in 273. At 273-close, draft prompts for a dedicated session that resolves it (mirroring how 206-close carved out 207 with full prompts drafted live). Update the standing stub in `sessions/session-outlines.md` § Rich-Text Surface Sanitization Hardening to point at the carved session number; update `sessions/release-plan.md` execution-order to insert the new session at the right position (likely after 274 squash, before C1 Notes Permissions). Confirm the position with the user before drafting the prompts.
- **Draft session 274 documents at close.** Copy `sessions/template-base-prompt.md` to `sessions/274. base-prompt.md`; write `sessions/274. Migration Squash & Code Optimization.md` using `sessions/template-session-prompt.md` as the format reference and the 208-session shape as the procedural precedent. The 274 prompt should follow the 208 shape exactly: Phase 1 inventory → Phase 2 user picks → Phase 3 apply → Phase 4 squash → Phase 5 verify. Reference lineage in design-decisions block: **062 → 082 → 108 → 142 → 181 → 208 → 274**.
- **No `docs/fleet-manager-agent-contract.md` updates** — non-boundary-touching.
- **Help-doc updates** — none expected for this session; W7 / W8 / Flag work is internal refactor.
- **Archive the previous session:** move all files matching `sessions/272. *.md` into `sessions/archived/`.
- **Commit:** stage all changed files (close-gate doc bundle + any iteration commits not already merged) on the final `session-273/N` branch. Notify the user. Do not push.
- **No FM-side handoff** — non-boundary-touching.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

The style rules in `sessions/template-base-prompt.md` apply. Session-specific:

- **Iteration commit messages describe the iteration's deliverable** — e.g. "Iteration /3 — Org cluster observers (W8 #2 / Flag W6/B)". Don't bundle unrelated extractions into one commit; each kind of refactor gets its own commit so the reviewer can bisect.
- **W7 / W8 outcome columns** in the log follow the 206 shape exactly: # / Item / Outcome (Applied in /N | Deferred | Skip).
- **Each Open Flag's outcome** is one row in the log's flag-outcomes table: Flag | Resolution | Applied in.
- **Carve-out artifacts** (the new Rich-Text Sanitization session's prompts) are non-scope to 273's W7/W8 work but land alongside 273's close-gate commit at user direction — note that explicitly in the log under "Non-scope artifacts produced during the session" (per the 206 log's shape for the 207 carve-out + the fleet-manager-planning-spec inclusion).
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
