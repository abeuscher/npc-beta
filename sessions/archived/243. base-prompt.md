# Base Prompt — Session 243

We are about to begin a new session: **243. Housekeeping Batch 1**.

This is the first of two split housekeeping sessions queued at session 242 close. It bundles six small, scope-bounded paper-cut items that surfaced during recent sessions' manual-test passes but don't justify a dedicated session each. Items were selected for size compatibility (each genuinely small) and high demo / onboarding visibility. The companion stub `Housekeeping — Batch 2` collects the four deferred items (revisit when timing fits or pressure rises).

The session immediately preceding 243 (session 242 — Fleet Manager Agent — Phase 2 Backup Pipeline) closed the Fleet Manager Agent track substantially complete and shipped the v1.2.0 contract bump. **243 is non-boundary-touching** — no Fleet Manager spec doc edits, no Cross-Repo block changes, no `app/Http/Controllers/Api/Fleet/*` modifications. The standing cross-repo flag in this base prompt remains active but should not fire on this session's diff.

The session immediately following 243 is queued and named: **244. Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria** (the meta-session that takes `sessions/release-plan-outline.md` and produces the actual pre-Beta-1 release gate). 243 closes out small paper-cut work *before* the release-readiness planning happens so the housekeeping items are not coupled into rehearsals that are not yet defined.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — the Active tracks block, the Cross-Repo block (no changes expected this session), the **Housekeeping — Batch 1** stub itself (the canonical scope for this session), and the **Housekeeping — Batch 2** stub (so you know what is *not* in scope and where to lift items if any prove larger than expected).
3. Read `sessions/242. Fleet Manager Agent — Phase 2 (Backup Pipeline) — Log.md` (or `sessions/archived/242. *.md` if 243 hasn't been close-gated yet to archive 241). 242 surfaced two of the six items in Batch 1 (the backup-pipeline tweaks); the log captures the manual-test context that led to them.
4. Read `docs/app-reference.md` — environment names, container names, view-to-file mappings, and key dependencies. Pay special attention to:
   - The **Backups** section (you will edit Restoration step 5 if `relative_path = base_path()` lands in Phase 2).
   - The **Admin UI — views and their files** tables — `MemoResource.php` for Phase 1 (location not currently in the table; confirm at session time and consider adding the Memos resource row to the CRM group section if missing).
   - The **CMS group** table — `CmsSettingsPage.php` for Phase 3 (the SEO/AEO bundle).
5. Read `docs/schema/README.md` for the table index, then read the individual table files relevant to:
   - Phase 1: the `memos` table file (or whichever schema doc covers the Memo content type).
   - Phase 3: any settings-related schema doc, specifically whether `noindex_global` belongs on a settings table or in `site_settings` JSON.
6. Read the full state of every file this session edits or extends. The session prompt enumerates them per-phase; before any code lands in a phase, read every file in that phase's "files touched" list.
7. Open `sessions/243. Housekeeping Batch 1.md` and read the session prompt carefully. Each phase carries its own design decisions, files touched, and acceptance criteria.
8. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case. **For this session specifically:** no new integrations are expected. Phase 2 touches the existing backup pipeline (already wired in 242). Phase 3's `/llms.txt` is a new public route but follows the existing Laravel route + controller pattern (`SitemapController`, `RobotsController`); reuse that shape.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first. **For this session:** Phase 3's `noindex_global` toggle may need a settings-table column or a `site_settings` JSON entry — design call at session time. If a migration is needed, write it; if a JSON-key addition suffices, do that instead.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Baseline at 242 close: 1684 fast cases passing. This session expects roughly +1 to +3 cases (a Memo factory test for the auto-default, possibly a Phase 3 test for the noindex meta tag injection); confirm at session time.
- **Front-end builds:** Phase 5 (list rendering / indent in admin Quill) and Phase 6 (mobile type scaling) both touch SCSS. Phase 5 may touch `resources/scss/admin.scss` or a Filament-editor-specific partial — admin-side only, the public bundle is unaffected. Phase 6 touches `resources/scss/public.scss` or a typography partial — that's the public bundle. Match the touched path to the build command per the standard rule:
  - `resources/scss/widgets/**` or admin-asset SCSS → `npm run build` for admin / `docker compose exec app php artisan build:public` for the widget bundle.
  - For Phase 5's admin Quill fix, `npm run build` is the right call (admin bundle).
  - For Phase 6's public-site type scaling, `docker compose exec app php artisan build:public` if the change touches widget bundle source; `npm run build` if it's only `public.scss`. Read the touched file's import chain to decide.
- **Tests for new behaviour:** every session that changes application behaviour should include tests unless the session prompt explicitly says otherwise. Most items in this batch are CSS or config — not behavior changes — and don't require tests. Phase 1 (Memo auto-date) and Phase 3 (noindex toggle injection) DO change behavior and should each get a small test.
- **Cross-repo coordination — Fleet Manager agent contract.** **243 is non-boundary-touching by design** — no edits to `app/Http/Controllers/Api/Fleet/*`, the auth middleware, the response schema, the VERSION pipeline, `docs/fleet-manager-agent-contract.md`, or the Cross-Repo block in `sessions/session-outlines.md`. If something in scope unexpectedly turns out to touch the boundary surface, **stop and surface it** — the contract is at v1.2.0 (live as of 242) and the FM-side cache is at v1.1.0 awaiting refresh on FM's next boundary-touching session.
- **Pause for manual testing.** Announce that testing is ready. The manual test loop is small for this session — visual check of each landed item:
  1. Memo create form pre-fills today's date.
  2. Run `docker compose exec app php artisan backup:run`; confirm the resulting zip is smaller and (if `relative_path` landed) stores media at `storage/app/public/...` not `var/www/html/storage/app/public/...`.
  3. Toggle the noindex setting in CMS Settings; view-source on a public page; confirm `<meta name="robots" content="noindex,nofollow">` appears (or doesn't, when toggled off). Hit `/sitemap.xml` and confirm the page+post catalog appears. Hit `/llms.txt` and confirm the AEO file renders.
  4. Open a `text_block` widget in the page builder; set horizontal alignment; confirm the public render aligns correctly. Repeat for any other widget in the audited set.
  5. Open a Quill editor in the admin; create a bulleted list and a numbered list; confirm only one marker type per list and that markers are inside the padded text area.
  6. View any public page on a narrow viewport (or DevTools device emulation); confirm headings and body type scale down at the standard mobile breakpoints.
  Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

The next session is already queued and named: **244. Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria**. The session takes `sessions/release-plan-outline.md` as its seed input. No 244-candidate menu is needed — the session is committed.

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/244. base-prompt.md`, customizing the read list for the Vetting session's specific inputs (the outline doc, the full roadmap, the 242 + 243 logs, any other reference material the session conversation surfaces).
- **Next session prompt**: draft `sessions/244. Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria.md` using `sessions/template-session-prompt.md` as the format reference. Base it on the stub in `session-outlines.md`, informed by what was learned during 243 (especially anything that touches release-readiness rehearsals).

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/243. Housekeeping Batch 1 — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 243's row to the index table.
- **Update `sessions/session-outlines.md`**: remove the **Housekeeping — Batch 1** stub (the work is now complete and lives in the session 243 log). Leave the **Housekeeping — Batch 2** stub in place for whenever those items get bundled. Update the **Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria** stub if 243 surfaced any new inputs the vetting session should consider.
- **No track to update** — 243 doesn't belong to a track.
- **Production deployment notes in the log**: per item, name any operator action required after deploy. Phase 1 (Memo auto-default) — none. Phase 2 (backup config) — none beyond `.env` already supporting `BACKUP_DISKS`. Phase 3 (noindex toggle) — operator opens CMS Settings and decides whether to enable; default off. Phase 3 (sitemap audit) — none unless a code change lands. Phase 3 (`/llms.txt`) — none. Phases 4-6 — frontend behavior only, no operator action.
- **Archive the previous session**: move all files matching `sessions/242. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, the completed-sessions index, the 244 base prompt + session prompt, and any files moved into `sessions/archived/`), commit on the current `session-243/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array. (None of Batch 1's items add new admin pages.)
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`. (None of Batch 1's items add new Filament pages.)
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
