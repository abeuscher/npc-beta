# Base Prompt — Session 253

We are about to begin a new session: **253. Fleet Manager Pivot Planning Session**.

This is a **drop-in planning session** triggered by an FM-side architectural decision that needs CRM-side absorption. A conversation at FM session 013 close surfaced that the existing FM architecture leaves it as a single point of catastrophic compromise (FM holds encrypted private keys for every install on its filesystem under `APP_KEY`, with no documented recovery path). FM-side has chosen a five-component pivot to make FM compromise recoverable on a documented timeline — three of those components have CRM-side dependencies (an ops script, a recovery runbook, and additive language in the contract spec doc). This session lands them OR sequences them into existing release-plan entries.

**Possibly boundary-touching.** The HTTP contract surface does NOT change and no version bump is expected — but `docs/fleet-manager-agent-contract.md` does get additive language in its Security Posture section, which is documentation-touching to the contract spec. Per the cross-repo discipline, that triggers a CHANGELOG entry and a Cross-Repo block update in `sessions/session-outlines.md`. Treat as boundary-touching for protocol purposes; the version field stays at v2.1.0.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md` — the 11 discipline rules + § A2 (Fleet Manager — node operations parity) + § A1b (recently-closed v2.0.0 mTLS migration). Skim the Out-of-gate register for context on what's been deferred.
3. Read `sessions/session-outlines.md`:
   - **Active tracks block** — Fleet Manager track is active; this session may add to it.
   - **Cross-Repo: Fleet Manager / CRM block** — this session likely updates it (last boundary-touching session, pending changes, deferred decisions).
   - Any existing FM-related stubs.
4. Read `sessions/tracks/fleet-manager-agent.md` — track status snapshot, phase retrospectives, forward plan. Pivot may add a new phase entry or an absorbed-decision retro line.
5. Read `docs/fleet-manager-agent-contract.md` end-to-end — particularly the **Security Posture** section (the target of the additive language) and the **CHANGELOG** at the bottom (where the v2.1.0 entries live and where this session's revision lands).
6. Read `docker-compose.prod.yml` and `docker/nginx/prod.conf` for context on where the trusted client cert is bind-mounted (`/opt/nonprofitcrm/nginx-certs/fm-client.crt` → `/etc/nginx/certs/fm-client.crt` in-container). The rotation script writes here.
7. Read `docs/runbooks/fleet-manager-cert-paste.md` (the prod-side runbook for the initial cert paste) for tone, format, and operator-procedure conventions. The recovery runbook this session may produce mirrors that style.
8. Open `sessions/253. Fleet Manager Pivot Planning Session.md` and read the session prompt carefully — it captures the FM-authored brief, the four open questions to resolve at session start, the three CRM-side deliverables, and the sequencing options.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.1.0.** Fleet Manager `/api/logs` shipped at session 251. This session may add an additive documentation revision to the spec doc but no version bump.
- **CI test suite is green** as of session 252 close (Test Suite CI Cascade — Root-Cause & Fix). The cascade was structural test isolation (an artisan command that ran `migrate:fresh --seed` from inside Pest, polluting the test DB). Session 252 removed `app:reset` and reopened the orphan-media-cleanup stub; the bug remains open per the stub in `session-outlines.md`.
- **Public demo droplet is live** at `rectanglemachine.com` (`147.182.214.147`), running off the `release/public-demo` branch. Standalone deploy infrastructure plus `docs/deployment/digital-ocean-droplet-install.md` runbook landed during session 252. If this session's deliverables affect the install flow, update the runbook in the same commit.
- **Bind-mount edit gotcha is documented** at [feedback_bind_mount_edit_breaks_container.md](../.claude/projects/-home-al-nonprofitcrm/memory/feedback_bind_mount_edit_breaks_container.md). Applies to this session if the rotation script edits `nginx-certs/fm-client.crt` in-place — consider whether `docker compose exec nginx nginx -s reload` works against a bind-mounted file edit, or whether the script needs to write a temp file and `mv` atomically.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **This is a planning session first, implementation second.** The four open questions in the session prompt resolve at session start with the user. Do not start writing the rotation script, runbook, or spec doc edits before those decisions land — the answers shape the artifacts.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. The four open questions are the explicit decision points; if a fifth surfaces during implementation, surface it.
- **Surface architectural choices before going deep.** The rotation script's reload mechanism (`nginx -s reload` in-container vs. `docker compose restart nginx` vs. `kill -HUP`) is one such choice — settle before writing the script. The contract spec doc additions are similar — settle the language before editing.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migration expected.** No schema changes; the deliverables are operator-tooling + documentation.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The deliverables are mostly non-test-touching (script + runbook + doc) — expect zero new tests unless the rotation script gets a Pest test for the cert-validation logic (decide during the session).
- **Front-end builds — when to run which:**
  - This session does not touch front-end source. **No build expected.** If the session somehow grows to touch admin CSS or widget assets (unlikely), apply the standard rules.
- **Cross-repo coordination — Fleet Manager agent contract.** This session is **boundary-touching for protocol purposes** (spec doc gets additive language). When the spec doc is edited, also update:
  - the spec doc's **CHANGELOG section** at the bottom (revision entry, no version bump);
  - the **Cross-Repo: Fleet Manager / CRM block** in `sessions/session-outlines.md` — bump the "Last boundary-touching session in this repo" to 253, resolve the pivot in "Pending boundary changes," update the threshold-derivation deferral note if relevant.
  - Notify the user that FM-side will need to re-WebFetch the canonical contract from this repo at the next FM-side boundary-touching session. The standing two-repo coordination protocol applies (see `sessions/fleet-manager-planning-spec.md`).
- **Verify objective outcomes yourself; pull the user in for judgment.** The objective deliverables (the rotation script's cert-parse validation, nginx reload exit code, the runbook's command sequences) — close the loop yourself by running them against the local stack or the demo droplet. The judgment calls (the runbook's clarity, the spec language's framing, the sequencing decision) — those are the user's.
- **Pause for manual testing only when human judgment is required.** Most of this session is decision-making and writing — surface decisions, write artifacts, verify what's verifiable, then announce the session is ready for review.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and review are complete, Claude stops and waits.

The next session after 253 is **254 — Importer Mapping Page UX** (E2). The 254 base prompt + session prompt are already drafted at `sessions/254. base-prompt.md` and `sessions/254. Importer Mapping Page UX.md` (renumbered from the original 253 drafts when the FM pivot displaced them). 253's close updates 254's "Starting state" section if any deliverable from 253 is relevant context for 254 (probably not — the FM pivot artifacts don't intersect the importer surface).

If 253 surfaces additional FM-side coordination work (e.g., the contract spec update opens a follow-on conversation that wants its own session), draft that prompt and slot it into the execution order at the close-gate.

### Phase 2 — Close

When the user says to close (after 254's prompt is reconfirmed):

- **Session log**: write `sessions/253. Fleet Manager Pivot Planning Session — Log.md` against `template-session-log.md`. Capture the four open-question resolutions, the sequencing decision (A/B/C), every artifact that landed (or was deferred), and the cross-repo coordination state.
- **Update `sessions/completed-sessions.md`**: append session 253's row.
- **Update `sessions/release-plan.md`**:
  - If sequencing option A or B was chosen: add the absorbed work as a sub-bullet under A2, OR add a new entry (A1c or similar) per the chosen option.
  - If sequencing option C: add a sub-bullet to A2 noting the script + runbook are folded in, and note the spec update absorbed in 253.
- **Update `sessions/session-outlines.md`**:
  - **Cross-Repo block**: bump "Last boundary-touching session in this repo" to 253; resolve the FM Security Posture Pivot under "Pending boundary changes"; document the FM-side absorption expectation.
  - **Active tracks block**: update the Fleet Manager track entry's last-update note to reflect 253.
  - Add or update any FM-related stubs (e.g., the post-Beta-1 follow-on around CRM-side super-admin audit sink, if that conversation reopens here).
- **Track doc**: update `sessions/tracks/fleet-manager-agent.md` — bump the status snapshot date and add a phase retrospective entry for the absorbed pivot if it's substantial enough.
- **Spec doc**: confirm `docs/fleet-manager-agent-contract.md` carries the additive Security Posture language and the CHANGELOG revision entry.
- **Memory file** if a discipline-level rule emerges (e.g., a pattern for handling FM-driven recovery procedures). Capture under `feedback_*.md` per existing convention.
- **Production deployment notes**: if the rotation script ships in this session, note that operators of existing prod installs may want to scp the script to `/opt/nonprofitcrm/bin/rotate-fm-cert.sh` (or wherever the install sequence settles) — same manual-placement pattern as the nginx conf and fm-client.crt.
- **Update 254's "Starting state" section** if 253's deliverables are relevant context for 254.
- **Archive the previous session**: move all files matching `sessions/252. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-253/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing conventions exactly — `docs/runbooks/fleet-manager-cert-paste.md` is the model for the recovery runbook; existing `bin/*.sh` scripts (e.g., `bin/dev-certs.sh`) are the model for shell-script style; the spec doc's existing Security Posture section is the model for the additive language.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones — the contract spec doc is an edit; the rotation script and runbook are new files.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- **Cross-repo discipline applies actively** — if the implementation surfaces ANY change beyond additive Security Posture language (a new endpoint, a response shape change, a bind-mount path change), pause and reconsider the version-bump question before continuing.
