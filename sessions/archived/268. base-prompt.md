# Base Prompt — Session 268

We are about to begin a new session: **268. Fleet Manager Contract v2.3.0 — Backup Blob Download Endpoint**.

This session lifts working-set entry **A1e** at execution-order position 22 — the position-22 slot was lifted at 266 close from FM session 020's follow-on planning thread but skipped at 267 in favor of E10 (Full-Width Architecture Enforcement) at the user's direction. We come back to A1e now. It adds a single mTLS-gated HTTP endpoint (`GET /api/backup/blob`) that streams the most recent backup zip from the configured backup destination disk to the FM caller. The endpoint is the CRM-side prerequisite for FM 021 + FM 022 (restore-to-fresh-node primitive — the (c) cell of A2's success-criterion matrix).

This session is **boundary-touching** — Fleet Manager contract bumps v2.2.0 → v2.3.0 (additive only; v2.2.0 consumers continue working unchanged).

The session is intentionally smaller than A1d (session 263) — no integrity-guard logic, no Artisan invocation, no `request_terminate_timeout` work. Just locate the freshest blob and stream the bytes with the right headers.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference; do not infer format from previous logs.
2. Read `sessions/release-plan.md`:
   - All 11 discipline rules.
   - Track A § A1e (lifted at 266 close) — confirm the entry is canonical and the success criterion still names this session's contract surface.
   - Track A § A2 — confirm A1e is referenced as the CRM-side prerequisite for A2(c).
   - Execution-order list — confirm A1e at position 22; E10 ✅ now at 23; E11 at 24.
3. Read `sessions/session-outlines.md` Cross-Repo block — confirm contract version 2.2.0 going in; this session bumps it to 2.3.0.
4. Read [`docs/fleet-manager-agent-contract.md`](../docs/fleet-manager-agent-contract.md) end-to-end. Pay attention to the `/api/backup/trigger` section (the immediate-precedent endpoint this session sits next to) and the CHANGELOG (newest-first convention).
5. Read [`app/Http/Controllers/Api/Fleet/BackupController.php`](../app/Http/Controllers/Api/Fleet/BackupController.php) end-to-end — the `trigger` action ships in this controller; the new `blob` action either lands as a sibling method here or as a new `BackupBlobController.php`. Pick a shape at session start (verify-at-start question 4).
6. Read [`app/Http/Controllers/Api/Fleet/HealthController.php`](../app/Http/Controllers/Api/Fleet/HealthController.php) and [`app/Http/Controllers/Api/Fleet/LogsController.php`](../app/Http/Controllers/Api/Fleet/LogsController.php) end-to-end — canonical envelope conventions, throttle decoration, test-side mTLS-gate handling.
7. Read [`docker/nginx/default.conf`](../docker/nginx/default.conf) and [`docker/nginx/prod.conf`](../docker/nginx/prod.conf) — focus on the existing `/api/health`, `/api/logs`, `/api/backup/trigger` location blocks. The new `/api/backup/blob` location follows the same shape with a per-location `fastcgi_read_timeout 600;` override mirroring `/api/backup/trigger` — defensive against multi-GB blobs streaming through PHP buffer backpressure to slow FM-side networks (the default 300s ceiling is between-packets, not total request, but a single read window stalling past 300s is plausible and cheap to defend against).
8. Read [`config/backup.php`](../config/backup.php) — focus on the `destination` block (specifically `disks` driven by `BACKUP_DISKS`) and `backup.name`. The `BackupDestination::create($disk, $backupName)->backups()->newest()` lookup uses both.
9. Read [`tests/Feature/Api/Fleet/BackupTriggerEndpointTest.php`](../tests/Feature/Api/Fleet/BackupTriggerEndpointTest.php) — canonical test pattern for mTLS-gated endpoints under this contract. Specifically: how the mTLS gate is exercised (or whether it's deferred), the throttle test pattern, the response-envelope assertion shape, the `Storage::fake()` setup pattern.
10. Read [`tests/Feature/Api/Fleet/HealthEndpointTest.php`](../tests/Feature/Api/Fleet/HealthEndpointTest.php) and [`tests/Feature/Api/Fleet/LogsEndpointTest.php`](../tests/Feature/Api/Fleet/LogsEndpointTest.php) if present — sibling test patterns to mirror.
11. Read `sessions/archived/263. Fleet Manager Contract v2.2.0 — Backup Trigger Endpoint — Log.md` — the implementation retrospective for the immediately preceding contract bump. Pay attention to the nginx single-file bind-mount caveat ("up -d --force-recreate, not restart") and the Cross-Repo block update mechanics.
12. Read `sessions/267. Full-Width Architecture Enforcement — Background and Content Split — Log.md` (the immediate predecessor) — fast-suite baseline (expect 2152) for this session's growth-target comparison.
13. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
    - **Throttle setting.** `60,1` (matching `/api/logs` since blob downloads are cheap relative to backup-trigger's 6,1) is the proposed default. Confirm this is right rather than 6,1 (matching `/api/backup/trigger`, framing blob fetches as "expensive operator-driven actions"). The blob fetch is the FM-side consumer's *one read after a successful trigger* — bursty would be a 1-2 retry sequence, not sustained polling. 60/min has plenty of headroom.
    - **Source-disk fallback ordering.** Behavior decided going in: `local` first if it's in `BACKUP_DISKS`, then fall through to whatever else is configured (e.g., `spaces`). Confirm by enumerating the actual `BACKUP_DISKS` env var values currently used in dev + prod and writing the ordering to honor those. The contract spec must document the rule so FM consumers know which disk's blob they're getting.
    - **Streaming mechanism.** Laravel offers `Storage::download($path)`, `response()->streamDownload(fn (), $name, $headers)`, or a manual `StreamedResponse`. The `streamDownload` shape is most ergonomic and handles `Content-Length` automatically when streaming from a known-size source. Confirm at session start which fits the spatie BackupDestination's blob-access pattern best — `Backup::path()` returns a path string usable with `Storage::disk($diskName)->download($path)`, which sets the right headers automatically. This is likely the right shape; verify against the actual API.
    - **New controller vs new method on `BackupController`.** Adding `blob()` as a sibling to `trigger()` on the existing `BackupController` is the smaller change. A separate `BackupBlobController` is the alternative if the trigger and blob actions diverge enough in helpers/state that one class would be awkward. Decide at session start; default to the sibling-method shape unless audit surfaces a reason to split.
    - **Filename in `Content-Disposition`.** Spatie blob filenames look like `nonprofitcrm-2026-05-08-12-30-00.zip` (default `<backup-name>-Y-m-d-H-i-s.zip`). Pass through verbatim — the FM side stores blobs by that filename and round-trips it for restore. No sanitisation needed (no operator input in the filename), but confirm the exact pattern at session start by triggering a backup once and inspecting the resulting filename.
    - **`Storage::fake` vs `BackupDestination` mocking for tests.** Spatie's `BackupCollection::newest()` filters disk contents by filename pattern + timestamp; pre-writing arbitrary bytes via `Storage::fake('local')->put(...)` may not register as a recognized backup. **Write test #1 against a single stub before committing the full ~10-test matrix.** If `Storage::fake` doesn't register the pre-written file, the alternative is mocking `BackupDestination::create()` directly via container binding, or extracting the disk-resolution logic into a thin service the controller delegates to and mocking the service. Document the chosen path in the session log.
14. Open `sessions/268. Fleet Manager Contract v2.3.0 — Backup Blob Download Endpoint.md` and read the session prompt carefully.
15. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.2.0** (per session 263). This session bumps to v2.3.0 (additive: new `/api/backup/blob` endpoint).
- **Fast test suite green** at the **267 close baseline of 2152** (confirm at session start). This session adds tests covering the new endpoint — estimate **~+7 to ~+12 new tests** (success path, no-backup-available 404, server-misconfiguration 500, disk-driver-error 500, disk-fallback ordering, mTLS gate, throttle, method enforcement, filename pass-through, Content-Length presence, Cache-Control header).
- **Slow test suite green** at **15 passed** (assumed unchanged at 267 close).
- **A1d ✅ closed at session 263** (CRM-side `/api/backup/trigger`). FM-side absorption shipped at FM session 020.
- **E10 ✅ closed at session 267** (full-width background/content split + render-pipeline convergence).
- **A1e lifts here** — CRM-side prerequisite for FM 021 + FM 022's restore-to-fresh-node primitive.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **Single-file bind-mount caution.** This session **will** touch `docker/nginx/default.conf` and `docker/nginx/prod.conf` (adding the new location block). Both are single-file bind-mounts; `docker compose restart nginx` will fail with an OCI error after `Edit`. Use `docker compose up -d --force-recreate nginx` instead (per the standing project rule, verified during 263/1 implementation).
- **No PHP-FPM `request_terminate_timeout` work.** A1d needed it; A1e doesn't — the 600s nginx ceiling matches what was already provisioned at 263. Flag if anything surprising surfaces, but no edits expected.
- **No CRM-side schema changes.** This session is application-PHP + nginx + spec-doc; no migrations.
- **No Composer changes.** All new code uses existing dependencies (`spatie/laravel-backup` is already in the lock file at the version A1d used).
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. Specifically: if the disk-fallback rule turns out to need more nuance than the simple "local first, then fall through" framing, surface before committing to a shape.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **Tests required for new behaviour.** New endpoint requires controller-level test coverage per Phase 3.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Baseline is 2152 passed (post-267). Slow group baseline: 15. **Target after this session: ~2159 to ~2164 passed fast** (+7 to +12).
- **Front-end builds — when to run which:** This session is application-PHP + nginx + docs only. **No `npm run build` or `build:public`.** If anything in `resources/js/**` or `resources/scss/**` changes unexpectedly, flag it before continuing.
- **Cross-repo coordination — Fleet Manager agent contract.** This session **is** boundary-touching. The standing two-repo coordination protocol applies: bump `Contract Version` in `docs/fleet-manager-agent-contract.md`, add a CHANGELOG entry above v2.2.0, update the Cross-Repo block in `sessions/session-outlines.md` ("Last boundary-touching session in this repo" + bump version field). FM-side absorbs at FM 021 (build the `BackupBlobClient` mirroring `BackupTriggerClient`) and FM 022 (the restore-to-fresh-node operator affordance).
- **Verify objective outcomes yourself; pull the user in for judgment.**
  - **Objective:** controller streams the right bytes with the right `Content-Type` / `Content-Disposition` / `Content-Length` headers; spec-doc shape matches the actual controller output; mTLS rejection returns 403; throttle returns 429 on the 61st request within a minute; 404 envelope returns when configured disks are all empty of backups; 500 envelope returns when `BACKUP_DISKS` is empty (server misconfiguration, distinct from 404); `migrate:status` clean (no schema drift).
  - **Judgment-required:** the actual filename pattern as it lands in `Content-Disposition` (cosmetics — does it read cleanly to an operator?); whether the 60/min throttle feels right after the live-curl smoke test; whether the 404-vs-500-vs-500 (no-backup / unconfigured / driver-error) split reads cleanly to an FM-side error classifier.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

Standard close gate per `sessions/template-base-prompt.md`. Session-specific notes:

- **Session log:** write `sessions/268. Fleet Manager Contract v2.3.0 — Backup Blob Download Endpoint — Log.md` against `template-session-log.md`. Document the controller shape (sibling vs new class), the streaming mechanism choice (`Storage::download` vs `streamDownload` vs raw `StreamedResponse`), the disk-fallback rule's actual implementation, the throttle decision, the FM-side handoff status, and any in-session absorptions.
- **Update `sessions/completed-sessions.md`:** append session 268's row.
- **Update `sessions/release-plan.md`:**
  - Check off **A1e (✅)** at "closed at session 268." Tighten the success criterion in place to reflect what shipped (the disk-fallback rule, the streaming mechanism, the throttle setting).
  - Confirm A2's success criterion still names "(c) restore a node from a backup blob" with the CRM-side half marked as A1e-shipped; FM-side half remains pending at FM 021 + 022.
  - Confirm the next entry in the execution order is **E11 at position 24** (E10 at 23 already ✅; A1e at 22 closes here).
- **Update `sessions/session-outlines.md`** Cross-Repo block:
  - Bump agent contract version `2.2.0 → 2.3.0`.
  - Bump "Last boundary-touching session in this repo" to 268.
  - Update "Pending boundary changes" to note FM-side absorption pending at FM 021 + FM 022.
- **Spec doc** — `docs/fleet-manager-agent-contract.md` at v2.3.0; CHANGELOG entry above v2.2.0 documenting the new endpoint, response shape, and disk-fallback rule.
- **Findings register** — flag any surprises (spatie BackupDestination quirks, S3/Spaces streaming behavior under `Storage::download`, Content-Length behavior for cloud-disk-sourced files, throttle-test setup gotchas).
- **Help-doc update** — none expected; this is API surface, not admin UI.
- **Archive the previous session:** move all files matching `sessions/267. *.md` into `sessions/archived/`.
- **Commit:** stage all changed files and commit on `session-268/N` branch. Notify the user. Do not push.
- **FM-side handoff:** signal that v2.3.0 is shipped + the `/api/backup/blob` endpoint is live. FM-side absorbs at FM 021 (`BackupBlobClient`) + FM 022 (restore affordance).
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

The style rules in `sessions/template-base-prompt.md` apply. Session-specific:

- Follow existing code conventions exactly. The `BackupController::trigger` action shape is the canonical reference; the new `blob` action slots in alongside it without restructuring either.
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** add historical-blob enumeration; do **not** add per-disk targeting via query params; do **not** add progress streaming; do **not** add Range / resume support; do **not** add CRM-side restore primitives — restore stays manual `pg_restore` per A2(c) framing, and any FM-driven restore is FM-side scope.
- Prefer editing existing files to creating new ones — except for the new test file (`BackupBlobEndpointTest.php`) and possibly a new `BackupBlobController.php` if the verify-at-start audit surfaces a reason to split.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
