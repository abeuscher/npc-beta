# Base Prompt — Session 325 (Events — Calendar Swap)

**Start now.** Execute the reading list below, open the session prompt at `sessions/325. Events — Calendar Swap.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **325. Events — Calendar Swap.**

This is the **first build session of the demo-site work** planned at session 324. It is **session A** in the multi-session plan at `sessions/demo website/demo-site-plan.md` (§3). It sits after the `── PUBLIC WEBSITE COMPLETE ──` milestone (reached at 323 close) and ahead of A2 (still blocked on FM 012), lifted because the demo-site work is unblocked and the owner is ready to build it.

This session is **NOT boundary-touching** — no Fleet Manager surface, no `/api/health` change. CRM contract stays at **v2.3.0**.

**Session shape:** feature-style (widget retirement + new widget). Retire the `EventCalendar` widget entirely (the back-end calendar use case is deferred to post-Beta scheduling work) and build a new server-rendered mini-calendar widget for the events-index right rail — a Carbon-computed month grid with event-density indicators, click-to-scroll, and month-nav, with **all date math server-side and no Vue runtime on the public side.**

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. **Read the demo-site plan** `sessions/demo website/demo-site-plan.md` — the canonical scope + design reference for all demo-site build sessions. §2b (calendar architecture), §3 (session A row), and §4 (EventCalendar retirement surface) are the load-bearing sections for this session. §2c (the `data-tour` anchor convention) governs the anchors this session lays down.
3. Read `sessions/session-outlines.md` — the active-tracks block and the Public Website Complete milestone framing.
4. Read `docs/app-reference.md` for environment/container names and the build-pipeline commands. Read `docs/schema/README.md`; no schema change is expected this session (a widget swap), so no table file should need editing.
5. **Read the retirement surface** so you sweep it comprehensively (enumerated in `demo-site-plan.md` §4): `app/Widgets/EventCalendar/*`, `app/Providers/WidgetServiceProvider.php`, `app/Services/AssetBuildService.php` (the `jcalendar` lib), `resources/js/public.js`, the `widget_types` seeder, `DemoBaselineSeeder`, and the tests that reference `event_calendar` / `jcalendar` (`WidgetManifestTest`, `InlineEditingFoundationSession304Test`, `WidgetJsLibsSession138Test`, `BuilderPublicRenderParityTest`, `WidgetTypesSession098Test`, `WidgetAssetResolverTest`).
6. **Read a comparable existing widget** as the build pattern for the new mini-calendar — `app/Widgets/EventsListing/` (events query, data contract, assets, template, script) and `app/Widgets/ThisWeeksEvents/` (a smaller events widget) are the closest references.
7. Note any drift between this prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules.

---

## Starting state inherited from session 324

- **CRM contract at v2.3.0** — unchanged. Session 324 was planning-only (no application code).
- **Schema baseline:** no schema change at 324; **no schema change expected at 325** (a widget swap — the `widget_types` row removal is seeder/data, not a column change).
- **Fast Pest green at 2694 / 0** (323 close baseline; 324 added no tests). New tests expected this session for the new mini-calendar widget (render, density computation, empty-month) and updates to the tests that reference the retired `event_calendar` / `jcalendar`.
- **Playwright suite** — baseline preserved at 323. No Playwright work expected unless an objective render outcome is faster verified with a short spec.
- **Planning state:** session 324 produced `sessions/demo website/demo-site-plan.md` — the canonical demo-site scope + design doc. The `── PUBLIC WEBSITE COMPLETE ──` divider is marked reached. A006 was retired and deleted at 323 close.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — feature-style session; do not absorb inbox items unless directly in the path of this work.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific notes:

- **Front-end build is required.** Removing the `jcalendar` lib and adding the new widget's assets changes the public bundle and the widget manifest — run `docker compose exec app php artisan build:public` after the asset changes, and `npm run build` if any Vite-compiled source (`resources/js/**`, `resources/scss/**`, `resources/js/public.js`) is touched. The manifest must no longer list `jcalendar` or `event_calendar`.
- **No Vue on the public side.** The mini-calendar is server-rendered Blade (Carbon) + a small vanilla `script.js`. Do not introduce a Vue runtime or vue-cal — that decision was made and rejected at 324 (see `demo-site-plan.md` §2b). No date arithmetic in JavaScript.
- **Lay down the `data-tour` anchor(s)** per `demo-site-plan.md` §2c — the new mini-calendar widget declares its stable anchor (e.g. `data-tour="events-index.mini-calendar"`). No tour code is written this session; just the anchor.
- **widget_types removal mechanism is a decision to settle at the open gate** — migration vs seeder edit, and whether existing demo/dev databases need a data migration to drop already-placed `event_calendar` instances. Bring a recommendation.
- **Surface architectural choices before going deep** — specifically the month-nav mechanism (endpoint-fetch vs pre-rendered ±1-month window) and the widget_types removal mechanism. Settle both at the open gate.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting**. Settle: (1) **widget_types removal mechanism** — migration vs seeder edit + whether placed-instance cleanup is needed; bring a recommendation; (2) **month-nav mechanism** — endpoint-fetch vs pre-rendered ±1-month window; bring a recommendation; (3) confirm the new widget's **handle, label, and right-rail placement** match the index design. One confirmation, then go.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Do not ask, suggest, or pipeline. Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 (attenuate + prepare next session) and Phase 2 (close). Session-specific details:

- **Log file:** `sessions/325. Events — Calendar Swap — Log.md`. Capture the retirement sweep (what was removed and where), the new widget's shape (handle/assets/data contract), the month-nav + widget_types-removal decisions, and the `data-tour` anchor laid down.
- **Bump `VERSION`** to `0.325.<iteration>`.
- **Update** `sessions/completed-sessions.md` (add 325's row) and `sessions/session-outlines.md` (note session A of the demo-site plan is complete; the plan lives at `sessions/demo website/demo-site-plan.md`).
- **Archive the previous session's files** — move `sessions/324. *.md` → `sessions/archived/`.
- **Next session:** session B (Events — listing upgrade) is the natural next per `demo-site-plan.md` §3, but the owner picks at close (B and D are both unblocked).
- **Commit on `session-325/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: follow the existing widget conventions exactly (mirror `EventsListing` / `ThisWeeksEvents` structure); commit messages name the deliverable ("325/N — EventCalendar retirement + server-rendered mini-calendar widget").
