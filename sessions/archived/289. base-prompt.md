# Base Prompt — Session 289

**Start now.** Execute the reading list below, open the session prompt at `sessions/289. Public Marketing Website — Pricing Layout Rebuild.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **289. Public Marketing Website — Pricing Layout Rebuild**.

This session continues the **Public Marketing Website track** (track doc at `sessions/tracks/public-marketing-website.md`). Session 288 closed with the `pricing_chart` widget shipped end-to-end — definition, template, SCSS, generic `repeater` field type, demo data, Pest coverage, and a static thumbnail. This session takes that widget and configures it inside a full Pricing page rebuild, executed against `sessions/public website/pricing-page-spec.md` using the layout-spec-driven pattern that 286 / 287 validated. Sequenced inside the **Public Website Complete milestone** at execution-order position 35 (one slot after the lifted 288 widget-build session).

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** content + visual-build (matches 286 / 287). The deliverables are a `pricing.json` rebuilt against the pricing-page spec, a re-import into the live local DB, a screenshot for the user's review, and gap-report appends for anything the rebuild surfaces. One delta from 286 / 287: Band 2 is a single `pricing_chart` widget instance configured against the spec's three columns, not a layout-container + text-block-cards composite — the new widget is the Band 2 carrier.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. Read `sessions/release-plan.md` § 289 (canonical for scope, success criterion, prerequisites, and artifact).
4. Read `sessions/tracks/public-marketing-website.md` for the track's status snapshot, gap-resolution discipline, and the 288-close retrospective. Confirm the track-doc note acknowledging the Pricing-build → Widget-build → Pricing-build sequence (this session is the second "Pricing-build" half).
5. Read **`sessions/public website/pricing-page-spec.md`** — canonical for cell-by-cell structure, image placements, padding, heading sizes, and cross-section rhythm. The spec's authority section overrides `brief.md` and `copy.md` layout interpretations for the Pricing page. The spec's "Gap callout — Border styling" section is now resolved at the widget level: v1 widget ships with background-tint emphasis only (`#f8f9fa` on the emphasized column); border slot exists in the template but is unfilled until G-pricing-1's `appearance_config` border-knob extension lands. Don't re-litigate borders — apply the widget as it ships.
6. Read **`sessions/public website/pricing-chart-widget-spec.md`** for context on the widget you're configuring. Read `app/Widgets/PricingChart/PricingChartDefinition.php` end-to-end to see the actual schema, defaults, and the `marketingSiteTiers()` helper method that returns the Band 2 column shape verbatim. **Important:** the widget ships its three-tier configuration via `demoConfig()` and the public `marketingSiteTiers()` helper, **not** via `presets()`. The columns field is content-group; preset rules forbid content-group keys. So Band 2's "one-click setup" path is: place the widget on the page, then write the columns array directly into the widget's config in the JSON (the widget's `marketingSiteTiers()` shape is what the spec calls for verbatim). See "Session-specific deltas" below.
7. Read `sessions/public website/copy.md` for any Pricing copy carried there. Read `sessions/public website/brief.md` for the system conventions and gap-surfacing protocol that remain in force.
8. Read `sessions/public website/audit-summary.md` for the system inventory and `sessions/public website/gap-report.md` for the current state of open gaps. G15 / G16 / G17 / G18 / G19 / G20 are open from prior sessions; the Pricing rebuild may exercise them. G-pricing-1 (border styling in `appearance_config`) is open at the system level — at the widget level, 288's resolution path covers it.
9. Read `sessions/public website/reference-pricing-table.png` for visual rhythm context (Band 2). Structural inspiration only — surface decoration is not borrowed.
10. Read `sessions/public website/about.json` and `sessions/public website/about-layout-spec.md` as the most recent worked example of the spec-driven approach (closer to this session's shape than the home, since About used the four-sibling-layout workaround for stacked text widgets — Pricing's à la carte band may want the same pattern). The format-1.1.0 JSON shape lives in any of `home.json` / `about.json` — copy from those.
11. Read `docs/app-reference.md` for the page-builder component map and `docs/schema/README.md` for the table index. No schema changes are expected this session.
12. Open `sessions/289. Public Marketing Website — Pricing Layout Rebuild.md` and read the session prompt carefully.
13. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

**Note on the existing pricing page:** Pricing is effectively greenfield for this session — there is no prior real Pricing page to preserve. (The user has a scratch page in the local DB used to test the new `pricing_chart` widget at session 288 close; that's a widget-test artifact, not the deliverable, and not authoritative for any structural decisions.) Build `pricing.json` from scratch against the spec; if the import overwrites a stub page with the same slug, that's the intended behavior.

---

## Starting state inherited from 288

- **CRM contract at v2.3.0** — unchanged. Session 288 was a widget build with no boundary surface touched. This session is content-only — also no boundary surface.
- **Schema baseline:** no schema changes since 280-close. Session 288 added a `widget_types` row via `WidgetTypeSeeder` — no migration; `docs/schema/` untouched.
- **Fast test suite at 2416 / 0 sequential** (288 close baseline). The pre-existing `DashboardBuilderApiControllerTest` failure that pre-dated 287 / 288 is no longer reproducing — the suite is fully green. No new tests expected this session — content-only work; regression-check after the re-import suffices.
- **Playwright suite** — baseline preserved. No Playwright in this session; the page-capture harness still lands at the track's close (now session 292, shifted from 291 by the 288 lift).
- **Planning state:** Public Marketing Website track active; PMW1 closed at 284; CMS fixes closed at 285; home rebuild closed at 286; About rebuild closed at 287; pricing_chart widget closed at 288. The Pricing layout lands here. After Pricing, expected sequence is Contact (290), Demo (291), Page-capture harness + track close (292).
- **CMS surface area carried forward (resolved at 285, exercised through 286 / 287):**
  - Layout-level `appearance_config` round-trips through export/import (G1 ✅).
  - TextBlock carries `ctas` + `cta_alignment` (G2 ✅). The à la carte band's optional CTAs and the final CTA band can use this.
  - `secondary-dark` button variant for dark/gradient backgrounds (G3 interim ✅). Used by the final CTA band's secondary text-link per the spec.
  - Typography defaults tuned (G13 / G14 ✅).
  - Image widget carries `aspect_ratio` + `max_width` (from 285).
- **New surface from 288:**
  - **`pricing_chart` widget** at `app/Widgets/PricingChart/`. Renders N columns side by side as CSS subgrid; per-column `emphasize` toggle for background-tint emphasis; CTAs pinned via subgrid; mobile collapse to single column; v1.1 border slot built but not lit. Schema: `eyebrow_label` / `heading` / `subheading` / `columns` (repeater) / `footnote` / `heading_alignment` / `gap`. Each column has: `emphasize` / `eyebrow` / `title` / `price` / `lead_content` / `attribute_rows` (nested repeater of label/value) / `ctas` (existing buttons field).
  - **Generic `repeater` field type.** Vue inspector picks it up via `RepeaterField.vue` + `RepeaterRowField.vue`; nested fields dispatch to existing field components and recurse into nested repeaters via dynamic import. Lives entirely in `config_schema` JSON — no `widget_types` column changes. Lint test (`WidgetDefaultsLintTest`) extended with a `repeater` case that allows array defaults.
  - **`PricingChartDefinition::marketingSiteTiers()`** — public helper returning the Band 2 three-column configuration verbatim (Instant Demo / 7-Day Trial / Monthly with Monthly emphasized + Recommended eyebrow). This session calls into this shape when authoring the JSON: paste the array literal into the widget's `config.columns`, then adjust labels/CTAs to match the live spec (the helper's content is already spec-aligned at the time of the widget build, but verify against the current spec before serializing).
- **Open from earlier sessions:**
  - **G15 / G16** (Hero-in-cell rendering quirks). The Pricing hero (Band 1) is gradient + text + image — same shape as About Band 1, where the workaround is Text widget + Image widget rather than Hero. Default to that pattern; flag continued G15 / G16 only if a Hero placement is unavoidable.
  - **G17** (TextBlock CTA-only band placeholder). The final CTA band (Band 5) is heading + body + two CTAs in a single Text widget — not CTA-only, so G17 doesn't manifest. À la carte cells likely don't carry CTAs either.
  - **G18** (Image cell-placement controls). Same full-cell-fill default applies to Band 1's hero portrait and Band 4's PII supporting image.
  - **G19 / G-pricing-1** (no `appearance_config` border knob). The pricing-chart widget's border slot is built in but unfilled — Band 2 ships with background-tint emphasis only per the spec's accepted v1 path. No new gap action this session unless the result reads visibly worse than expected.
  - **G20** (`.widget--text_block { height: 100% }` collides with stacked siblings in single-column layouts). Pricing's à la carte band (Band 3) is a 2×2 grid of Text widgets — that's stacked-siblings-in-multi-column, NOT stacked-siblings-in-one-cell, so G20 doesn't manifest. If the spec's heading-then-grid composite triggers it, apply the four-sibling-layout workaround validated on About Band 2.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — content sessions don't absorb housekeeping items unless a finding directly ties to pricing-page rendering. Touch only if a finding surfaces inside this session's scope; otherwise carry forward.

---

## Process rules for every session

- **Before writing any code or JSON**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. *Session-289 note: no integrations in scope. Pricing's à la carte band has no form. The hero CTAs and Band 5 CTAs route to `/demo` and `/contact` per the spec; both are existing routes (Demo lands at session 291, Contact at 290 — neither resolves yet, which is expected per the link convention). Do not log unresolved internal links as gaps; the user is handling that pass before the site goes live.*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** Most likely 289-specific candidates: the Band 2 columns content (verify the widget's `marketingSiteTiers()` shape matches the current spec; if the spec moved since 288 close, decide whether to update the helper or override per-instance and note); image placeholder picks for the hero portrait (Band 1) and PII supporting image (Band 4); G20 manifestation in any sub-band that might require the four-sibling-layout workaround.
- **Adapt to drift; don't ask about it.** The pricing-page spec is recent; the widget is fresh from 288. Drift expected at code-level fine detail (helper names, field-key spelling). Default to silent adaptation + a one-line work-log note.
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that propagate across bands or set a precedent for Contact / Demo. Local edits — just decide and note.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them. *Session-289 note: no migrations expected.*
- **Update `docs/schema/`** after writing any migration. (No-op this session.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 288 close baseline of **2416 / 0**. No new tests expected; this is a regression check.
- **Front-end builds.** No SCSS / JS expected this session. The widget is already bundled (288 ran `build:public`). Don't run builds if not needed. If the rebuild touches the widget's SCSS or any `widget_types.assets`-referenced file (it shouldn't — content-only), then `build:public`.
- **Tests for new behaviour:** no new tests expected. Content-only sessions skip the tests-for-new-behaviour rule per the base-prompt convention.
- **Fast vs slow classification:** N/A.
- **Playwright artifact hygiene:** N/A this session.
- **Cross-repo coordination — Fleet Manager.** N/A.
- **Verify objective outcomes yourself; pull the user in for judgment.** Objective surfaces: the JSON imports without errors; the page renders at `/pricing`; the bands appear in spec order; the cross-section rhythm matches the spec's table; Band 2 renders as a single `pricing_chart` widget instance with three columns and Monthly emphasized. Close those loops via re-import + curl + screenshot. The visual surface — whether the rebuild reads like the references, whether the widget's v1 emphasis is subtle enough or too subtle, whether the footnote-as-italic workaround reads — is the user-judgment surface. Surface for review at session-end.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that Pricing is rebuilt against the layout spec, re-imported, and ready for visual review at `/pricing`. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/290. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to **290. Public Marketing Website — Contact Layout Rebuild**. Same spec-then-execute pattern. Pre-pass discipline continues — the Contact layout spec lands as `sessions/public website/contact-page-spec.md` before 290 starts.
- **Next session prompt** — draft against the layout-spec-approach pattern.

**Pre-next-session ask in the close handoff.** Contact is greenfield per the brief. The only optional pre-input is a Contact layout spec — surface a reminder: *"If you'd like an outside-agent pre-pass on the Contact layout spec, place it as `sessions/public website/contact-page-spec.md` before session 290 starts."*

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/289. Public Marketing Website — Pricing Layout Rebuild — Log.md` using `template-session-log.md` as the format reference. Document: per-band decisions (image placeholder picks, the Band 2 widget configuration approach, alignment calls, fallback paths taken), gap-report appends, manual-testing observations from the user's review, validation that the layout-spec approach continued to deliver predictable results, and whether the pricing_chart widget rendered acceptably in its first real-page placement.
- **Update `sessions/public website/gap-report.md`:** append any new rows surfaced. Note continued G15 / G16 / G17 / G18 / G19 / G20 manifestations under their existing rows. Update G-pricing-1's status with a note on whether v1 background-tint emphasis read acceptably in the live page.
- **Update `sessions/completed-sessions.md`:** append session 289's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status to reflect 289 closure and the next phase queued at 290.
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (last update date; pricing rebuild complete; Contact queued at 290).
- **Update `sessions/release-plan.md`:** mark session 289's entry ✅ closed at session 289.
- **Archive the previous session:** move all `sessions/288. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-289/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Pricing rebuilt against layout spec + screenshot + gap-report appends".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **The rebuild is operator-readable.** Image placeholder labels in widget labels or custom fields are how the eventual photo swap stays mechanical; honor the spec's image-naming convention (`pricing-hero-portrait`, `pricing-pii-supporting-image`).
- **Hero-cell carrier defaults to Text widget + Image widget**, mirroring About Band 1's pattern. Do not use the Hero widget for Band 1 unless the spec forces it (it doesn't).
- **Band 2 is a single `pricing_chart` widget**, not a layout container with text-block cards. Band 2's heading + subheading live inside the widget's own `heading` / `subheading` config, not in a separate Text widget above it. The footnote lives in the widget's own `footnote` config, not in a separate Text widget below it. The widget owns the whole band — one widget, one band.
- **Footnote styling.** The spec asks for the footnote in smaller body text. The typography system has no caption / small-body row. The widget renders the footnote rich text as-is; apply italic + lighter weight inside the Quill content via the `<em>` tag (the demoConfig already does this). Log a continued gap if the result reads too subtle.
