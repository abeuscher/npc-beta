# 345. Code Review & Cleanup (Cycle 3 — Apply)

The apply session of Cycle 3 — consumes the audit's backlog across six iterations on one branch. The **344 audit log is canonical for scope** (every pick + file:line + disposition); this prompt is the delta — the agreed iteration shape and the decisions to settle at the open gate.

---

## Stub reference

Not a `release-plan.md`-numbered entry. Driven by the **344 audit log** (`sessions/344. Code Review & Cleanup (Cycle 3 — Audit) — Log.md`) and the **Forward plan — Cycle 3 shape** in `sessions/tracks/code-review-and-cleanup.md`. The Cycle-3 cluster is recorded in `release-plan.md`'s post-milestone section.

---

## Session-specific deltas

- **Six iterations, hard ceiling.** The two large file-splits are already carved out (346 bundle Import/Export, 347 InlineFormatToolbar) — **out of scope here.**
- **Two decisions to settle at the open gate** (the audit deferred them deliberately): Flag 344-B (rename vs delete the dead `getRelationManagers`) and Flag 344-F (explicit `canAccess()` vs bless the `canViewAny` delegation). See the open gate in the base prompt.
- **The convention-drift Pest test is this session's standing Cycle-3 deliverable** (iteration /5) — the audit scoped exactly what it covers.

---

## Phases (iterations)

Each is one commit on `session-345/1`. Re-read each file before editing; the audit's line numbers are advisory.

### /1 — Security + demo batch
- **Flag 344-A** — wire `SvgSanitizer::sanitize()` into the media-upload pipeline (ask the existing-infra question first — likely a media-added listener), OR drop SVG from accepted types, OR serve uploaded SVG as attachment. **Correct the false "SVGs run through SvgSanitizer" claim in the 329 record** either way. Add a regression test (uploaded `<script>`-bearing SVG is neutralized / rejected).
- **Flag 344-D** — add `Page::query()->update(['locked' => true])` to `DemoRestoreCommand::handle()` (mirror `demo:reset`) + a test asserting post-restore lock.
- **Flag 344-E** — add `BlockDemoUploads` middleware to the two builder `uploadAppearanceImage` routes (`admin-dashboard-api.php`, `admin-record-detail-view-api.php`); extend `DemoRoleLockdownTest`.

### /2 — Correctness
- **Flag 344-C** — suppress events at the two importer EventRegistration create-loops (`ContentImporter.php`, `ImportEventsProgressPage.php`) via `withoutEvents()` (the `RandomDataGenerator` precedent), or a `source`-guard early-return in `EventRegistrationObserver::created`. Add a test asserting an imported registration sends no confirmation email.
- **Flag 344-B** — `getRelationManagers`→`getRelations` (activates `CollectionItemsRelationManager`) or delete both, per the open-gate decision. Add/adjust a test.

### /3 — Dead-code sweep
- 6 dead imports (`FieldMapper` ×4, `PermissionTable`, `DOMNode`) + dead `ImportEventsProgressPage::contactNotFoundMessage()` + stale "five→seven/six" docblocks in the two importer concerns.
- `editor.ts` dead exports (`childrenOf`/`columnTargets`/`rootWidgets`).
- `base_url` absent-row default normalization across readers (W8/seam #2).
- (Front-end touched → `npm run build`.)

### /4 — Importer W7 trait-dedup
- W7 #1–#4: `afterPiiScan()` default-into-trait + `customFieldModelType()` seam; `accumulateBaseOutcome()` head helper; `resolveRowContact()` helper (5 sites, careful return contract); `baseNamespacedContext()` helper. Characterization tests gate behaviour parity (the existing importer test suite is the net).

### /5 — Convention-drift Pest test + boolean-settings + canAccess uniformity
- **Convention-drift Pest test** (standing Cycle-3 deliverable) covering: a guard wrapping `scripts/filament-signature-scan.php` asserting no `[KNOWN-BAD]` hits (pins the `getRelationManagers` regression); `CustomFieldDef`/`import_id_maps` `model_type` lowercase-short-name at the derivation point; trait-constant access; the boolean-`SiteSetting` convention (the 8-key list from the audit).
- Boolean-settings fixes: `use_pico` (→ `type='string'`/`'false'` or drop), `event_auto_publish` read-default alignment, delete the dead `castValue` `'boolean'` branch.
- **Flag 344-F** per the open-gate decision.

### /6 — W12 + W8 cleanup
- W12 #1–#2: `app/Widgets/EventRegistration/script.js` (register `window.NPWidgets.eventRegistration`) replacing the inline IIFE + the two `onsubmit=` guards; widget `assets()` declaration. (Widget asset surface → `build:public`.)
- W12 #3: AdminPanelProvider notification-bell CSS → `public/css/admin.css` or admin SCSS partial.
- W8 #3: `composables/hashFile.ts`→`utils/`; consolidate `helpers/`→`utils/`. (`npm run build`.)

---

## Out of scope

- The two carve-outs (346/347) — the big file-splits.
- Migration squash (348).
- Boundary edits / `CONTRACT_VERSION`.
- Reaffirmed won't-fixes (three large importer PHP files, ImporterPage closures, chunked-tick→queue, Flag B, `_custom.scss`, PostResource rename, DashboardConfig→DashboardView rename) — note as standing.
- Flag 344-G (Transaction/Campaign/Fund observers) — decide in-session whether to do the Transaction observer or defer all three; not a required pick.

---

## Testing

- **Slow test groups to run this session:** none expected (no slow-grouped surface touched). Run if a touched path has a slow-grouped test.
- **New tests expected:** yes — convention-drift test (/5), importer W7 characterization (/4), security/correctness regression tests (/1, /2).
- **Front-end build:** `npm run build` after /3 + /6; `build:public` after /6 (EventRegistration widget asset).
- **Baseline:** fast Pest 2799 / 0 at the 344 close; expect a net increase from the new tests.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file:** `sessions/345. Code Review & Cleanup (Apply) — Log.md` — per-iteration breakdown + W7/W8/Open-Flags outcome tables + test deltas.
- **Branch:** `session-345/1` (single branch; 6 iterations).
- **Track doc + outlines + release-plan** Cycle-3 status bump (audit + apply done).
- **Next:** re-verify the already-drafted 346 + 347 carve-out prompts against the post-345 tree; do not draft 348 yet.
- **Archive:** `sessions/344. *` → `sessions/archived/`.
