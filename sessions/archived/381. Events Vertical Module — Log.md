# Session 381 — Events Vertical Module — Log

**Date:** 2026-08-18
**Branch:** session-381/1

---

## What was built

### Phase 1 — The behavioral carve

- Public + portal registration controllers, `EventRegistrationQuantities`, both observers, the `events:send-reminders` command + its daily schedule, and `EventPolicy` moved to `plugins/Events/` with namespace change; `EventsServiceProvider` owns observer registration, `Gate::policy`, command + schedule (removal unschedules), and the three routes with identical names/URIs/middleware.
- Core strips: `routes/web.php` events block, `routes/console.php` schedule line, `AuthServiceProvider` policy line, `#[ObservedBy]` attributes off both models.
- Route parity 272/272, action FQCNs only. Mails staged deliberately across phases (`EventReminder`/`EventCancellation` at P2 with `EditEvent`; `RegistrationConfirmation` at P4 with the inversion) so no intermediate commit reached the plugin from `app/` or from Payments.

### Phase 2 — The admin socket goes real

- `EventResource` + four pages (+ an unreferenced relation manager, moved as-is) into `plugins/Events/Filament/Resources/`, discovered via `AdminContribution` `resourcesPath` — the first shipping admin-page plugin. Nav placement and all admin route names/URIs/middleware identical. No permission names declared (decision 6 — the `event` permissions stay core-seeded).
- Landing-page factory lifted to core `App\Services\EventLandingPageFactory` — `Event::duplicate()`, `RandomDataGenerator`, and the events importer are core callers that survive the plugin's absence.
- Core reaches into the resource replaced with route-name lookups that degrade gracefully: `QuickActions` (null-URL entries skipped in the template), the Organization "Events Sponsored" `recordUrl`, the admin tour map.
- `ViewRegistrations` blade served through the new `plugin-events::` view namespace.
- Absorbed inbox backfill: the `EditEvent` cancellation-wizard send got its first test (Mail-faked `callAction`, per-recipient + status-flip assertions) plus a hidden-when-cancelled mirror.

### Phase 3 — Widgets and the importer socket

- Seven widget folders into `plugins/Events/Widgets/` (nested shape — the at-scale precedent): namespaced definitions with `template()` overrides via the `plugin-events-widgets::` namespace, partials re-pointed, asset paths updated. `widgets:sync` clean (41 definitions; the seven `widget_types` rows keep their handles with plugin asset paths); `build:public` verified — bundle carries all three moved scripts and both stylesheets. EventsListing keeps core BlogPager's stylesheet as a build input (repo-relative path, not a namespace reach).
- Guard globs extended for the nested shape (`WidgetTemplateBoundaryTest`, `WidgetColorTokenConsumptionTest`); `generate-thumbnails.js` probes `plugins/*/Widgets/`.
- Importer socket (contract surface 8): core `ImporterContribution` + `ImporterRegistry` bound by `PluginServiceProvider`; the events field-registry trio + `EventFieldMapper` + both import wizard pages (+ views) into the plugin, pages discovered via `pagesPath`. Core consumers resolve through the registry and skip cleanly when absent: `CsvTemplateService::headersFor()`, the importer hub cards + template-download row, `SeedFakeImportSourcesCommand`, `GenerateFakeImportCsvsCommand`, `FixtureRunner`. Core importers stay hard-wired (session call — they join when their domains extract).

### Phase 4 — The inversion, the guards, the docs

- Core `App\Payments\Events\CheckoutSettled` (session + metadata); the Payments webhook's events branch reduced to dispatch-and-200 (metadata routing stays payments infrastructure); `Plugins\Events\Listeners\PromotePaidRegistrations` owns the former handler behavior byte-for-byte — pending-promotion, contact resolution, one `Transaction::recordStripe`, one queued confirmation, pending-only idempotency. Existing webhook tests re-pointed to the dispatch path with assertions unchanged.
- Standing guard `EventModuleBoundaryTest`: zero `Plugins\Events\` references in `app/`, zero allowlist, positive control on `config/plugins.php`.
- Presence matrix executable: `EventsPluginSession381Test` (enabled twin — routes incl. catch-all precedence, observers, policy, schedule, widgets, admin discovery, importer socket, dispatch/replay/webhook-branch), `EventsRemovedTestCase` + `EventsPluginRemovalSession381Test` (vanish: POST paths 405, JSON path falls to the catch-all 404, admin + importer gone, no schedule, `widgets:sync` drops the seven rows, landing pages render with missing widgets skipped, no-listener dispatch leaves rows pending, data kept), `EventsWithoutPaymentsSession381Test` (free/comp registration with confirmations, paid degrades to not-configured).
- `docs/plugin-contract.md` → **0.5.0**: surfaces 3, 4, 8 PROVEN; 10 PROVEN scoped to the events slice with the per-vertical rollout note; surface 2 at-scale contributor note; surface 13 first in-plugin consumer + tested matrix; surface 3 extracted-vertical permission nuance; SocketProbe kept as a standing regression fixture. Events-owned schema annotation recorded in the plan doc § 6.7 (the P7 squash-redraw input). `docs/app-reference.md` rows re-pointed for the moved files (including the stale `Api\EventsController` row — the route is a closure).

### Notable drift from the survey (adapted, logged)

- Core reaches into `EventResource` the survey missed: `Event::duplicate()`, `RandomDataGenerator`, `QuickActions`, the Organization relation manager, the admin tour map — resolved via the core factory lift + route-name lookups.
- Extra importer seams: `FixtureRunner`'s page map, the importer hub blade, `GenerateFakeImportCsvsCommand`, and `EventFieldMapper` (unlisted; only consumer moved, so it moved).
- `ImportSession191Test` never booted the app (the old header method was purely static); it gains a `uses(TestCase::class)` line — harness-only, assertions untouched.

---

## Security considerations

Behavior-preserving extraction: no new endpoints, no auth or permission changes, no grant changes (decision 6 — the role matrix is untouched). The three public routes keep their exact middleware (throttle, portal-auth + verified stack). The webhook inversion preserves signature verification in Payments and moves fulfillment behind a core-owned event with identical idempotency; with Events absent, a late webhook writes nothing. Plugin admin pages remain inside the panel's auth stack via Filament discovery. The registrant CSV export and cancellation wizard moved intact with their existing permission gates, and the wizard send now has test coverage.

---

## Schema changes

None. Models, tables, migrations, and factories stay core (front-loaded decision 2); the events-owned schema annotation in `sessions/plugin-architecture-plan.md` § 6.7 records what moves at P7.

---

## Production deployment notes

None beyond standard `migrate`. Deploy already runs `widgets:sync` + `build:public`, which picks up the moved widget asset paths; the seven `widget_types` rows keep their handles, so no content or page changes occur.

---

## Deferred decisions

- The public/portal controller paid-branch duplication — pre-existing, backlog (do-not-merge held).
- Per-plugin test suites and the events schema/migrations move — P7, with the squash-boundary redraw (annotation prepared).
- EventsListing's core-BlogPager build input and the vanish-test semantics once schema is plugin-owned (data-kept-on-disable vs fresh-install-without-plugin) — design notes carried to P7 via the 382 prompt.
- Events e2e coverage gap — backlog (unchanged).
- Resolver-arm contribution mechanics for plugin-owned models — needed no earlier than P7.

---

## P6 recommendation (Stage B entry)

The in-repo boundary held with no awkward workarounds; nothing blocks Stage B. Three things are cheaper to settle at P6 than after packaging, and they are front-loaded into the 382 prompt:

1. **Keep `config/plugins.php` as the activation + ordering authority; packages skip Laravel auto-discovery.** Provider ordering proved load-bearing twice (core registries bind before plugin `register()`; plugin `boot()` registers routes ahead of core's page catch-all — the events JSON endpoint depends on it), and every remove-the-line twin/mirror keys on the config list. This amends contract surface 1's Stage B sentence.
2. **Package LogoGarden alone at P6.** The vertical's packaging (schema, tests, cross-plugin build input) belongs with P7's redraw; one small package proves the path-repo mechanics, including the Dockerfile wrinkle (both stages `composer install` before source copy — manifests must be copied first).
3. **Do the plain T1 squash at P6, the boundary redraw at P7** — consistent with the owner-approved 381 statement ("P7 moves them with the squash redraw"); the plan doc's older P6 wording gets aligned at 382 close.

---

## Test result

3226 passed, 0 failed (fast suite, parallel). +37 over the 380 baseline: Events enabled twin (15), removal mirror (15), Events-without-Payments matrix (4), boundary guard (1), cancellation-wizard backfill (2). Route parity 272/272 before/after every phase (action FQCNs excepted on moved classes). `build:public` bundle verified; CI `Tests` green on the pushed branch.
