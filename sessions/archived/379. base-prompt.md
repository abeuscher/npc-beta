# Base Prompt — Session 379 (Admin-Shell Sockets)

**Start now.** Execute the reading list below, open the session prompt at `sessions/379. Admin-Shell Sockets.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **379. Admin-Shell Sockets**.

This is **arc position P3** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary). P3 turns the admin shell into a socket: `AdminPanelProvider`'s ~100-line inline `->routes()` closure decomposes into per-feature route files, and the three owner-approved Filament conventions land as real mechanics — core-owned nav groups with plugin-declared group + sort weight, plugin-declared permissions seeded at enable and granted to no shipped role but super-admin, and the core-owned admin theme (plugins ship no admin CSS). This is the session that makes `docs/plugin-contract.md` surfaces 3 and 4 buildable-against rather than aspirational; the contract doc is the arbiter.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**.

**Session shape:** feature-style / behavior-preserving refactor plus socket mechanics. The admin route surface must be byte-identical before and after (same names, URIs, middleware); the socket mechanics are new core capability proven by a test-fixture plugin, not a shipped one. **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` §§ 2–4 and § 7, and `docs/plugin-contract.md` v0.2.0 (surfaces 3 and 4, the normative rules, and the surface-2 entry as the worked precedent for "make a surface real").
3. Open `sessions/379. Admin-Shell Sockets.md` — the front-loaded decisions there are settled; do not reopen them.
4. Read the code being touched: `app/Providers/Filament/AdminPanelProvider.php` (the inline `->routes()` closure is the decomposition target; the render hooks and middleware stacks are NOT), `app/Providers/PluginServiceProvider.php`, `config/plugins.php`, `plugins/LogoGarden/LogoGardenServiceProvider.php` (the provider pattern plugins follow), `routes/admin-api.php` + siblings (the existing route-file precedent), `database/seeders/PermissionSeeder.php`, and `tests/Feature/PluginPilotSession377Test.php` + `tests/Feature/WidgetTemplateBoundaryTest.php` (the socket-proving and standing-guard patterns).
5. Skim `sessions/session-outlines.md` (Plugin Architecture track entry) and `docs/app-reference.md` (Admin UI section).
6. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 378

- **CRM contract at v2.7.0** — 378 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** widget boundary is real across all 41 (arc P2 complete) — twelve templates retrofitted onto declared contracts; `dataContracts()` named multi-contract seam live; new resolver arms (`fund`, `membership_tier`, `navigation_menu`, `portal_member`, and `SOURCE_SERVICE`: `setup_checklist` / `scrub_counts`); the standing guard `tests/Feature/WidgetTemplateBoundaryTest.php` bans template reaches with zero allowlist; `WidgetDefinition::baseDir()` generalizes folder-relative resolution. `docs/plugin-contract.md` at **v0.2.0** (surfaces 1, 2, 11, 12 PROVEN; 3–10, 13 DECLARED).
- **Schema baseline:** no schema changes at 375–378.
- **Fast Pest suite green at 3144 / 0** (378 close; +28 over the 377 baseline: 11 arm, 14 render-parity, 2 boundary-guard, 1 token). Expected delta this session: moderate — route-parity check, socket-mechanics tests via a fixture plugin, permission-seeding tests, a plugin-admin-CSS guard case.
- **Playwright suite** — baseline preserved; does not run this session, CI is the signal.
- **Planning state:** no planning-doc changes at 378 beyond the arc entry marking P2 complete (done at 378 close).
- **Housekeeping inbox** — the 378 capture buffer was empty at close; nothing folds in. No inbox items are absorbed by this session shape.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Route parity is the bar for the decomposition.** Capture `php artisan route:list --json` before touching the provider; after the move, names, URIs, methods, and middleware must be identical. Record the diff result (empty) in the work log. The existing feature tests for every moved cluster (2FA, invitations, uploads, QuickBooks, theme, dev-tools, setup-checklist, exports, the three builder APIs) are the behavioral net.
- **Socket mechanics are proven by a test fixture, not a shipped plugin.** No real widget or vertical moves into `plugins/` this session. Contract honesty: surfaces 3 and 4 stay DECLARED — the doc records the mechanics as built and fixture-tested, promotion to PROVEN waits for the first shipping consumer (P5 or later).
- **The front-loaded decisions in the session prompt are settled.** Implementation details within them are yours to decide and log.
- **Scope is binding.** The render hooks, middleware stacks, and brand/theming block of `AdminPanelProvider` stay put — only the `->routes()` closure decomposes. Capability detection (P4), payments (P4), verticals (P5), packaging (Stage B) — all out. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** update surfaces 3 and 4 in `docs/plugin-contract.md` per the honesty rule (mechanics recorded, DECLARED status kept, fixture-test named), bump the doc's version, changelog entry.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the route-closure survey in the session prompt still matches the provider, state the decomposition order and any drift found — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
