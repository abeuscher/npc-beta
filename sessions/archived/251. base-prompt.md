# Base Prompt — Session 251

We are about to begin a new session: **251. Fleet Manager Contract v2.1.0 — Log-Fetch Endpoint**.

This session lands the CRM-side companion to A2 (Fleet Manager — node operations parity) from `sessions/release-plan.md`: an additive `/api/logs` endpoint that FM 013+ blocks on for the operator log-reading affordance. Boundary-touching session — bumps the contract from `v2.0.0` to `v2.1.0` (additive within the v2 major).

FM-side state at session start: FM 012 absorbed v2.0.0 mTLS at FM 012 close; FM has caught up. FM 013+ resumes A2 affordance work on the mTLS foundation. The log-reading affordance is the slice queued for FM 013 (or wherever FM splits A2); CRM-side this session ships the endpoint it consumes.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md` — the 11 discipline rules + § A2.
3. Read `sessions/session-outlines.md`:
   - Active tracks block (Fleet Manager Agent track is active).
   - Cross-Repo block — confirm the contract is at `v2.0.0` going in. This session bumps to `v2.1.0`. The "Pending boundary changes" subsection lists log-fetch + threshold-derivation; this session settles log-fetch (and possibly threshold-derivation per the Design decision at Phase 1).
   - Fleet Manager — Node Operations Parity stub.
4. Read `docs/app-reference.md` — environments, container names, view-to-file mappings, key dependencies. No schema docs expected (no migration this session).
5. Read `docs/fleet-manager-agent-contract.md` end-to-end. This is the canonical spec. Pay attention to:
   - The Endpoint section — `/api/health` is the only endpoint today; this session adds `/api/logs` (or whatever path is agreed at Phase 1).
   - The Auth section — mTLS at the TLS layer. The new endpoint reuses the same mTLS gate (per-location `if ($ssl_client_verify != "SUCCESS")` at nginx); no application-layer auth code.
   - The Version negotiation section — `HealthController::CONTRACT_VERSION` is the source of truth; bump to `2.1.0`.
   - The CHANGELOG — most recent v2.0.0 entry shows the shape of a session-close changelog.
6. Read `sessions/tracks/fleet-manager-agent.md` — the track doc. Status snapshot, phase retrospectives, forward plan. The status snapshot needs updating at close.
7. Read `sessions/fleet-manager-planning-spec.md` ("Two-Repo Coordination Protocol" section) — the cross-repo discipline.
8. **For the existing endpoint shape** — `app/Http/Controllers/Api/Fleet/HealthController.php` is the model for response shape, throttling, content-type, error-envelope discipline. The new `/api/logs` controller follows the same conventions: stateless, API middleware group, no CSRF, `throttle:60,1` (or whatever the agreed shape is), JSON envelope, no internal paths or stack traces in messages.
9. **For the route registration** — `routes/api.php` is where `/api/health` lives. The new endpoint registers alongside.
10. **For the nginx mTLS gate** — `docker/nginx/default.conf` and `docker/nginx/prod.conf` carry the per-location `if ($ssl_client_verify != "SUCCESS") { return 403; }` block on `/api/health`. The new endpoint needs the same block on its location. **Boundary-touching — surface and confirm before editing nginx.** Production cert-paste is unaffected (same `fm-client.crt`).
11. **For the log source** — Laravel writes daily logs to `storage/logs/laravel-YYYY-MM-DD.log` via the `daily` channel. FM 013+ needs to fetch these without operator SSH. Shape question (Design decision at Phase 1):
    - **What range does FM ask for?** Last N lines? Last N hours? A specific date (UTC)? A trailing range?
    - **What format does the response carry?** Plain-text body with `Content-Type: text/plain`? JSON envelope wrapping the lines as an array? Newline-delimited JSON for streaming?
    - **What size cap?** Daily Laravel logs can grow to many MB; an unbounded fetch is a bandwidth foot-gun. Default cap proposed at 1 MB or 10K lines per request.
    - **Filter shape?** Probably none for v2.1 — FM filters client-side. If filtering becomes a need, an additive v2.x bump can fold it in.
12. Open `sessions/251. Fleet Manager Contract v2.1.0 — Log-Fetch Endpoint.md` and read the session prompt carefully.
13. Summarise your understanding and confirm you are ready to proceed. **Surface the FM-12-absorption check at the top:** if FM 012 surfaced any CRM-side findings during absorption that haven't been recorded in the Cross-Repo block, the user should drop them into the conversation at session start so they can fold into 251 or split into 252 per Rule 11.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration**, ask the user what existing system handles it. The session leans on existing infrastructure: Laravel's `daily` log channel for the source, `HealthController` for response-shape conventions, the nginx mTLS gate for auth. Do not introduce new logging libraries or auth shapes.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** The endpoint shape (range parameters, response format, size cap) is the load-bearing decision this session — settle it in Design decisions before writing the controller.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migration expected.** No schema changes. If verification surfaces a need (e.g., a log-access audit trail in the DB), surface and decide.
- **Update `docs/fleet-manager-agent-contract.md`** at close — body extension for the new endpoint, CHANGELOG entry for v2.1.0, bump the `Contract Version` field at the top.
- **Update the Cross-Repo block in `sessions/session-outlines.md`** — bump `Agent contract version` to `2.1.0`, update the "Last boundary-touching session in this repo" to 251, resolve "Log-fetch endpoint" under "Pending boundary changes" (move to a "Resolved at" inline note or remove), keep the threshold-derivation deferral entry unless it's settled in this session per the Design decision at Phase 1.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline at 250 close: 1784 fast tests / 9336 assertions. Expect modest growth — new tests for the log-fetch controller (mTLS-pretend integration test, range parameter handling, size-cap clipping, throttle, JSON envelope shape).
- **Front-end builds — when to run which:**
  - No SCSS or page-builder JS expected — this session is admin-side / api-side PHP + nginx config only.
  - No admin-bundle build required.
  - No public-bundle build required.
- **Cross-repo coordination — Fleet Manager agent contract.** This session **is** boundary-touching. The standing cross-repo discipline applies: update the contract spec, bump the version constant, update the CHANGELOG, update the Cross-Repo block in this repo's `sessions/session-outlines.md`. The FM repo's coordination block needs a corresponding update at FM-side absorption (FM 013 or wherever it lands); this CRM-side session updates only this repo's coordination block. The contract surface goes from `v2.0.0` to `v2.1.0` at this session's close.
- **Verify objective outcomes yourself; pull the user in for judgment.** When you can observe the result directly, do — don't ask the user to run it and report back. Close the loop yourself for CLI output, file contents, exit codes, logs, and anything Playwright can verify. **Playwright is a verification mechanism, not just an existing test suite** — though for this session, the verification is HTTP-shaped (hit the new endpoint with a curl that presents the FM-supplied client cert; verify response shape, range parameters, size cap, throttle behaviour). A Pest feature test using `$this->getJson('/api/logs?...')` with mTLS faking covers most of it. Pull the user in for endpoint-shape questions that need product judgment (range parameter names, default sizes, format choice) — surface those in Design decisions at Phase 1.
- **Pause for manual testing only when human judgment is required.** The endpoint is HTTP-shaped and self-verifiable. After implementation, run the fast suite + a manual curl against `/api/logs` (presenting `localhost.crt` against the local mTLS gate); verify the response shape and the size-cap behaviour. Announce results, then stop and wait. Do not suggest closing the session.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The next session after 251 depends on FM-side state and what 251 surfaces:

- **If FM 013+ surfaces additional CRM-side requests during absorption**, those land first (252).
- **If 251 settled both pending boundary items (log-fetch + threshold-derivation)**, the next CRM-side beat goes back to the pre-Beta-1 release plan: **E2 — Importer Mapping Page UX** (B2 prerequisite, queued earlier and bumped to make room for 251).
- **If 251 only settled log-fetch** (per the Design decision at Phase 1), threshold-derivation stays deferred to a future v2.x bump and is not the immediate next session.

Decide at 251 close based on FM-side state. Draft the 252 base prompt + session prompt against the agreed shape.

### Phase 2 — Close

When the user says to close (after 252's prompt is agreed):

- **Session log**: write `sessions/251. Fleet Manager Contract v2.1.0 — Log-Fetch Endpoint — Log.md` against `template-session-log.md`.
- **Update `sessions/completed-sessions.md`**: append session 251's row.
- **Update `sessions/release-plan.md`**: A2 stays open (CRM-side companion partial; FM-side affordance work continues). No checkmark on A2 yet.
- **Update `sessions/session-outlines.md`**:
  - Cross-Repo block: bump `Agent contract version` to `2.1.0`, update "Last boundary-touching session in this repo" to 251, resolve the log-fetch line under "Pending boundary changes."
  - Fleet Manager — Node Operations Parity stub: note CRM-side log-fetch shipped at 251.
- **Update `sessions/tracks/fleet-manager-agent.md`**: status snapshot bump (last update date, what's complete, what's active). If A2 has a phase shape on the track, may trigger phase-expiry compression — judge at close.
- **Spec doc**: `docs/fleet-manager-agent-contract.md` — body extension for `/api/logs`, CHANGELOG entry for v2.1.0, bump the Contract Version field at the top.
- **Archive the previous session**: move all files matching `sessions/250. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-251/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly — `HealthController` is the controller-shape model, `routes/api.php` is the route-registration model, `docker/nginx/{default,prod}.conf` is the mTLS-gate model.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones — `routes/api.php` and the nginx config files exist; the new controller is the one new file expected.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- No new admin pages or resources expected this session — purely api-side + spec doc + nginx config.
- **Fleet Manager contract discipline** — every CRM-side change to the boundary surface (controller, route, nginx mTLS gate, response shape, contract version constant) updates the spec doc + CHANGELOG + Cross-Repo block in the same session. The contract is the source of truth; code drift from spec is a process failure, not a "fix in next session" item.
