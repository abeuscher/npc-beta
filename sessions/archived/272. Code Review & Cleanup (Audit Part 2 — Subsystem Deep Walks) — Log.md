# Session 272 — Code Review & Cleanup (Audit Part 2 — Subsystem Deep Walks) — Log

**Date:** 2026-05-08
**Branch:** session-272/1

---

## What was built

Subsystem-walk half of the 271+272 audit pair. Five lifecycle walks (W2 / W3 / W4 / W4b / W4c) end-to-end on a single iteration branch; one safe in-place fix landed; 271's starter W7/W8 tables and Open Flags merged with 272's subsystem-driven rows; pre-loaded findings ratified or refined; user accepted all recommendations at close walkthrough; 273 apply-session prompts drafted live.

### Per-workstream summary

**W2 — CRM Importer subsystem.** Lifecycle walked end-to-end across the 7 content types (Contacts / Events / Donations / Memberships / Invoice Details / Notes / Organizations) and the two shared traits. The 206-time `ImportSessionActions` extraction held cleanly — `ImporterPage` is now 238 LOC; the three Filament action closures (approve / rollback / delete) cleanly delegate to `app(ImportSessionActions::class)->method($record)` with the 3-layer defense intact (`hidden` + `requiresConfirmation` + `abort_unless` inside the closure). All 7 ProgressPages implement the same template-method shape (`emptyDryRunReport` / `processOneRow` / `accumulateOutcome` / `buildRowContext` / `saveMappingToSource`); size differences are real domain complexity, not drift. ImportInvoiceDetailsProgressPage at 786 LOC legitimately overrides `runDryRun` / `runCommit` (`usesChunkedTick(): false`) because invoice-detail rows compose into invoice groups — multi-row group semantics, not row-by-row. The 6 namespaced wizards (Events / Donations / Memberships / InvoiceDetails / Notes / Organizations) all use `serializeColumnMaps()` from the trait; ImportContactsPage (1014 LOC) inlines its own ~50-LOC serialization at `runImport():940-992`, mirroring but not using `serializeColumnMaps()` — confirming Flag B's structural divergence remains real. AggregatingRegistry / FieldRegistry / DerivesFromFillable abstract bases (post-206 extraction) are clean; OrganizationImportFieldRegistry follows the namespaced shape correctly (single-bucket aggregator with three Org sentinels).

**Flag B (carried from 206) — re-evaluation outcome:** divergence is load-bearing — bare `__custom__` sentinel (vs `__custom_contact__`), bare field grammar (`email` vs `contact:email`), bespoke 3-strategy collision-resolution UI (prefer / split / drop), inline `runImport()` serialization. 11 sessions of namespaced-pattern maturity have not shifted the cost calculus — namespaced wizards are 230-356 LOC each; ImportContactsPage is 1014 LOC; each is doing the work it needs to. **Re-affirm 206 disposition: leave as is.** Carry forward as a standing won't-fix; revisit only if a forcing function emerges.

**Two dead imports** surfaced and **fixed in-place at `2cd6571`:** `Str` from `ImportNotesProgressPage` (no `Str::` reference); `Contact` from `ImportInvoiceDetailsProgressPage` (no `Contact::` reference, the page uses lowercase `$contact` variables sourced from trait helpers `resolveContactByNamespacedKey` / `autoCreateContact`). Total dead-import sweep across 271+272: **24 imports across 19 files.**

**W3 — Organizations / Affiliations / Donation Credits cluster.** Pattern parity Org-vs-Contact strong on the model layer: both carry `EnforcesScrubInheritance, HasFactory, HasSourcePolicy, HasUuids, SoftDeletes` and both expose `notes()` / `tags()` / `affiliations()` / `softCreditsReceived()` relations.

Affiliations junction has a clean single-source-of-truth lifecycle: every create routes through `Affiliation::bindContactToOrganization()` ([app/Models/Affiliation.php:50](app/Models/Affiliation.php#L50)), used by `ImportProgressPage:444` + `:509` (contacts importer's auto-create-on-existing-update path), `InteractsWithImportProgress:828` (the other importers' `applyContactOrganization`), and `ContactFactory:66`. `is_primary` constraint enforced at both layers — model `booted()` saving hook clears prior primaries; DB partial unique index `affiliations_one_primary_per_contact`. Deletion: cascade on Contact / Organization delete (DB-level FK CASCADE); `OrganizationResource::guardDeletion()` blocks soft-delete with a 5-relation count (affiliations, donations, memberships, sponsored events, transactions).

DonationCredit junction also has a single-source-of-truth lifecycle: all creates route through `SoftCreditsRelationManager` only ([app/Filament/Resources/DonationResource/RelationManagers/SoftCreditsRelationManager.php](app/Filament/Resources/DonationResource/RelationManagers/SoftCreditsRelationManager.php)). No importer entry per the schema doc's v0 scope.

**Flag W6/B — Observer coverage gap (carried from 271): confirmed.** `Note`, `Organization`, `Affiliation`, `DonationCredit` have no observers. CUD bypasses `App\Services\ActivityLogger::log` entirely. Comparison: every other tracked CRM model (Contact, Donation, Membership, Event, EventRegistration, Page, Product, ProductPrice, Purchase, CustomFieldDef) has an observer that writes activity-log entries. Pre-resolution at 272-close walkthrough: path (a) — add 4 observers in 273 to match the established pattern.

**NEW W8 finding — observer registration pattern divergence.** 7 models register via `#[ObservedBy(...)]` PHP attribute (the modern Laravel pattern). `Contact` / `Membership` / `Donation` register via `Model::observe(...)` calls in `app/Providers/AppServiceProvider.php:33-35` (the legacy pattern). Same effect, mixed mechanism. Single-line-per-model fix to converge.

**W4 — Fleet Manager API surface (read-only).** Four endpoints walked end-to-end against the v2.3.0 contract spec. Error envelope shapes uniform across the read endpoints — `/api/logs` and `/api/backup/blob` both use `{error, message}`. `/api/backup/trigger` uses `{contract_version, status: success|failed, last_backup_at, duration_ms, message}` even on `failed` — divergent but principled (action endpoint, 200-with-failed-status by design). `contract_version` field handling: carried on `/api/health` and `/api/backup/trigger` (success + failed envelopes); not on `/api/logs` envelopes; not on `/api/backup/blob`'s 404 / 500 envelopes. Per spec doc CHANGELOG: "v2.3.0 does not change cross-endpoint error-envelope shape; if a future revision adds `contract_version` to error envelopes, that lands as a deliberate cross-endpoint sweep." → **268-time deferred decision documented as intentional asymmetry; no flag carried.**

`sanitise()` usage: only on `BackupController` (private method [app/Http/Controllers/Api/Fleet/BackupController.php:169-180](app/Http/Controllers/Api/Fleet/BackupController.php#L169-L180)). LogsController uses `class_basename($e)` for 500s; HealthController same for db / redis subchecks. Asymmetry is principled — only `backup:run` produces multi-line artisan output that needs path-strip + newline-collapse + 500-char-cap. Throttle buckets: 60/min for `/health`, `/logs`, `/backup/blob` (reads); 6/min for `/backup/trigger` (one expensive write). Documented in spec doc.

**Version-stamp parity confirmed at v2.3.0** across all three sites: `HealthController::CONTRACT_VERSION:15`, `BackupController::CONTRACT_VERSION:17`, `docs/fleet-manager-agent-contract.md` line 3 (`Contract Version:`). 271 W9 confirmed parity; 272 W4 proposes a structural fix to prevent drift (caught once at 268).

**NEW Flag W4/A — FM contract version-stamp drift prevention.** Three independent sources of truth. Pre-resolution at walkthrough: combination structural fix — (i) extract `App\Http\Controllers\Api\Fleet\Concerns\HasContractVersion` trait declaring the constant once; both controllers `use` it; (ii) add Pest test asserting the trait constant matches the spec doc's `Contract Version:` line at test time. Lands in 273.

**W4b — Page Builder Vue subsystem.** `editor.ts` walked: 930 LOC, ~77 keys in the return block ([resources/js/page-builder-vue/stores/editor.ts:841-930](resources/js/page-builder-vue/stores/editor.ts#L841)). Pinia store concentrates persistence-state, selection state, bootstrap-derived data, in-flight mutation state, CRUD + layout actions, image upload/remove, local config dispatch, preview/debounce machinery, preset machinery. Cohesive composable-extraction candidates: `useUploadActions` (4 image fns), `usePresetActions` (4 preset fns + applyPreset), `useDebouncedSave` (~150 LOC), `useRefreshPreview` (~100 LOC). `api.ts` (275 LOC): 207's no-module-level-API-exports cleanup held; `createApiClient` factory is the entry point; every method threads `signal?: AbortSignal` consistently — request-cancellation pattern uniform.

**Flag A (carried from 205) — re-evaluation outcome: substantially closed by 207's work.** `page_layouts.appearance_config jsonb` landed at 207. `LayoutInspectorPanel.vue:540` already reuses `BackgroundPanel` and `:531` reuses `SectionLayoutPanel` via data-driven props (`config`, `show-image: false`, `show-full-width: false`). The widget-and-layout panels are unified. The remaining 780 LOC is the bespoke Column Settings tab (display / columns / grid / flex / full-width) — genuinely layout-only domain. **Re-frame the flag:** 205's "architectural divergence" is closed; the W11 size finding becomes "Column Settings tab ~400 LOC inside the 780-LOC file — extract to `LayoutColumnSettingsTab.vue`" — mechanical sub-component split, no architectural choice required.

**Other W11 Vue outliers (PreviewCanvas 620, GradientPicker 519, ColorPicker 482, InspectorPresetGallery 475, TypographyPanel 515) — sized but principled domain complexity.** Recommend keeping as-is; iteration plan does not target them.

**W4c — Rich-text surfaces.** Standing 250-time stub still entirely accurate. Zero `HtmlSanitizer` / HTMLPurifier / Quill-allow-list anywhere in `app/`. The few `strip_tags()` call sites are excerpt / meta-description derivation paths (legitimately want plain text), never at a rich-text-store boundary. `ContentImporter::sanitizeWidgetConfig` ([app/Services/ImportExport/ContentImporter.php:419](app/Services/ImportExport/ContentImporter.php#L419)) does collection-handle existence check + image / video media-id clearing only. No HTML sanitization on rich-text-typed schema fields.

**Trix surface still alive — broader than just Memos.** `Collection::getFormSchema():126` ([app/Models/Collection.php#L126](app/Models/Collection.php#L126)) uses `Forms\Components\RichEditor::make("data.{$key}")` (Trix) for any custom collection's `rich_text` field type. So every custom collection with a `rich_text` field renders Trix on edit — not just Memos.

Surface inventory (every model boundary that stores rich-text HTML, all unsanitized):
- `PageWidget.config` rich-text-typed schema fields (Quill)
- `Collection.data.*` rich_text fields (**Trix**, via `Collection.php:126`)
- `Note.body` (Quill, NoteResource:117)
- `Event.description` + `meeting_details` (Quill, EventResource:248,316)
- `EmailTemplate.body` (Quill, EmailTemplateResource:51)
- `SiteSetting` rich-text keys: `dashboard_welcome` (Quill, GeneralSettingsPage:101), `system_page_content_reset_password` (Quill, :205), `system_page_content_email_verify` (Quill, :211)
- `CustomFieldDef.field_type='rich_text'` → renders Quill at `CustomFieldDef:67`; values land in any model's `custom_fields` jsonb
- All round-trip through `ContentImporter::sanitizeWidgetConfig` without sanitization on import

**NEW Flag W4c/A — Rich-Text Surface Sanitization Hardening.** Pre-resolution at 272-close walkthrough: **carve out as a dedicated successor session.** Scope (one `App\Support\HtmlSanitizer` + ~10 model-boundary apply sites + `ContentImporter` walk + Memos/Collection Trix→Quill convergence with data migration) is too large for a 273 iteration. 273-close drafts prompts for the dedicated session; release-plan position confirmed at that walkthrough.

### Pre-loaded findings — validated

All five 271-seeded subsystem findings ratified:

- **W2/1** `InteractsWithImportWizard.php` 1192 LOC — large-because-of-namespaced-pattern-shape; sub-flow extractions (`processUploadedFileNamespaced`, `serializeColumnMaps`, `buildNamespacedMappingRow`, `buildNamespacedPreviewSchema`) are intrinsic to per-step orchestration. **W11 picks: skip** — file size is consequence-of-pattern.
- **W2/2** `InteractsWithImportProgress.php` 950 LOC — same reasoning. Skip.
- **W2/3** All 7 ProgressPages template-method-shaped uniformly. Divergence is principled (ImportInvoiceDetails opts out of chunked tick for invoice-group semantics).
- **W2/4** Flag B re-evaluation: leave as is.
- **W2/5** `ImportSessionActions.php` 505 LOC — `match($session->model_type)` dispatch dense at 7 importer types. NEW W7 finding: `rollback()` and `delete()` differ only in the rollback's Contact-discard-note pass; extract a private `cascadeForType()` helper.
- **W3/1** Flag W6/B confirmed; pre-resolution path (a).
- **W3/2** Pattern parity Org-vs-Contact strong; no missing-on-X gaps.
- **W3/3** Affiliations lifecycle: single source of truth via `bindContactToOrganization()`. `is_primary` constraint enforced at model + DB layers.
- **W3/4** DonationCredits lifecycle: SoftCreditsRelationManager only; no importer entry.
- **W3/5** B1c importer parity clean; help-doc gap from 271 already closed in-session.
- **W4/1** Four-endpoint envelope walk: divergence documented; trigger envelope's `contract_version` on failed is principled.
- **W4/2** Version-stamp parity confirmed at v2.3.0 (ratified — no drift).
- **W4/3** 268 deferred `contract_version` on error envelopes: documented as intentional asymmetry; no flag.
- **W4b/1** Flag A re-framed (substantially closed at 207; remaining size is W7 #5 mechanical extraction).
- **W4b/2** `editor.ts` composable extraction candidates: 4 cohesive splits (uploads / presets / debouncedSave / refreshPreview).
- **W4b/3** Other Vue W11 outliers: principled domain complexity; skip.
- **W4b/4** `api.ts` clean; 207 cleanup held.
- **W4c/1** Standing 250-time stub still accurate; no surface added since has worsened the pattern.
- **W4c/2** Memos Trix→Quill convergence: scope hasn't grown but is broader than Memos (any custom collection with a `rich_text` field).
- **W4c/3** `ContentImporter::sanitizeWidgetConfig` confirmed: media-id clearing only, no HTML sanitization.

### One commit landed on `session-272/1`

`2cd6571` — **W2 dead-import sweep follow-up — 2 imports across 2 importer progress pages.** `Str` from `ImportNotesProgressPage`, `Contact` from `ImportInvoiceDetailsProgressPage`. Both verified by per-class-basename grep count = 1 (the use line itself). Total dead-import sweep across 271+272: **24 imports across 19 files.**

---

## W7 — Duplicated logic (271 starter rows + 272 subsystem rows; merged)

| # | What it does | Where | Proposed extraction |
|---|---|---|---|
| 1 | Password-mismatch validation IIFE — input listeners showing/hiding hint, identical logic | `resources/views/portal/reset-password.blade.php:49-64`; `resources/views/admin/invitation-set-password.blade.php:111-122`; `app/Widgets/PortalChangePassword/template.blade.php:43-54` | One shared `resources/js/portal/password-mismatch.js` factory. Cross-references W12 candidates 4-6. |
| 2 | `ImporterPage` action closure repetition — three near-identical 20-LOC blocks (approve / rollback / delete) differing only in icon / color / label / service-method | `app/Filament/Pages/ImporterPage.php:172-232` | **Skip** — Filament's idiomatic shape is per-action closure declarations; factoring would obscure the action surface. |
| 3 | `ImportSessionActions::rollback` vs `delete` — near-identical structure with same 5-branch dispatch (Event / Financial / Note / Org / Contact-default); rollback creates discard-notes for Contact subjects, delete doesn't | `app/Services/Import/ImportSessionActions.php:81-150` (rollback), `:152-198` (delete) | Extract a private `cascadeForType(ImportSession): void` helper that both methods call; `rollback()` does its Contact-discard-note pass after. ~50 LOC removed. Risk: medium (characterisation tests gate behaviour parity). |
| 4 | `editor.ts` Pinia store at 930 LOC (~77 keys in the return block) — concentrates uploads, presets, debounced save, refresh-preview, plus core CRUD / selection / layout work | `resources/js/page-builder-vue/stores/editor.ts` | Composable extraction: `useUploadActions` (4 image fns), `usePresetActions` (4 preset fns + applyPreset), `useDebouncedSave` (~150 LOC), `useRefreshPreview` (~100 LOC). Net: store drops to ~550 LOC. Risk: medium-high (highest behaviour-drift risk). |
| 5 | `LayoutInspectorPanel.vue` Column Settings tab is ~400 LOC inside the 780-LOC file — the rest is header / tab orchestration / appearance-panel reuse | `resources/js/page-builder-vue/components/LayoutInspectorPanel.vue` | Extract Column Settings tab to `LayoutColumnSettingsTab.vue` (~400 LOC); orchestrator drops to ~380 LOC. Risk: low (purely structural). Resolves Flag A's re-framing. |

---

## W8 — Framework alignment (271 starter rows + 272 subsystem rows; merged)

| # | File:lines | Current approach | Framework alternative | Risk |
|---|---|---|---|---|
| 1 | `app/Providers/AppServiceProvider.php:33-35` registers Contact / Membership / Donation observers via `Model::observe(...)` calls; 7 other models use `#[ObservedBy(...)]` PHP attribute | Mixed observer-registration mechanism | Convert the 3 to `#[ObservedBy(...)]` attribute on model classes; delete imports + calls from `AppServiceProvider::boot()` | Low |
| 2 | `app/Models/{Note,Organization,Affiliation,DonationCredit}.php` have no observers | Note / Org / Affiliation / DonationCredit CUD does not appear in `activity_logs` | New observers in `app/Observers/` mirroring `ContactObserver`'s `created/updated/deleted/restored → ActivityLogger::log` shape; register via `#[ObservedBy(...)]`. 4 new feature tests with `RefreshDatabase` + activity-log assertions | Medium — coordinate Note observer's scope with the standing C1 Notes Permissions stub. Resolves Flag W6/B |
| 3 | `HealthController::CONTRACT_VERSION:15` and `BackupController::CONTRACT_VERSION:17` are independent sources of truth; neither verifies against `docs/fleet-manager-agent-contract.md`'s `Contract Version:` line | Three independent sources of `'2.3.0'`; drift caught once at 268 | (i) Extract `App\Http\Controllers\Api\Fleet\Concerns\HasContractVersion` trait declaring the constant once; both controllers `use` it. (ii) Add `tests/Feature/Infrastructure/FmContractVersionParityTest.php` asserting the trait constant matches the spec doc's `Contract Version:` line | Low — pure consolidation + CI-enforced parity test. Resolves Flag W4/A |
| 4 | `app/Filament/Pages/Concerns/InteractsWithImportProgress.php::tick()` is a hand-rolled chunked-job with file-offset bookkeeping | Laravel queues + jobs are the framework-native chunked-processing pattern | Replace with a queued job + Filament polling for status | High — Filament's polling + reactive UI is the integration point; ImportInvoiceDetails already opts out via `usesChunkedTick(): false`. **Skip / defer** — substantial rewrite; future-session candidate if a forcing function emerges |

---

## Open Flags block (merged from 271 + 272; pre-resolved at 272-close walkthrough)

User direction at 272-close: **accept all recommendations.** Each flag's resolution path below is pre-agreed; 273 walkthrough ratifies and applies.

### Flag A — LayoutInspectorPanel architectural divergence *(carried from 205, re-framed at 272)*

Re-framed: 205's architectural divergence substantially closed by 207's `page_layouts.appearance_config jsonb` migration plus `LayoutInspectorPanel.vue` already reusing `BackgroundPanel` and `SectionLayoutPanel` via data-driven props. The remaining 780 LOC is the bespoke Column Settings tab — genuinely layout-only domain. **Pre-resolution: re-frame as W7 #5 mechanical sub-component split** (extract `LayoutColumnSettingsTab.vue`).

### Flag B — ImportContactsPage convergence with the namespaced pattern *(carried from 206)*

Divergence is real and load-bearing. 11 sessions of namespaced-pattern maturity have not shifted the cost calculus. **Pre-resolution: re-affirm 206 disposition — leave as is.** Standing won't-fix; revisit only if a forcing function emerges.

### Flag W6/B — Observer coverage gap *(NEW at 271)*

Note / Organization / Affiliation / DonationCredit have no observers. **Pre-resolution: path (a)** — add 4 observers in 273 mirroring `ContactObserver`; register via `#[ObservedBy(...)]`. Resolves via W8 #2.

### Flag W4/A — FM contract version-stamp drift *(NEW at 272, surfacing 268's deferred concern)*

Three independent sources of `'2.3.0'`. **Pre-resolution: combination structural fix** — `HasContractVersion` trait + Pest parity test against the spec doc. Resolves via W8 #3.

### Flag W4c/A — Rich-Text Surface Sanitization Hardening *(NEW at 272, surfacing the 250-time stub)*

Zero HTML sanitization at any rich-text-store boundary. Surface inventory ~10 models. **Pre-resolution: carve out as a dedicated successor session** (mirroring how 206-close carved out 207). Drafts land at 273-close; release-plan position confirmed at that walkthrough.

### Flag W10/A — FilePond upload-completion polling flake *(carried from 271)*

Two Playwright specs flake at `tests/e2e/helpers/wizard.ts:61`. **Pre-resolution: include as a 273 iteration** (likely poll-with-retries pattern). Lands in `session-273/1`.

### Flags W6/A, W9/A, W9/B, W9/C *(closed in-session at 271)*

All four resolved during 271/1: 18 admin Resources gained `canAccess()`; B1c importer routes registered in `import-contacts.md`; Notes Resource routes registered in `notes.md`; orphaned `generate-tax-receipts.md` deleted. No carry-forward.

---

## Pre-loaded findings to seed into 273's prompt (already populated)

273's session prompt carries the merged W7 / W8 tables, the Open Flags block with pre-resolved paths, the W11 outlier picks (PHP outliers mostly skip; Vue and TS picks land via W7 #4 + W7 #5), the W12 picks (rows 1-6 land; rows 7-8 skip), and the suggested 6-iteration shape. See `sessions/273. Code Review & Cleanup (Apply).md`.

---

## Security checklist — verified

- [x] No stale references to moved/deleted classes, services, models, blade variables, or JS modules surfaced during the subsystem walks
- [x] No dead imports referencing non-existent classes surfaced (2 found and fixed in-place at `2cd6571`; total cumulative across 271+272: 24 across 19 files)
- [x] Importer 7-type structural consistency walked (W2)
- [x] Organizations / Affiliations / Donation Credits cluster pattern parity walked (W3)
- [x] FM API four-endpoint divergence walked + structural-fix proposal drafted (W4 → W8 #3 / Flag W4/A)
- [x] Page Builder Vue composable-extraction candidates surfaced + Flag A decision recommended (W4b → W7 #4 + W7 #5)
- [x] Rich-text sanitization surface inventory complete (W4c → Flag W4c/A carve-out)
- [x] Merged W7 duplicated-logic table complete with file:line citations (5 rows: 1 starter + 4 subsystem)
- [x] Merged W8 framework-alignment table complete with file:line citations (4 rows: 0 starter + 4 subsystem)
- [x] Merged Open Flags block captures every architectural call from both audit sessions (6 carry-forward + 4 closed-in-session)
- [x] Schema docs and help docs match current surface (gaps closed in-session at 271 W9; no new gaps surfaced at 272)
- [x] FM contract version-stamp parity confirmed at v2.3.0 across all three sites (HealthController + BackupController + spec doc); structural-fix proposal in W8 #3
- [x] Pre-existing test failures documented (W10/A FilePond polling flake unchanged from 271 — same 2 specs at the same line)
- [x] Playwright suite baseline carried from 271 close (40 passed / 2 failed flaky; flake captured for 273)
- [x] Importer permission gates consistent across every per-type page and action (re-verified during W2 walk)
- [x] Schema-key filtering still enforced on `PageBuilderApiController::update()` (no page builder API work this session)

---

## Schema changes

None. This is an audit; safe-fix-in-place limited to the 2 dead-import deletions — no schema touch.

---

## Production deployment notes

- Standard deploy. No migrations, no env-var changes, no Composer changes.
- No front-end build required (the 2 dead-import deletions are PHP-only).
- No `build:public` (no widget asset surface touched).
- No FM-side handoff; non-boundary-touching.

---

## Deferred decisions

All carried into 273 with pre-resolved paths per the user's "accept all recommendations" direction at 272-close walkthrough:

- **Flag A (LayoutInspectorPanel)** → 273 W7 #5 (re-framed as `LayoutColumnSettingsTab.vue` extraction).
- **Flag B (ImportContactsPage convergence)** → standing won't-fix; revisit on forcing function.
- **Flag W6/B (Observer coverage gap)** → 273 W8 #2 (add 4 observers).
- **Flag W4/A (FM contract version-stamp drift)** → 273 W8 #3 (`HasContractVersion` trait + parity test).
- **Flag W4c/A (Rich-Text Sanitization Hardening)** → carved out at 273-close to a dedicated successor session; release-plan position confirmed at that walkthrough.
- **Flag W10/A (FilePond polling flake)** → 273 iteration `/1` (poll-with-retries fixture-level fix).
- **W7 #2 (ImporterPage action closure repetition)** → skip (Filament idiomatic shape).
- **W8 #4 (chunked-tick → queued-job)** → skip / defer (substantial rewrite; future-session candidate).
- **W11 PHP outliers #1, #2, #3** → skip (large-because-of-pattern; not extractable today).
- **W12 candidates 7, 8** → skip (below extraction threshold).

Other W11 / W12 candidates land via W7 #4, W7 #5, W12 #1-#6 (password-mismatch JS + admin invitation SCSS + record-timeline SCSS extractions).

---

## Test result

- **Fast suite:** 2164 passed, 0 failed (560.63s, 12850 assertions). Identical to 271 close baseline (2164, 12849, 588s); +1 assertion delta is irrelevant noise.
- **Slow suite:** 16 (carried from 268; not re-run this session per scope — no slow-grouped tests touched).
- **Playwright:** not re-run this session (carried from 271 close: 40 passed / 2 failed flaky; same 2 specs at `wizard.ts:61` per Flag W10/A).
