# Base Prompt — Session 374 (C3c — Comp-Tier Polish + Skip-Stripe-on-Zero-Total)

**Start now.** Execute the reading list below, open the session prompt at `sessions/374. C3c — Comp-Tier Polish + Skip-Stripe-on-Zero-Total.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **374. C3c — Comp-Tier Polish + Skip-Stripe-on-Zero-Total**.

This is **launch-schedule position 5** in `sessions/release-plan.md` — **C3c, the second of the Gate-2 money-path sessions** (positions 4–10). It is the **events-side sibling of C3b/373**: where 373 wired the automatic acknowledgment onto the donation webhook, C3c wires the **free / complimentary** case of the event-registration flow so a $0 order never touches Stripe. It is a direct prerequisite for **C5** (the "event with everything" rehearsal, position 9), which walks the paid + comp + capacity paths end to end. Its own prerequisite — **C2a, multi-quantity tickets** — already shipped.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. The CRM contract **stays at v2.7.0** (S2's app-layer gate, session 372). If the work somehow reaches `app/Http/Controllers/Api/Fleet/*` or the `/api/health` payload — it should not — the Two-Repo Coordination Protocol reactivates; otherwise leave the contract and the Cross-Repo block untouched.

**Session shape:** feature-style / polish + targeted fix (Gate-2 events path), 1 session (may split per Rule 11 if the email-timing fix + comp label outgrow one context). Adds an admin **complimentary-tier label** to ticket tiers, locks in the **free/comp registration path** that C5 depends on, and **fixes a latent confirmation-email timing bug** on this seam. **Read this drift before the Open Gate:** the plan's headline — "skip Stripe when the tier(s) total $0" — **already exists and is tested** (`EventRegistrationQuantities::isPaid()` diverts a pure-$0 order to a server-side confirm with no Stripe session), so it is locked-in-with-a-test, not rebuilt. The real work is (a) the comp label and (b) fixing the confirmation email, which today fires on row-creation with no status guard → paid rows email before payment, and multi-tier orders email once per line. The session prompt maps all of this. This is **polish + a targeted fix on an existing seam** (owner-confirmed scope — "we want the system to work"), not a greenfield build.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/release-plan.md` § **C3c** (the working-set entry — success criterion, prerequisites, artifact), plus the § "Launch schedule" list and the § **C5** entry (C3c is C5's prerequisite — know what the event rehearsal will walk). Skim §§ C2 / C2a so you know what the tiers + multi-quantity work did and did not build.
3. Read `sessions/session-outlines.md` — the active-tracks block and the Cross-Repo block (context only; not touched this session).
4. **Read the code before writing anything** (process rule; this is a wiring session, so the existing seam is load-bearing): the public event-registration entry point (`EventController::register` and/or `EventCheckoutController::store` — find which one decides *pay-via-Stripe* vs *confirm-directly*, and whether any zero-total short-circuit already exists); the Stripe Checkout-session build for events and `StripeWebhookController::handleEventRegistrationCheckout` (the paid-completion branch — the pattern to mirror); the `TicketTier` model + wherever ticket-tier pricing is entered in the admin (an `EventResource` form / relation manager); the `EventRegistration` model + its status lifecycle (`pending → registered`); and `App\Observers\EventRegistrationObserver` + `App\Mail\RegistrationConfirmation` (**when** the thank-you fires today — on create? only with an email? only when registered? — this governs the free-path dispatch).
5. Read `docs/app-reference.md` for environment/container names + the public-controller rows ("Event registration (POST)", "Event checkout (POST)"). Read `docs/schema/README.md` then the `ticket_tiers`, `event_registrations`, and `events` table files — a comp/complimentary flag, if one is added, likely lands as a small migration on `ticket_tiers`.
6. Open `sessions/374. C3c — Comp-Tier Polish + Skip-Stripe-on-Zero-Total.md` and read the session prompt carefully — it maps the drift between the plan entry's "stub" framing and the actual (further-along) code.
7. Note any drift between the session prompt and the actual code in a brief work-log entry; proceed per the drift and decision-threshold rules.

---

## Starting state inherited from session 373

- **CRM contract at v2.7.0** — 373 (C3b) was **NOT** boundary-touching. Rely on v2.7.0; don't revisit it. (S2/372 was the security track's one boundary-touching session; nothing since has moved the contract.)
- **What 373 shipped (rely on it, don't revisit):** an automatic **per-gift donation acknowledgment email** on the Stripe donation-checkout webhook — a new `donation_acknowledgment` EmailTemplate handle (admin-editable under System Emails), an `acknowledged_at` idempotency marker on `donations`, and a **queued** dispatch so a mail hiccup never fails the webhook. The annual `donation_receipt` statement + `DonorsPage` were left untouched, and **no EIN is stored** (owner data-minimization call). C3c is the same "wire an existing seam" pattern applied to the **events** flow.
- **Schema baseline:** 373 added `acknowledged_at` to `donations` (migration `2026_07_17_000001`). **C3c likely adds one** — a complimentary flag on `ticket_tiers` (e.g. `is_complimentary`) — decided at the Open Gate; if it lands, update `docs/schema/` and run it via Docker.
- **Fast Pest baseline: 3096 / 0** (parallel; 3091 + 5 new C3b tests, verified at 373). **New tests expected this session** — a $0 order skips Stripe + confirms server-side + sends exactly one thank-you; a mixed comp+paid order still routes through Stripe (regression guard); the comp-tier label behaves; no double-register / double-email (see the session prompt's Testing section).
- **Playwright:** baseline preserved. **Unlike C3b** (a webhook-only path, exercised purely by Pest), C3c changes a **public browser flow** — a $0 event registration now confirms without a Stripe redirect. A short Playwright spec on the public $0-registration path may be the right objective verification; reach for it if the public surface materially changes, otherwise Pest feature tests on the controller suffice. (Per the base-prompt Playwright rule: a mid-session verification spec is torn down at close unless it earns a place as a standing guard.)
- **VERSION** is `0.373.01` (373's merge). This session's close bumps to `0.374.01`. If this session deploys mid-flight to test the live $0 registration flow, bump VERSION *before* the deploy (the `main` deploy enforces immutable version tags — `CLAUDE.md` Git Workflow / `feedback_deploy_version_bump`).
- **Planning state:** launch-schedule positions 1 (S1/370), 2 (S3/371), 3 (S2/372), 4 (C3b/373) complete + merged; this is position 5 (C3c). After C3c: A3/A4 node-readiness + backup-drill (6–7), C4/C5 money-path rehearsals (8–9), F1 payment-depth E2E (10), then S4/S5 (11–12) close the security track. **Housekeeping wrinkle:** 373 merged as its feature commit **without a formal close** — there is no `373 … — Log.md` and no `completed-sessions.md` row for 373 (the index currently ends at 372). This session's close should archive the 373 files per the normal step and may fold in 373's completed-sessions row; surface it to the user at close.
- **Housekeeping:** feature-style shape — does **not** absorb inbox items by default (that's the audit-session privilege, Rule 2). Check `sessions/housekeeping-incoming.md` at start; digest anything captured there at the Phase-1 close step.

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md` § Process rules for every session). Session-specific:

- **This is a wiring session — read the seam before deciding anything.** The existing registration flow (public controller, Stripe Checkout-session build, `EventRegistration` lifecycle, `EventRegistrationObserver` + `RegistrationConfirmation`, the webhook completion branch) is the material you build with. Default hypothesis is "add a zero-total short-circuit + a comp label + fire the existing thank-you immediately," not "build an events system." (`feedback_audit_integration_seams`.)
- **Surface the architectural calls at the Open Gate before going deep** (`feedback_verify_objective_outcomes`): the comp-flag data model (a new `ticket_tiers` column vs `price = 0` as the only signal), where the zero-total short-circuit lives in the flow, how mixed comp+paid orders behave, how the thank-you fires on the free path, and whether a migration is needed. The session prompt lists these with recommendations — bring them, confirm, then proceed. Don't commit-and-unwind a data-model choice.
- **Mixed comp+paid orders stay on Stripe.** The plan is explicit: a mixed order "continue[s] through Stripe with the $0 line item as today (no change)." Only the **pure $0** order (free event or comp-only) skips Stripe. Don't refactor the paid/mixed path.
- **The headline behaviour already exists — don't rebuild it.** Pure-$0 orders already skip Stripe (the `isPaid()` gate) and already send the thank-you on server-side confirm; lock it in with a regression test rather than reimplementing it. The net-new work is the comp label and the email-timing fix.
- **Exactly one thank-you per order, at the right moment.** The multi-row-per-order model means the confirmation observer currently fires once per tier line (N emails per order), and paid rows email at pending-creation before payment. The email-timing fix is in scope: prove the thank-you fires once per order, at confirm time (free path immediately; paid path on webhook-confirm, not at pending-creation), while preserving the observer's contact-autocreate + opt-in side-effects. Recommended approach: relocate the dispatch out of the per-row observer into the free-path controllers + the paid webhook.
- **Verify objective outcomes yourself** — a Pest feature test (order-totals-$0 → no Stripe session, registration confirmed, one email; mixed order → Stripe as before) is the close-the-loop signal; add a short public-flow Playwright spec if the browser surface materially changes.
- **Do not touch:** the C3b donation acknowledgment path (rely on it); the membership / product / general Stripe webhook branches beyond reading `handleEventRegistrationCheckout` for the pattern; the annual `donation_receipt` path.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation and **wait for the owner to confirm before starting.** Scope is **settled** — comp label + the confirmation-email timing fix (owner-confirmed) — so the Gate confirms the **approach**, not the whether. Bring: (1) the recommended **email-fix approach** — relocate the `RegistrationConfirmation` dispatch out of the per-row observer into the free-path controllers (`EventController::register` + the portal twin) and the paid webhook (`handleEventRegistrationCheckout`), one email per order, preserving the observer's contact-autocreate + opt-in side-effects; (2) the **comp-flag data model** — a new `is_complimentary` column on `ticket_tiers` (display label; behaviour stays price-driven) + repeater toggle + picker label, vs. driving the label off `price == 0` with no migration; (3) the **create-path audit** the relocation forces — importer/admin-created registrations also email via the observer today, so confirm which paths *should* email (a bulk import emailing every attendee is likely unwanted); (4) **idempotency** for the paid-path email — rely on the existing pending-row filter (recommended) vs. an explicit marker; (5) whether a **Playwright spec** on the public $0-registration flow is warranted; and (6) anything else the code read contradicts in the plan's C3c framing. One confirmation, then proceed.
