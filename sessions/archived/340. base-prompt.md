# Base Prompt — Session 340 (Public Chrome Parity — header/footer mobile collapse + per-layout appearance + render polish)

**Start now.** Execute the reading list below, open the session prompt at `sessions/340. Public Chrome Parity — header-footer mobile collapse + per-layout appearance.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **340. Public Chrome Parity — header/footer mobile collapse + per-layout appearance + render polish.**

This is a **non-roadmapped housekeeping batch** (not a `release-plan.md` entry) — a filler session run while awaiting designer feedback on the public-site restyle (339). It consolidates a tight set of **public-render / chrome** items from `sessions/housekeeping-inbox.md`, centred on bringing the **header/footer chrome renderer to parity with the page-body renderer** (the ⭐ owner-designated chrome item + the footer-doesn't-collapse-on-mobile item), plus three small public-render fixes and the finish of the hero flush-left fix that 339 only worked around.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; all work is public-render code (`ChromeRenderer`, `AppearanceStyleComposer`, a Blade partial, public SCSS, hero SCSS). **CRM contract stays at v2.3.0**, no `CONTRACT_VERSION` bump.

**Session shape:** housekeeping (design/render-fix batch). Make header/footer chrome columns collapse on mobile and honor per-layout appearance like page bodies do; remove a nav-landmark override; quote `url()` background-images; ship the missing `.sr-only` utility; finish the hero left-flush fix.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. Read `sessions/session-outlines.md` (active-tracks block). The **Page Composition Fidelity** track (`sessions/tracks/page-composition-fidelity.md`) is context: the hero flush-left finish closes gap **G2** in that track's backlog, and the chrome-collapse work relates to G3/G4 thinking — update the gap backlog at close if a gap is closed.
3. **Read the inbox items this batch consumes** in `sessions/housekeeping-inbox.md`, and **re-verify each against current code before committing it to an iteration** (the staleness guard — most are 0.316–0.324 stamps): the chrome per-layout-appearance item `[0.324.01]` (⭐, paired with the `widgets:sync` item that becomes session 341); the footer-collapse item `[0.326.01]`; nav landmark `[0.317.01]`; unquoted `url()` `[0.316.01]`; `.sr-only` undefined `[0.317.01]`; hero left-flush `[0.324.01]`.
4. Read the two layout renderers and the appearance composer — **the core of this session**: `app/Services/Media/ChromeRenderer.php` (the chrome renderer, behind on both counts), `app/Services/PageBlockRenderer.php` (the page-body renderer — the parity target; note line ~103-104 emits `data-collapse-mobile`), `app/Services/AppearanceStyleComposer.php` (`composeForLayout` + `resolveFullWidthForLayout` are what chrome should route through; also carries the unquoted-`url()` emission to fix). Read `resources/scss/_layout.scss` (the `@container (max-width: $bp-md) .layout-grid[data-collapse-mobile="true"]` collapse rule chrome needs to trigger).
5. Read the hero widget for the flush-left finish: `app/Widgets/Hero/template.blade.php` (the content nests a second `.site-container`) + the `.hero-body`/`.hero-inner`/`.hero--pos-*-left` rules in `resources/scss/_custom.scss`. The fix is the left-position variants only; ~a few lines.
6. Read `resources/views/components/site-header.blade.php` (the nav-landmark `role="group"` override) and confirm where `.sr-only` should land in the public bundle (`resources/scss/public.scss` + partials).
7. Read `docs/app-reference.md` for the public-render pipeline + build commands, and `docs/schema/page_layouts.md` (the nested `appearance_config` shape chrome must read).
8. Note any drift between this prompt and the actual code in a brief work-log entry; proceed unless something needs a decision per the drift / decision-threshold rules.

---

## Starting state inherited from session 339

- **CRM contract at v2.3.0** — unchanged. 339 was the public-site restyle (designer comp, non-boundary). This session is also non-boundary; no version bump for contract.
- **Schema baseline:** no change since 328 (`pages.locked` + `edit_locked_pages`). 339 added none. **This session expects no schema change.**
- **Fast Pest suite green at 2777 / 0** (339 close baseline). **New Pest expected: yes** — a regression test asserting a chrome layout's `data-collapse-mobile` attribute **and** its nested `appearance_config` padding/background reach the rendered HTML (the silent-drift class this whole batch is about). The **`design` group** (`./dev test:design`) is the inner-loop signal.
- **Playwright:** baseline preserved; **no new specs expected** — the chrome render is server-side and Pest-testable.
- **Planning state:** 339 opened the **Page Composition Fidelity** track (`sessions/tracks/page-composition-fidelity.md` + its finding-template), with gap backlog G1–G4 feeding Widget Autonomy. The hero left-flush finish in this session closes **G2**; record that in the track's gap backlog at close. 339's deliverable was exportable local CMS/theme state (owner exports + pushes) — no code shipped from 339.
- **Housekeeping inbox:** this **is** a housekeeping batch — it consumes the six items named in reading-list step 3. Verify each against current code at the walk; drop or re-scope any that turn out already-fixed. The remaining inbox items stay for future batches. The `widgets:sync` ⭐ item `[0.324.01]` is **deliberately not in this batch** — it's deploy-pipeline work, scheduled as **session 341 immediately after**.

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md`). Session-specific notes:

- **Parity, not reinvention.** The page-body renderer (`PageBlockRenderer`) + `AppearanceStyleComposer` are the source of truth for how a layout collapses and how its appearance composes. Bring `ChromeRenderer` to *match* them — route through the same composer methods rather than writing a second appearance path. The bug class is precisely that chrome is a stale *duplicate*.
- **Build rule:** `.sr-only` lands in `resources/scss/**` and the hero finish edits `resources/scss/_custom.scss` → run `npm run build`. `ChromeRenderer` / `AppearanceStyleComposer` / the Blade partial are PHP/Blade → no build needed for those, but run `npm run build` once for the SCSS changes before announcing completion.
- **Regression test is required** for the chrome parity (collapse attribute + nested padding reaching rendered HTML) — this is the standing guard against the duplicate-renderer drifting again.
- **Verify the chrome fix on real chrome.** The `_header` / `_footer` system pages carry real column layouts (the `_header` 2-col layout has nested `appearance_config.layout.padding`); render them and confirm collapse + padding on mobile, not just the unit test.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) the per-item staleness check result (which of the six are still live vs. already-fixed); (2) the chrome-parity approach (route `renderLayoutBlock` through `composeForLayout` + `resolveFullWidthForLayout` + emit `data-collapse-mobile`); (3) confirmation the hero finish is the left-position-variant CSS only (closes G2). One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 + Phase 2. Session-specific details:

- **Log file:** `sessions/340. Public Chrome Parity — header-footer mobile collapse + per-layout appearance — Log.md`. Record each inbox item's disposition (shipped / dropped-as-stale) so the inbox fold is clean.
- **Bump `VERSION`** to `0.340.<iteration>`.
- **Update** `sessions/completed-sessions.md` + `sessions/session-outlines.md`, and the **Page Composition Fidelity** gap backlog if G2 (hero flush) closed.
- **Fold the housekeeping items** out of the inbox per the consumed list; clear the incoming buffer if it carried anything.
- **Archive the previous session's files** — move `sessions/339. *.md` → `sessions/archived/`.
- **Next session:** 341 (Widget Schema Sync in Deploy Path) — already drafted; owner confirms at close.
- **Commit on `session-340/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: the win is **header/footer chrome behaving exactly like a page body** (columns collapse on mobile; per-layout padding/background/border/full-width honored), plus three clean small render fixes and the hero left-flush finish — no new mechanisms, just parity and small corrections. Resist scope creep into the Nav-widget mobile-menu logic (already fixed), the page restyle (awaiting designer), or the `widgets:sync` deploy work (session 341). Commit messages name the deliverable ("340/N — chrome parity: {scope}").
