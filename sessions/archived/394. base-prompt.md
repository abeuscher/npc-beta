# Base Prompt — Session 394 (Member Portal Carved)

**Start now.** Execute the reading list below, open the session prompt at `sessions/394. Member Portal Carved.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **394. Member Portal Carved**.

This is **arc position D5, first session of the block** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary). D4 closed at 393 (`nonprofitcrm/memberships` VCS-resolved from `crm-plugin--memberships`, the fifth extraction, `plugins/` empty). This session opens the D5 block (carve → package → extract, settled decision 4): the Member Portal behavioral surface — the six portal widgets, the six portal controllers and their ~18 front-of-house routes, the portal auth guard + middleware stack, and the three portal mailables — carves into `plugins/MemberPortal/`, refilling `plugins/`. The block's estimated span is ~2 sessions; the closing shape (one combined package + extract session, the D4 precedent, or two) is decided at this session's close per the block-shape discipline.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.17.0 → 0.18.0 at close (surface 13 gains its **second shipping capability** — `member-portal`, consumed by a `crm-plugin--events` release; surface 1/4 notes for the first plugin-owned auth guard + middleware alias; changelog).

**Session shape:** feature-style carve, behavior-preserving. Runtime behavior identical on the full composition: routes 272/272 byte-identical, every portal assertion unchanged beyond path/namespace re-points, no schema change (the two portal tables stay in core's dump until the package phase), no visual change. **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D4 ✅, D5 open) and § 6.7 (models stay core — the precedent that keeps `PortalAccount` core this session).
3. Read the 393 session log (`sessions/393. Memberships Packaged & Extracted — Log.md`) — especially its **D5 kickoff input** section: the portal survey, the Events cross-plugin auth reach, and the signup-seam gates that now travel with the carve.
4. Read `docs/plugin-contract.md` at 0.17.0 — surface 13 in full (the capability pattern this session extends: `provide` at `register()`, consumers degrade; the Payments precedent), surface 10 (the release pattern the Events rider follows — contract-first, sibling repo, tag, core lock bump same-commit), surface 4 (the front-of-house route-file half the portal routes move into), surface 2 (nested `Widgets/` shape), § In-repo plugin shape + surface 1 (the `folder` manifest entry a carve adds).
5. Read the carve playbooks: the 392 log (`sessions/archived/392. Memberships Carved — Log.md` if swept, else `sessions/`) and the 389 log (`sessions/archived/389. Donations Carved — Log.md`) — the carve mechanics this session repeats.
6. Open `sessions/394. Member Portal Carved.md` — the survey and front-loaded decisions; note the Open-Gate items (sibling checkout freshness ×4; the auth-stack disposition ratified).
7. Skim the surfaces being carved: `app/Http/Controllers/Portal/` (six controllers), the portal block in `routes/web.php` (~lines 75–110), `app/Http/Middleware/PortalAuthenticate.php` + the `portal.auth` alias in `bootstrap/app.php`, the portal guard/provider/broker entries in `config/auth.php` (~lines 45, 79, 111), the six `app/Widgets/Portal*/` folders, and the three portal mailables (`PortalEmailVerification`, `PortalPasswordReset`, `PortalEmailChange`).
8. Read the cross-plugin reach being capability-gated: `vendor/nonprofitcrm/events`'s route file (`routes/web.php` — `portal.events.register` rides `['portal.auth', 'verified:portal.verification.notice']`) and the sibling checkout at `../crm-plugin--events`.
9. Read the core reaches that stay: `app/WidgetPrimitive/ContractResolver.php` (`portal_member` arm — reads `auth('portal')`), `app/Http/Controllers/FormSubmissionController.php` (the portal-collision check + `PortalFormCollision` mailable — form-submission-subject, stays core), `app/Filament/Resources/ContactResource.php` (the portal-account admin section), `database/seeders/{SystemPageSeeder,PortalPageSeeder}.php` (the seeded page topology).
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry) and note the housekeeping-inbox posture for this shape.
11. Note any drift between the session prompt's captured survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 393

- **CRM contract at v2.7.0** — 393 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D4 complete. **Five extracted plugins** (`crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` v0.3.0, `crm-plugin--events` v0.1.1, `crm-plugin--donations` v0.1.0, `crm-plugin--memberships` v0.1.0 — the D4 block closer, shipped at 393). `plugins/` is **empty** — this session's carve refills it (the 388 tolerate-empty guard relaxations tolerate the refill by construction). Plugin contract **v0.17.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** the nine plugin-owned tables live in their packages' migrations; `portal_accounts` + `portal_password_reset_tokens` are core-dump tables — **they stay core this session** (the package phase moves them; note `portal_accounts_id_seq` — a sequence-carrying redraw, the donations precedent, unlike D4's no-sequence one).
- **Fast Pest suite green at 3355 / 0 parallel** (393 close: 3349 baseline + 6 additive — the schema twin pair). The `Memberships` testsuite (8 files, 71 tests) runs from `vendor/nonprofitcrm/memberships/tests`. Expected delta this session: **additive** — the enabled twin, removal mirror, flag-vanish mirror, boundary guard, and the Events-without-Portal presence coverage; existing portal assertions unchanged beyond path re-points.
- **Route baseline: 272 routes** (dev/test), byte-identical to main at 393 close — unchanged this session (the portal routes re-register plugin-side with identical names/URIs/middleware).
- **Composition-safety state:** the memberships-absent portal-signup seam is gated (resolver arm, PortalSignup template, SignupController tier handling, the DatabaseSeeder tier-seeder call — all on the `membership.checkout` route-presence signal). **These gates travel with the carve** — the gated files move into the plugin with the gates intact.
- **Playwright suite** — CI signal, non-gating; the parked chrome-parity spec remains the known failure. No local Playwright.
- **Housekeeping inbox** — the two 389-folded items stay (`[plugin-arch]` EmailTemplateResource tokenHint, portal_form_collision defaults — note the second names a portal surface; check at close whether the carve resolved or re-homed it). The 393 capture buffer state is whatever the 393 close recorded. This shape absorbs no items.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total on the full composition.** Every portal net passes with assertions unchanged beyond path/namespace re-points; routes 272/272 byte-identical (method+URI+name+middleware, stash-capture diff against main); no schema change; no visual change.
- **The carve is the 389/392 playbook:** `plugins/MemberPortal/` + `MemberPortalServiceProvider` (namespace `Plugins\MemberPortal\`, handle `member-portal`); `distribution.json` gains a `folder` entry (sixth line, appended last); `plugins:manifest-sync` regenerates; files move with namespace changes only; core strips resolve through the established registries.
- **The auth stack moves with the plugin per the front-loaded disposition** (session prompt decision 2): guard + provider + password-broker config injected from the provider's `register()` (the Payments `services.stripe` config-injection precedent), the `portal.auth` middleware alias registered in `boot()` (`Route::aliasMiddleware` — the first plugin-owned alias; contract note at close). `PortalAccount` and the `portal_member` resolver arm stay core (§ 6.7; the arm is core engine).
- **The `member-portal` capability is the session's contract event** (surface 13's second shipping capability): declared at `register()`, consumed by the Events rider release (`crm-plugin--events` v0.1.2 — its portal registration route registers only when the capability is present; the 389 rider mechanics: contract wording first, sibling repo edit, tag, core lock bump + constraint in composer.json **and** distribution.json same-commit). With Portal absent, `portal.events.register` vanishes cleanly instead of 500ing on an unresolvable middleware alias.
- **The signup seam's memberships gates travel intact** — `PortalSignup`'s template, `SignupController`'s tier handling. The moving code keeps reaching core models (`Membership`, `MembershipTier` — legal, models are core) and never references `Plugins\Memberships\` (the boundary guard proves it; verticals reach each other only through core).
- **Rule-5 test-membership pass:** portal-subject tests move to `plugins/MemberPortal/tests/` as the `MemberPortal` phpunit testsuite, with the 383 reduction-pass discipline (deletions logged); tests where portal rows are fixture data stay core. Twin fixtures `MemberPortalRemovedTestCase` + `MemberPortalDisabledViaFlagTestCase` (no migrator path — schema is core until the package phase; the expiring-caveat docblock, the 392 wording). Boundary guard `MemberPortalModuleBoundaryTest` (zero allowlist, the donations pattern).
- **Gate 4 at the carve:** the two `script.js` assets (PortalSignup, PortalChangePassword) re-point to `plugins/MemberPortal/Widgets/…`; `widgets:sync` + `build:public` + bundle-content proof (the registered Alpine components).
- **The manifest is edited, never the generated file.** Scope is binding: no schema move (package phase), no extraction (no repo needed yet), no portal-vertical feature work, no per-plugin CI (D11), no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.17.0 → 0.18.0 (surface 13 second capability + the Events consumer; the plugin-owned guard/alias notes on surfaces 1/4; changelog); plan doc § Stage D D5 line annotated (block open, carve done); block-shape call for the closer session(s) decided at close (decision 8 pattern) and the next prompts drafted from what the carve surfaces.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the 393 kickoff survey still matches the code, state the carve plan, note any drift found — and resolve the open items: **(1)** the sibling `crm-plugin--payments` / `crm-plugin--events` / `crm-plugin--donations` / `crm-plugin--memberships` checkouts are current (`git pull` each; **an `crm-plugin--events` edit is expected this session** — the capability rider), and **(2)** the owner ratifies the auth-stack disposition (session prompt decision 2 — the guard, provider config, and middleware alias move into the plugin; the alternative that keeps auth infrastructure core is a one-line decision reversal at this point and a costly one later). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
