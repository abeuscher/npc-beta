# Base Prompt — Session 396 (Blog Carved, Packaged & Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/396. Blog Carved, Packaged & Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **396. Blog Carved, Packaged & Extracted**.

This is **arc position D6, the whole block in one session** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical). D5 closed at 395 (`crm-plugin--member-portal` v0.1.1; contract 0.19.0; `plugins/` empty a fourth time). This session runs the Blog block start to finish — carve (`plugins/Blog/`: `PostController` + the two `blog_prefix` routes, the BlogListing/BlogPager widgets, the Post admin resource) → package (`nonprofitcrm/blog`, P6 mechanics) → the **seventh extraction-runbook run** to `crm-plugin--blog`. **There is no squash-boundary redraw**: the survey found the blog owns no live tables — posts are `Page` rows of type `post`, and the `posts` table in the dump is dead schema (zero readers, zero rows; its drop is front-loaded decision 3). The block's design freight is the **BlogPager cross-plugin asset input** — `crm-plugin--events`' EventsListing declares core `app/Widgets/BlogPager/styles.scss`, resolved by lifting the shared pager styles core-side plus an Events **v0.1.3 rider** (front-loaded decision 2).

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.19.0 → 0.20.0 at close (Package shape + External repo seventh consumer; surface 11 cross-input case updated for the rider; the first no-owned-schema vertical extraction recorded; changelog).

**Session shape:** carve + package + extraction, behavior-preserving on the full composition. Routes 272/272 byte-identical (the moved controller FQCN is the 381 exception); no visual change; the one schema event is the dead-table drop (its own commit, 348-precedent identity check). **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D5 ✅, D6 open) and § 6.7 (models stay core — note there is no Post model; posts are Pages, and the Page model is core engine).
3. Read the 395 session log (`sessions/395. Member Portal Packaged & Extracted — Log.md`) — especially its **D6 kickoff input** section, and the flake postmortem (the ContactFactory nullable-email lesson: pin factory emails in arranges that feed NOT NULL columns).
4. Read `docs/plugin-contract.md` at 0.19.0 — § Package shape (P6 mechanics), § External repo (the extraction posture), § The distribution manifest (the `folder` → `path` → `vcs` walk and the guard interlock), surface 11 in full (the cross-plugin asset-input case — this session's design freight), surface 4 (front-of-house route ordering ahead of the catch-all).
5. Read `docs/plugin-extraction-runbook.md` — the seventh run; the 393/395 worked examples carry the same-session compression nuances.
6. Read the 394 session log (`sessions/archived/394. Member Portal Carved — Log.md`) for the carve playbook and the rider mechanics (the Events v0.1.2 precedent — this session ships v0.1.3), and the 393 log (`sessions/archived/393. Memberships Packaged & Extracted — Log.md`) for the one-combined-session package + extract playbook.
7. Open `sessions/396. Blog Carved, Packaged & Extracted.md` — the survey and front-loaded decisions; note the Open-Gate items (three ratifications; the owner-created `crm-plugin--blog` repo; sibling freshness with an Events edit expected).
8. Skim the surfaces: `app/Http/Controllers/PostController.php`; `routes/web.php` ~39–41 (the two blog routes) and ~108 (the catch-all); `app/Widgets/BlogListing/` + `app/Widgets/BlogPager/` (definitions, assets, the pager template's `type === 'post'` read); `app/Filament/Resources/PostResource.php` + its four pages (`$model = Page::class`, `HasPageBuilderForm`); `vendor/nonprofitcrm/events/Widgets/EventsListing/EventsListingDefinition.php` line ~45 (the cross-input declaration); `database/seeders/BasePageSeeder.php` ~72–100 (the blog page + News nav item); the `posts` table DDL in `database/schema/pgsql-schema.sql` (~line 1084 post-395); the survey's stays-core list (`PageObserver`, `CmsFormFields`, `PageBuilder`, `GeneralSettingsPage`, `NavigationMenuResource`, `LlmsTxtController`, `RandomDataGenerator`).
9. Note any drift between the session prompt's captured survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 395

- **CRM contract at v2.7.0** — 395 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D5 complete. **Six extracted plugins** (`crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` v0.3.0, `crm-plugin--events` **v0.1.2**, `crm-plugin--donations` v0.1.0, `crm-plugin--memberships` v0.1.0, `crm-plugin--member-portal` **v0.1.1** — the 395 flake-fix release); **`plugins/` is empty** until this session's carve refills it. Plugin contract **v0.19.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** eleven plugin-owned tables in their packages' migrations; the core dump is core-only (guard-enforced). The dead `posts` table sits in the core dump — its drop is this session's decision 3. **No blog tables move — this block has no redraw.**
- **Fast Pest suite green at 3391 / 0 parallel** (395 close: 3384 baseline + 7 additive). The `MemberPortal` testsuite (6 files, 44 tests) runs from `vendor/nonprofitcrm/member-portal/tests`. Expected delta this session: **additive** — the Blog twin/mirror/flag/boundary set; existing blog assertions unchanged beyond path/namespace re-points.
- **Route baseline: 272 routes** (dev/test), byte-identical to main at 395 — unchanged this session (the two blog routes move but keep names/URIs/middleware; the controller FQCN is the 381 exception).
- **Composition-safety state:** the member-portal capability gates every core portal reach (394 + 395 — do not touch); the blog needs **no capability** (nothing consumes blog presence — survey item 9) and the survey found no crash-capable core reads (no tables, no guard).
- **The twin fixtures this session carry no migrator path, permanently** — the Blog plugin owns no schema; their docblocks say so explicitly (not the expiring-caveat wording).
- **Playwright suite** — CI signal, non-gating; the parked chrome-parity spec remains the known failure. No local Playwright.
- **Housekeeping inbox** — the two 389-folded items and the `portal_form_collision` defaults item stand. This shape absorbs no items.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **The carve is the 389/392/394 playbook** at its smallest: provider + capability-free registration, the two routes registered at `boot()` ahead of the catch-all byte-identically, the two widgets nested under `Widgets/` with asset paths re-pointed, the rule-5 testsuite pass, twins/mirrors/flag/boundary guard. `widgets:sync` + `build:public` + bundle proof at the carve.
- **The pager-styles lift + Events v0.1.3 rider are decision 2's mechanics** (owner-ratified at the gate): shared pager rules to a core-owned stylesheet; BlogPager and the Events rider both declare the core path; the rider ships contract-first (wording → sibling edit → tag → core lock bump same-commit, `^0.1` unchanged). Gate 4 proves the pager rules from both declarations.
- **The dead-table drop is decision 3's mechanics** (owner-ratified at the gate): a core drop migration + dump regeneration whose diff is exactly the posts DDL (the 348-precedent identity check), `docs/schema/posts.md` removed with a README note. Its own commit.
- **The package + extraction are P6 + the runbook verbatim** (the 393/395 compression run): explicit version → path repo → manifest walk → Dockerfile COPY add; then repo copy (version field dropped, vendor-relative asset paths, README on the member-portal model), tag `v0.1.0`, core swap `path` → `vcs` + `git rm`, `composer update nonprofitcrm/blog` (composer before artisan), COPY lines removed, phpunit `Blog` suite → the vendor path, docs re-points. In-image proof: seven packages VCS-resolved, `plugins/` absent.
- **No redraw, recorded** — the no-owned-schema category is stated in the log and the contract note, never skipped silently (the 387/393 wording discipline).
- **Factory-email discipline:** any new arrange feeding a NOT NULL column pins its factory values (the 395 flake lesson — `ContactFactory` leaves `email` null 20% of the time).
- **The manifest is edited, never the generated file.** Scope is binding: no CMS-engine refactor (the `post` type and its core consumers stay), no blog capability, no per-plugin CI (D11), no FM surface, no D7 work. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.19.0 → 0.20.0 (seventh Package-shape + External-repo consumer; surface 11 cross-input update; the no-schema extraction note; changelog); plan doc § Stage D D6 line closed ✅; the D7 kickoff survey captured at close (Forms) and the 397 prompts drafted.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the survey still matches the code, state the carve → package → extract plan — and resolve the open items: **(1)** the owner ratifies the three front-loaded calls (one-session block shape; the pager-styles core lift + Events v0.1.3 rider; the dead `posts` table drop); **(2)** the owner has created the public `crm-plugin--blog` repository (blocks only the extraction phase); **(3)** the sibling checkouts are current (`git pull` each of the six — an **Events edit is expected** this session, the v0.1.3 rider). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
