# Base Prompt — Session 312 (Code Shape & Fit Audit — Cycle 1)

> **Scheduled as session 312.** First audit of the Code Shape & Fit track. Pulled forward from its original trailing-pair-after-cleanup-Cycle-3 slot at user direction. Pairs immediately with session 313 (apply); the two run back-to-back as one cycle.

**Start now.** Execute the reading list below, open the session prompt at `sessions/312. Code Shape & Fit (Audit) — Cycle 1.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **312. Code Shape & Fit (Audit) — Cycle 1.**

This is the **first audit half of the Code Shape & Fit track's first cycle.** The track doc at `sessions/tracks/code-shape-and-fit.md` is canonical for shape, workstreams, and discipline. The Deliberately Kept register at `sessions/refactor-track-deliberately-kept.md` already exists with three carve-outs (FieldMapper services, Page vs View distinction, inline-eligibility per-widget roster) — this session validates the register, surfaces stale entries for retirement, and proposes additions for the 313 walkthrough.

This session is **NOT boundary-touching** — no Fleet Manager surface, no CRM contract change. **No code changes during the audit** per the track's audit rule (this inverts cleanup's fix-in-place default; the inversion is deliberate). Every finding goes on the backlog and is ratified at session 313 before execution.

**Session shape:** track-phase / audit. Walk W-R1 (Cardinality) / W-R2 (Locality) / W-R3 (Directness) / W-R4 (Necessary vs accidental) / W-R5 (Seam quality) and produce backlog rows with Intent citation, plus Open Refactor Flags for over-scope items.

Before doing anything else:

1. Re-read session templates only if uncertain about a structural detail.
2. Read `sessions/tracks/code-shape-and-fit.md` — the track doc; workstream definitions, audit discipline, finding row format.
3. Read `sessions/refactor-track-deliberately-kept.md` — the carve-out register the audit must consult.
4. Read `sessions/session-outlines.md` § "Code Shape & Fit — 2-session cycle" stub.
5. Read `sessions/release-plan.md` for the cycle's plan-entry position.
6. **Read the full session prompt** at `sessions/312. Code Shape & Fit (Audit) — Cycle 1.md` — workstreams, discipline rules, log format, close-gate.
7. Note drift in a brief work-log entry.

---

## Starting state inherited from session 311

- **CRM contract at v2.3.0** — unchanged.
- **Schema baseline:** no schema changes since session 308.
- **Fast Pest at 311 close** — 2580 passed (=2547 baseline at 310 close + ~33 from A001 iterations + the iteration-2 typography fix). No new tests expected this session — audit-only, no code changes.
- **Playwright suite** running on every CI push. No new specs expected this session.
- **Planning state:** session 311 dropped its original Table-widget scope mid-session (wrong feature shape; rewind documented in 311's log). Landed work was the Page Builder picker slot-filter fix + a typography bucket cascade + nav-font CSS path. A001 cloud sessions extended import/export coverage; their VERSION track is `0.9NN.NN`, this session continues the `0.31N.NN` track. The Cycle 1 trigger was originally scheduled for ~session 324 per the track's "≈ session 324 or forcing function" rule — pulled forward at user direction; the audit operates on the codebase as it stands at 311's close rather than at cleanup Cycle 3's squash. Note the calibration delta in the audit log.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this session shape does not absorb housekeeping items; audit findings go on the W-R1 through W-R5 backlogs.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **No code changes during the audit** — every finding is a backlog row, not a fix. See the session prompt's "Design decisions" block for the discipline rationale.
- **Intent column requires citation.** Every finding's Intent cites a test name, docstring, commit message, session-log reference, or memory entry. No-citation findings file as intent-unrecoverable in a separate section.
- **Consult the Deliberately Kept register before landing a finding.** Findings matching a registered carve-out get skipped, not landed.
- **Class findings collapse into single rows.** N instances of the same structural shape → one class-finding row with both dispatch options.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **Questions are not instructions** per `feedback_questions_are_not_instructions`.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

312 is **immediately followed by 313 (apply).** At close, the audit log feeds the 313 prompt's Open Refactor Flags + W-R1 through W-R5 backlog sections; proposed register additions/retirements land in 313's walkthrough block. The 313 session prompt already exists — at close, update its placeholder sections (Open Flags, per-workstream backlogs, register walkthrough) with the audit's actual content.

### Phase 2 — Close

When the user says to close: write `sessions/312. Code Shape & Fit (Audit) — Cycle 1 — Log.md`. Update `completed-sessions.md`. **Archive `sessions/311. *` into `sessions/archived/`.** Commit on the final `session-312/N` branch, notify, do not push. Never push to main / force-push / merge to main. **Update `release-plan.md` and `session-outlines.md`** — mark the 312 child stub closed; preserve 313 stub untouched.

The new artifact `sessions/tracks/code-shape-and-fit-finding-template.md` (first-cycle-only artifact) ships with the session log commit. The Deliberately Kept register is **not edited during this session** — register changes are 313 walkthrough work.

---

## Style rules

Base-prompt style rules apply. Session-specific: log entries cite Intent sources verbatim where possible (don't paraphrase a docstring — quote it); class-finding rows are one row, not N; no docstrings/comments on code you did not write. **No fixes during the audit, even trivial-looking ones** — the discipline is the whole point of the audit/apply split.
