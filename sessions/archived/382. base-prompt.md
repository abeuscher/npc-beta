# Base Prompt — Session 382 (First Composer Package & Migration Squash)

**Start now.** Execute the reading list below, open the session prompt at `sessions/382. First Composer Package & Migration Squash.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **382. First Composer Package & Migration Squash**.

This is **arc position P6** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary) — the **Stage B entry**: packaging mechanics proven while iteration stays cheap. P6 makes the LogoGarden pilot the first true composer package (its own `composer.json`, resolved through a path repository, activation still owned by `config/plugins.php`) and lands the absorbed **T1 migration squash** (`release-plan.md` § T1, owner-dispositioned into this arc at 376): the three live migrations collapse into a regenerated `database/schema/pgsql-schema.sql` with the fresh-install identity check per the session-348 precedent. **Scope note settled at prompt-draft time:** the squash *boundary redraw* (core dump excludes plugin tables; plugin-owned migrations; per-composition identity check) is **P7's**, landing when the Events vertical becomes a package — the owner-approved 381 prompt fixed that location, and the plan doc's older P6 wording gets aligned at close.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**. The plugin contract (`docs/plugin-contract.md`) bumps 0.5.0 → 0.6.0 at close (surface 1's Stage B activation posture is deliberately amended — see the session prompt's decision 2). (The client-billing surface remains out of scope and guard-protected as always.)

**Session shape:** infrastructure / behavior-preserving. No route, schema-shape, or visual change; the diff is dependency plumbing (composer, Dockerfile, lockfile), a migration collapse, and docs. **Expect one or two iterations**; each phase below is independently committable. **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § 4d (distribution end state — what these mechanics grow into), § 6 disposition 7 (migrations vs squash, including the 381 events-owned schema annotation) and § 7 (the arc; note the P6/P7 wording this session aligns), and `docs/plugin-contract.md` v0.5.0 — surfaces 1, 5, 11, 12 and the in-repo plugin shape section (which gains the package shape at close).
3. Open `sessions/382. First Composer Package & Migration Squash.md` — the survey and front-loaded decisions there are settled; do not reopen them.
4. Read the squash surface: the three files in `database/migrations/`, the header of `database/schema/pgsql-schema.sql`, `docs/schema/README.md` (the 348 squash note), and the 348 procedure in `sessions/archived/348. Code Review & Cleanup (Cycle 3 — Migration Squash) — Log.md` (identity-check steps, the data-phase verification, the migration-coupled-test buffer precedent). Grep tests for the three migration filenames before deleting anything.
5. Read the packaging surface: root `composer.json` (autoload, scripts, plugins PSR-4), `composer.lock` (skim structure only), everything under `plugins/LogoGarden/`, `config/plugins.php`, `app/Providers/PluginServiceProvider.php`, both `composer install` layers in `Dockerfile` (~L16 and ~L104–140; note the manifests-before-source layer-caching shape the fix must preserve), and the composer steps in `.github/workflows/tests.yml` (~L61, L144) plus which CI jobs build the image.
6. Read the nets that must not notice: `tests/Feature/PluginPilotSession377Test.php` (+ the pilot removal coverage), `tests/Fixtures/Plugins/PaymentsRemovedTestCase.php` + `EventsRemovedTestCase.php` (the strip-the-line mechanism packaging must not disturb), `tests/Feature/EventsPluginSession381Test.php` (pins the plugin-boot-before-core-routes ordering via `api.events.json`), and the standing guards (`EventModuleBoundaryTest`, `PaymentModuleBoundaryTest`, `ConventionDriftTest`, `WidgetTemplateBoundaryTest`, `PluginAdminCssGuardTest`, `WidgetColorTokenConsumptionTest`).
7. Skim `sessions/session-outlines.md` (Plugin Architecture track entry) and `docs/app-reference.md` § Build server / § Environments.
8. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 381

- **CRM contract at v2.7.0** — 381 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** arc P5 complete (381) — the Events vertical lives in `plugins/Events/` (controllers/routes, quantities service, observers, three mailables, reminders command + schedule, policy, `EventResource` + four pages, both import wizard pages, seven widgets under `Widgets/`, the events importer); plugin contract **v0.5.0** (surfaces 1, 2, 3, 4, 8, 11, 12, 13 PROVEN; 10 PROVEN scoped to the events slice; 5, 6, 7, 9 DECLARED). Core sockets: `PluginAdminRegistry`, `CapabilityRegistry`, `ImporterRegistry`, all bound by `PluginServiceProvider` before the plugin loop. Standing guards `EventModuleBoundaryTest` + `PaymentModuleBoundaryTest` (zero allowlists). The typed `CheckoutSettled` inversion is live (Payments dispatches, Events listens). Landing-page factory is core (`App\Services\EventLandingPageFactory`) because models are core.
- **Schema baseline:** no schema-shape changes at 375–381. Three live migrations since the 348 squash (users two-factor, donations acknowledged_at, ticket_tiers is_complimentary) — this session's squash collapses them; **no shape change**, representational only. Events tables stay in the dump until P7 (381's § 6.7 annotation is the prepared input).
- **Fast Pest suite green at 3226 / 0** (381 close; +37 over the 380 baseline: Events twin/mirror, presence matrix, inversion, boundary guard, cancellation-wizard backfill). Expected delta this session: minimal — additive packaging assertions only if they earn their keep; the pilot + twin/mirror tests must pass unchanged.
- **Route baseline: 272 routes** — byte-identical before/after this session (nothing touches routing).
- **Playwright suite** — baseline preserved; does not run this session, CI is the signal.
- **Planning state:** 381 close marks P5 complete in the plan doc + track entry; the P6 recommendation (config-list activation authority, LogoGarden-first, P6/P7 squash split) is folded into the 382 prompt's front-loaded decisions.
- **Housekeeping inbox** — the 381 capture buffer was empty at close; nothing folded. This session absorbs no inbox items (infrastructure shape; the open `[test-integrity]` items (2)–(3) stay in the inbox).

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** No route changes (272/272 byte-identical, action FQCNs included — nothing moves), no schema-shape change (the squash is representational: the regenerated dump must produce the same schema the migrations produced), no visual change, no new third-party dependencies.
- **The config list is sacred.** `config/plugins.php` remains the sole activation + ordering authority (front-loaded decision 2); the package must not introduce Laravel auto-discovery. Every twin/mirror test that strips `config('plugins')` entries must pass unchanged — they are the proof the guarantee survived packaging.
- **Layer caching is a requirement, not a nicety.** The Dockerfile fix (manifests copied before `composer install`) must keep source-only edits from busting the dependency layers in both stages.
- **The identity check follows the 348 procedure exactly** — `migrate:fresh --seed` clean plus the data-phase spot verification from the 348 log. Run it against the regenerated dump before deleting anything from your working tree is unrecoverable (the migrations are in git history regardless).
- **The front-loaded decisions in the session prompt are settled.** Implementation details within them are yours to decide and log.
- **Scope is binding.** One package (LogoGarden). No Payments/Events packaging, no plugin-owned migrations, no test-suite moves, no per-install activation, no separate repos, no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Front-end build:** none expected. Widget folders do not move; asset paths do not change. If that assumption breaks, stop — it means scope drifted.
- **Contract doc update at close:** surface 1's Stage B sentence amended to the settled activation posture (config list authoritative; auto-discovery deliberately unused through Stage B); the in-repo plugin shape section gains the package shape (own composer.json, path repository, PSR-4 overlap note); bump to 0.6.0, changelog entry.
- **P7 input at close:** record in the session log what the package mechanics revealed for P7 (the vertical-as-package session), plus the two carried design notes: the EventsListing→BlogPager cross-plugin build input, and vanish-test semantics once schema is plugin-owned (data-kept-on-disable vs fresh-install-without-plugin).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code (the three migrations, the Dockerfile layer shape, the LogoGarden file set, the config-list mechanics), state the phase order and any drift found — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
