# Base Prompt — Session 252

We are about to begin a new session: **252. CI Test Suite Cascade — Root-Cause & Fix**.

This is an investigation-shaped session, lifted ahead of the planned E2 (Importer Mapping Page UX) work after a fresh CI run surfaced that **session 251's `memory_limit` bump from 256M to 1G fixed the OOM but did not fix the cascade.** The same 86-test failure pattern is back, this time with no memory signature anywhere in the output (zero `Allowed memory size` / `Fatal error` / `exhausted` mentions across 2202 lines of log). The cascade is **structural test-isolation**, not memory pressure. Session 252 finds the root cause and fixes it. Once green, session 253 (Importer Mapping Page UX) picks up where 251 left off on the feature track.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. **Read `sessions/test-failures.txt`** — the user-supplied CI output that motivated this session. The first FAILED entry is at line 17 (`BlogPagerEndToEndTest`); the last is at line 2197 (`TemplateTest`); summary at line 2199 reads `86 failed, 1724 passed`. Search-key the file for the three known pattern groups: `site_settings_key_unique` (`blog_prefix`), `navigation_menus_handle_unique` (`primary`), `help_articles_slug_unique` (`contacts` / `donations`). All three are rows that `database/seeders/DatabaseSeeder` (or its callees) inserts via `firstOrCreate`. The failing tests insert via `create()` (e.g., `BlogPagerEndToEndTest.php:13`).
3. Read `sessions/release-plan.md` — the 11 discipline rules. This session is **out-of-gate work** (a CI fix, not a release-plan working-set entry). It does not have a working-set entry; it lifts ahead of E2 because E2 cannot ship green tests on top of a broken suite.
4. Read `sessions/session-outlines.md` — Cross-Repo block (informational, no change), Active tracks (informational, this session is not boundary-touching).
5. Read `docs/app-reference.md` — environments, container names, the test-DB shape (`nonprofitcrm_test` on `postgres:5432` per `phpunit.xml`).
6. **For the test-bootstrap shape** — read these files end-to-end:
   - `phpunit.xml` (env vars, no `<server>` overrides for seeding)
   - `tests/TestCase.php` (currently a thin shim — confirm)
   - `bootstrap/app.php` (any test-environment hooks, scheduled commands, listener registrations that might fire on test boot)
   - `app/Providers/AppServiceProvider.php` (the `boot()` method — already known to read `SiteSetting::all()`, but does it write? Verify)
   - `database/seeders/DatabaseSeeder.php` (the seed source for `blog_prefix`, `primary`, `contacts`/`donations`)
   - `database/seeders/BasePageSeeder.php` (the seed source for `navigation_menus.handle = 'primary'`)
7. **For the failing-test shape** — read each of these files' headers + beforeEach blocks:
   - `tests/Feature/BlogPagerEndToEndTest.php`
   - `tests/Feature/PostTest.php`
   - `tests/Feature/TemplateTest.php` (failure trace points to line 211, but the actual exception fires inside the request — investigate)
   - `tests/Feature/ChromeWidgetTest.php` (navigation_menus collision)
   - `tests/Feature/HelpArticlePageTest.php`, `HelpArticleServiceTest.php`, `HelpSearchTest.php` (help_articles collision)
8. Open `sessions/252. CI Test Suite Cascade — Root-Cause & Fix.md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from 251

- **CRM contract at v2.1.0.** Fleet Manager `/api/logs` shipped at session 251. This session is not boundary-touching.
- **PHP `memory_limit` is 1G** ([docker/php/local.ini](../docker/php/local.ini)) — bumped at 251 close. The 1G is necessary but not sufficient; this session is the "not sufficient" fix.
- **The diagnosis at 251 close was wrong.** Session 251's log claims the memory cascade was the root cause of the 86 failures. The fresh CI run with 1G memory in place produced the same 86 failures with no OOM. Memory was an independent local OOM I reproduced at the time; the CI cascade has always been from something else. The 251 log is not retracted (memory bump is still correct), but the "cascade explanation" portion of that diagnosis was incomplete.
- **Bind-mount edit gotcha** documented at [feedback_bind_mount_edit_breaks_container.md](../.claude/projects/-home-al-nonprofitcrm/memory/feedback_bind_mount_edit_breaks_container.md). May or may not apply this session depending on what the fix touches.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any change**, **reproduce the failure locally** in a CI-shape environment. The fix must follow the reproduction, not precede it. If the failure does not reproduce locally with the same invocation pattern, that asymmetry is itself the next investigation thread (e.g., CI uses `127.0.0.1` Postgres on a fresh container per job; local uses the long-running `postgres` service container — different connection pooling, different settings).
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. This session especially — the fix shape is unknown until investigation completes.
- **Surface architectural choices before going deep.** If the investigation lands on "rewrite test bootstrap" or "rewrite all `db:seed`-using tests," surface the scope before going deep. The user may prefer a narrower in-session fix and a separate session for follow-up cleanup.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migration expected.** This session is test infrastructure, not schema. If verification surfaces a migration need, surface and decide.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the **full test suite** the same way CI runs it: `docker compose exec app php artisan test` (no excludes, no flags). The fast suite passing is necessary but not sufficient — the cascade was invisible to the fast suite at 251 close. Match CI's invocation exactly.
- **Front-end builds — when to run which:**
  - This session is test infrastructure only — no SCSS, no Vue, no widget assets, no admin CSS. **No build required.**
- **Cross-repo coordination — Fleet Manager agent contract.** Not boundary-touching. Standing rule applies as a passive check; if the fix surprisingly touches `app/Http/Controllers/Api/Fleet/*` or the contract, surface and pause.
- **Verify objective outcomes yourself; pull the user in for judgment.** This session's objective outcome is *"green CI."* Reproduce locally, fix, verify locally, then push to a CI test branch for end-to-end confirmation. CI green is the close-gate criterion.
- **Pause for manual testing only when human judgment is required.** This session is fully objective. The user comes in only for two judgment calls: (a) the *scope* of the fix when investigation surfaces alternatives; (b) the *commit shape* if the fix is large.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and CI verification are complete, Claude stops and waits.

The next session after 252 is **session 253 — Importer Mapping Page UX** (E2). The 253 base prompt + session prompt are already drafted at `sessions/253. base-prompt.md` and `sessions/253. Importer Mapping Page UX.md` (lifted from the original 252 drafts when 252 was renumbered to make room for this one). 252's close updates 253's "starting state" section if the test-isolation fix shape is relevant context for 253.

### Phase 2 — Close

When the user says to close (after 253's prompt is reconfirmed):

- **Session log**: write `sessions/252. CI Test Suite Cascade — Root-Cause & Fix — Log.md` against `template-session-log.md`. Capture the root cause clearly (this session's whole purpose is the diagnosis; the log is the record).
- **Update `sessions/completed-sessions.md`**: append session 252's row.
- **Update `sessions/release-plan.md`**: this session is out-of-gate (not a working-set entry). No checkmark. **Out-of-gate register**: add a one-line entry crediting the fix so future-us doesn't lose track of why CI was broken between 251 close and 252 close.
- **Update `sessions/session-outlines.md`** if the fix surfaces something worth recording as a project-level pattern (e.g., "all `db:seed`-using tests must use RefreshDatabase + here's why"). Otherwise no change.
- **No track doc updates** — this session does not belong to an active track.
- **Spec doc** — no Fleet Manager contract change.
- **Memory file** if a discipline-level rule emerges (e.g., a Pest pattern for test-bootstrap that future sessions should follow). Capture under `feedback_*.md` per the existing convention.
- **Archive the previous session**: move all files matching `sessions/251. *.md` into `sessions/archived/`.
- **Update 253's "Starting state" section** if 252's fix is relevant context for 253 — e.g., "test bootstrap was rewritten at 252; the new pattern is X."
- **Commit**: stage all changed files and commit on `session-252/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing test conventions exactly — `RefreshDatabase` is the standard trait; existing Pest test files are the model.
- Do not add features, refactor, or improve anything beyond what the session specifies. **Especially: do not improve test coverage in this session** — that's D4's territory.
- Prefer editing existing files to creating new ones — most likely the fix is a small change to a single test file, a single trait, or `bootstrap/app.php`. If the fix grows beyond that, surface scope before going deep.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- **No new tests for already-tested behavior.** The fix should make existing tests pass, not add new tests as a workaround.
