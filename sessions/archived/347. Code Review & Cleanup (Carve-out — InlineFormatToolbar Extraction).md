# 347. Code Review & Cleanup (Cycle 3 — Carve-out B: InlineFormatToolbar Extraction)

Behaviour-preserving legibility split of `resources/js/page-builder-vue/components/InlineFormatToolbar.vue` (≈1768 LOC, the dominant Vue outlier) into composables + popover sub-components. Carved out of the 345 apply session at the 344 close. The last session before the squash.

---

## Stub reference

Not a `release-plan.md`-numbered entry. Driven by the **344 audit log** (W11 + W4b) and the Cycle-3 carve-out rule in `sessions/tracks/code-review-and-cleanup.md`. Recorded in `release-plan.md`'s Cycle-3 cluster entry. Built originally at session 306 to `docs/inline-formatting-toolbar-spec.md`.

---

## Session-specific deltas

- **Admin-only, behaviour-preserving.** The owner confirmed at the 344 close: this component runs only in the page-builder editor, never in the public-site bundle, so length carries **no performance cost** — the split is justified by the >1k monolith/legibility concern alone, not bundle size.
- **Behaviour-sensitive surface.** Inline rich-text editing sits on the builder↔public render-parity surface (the 307 HTML-layer guard) over the Quill substrate (eight substrate bugs were fixed at 306). The cascading indicator timers, AbortController cancellation, and popover flip/anchor behaviour must survive unchanged.
- **Pest doesn't see Vue** — the behaviour net is the inline-editing Playwright suite (306, ~20 cases) + the `design`-group parity guard (307) + `npm run build`.

---

## Open questions to resolve at session start

- **Extraction boundaries** (architecture — settle before going deep): `useInlineToolbarPosition` (popover positioning/flip + measurement) and `useInlineLinkPopover` (the ~200-LOC link popover) are the clear composable seams. Decide which of the color/highlight, image, and heroicon popovers become sub-components vs stay inline. The orchestrator keeps format-state computation + control dispatch.
- **Standing spec?** Whether the existing inline-editing suite covers the extraction adequately or a new standing spec is warranted (keep if standing; tear down if one-off per the Playwright rule).

---

## Phases

Behaviour-preserving extraction, building after each step.

### Phase 1 — position/measurement composable
Extract the JS popover positioning/flip + measurement logic into `useInlineToolbarPosition`. Build + run the inline-editing Playwright suite.

### Phase 2 — popover extractions
Extract `useInlineLinkPopover` (the link popover) and the agreed popover sub-components (color/highlight/image/heroicon). Build + Playwright after each.

### Phase 3 — orchestrator slim + verify
The component retains format-state computation + dispatch; confirm it drops well under 1k. Full inline-editing Playwright suite + the `design`-group parity guard green. `npm run build` clean.

---

## Out of scope

- Any behaviour change to inline editing.
- The other Vue outliers (PreviewCanvas/ColorPicker/TypographyPanel/InspectorPresetGallery/GradientPicker) — principled domain complexity per the audit.
- `editor.ts` re-extraction (an apply-pick-sized W7 item, not this carve-out) and the 348 squash.

---

## Testing

- **Slow test groups:** none (Vue-only).
- **New tests expected:** possibly one standing Playwright spec if the extraction warrants finer coverage; otherwise the existing inline-editing suite is the gate.
- **Front-end build:** `npm run build` after every step (the only compile signal for Vue/TS).
- **Baseline:** fast Pest carried from 346 close (unchanged — no PHP). Playwright inline-editing suite + `design` parity guard must stay green.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific:

- **Log file:** `sessions/347. Code Review & Cleanup (Carve-out — InlineFormatToolbar Extraction) — Log.md` — before/after LOC + extracted composables/sub-components + Playwright/parity verification.
- **Branch:** `session-347/N`.
- **Track doc + outlines + release-plan** Cycle-3 status bump (carve-outs done; squash next).
- **Draft the 348 squash prompt + base prompt** (this is the last pre-squash session; its inputs now exist — use the 208→274 squash recipe; keep a Phase-5 buffer).
- **Archive:** `sessions/346. *` → `sessions/archived/`.
