# Base Prompt — Session 288

**Start now.** Execute the reading list below, open the session prompt at `sessions/288. Public Marketing Website — Pricing Chart Widget Build.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **288. Public Marketing Website — Pricing Chart Widget Build**.

This session continues the **Public Marketing Website track** (track doc at `sessions/tracks/public-marketing-website.md`). It is a **gap-resolution session lifted out of the Pricing page build** — the pricing-page spec authored at 287 close exposed a missing widget (no comparison-table primitive in the system), and the brief's "no invented widgets — surface for discussion" protocol routes that surface to a dedicated widget-build session. Per the user's call at 287 close, build the widget here, then run the Pricing page layout build at 289 against the now-existing widget. Sequenced inside the **Public Website Complete milestone**, between About (287) and Pricing layout (289). The downstream pages (Contact, Demo, page-capture harness + close) shift one slot each.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** feature-style. Build a new `pricing_chart` widget end-to-end: definition class with schema and defaults, Blade template, SCSS, demo data path (config-only or seeder per the agent's read of existing patterns), Pest tests for schema shape and rendering, public asset rebuild, and a `/dev/widgets/pricing_chart` thumbnail. The widget needs to render correctly in three surfaces — admin inspector (Vue), `/dev/widgets/{handle}` dev preview, and a public page that places it. The widget's downstream consumer at 289 is the Pricing page; build for that consumer's needs first, with the widget reusable for any customer's pricing page second.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. Read `sessions/release-plan.md` for the session's position inside Public Website Complete (this is a track-lifted gap-resolution session — no plan entry exists at session start; the entry lands at close).
4. Read `sessions/tracks/public-marketing-website.md` for the track's status snapshot, gap-resolution discipline, and the 287-close retrospective. Confirm the track-doc note acknowledging the Pricing-build → Widget-build → Pricing-build sequence shift.
5. Read **`sessions/public website/pricing-chart-widget-spec.md`** — canonical for the widget's data shape and visual intent. Implementation details (Blade structure, SCSS approach, how repeaters are realized, horizontal row alignment) are explicitly left to the agent. The spec's authority section says: when spec asks for something that conflicts with `widget-development.md` conventions, the conventions win — surface the conflict in the gap report. *(If the spec is not present at session start, stop and ask the user.)*
6. Read **`resources/docs/widget-development.md`** end-to-end. This is the canonical implementation guide — directories, definition class shape, schema field types, Vue inspector field-type → component map, asset bundling pipeline, demo data patterns (collection seeder vs `demoConfig()`), manifest metadata, presets, the quick-start checklist. The widget must follow these conventions.
7. Read `sessions/public website/pricing-page-spec.md` for context on how the widget will be configured downstream — Band 2 of the Pricing page (three columns: Instant Demo / 7-Day Trial / Monthly, with Monthly emphasized). The pricing-page spec is the widget's first concrete consumer; schema decisions should make that configuration natural.
8. Read `sessions/public website/audit-summary.md` for the system inventory (typography rows, button variants, widget registry, sample image library) and `sessions/public website/gap-report.md` for current gap state. **G-pricing-1** (border styling in `appearance_config`) is the open architectural gap this widget must work around — build the border slot, leave it unfilled at v1, ensure the v1.1 upgrade lands as a config change rather than a template rewrite. **G19** (no border knob for pull-quote / panel treatment) is the same underlying surface — the resolution path eventually serves both.
9. Read the existing `app/Widgets/ThreeBuckets/` widget folder end-to-end (definition, template, SCSS) as the closest existing reference — it's a structurally similar three-column content widget. Look at one or two collection-backed widgets too (e.g. `app/Widgets/BoardMembers/`, `app/Widgets/LogoGarden/`) so the collection-vs-config-array decision is informed.
10. Read `docs/app-reference.md` for environment / container / view-to-file map. Read `docs/schema/README.md` for the `widget_types` table file and skim that file to confirm the column shape this widget needs to seed against.
11. Open `sessions/288. Public Marketing Website — Pricing Chart Widget Build.md` and read the session prompt carefully.
12. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

---

## Starting state inherited from 287

- **CRM contract at v2.3.0** — unchanged. Session 287 was content-only; no boundary surface touched. This session adds a new widget — also no boundary surface.
- **Schema baseline:** no schema changes since 280-close. This session adds a row to `widget_types` via `WidgetTypeSeeder`, not a migration. Schema files under `docs/schema/` do not need updating unless the widget surfaces a need to extend an existing table (e.g. adding a `buttons` field type to `CollectionItem` if the columns are collection-backed and need per-column CTA repeaters — flag and ask before doing this).
- **Fast test suite at 2407 / 1 sequential** (287 close baseline). The 1 failure (`DashboardBuilderApiControllerTest > it rejects widg…`) pre-dates this session's scope. New tests expected this session: schema-shape coverage for the new widget definition (defaults round-trip, required fields present), render coverage (the widget renders without errors against a representative config), and the manifest validation test (`WidgetManifestTest`) auto-picks up the new widget — keep it green. If the agent ships presets, the manifest test enforces preset-shape rules. Update the widget count assertion in `WidgetPickerSession119Test` per the widget-development.md quick-start, item 11.
- **Playwright suite** — baseline preserved. No Playwright in this session; the dev-preview render at `/dev/widgets/pricing_chart` is curl-verifiable and the inspector render is manually verifiable in the admin. Page-capture harness still lands at the track's close (291, shifted from 290).
- **Planning state:** the Pricing-build / Widget-build / Pricing-build sequence shift agreed at 287 close. Updated track ordering: 288 = pricing chart widget (this session), 289 = Pricing layout rebuild against the widget, 290 = Contact, 291 = Demo, 292 = page-capture + track close. Track doc's status snapshot reflects the shift; release-plan entry for 288 lands at this session's close.
- **CMS surface area carried forward:**
  - Layout-level `appearance_config` round-trips through export/import (G1 ✅).
  - TextBlock carries `ctas` + `cta_alignment` (G2 ✅).
  - `secondary-dark` button variant for dark/gradient backgrounds (G3 interim ✅).
  - Typography defaults tuned (G13 / G14 ✅). H1 at 3.5rem, heading margin-bottoms calibrated. The fine-print "small body" surface called out in the pricing-page spec is **still open** — the widget's footnote slot needs a smaller body size; if the typography system has none, fall back to italic + lighter weight per the spec and log a continued gap.
  - Image widget carries `aspect_ratio` + `max_width` (285).
- **New gap-report rows from 287** (open):
  - **G19** — `appearance_config` has no border knob (surfaced by About's pull-quote treatment). The pricing chart widget hits the same surface for card borders. The widget should build the border slot (template hook + SCSS reservation) but not depend on it for v1; v1.1 = appearance_config extension lights up the slot.
  - **G20** — Global `.widget--text_block { height: 100% }` rule breaks stacked text widgets in a single layout cell. Not directly exercised by this session (widget is its own SCSS namespace), but worth flagging if the pricing chart's card height-equalization needs a similar rule and you want to avoid the same trap — scope the rule with `:only-child` or use flex column + `margin-top: auto` per the spec's design note.
- **Open from earlier sessions:**
  - **G15 / G16** (Hero-in-cell rendering quirks) — not exercised this session.
  - **G17** (TextBlock CTA-only band placeholder requirement) — not exercised this session.
  - **G18** (Image cell-placement controls) — not exercised this session.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — feature sessions absorb items only when a finding directly ties to the work at hand. Touch only if a widget-system housekeeping item directly affects the build; otherwise carry forward.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify. Read at least one existing widget end-to-end before authoring this one — `ThreeBuckets` is the closest structural cousin.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. *Session-288 note: no integrations in scope. CTAs in the widget use the existing button-style system; sample images for the demo data come from `SampleImageLibrary` per the standard pattern.*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. *See the drift and decision-threshold rules below for the calibration of what counts as a decision point worth surfacing.*
- **Surface architectural choices before going deep.** Four load-bearing choices were resolved at planning time and are spelled out as settled deltas in the session prompt: **(1)** columns are an in-widget config repeater (not a collection); **(2)** per-column "Emphasize this column" toggle (not a widget-level emphasized_column_index; multiple emphasis allowed); **(3)** cross-card row alignment via CSS subgrid; **(4)** ship one v1 preset matching the marketing site's three-tier configuration. Build directly to those — they aren't open questions to re-litigate. The remaining open surfaces in the session prompt (the new `repeater` field type's exact shape, whether to compose the existing `buttons` field type for per-column CTAs, and whether `widget_types` needs any schema-level additions) are smaller — surface briefly, decide, note in the work log. Reserve "pause and ask" for things that change the resolved decisions or surface a system extension larger than a new field type.
- **Adapt to drift; don't ask about it.** Widget-development.md is the canonical implementation reference but small surface details (file paths, helper names, exact method signatures) may have moved. Default to silent adaptation + a one-line work-log note. Flag only when drift changes intent (e.g. a documented pattern is no longer the convention).
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that propagate across the widget's surface or set a precedent for future widgets. Local choices (variable naming, SCSS class composition, where in the template a guard goes) — just decide.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot. *Session-288 note: the build server is involved (asset bundling). If `php artisan build:public` fails because the build server is unreachable, stop and ask.*
- **Run migrations via Docker** after writing them. *Session-288 note: no migrations expected. If the agent reaches for a migration to extend `CollectionItem` field types or `widget_types` schema, stop and ask first — that's an architectural decision that propagates beyond this widget.*
- **Update `docs/schema/`** after writing any migration. (No-op this session unless a schema extension lands per the line above.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 2407 baseline + new tests for this widget (config shape, render, manifest if presets shipped). The 1 pre-existing failure remains; everything else green.
- **Front-end builds.** This session touches `resources/scss/` (likely — widget SCSS at `app/Widgets/PricingChart/styles.scss`) and `widget_types.assets` (definitely — declares the SCSS path). Decision rule per the template:
  - Widget SCSS or any file referenced in `WidgetType.assets` → `docker compose exec app php artisan build:public`. **Required this session.** Confirm `public/build/widgets/manifest.json` reflects the new widget's bundle entry afterward.
  - If the agent adds anything under `resources/js/` (e.g. a new Vue field component for the inspector — only needed if a new field type lands) → `npm run build`. Likely no-op for this session.
- **Tests for new behaviour.** Required. The widget's definition class needs schema-shape coverage (Pest test asserts the schema keys, defaults round-trip, and required fields are present). The render path needs at least one happy-path test (e.g. a Pest test that places the widget on a page with representative config and asserts the rendered HTML contains the column titles, prices, and CTAs). The `WidgetManifestTest` auto-picks up the new widget — if presets ship, that test enforces appearance-group-only rules.
- **Fast vs slow classification.** The widget's new tests should fit in the fast suite (under 5s each). If a render test exercises full page composition + media library setup and runs slow, group it `slow`; otherwise default.
- **Playwright artifact hygiene.** N/A this session — no Playwright spec lands.
- **Cross-repo coordination — Fleet Manager.** N/A.
- **Verify objective outcomes yourself; pull the user in for judgment.** Objective surfaces — all of which the agent closes the loop on:
  - Widget definition registers cleanly via `WidgetServiceProvider` and `WidgetTypeSeeder` runs without error.
  - Schema-shape Pest test passes; render Pest test passes; full fast suite at 2407 + new tests / 1 (pre-existing).
  - `/dev/widgets/pricing_chart` returns 200 and the demo render shows three columns in spec shape (curl + spot-check the HTML; one Playwright screenshot is reasonable for the user-review handoff).
  - Public widget asset bundle rebuilds cleanly (`build:public`) and the manifest carries the new entry.
  - The widget can be placed on a test page through the admin and saves cleanly (if the agent has time to manually exercise this path it's worth the loop-close; if not, the inspector render will be verified at session-end handoff).
  - Thumbnail generates via `node scripts/generate-thumbnails.js --widget=pricing_chart` and writes to `app/Widgets/PricingChart/thumbnails/static.png`.
  
  User-judgment surfaces — surface for review at session-end, don't decide unilaterally:
  - The visual treatment of the cards (white background, padding, emphasis tint on the recommended column) — does it read as "card-like enough" without borders, or is the v1 degraded version too subtle?
  - Whether the agent's columns-repeater shape decision matches the user's intuition for how customers will configure pricing tables.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that the widget is built, registered, asset-bundled, dev-preview-verified, and ready for the user to (a) place it on a scratch admin page and exercise the inspector, (b) review the v1 visual treatment, (c) decide whether the columns-repeater shape is right for the downstream consumer at 289. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/289. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to **289. Public Marketing Website — Pricing Layout Rebuild**. The pricing-page spec at `sessions/public website/pricing-page-spec.md` is the canonical input; the now-existing pricing chart widget is the Band 2 carrier. Layout-spec-driven execution pattern continues per 286 / 287.
- **Next session prompt** — draft against the layout-spec-approach pattern that 286 and 287 validate, with one delta: Band 2 is now a single `pricing_chart` widget instance configured against the spec's three columns, not a layout-container + text-block-cards composite. The spec's "Gap callout — Border styling" section becomes a one-line note that v1 widget ships with background-tint emphasis only, per session-288's resolution path.

**Pre-next-session ask in the close handoff.** None expected — the pricing-page spec is already in place (authored before 287 close), and the pricing chart widget exists after this session. The only user-side decision before 289 starts is whether any visual revisions to the widget are needed based on the manual review at 288 close.

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/288. Public Marketing Website — Pricing Chart Widget Build — Log.md` using `template-session-log.md` as the format reference. Document: the columns-repeater shape decision (collection vs config-array, with rationale), the demo-data path picked, schema field choices, any system extensions made (and why each was unavoidable), gap-report appends, manual-testing observations from the user's review, and validation that the widget meets the pricing-page spec's downstream needs.
- **Update `sessions/public website/gap-report.md`:** if the build resolved any open gap (most likely G-pricing-1's workaround surface, possibly G19 if the border slot work touches `appearance_config`), mark the resolution. Append any new rows surfaced.
- **Update `sessions/completed-sessions.md`:** append session 288's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status to reflect 288 closure (widget built, Pricing layout queued at 289).
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (last update date; widget complete; sequence shift codified; Pricing layout queued at 289).
- **Update `sessions/release-plan.md`:** add the retroactive entry for session 288 (the lifted gap-resolution session) and mark it ✅ closed at session 288. Adjust downstream entries' execution-order positions to reflect the one-slot shift (289 Pricing → 290 Contact → 291 Demo → 292 page-capture).
- **Update `resources/docs/widgets.md`** if the file is the canonical widget index (check at close — if widgets are listed there, add the pricing chart row). If a per-widget help doc is conventional (check the `resources/docs/widget-*.md` family), create `resources/docs/widget-pricing-chart.md` following the existing frontmatter convention.
- **Archive the previous session:** move all `sessions/287. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-288/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Pricing chart widget: definition + template + SCSS + demo data + Pest coverage".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.** Match the existing widget code's style (brevity, no JSDoc, no inline narration).
- **The widget is a real product surface, not just a marketing-site one-off.** Code quality matches the rest of `app/Widgets/`. The widget should make sense to a customer building their own pricing page in the CMS.
- **Follow widget-development.md's quick-start checklist literally.** Items 1–11 are the canonical author-a-new-widget recipe. Don't invent variations.
- **The border-styling gap (G-pricing-1) is open.** Build the v1 widget without depending on borders for visual differentiation, but build the slot so the v1.1 upgrade is a config change, not a template rewrite. Per the spec: "Don't hardcode a no-border assumption into the widget's template — build the border slot, leave it empty until appearance config can fill it."
