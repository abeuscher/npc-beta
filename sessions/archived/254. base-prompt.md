# Base Prompt — Session 254

We are about to begin a new session: **254. Importer Mapping Page UX**.

This session lands E2 from `sessions/release-plan.md` — three focused UX improvements to the **Map Columns** step of the importer wizard. The shared infrastructure already exists ([`InteractsWithImportWizard`](../app/Filament/Pages/Concerns/InteractsWithImportWizard.php) trait, six thin consuming pages, identical blade views); this session edits the two row-builder methods that all six wizards funnel through, plus a CSS sprinkle. No refactor, no new abstractions. **Not boundary-touching** — Fleet Manager contract stays at v2.1.0.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md` — the 11 discipline rules + § E2.
3. Read `sessions/session-outlines.md`:
   - Active tracks block (informational — Fleet Manager track is active but this session is not boundary-touching).
   - Cross-Repo block — confirm v2.1.0 going in; this session does not change it.
   - The "Importer Mapping Page UX" stub *(if it still exists — may have been folded into release-plan E2; verify)*.
4. Read `docs/app-reference.md` — environments, container names, view-to-file mappings, key dependencies. **Important for this session:** the `public/css/admin.css` hand-edit convention (no build step) and Filament admin asset pipeline. No schema docs expected (no migration this session).
5. **For the shared row-builder pattern** — read [`app/Filament/Pages/Concerns/InteractsWithImportWizard.php`](../app/Filament/Pages/Concerns/InteractsWithImportWizard.php) end-to-end. Pay particular attention to:
   - `buildNamespacedMappingRow($header, $groupedOptions, $customSentinels)` (the per-column row used by donations / events / memberships / notes / invoice-details).
   - The state shape — `column_map.col_N` is the per-column destination key; `cf_*_N` are the custom-field sub-form keys.
   - `getReviewStepSchema()` (Step 2) and `buildNamespacedPreviewSchema()` (Step 4) for context — these stay untouched this session.
6. **For the Contacts divergence** — read [`app/Filament/Pages/ImportContactsPage.php`](../app/Filament/Pages/ImportContactsPage.php) lines 521–628 (`columnMappingRowSchema`). Same shape as the trait's namespaced row, parallel edits required to keep them in sync.
7. **For one consuming page (representative)** — read [`app/Filament/Pages/ImportDonationsPage.php`](../app/Filament/Pages/ImportDonationsPage.php) end-to-end. Confirms how `getColumnMappingSchema()` wraps the row builder loop.
8. **For the existing CSS conventions** — `grep -n "import-" public/css/admin.css` to see whether any importer-related rules already exist.
9. Open `sessions/254. Importer Mapping Page UX.md` and read the session prompt carefully.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.1.0** (or whatever 253 produced — read 253's log). Fleet Manager `/api/logs` shipped at session 251. Session 253 was a Fleet Manager pivot planning session that absorbed FM-side security architecture changes. Per the 253 brief, no version bump was expected (additive documentation revision only) — but verify against 253's log before assuming the contract version stayed flat.
- **Session 253 deliverables may now be present on every install.** The 253 pivot brief named three CRM-side artifacts: `bin/rotate-fm-cert.sh` (operator-facing cert rotation script), `docs/runbooks/fm-compromise-recovery.md` (operator runbook), and additive language in `docs/fleet-manager-agent-contract.md` (security posture section). Whether all three landed in 253 or were folded into A2 depends on the sequencing decision 253 made (option A / B / C from the brief). Read 253's log to confirm what's in the tree going into this session.
- **PHP `memory_limit` is now 1G** ([docker/php/local.ini](../docker/php/local.ini)) — bumped from 256M at 251 close. The 1G headroom is real but not infinite; if this session adds memory-heavy tests (Playwright fixtures, image-manipulation paths), keep an eye.
- **CI test suite is green** as of session 252 close (Test Suite CI Cascade — Root-Cause & Fix). The cascade pattern that surfaced post-251 (unique-constraint violations on seeded keys across `site_settings` / `navigation_menus` / `help_articles`) was structural test-isolation, not memory. 252's fix lands the working test suite that 254 builds on top of.
- **Bind-mount edit gotcha is documented** at [feedback_bind_mount_edit_breaks_container.md](../.claude/projects/-home-al-nonprofitcrm/memory/feedback_bind_mount_edit_breaks_container.md). Doesn't apply to this session — admin.css is served directly out of the bind-mounted tree (`./:/var/www/html`), not a single-file mount.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration**, ask the user what existing system handles it. This session leans entirely on existing infrastructure (`InteractsWithImportWizard` trait, Filament Forms components, `public/css/admin.css`). Do not introduce new patterns or new asset pipelines.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** This session's load-bearing question — *how does the row's status class get bound to its mapping state* — is settled in the session prompt's Design decisions (Filament `Group::extraAttributes()` reactive closure). If implementation surfaces a reason to deviate, surface it before going deep.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migration expected.** No schema changes. The session edits two PHP methods + admin.css.
- **The two row builders must be edited in parallel.** `buildNamespacedMappingRow()` in the trait and `columnMappingRowSchema()` in `ImportContactsPage` return almost-identical component arrays. Whatever wrapping/styling lands in one must land in the other. The session-close test pass should walk both code paths.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline carries over from 252 close (numbers will be in 252's log). Expect modest growth — new tests for the row-builder structural assertions (incomplete-class on unmapped rows, complete-class on mapped rows, `searchable=true` on the Select).
- **Front-end builds — when to run which:**
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - No SCSS, no Vue, no widget asset changes — no `npm run build` or `build:public`.
- **Cross-repo coordination — Fleet Manager agent contract.** This session is **not** boundary-touching. The standing cross-repo discipline applies as a passive check — if the implementation surfaces any change to `app/Http/Controllers/Api/Fleet/*`, `routes/api.php`, `docker/nginx/*.conf`, or the contract spec doc, pause and surface. Otherwise, no change to the Cross-Repo block.
- **Verify objective outcomes yourself; pull the user in for judgment.** This session has both kinds of work — the Pest tests and the structural HTML assertions are objective (close the loop yourself). The visual feel — does the green check look right against the Filament admin's existing palette, does the row spacing read clean — is a judgment call (announce that testing is ready, then stop and wait). **Playwright is a verification mechanism, not just an existing test suite** — for the visual confirmation, write a small spec walking through one importer's mapping step (upload a fixture CSV, advance to Step 3, map a column, assert the green-check class appears, assert the unmapped sibling rows still carry the red-X class). Spec stays in the suite afterwards.
- **Pause for manual testing only when human judgment is required.** After implementation + fast suite + Playwright verification, announce that visual testing is ready (the user opens an importer page and walks through a real CSV). Stop and wait. Do not suggest closing the session.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The next session after 254 depends on what 254 surfaces. Default forward path per `sessions/release-plan.md` execution order:

- **B1 — Organizations Model Overhaul** (next in sequence; B2's other prerequisite alongside E3 already shipped at 250 and E2 shipping at this session).
- If 254 surfaces UX findings the user wants to address before B1 (e.g., a row-spacing issue that wants a follow-up pass, a discovered gap in the importer's behavior), those land first.

Decide at 254 close. Draft the 255 base prompt + session prompt against the agreed shape.

### Phase 2 — Close

When the user says to close (after 255's prompt is agreed):

- **Session log**: write `sessions/254. Importer Mapping Page UX — Log.md` against `template-session-log.md`.
- **Update `sessions/completed-sessions.md`**: append session 254's row.
- **Update `sessions/release-plan.md`**: check off E2 (✅) with a "closed at session 254" note plus a one-line summary of the three landed items.
- **Update `sessions/session-outlines.md`**:
  - If a stand-alone "Importer Mapping Page UX" stub still exists, fold it into the E2 closure note or remove it.
  - **Do not add a stub for "Optional grouping by entity"** — that path was deliberately deferred at 252-prompt-drafting time on the grounds that programmatic regex-based grouping is brittle without LLM/MCP-shape help, and revisiting it requires user feedback on real importer use, not a stub-and-defer dance. If the question reopens it does so via a fresh conversation, not a stub.
- **No track doc updates** — this session does not belong to an active track.
- **Spec doc** — no Fleet Manager contract change; no edits to `docs/fleet-manager-agent-contract.md`.
- **Archive the previous session**: move all files matching `sessions/253. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-254/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly — `InteractsWithImportWizard` is the row-builder model; `ImportContactsPage::columnMappingRowSchema` is the parallel Contacts variant; existing `np-*` BEM-pattern class names in `public/css/admin.css` are the styling model (per `docs/app-reference.md`).
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones — both row builders, `public/css/admin.css`, and one Pest test file are the expected edits. The Playwright spec is a new file (under `tests/e2e/` per existing convention).
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- No new admin pages, resources, or migrations expected this session — pure UX changes inside existing surfaces.
- **Cross-importer mirror discipline** — every change to `buildNamespacedMappingRow()` lands in `columnMappingRowSchema()` in the same commit. Drift between them is a process failure.
