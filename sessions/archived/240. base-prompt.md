# Base Prompt — Session 240

We are about to begin a new session: **240. Fleet Manager Agent — Contract v1.1.0 (`unknown` Status for `last_backup_at`)**.

This is a small, focused, single-purpose boundary-touching session. The Fleet Manager workstream has flagged that `last_backup_at` reporting `status: "green"` when its `value` is `null` is semantically wrong — null means "we don't know," not "things are fine." This session lands the additive contract bump (`v1.0.0 → v1.1.0`): adds `unknown` as a fourth valid subcheck status value, flips `HealthController::checkLastBackupAt()` to emit `unknown` instead of `green` when no backup pipeline exists yet, and treats `unknown` as equivalent to `yellow` in the overall-status worst-of derivation.

Sequencing rationale: the FM repo's next session (FM 004 — CRM Integration — Health Polling Skeleton + HTTP Client) is also boundary-touching and consumes the contract. Landing v1.1.0 *before* FM 004 starts means FM's contract validator builds against the final shape from day one, with no retrofit. Phase 2 (Backup Pipeline) stays queued — its forcing function (second client install in flight, or FM v1 launch approaching) has not fired.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — the Cross-Repo block at the top is the live coordination state, and the Fleet Manager Agent track entry confirms Phase 2 stays queued (this session is not Phase 2).
3. Read `sessions/tracks/fleet-manager-agent.md` — status snapshot, Phase 1 retrospective, forward plan. The Phase 2 plan section calls out the contract-bump option explicitly (1.0.0 → 1.1.0 vs rolling into Phase 2).
4. Read `sessions/fleet-manager-planning-spec.md` — the canonical product spec for both repos. Sections "Two-Repo Coordination Protocol" and the contract-evolution discipline are the rules this session operates under.
5. Read `docs/fleet-manager-agent-contract.md` — the spec doc this session bumps. Pay attention to the status-enum surface, the response 200 schema, the overall-status worst-of rule, and the existing CHANGELOG.
6. Read `docs/app-reference.md` for environment names, container names, and the Fleet Manager controller location.
7. Read the most recent CRM session log establishing the immediate context: `sessions/239. Donation Status Enum Alignment (Importer + Backfill) — Log.md` (or `sessions/archived/239. *.md` if archived). 239 was a parallel-track session (importer fix, not Fleet Manager) and does not block 240; it establishes the current `main` baseline.
8. Read the full state of every file this session edits:
   - **The boundary surface (changes this session):**
     - `app/Http/Controllers/Api/Fleet/HealthController.php` — `CONTRACT_VERSION` constant, `checkLastBackupAt()` method, `index()` overall-status composition.
     - `docs/fleet-manager-agent-contract.md` — version field, status enum table, response example, worst-of rule, CHANGELOG.
     - `sessions/session-outlines.md` — Cross-Repo block (version, last-boundary-touching session, pending changes).
     - `sessions/tracks/fleet-manager-agent.md` — status snapshot + retrospective for the v1.1.0 bump.
   - **The auth + middleware layer (read-only context — not changed this session):**
     - `app/Http/Middleware/AuthenticateFleetManagerAgent.php`
     - `bootstrap/app.php` (alias registration)
     - `routes/api.php`
     - `config/fleet.php`
   - **The test file (changes this session):**
     - `tests/Feature/Api/Fleet/HealthEndpointTest.php` — adjust existing cases that assert `green` on `last_backup_at` or assert `contract_version === '1.0.0'`; add new cases for the `unknown` status semantics and the worst-of derivation.
9. Read FM-side context for sequencing awareness — these confirm the local cache state but are not edited from the CRM repo:
   - `/home/al/fleetmanager/docs/imported/fleet-manager-agent-contract.md` — the FM-side cached copy of v1.0.0. After 240 ships, FM-side will refresh this on its next boundary-touching session (FM 004). The CRM does not update the FM cache; FM does.
   - `/home/al/fleetmanager/sessions/session-outlines.md` Cross-Repo block — confirms the pending change FM-side requested.
10. Open `sessions/240. Fleet Manager Agent — Contract v1.1.0 (unknown Status for last_backup_at).md` and read the session prompt carefully.
11. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migrations expected** this session — pure-PHP contract evolution. If a migration becomes necessary, stop and ask.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds:** none expected. Pure-PHP session.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session adjusts ~3 existing cases (contract_version literal, all-green happy path, subcheck status enum) and adds ~3 new cases (`last_backup_at` emits `unknown`; overall is `yellow` when only `unknown` and `green` subchecks present; overall is `red` when any red regardless of `unknown` presence).
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Cross-repo coordination — Fleet Manager agent contract.** **This session is boundary-touching by design — the entire scope is the contract surface.** Update `docs/fleet-manager-agent-contract.md` (body + CHANGELOG + bump `Contract Version: 1.0.0 → 1.1.0` + bump `Status:` if shape evolves — likely stays `active`) and the Cross-Repo block in `sessions/session-outlines.md` (version, last-boundary-touching session: 238 → 240, clear pending-changes line) before announcing completion. The FM-side cached copy at `/home/al/fleetmanager/docs/imported/fleet-manager-agent-contract.md` is **not** updated by this session — FM-side refreshes on its next boundary-touching session per the Two-Repo Coordination Protocol.
- **Pause for manual testing.** Announce that testing is ready (the manual test is curl-based — hit `/api/health` with the bearer token, confirm the `last_backup_at` subcheck reports `status: "unknown"` and the top-level `status` is `yellow`). Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents. **241 candidate slots:**

- **Fleet Manager Agent — Phase 2 (Backup Pipeline).** Greenfield CRM-side backup (pg_dump + media → DigitalOcean Spaces, scheduler cadence, success-record location populated for `last_backup_at` — flipping the subcheck from `unknown` to threshold-driven). Lands when the forcing function fires.
- **Phase B — Admin gating generalization** from the Financial Data Origin track.
- **Code Review & Cleanup audit-then-apply pair** (the 205/206 pattern).
- **Notes Permissions & Permissions Audit** stub — deferred since session 198.
- **Other Beta-1 stubs** from the roadmap.

The user picks at 240 close.

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/241. base-prompt.md`.
- **Next session prompt**: draft `sessions/241. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/240. Fleet Manager Agent — Contract v1.1.0 (unknown Status for last_backup_at) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 240's row.
- **Update `sessions/session-outlines.md`** Cross-Repo block: agent contract version `1.0.0 → 1.1.0`; last boundary-touching session in this repo `238 → 240`; pending boundary changes line cleared (the `unknown`-for-null change is now landed).
- **Update `sessions/tracks/fleet-manager-agent.md`**: bump status snapshot date; add a compressed retrospective entry under Phase Retrospectives for the v1.1.0 bump (sub-phase, not a full new phase — Phase 2 still owns the eventual threshold-driven flip). Forward plan unchanged.
- **Production deployment notes in the log**: explicit one-liner — *"No migration. The next deploy emits the new `unknown` status and `contract_version: '1.1.0'` automatically. Fleet Manager (when it begins polling against this contract) will see `last_backup_at.status === 'unknown'` instead of `'green'` until the Backup Pipeline (Phase 2) lands."*
- **Archive the previous session**: move all files matching `sessions/239. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (the controller edit + the spec doc edit + the test edits + the log + the updated outlines Cross-Repo block + the updated track doc + the completed-sessions index + the 241 base prompt + session prompt + any files moved into `sessions/archived/`), commit on the current `session-240/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
