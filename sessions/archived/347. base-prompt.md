# Base Prompt — Session 347 (Code Review & Cleanup — Carve-out B: InlineFormatToolbar Extraction)

**Start now.** Execute the reading list below, open the session prompt at `sessions/347. Code Review & Cleanup (Carve-out — InlineFormatToolbar Extraction).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **347. Code Review & Cleanup (Carve-out B — InlineFormatToolbar Extraction).**

This is **carve-out B** of Cycle 3 — the second of the two >1k-line file-splits lifted at the 344 audit close. Order: audit 344 ✅ → apply 345 ✅ → carve-out A 346 ✅ (bundle Import/Export) → **carve-out 347** → squash 348. The finding is W11/W4b in the 344 audit log. **This is the last session before the squash — draft the 348 squash prompt at this session's close.**

This session is **NOT boundary-touching** — no Fleet Manager surface. CRM contract stays **v2.3.0**, no `CONTRACT_VERSION` change.

**Session shape:** track-phase (carve-out — legibility refactor). Split `resources/js/page-builder-vue/components/InlineFormatToolbar.vue` (≈1768 LOC, the dominant Vue outlier, session 306 inline editing) into composables + popover sub-components, shrinking the orchestrator. **Admin-only** — this component runs only in the page-builder editor; it is never in the public-site bundle, so the split is **legibility/maintainability only, zero public-render impact.** Behaviour-sensitive (inline rich-text editing on the builder↔public parity surface, Quill substrate) — needs Playwright coverage.

Before doing anything else:

1. Re-read the session templates.
2. Read the **344 audit log** W4b for the seam analysis: clean composable boundaries are `useInlineToolbarPosition` (the JS popover positioning/flip + measure block) and `useInlineLinkPopover` (the ~200-LOC link popover); the color/highlight/image/heroicon popovers are candidate sub-components; the orchestrator keeps format-state computation + dispatch.
3. Read `docs/inline-formatting-toolbar-spec.md` (the 306 design contract the toolbar was built to) and `docs/app-reference.md` (page-builder map).
4. Read the standing builder↔public render-parity guard (the `design` Pest group, added 307) and the inline-editing Playwright specs (the 306 ~20-case suite; 307's HTML-layer parity guard) — these are the behaviour net.
5. **Read `InlineFormatToolbar.vue` end-to-end before touching it.** Re-verify the 345 front-end edits (editor.ts dead-export removal /3; `hashFile.ts`→`utils/` + `helpers/`→`utils/` consolidation /6) — they're siblings; `InlineFormatToolbar` itself wasn't touched by 345 but its imports may reference the moved utils.
6. Note drift vs the audit's seam map; proceed unless a decision is needed.

---

## Starting state inherited from session 346

- **CRM contract at v2.3.0** — unchanged. 346 split the bundle Import/Export files into per-domain collaborator classes (behaviour-preserving; ContentExporter 1168→404, ContentImporter 1961→555). Non-boundary. **346 closed at VERSION 0.346.01** (so 347 closes at `0.346.<iter>`).
- **Schema baseline:** unchanged. This carve-out is Vue-only — **no migration, no PHP.**
- **Deploy note (informational):** mid-346 an emergent FM-surfaced **deploy-brick** was fixed on `session-345/2` (a stale `bootstrap/cache` package manifest naming the 342-removed Debugbar provider shadowed the new image at bootstrap; fix = entrypoint manifest-clear + baked `HEALTHCHECK`/`app:health-check` + dropped the persistent cache volume + `BootstrapCacheVolumeTest`), **merged to main + deployed by the owner at VERSION 0.345.07**, and merged into the 346 branch. Not in 347's scope — noted only so the deploy-path/test changes aren't a surprise.
- **Fast Pest baseline:** **2816 / 0** carried from 346 close (2814 refactor baseline + the 2 `BootstrapCacheVolumeTest` guards from 345/2). Pest does not exercise Vue — the behaviour net here is **Playwright + the `design`-group HTML-parity guard**, plus `npm run build` (Pest won't catch Vue/TS errors).
- **Playwright:** the inline-editing specs are the behaviour gate; run them. A new standing spec may be warranted if the extraction warrants finer coverage (decide at the open gate; keep it if standing, tear down if one-off).
- **Planning state:** Cycle 3 — audit + apply + carve-out A done; this is carve-out B, the last session before the squash. **The 348 squash prompt is drafted at this close** (its inputs — accumulated migrations + 345-347 leftovers — now exist).

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Behaviour-preserving only.** The cascading indicator timers, AbortController semantics, popover flip/anchor behaviour, and Quill-substrate interactions must survive the split byte-for-byte in effect. No feature change.
- **`npm run build` after every extraction step** — Pest won't catch a Vue/TS compile error; the build + Playwright are the only signals.
- **Surface the extraction boundaries before going deep** (open gate): which popovers become sub-components vs which stay inline, and whether the position/flip logic is one composable or split. Architecture choice → decide first.
- **Don't relitigate** the other Vue outliers deemed principled domain complexity in the audit (PreviewCanvas/ColorPicker/TypographyPanel/etc.) — out of scope.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation this is a behaviour-preserving, admin-only legibility split (no public-render impact, no schema); (2) the **extraction boundaries** — `useInlineToolbarPosition` + `useInlineLinkPopover` as composables, plus which of the color/highlight/image/heroicon popovers become sub-components; (3) the verification plan — run the existing inline-editing Playwright suite + the `design`-group parity guard, and whether to add a standing spec. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/347. Code Review & Cleanup (Carve-out — InlineFormatToolbar Extraction) — Log.md` — before/after LOC of the component + the extracted composables/sub-components + the Playwright/parity verification.
- **Bump `VERSION`** to `0.347.<iteration>`.
- **Update** the track doc + outlines + release-plan Cycle-3 status (carve-outs done; squash next).
- **Draft the 348 squash prompt + base prompt now** — this is the last session before the squash; its inputs (accumulated migrations since the 274 squash baseline + any 345-347 leftovers) now exist. Use the squash recipe lineage (208 → 274 → 348) as the format; keep a Phase-5 buffer slot for post-squash fallout.
- **Archive** `sessions/346. *.md` → `sessions/archived/`.
- **Commit on `session-347/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: follow existing Vue/composable conventions exactly (`use*` naming, the `composables/` vs `utils/` home the 345 cleanup settled); the win is the toolbar dropping well under 1k with clean composable seams and a still-green inline-editing Playwright suite + parity guard — no behaviour change.
