# Session A007 base prompt

> Cloud-agent session. Out-of-sequence A-track work, parallel to the
> numbered session cadence. Branch is `session-A007/N`. The numbered-
> session conventions for branch / commit / push / merge all apply
> (see `CLAUDE.md` Git Workflow).

---

**Start now.** Execute the reading list below, open the session prompt at `sessions/A007. Playwright e2e Re-Stabilization (Round 2).md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **A007. Playwright e2e Re-Stabilization (Round 2)**.

This session is **out-of-sequence A-track** work, not a position in `sessions/release-plan.md`. The numbered sequence continues independently; the A-series (A001 Content Import/Export, A002 Public-Bundle Consolidation, A003 Playwright e2e Re-Stabilization, A004 Code Shape & Fit, A005 Test Audit — Cycle 1) are cloud-agent forcing-function sessions running in parallel. A007's forcing function is a recurrence of the same regression A003 closed: the `Playwright e2e (isolated stack)` job has gone red again on `main` since A005 closed. The aggregate `Tests` status check (Pest fast + slow) is green by design — e2e is non-gating — so merges have been landing with a red e2e job. This session re-stabilizes it. The methodology of A003 and the prior `sessions/archived/playwright-test-fix.md` stabilization is the precedent for *how* (evidence-first; results-readable-via-committed-digest; restore-on-handback), **not** for *what* — do not carry forward either round's specific diagnosis; gather this round's evidence fresh.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0. The Fleet Manager `/api/health` route, its auth, its response schema, and `app/Http/Controllers/Api/Fleet/*` are out of scope.

**Session shape:** stabilization (test-infra / e2e). Gather evidence from CI, classify each residual failure (environment vs spec-drift-against-the-shipped-product vs genuine product regression vs ambiguous), apply the lightest fix per the brief's decision tree, and hand back with the `Playwright e2e (isolated stack)` job reliably green on the isolated stack. No widening of scope into product redesign or new infra absent evidence demanding it.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) only if the format has changed since your last session or you're uncertain about a structural detail. They remain the **canonical format reference** — do not infer format from previous session logs.
2. Read `sessions/session-outlines.md` (the roadmap) for the active-tracks block. There is no Beta 1 stub for this session — it is emergent. Note the **Test Audit** row (lifted at A003 close, Cycle 1 closed at A005) — A007 is a stabilization, *not* a Test Audit cycle, but the discipline doc that track shipped governs how any new/rewritten spec is authored.
3. Read `sessions/A007. Playwright e2e Re-Stabilization (Round 2).md` (the session prompt) carefully.
4. **Read the A003 log** (`sessions/archived/A003. Playwright e2e Re-Stabilization — Log.md`) and the **A005 log** (`sessions/archived/A005. Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain) — Log.md`). A003 is the method precedent (committed-digest read channel, decision tree, restore-on-handback) and documents the page-builder cold-load cluster it deferred. A005 deleted the redundant importer specs and the two `describe.skip`'d specs (`inline-formatting-toolbar`, `ticket-tier-picker`) and shipped the local-first authoring doc; its D1 deletion record is the current spec inventory's provenance.
5. **Read `docs/testing/playwright-discipline.md`** — the local-first authoring workflow A005 shipped. The smoking gun across both prior rounds is specs committed without ever running. Any spec A007 rewrites or adds is authored to this discipline.
6. Read `sessions/housekeeping-inbox.md` — the `[test-integrity]` entries are the current e2e disposition ledger. Note in particular the page-builder cold-load deferral, the ordered/unordered-list rendering guard request, and the widget "knockout" controllability guard request — adjacent candidates that may resolve here only if evidence puts them in scope.
7. Read `docs/app-reference.md` for environment names, container names, and the public-render / build-pipeline surface. The relevant subsystem files for this session — `playwright.config.ts`, `.github/workflows/tests.yml`, `docker-compose.e2e.yml`, `tests/e2e/global-setup.ts`, `tests/e2e/README.md`, and the spec files the digest names (likely first-time-in-CI candidates `tests/e2e/text-widget-border.spec.ts` and `tests/e2e/accessibility.spec.ts`) — are read on demand once the diagnostic evidence identifies which are in scope.
8. Skim `sessions/archived/playwright-test-fix.md` (the original stabilization) for the CI-results-to-readable-channel mechanism — **as a precedent for method, not as a diagnosis to inherit.**
9. Note any drift between the session prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

---

## Starting state inherited from sessions 325 / A005

- **CRM contract at v2.3.0** — unchanged. No A-series or numbered session since has touched the Fleet Manager boundary.
- **Schema baseline:** no schema changes relevant to this session. A007 is not expected to write migrations.
- **Fast + slow Pest green** at the 325 close baseline; the `Tests` aggregate has stayed green on every merge (Pest-only by design). No new fast Pest tests expected this session unless a fix lands product code that warrants one.
- **Playwright suite — RED on `main` again.** A005 closed with the suite at 16 specs, green-with-retry. Since then two e2e specs landed and were merged **without ever running in CI** — the same authoring gap A003/A005 surfaced:
  - `tests/e2e/text-widget-border.spec.ts` (session 323, the E17 Border tool)
  - `tests/e2e/accessibility.spec.ts` (the `@axe-core/playwright` Aria-sweep guard, against `/` + `/news`)
  Product changes that may also have drifted existing specs against the shipped contract: session 322 (E16 header/footer defaults — centered header chrome, footer stacking-nav), session 323 (E17 universal widget borders), session 325 (`EventCalendar` retirement → server-rendered `event_mini_calendar` widget). The previous stabilizations' permanent fixes (Composer dev-deps on the e2e image; nginx mounting the Vite build volume; runtime-SCSS warm-up step in `.github/workflows/tests.yml`) remain in place — verify they are still there before assuming a regression elsewhere.
- **Planning state:** no `sessions/release-plan.md` entry exists for this session. The Test Audit track is *between cycles* (Cycle 1 closed at A005; next trigger ≈ session 365). A007 is a stabilization, not a cycle; it does not consume a Test Audit cycle slot.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — stabilization-shape session; per Rule 2, this shape does not pull in unrelated housekeeping items. Items adjacent to the e2e surface (the page-builder cold-load deferral, the list-rendering guard, the knockout-controllability guard) may be touched if and only if the evidence puts them in scope.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. *See the drift and decision-threshold rules below for the calibration of what counts as a decision point worth surfacing.*
- **Surface architectural choices before going deep.** "Should I use X or Y here" is a question for the user, not something to commit to and unwind later. This includes data model shape, new abstractions, framework patterns that will propagate, and any decision that's expensive to reverse. Cheap and local — just decide.
- **Adapt to drift; don't ask about it.** A session prompt is a snapshot of the code at time of writing. Expect small inaccuracies in field names, payload shapes, method signatures, and similar surface details vs the actual code. Default to adapting silently to match what's there and note the adaptation in one line in your work log. Only flag drift when it requires a decision: two reasonable interpretations would produce meaningfully different behavior, the drift reveals a likely bug in spec or code, resolving it would expand scope beyond the session, or the spec describes behavior the system doesn't have and you can't tell whether to build it or treat it as a misread. Heuristic: *fix vs. decide — if you can resolve it by editing your own output without changing the system's intent, just fix it.*
- **Decision threshold scales with project maturity.** This project is well past its loose phase — conventions are established, the app's shape is settled, phases are well-defined. Reserve "pause and ask" for decisions that are genuinely expensive to reverse: data model shape, new abstractions, framework patterns that will propagate, anything cross-cutting. For local decisions with an obvious answer given existing code (naming, file placement, which existing helper to use, structural mirror of nearby code), just decide and note it. Heuristic: *if a reasonable reading of the surrounding code would land on the same answer, you don't need to ask.*
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). *(This session is not expected to write migrations; included for canon. Note the cloud-session constraint below — no Docker daemon is available in the agent's environment, so the canonical local run does not apply this session.)*
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow` (or `./dev test`). Fix any failures before proceeding. This session's primary signal is the Playwright e2e suite running against the isolated stack via the CI mechanism — but any Pest changes touched by an in-scope fix must still go through the fast suite.
- **CI is the authoritative full-suite signal (additive, staged — does not replace the line above).** Every push runs the full suite (fast → slow) asynchronously via `.github/workflows/tests.yml`; the aggregate **`Tests`** status check is green only when both the fast and slow suites pass. The `Playwright e2e (isolated stack)` job runs alongside and is **non-gating by design** (not in `tests.needs`); this session's deliverable is that job reliably green on the pushed branch.
- **Cloud-session constraints.** No Docker daemon and no local Playwright install in the agent's environment; the canonical local Pest run (`./dev test`) and a local `npm run test:e2e` are both unavailable. CI is the authoritative verification signal, and the GitHub MCP tools do not expose Actions logs or artifacts — so re-establish the committed-digest read channel (below) to see what's failing. Push the branch; read the digest via `mcp__github__get_file_contents`.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* *(A stabilization session is not expected to touch front-end source; if a product fix does, this rule applies — and the cloud environment may require the build to be verified via CI rather than locally.)*
- **Tests for new behaviour:** every session that changes application behaviour should include tests unless the session prompt explicitly says otherwise. For this stabilization session, test changes (additions, deletions, hardening) are expected on the Playwright side; Pest additions only if a fix lands product code that warrants one.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test may stay.
- **Cross-repo coordination — Fleet Manager agent contract.** Dormant for this session — out of scope.
- **Verify objective outcomes yourself; pull the user in for judgment.** When you can observe the result directly, do — don't ask the user to run it and report back. Close the loop yourself for CLI output, file contents, exit codes, logs, and anything CI/Playwright can verify. Pull the user in for judgement calls only.
- **Pause for manual testing only when human judgment is required.** This session's success criterion is objective and self-verifiable via CI; run the verification yourself and report results.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation is complete, if the user requests a follow-up A-track iteration (A007/2 etc.) or schedules a numbered successor, draft the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to the next iteration's path, updating the session number/title and any session-specific read-list items.
- **Next session prompt**: draft using `sessions/template-session-prompt.md` as the format reference.

Then stop. The user reviews the work and the next session's drafts, and initiates close when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed, if any):

- **Session log**: write a log file at `sessions/A007. Playwright e2e Re-Stabilization (Round 2) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append this session's row to the index table, per the existing `A00N` row pattern; `[A-track]` label is fine.
- **Update `sessions/session-outlines.md`** (the roadmap) only if A007 resolved a forward stub or changed an active-tracks line.
- **Update `sessions/housekeeping-inbox.md`**: write a `[test-integrity]` line per deleted assertion (decision-tree (b) cases), capturing what was removed and why with the coverage-tradeoff statement; touch the page-builder cold-load / list-rendering / knockout entries only if A007's work changed their disposition.
- **Archive the previous loose session**: A-track sessions follow the same archival rule — move the previous session's loose `sessions/A006. *.md` files into `sessions/archived/` if any exist and have not already been archived. Skip silently if those files don't exist (A006's cloud framing was retired at 323 close).
- **Bump the `VERSION` file** at the project root per the close-gate rule (`0.<session>.<iteration>`; A-track uses the A-prefixed marker convention settled by prior A-sessions — check `sessions/template-base-prompt.md` for the canonical form at session time). VERSION marks the close, not the surface touched.
- **Commit**: stage all changed files, commit on the current `session-A007/N` branch, and notify the user. Do not push unless the user asks — the user pushes (or doesn't) on their own cadence. Never push to `main`, never force-push, never merge to `main` yourself (see `CLAUDE.md` Git Workflow for the full forbidden list).
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
