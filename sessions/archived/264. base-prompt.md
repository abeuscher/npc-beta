# Base Prompt — Session 264

We are about to begin a new session: **264. Affiliations Junction — Schema, Migration, Admin UI**.

This session lifts the structural half of working-set entry **B1b** (Affiliations Junction & Soft-Credit Layer) per the post-263 execution order. B1b was flagged in `release-plan.md` as "1–2 sessions, may split per Rule 11." Splitting against that allowance: **264 lands affiliations + Org-side gaps + admin UI**, **265 lands `donation_credits` + soft-credit display**. Splitting between sessions deploys cleanly between two distinct structural changes — the affiliations data migration is irreversible-shaped and benefits from landing alone before soft-credit complexity layers in.

This session is **NOT boundary-touching** — no Fleet Manager contract change. CRM contract stays at v2.2.0 (per session 263).

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference; do not infer format from previous logs.
2. Read `sessions/release-plan.md`:
   - All 11 discipline rules.
   - Track B § B1b — confirm the entry shape; this session covers the affiliations half of the success criterion.
   - Execution-order list — confirm position 19 (B1b) is now where 264 sits, with 265 splitting the entry per Rule 11.
3. Read `sessions/session-outlines.md` § "Organizations Model Overhaul" — the B1b stub there.
4. Read `docs/schema/README.md` for the table index. Then read the individual schema files for `contacts`, `organizations`, plus any other tables this session touches.
5. Read `app/Models/Contact.php` and `app/Models/Organization.php` end-to-end. Pay attention to the existing `organization()` belongsTo relation on Contact (which this session removes) and the `contacts()` hasMany on Organization (which this session refactors through `affiliations`).
6. Read `app/Filament/Resources/ContactResource.php` plus its `Pages/` subdir end-to-end. The Contact admin form is where the affiliations editor lands. Pay attention to: existing form layout, custom-field schema integration, the polymorphic Notes timeline pattern (added at 255), how the Org belongsTo currently surfaces in the form.
7. Read `app/Filament/Resources/OrganizationResource.php` plus its `Pages/` subdir end-to-end. Pay attention to: the deletion-guard "block-with-counts" logic from session 255, the existing Events Sponsored panel (the canonical reference for the new Affiliated Contacts panel), the ellipsis-menu "View affiliated contacts →" link this session removes when promoting to a panel.
8. Read `sessions/255. Organizations Model Overhaul (Min) — Log.md` end-to-end — context for the deletion-guard pattern, the four transactional FKs this session sits alongside, and the strategy decisions that frame B1b.
9. Read `sessions/256. Organizations Importer — Log.md` — context for the existing Org-as-source sentinels (`__org_donor__`, `__org_member__`, `__org_sponsor__`, `__org_invoice_party__`); this session does NOT change those, but the read-through confirms they stay routed to transactional FKs (not to affiliations).
10. Read the most-recent five migrations in `database/migrations/` — confirms the migration-shape conventions this session inherits.
11. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
    - **`Contact.organization_id` reader audit.** Grep the codebase for every read path consuming `Contact.organization_id` (or the `organization()` relation). Catalogue. The data migration drops the column; every reader needs to migrate to walking through `affiliations`. Surface the catalogue before touching code.
    - **Existing morph-target convention.** Find at least one existing polymorphic relation in the codebase (the Notes polymorphic Timeline pattern at 255 is one; Spatie Media Library's `mediable` is another; audit events if present). Confirm the `morphTo` naming convention so 265's `donation_credits.attributable_type` declaration matches in advance.
    - **Importer touch surface.** The Contacts importer's `organization` column-mapping currently lands a value in `Contact.organization_id`. After this session, that path needs to create an `affiliations` row with `is_primary = true`. Locate the exact site (`ImportProgressPage::processOneRow` or similar) before authoring.
    - **`is_primary` uniqueness enforcement.** A contact should have at most one `is_primary = true` affiliation. Decision: enforce via a partial unique index (`UNIQUE(contact_id) WHERE is_primary = true`) at the DB layer paired with a model boot-hook clearing prior primaries on save. Confirm the partial-index syntax against the existing PostgreSQL conventions before authoring the migration.
12. Open `sessions/264. Affiliations Junction — Schema, Migration, Admin UI.md` and read the session prompt carefully.
13. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.2.0** (per session 263) — unchanged this session.
- **Fast test suite green** at **2092 passed** (263 close baseline — +14 from backup-trigger endpoint). This session's structural changes are likely to surface test-update needs across Contact/Organization-related fixtures; estimate **~+15 to ~+25 net new tests** plus updates to existing tests that asserted against `Contact.organization_id`.
- **Slow test suite green** at **15 passed**.
- **B1a ✅ closed at session 255** — five transactional FKs, four importer Org-as-source sentinels, Org admin form rebuild. This session inherits that surface; deletion-guard counts from B1a get extended with affiliations.
- **B1c ✅ closed at session 256** — Organizations importer + the importer-side scrub-source plumbing. This session does not touch the Org importer.
- **B2 ✅ closed at session 258** — onboarding rehearsal cluster surfaced the affiliation requirement. The findings register from 258 frames B1b's shape.
- **A1d ✅ closed at session 263** — backup-trigger endpoint. Unrelated to this session.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **CRM-side schema changes — yes.** This session is the structural session of the B1b cluster: new `affiliations` table, dropped `contacts.organization_id` column, gap-fill columns on `organizations` (industry / EIN). Update `docs/schema/contacts.md`, `docs/schema/organizations.md`, and add `docs/schema/affiliations.md` after writing migrations. Register the new table in `docs/schema/README.md`.
- **Data migration is destructive-shaped.** The migration drops `contacts.organization_id` after copying its rows into `affiliations`. Per user direction at 264 prompt-time ("data integrity not important until ship"), no rollback shim — the column is dropped in the same migration. If the migration fails partway, the recovery is `migrate:fresh --seed` against the dev environment. The `down()` method drops what `up()` created; it does not attempt to restore `contacts.organization_id`.
- **No Composer changes.** All new code uses existing dependencies.
- **No `npm run build` or `build:public`.** This session is models + migrations + admin forms; no frontend touches.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. Particularly: the four verify-at-start questions in step 11 are decision points — surface answers before going deep.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **Tests required for new behaviour.** The data-migration step gets test coverage. Eloquent relations get coverage. Admin form repeater gets coverage via Livewire test. Importer touch site gets coverage.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline is 2092 passed (post-263). **Target after this session: ~2107 to ~2117 passed fast** (+15 to +25, accounting for both new tests and the likely update-cycle on existing tests that asserted against `Contact.organization_id`).
- **Verify objective outcomes yourself; pull the user in for judgment.**
  - **Objective:** schema migration runs cleanly; data migration produces the right `affiliations` rows; `migrate:status` clean; admin form repeater renders + saves; tests green.
  - **Judgment-required:** how the Contact admin form's affiliations repeater positions visually within the existing form layout; the operator-readable phrasing of the deletion-guard's new affiliated-contacts line; the row-preview format on the collapsed repeater.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and verification are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The natural next session after 264 is **265. Donation Credits — Soft-Credit Layer** per the post-263 execution-order revision. 265 is canonically prepared (its base-prompt + session prompt land alongside this session's pair, drafted at 264 prompt-time). Confirm at 264 close — if any in-session findings reshape 265's scope (e.g., the morph-target convention discovery surfaces a different shape than 265 assumes), lift them at close-time before 265 starts.

### Phase 2 — Close

When the user says to close:

- **Session log**: write `sessions/264. Affiliations Junction — Schema, Migration, Admin UI — Log.md` against `template-session-log.md`. Document the migration shape, the `Contact.organization_id` reader-migration catalogue (with line counts), the deletion-guard count update, the Contact admin form repeater shape, the importer touch site, the Org-side panel decision, and any in-session absorptions.
- **Update `sessions/completed-sessions.md`**: append session 264's row.
- **Update `sessions/release-plan.md`**:
  - B1b stays open — its checkmark lands at 265 close. Annotate B1b's success criterion with "structural half (affiliations + Org gaps + admin UI) shipped at 264; soft-credit half (donation_credits) ships at 265."
  - Confirm the execution-order list shows 264 + 265 as the B1b split; 265 inherits position 20 (E10 reflows to position 21).
- **Update `docs/schema/`**: ensure `contacts.md` (column drop), `organizations.md` (industry / EIN add), and the new `affiliations.md` are accurate. Update `docs/schema/README.md` to register `affiliations.md`.
- **Findings register** — flag any surprises (data-migration edge cases, repeater UX gotchas, Eloquent through-relation pitfalls, importer touch-site complications, morph-naming-convention discoveries that affect 265's plan).
- **Help-doc update** — none expected; the affiliations editor surfaces inline on the Contact admin form. If the operator-facing UX warrants a doc note, lift to a follow-on session rather than absorbing.
- **Archive the previous session**: move all files matching `sessions/263. *.md` into `sessions/archived/`. Also clean up `sessions/draft-fleet-manager-contract-v2.2.0-backup-trigger.md` (stale FM-side prep file, superseded by the canonical 263 session prompt) — `git rm` it.
- **Commit**: stage all changed files and commit on `session-264/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly. The B1a transactional-FK pattern is the canonical reference for the affiliations table's index + FK shape; the Notes polymorphic Timeline pattern (255) is a useful reference shape but NOT structural for this session (affiliations is not polymorphic — that's 265).
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** add Org-Org relationships (parent / subsidiary / fiscal-sponsor); do **not** lift soft-credit shape into this session (that's 265); do **not** add affiliation-shape importer logic beyond the Contacts importer's `organization` column rerouting; do **not** add any UI affordance for affiliation-history reporting (out of scope).
- Prefer editing existing files to creating new ones — except for the new `Affiliation` model + migration + (likely) a Filament repeater partial if one is needed.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
