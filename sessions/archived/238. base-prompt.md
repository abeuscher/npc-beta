# Base Prompt — Session 238

We are about to begin a new session: **238. Fleet Manager Agent — Phase 1 (CRM-Side MVP + v1.0.0 Contract)**.

This is the first session in the Fleet Manager Agent track. It lands the CRM-side surface that the Fleet Manager workstream consumes — `/api/health` route, bearer-token auth, six subcheck implementations, version reporting via Docker build-arg, and v1.0.0 of the canonical agent contract spec doc. The session unblocks the parallel Fleet Manager repo workstream, which has been waiting on a real contract surface.

This session **trips the cross-repo coordination flag** — by design. It modifies `/api/health`, the auth middleware, the response schema, the version-reporting path, and the spec doc itself. Every artifact the flag is meant to govern lands here.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block, the Cross-Repo: Fleet Manager / CRM block at the top, and the Fleet Manager Agent — Phase 1 stub.
3. Read `sessions/fleet-manager-planning-spec.md` end to end. **This is the canonical product spec for both repos.** The "Architecture Overview," "CRM-side starting state," "Communication model," "Polling & Health Checks," "Two-Repo Coordination Protocol," "Local Development Scheme," and "Resolved leans" sections are all directly load-bearing for this session's design decisions.
4. Read `docs/fleet-manager-agent-contract.md` — currently a v0.0.0 stub. **This session bumps it to v1.0.0** with the actual HTTP contract.
5. Read `docs/app-reference.md` for environment names, container names, and the deploy-server shape.
6. Read the most recent CRM session log establishing the immediate context: `sessions/237. Widget Primitive — Phase 6 (Page-as-View Adapter, Placeholder Sweep, Track Closure) — Log.md` (or `sessions/archived/237. *.md` if archived). 237 created the v0.0.0 stub spec doc, the Cross-Repo block in `session-outlines.md`, and the cross-repo coordination flag in the prompt template — all artifacts this session builds on.
7. Read the full state of every file this session edits or creates:
   - **Routes & middleware:** `routes/api.php`, the existing `app/Http/Middleware/` directory (to follow naming and shape conventions), `bootstrap/app.php` or `app/Http/Kernel.php` (wherever middleware aliases live in Laravel 11).
   - **Controller location:** `app/Http/Controllers/Api/` (likely empty or sparse — verify); the new controller lands at `app/Http/Controllers/Api/Fleet/HealthController.php`.
   - **Config:** `config/` directory state — the new `config/fleet.php` config file lands here.
   - **Docker / deploy:** `Dockerfile` (the `app` target specifically), `docker-compose.yml` (local), `docker-compose.prod.yml` (read-only context — this file does NOT change), `.github/workflows/deploy.yml`.
   - **`.env.example`:** the new `FLEET_MANAGER_AGENT_KEY=` entry lands here.
8. Open `sessions/238. Fleet Manager Agent — Phase 1 (CRM-Side MVP + v1.0.0 Contract).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migration expected this session.** The agent API key lives in `.env`, not the database. No `docs/schema/` updates.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds:** none expected. This session is pure-PHP + YAML + Dockerfile. **No `npm run build` or `build:public` required.**
- **Tests for new behaviour:** every session that changes application behaviour should include tests. Phase 1 lands ~10–15 new cases covering: auth (valid/invalid/missing token, timing-safe comparison), response shape (top-level fields, contract_version field, subcheck shapes), subcheck happy paths (each of the six), subcheck failure paths where simulable (DB facade swap, Redis facade swap), and version-reading helper (with and without VERSION file present).
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Cross-repo coordination — Fleet Manager agent contract.** **This session is the inaugural fire of the cross-repo flag.** Every change here lands in `docs/fleet-manager-agent-contract.md` (v0.0.0 → v1.0.0, body fully filled, CHANGELOG entry) and the Cross-Repo block in `sessions/session-outlines.md` (last boundary-touching session: 238). The discipline takes effect from this session forward.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents. **239 candidate slots** (locked in at session-238 prompt-drafting time):

- **Importer→widget status mismatch fix.** Donations importer's `mapDonationStatus()` writes `'completed'` for `active`/`paid`/`succeeded`/blank input, falling outside the canonical four-value enum and outside the Recent Donations widget filter. Small, focused — one-line fix to emit `'active'` plus a one-time backfill. Surfaced during 234 manual testing; carried forward through 235/236/237/238.
- **Fleet Manager Agent — Phase 2 (Backup Pipeline).** Greenfield CRM-side backup (pg_dump + media → DigitalOcean Spaces, scheduler cadence, success-record location populated for `last_backup_at`). Lands when needed; not strictly next.
- **Phase B — Admin gating generalization** from the Financial Data Origin track. Lifting the `! $record->stripe_id` action-visibility pattern from `TransactionResource` into a shared trait keyed off `source`. Lands when a Refund/Cancel/Sync action surface is imminent.
- **Code Review & Cleanup audit-then-apply pair** (the 205/206 pattern). Periodic codebase sweep.
- **Notes Permissions & Permissions Audit** stub — deferred since session 198.
- **Other Beta-1 stubs** from the roadmap.

The user picks among these at 238 close. The Fleet Manager **repo** workstream runs in parallel and does not need to be 239 — that's a separate workstream consuming the contract this session ships.

- **Next session base prompt** (whatever 239 ends up being): copy `sessions/template-base-prompt.md` to `sessions/239. base-prompt.md`.
- **Next session prompt**: draft `sessions/239. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/238. Fleet Manager Agent — Phase 1 (CRM-Side MVP + v1.0.0 Contract) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 238's row.
- **Update `sessions/session-outlines.md`** (the roadmap): the Fleet Manager Agent — Phase 1 stub becomes a "complete" pointer; the Cross-Repo block reflects v1.0.0 and last-boundary-touching session 238. The Phase 2 (Backup Pipeline) stub stays.
- **Create the track doc** at `sessions/tracks/fleet-manager-agent.md`. Initial state: Status snapshot ("Phase 1 complete (session 238); Phase 2 — Backup Pipeline — not yet scheduled"), Phase Retrospectives section with the Phase 1 entry, Forward plan referencing Phase 2 + the planning spec, Carry-forwards. Use the widget-primitive track doc as a structural reference; keep it lean — the planning spec is the canonical product reference.
- **Update `docs/fleet-manager-agent-contract.md` final state for the session** — confirm v1.0.0 is fully fleshed out with the actual surface, CHANGELOG entry is present.
- **Production deployment notes in the log**: explicit one-liner — *"After merging this branch, set `FLEET_MANAGER_AGENT_KEY` in the droplet's `.env` to a fresh `Str::random(64)` value before the next deploy completes. Without it, the auth middleware will fail-closed on every poll."*
- **Archive the previous session**: move all files matching `sessions/237. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (the new controller + middleware + config + Dockerfile/compose/workflow edits + the v1.0.0 contract doc + the Cross-Repo block update + the new track doc + the log + the updated outlines + the completed-sessions index + the 239 base prompt + session prompt + any files moved into `sessions/archived/`), commit on the current `session-238/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **Bearer-token comparison must be timing-safe** — `hash_equals()`, never `===` or `==`.
