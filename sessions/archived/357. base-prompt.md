# Base Prompt — Session 357 (Housekeeping — Image Uploader & Widget Thumbnails + Demo-Node Polish)

**Start now.** Execute the reading list below, open the session prompt at `sessions/357. Housekeeping — Image Uploader & Widget Thumbnails + Demo-Node Polish.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **357. Housekeeping — Image Uploader & Widget Thumbnails + Demo-Node Polish.**

This is **not a `release-plan.md` entry** — it is an **emergent, owner-driven housekeeping batch** drawn from `sessions/housekeeping-inbox.md`. The roadmapped tracks are largely complete, so picks are non-roadmap-pressure-driven; the owner asked for a housekeeping pass anchored on the image-upload work surfaced during the 356 media-picker session. **Four items** are packaged here (one image-uploader refactor + three smaller polish items). It is **not** part of any active track.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract **stays at v2.4.0** (inherited; 356 was non-boundary). No `docs/fleet-manager-agent-contract.md` or Cross-Repo-block changes. **Note:** two of the four items are **demo-mode** changes (demo re-enter affordance, demo dashboard seed) — these touch the demo baseline (`demo:restore` / `demo:reset`), which is a CRM-side local concern, **not** the FM HTTP contract; no version bump from that.

**Session shape:** **housekeeping.** A batch of four small, design-settled inbox items: (1) **homogenize the page-builder image uploader** — fold the widget config image field and the appearance background-image panel onto one shared upload component (the richer background model as the base); (2) **widget thumbnail audit** — give widgets that render blank picker thumbnails representative demo data; (3) a **"re-enter demo" affordance** on the login page (demo-mode only); (4) the **demo dashboard empty-state** — seed a dashboard arrangement for the `demo` role so it stops showing the generic "ask an admin" message. Item 1 is the centerpiece (a behaviour-preserving Vue refactor); 2–4 are smaller.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is an **emergent/housekeeping** session, so use the fuller session-prompt shape (explicit phases, open questions, out-of-scope, drift signals).
2. Read `sessions/session-outlines.md` — the active-tracks block. This session is **not** a stub there (housekeeping pick); its source is the inbox, not the roadmap. Confirm the editor↔public parity stub shows **Track 1 resolved (355)** and Track 2 still open/gated, and note 356 (media picker) as the most recent close.
3. **Read `sessions/housekeeping-inbox.md` and apply the verify-at-walk staleness guard.** This **is** a housekeeping session, so it **drains** the four target items (Rule 2). Each is a snapshot at capture time — **re-verify the premise against current code before committing it to an iteration.** The four (by inbox stamp):
   - **Image uploader homogenization** — `[0.355.01 · 2026-06-11]` (captured mid-356; folds into the inbox at the 356 close).
   - **Widget thumbnail audit** (blank thumbnails) — `[0.329.02 · 2026-05-31]`.
   - **Demo re-enter button on login** — `[0.351.01 · 2026-06-10]`.
   - **Demo dashboard empty-state** — `[0.346.01 · 2026-06-07]` + the related `[0.323.01 · 2026-05-29]` (same root: the `demo` role has no seeded dashboard).
   The 356 close already **dropped** the `[0.351.01]` "browse the media library for images" item (resolved by the 356 media picker) — confirm it is gone, do not re-pick it.
4. **Read the session prompt carefully** (`sessions/357. Housekeeping — Image Uploader & Widget Thumbnails + Demo-Node Polish.md`) — it carries the **verified per-item map** (the seams for each of the four), the open questions, and out-of-scope. **Build from that map; verify each cited path against current code as you go.**
5. **Read the image-uploader seam (item 1 — the anchor):**
   - `resources/js/page-builder-vue/components/fields/ImageUploadField.vue` — the widget config image field (bare file input + preview + remove + the 356 "Browse library" entry).
   - `resources/js/page-builder-vue/components/appearance/BackgroundPanel.vue` — the appearance background-image sub-panel (the **richer** model: fit, alignment, "use current page's header", upload-block/preview, + the 356 "Browse library" entry). This is the base to converge on.
   - `resources/js/page-builder-vue/components/InspectorPanel.vue` — wires the appearance-image events (`upload-image` / `remove-image` / `browse-library`) and `LayoutInspectorPanel.vue` reuses `BackgroundPanel` with `show-image=false`.
   - `resources/js/page-builder-vue/composables/useUploadActions.ts` — `uploadImage` / `removeImage` / `uploadAppearanceImage` / `removeAppearanceImage` / `useExistingMedia` (the shared backend actions both fields already call).
   - `resources/js/page-builder-vue/components/MediaBrowserModal.vue` — the 356 shared browser both fields open; already homogenized, the model for "one shared surface."
6. **Read the widget-thumbnail seam (item 2):**
   - `scripts/generate-thumbnails.js` — the thumbnail generator (renders each widget and screenshots `static.png`).
   - `app/Http/Controllers/WidgetThumbnailController.php` and `app/Models/WidgetType.php` — the render surface + how the thumbnail is stored/served.
   - A widget Definition's `demoSeeder()` / `demoConfig()` / `demoAppearanceConfig()` hooks (e.g. `app/Widgets/Hero/HeroDefinition.php`, `app/Widgets/TextBlock/TextBlockDefinition.php`) and the existing `DemoSeeder.php` examples (`BoardMembers`, `LogoGarden`, `Carousel`, `BarChart`, `DonationForm`) — the pattern for giving a widget representative thumbnail data. Most widgets have **no** DemoSeeder; the audit identifies which render blank and need one (or a `demoConfig`).
7. **Read the demo seams (items 3 + 4):**
   - `routes/web.php` (the `demo/enter` route, `demo.enter`), `app/Http/Controllers/DemoLoginController.php` (auto-login, `abort_unless(isDemoMode(), 404)`), `app/helpers.php` (`isDemoMode()`), and `app/Providers/Filament/AdminPanelProvider.php` (`->login()` + the `PanelsRenderHook` registrations — the re-enter affordance attaches to a login/auth render hook, demo-mode-gated).
   - `resources/views/filament/widgets/dashboard-slot-grid.blade.php` (the "No dashboard arrangement for your role…" empty-state message) and `database/seeders/DashboardViewSeeder.php` (seeds `super_admin` only today). The demo dashboard arrangement must reach the demo node via the **demo baseline** (`demo:restore` / `demo:reset`), not just the dev seed.
8. Read `docs/app-reference.md` for the **front-end build command** (WSL node PATH per the build-command note), the **isolated Playwright e2e stack** (`./dev e2e:up` / `e2e:down`, the image-baked `public/build` named volume a host build must be `docker cp`'d into), and the **canonical e2e runner** caveat (run via `npm run test:e2e`; a bare `playwright test` with only `APP_URL` sends `migrate:fresh --seed` to your **dev** DB).

---

## Starting state inherited from session 356

- **CRM contract at v2.4.0** — unchanged (356 was non-boundary; the media picker added a read-only list endpoint + a Vue modal, no Fleet surface). **This session is non-boundary; the contract is unchanged.**
- **Schema baseline:** no migrations; `database/migrations/` empty (post-348-squash). **None expected** — all four items are Vue / seeder / render-hook / view changes. The demo dashboard seed is data, not schema.
- **Fast Pest green at 2937 / 0** (356 close). Expected delta: **new Pest** for the demo-dashboard seed (the `demo` role now resolves a dashboard arrangement) and possibly the demo re-enter route/affordance; the image-uploader refactor is Vue (Pest doesn't see it — guarded by Playwright); the thumbnail audit is data/asset (a count/coverage Pest is optional).
- **Playwright** — 356 added `media-picker.spec.ts` as a standing guard, joining `full-width-matrix.spec.ts` (333) + `chrome-parity.spec.ts` (354) as the page-builder net. **The image-uploader refactor is guarded by `media-picker.spec.ts` (browse + upload on a widget field) + `full-width-matrix.spec.ts` (the appearance background path)** — re-run them after the refactor; add a spec only if the refactor opens a gap they don't cover. Run Pest and Playwright **sequentially**, via `npm run test:e2e`.
- **Planning state:** 356 shipped the page-builder media picker (browse-or-upload modal + a `GET api/page-builder/media` list endpoint), **resolved + dropped** the `[0.351.01]` "browse the media library" inbox item, and **folded in** the `[0.355.01]` image-uploader-homogenization note (now a target of this session). No release-plan change. This batch has no standing stub — it is an emergent housekeeping pick.
- **Housekeeping inbox** (`sessions/housekeeping-inbox.md`) — **this IS a housekeeping session;** it **drains the four target items** above (Rule 2) and digests the capture buffer (`housekeeping-incoming.md`) at close per the standard gate. The remaining inbox items (feature-shaped / design-gated / `[post-milestone]`-tagged) stay parked.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Verify each inbox item's premise at the walk before implementing.** The four are design-settled, but re-confirm each against current code (already addressed? changed shape?) before committing it to an iteration — catch staleness at the walk, not mid-implementation.
- **Item 1 is a behaviour-preserving refactor — converge, don't redesign.** Fold both image fields onto one shared upload component with the background model as the base; the background-only controls (fit / alignment / use-page-header) stay background-only (props/slots), and the bare widget-config field gets the shared preview/remove/upload/browse without those extras. **Do not change** the backend upload/use-existing/remove actions or the `MediaBrowserModal`. If the refactor starts changing observable behaviour, stop and surface. Re-run `media-picker.spec.ts` + `full-width-matrix.spec.ts` as the net.
- **Demo-mode changes must reach the demo baseline, not just dev.** The re-enter affordance is demo-mode-gated (`isDemoMode()`); the demo dashboard arrangement must be seeded into the **demo baseline** the node restores (`demo:restore` / `demo:reset`), or it won't appear on the demo node. Confirm the seed path, not just the dev `migrate:fresh --seed`.
- **Thumbnail audit — give data, don't special-case the renderer.** Prefer adding a `demoSeeder()` / `demoConfig()` to widgets that render blank (the established pattern) over per-widget renderer hacks. `log()` which widgets were left blank-by-design (e.g. portal/auth widgets that have no meaningful standalone preview) rather than forcing content.
- **Build after front-end changes.** Any `resources/js/**` change → `npm run build`, then `docker cp public/build/. nonprofitcrm_e2e_app:/var/www/html/public/build/`. Regenerated widget thumbnails (`scripts/generate-thumbnails.js`) follow that script's own documented flow — check `docs/widget-development.md`.
- **Verify objectively; pull the owner in for the visual call.** Close the objective loop yourself (the unified field still uploads/removes/browses + persists; demo re-enter logs back in; the demo role resolves a dashboard; thumbnails render non-blank). Surface the look/feel of the unified image field and the regenerated thumbnails for the owner.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the **four-item scope** and that each item's premise still holds at the walk (flag any that turn out stale); (2) the **iteration shape** — recommend whether the four land together on one branch or split (Rule 11), e.g. the image-uploader refactor as its own iteration if it proves load-bearing, the three small items batched; (3) your **answer to the open questions** in the session prompt — for item 1, confirm the shared-component shape (background model as base, background-only controls kept conditional); for item 4, the demo dashboard decision (seed a real arrangement vs a demo-specific empty message — recommend a seeded arrangement) and which widgets it should contain. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/357. Housekeeping — Image Uploader & Widget Thumbnails + Demo-Node Polish — Log.md` — per item: what changed and where, the shared-component shape (item 1), which widgets got demo data + which were left blank-by-design (item 2), the re-enter affordance + its render hook (item 3), the demo dashboard seed + its baseline wiring (item 4), the Playwright re-run result, the build run, and Pest results.
- **Bump `VERSION`** to `0.357.<iteration>`.
- **Non-boundary:** no contract-doc or Cross-Repo-block change (CRM stays v2.4.0). Confirm `FmContractVersionParityTest` still green if any Fleet file is incidentally touched (it shouldn't be).
- **Fold the four drained items out of the inbox** (under "Recently dispositioned" with the date + where each went); digest the capture buffer (`housekeeping-incoming.md`) per the standard gate.
- **Archive** `sessions/356. *.md` → `sessions/archived/`.
- **Commit on `session-357/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: **this is a batch of small, settled items — resist scope creep.** Item 1 is a *convergence refactor*, not a redesign of the image-upload UX (no new controls, no metadata, no folders). Item 2 is *give widgets demo data*, not a thumbnail-pipeline rewrite. Items 3–4 are *small demo-mode affordances*. The adjacent feature-shaped items in the inbox (image-editing toolbar, querystring personalization, people-list widget, popout repeater editor, full-screen longform writing) are **explicitly out of scope** — they each need their own entry or a design pass. The `[post-milestone]`-tagged items stay parked per Rule 12.
