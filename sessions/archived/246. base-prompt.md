# Base Prompt — Session 246

We are about to begin a new session: **246. Housekeeping Batch 2 — UI Fixes Bundle**.

This session pulls forward three items from the *Housekeeping — Batch 2* stub in `session-outlines.md` while session A2 (Fleet Manager — Node Operations Parity) is on architectural hold in the FM repo. Per Rule 11 in `release-plan.md`, Batch 2 splits across multiple sessions; this is the small/fast UI subset. The complementary session (247) handles the `app:reset` artisan + orphan media cleanup; the build-trigger audit (Batch 2 item 2) defers to a later slot.

The three items in this session are independent — each can stand or fall on its own and they do not share infrastructure beyond living in the admin / page-builder / public-rendering surfaces.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/release-plan.md` in full — the 11 discipline rules at the top govern every session in the plan, and § E12 is the parent stub. Pay particular attention to Rules 5, 6, 11.
3. Read `sessions/session-outlines.md` for the active-tracks block, the Cross-Repo / Fleet Manager status, and the Housekeeping — Batch 2 stub which carries the original scope detail for all five items.
4. Read `docs/app-reference.md` for environment + container names + the dashboard view-to-file mappings + the build-server pipeline. Read `docs/schema/README.md` for the table index. No schema-doc reads are needed for this session — it does not touch persistence layers (UI/template/widget-config only).
5. **For the Blog Post Pager item** (Phase 1):
   - Read `app/Widgets/BlogPager/BlogPagerDefinition.php` and `template.blade.php`.
   - Read `app/WidgetPrimitive/Projectors/RecordContextProjector.php` (the projector that resolves record context tokens for record-detail-sidebar / record-aware widgets).
   - Read `app/WidgetPrimitive/RecordContextTokens.php` (the token registry).
   - Read `app/WidgetPrimitive/ContractResolver.php` (the resolver that walks `dataContract()` against the active SlotContext).
   - Read at least one passing record-aware widget for comparison — `RecentNotes` or `MembershipStatus` is a clean reference (both render in `record_detail_sidebar` and consume record-context tokens correctly).
6. **For the text widget vertical alignment item** (Phase 2):
   - Read `app/Widgets/TextBlock/TextBlockDefinition.php` and `template.blade.php`.
   - Read `app/Services/AppearanceStyleComposer.php` (decides whether vertical alignment lives on the widget or in the shared appearance layer).
   - Skim 2–3 other widget templates (Hero, ThreeBuckets, Image) to see how they handle vertical layout today.
7. **For the heroicon picker item** (Phase 3):
   - Read the existing Quill setup: `public/css/admin.css` Quill section, `resources/js/admin.js` for any Quill-extension registration patterns.
   - Confirm the project's icon library at session start (heroicons is the assumed set — `heroicon-o-*` strings appear throughout Filament resources). Verify by grepping for `heroicon` in widget definitions / Filament resources.
   - Look at how Quill custom formats are registered in Quill v2 (the version Filament's RichEditor uses).
8. Open `sessions/246. Housekeeping Batch 2 — UI Fixes Bundle.md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. (No migrations expected for this session — UI-only work.)
- **Update `docs/schema/`** after writing any migration. (Not expected for this session.)
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Fix any failures before proceeding. Baseline at 245 close: 1733.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures. This session likely touches:
  - `public/css/admin.css` (Quill heroicon styling) — hand-edited, no build required.
  - `resources/js/admin.js` (Quill extension) → `npm run build` if touched.
  - Public-side widget SCSS (TextBlock alignment, BlogPager fix) → `npm run build`.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. Blog Pager fix wants a regression test that walks the post-detail context resolution. Text-block vertical alignment wants a render test asserting the wrapper class. Heroicon picker is a Quill custom-format addition — visual verification is fine; no automated test needed unless a unit-testable parser surface emerges.
- **Cross-repo coordination — Fleet Manager agent contract.** This session does not touch the FM agent contract surface. CRM contract stays at v1.2.0; FM-side replanning continues independently.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

The next session after 246 is **247 — Housekeeping Batch 2 — app:reset Artisan + Orphan Media Cleanup** per the agreed Batch 2 split. Both prompts (246 + 247) were drafted at 245 close-gate Phase 1 and committed alongside 245's closing artifacts. Re-confirm at 246 close that 247's prompt is still the right next step (the FM replan may have surfaced a higher-priority interrupt).

- **Next session base prompt**: confirm `sessions/247. base-prompt.md` exists and matches the intended next shape.
- **Next session prompt**: confirm `sessions/247. Housekeeping Batch 2 — app:reset Artisan + Orphan Media Cleanup.md` is still accurate against any 246 discoveries.

### Phase 2 — Close

When the user says to close (after 247's prompt is re-confirmed):

- **Session log**: write a log file at `sessions/246. Housekeeping Batch 2 — UI Fixes Bundle — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly.
- **Update `sessions/completed-sessions.md`**: append session 246's row.
- **Update `sessions/release-plan.md`**: 246 partially closes E12 (the Batch 2 stub). Per Rule 6, only land a checkmark when the *entire* E12 entry closes — three items remaining (build-trigger audit, app:reset/media cleanup, plus any post-247 carry-forwards). Leave the E12 row unchecked; record progress in the close-gate log.
- **Update `sessions/session-outlines.md`**: edit the *Housekeeping — Batch 2* stub to strike through items shipped in 246 (text alignment, heroicon picker, blog pager bug). Surviving items: `app:reset` (going to 247), build-trigger audit (deferred to a later session).
- **Production deployment notes in the log**: list any operator action needed (rebuild paths, cache flushes, etc.).
- **Archive the previous session**: move all files matching `sessions/245. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files and commit on `session-246/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array. (Not expected this session — no new admin pages.)
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`. (Not expected this session.)
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. (Out of scope for this session — no portal code touched.)
