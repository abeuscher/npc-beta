# 293. Public Marketing Website — Page-Capture Harness & Track Close

Build the standalone Playwright page-capture script for the five published public pages, commit the screenshots, write the track's `build-summary.md`, and close the Public Marketing Website track.

---

## Stub reference

No `release-plan.md` § entry — this is **Phase 4** of the Public Marketing Website track and its **terminal session**. Canonical sources, in order:

1. **`sessions/tracks/public-marketing-website.md`** — Phase 4 sketch (the `scripts/generate-page-screenshots.js` pseudocode), the "Working folder" inventory, the gap-resolution discipline, and the **Stance** (deliverable is content/tooling, not production code; Phase 4's log doubles as the track-closure entry). Read first.
2. **`sessions/public website/brief.md`** — the Phase 4 success criteria (~lines 110–111: a Playwright capture of each rendered page via the dev environment committed as `screenshots/page-{slug}.png`; a short `build-summary.md` of completed / deferred / needs-user-input). Canonical for the track.
3. **`scripts/generate-thumbnails.js`** — the reference for how Playwright is invoked in this codebase. 293 writes a **parallel** script, not an extension.

The §-style close bookkeeping is in the base prompt's Phase 2 (no new release-plan § — track phase; the track's release-plan position collapses to a one-liner at this close).

---

## Session-specific deltas

- **Standalone, run-on-demand tooling — not a suite test.** Per the brief, the harness is "run on demand, not in CI." It is a host-run ESM Node script committed to the repo, and its PNG outputs are committed to the working folder so the user can review without running it. It is **not** added to Pest or the Playwright suite. No new Pest is expected this session.
- **Five published URLs, slug-keyed outputs.** `home → /` (root; served by `PageController@home`, **not** `/home`), `about → /about`, `pricing → /pricing`, `contact → /contact`, `demo → /demo`. Outputs: `sessions/public website/screenshots/page-{home,about,pricing,contact,demo}.png` (create the `screenshots/` directory). `fullPage: true`.
- **The demo page captures as-is.** `/demo` renders fine on the dev instance (it is a static split; only the button *click* 404s off-demo, and the harness does not click). The dark side shows lorem placeholder copy flagged `pending-copy` — **expected**, a milestone-close gate, not a defect. Do not treat the lorem or a non-functional local button as a capture problem.
- **Pre-existing red carried in.** The fast suite is **2446 / 1**; the 1 is the 291 contract-doc "seven-character git SHA" self-collision (`AppVersionStampTest`), not a 292/293 regression. Resolve at session start whether the user wants the one-line doc-prose fix folded into 293 (recommended; CRM doc-hygiene, no contract/runtime effect) or left flagged in `build-summary.md`. See the base prompt's carry-forward note.
- **`VERSION` bump is part of this session** — set the repo-root `VERSION` to `0.293.<iteration>` (291 discipline; deploy pipeline hard-fails a forgotten/duplicate version).

---

## Open questions to resolve at session start

- **AppVersionStampTest carry-forward:** confirm with the user (or from the work since 292) whether the one-line 291-CHANGELOG reword is folded into 293 or left as a `build-summary.md` carry-forward. Decision is the user's; default recommendation is fold it in (it's a one-line doc edit, no contract surface).
- **Capture viewport / dimensions:** pick a sane desktop viewport for full-page marketing capture (the thumbnail script uses `800×500` for widget tiles — the public pages want a wider desktop width, e.g. `1280`-class, full height via `fullPage: true`). Settle descriptively from the thumbnail script's pattern; not a user decision unless the brief constrains it.

---

## Phases

### Phase 1 — The page-capture harness

Write `scripts/generate-page-screenshots.js`: a host-run ESM Node script mirroring `generate-thumbnails.js`'s invocation ergonomics (`--base-url` defaulting to `http://localhost`, the global-Playwright + `NODE_PATH` note, the same launch/teardown shape). For each of the five published URLs: navigate, wait for fonts + the public asset bundle to settle (manifest-load / network-idle — the thumbnail script's wait strategy is the reference), take a `fullPage: true` screenshot, and write `sessions/public website/screenshots/page-{slug}.png`. Run it; confirm the five files exist and are non-trivial in size and show the right page. Commit the script and the five PNGs.

### Phase 2 — `build-summary.md` + track close

Write `sessions/public website/build-summary.md`: what shipped per phase across the whole track (Home/About/Pricing/Contact/Demo + the harness), what was deferred, and what needs the user's input before going further — explicitly the milestone-close gates: Privacy Policy + Terms pages, real photography across all five pages, Demo real copy, the Fleet-Manager shared-contract arc, and the `AppVersionStampTest` carry-forward if not folded in. Then the close steps in the base prompt's Phase 2 close the track: Phase 4 phase-expiry compression + track-level closure in the track doc, release-plan position collapsed to a one-liner, gap-report final-status pass, track moved out of the Active-tracks block.

---

## Out of scope

- **Resolving any open gap** — per the track's gap-resolution discipline, gaps are recorded with status in `build-summary.md`; resolution is the user's per-gap lift on separate sessions. No new widget / button variant / config option.
- **Real photography, Privacy/Terms pages, Demo real copy, the Fleet-Manager shared-contract arc** — milestone-close gates, tracked in `build-summary.md`; not 293 deliverables.
- **Adding the harness to CI or the Pest/Playwright suite** — explicitly run-on-demand per the brief.
- **Any `/api/health` / Fleet contract / response-schema change** — not contract-touching; CRM contract stays v2.3.0. The pre-existing `AppVersionStampTest` red is a doc-prose carry-forward, not a contract change.
- **Drafting `294. *`** — 293 is the track's terminal session; the next session is the user's choice from the milestone order. Draft 294 only when the user names it.

---

## Testing

- **Slow test groups to run this session:** none.
- **New tests expected:** no. The harness is run-on-demand tooling, not a suite spec; its check is "the five PNGs exist, are non-trivial, and render the correct page" (agent-verified) plus the user's visual review of the committed images. Regression target: the fast suite remains at its 292 baseline — **2446 / 1** (the known pre-existing 291-doc red), or **green** if the user directs the one-line doc fix be folded in. No positive Pest delta from 293 itself.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific:

- **Log file:** `sessions/293. Public Marketing Website — Page-Capture Harness & Track Close — Log.md`. **This log doubles as the track-closure entry.**
- **Artifact:** `scripts/generate-page-screenshots.js` + the five committed `sessions/public website/screenshots/page-*.png` + `sessions/public website/build-summary.md` + `VERSION` `0.293.x` + the track-doc Phase 4 retrospective / track closure + release-plan one-liner collapse + gap-report final-status pass.
- **Branch:** `session-293/N` (final iteration).
- **Terminal session:** do not auto-draft `294. *`; surface the milestone-close-gate list and let the user name the next session.
