# Base Prompt — Session 247

We are about to begin a new session: **247. Housekeeping Batch 2 — app:reset Artisan + Orphan Media Cleanup**.

This session lands the second tranche of *Housekeeping — Batch 2* (item 3 in the stub). Forcing function: the 14.6 GB backup incident surfaced at session 245's UAT — that backup ballooned because every dev-cycle `migrate:fresh --seed` left the previous run's media files behind on disk. This session fixes that pattern at the operator-tooling layer, plus pairs it with Spatie's built-in orphan-conversion sweep on the scheduler.

The build-trigger audit (Batch 2 item 2) defers to a later sub-session; that's audit-shaped work and benefits from its own slot.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/release-plan.md` in full — the 11 discipline rules at the top govern every session in the plan, and § E12 is the parent stub. Pay particular attention to Rules 5, 6, 11.
3. Read `sessions/session-outlines.md` for the active-tracks block, the Cross-Repo / Fleet Manager status, and the Housekeeping — Batch 2 stub which carries the original scope detail. The stub at session 247 start should reflect 246's strike-throughs (text alignment, heroicon picker, blog pager bug all shipped).
4. Read `docs/app-reference.md` for environment + container names + the `## Backups` section (Spatie laravel-backup config, scheduler-runner gap, retention configuration). Read `docs/schema/README.md` for the table index. No schema changes expected this session.
5. **For the `app:reset` artisan command** (Phase 1):
   - Read `bootstrap/app.php` for the existing `withSchedule()` block (where session 242 wired `backup:run` + `backup:clean`).
   - Read `app/Console/Commands/` for the existing artisan command pattern (file structure, test pattern). Pick a small command like `WidgetsManifestJsonCommand.php` as a structural reference.
   - Read `database/seeders/DatabaseSeeder.php` to understand what `migrate:fresh --seed` produces today and what depends on it.
   - Read `config/medialibrary.php` (or wherever Spatie Media Library config lives) for the `disk_name` setting that points at `storage/app/public/`.
6. **For the scheduled `media-library:clean`** (Phase 2):
   - Read the Spatie Media Library docs reference for `media-library:clean` semantics — it removes orphan media records (no model) AND orphan conversions (model exists but conversion file references no longer in DB). Behavior nuance: it does NOT remove files whose model row was hard-deleted bypassing the observer (that's exactly the gap `app:reset` addresses for the wholesale migrate:fresh case).
   - Read the existing scheduled commands in `bootstrap/app.php` to see the cadence + frequency conventions used (daily, weekly, etc.).
7. **For the production-refusal gate** (Phase 1):
   - Read existing artisan commands that have safety gates — `migrate:fresh` itself uses `--force` for non-interactive prod safety; mimic that pattern. Search for `APP_ENV` checks in existing command code.
8. Open `sessions/247. Housekeeping Batch 2 — app:reset Artisan + Orphan Media Cleanup.md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. (No migrations expected this session.)
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Fix any failures before proceeding. Baseline at 246 close: TBD (likely 1733 + a few new tests from 246).
- **Front-end builds — when to run which:** No front-end touch points expected this session — pure backend (artisan command + scheduler wiring).
- **Tests for new behaviour:**
  - `app:reset` command: integration test that asserts the production-refusal gate fires when `APP_ENV=production`, asserts the confirmation prompt appears in interactive mode, asserts the dev-mode happy path wraps `migrate:fresh --seed` and clears the storage tree.
  - Scheduled `media-library:clean` registration: smoke test asserting `php artisan schedule:list` shows the new entry.
- **Cross-repo coordination — Fleet Manager agent contract.** This session does not touch the FM agent contract surface. CRM contract stays at v1.2.0.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

The next session after 247 depends on the FM replan resolution. Three branches:

- **If FM has resolved A2's shape**: drop into the agreed CRM-side or FM-side follow-up (could be a contract bump, an A2 sub-session, or skip to A3).
- **If FM is still working**: pull forward another no-prereq item from the release-plan execution order — strongest candidates are E1 (Onboarding/Install Dashboard Widget), A5 (2FA), or E11 (Page Builder Focus-Scroll Clamp).
- **If the build-trigger audit feels timely**: lift Batch 2 item 2 here.

Decide at 247 close based on the current state of FM's planning. Draft the 248 base prompt + session prompt against whichever shape is agreed.

### Phase 2 — Close

When the user says to close (after 248's prompt is agreed):

- **Session log**: write a log file at `sessions/247. Housekeeping Batch 2 — app:reset Artisan + Orphan Media Cleanup — Log.md`. Use `sessions/template-session-log.md` as the format reference.
- **Update `sessions/completed-sessions.md`**: append session 247's row.
- **Update `sessions/release-plan.md`**: 247 is the second tranche of E12. Per Rule 6, only land a checkmark when *all* of E12 closes — the build-trigger audit (Batch 2 item 2) remains open. Leave the checkmark off; record progress in the close-gate log.
- **Update `sessions/session-outlines.md`**: edit the *Housekeeping — Batch 2* stub to strike through item 3 (`app:reset` shipped). The remaining surviving item is the build-trigger audit.
- **Production deployment notes in the log**: list any operator action needed. Specifically: the new `media-library:clean` schedule entry only fires once a scheduler-runner is in place (per the docs/app-reference.md "Scheduler runner — known gap"). Note this as a dependency on the Beta-1 scheduler-runner work.
- **Archive the previous session**: move all files matching `sessions/246. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-247/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array. (Not expected this session — artisan command, no admin UI.)
- Every new Filament page must include `getBreadcrumbs()` following the established pattern. (Not expected this session.)
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. (Out of scope for this session — no portal code touched.)
