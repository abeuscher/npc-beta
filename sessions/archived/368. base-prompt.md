# Base Prompt — Session 368 (Client Billing — Demo Conversion Cleanup Command)

**Start now.** Execute the reading list below, open the session prompt at `sessions/368. Client Billing — Demo Conversion Cleanup Command.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **368. Client Billing — Demo Conversion Cleanup Command**.

This is **execution-order position 48c** in `sessions/release-plan.md` — **CB3, "Client Billing — Demo Conversion Cleanup Command (node half)"**, under the `first-customer` gate (commercial readiness, *not* Beta-1-blocking). It is the **third and final CRM-side build session** of the Client Billing & Account track (`sessions/tracks/client-billing-and-account.md`), following 366 (CB1: contract v2.6.0 + the node suspension gate) and 367 (CB2: the "My Account" page + `manage_account` permission). CB3 depends on **CB1 only** and is the **node half of a conversion flow whose other half is Fleet Manager's FM-B4 session** (FM = Fleet Manager, the operator's separate control-panel application). It ships the one small artisan command FM invokes at step 5 of the conversion sequence; nothing here waits on any FM session, and after this the CRM lane for the whole billing track is complete.

This session is **NOT boundary-touching** — it consumes contract v2.6.0 and does not change it. The command is invoked over FM's **existing SSH channel** (`docker compose exec app php artisan …`), not over HTTP: no new `/api/*` surface, no response-schema change, no `CONTRACT_VERSION` bump. **CRM contract stays at v2.6.0.** The Cross-Repo block and `docs/fleet-manager-agent-contract.md` do not change this session.

**Session shape:** feature-style, non-boundary, **small (≤0.5 session)**. Builds one one-shot, idempotent, non-destructive artisan command that runs on a node *just after* Fleet Manager has flipped it out of demo mode: it removes the shared demo login account and unlocks the pages the demo reset had locked, so a converted prospect's node looks exactly like a fresh production install. Plus tests.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/session-outlines.md` (the roadmap) — the active-tracks block, and confirm the Cross-Repo block needs no edit this session.
3. Read the **release-plan entry** — `sessions/release-plan.md` § `#### CB3.` (position 48c). It is canonical for scope, success criterion, prerequisites, and artifact.
4. Read **`sessions/tracks/client-billing-and-account.md`** — canonical for this session's design: **§ Design decision 4** (demo → paying conversion; the **fixed seven-step order** is the thing to internalise — this command is **step 5**, and it runs *after* step 4's `APP_ENV` demo→production flip), plus **Appendix A § CB3** (success criterion) and **Appendix B § FM-B4** (the FM-side half that will call it).
5. Read **the demo surface this command cleans up** — and note carefully what each one *guards on*:
   - `app/Console/Commands/DemoResetCommand.php` — hard-gated `isDemoMode()`; blanket-locks every page (`Page::query()->update(['locked' => true])`) on each reset.
   - `app/Console/Commands/DemoRestoreCommand.php` — same gate; re-asserts the blanket page lock after a blob restore.
   - `app/Http/Controllers/DemoLoginController.php` — `abort_unless(isDemoMode(), 404)`; `firstOrCreate`s the shared account **`demo@demo.local`** and `syncRoles(['demo'])`.
   - `app/helpers.php` → `isDemoMode()` (i.e. `app()->environment('demo')`), and the `/demo/enter` route in `routes/web.php`.
   - `docs/schema/pages.md` — the `locked` column (**default `false`**; enforced in `PagePolicy::update()` + the page-builder save API; gated by the `edit_locked_pages` permission).
   - `database/seeders/PermissionSeeder.php` — the seeded `demo` role and the `edit_locked_pages` grants.
6. **The inverted guard — read this twice.** Every existing demo command *refuses to run unless* `isDemoMode()` is true. This command is the **exact inverse**: FM flips `APP_ENV` demo→production at conversion **step 4**, then invokes this command at **step 5** — so at the moment it runs, `isDemoMode()` is **false**. It must therefore carry **no `isDemoMode()` guard**, or it will refuse to run at the only moment it is ever needed. Whatever safety gate it does carry (see the session prompt's open question) must not be that one.
7. Read `docs/app-reference.md` for container names and the artisan invocation shape, and the **367 log** (`sessions/367. … — Log.md`) for what CB2 shipped and any carry-forwards.
8. Note any drift between this prompt / the track doc and the code in a brief work-log entry; proceed per the drift rules.

---

## Starting state inherited from session 367

- **CRM contract at v2.6.0** — 366 (CB1) shipped it; 367 (CB2) consumed it without change. **This session is non-boundary — it consumes v2.6.0 and does not bump it.**
- **CB1 + CB2 surface exists and is tested — consume it, don't rebuild:** `App\Services\Billing\BillingStateReader` / `BillingState` / `SuspensionState`, `App\Http\Middleware\EnforceSuspensionState`, the `suspension` `/api/health` subcheck, `App\Filament\Pages\Settings\AccountPage`, the seeded `manage_account` permission (granted to no shipped role), and the slim panel-wide billing banner (a `panels::page.start` render hook in `AdminPanelProvider`). **None of it is in this session's scope** — the cleanup command touches the demo surface, not the billing surface.
- **Schema baseline:** no migration since 359's `users` 2FA columns. **This session adds no migration** — the command mutates existing rows (`users`, `pages.locked`) only.
- **Fast Pest baseline: 3045 / 0** (parallel, 367 close). Expected delta: **new tests** for the command — the happy path (demo account removed, pages unlocked), idempotence (safe to re-run), non-destructiveness, and the safety gate resolved at the Open Gate.
- **Playwright suite:** not expected to run — this is a CLI command with no browser surface. Its outcomes (rows removed, flags flipped, exit codes) are fully Pest-verifiable.
- **VERSION** is `0.367.01`. Bump to `0.368.<iteration>` at the close (or earlier if a mid-session deploy is wanted — `deploy.yml` rejects a duplicate `VERSION`).
- **Planning state:** CB2 marked ✅ in `sessions/release-plan.md` and in the track doc's Appendix A at the 367 close; the track-doc status snapshot marks CB1 + CB2 complete with CB3 the last CRM entry. FM's lane (FM-B1 → FM-B2 → …) runs independently; **FM-B2 is the FM-side session that begins pushing billing-state documents**, which is what makes CB2's Account page visible on real nodes.
- **Housekeeping:** feature shape — no inbox absorption. `sessions/housekeeping-inbox.md` carries one item folded in at the 367 close: **form spam / CAPTCHA** (the owner asked whether a non-CAPTCHA mitigation is preferable). Not this session's work; it needs its own sizing pass.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The track doc's locked decisions govern.** Design questions resolve against `sessions/tracks/client-billing-and-account.md` § Design decision 4; genuine conflicts go to the owner at the gate.
- **Non-destructive by design, and say what that means.** The command removes exactly one purpose-built account (`demo@demo.local`) and flips `pages.locked` back to its fresh-install default. It deletes no content, no contacts, no donations, no media, and no other user. Nothing in it is a "cleanup sweep" — resist any urge to generalise it.
- **Idempotent, and safe to re-run.** FM may invoke it more than once (retry after a transient SSH failure). A second run must succeed quietly with the same end state, not error.
- **It runs in production mode.** See reading-list item 6. Design and test it for `APP_ENV=production`, not demo.
- **Exit codes are the contract.** FM reads them over SSH — `SUCCESS` on both the did-work and the nothing-to-do paths; `FAILURE` only on a genuine error.
- **No migration. No new permission. No new page.** This is one command class plus tests.
- **Non-boundary checklist:** confirm no contract file (`docs/fleet-manager-agent-contract.md`, `HealthController::CONTRACT_VERSION`, the Cross-Repo block) is touched. If the work reaches for the boundary, stop — that's a scope signal, not a step.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation (per the CLAUDE.md reporting rules) and **wait for the owner to confirm before starting.** Bring:

1. **The safety-gate question (the one real design decision).** Blanket-unlocking every page is a no-op on a *fresh* install (`pages.locked` defaults to `false`) — but it is **not** a no-op on an established client node, where a real CMS editor may have deliberately locked pages via the `edit_locked_pages` permission. So: what stops this command, if ever run on the wrong node, from silently unlocking a real client's locked pages? Come with a recommendation (e.g. gate the unlock on evidence the node *was* a demo — the presence of the `demo@demo.local` account — versus an explicit `--force` / confirmation, versus unconditional), and be explicit about how the chosen gate interacts with the idempotence requirement (an evidence gate makes the second run a no-op, which may be exactly right, or may mean a half-finished first run can't be completed).
2. **Confirmation of the inverted guard** — the command carries **no** `isDemoMode()` check, because FM has already flipped the node to production by the time it runs.
3. **The `demo` role's fate** — the shipped `demo` role row stays seeded (harmless vocabulary; `/demo/enter` 404s once out of demo mode, so the account cannot be recreated). Confirm we remove the *account*, not the role.
4. **Command naming** — `demo:convert-cleanup` vs `demo:cleanup` vs `conversion:cleanup`. Cheap to confirm; the `demo:` prefix groups it with `demo:reset` / `demo:restore` in `artisan list`, which is where an operator will look for it, even though it deliberately doesn't share their demo-mode guard.

One confirmation, then proceed.
