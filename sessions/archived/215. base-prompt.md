# Base Prompt — Session 215

We are about to begin a new session: **215. Widget Primitive — Phase 3d (Dashboard Config UI & Storage)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session adds one new table (`dashboard_configs`) and reuses `page_widgets` with a new polymorphic owner type — read `docs/schema/page_widgets.md` and `docs/schema/page_layouts.md` for the current owner-polymorphism shape.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise, especially the "Every configurable surface in the app is configured the same way" principle that motivates reusing the Vue builder rather than building a parallel Filament config UI.
   - `sessions/widget-primitive-migration.md` — the migration sketch. Pay specific attention to the **"Polymorphic owner discipline"** bullet under the Preparation discipline moves (load-bearing for the storage decision this session) and to the Phase 6 "Page-builder convergence" sketch (this session is the first concrete step of that phase arriving naturally).
4. Read session 214's log. It landed the three dashboard-native widgets, the `publicSurface` field on `SlotContext`, `ContractResolver` event-model support, and retired the legacy `DashboardQuickActionsWidget`. The hardcoded `DashboardSlotGridWidget::widgets()` from 214 is the starting point — this session replaces it with DB-backed arrangement.
   - `sessions/214. Widget Primitive — Phase 3c (Dashboard-Native Widgets) — Log.md`
5. Read the mount point and the three dashboard-native widget definitions this session rearranges:
   - `app/Filament/Widgets/DashboardSlotGridWidget.php` — widgets() currently returns three hardcoded entries. This session switches it to read from `DashboardConfig` scoped to the current admin's role.
   - `app/Widgets/Memos/MemosDefinition.php`, `app/Widgets/QuickActions/QuickActionsDefinition.php`, `app/Widgets/ThisWeeksEvents/ThisWeeksEventsDefinition.php` — existing definitions already declare `allowedSlots(): ['dashboard_grid']` and `category(): ['dashboard']`. This session is the first to *enforce* `allowedSlots()` at the picker level.
6. Read the slot declaration — the session enforces its layout-constraints declaratively for the first time:
   - `app/WidgetPrimitive/Slots/DashboardGridSlot.php` — `layoutConstraints()` already declares `allowed_appearance_fields: ['background', 'text']`, `column_stackable: false`, `full_width_allowed: false`. The inspector honors these when running in dashboard mode.
7. Read the existing page-builder plumbing that the Vue reuse mirrors:
   - `app/Livewire/PageBuilder.php` — Livewire shell that bootstraps the Vue app. The new `DashboardBuilder` Livewire shell mirrors this structure but owns a `DashboardConfig` rather than a `Page`.
   - `resources/views/livewire/page-builder.blade.php` — bootstrap blade (how the Vue app mounts).
   - `resources/js/page-builder-vue/main.ts` — entry point; reads `data-bootstrap` JSON. The `BootstrapData` shape needs a new `mode` field.
   - `resources/js/page-builder-vue/App.vue` — root component.
   - `resources/js/page-builder-vue/stores/editor.ts` — Pinia store. Needs mode-aware API routing.
   - `resources/js/page-builder-vue/api.ts` (read `ApiClient` shape) — endpoint dispatch.
   - `resources/js/page-builder-vue/components/PreviewCanvas.vue`, `components/InspectorPanel.vue` — components that need slot-aware filtering.
   - `app/Http/Controllers/Admin/PageBuilderApiController.php` — pattern for the new `DashboardBuilderApiController`.
8. Read the widget primitive base class and existing enforcement surfaces:
   - `app/Widgets/Contracts/WidgetDefinition.php` — `allowedSlots()`, `defaultAppearanceConfig()`, and the appearance schema the inspector reads.
   - `app/WidgetPrimitive/Slot.php` — the `layoutConstraints()` shape.
9. Read the role surface the per-role ownership plugs into:
   - `Spatie\Permission\Models\Role` (vendor) — the Spatie role model the polymorphic owner points at.
   - `database/seeders/DatabaseSeeder.php` — shows how `super_admin` and `cms_editor` are created. `DashboardConfigSeeder` slots in here.
10. Open `sessions/215. Widget Primitive — Phase 3d (Dashboard Config UI & Storage).md` and read the session prompt carefully. The **Design decisions (resolved before starting)** block settles the storage-shape and Vue-reuse calls made during 214's close conversation; they are not open for rediscussion.
11. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. The admin bundle, the public site CSS, and the page-builder Vue app all compile through Vite, so this one command covers all three surfaces.
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. This rebuilds the public widget asset bundle on the build server and regenerates `public/build/widgets/manifest.json` (which the admin panel and public layout both read).
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/216. base-prompt.md`. Expected next session: TBD — plausible candidates include (a) completing Phase 6 convergence by migrating the public page builder to the same contract-bound resolver path the dashboard now uses, (b) beginning Phase 4 retrofit of existing widgets onto contracts (BlogListing is the smallest, since its contract already exists from session 209), or (c) a Memos composer widget that exercises the write-side dogfooding the 214 close discussion surfaced. Scope to be agreed with the user.
- **Next session prompt**: draft `sessions/216. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/215. Widget Primitive — Phase 3d (Dashboard Config UI & Storage) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: under the Widget Primitive track stub, mark Phase 3d complete (dashboard config UI and storage landed). Note that Phase 3 is now effectively closed except for any residual polish; Phase 6 convergence is partially underway via the Vue reuse.
- **Archive the previous session**: move all files matching `sessions/214. *.md` into `sessions/archived/`. This includes the previous session's base prompt, session prompt, and log. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-215/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
