# Base Prompt — Session 236

We are about to begin a new session: **236. Widget Primitive — DashboardConfig → DashboardView Retrofit**.

This is the cheap, named-ad-hoc follow-up post-Phase-5d that the Widget Primitive track has been carrying as a status-snapshot bullet for several sessions. The dashboard's per-role widget-composition model (`DashboardConfig`, session 215) predates the unified View vocabulary that landed in 5a/5b/5c (sessions 227–229) and the `IsView` PHP interface that DashboardView is *named* against in [app/WidgetPrimitive/IsView.php](app/WidgetPrimitive/IsView.php) but does not yet implement. This session lifts `DashboardConfig` into the View vocabulary natively — both the model class (`App\Models\DashboardConfig` → `App\WidgetPrimitive\Views\DashboardView`) and the table (`dashboard_configs` → `dashboard_views`), to mirror `RecordDetailView`'s shape side-by-side. The three existing dashboard-native widgets from session 214 (Memos, Quick Actions, This Week's Events) ride through unchanged and provide live verification that the slot-grid render path still works end-to-end after the rename.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top.
3. Read `sessions/tracks/widget-primitive.md` — this is the active track for this session. The status snapshot, the Phase Retrospectives for 5a / 5b / 5c / 5c.5 / 5d / 5e, the "Cheap follow-up" line in the Phase 5 sequence, and the `Design decisions locked in` section's "a+" View framing entry are all directly load-bearing.
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/dashboard_configs.md` (current state — will be renamed/superseded this session) and `docs/schema/record_detail_views.md` (the precedent shape).
5. Read the most recent session logs that established the View / IsView / DashboardConfig pattern this session lifts into alignment:
   - `sessions/archived/215. Widget Primitive — Phase 3d (Dashboard Config UI & Storage) — Log.md` — the original `dashboard_configs` table + polymorphic owner discipline + Vue page-builder reuse in `dashboard` mode.
   - `sessions/archived/227. Widget Primitive — Phase 5a (Typed Ambient Context Primitive) — Log.md` — the "a+" View framing decision; DashboardView named in the IsView interface comment.
   - `sessions/archived/228. Widget Primitive — Phase 5b (Record-Detail Filament Mount + Ambient Wiring + IsView Primitive) — Log.md` — `IsView` PHP interface declared.
   - `sessions/archived/229. Widget Primitive — Phase 5c (record_detail_views Table + IsView Registry + Sub-Nav Rendering Primitive) — Log.md` — the table-backed precedent (`record_detail_views` table, `App\WidgetPrimitive\Views\RecordDetailView` model, `ViewRegistry` singleton).
6. Read the full state of every file this session edits, moves, or creates:
   - **Model + interface (move + implement):** `app/Models/DashboardConfig.php` (move to `app/WidgetPrimitive/Views/DashboardView.php`), `app/WidgetPrimitive/IsView.php` (read-only — interface contract), `app/WidgetPrimitive/Views/RecordDetailView.php` (read-only — shape reference).
   - **Migration + seeder + schema doc:** `database/migrations/2026_04_24_022229_create_dashboard_configs_table.php` (read-only — current shape), `database/seeders/DashboardConfigSeeder.php` (rename to `DashboardViewSeeder.php`, update FQN references), `database/seeders/DatabaseSeeder.php` (update reference), `docs/schema/dashboard_configs.md` (rename to `docs/schema/dashboard_views.md`), `docs/schema/README.md` (update table index).
   - **Callers (FQN updates):** `app/Filament/Widgets/DashboardSlotGridWidget.php`, `app/Filament/Pages/DashboardSettingsPage.php`, `app/Http/Controllers/Admin/DashboardBuilderApiController.php`, `app/Livewire/DashboardBuilder.php`.
   - **Tests touching the renamed model or seeder:** `tests/Unit/Models/DashboardConfigTest.php` (rename to `DashboardViewTest.php`, update FQN + table assertions), `tests/Feature/DashboardConfigSeederTest.php` (rename to `DashboardViewSeederTest.php`), `tests/Feature/DashboardSettingsPageTest.php`, `tests/Feature/DashboardSlotGridTest.php`, `tests/Feature/DashboardBuilderApiControllerTest.php`, `tests/e2e/dashboard-settings/dashboard-settings.spec.ts`, `tests/e2e/helpers/db.ts` (any seeder-class references).
7. Open `sessions/236. Widget Primitive — DashboardConfig → DashboardView Retrofit.md` and read the session prompt carefully.
8. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. **One migration is expected this session — the `dashboard_configs` → `dashboard_views` rename plus the polymorphic `page_widgets.owner_type` backfill.**
- **Update `docs/schema/`** after writing the migration — rename `docs/schema/dashboard_configs.md` to `docs/schema/dashboard_views.md` and update `docs/schema/README.md`'s table index entry to match.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session may touch Vue page-builder source if the `dashboard` editor mode references the model class name anywhere — if it does, `npm run build` is required. Pure-PHP rename otherwise. No `build:public` expected.**
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session's behavior is byte-equivalent — the rename should leave the dashboard's render and admin paths unchanged. Existing tests stay green after the FQN updates; one new test pins the IsView interface implementation on `DashboardView`.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents. **237 candidate slots (locked in at session-236 prompt-drafting time):**

- **Phase 5d-4 — Recent Activity widget**, if pursued. Aggregates across multiple system models (notes, memberships, donations, event registrations, transactions) — would be its own design surface and a stress-test of Phase 5e's `requiredPermission` grammar against a multi-model widget. Defer unless a design conversation surfaces a need.
- **Phase 6 — Page-builder convergence.** 0–1 standalone sessions; partially landed via the Vue reuse in 215, deepened by the same reuse for record-detail in 230. The DashboardView retrofit removes the last name-asymmetry that would have shown up in a pure convergence pass — 237 may surface this as a clean Phase-6 close.
- **Importer→widget status mismatch fix** (surfaced during 234 manual testing). Donations importer's `mapDonationStatus()` writes `'completed'` for `active`/`paid`/`succeeded`/blank input, falling outside the canonical four-value enum and outside the Recent Donations widget filter. Small, focused — one-line fix to emit `'active'` plus a one-time backfill.
- **Phase B — Admin gating generalization** from the Financial Data Origin track. Lifting the `! $record->stripe_id` action-visibility pattern from `TransactionResource` into a shared trait keyed off `source`. Lands when a Refund/Cancel/Sync action surface is imminent — not yet.
- **Placeholder widget removal.** Long-running carry-forward from 5d-1 — `RecordDetailPlaceholderDefinition` has been on disk and registered for ~5 sessions with no live consumer.

The user picks among these at 236 close.

- **Next session base prompt** (whatever 237 ends up being): copy `sessions/template-base-prompt.md` to `sessions/237. base-prompt.md`.
- **Next session prompt**: draft `sessions/237. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/236. Widget Primitive — DashboardConfig → DashboardView Retrofit — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 236's row.
- **Update `sessions/session-outlines.md`** (the roadmap): collapse references to the DashboardConfig retrofit to reflect it as complete. Adjust the active-tracks block accordingly.
- **Update the Widget Primitive track doc** at `sessions/tracks/widget-primitive.md`: bump the status snapshot (last update date, what's complete, what's active). Drop the "Cheap follow-up post-5d — convert DashboardConfig…" bullet from the remaining-in-track list. The retrofit is small enough that a one-line addition under the Phase 5 sequence is sufficient — no full retrospective needed unless the session surfaces something genuinely material.
- **Archive the previous session**: move all files matching `sessions/235. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the migration, the moved model, the renamed seeder, the renamed schema doc, every caller's FQN update, the test renames + assertion updates, the log, the updated outlines, the track doc, the completed-sessions index, the 237 base prompt + session prompt, and any files moved into `sessions/archived/`), commit on the current `session-236/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
