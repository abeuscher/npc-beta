# Base Prompt — Session 313 (Code Shape & Fit Apply — Cycle 1)

> **Scheduled as session 313.** Runs immediately after session 312 audit closes. The apply half of the Code Shape & Fit track's first cycle.

**Start now.** Execute the reading list below, open the session prompt at `sessions/313. Code Shape & Fit (Apply) — Cycle 1.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **313. Code Shape & Fit (Apply) — Cycle 1.**

This is the **apply half of the Code Shape & Fit track's first cycle.** Session 312 produced the W-R1 through W-R5 backlogs, the intent-unrecoverable findings section, the Open Refactor Flags, and the proposed Deliberately Kept register additions/retirements. This session walks them with the user, ratifies pre-resolutions, and applies iteration-sliced.

This session **MAY be boundary-touching depending on which refactors land** — most cycles aren't, but a W-R5 seam move can cross a contract surface. CRM contract bump posture is **no bump unless an iteration explicitly requires it** (additive-only seam moves don't); the close-log states the final posture. Refactors that change public API shape (DB column meaning, JSON response shape, migration writes) are out of scope; those go to the next cleanup cycle's squash.

**Session shape:** track-phase / apply. Walks the 312 backlog with the user, applies iteration-sliced. Instance-fix is the default on class findings; pattern-fix requires explicit user opt-in at walkthrough. Six-iteration ceiling (past that is the carve-out smell).

Before doing anything else:

1. Re-read session templates only if uncertain about a structural detail.
2. Read `sessions/tracks/code-shape-and-fit.md` — track doc.
3. Read `sessions/refactor-track-deliberately-kept.md` — the carve-out register.
4. **Read `sessions/312. Code Shape & Fit (Audit) — Cycle 1 — Log.md`** — the audit output is the canonical input to this session. Open Refactor Flags, W-R1 through W-R5 backlogs, proposed additions/retirements all live there.
5. **Read the full session prompt** at `sessions/313. Code Shape & Fit (Apply) — Cycle 1.md` — Open Flags, walkthrough sections, iteration strategy, close-gate.
6. Read `sessions/session-outlines.md` for the cycle stub; `sessions/release-plan.md` for the plan-entry position.
7. Note drift in a brief work-log entry.

---

## Starting state inherited from session 312

- **CRM contract at v2.3.0** — unchanged by 312 (audit-only).
- **Schema baseline:** no schema changes since session 308; 312 added none (audit-only).
- **Fast Pest at 312 close** — *baseline carried in from 311's 2580; 312 expected to add zero (no code).* Confirm at session start.
- **Playwright suite** running on every CI push. New specs expected this session for behaviour-changing extractions (per the test-coverage rule for new seams).
- **Planning state:** Cycle 1 was pulled forward from its trailing-pair-after-cleanup-Cycle-3 slot at user direction — pulled forward calibration delta documented in 312's audit log. The Deliberately Kept register was validated (no unilateral edits) by 312; this session ratifies the proposed additions/retirements.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this session shape does not absorb housekeeping items; backlog deferrals from 312 go to the next cycle's pre-loaded findings.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Every row is a candidate, not a mandate.** Walk Open Refactor Flags at session start to ratify each pre-resolution and confirm iteration ordering.
- **Instance-fix default on class findings.** Pattern-fix requires explicit user opt-in at walkthrough.
- **Six-iteration ceiling** is the carve-out smell — at iteration 6+, the next iteration's content is the carve-out.
- **Tests for behaviour-changing extractions.** Pure structural extractions with byte-equivalent semantics rely on the existing suite; the commit message notes that explicitly.
- **Close-gate diff narration in prose** — name what was consolidated, cite specific abstractions / files / call sites removed and any new ones added. LOC delta is context, not target.
- **Intent-citation preserved through commits.** Each iteration commit message carries the Intent citation from the backlog row.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **Questions are not instructions** per `feedback_questions_are_not_instructions`.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

313 closes Cycle 1 of the Code Shape & Fit track. The track's cycle-retrospective entry lands at this session's close (window covered, sessions list, quantitative outcomes table, load-bearing decisions, blind spots, process incidents, won't-fixes reaffirmed) per the cleanup-track retrospective shape. Next cycle is post-cleanup-Cycle-3 per the track's forward-plan rule — no immediate successor session.

### Phase 2 — Close

When the user says to close: write `sessions/313. Code Shape & Fit (Apply) — Cycle 1 — Log.md`. Update `completed-sessions.md`. **Archive `sessions/312. *` into `sessions/archived/`.** Commit on the final `session-313/N` branch, notify, do not push. Never push to main / force-push / merge to main. **Update `release-plan.md` and `session-outlines.md`** — mark the 313 child stub closed; collapse the cluster's release-plan position to a one-liner pointing at the track doc.

**Update the Deliberately Kept register** at `sessions/refactor-track-deliberately-kept.md`:
- Walkthrough-ratified additions (won't-fix items with citation pointing at this session) land in the register file.
- Stale entries flagged by 312 and ratified-for-retirement at walkthrough get removed; the removal goes in the register's Retirement log with the session-313 citation.

**Update the track doc** `sessions/tracks/code-shape-and-fit.md` § Cycle Retrospectives with the Cycle 1 retrospective entry.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages name the workstream (W-R1 / W-R2 / etc.) and carry the Intent citation; close-log diff narration is in prose, not a LOC delta table; no docstrings/comments on code you did not write. **Don't bundle across workstreams or risk tiers** — see iteration strategy in the session prompt.
