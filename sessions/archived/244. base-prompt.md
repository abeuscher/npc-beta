# Base Prompt — Session 244

We are about to begin a new session: **244. Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria**.

This is a planning / design session, not an implementation session. It takes the working-notes outline at `sessions/release-plan-outline.md` and produces the actual pre-Beta-1 release gate: a vetted working set of rehearsals, a per-scenario success criterion, an execution order, and a roadmap update that lifts the selected rehearsals into named follow-up sessions while marking the un-selected ones as post-1.0 hardening. The session does **not** execute the rehearsals — each selected rehearsal becomes its own subsequent session.

The session immediately preceding 244 (session 243 — Housekeeping Batch 1) closed five of six small paper-cut items; Phase 6 (Mobile Type Scaling) was lifted to its own pre-Beta-1 stub mid-session against a user-supplied design (3 size fields per element beneath the size picker, 25%-per-step default, mapped to the existing `$bp-md`/`$bp-lg`/`$bp-xl` breakpoints). 243 also surfaced two items directly relevant to the 244 vetting work: (a) the backup pipeline gzip + relative-path tweaks landed, which means the **DB wipe + backup recovery** capsize drill the outline names already has its updated recipe in `docs/app-reference.md` Backups → Restoration; (b) the new `/llms.txt` route + the global `noindex_global` toggle add new crawler-facing surfaces that any compatibility / SEO rehearsal should now exercise.

The session immediately following 244 is **not yet committed** — it depends on which rehearsals win the vetting. The 244 deliverable includes the per-rehearsal-session stub set, so the next-up session falls out of 244's output.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/release-plan-outline.md` in full. This is the seed input for 244 — the user's working notes capturing candidate rehearsals across five categories (Onboarding, Capsize, Scale, Workflow, Compatibility), the four vetting criteria, and the explicit "Not yet decided" register at the bottom. Every candidate is a *maybe* until the 244 conversation converts it.
3. Read `sessions/session-outlines.md` in full. The 244 stub in the Beta-1 scope section names the concrete inputs to bring; pay particular attention to:
   - **Existing pre-Beta-1 stubs that already overlap rehearsal candidates** — the stub explicitly flags the Notes Permissions & Permissions Audit stub ↔ workflow `Permission audit`, the Organizations Model Overhaul stub ↔ onboarding `Custom fields + public collection ingestion`, and the Event Ticket Tiers stub ↔ workflow `Event with everything`. These overlaps mean the vetting may consume some existing stubs wholesale (by selecting the rehearsal as the canonical instance of that work) or leave both in place if they cover different ground.
   - **The Active tracks block** — Widget Primitive and Fleet Manager Agent are both substantially complete; nothing in 244 should reopen either.
   - **The Cross-Repo block** — non-boundary-touching by design; 244 produces planning docs only.
4. Read `sessions/242. Fleet Manager Agent — Phase 2 (Backup Pipeline) — Log.md` and `sessions/243. Housekeeping Batch 1 — Log.md` (the latter lands during 243's close gate; if 244 starts before 243's log is written, just read the 243 commit messages on the `session-243/1` branch).
   - 242's log carries the verified DB wipe + backup recovery procedure exercised twice end-to-end. The vetting session decides whether to keep this scenario as a release gate or fold it into the Backups documentation.
   - 243's log carries the lift-out reason for Mobile Type Scaling (deeper theme-architecture concern that doesn't fit a small CSS edit) and the user's design sketch — relevant if the vetting session decides any rehearsal needs the typography system stabilized first.
5. Read `docs/app-reference.md` Backups section and `docs/fleet-manager-agent-contract.md` if any rehearsal touches operator runbooks (Capsize drills) or external contract surfaces (Compatibility drills against the FM agent).
6. Open `sessions/244. Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria.md` and read the session prompt carefully.
7. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify. (244 produces docs and roadmap edits — no code is expected. If a code-touching design call surfaces, surface it and stop rather than implementing.)
- **Before implementing any integration**, ask the user what existing system handles it. **For 244 specifically:** no integrations are expected. The session output is planning artifacts.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. The vetting itself is full of decision points by design — work through them with the user, do not unilaterally pick.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot. (Not applicable to 244.)
- **No migrations expected.** No schema doc updates expected.
- **No fast-test run expected.** 244 produces no code change. If the session somehow lands a code edit, run the fast suite per the standard recipe (`docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`); 243 close baseline is 1689.
- **No front-end build expected.**
- **Cross-repo coordination — Fleet Manager agent contract.** **244 is non-boundary-touching by design** — the agent contract stays at v1.2.0; the FM-side cache is at v1.1.0 awaiting refresh on FM's next boundary-touching session. If a Compatibility rehearsal vetted into the working set wants to *probe* the contract surface from the FM side, that's a rehearsal-execution concern, not a 244 concern — 244 produces the plan, not the rehearsal output.
- **Pause for manual testing.** 244 produces docs only — there is no manual-test loop. Instead, the **review loop** is: present the vetted working set, the per-scenario success criteria, and the execution order to the user; iterate until the user signs off; then close.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after the user signs off on the working set. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After the working set is agreed, Claude stops and waits. The user reviews the deliverables and brings up the next session's prompt when ready.

The next session after 244 falls out of 244's output — the first rehearsal in the execution order becomes the 245 session. Naming and prompt drafting deferred until the working set is finalized inside 244 itself.

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/245. base-prompt.md`, customizing the read list for the chosen first rehearsal. The prompt should name the success criterion (lifted from the finalized `sessions/release-plan.md`) up front so 245 doesn't relitigate it.
- **Next session prompt**: draft `sessions/245. <Rehearsal Title>.md` using `sessions/template-session-prompt.md` as the format reference. Base it on the corresponding subsection of the finalized `sessions/release-plan.md`.

### Phase 2 — Close

When the user says to close (after 245's prompt is agreed):

- **Session log**: write a log file at `sessions/244. Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly. The log captures: each candidate's vetting verdict (in / out / merged-into-existing-stub), the per-scenario success criterion text for the inbound set, the execution order, and any rehearsals surfaced during 244 that were not in the original outline.
- **Update `sessions/completed-sessions.md`**: append session 244's row.
- **Replace `sessions/release-plan-outline.md` with `sessions/release-plan.md`**. The outline doc is working notes; the plan doc is the canonical pre-Beta-1 release gate. Move the file (`git mv` if appropriate) and rewrite its contents per the session deliverables. The plan doc is the single source of truth that subsequent rehearsal sessions read at start.
- **Update `sessions/session-outlines.md`**:
  - Remove or fold any existing pre-Beta-1 stubs that the vetting picked up wholesale (e.g., if `Notes Permissions & Permissions Audit` becomes the canonical "Permission audit" rehearsal, the stub itself is replaced by a pointer into `release-plan.md`; if it remains a separate concern, both stay).
  - Add new stubs under a new `### Rehearsal Sessions` subsection within the Beta-1 scope block, one per rehearsal in the working set (with each pointing into the corresponding subsection of `release-plan.md`).
  - Confirm the existing 244 stub is removed (the work it described is complete and lives in this session's log + `release-plan.md`).
- **No track to update** — 244 doesn't belong to a track.
- **Production deployment notes in the log**: none. 244 produces no code, no migration, no operator action.
- **Archive the previous session**: move all files matching `sessions/243. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (the log, the new `release-plan.md`, the deleted `release-plan-outline.md`, the updated `session-outlines.md`, the completed-sessions index, the 245 base prompt + session prompt, and any files moved into `sessions/archived/`), commit on the current `session-244/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly. (No code is expected for 244, but if a code edit surfaces, follow project conventions.)
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones. (244 creates `sessions/release-plan.md` as a renamed/replaced version of `sessions/release-plan-outline.md`, plus the per-rehearsal stubs inside `session-outlines.md`. No other new files expected.)
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. (Out of scope for 244 — no portal code touched.)
