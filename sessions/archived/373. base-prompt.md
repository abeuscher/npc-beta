# Base Prompt — Session 373 (C3b — Auto Tax Receipt Email)

**Start now.** Execute the reading list below, open the session prompt at `sessions/373. C3b — Auto Tax Receipt Email.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **373. C3b — Auto Tax Receipt Email**.

This is **launch-schedule position 4** in `sessions/release-plan.md` — **C3b, the first of the Gate-2 money-path sessions** (positions 4–10). The security-hardening front-load is done: positions 1–3 (S1/370, S3/371, S2/372) shipped, so every money-path rehearsal from here runs against the final hardened surface. C3b closes the "→ receipt" leg of Gate 2's must-work money path (**Stripe → CRM records → receipt**): every successful donation must be **provably acknowledged automatically**. It is a direct prerequisite for **C4** (the donation-to-acknowledgment loop rehearsal, position 8).

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. The CRM contract **stays at v2.7.0** (S2's app-layer gate, session 372). If the work somehow reaches `app/Http/Controllers/Api/Fleet/*` or the `/api/health` payload — it should not — the Two-Repo Coordination Protocol reactivates; otherwise leave the contract and the Cross-Repo block untouched.

**Session shape:** feature-style (Gate-2 money-path), 1 session (may split per Rule 11 if the per-gift-vs-annual template decision + recurring-donation scope + idempotency surface more than one context can hold). Wires an **automatic per-gift tax-receipt/acknowledgment email** into the successful-donation Stripe-webhook path, reusing the existing receipt template/Mailable machinery, with a per-donation idempotency guard so a webhook replay never double-emails a donor. This is **wiring an existing seam** (`StripeWebhookController::handleDonationCheckout` records the gift and sends nothing today), not a greenfield build.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/release-plan.md` § **C3b** (the working-set entry — success criterion, prerequisites, artifact), plus the § "Launch schedule" list and the § C4 entry (C3b is C4's prerequisite — know what C4 will rehearse). Skim §§ C3, C3a, C3c so you know what the C3-family split did and did not lift (C3a/#32b deferred to first customer; #32c absorbed into S3; C3c is the sibling event-comp session at position 5).
3. Read `sessions/session-outlines.md` — the active-tracks block and the Cross-Repo block (context only; not touched this session).
4. **Read the code before writing anything** (process rule; this is a wiring session, so the existing seam is load-bearing): `app/Http/Controllers/StripeWebhookController.php` (esp. `handleCheckoutSessionCompleted` → `handleDonationCheckout`, and `handleInvoicePaymentSucceeded` for recurring renewals); `app/Mail/DonationReceipt.php` (the existing **annual-aggregate** Mailable) and `app/Models/DonationReceipt.php` (the sent-receipt record, keyed `(contact_id, tax_year)`); `app/Filament/Pages/DonorsPage.php` (the manual annual "Send Receipts" flow that **stays** as backfill); `app/Models/EmailTemplate.php` + `app/Filament/Resources/EmailTemplateResource.php` (the `forHandle` / `render` / `resolveWrapper` machinery and the `manage_email_templates` admin surface); the `Donation` + `Transaction` models; and however Finance settings store org data (look for an EIN / org tax-id `SiteSetting`).
5. Read `docs/app-reference.md` for environment/container names + the queue-worker posture (the Mailable is `Queueable`; confirm the `worker` container runs). Read `docs/schema/README.md` then the `donations` (and `donation_receipts` / `transactions`) table files — the idempotency marker likely lands as a small migration on `donations`.
6. Open `sessions/373. C3b — Auto Tax Receipt Email.md` and read the session prompt carefully — it maps the drift between the plan entry's "stub" framing and the actual (further-along) code.
7. Note any drift between the session prompt and the actual code in a brief work-log entry; proceed per the drift and decision-threshold rules.

---

## Starting state inherited from session 372

- **CRM contract at v2.7.0** — 372 (Security S2) **was** the security track's one boundary-touching session: it added the app-layer `VerifyFleetAgent` gate behind the nginx mTLS termination on the five FM `/api/*` endpoints (per-install `FLEET_GATE_SECRET` shared-secret header, inert until provisioned, additive/no-flag-day), bumped `HasContractVersion` 2.6.0 → 2.7.0, and resolved the `/api/logs` PII tension as an accepted Gate-3-register residual. **373 is NOT boundary-touching** — rely on v2.7.0, don't revisit it.
- **What 372 shipped (rely on it, don't revisit):** the second lock on the fleet endpoints + the contract v2.7.0 additive bump + both coordination docs (contract doc, Cross-Repo block) updated first per the Two-Repo Protocol. FM-side absorption (send the header, then push the secret) is FM-repo work, pending.
- **Schema baseline:** no migration since 359 (the 2FA columns). S1/S2/S3 added none. **C3b likely adds one** — a per-donation idempotency marker (e.g. `acknowledged_at` on `donations`) — decided at the Open Gate; if it lands, update `docs/schema/` and run it via Docker.
- **Fast Pest baseline: 3091 / 0** (parallel, verified at 372: 3076 baseline + 15 new fleet-gate tests). **New tests expected this session** — webhook-triggers-one-acknowledgment, idempotent-on-replay, correct token content, and an annual-path regression guard (see the session prompt's Testing section).
- **Playwright:** baseline preserved; **not expected to run** — an automatic webhook→email path is exercised by Pest feature tests with synthetic Stripe events + `Mail::fake()`, not a browser. (Reach for a spec only if a public donate-flow surface actually changes.)
- **VERSION** is `0.372.01` (372's close). This session's close bumps to `0.373.01`. If this session deploys mid-flight to test the live webhook against Stripe, bump VERSION *before* the deploy (the `main` deploy enforces immutable version tags — `CLAUDE.md` Git Workflow / `feedback_deploy_version_bump`).
- **Planning state:** launch-schedule positions 1 (S1/370), 2 (S3/371), 3 (S2/372) complete; this is position 4 (C3b). After C3b: C3c (5), A3/A4 node-readiness + backup-drill (6–7), C4/C5 money-path rehearsals (8–9), F1 payment-depth E2E (10), then S4/S5 (11–12) close the security track and assemble the Gate-3 findings register.
- **Housekeeping:** feature-style shape — does **not** absorb inbox items by default (that's the audit-session privilege, Rule 2). Check `sessions/housekeeping-incoming.md` at start; digest anything captured there at the Phase-1 close step.

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md` § Process rules for every session). Session-specific:

- **This is a wiring session — read the seam before deciding anything.** The existing receipt infrastructure (annual Mailable, `DonationReceipt` model, `EmailTemplate` machinery, `DonorsPage` manual flow) is the material you build with. Default hypothesis is "add the dispatch + a per-gift idempotency marker + a per-gift template," not "build a receipt system." (`feedback_audit_integration_seams`.)
- **Surface the architectural calls at the Open Gate before going deep** (`feedback_verify_objective_outcomes`): per-gift-vs-annual template shape, idempotency key (likely a migration), recurring-donation scope, EIN/IRS-language source, sync-vs-queued dispatch. The session prompt lists these with recommendations — bring them, confirm, then proceed. Don't commit-and-unwind a data-model choice.
- **The annual statement is out of scope.** `DonorsPage` + the `donation_receipt` handle are a *different document* (year-end aggregate) and stay as-is. The manual "Send Receipts" action remains the backfill/resend path. Don't refactor it.
- **Idempotency is a correctness requirement, not a nicety.** Stripe replays webhooks. Prove the no-double-email guard with a test that replays the same event.
- **The webhook must stay fast and never 500 on a mail failure.** It returns 200 to Stripe; queue the send (or otherwise isolate mail failures) so a mail-provider hiccup doesn't fail the handler.
- **Verify objective outcomes yourself** — `Mail::fake()` + assert-sent/assert-not-sent-on-replay is the close-the-loop signal here; you don't need the user to run it.
- **Do not touch:** the S1 headers/CSP, S3 XSS/exposure, or S2 fleet-gate work (rely on them); the sibling Stripe webhook branches (membership / product / event registration) beyond reading them for the pattern.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation and **wait for the owner to confirm before starting.** Bring: (1) the **template shape** — new per-gift acknowledgment handle vs. adapting the existing annual template, with the recommendation (new handle, reuse the render machinery); (2) the **idempotency approach** and whether it needs a migration (likely an `acknowledged_at` on `donations`); (3) the **recurring-donation scope** (initial gift only, or initial + each renewal); (4) the **EIN / IRS-language source** (a Finance `SiteSetting` — confirm one exists or add it); (5) **sync vs queued** dispatch; and (6) anything the code read contradicts in the plan entry's C3b framing. One confirmation, then proceed.
