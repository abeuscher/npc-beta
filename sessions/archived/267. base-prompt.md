# Base Prompt — Session 267

We are about to begin a new session: **267. Full-Width Architecture Enforcement — Background and Content Split**.

This session lifts working-set entry **E10** (position 22 in the post-265 execution order). Two pieces of work are folded into one session: (a) the original E10 stub — make widget compliance with the page-layout's `full_width` contract structural rather than per-widget discipline; (b) split the single `full_width` toggle into two — `background_full_width` and `content_full_width` — applied to BOTH the widget Appearance layer AND the column-layout primitive. Folded together because they share the same render-pipeline surface; doing enforcement on a soon-to-be-renamed knob would be wasted work.

This session is **NOT boundary-touching** — no Fleet Manager contract change. CRM contract stays at v2.2.0 (per session 263).

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference.
2. Read `sessions/session-outlines.md` § "Full-Width Architecture Enforcement" (line 217-219) — the original stub naming the three candidate fix shapes.
3. Read `sessions/release-plan.md` § E10 (line 343-348) and the execution-order entry at position 22 (line 467).
4. Read `sessions/266. Backup notification hardening — FM 020 finding — Log.md` (the immediate predecessor) — fast-suite baseline (expect ~2140) for this session's growth-target comparison.
5. Read `docs/schema/page_widgets.md` and `docs/schema/page_layouts.md` end-to-end — the appearance/layout JSON shape canonical references. Note especially the rationale at `page_layouts.md:43` for why `full_width` lives on `layout_config` (not on `appearance_config.layout`) for column layouts: the schema treats it as a layout-behavior toggle, not an appearance override. The fold-in respects this — the two-knob split lands on BOTH paths in parallel without trying to unify storage.
6. Read `app/Services/AppearanceStyleComposer.php` end-to-end. Phase 2 edit target. Current full-width resolution is at lines 75-76 (instance override + per-type default); the converged path lands here.
7. Read `app/Services/PageBlockRenderer.php` end-to-end. Phase 2 edit target. Pay attention to the three current full-width read sites at lines 28-29, 38, 118 — these are part of why E10's structural-enforcement half is needed; they should converge.
8. Read `resources/views/components/page-widgets.blade.php` end-to-end. The render-side blade that consumes `full_width` and emits the wrapper. Phase 2 CSS / wrapper-emission target.
9. Read `resources/js/page-builder-vue/components/appearance/SectionLayoutPanel.vue` end-to-end. Phase 3 edit target (widget Appearance toggle). Note: the existing `fullWidthDisabled` + `fullWidthDisabledReason` props pattern is sibling precedent for the gray-out-with-tooltip behavior the new background toggle needs when content is full-width — reuse, don't invent.
10. Read `resources/js/page-builder-vue/components/LayoutInspectorPanel.vue` lines 60-220. Phase 3 edit target (column-layout toggle). Parallel UI surface to the widget Appearance panel; the parallel toggle addition should match the widget panel's UX exactly.
11. Read `resources/js/page-builder-vue/types.ts` lines 14-25 (widget appearance) and 100-115 (layout). Type definitions to update.
12. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
    - **`widget_types.full_width` shape.** Confirm whether this is a database column on `widget_types` or a JSON key inside a config column. The migration approach differs (column-replace vs. JSON-key rewrite). Check `docs/schema/widget_types.md` and the latest `widget_types`-touching migration.
    - **Bypass audit.** Identify which specific widgets currently bypass the layout's full-width instruction. Candidates: search `app/Widgets/*/[Definition].php` and the corresponding blade templates for direct `width: 100vw`, negative margins, `position: absolute` wrapper escapes, or other CSS that ignores the wrapper. The audit's findings inform Phase 2's enforcement-shape choice — surface the audit summary to the user before committing to a specific fix shape (per the original E10 stub's three candidates: layout-aware wrapper / contract-level composer guard / per-template CSS cleanup).
    - **Inner content wrapper precedent.** The two-knob shape introduces a new "background bleeds, content boxed" rendering, which needs a content-level wrapper inside the outer (background) wrapper. Check whether a content wrapper already exists in `page-widgets.blade.php` or the public-site layout — if so, reuse; if not, Phase 2 introduces it. The container max-width rule should be the authoritative source for "boxed" content.
    - **Per-type default audit.** What does each widget type's current per-type `full_width` default look like? With the split, what should each type's `(background_full_width, content_full_width)` defaults be? Surface candidates (especially for hero / banner / cta / spacer widget types where defaults likely flip toward `(background: true, content: false)`) before the migration commits values.
13. Open `sessions/267. Full-Width Architecture Enforcement — Background and Content Split.md` and read the session prompt carefully.
14. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.2.0** (per session 263) — unchanged.
- **Fast test suite green** at the **266 close baseline of ~2140** (confirm at session start).
- **Slow test suite green** at **15 passed** (assumed unchanged at 266 close).
- **A1d' ✅ closed at session 266** — backup notification hardening + spatie SiteSetting bridge.
- **E10 lifts here** with the two-knob fold-in.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **Schema changes are JSON-key data migrations** for both `page_widgets.appearance_config.layout` and `page_layouts.layout_config`, plus possibly a column-shaped migration for `widget_types.full_width` (per verify-at-start finding #1). Update `docs/schema/page_widgets.md`, `docs/schema/page_layouts.md`, and `docs/schema/widget_types.md` to reflect the new key shape.
- **No Composer changes.** No new dependencies expected.
- **Front-end builds required:**
  - `npm run build` — Vue / TS changes to the two inspector panels and `types.ts`.
  - `docker compose exec app php artisan build:public` — widget renderer / blade wrapper changes affect the public-site bundle.
- **No agent contract change.** Internal CRM. No `docs/fleet-manager-agent-contract.md` edit, no version bump.
- **Pause and ask** if the bypass audit (verify-at-start question 2) surfaces a fix shape that exceeds the original three candidates (a/b/c). The candidates were the stub's framing, but the actual bypasses might call for something else.
- **Tests required for new behaviour.** Estimated ~15-25 new tests across migration, composer/renderer, UI, and one Playwright spec covering the three meaningful render states (boxed / background-bleed / full-bleed) on a representative widget.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. **Target: 266 baseline (~2140) + ~15-25 new tests.**
- **Manual verification expected.** Render-pipeline changes affect what users see on every page. After the fast suite is green, render a representative page in the page-builder preview AND on the public-site at all three states; pull the user in for the visual judgment call before close.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

Standard close gate per `sessions/template-base-prompt.md`. Session-specific notes:

- **Session log:** write `sessions/267. Full-Width Architecture Enforcement — Background and Content Split — Log.md`. Document the bypass-audit findings (which widgets needed individual cleanup vs. caught structurally), the converged read-path shape, the per-type default flips, the migration approach (column vs. JSON-key for `widget_types`), and any in-session absorptions.
- **Update `sessions/completed-sessions.md`:** append session 267's row.
- **Update `sessions/release-plan.md`:**
  - **E10 ✅** flipped at line 343-348; tighten the success criterion to reflect the two-knob fold-in (the line 343-348 entry currently says only "make full-width compliance structural"; update to name the background/content split + bypass-audit findings).
  - Confirm the next entry in the execution order is E11 at position 23.
- **Update `sessions/session-outlines.md`** § "Full-Width Architecture Enforcement" (line 217-219) — mark complete or rewrite with the post-session shape; the rationale lines about `appearance_config` vs. `layout_config` separation may want pulling up into a closed-session retrospective.
- **`docs/schema/`** — update `page_widgets.md`, `page_layouts.md`, and `widget_types.md` (or only the first two if `widget_types.full_width` turned out to be JSON-keyed).
- **No `docs/fleet-manager-agent-contract.md` updates** — non-boundary-touching.
- **Findings register** — flag the bypass-audit results, the per-type default flips, any rendering-divergence-on-edge-cases findings, and any test-pattern friction around exercising the inner-wrapper rendering.
- **Help-doc update** — only if the bypass audit surfaces operator-visible behaviour changes worth documenting; not expected by default.
- **Archive the previous session:** move all files matching `sessions/266. *.md` into `sessions/archived/`.
- **Commit:** stage all changed files and commit on `session-267/N` branch. Notify the user. Do not push.
- **No FM-side handoff** — non-boundary-touching.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

The style rules in `sessions/template-base-prompt.md` apply. Session-specific:

- **Concrete values rule applies to the migration.** Existing rows must have concrete values (`true` or `false`) for both new keys post-migration; no null/missing as proxies for "unset." Migration fills `(background_full_width, content_full_width) = (existing_full_width, existing_full_width)` to preserve current behavior exactly.
- **Columns are not widgets.** `page_widgets` and `page_layouts` are distinct schema surfaces (different JSON columns: `appearance_config.layout` vs. `layout_config`). Apply the two-knob split to BOTH paths in parallel without trying to unify the storage location.
- **Uniform UX across the two surfaces.** The widget Appearance panel and the column LayoutInspectorPanel are different code paths but should present the two-toggle UX identically — same labels, same gray-out behavior, same tooltip wording. Per-type plumbing cost is acceptable to keep the UX uniform.
- **Page is not a View.** Don't lift the column-layout fix shape into `RecordDetailView` / `DashboardView` unless the bypass audit specifically touches them.
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** introduce new full-bleed widget primitives (lives in post-Beta-1 "Hero Full-Bleed Promotion"); do **not** unify the two storage paths; do **not** change the inspector-panel layout philosophy beyond adding the second toggle.
- Prefer editing existing files to creating new ones — except for the migration file, the new migration test, and (if warranted) one new Playwright spec.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
