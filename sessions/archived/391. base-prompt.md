# Base Prompt — Session 391 (Donations Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/391. Donations Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **391. Donations Extracted**.

This is **arc position D3, third and final session of the block** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary). The block ran carve (389) → package (390); this session is the **extraction closer** — the fourth runbook run: `nonprofitcrm/donations`, already a Stage B path package owning its four-table schema and carrying a 14-file testsuite, leaves the repo for `crm-plugin--donations` via `docs/plugin-extraction-runbook.md`, walking its `distribution.json` source kind `path` → `vcs`. Every edge is a known edge from D2 (388, Events): the fixture migrator re-points, the vendor-path testsuite move, gate 4 on a real asset, and `plugins/` emptying again until D4's carve. Nothing here is novel — this is the runbook exercised on the second migration-owning, second testsuite-carrying plugin.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change (the 385/387/388 precedent: a public VCS-resolved package needs nothing from the deploy pipeline). The plugin contract (`docs/plugin-contract.md`) bumps 0.14.0 → 0.15.0 at close: External repo section gains the fourth extracted consumer (second migration-owning + testsuite-carrying), runbook deltas folded if any surface.

**Session shape:** infrastructure, behavior-preserving, single-phase (extract only — Donations is already packaged). Runtime behavior byte-identical throughout: routes 272/272 (dev/test), fast suite at the 390 baseline with assertions unchanged (path re-points only), same test counts, no schema work (the four tables travel with the package — the 390 redraw made that the point). **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D2 ✅; D3 carve + package done, extraction is the block closer) and § Stage D settled decision 4 (per-vertical blocks end with extraction).
3. Read the 390 session log (`sessions/390. Donations Packaged — Log.md`), especially its **block handoff** section — the extraction survey below was captured there at close.
4. Read `docs/plugin-contract.md` at 0.14.0 — § External repo (Stage C-lite) in full (the vcs shape, the version-field inversion, the public-repo auth posture, the two test-membership postures, the may-empty-`plugins/` note), § The distribution manifest (the `path` → `vcs` walk + guard interlock), surface 5 (the fixture semantics that must survive the move), surface 11 (vendor-relative asset paths + the bundle-content gate).
5. Read `docs/plugin-extraction-runbook.md` end to end — fourth run; the worked examples are LogoGarden, Payments, and Events (D2 is this session's closest analogue — same edges, smaller scale).
6. Open `sessions/391. Donations Extracted.md` — the survey (captured at 390 close) and front-loaded decisions; the Open-Gate item is the owner-created repo.
7. Skim the plugin being moved: `plugins/Donations/` top-level shape — read `DonationsServiceProvider.php` fully (confirm `__DIR__`-relative loading throughout), `plugins/Donations/composer.json` (the `version` field that drops), and `Widgets/DonationForm/DonationFormDefinition.php` (the one asset declaration that goes vendor-relative).
8. Read the re-point surfaces the survey names: `tests/Fixtures/Plugins/DonationsRemovedTestCase.php` + `DonationsDisabledViaFlagTestCase.php` (the two migrator `basePath` reaches), `plugins/Donations/tests/Feature/DonationsSchemaSession390Test.php` (the migrator-path assertion) and `DonationCreditsImporterUntouchedTest.php` (three `base_path` file reads), `tests/Feature/PluginPackageGuardTest.php` (the four-table owner map + extracted-set assertions), `tests/Feature/DistributionManifestGuardTest.php`, `phpunit.xml` (the `Donations` testsuite line).
9. Read the build surface: root `composer.json` (the path repository entry + require), `Dockerfile` (the two Donations COPY lines that go), `distribution.json` (the Donations `path` entry).
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry) and note the housekeeping-inbox posture for this shape.
11. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 390

- **CRM contract at v2.7.0** — 390 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D2 complete; **D3 carve (389) + package (390) done.** `plugins/Donations/` is the fourth path-repo package `nonprofitcrm/donations` 0.1.0 (explicit `version` field — drops at extraction), path repository + `^0.1` require, manifest source `path`, Dockerfile manifest COPY ×2 stages. The three extracted plugins: `crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` v0.2.0, `crm-plugin--events` v0.1.1. Plugin contract **v0.14.0**.
- **Schema baseline:** the four donation tables are **plugin-owned since 390** — created by `plugins/Donations/database/migrations/` (byte-faithful raw DDL, FK order `funds` → `donations` → `donation_credits` → `donation_receipts`), out of the core dump, both per-composition identity checks verified clean at 390. **No schema work this session** — the migrations travel with the package; the fixtures and guards re-point.
- **Composition-safety state:** the setup checklist's default-fund row is route-gated (the 390 find); donor scopes / resource delete-path reads / random-data generator / resolver arms are recorded, accepted composition debt. Nothing here changes at extraction (runtime-identical move).
- **Fast Pest suite green at 3314 / 0 parallel** (390 close — 3309 baseline + 5 additive). The `Donations` testsuite: **14 files at `plugins/Donations/tests` — 103 fast + 1 slow** (measured at 390; the 389 log's "18 files / 99 fast" overcounted). Expected delta this session: **zero** — path re-points only, same counts from the vendor path.
- **Route baseline: 272 routes** (dev/test environment) — byte-identical this session.
- **Playwright suite** — CI signal; the known parked chrome-parity failure stands (pre-existing, non-gating). No local Playwright.
- **Housekeeping inbox** — infrastructure shape absorbs no items; the two 389-folded items (`[plugin-arch]` EmailTemplateResource tokenHint, portal_form_collision defaults) stay.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Every Donations behavior net — enabled twin, removal mirror, flag-vanish, schema-ownership pair, checkout/acknowledgment/receipts/soft-credits/importer/widgets — passes with assertions unchanged except the surveyed path re-points; the fixtures prove disabled-≠-uninstalled against the **vendor'd** migrations. Routes 272/272. No schema, no visual change.
- **The manifest is edited, never the generated file.** The `path` → `vcs` walk happens in `distribution.json`; `plugins:manifest-sync` is a provider-list no-op; the guards verify the rest.
- **Gate 4 is live for one asset** — DonationForm's `script.js` path goes vendor-relative; after the rewrite, run `widgets:sync` then `build:public` and prove the built bundle still carries the `donationForm` Alpine component (the 389/390 verification method). RecentDonations declares no assets; there is no cross-plugin asset input (unlike D2's BlogPager edge). A green suite alone is not the proof.
- **The testsuite move is settled** (the D2 posture, contract § External repo): phpunit's `Donations` suite re-points to `vendor/nonprofitcrm/donations/tests` — same 14 files, same counts, running in core CI unchanged. If the vendor-path suite misbehaves under paratest/CI, stop and surface it.
- **`plugins/` empties again** — the 388 guard relaxations already tolerate it; no new relaxation expected. Root `"Plugins\\"` PSR-4 stays (D12 endgame).
- **Lockfile changes ship in the same commit as their composer.json changes.** Runbook step-5 ordering (the 387 caveat): with the source dir deleted, artisan cannot boot until `composer update` re-resolves — composer first, then `plugins:manifest-sync`.
- **Scope is binding.** No D4+ work (Memberships is the next block); no model/factory moves (§ 6.7 stands); no per-plugin CI (D11); no root-PSR-4 retirement (D12); no FM surface; no events-handle registry migration. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.14.0 → 0.15.0 (External repo fourth consumer; reference implementations updated; runbook deltas folded); plan doc § Stage D D3 line marked ✅ (block closed); arc header advanced to D4.
- **D4 kickoff input at close:** record in the session log what the Memberships block (D4) inherits — the membership webhook branch still inline in Payments (the 389 inversion deliberately left it; disjoint subscription ids), tiers + MembershipStatus/PricingChart widgets, the memberships importer, and anything this run adds.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code, state the plan, note any drift found — and confirm the one owner-facing item: **the owner has created the empty public `crm-plugin--donations` repo** (public for credential-free composer resolution in every build context, checked out as a sibling of this repo). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
