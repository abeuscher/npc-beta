# Session 105 — QuickBooks Per-Type Account Mapping — Log

**Date:** 2026-03-31
**Branch:** session-105/1 … /3

---

## What was built

### Phase 1 — Account Resolution Logic

- Migration: added `quickbooks_account_id` (nullable string) to `funds` table
- New `resolveIncomeAccount(Transaction)` method on `QuickBooksClient` with resolution order: fund override → type mapping (`donation`/`purchase`) → account map default → global `qb_income_account_id` → exception
- Subject type key mapping: `Donation` → `donation`, `Purchase` → `purchase`, everything else → `default`
- Updated `createSalesReceipt` and `createRefundReceipt` to call `resolveIncomeAccount`
- Replaced `getIncomeAccounts()` with `getDepositAccounts()` — queries Bank + Other Current Asset types instead of Income (corrects `DepositToAccountRef` requirement)
- Removed diagnostic all-accounts logging from session 103

### Phase 2 — Settings UI

- Replaced single income account dropdown with three dropdowns: Default Account, Donations, Product Purchases
- Added refund info note: "Refunds automatically post to the same account as their original transaction"
- Save logic constructs `qb_account_map` JSON site setting and keeps `qb_income_account_id` in sync as the global default for the sync job skip condition
- "Refresh Accounts" button updated to use new `qb_deposit_accounts` cache key
- Added optional "Deposit Account Override" dropdown to Fund create/edit form (only visible when QB is connected)

### Git workflow update

- Replaced `session-NNN` + `session-NNN-patch-NNN` convention with `session-NNN/N` iteration model
- Updated CLAUDE.md, template-base-prompt.md, template-session-prompt.md, template-session-log.md
- Added convention change note to session-outlines.md

### Bug fix (iteration /2)

- Fixed `json_decode` TypeError in `FinanceSettingsPage::mount()` — `SiteSetting::get` auto-decodes JSON types to arrays; now handles both array and string returns

### Session 107 prompt

- Wrote session prompt and base prompt for 107: Paid Event Registration & Membership Checkout
- Confirmed event registrations and membership signups have no Stripe integration yet — `EventController::register` rejects paid events with "not yet available", `SignupController::store` has no payment flow

---

## Security checklist — verified

- [x] Account mapping UI is gated to `manage_financial_settings` (inherits from Finance Settings page)
- [x] Fund QB account field on FundResource inherits existing fund permission gates
- [x] No QB credentials appear in the account map setting or fund column
- [x] `qb_account_map` JSON contains only QB account IDs — no sensitive data

---

## Schema changes

- Added `quickbooks_account_id` (nullable string) to `funds` table
- Updated `docs/schema.md` to reflect the new column

---

## Production deployment notes

Run migration after deploying: `docker exec nonprofitcrm_app php artisan migrate`

---

## Deferred decisions

- Per-event and per-membership-tier QB account mapping — uses `default` account for now (session 107 scope note)
- The `display_name` bug in the Donations admin list sort query (references a column that doesn't exist on `contacts`) — pre-existing, not from this session

---

## Test result

422 passed, 0 failed.
