# Base Prompt — Session 225

We are about to begin a new session: **225. Date Handling — Centralized Vocabulary + Schema Audit**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session adds one column to two tables — read `docs/schema/events.md` and `docs/schema/products.md` to confirm current shape before drafting the migration.
3. Read the framing for this session — the **Date Handling — Centralized Vocabulary + Schema Audit** stub in `sessions/session-outlines.md` (the section after the Widget Primitive track stub). The stub captures the why; the session prompt assumes you have read it.
4. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — established the `_label` projector-key naming convention this session migrates onto `DateFormat`.
   - `sessions/archived/221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer) — Log.md` — established the QuerySettings `orderByOptions` allowlist; the events allowlist falls back to `created_at` because `published_at` doesn't exist on events. This session fixes that.
   - `sessions/archived/222. Widget Primitive — Phase 4f (BlogPager Retrofit + PageContext::posts Retirement) — Log.md` — first session to flag the date-vocabulary concern; the Date Handling stub originated mid-session there.
   - `sessions/223. Widget Primitive — Phase 4g (Event Single-Row Pair) — Log.md` (or `sessions/archived/` if 224 close has moved it) — established the `Carbon::parse(ISO)` template-side conditional date formatting pattern (EventDescription's `g:ia` vs `ga` toggle) that this session collapses into `DateFormat::TIME_SMART`.
   - `sessions/224. Widget Primitive — Phase 4h (Product Pair Retrofit + WidgetDataResolver Shell Retirement) — Phase 4 Close — Log.md` — closed Phase 4 of the Widget Primitive track. Every retrofit candidate now flows through `ContractResolver`. This session is the first beat of post-Phase-4 cleanup.
5. Read the full state of every file this session edits or touches. Specifically:
   - `app/WidgetPrimitive/Projectors/SystemModelProjector.php` — three format calls (`projectPost::published_at_label`, `projectEvent::starts_at_label`, `projectEvent::ends_at_label`).
   - `app/Services/PageContextTokens.php` — two format calls (`date`, `starts_at`).
   - `app/Widgets/ThisWeeksEvents/template.blade.php` — one inline format call that overrides the projector.
   - `app/Widgets/Memos/template.blade.php` — one `Carbon::parse(...)->format(...)` call.
   - `app/Widgets/EventDescription/template.blade.php` — the `'F jS'` + conditional `'ga'`/`'g:ia'` block (the most complex callsite — the only one with smart minute-zero behaviour).
   - `app/Widgets/PortalEventRegistrations/template.blade.php` — one format call.
   - `app/Filament/Resources/ContactResource.php` — portal join date.
   - `app/Filament/Resources/MemberResource.php` — membership starts_on column.
   - `app/Filament/Resources/TransactionResource.php` — QB-synced tooltip (currently uppercase `PM` — flips to lowercase under canonical vocabulary; document the visible casing change in the log).
   - `app/Filament/Pages/Settings/FinanceSettingsPage.php` — cookie expiry display (uses `'M j, Y g:i A T'` with timezone tail; the `T` portion stays inline since it's a one-callsite need).
   - `app/Filament/Pages/Concerns/InteractsWithImportWizard.php` — default-label generator (uppercase `PM` → lowercase `pm` casing change).
   - `app/Models/Event.php` and `app/Models/Product.php` — schema changes land here. Add `published_at` to `$casts` (Eloquent infers datetime from timestamp columns, but explicit is preferred).
   - `app/Observers/EventObserver.php` — extend with `creating` / `updating` logic to set `published_at` on first publish. The observer exists today and only logs activity; this session adds publish-time tracking.
   - `app/Observers/ProductObserver.php` — **does not exist today.** Create it, register via `#[ObservedBy(...)]` attribute on `Product` (mirror of `Event`'s registration). The only behaviour this session lands on it is the `published_at` set; future product lifecycle hooks can join it.
   - `app/Widgets/EventsListing/EventsListingDefinition.php` — `querySettings()` `orderByOptions` map gains `'published_at' => 'Published'`.
   - `app/Widgets/ThisWeeksEvents/ThisWeeksEventsDefinition.php` — same map, same addition (it shares the events allowlist shape).
   - `app/Widgets/ProductCarousel/ProductCarouselDefinition.php` — `querySettings()` `orderByOptions` map gains `'published_at' => 'Published'`.
6. Check whether `App\Observers\PageObserver` actually sets `published_at` on first publish (it does **not** — pages rely on the admin's Filament form field). The stub in `session-outlines.md` describes the proposed pattern as "mirror of `PageObserver`'s pattern" — that phrasing is aspirational, not descriptive. The session's design decisions clarify: this session adds an **observer-driven auto-set on first publish** for events and products (because they have no admin form field for `published_at` today, unlike pages); a future session can add a Filament form field for explicit override if user-driven scheduling becomes a need.
7. Read the relevant existing tests that this session's migration may affect:
   - `tests/Feature/EventsListingContractRetrofitTest.php` — Test A's rendered HTML asserts the projector's `starts_at_label` output. After the case-collapse (uppercase PM → lowercase pm), assertions referencing `'PM'` need updating to `'pm'`. Run before changes to capture the current strings; update expected values together.
   - `tests/Feature/EventDescriptionContractRetrofitTest.php` — same caveat for any starts_at-formatted assertion.
   - `tests/Feature/QuerySettingsApiTest.php` — extends to assert `published_at` appears in `orderByOptions` for events / this_weeks_events / product_carousel.
   - `tests/Feature/QuerySettingsContractRoundtripTest.php` — extends with one case asserting `published_at` flows into the events resolver SQL (mirror of the existing post `published_at` assertion).
   - Any test that asserts a date-formatted string in an admin-side rendered output (TransactionResource, FinanceSettingsPage, importer wizard) — same casing-collapse caveat.
8. Open `sessions/225. Date Handling — Centralized Vocabulary + Schema Audit.md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`. **This session adds `published_at` to `events` and `products` — both schema docs must be updated.**
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216–224. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session touches no widget asset files** (SCSS, JS, manifest), so neither build should be required. Run `build:public` once at end-of-session as a safety check anyway — it's cheap.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands new behaviour in two places: the `DateFormat` class (unit-tested per-format) and the `published_at` migration + observer wiring (feature-tested for backfill correctness and observer auto-set on first publish).
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/226. base-prompt.md`. Expected next session: **Phase 5 — Record-Detail Slot + Ambient Context Refactor** (the next architectural beat for the Widget Primitive track, deferred from Phase 4's close in 224). `PageContext` generalizes to `SlotContext` with subtypes (page, dashboard, record-detail). Record-detail sidebar becomes a slot. Widgets that need "the current contact" get it from the slot's ambient context, not from controller plumbing. Three to six sessions estimated.
- **Next session prompt**: draft `sessions/226. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/225. Date Handling — Centralized Vocabulary + Schema Audit — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: mark the Date Handling stub complete. Confirm the next stub (Phase 5 — Record-Detail Slot) follows.
- **Archive the previous session**: move all files matching `sessions/224. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-225/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
