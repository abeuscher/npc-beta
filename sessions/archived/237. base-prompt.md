# Base Prompt — Session 237

We are about to begin a new session: **237. Widget Primitive — Phase 6 (Page-as-View Adapter, Placeholder Sweep, Track Closure)**.

This is the closure session for the Widget Primitive track. Phases 1–5 (sessions 209–235) plus session 236's `DashboardConfig → DashboardView` retrofit have landed every committed milestone in the migration sketch. The track has carried an open Page-as-View elective ("a+" decision, locked in at 5b close) and a still-on-disk placeholder widget (5b's `RecordDetailPlaceholderDefinition`, no live consumer since 5d-1 attached the first concrete record-detail widget to `contact_overview`). 237 closes both, compresses the Phase Retrospectives into a milestone-closure block, and marks the track substantially complete.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top.
3. Read `sessions/tracks/widget-primitive.md` — this is the active track for this session. The status snapshot, the **"a+" Page-as-View decision** under "Design decisions locked in," the **"Page-as-View — the elective bottom of the View stack"** entry under "Forward hooks — elective, not required," and the Phase 6 description under "The 2.0 transition — rough sequence" are all directly load-bearing.
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies.
5. Read the most recent session logs that establish the surfaces this session touches:
   - `sessions/archived/228. Widget Primitive — Phase 5b (Record-Detail Filament Mount + Ambient Wiring + IsView Primitive) — Log.md` — `IsView` interface declaration; the placeholder widget's introduction; the "a+" Page-as-View framing recorded in the close-gate.
   - `sessions/archived/229. Widget Primitive — Phase 5c (record_detail_views Table + IsView Registry + Sub-Nav Rendering Primitive) — Log.md` — `RecordDetailView` as the first table-backed `IsView` impl.
   - `sessions/archived/231. Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget) — Log.md` — the moment the placeholder was no longer the only widget on `contact_overview`. (If 231 isn't archived yet, check `sessions/archived/231. *.md` after Phase 2's archival sweep.)
   - `sessions/236. Widget Primitive — DashboardConfig → DashboardView Retrofit — Log.md` — the final pre-Phase-6 vocabulary alignment.
6. Read the full state of every file this session edits, moves, or deletes:
   - **Page-as-View:** `app/Models/Page.php` (read-only — adapter target), `app/WidgetPrimitive/IsView.php` (read-only — interface contract), `app/WidgetPrimitive/Views/DashboardView.php` and `app/WidgetPrimitive/Views/RecordDetailView.php` (read-only — sibling impls for shape parity).
   - **Placeholder removal:** `app/Widgets/RecordDetailPlaceholder/RecordDetailPlaceholderDefinition.php`, `app/Widgets/RecordDetailPlaceholder/template.blade.php` (delete), `app/Providers/WidgetServiceProvider.php` (drop the registration), `tests/Unit/Widgets/RecordDetailPlaceholderDefinitionTest.php`, `tests/Feature/RecordDetailPlaceholderRenderTest.php` (delete), `tests/Feature/EditContactRecordDetailViewTest.php` (the placeholder-rendering case adapts or drops), and any widget-count assertion that bumped `widget_types` count when the placeholder was added in 5b (`tests/Feature/ProductCarouselTest.php` and `tests/Feature/WidgetPickerSession119Test.php` per the 5b log; verify with grep before editing).
   - **Track doc closure:** `sessions/tracks/widget-primitive.md` — compress Phase Retrospectives into a closure block; bump status snapshot to "substantially complete"; carry-forward only the genuine remainders (Forms widget retrofit + `PageContext` retirement, both still no-scheduled-session).
7. Open `sessions/237. Widget Primitive — Phase 6 (Page-as-View Adapter, Placeholder Sweep, Track Closure).md` and read the session prompt carefully.
8. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **No migration is expected this session.** The placeholder widget's `widget_types` row is cleaned up by re-running `WidgetTypeSeeder` (which upserts on handle and removes orphans per its existing semantics — verify that behavior before relying on it; if it doesn't, write a one-line console command or a migration that deletes the row directly). No schema changes.
- **No `docs/schema/` updates expected** — the only schema-adjacent change is the placeholder widget's `widget_types` row removal, which is data not structure.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session is pure-PHP — no Vue, no SCSS. No build expected.**
- **Tests for new behaviour:** every session that changes application behaviour should include tests. Page-as-View gets one new IsView interface assertion (one or two cases on a new `tests/Unit/Models/PageIsViewAdapterTest.php` — or a Pest dataset case folded into an existing Page test, depending on what's cleanest). Placeholder removal *deletes* tests rather than adding them; the test count goes down. Track doc closure is documentation-only.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** no Playwright work expected this session. If any e2e spec touched the placeholder widget specifically (it doesn't, per grep), update accordingly.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents. **238 candidate slots (locked in at session-237 prompt-drafting time):**

- **Importer→widget status mismatch fix.** Donations importer's `mapDonationStatus()` writes `'completed'` for `active`/`paid`/`succeeded`/blank input, falling outside the canonical four-value enum and outside the Recent Donations widget filter. Small, focused — one-line fix to emit `'active'` plus a one-time backfill. Surfaced during 234 manual testing; carried forward through 235/236/237.
- **Phase B — Admin gating generalization** from the Financial Data Origin track. Lifting the `! $record->stripe_id` action-visibility pattern from `TransactionResource` into a shared trait keyed off `source`. Lands when a Refund/Cancel/Sync action surface is imminent — not yet, so this is conditional on what 238 brings.
- **Code Review & Cleanup audit-then-apply pair** (the 205/206 pattern). With the Widget Primitive track substantially closed, a periodic codebase sweep is the natural next batch of work.
- **Notes Permissions & Permissions Audit** stub — deferred since session 198.
- **Other Beta-1 stubs** from the roadmap — Organizations Model Overhaul, Event Ticket Tiers, Theme Colors Refactor, Importer Source Presets, etc. The roadmap's prioritized list at `sessions/session-outlines.md` is the menu.

The user picks among these at 237 close.

- **Next session base prompt** (whatever 238 ends up being): copy `sessions/template-base-prompt.md` to `sessions/238. base-prompt.md`.
- **Next session prompt**: draft `sessions/238. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/237. Widget Primitive — Phase 6 (Page-as-View Adapter, Placeholder Sweep, Track Closure) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 237's row.
- **Update `sessions/session-outlines.md`** (the roadmap): collapse the "Widget Primitive — Remaining Phases" entry to indicate the track is substantially complete (carry-forwards: Forms widget retrofit + `PageContext` retirement, neither scheduled). Update the active-tracks block accordingly — the track may move from "active" to "substantially complete" framing.
- **Update the Widget Primitive track doc** at `sessions/tracks/widget-primitive.md`:
  - Bump status snapshot to "Phase 6 complete; track substantially complete."
  - Compress the Phase Retrospectives section into a single closure block summarizing Phases 1–6 in 5–10 lines (per-session detail stays in the matching session log files).
  - Move "Page-as-View — the elective bottom of the View stack" out of "Forward hooks" into Phase 6's compressed retrospective (it's no longer elective if 237 implements it; if 237 leaves it elective, document the deliberate retirement of the hook).
  - Drop "DashboardConfig → DashboardView retrofit" / "Phase 5d-4 if pursued" / "Phase 6" from the remaining-in-track list.
- **Archive the previous session**: move all files matching `sessions/236. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (the Page model + IsView adapter, the deleted placeholder widget folder + tests, the WidgetServiceProvider edit, any widget-count assertion bumps that go *down*, the log, the updated outlines, the compressed track doc, the completed-sessions index, the 238 base prompt + session prompt, and any files moved into `sessions/archived/`), commit on the current `session-237/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
