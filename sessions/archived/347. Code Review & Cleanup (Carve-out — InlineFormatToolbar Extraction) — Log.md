# Session 347 — Code Review & Cleanup (Carve-out — InlineFormatToolbar Extraction) — Log

**Date:** 2026-06-07
**Branch:** session-347/1

---

## What was built

Behaviour-preserving, admin-only legibility split of `resources/js/page-builder-vue/components/InlineFormatToolbar.vue` — the dominant Vue outlier (1768 LOC) flagged W4b/W11 at the 344 audit. **1768 → 1071 LOC (−697, −39%)**, into 4 composables + 3 popover sub-components, with the orchestrator keeping format-state computation + dispatch per the audit's seam map. The component runs only in the page-builder editor (never the public bundle), so the split is legibility-only — zero public-render impact.

### Phase 0 — Standing Playwright behaviour net (re-established)

- The toolbar had **no committed behaviour coverage**: the 306 ~20-case suite was a parked `describe.skip` spec deleted by the A005 Playwright-discipline pass, and the `design`-group parity guard covers rendered *widget* HTML, not the toolbar. Re-established it as a committed standing spec `tests/e2e/page-builder/inline-formatting-toolbar.spec.ts`, **green against the un-refactored code first** as the byte-for-byte baseline. Cases pin spec rules to extraction seams: positioning + outside-click dismiss (§B/§C), §C14 chrome suppression, bold persist (§F dispatch), heading via the text-style menu (§F1), color popover open/render/toggle (§H), link save href+target+rel (§G/§6.2), and (added with the keyboard composable) Cmd/Ctrl+K → link popover (§F4.2).

### Phase 1 — `useInlineToolbarPosition`

- Extracted the geometry seam — the §C bar flip/clamp placement, the anchored-popover positioning, and the §J responsive-collapse measure — into `composables/useInlineToolbarPosition.ts` (159 LOC). The orchestrator keeps the lifecycle wiring and calls into it. **1768 → 1659.**

### Phase 2a — `useInlineLinkPopover`

- The §G link flow (insert/edit, the site-page picker, the link-bounds walk, the Quill insert/format/remove mutations, the DOM target/rel write the Delta can't carry, the focus restore) into `composables/useInlineLinkPopover.ts`, taking the orchestrator's Quill helpers / open-popover state / anchoring / format-state recompute as injected deps. **1659 → 1456.**

### Phase 2b — popover sub-components

- The three active popovers → presentational sub-components under `components/inline-toolbar/`: `InlineTextStyleMenu.vue` (64), `InlineColorPopover.vue` (21), `InlineLinkPopover.vue` (183, a pure view bound to the link controller via `:ctl`). The orchestrator supplies the shared `.ift-popover` frame (position, data-attr, keydown) as fall-through attributes and owns dispatch via `@apply`/`@pick`. Dropped the dead `colorPickerRef` and the now-unused `Check` / `ColorPicker` imports. The dormant overflow menu (unreachable in the two-row layout) stays inline. **1456 → 1245.**

### Phase 3a — `useInlineMediaInsert` + `useInlineToolbarKeyboard`

- `composables/useInlineMediaInsert.ts` — the §F7 async image upload + heroicon embed + their shared error toast (owns its own `onBeforeUnmount` cleanup). `composables/useInlineToolbarKeyboard.ts` — the §K roving-tabindex registry + Alt+F10 / Cmd+K, self-binding its window listener. Colour (§H) stays in the orchestrator — it is format dispatch. **1245 → 1090.**

### Phase 3b — dead-code cleanup

- Removed the dead `restoreRange` helper (a 306-era orphan, zero call sites, present on `main`). **1090 → 1071.**

Final shape: orchestrator 1071 LOC (format-state compute + dispatch + lifecycle + the bar) + `useInlineToolbarPosition` / `useInlineLinkPopover` / `useInlineMediaInsert` / `useInlineToolbarKeyboard` + `InlineTextStyleMenu` / `InlineColorPopover` / `InlineLinkPopover`. Six commits on `session-347/1`.

---

## Security considerations

Admin-only editor-surface refactor — the toolbar never loads on the public site, so it carries no public-facing attack surface. No security boundary changed: the link popover's `target`/`rel` DOM write and the rich-text → `HtmlSanitizer` persistence path were relocated into `useInlineLinkPopover` byte-for-byte, not altered. No new untrusted-input → write paths. None beyond standard convention.

---

## Schema changes

None. Vue-only — no migration, no PHP.

---

## Production deployment notes

None beyond the standard front-end build the deploy pipeline already runs (the Vue/TS source compiles through Vite). No widget-asset rebuild — no widget SCSS was touched.

---

## Deferred decisions

- **Stopped at 1071 LOC rather than pushing "well under 1k."** The owner accepted this as the clean, low-risk stopping point. Further reduction would require lifting format dispatch (the audit says keep it in the orchestrator), the risky bar-button DRY (tied to the roving-tabindex DOM registry, not covered by the spec), or extracting the dormant overflow menu (plumbing for unreachable code) — all declined.
- **The `useInlineMediaInsert` image-upload + heroicon paths are not covered by the standing spec** (would need upload-endpoint mocking). They are a faithful, build-verified relocation; left to manual/visual verification.

---

## Test result

The behaviour net for this Vue-only session (Pest does not exercise Vue): the standing `inline-formatting-toolbar.spec.ts` Playwright spec **7 passed, 0 failed**; the `design`-group HTML-parity Pest guard **259 passed, 0 failed**; `npm run build` clean. Fast Pest carries **2816 / 0** from the 346 close, unchanged (no PHP surface touched; not re-run this session).
