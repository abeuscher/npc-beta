# Base Prompt — Session 219

We are about to begin a new session: **219. Widget Primitive — Phase 4d (BarChart Retrofit)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session adds no new tables and modifies no existing ones; no `docs/schema/` updates are expected.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise. Pay attention to the "Widgets do not receive raw Eloquent models" section. BarChart is the fourth widget being brought under that invariant, and the second `SOURCE_WIDGET_CONTENT_TYPE` lock — but unlike Carousel (audit), this is a true retrofit: BarChart has no `dataContract()` today and its template walks `$collectionData['data']` directly.
   - `sessions/widget-primitive-migration.md` — the migration sketch. **Phase 4** ("Retrofit existing widgets onto contracts") is the active phase; BarChart is the first widget where `dataContract()` builds its `fields` list dynamically from user config (`x_field` and `y_field`), confirming the contract language scales to per-instance shapes. Pay specific attention to the design-decisions-locked-in bullet that says *"Capability grain varies by source. Fail-closed per-field discipline applies where the data is sensitive and the relationships are walkable. SOURCE_SYSTEM_MODEL **and SOURCE_WIDGET_CONTENT_TYPE** keep per-field declaration"* — for BarChart, "per-field declaration" means "the two fields the admin configured for X and Y," not a fixed list.
4. Read the three prior Phase 4 session logs to understand the audit + test-lock + cleanup pattern this session repeats — and how 218 settled the demo-data flow as "contract-locked widgets do not declare slots":
   - `sessions/archived/216. Widget Primitive — Phase 4a (BlogListing Retrofit — Audit, Test-Lock, Cleanup) — Log.md` — original audit/test-lock/cleanup playbook.
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — true retrofit with template rewrite (BarChart resembles this shape).
   - `sessions/archived/218. Widget Primitive — Phase 4c (Carousel Retrofit — Audit, Test-Lock, Cleanup) — Log.md` — first SWCT lock; established the system rule that contract-locked widgets do not declare `collections()` (and therefore admin preview shows the unmounted state for empty/unconfigured widgets, with demo data flowing only through `DemoSeeder` for dev/seeded paths).
5. Read BarChart as it stands today — this is a **true retrofit**, not an audit. BarChart has no contract, walks the legacy `$collectionData` path, and consumes user-picked Collection fields by name:
   - `app/Widgets/BarChart/BarChartDefinition.php` — no `dataContract()` method. Declares `collections(): ['data']`, requires `collection_handle`, `x_field`, `y_field` in `requiredConfig`.
   - `app/Widgets/BarChart/template.blade.php` — line 9 reads `$collectionData['data'] ?? []`. Lines 12–15 iterate items and access fields by user-configured names: `$item[$xField]` and `$item[$yField]`. Empty-items guard at line 26 returns the unmounted state.
   - `app/Widgets/BarChart/DemoSeeder.php` — creates `chart-demo` Collection with two fields (`label`, `value`) and ten data points. `accepted_sources` includes `Source::DEMO`. The dev demo route at `/dev/widgets/bar_chart` runs this seeder and renders against the real seeded Collection (no fallback path involved).
6. Read the contract resolution path BarChart will route through after the retrofit:
   - `app/WidgetPrimitive/ContractResolver.php::resolveWidgetContentType()` — same path Carousel uses today. Looks up Collection by `resourceHandle`, pulls items with `media` eager-loaded, projects through `WidgetContentTypeProjector::project()`. The fallback half (`projectFallback`) will not fire for BarChart in production because contract-locked widgets do not declare slots and `WidgetPreviewRenderer` therefore supplies no fallback rows (per 218's settled flow).
   - `app/WidgetPrimitive/Projectors/WidgetContentTypeProjector.php::projectRow()` — strips undeclared fields. BarChart's `dataContract()` will declare `fields: [$xField, $yField]` (filtered for non-empty), so the projector will emit rows with exactly those two keys plus the `_media` map (which will be empty since BarChart's content type declares no image fields).
7. Read the legacy data path BarChart uses today and that the retrofit retires (template-side):
   - `app/Services/WidgetRenderer.php` line 46 — the legacy `foreach ($widgetType->collections ?? [])` loop. After 219, BarChart's `widgetType->collections` is `[]` (the seeder writes whatever `BarChartDefinition::collections()` returns), so this loop iterates 0 times for BarChart, same as the post-218 Carousel state.
   - `app/Services/WidgetDataResolver.php::resolveCustom()` — the path `$collectionData['data']` came from before. After 219, BarChart no longer routes through it.
8. Read existing BarChart-adjacent test coverage:
   - Grep `tests/**/*` for `bar_chart` and `BarChart` — list every test that references BarChart. Establish baseline coverage before adding the retrofit-lock pair.
   - `tests/Feature/AppearanceLayerRegressionTest.php` — covers BarChart appearance rendering.
   - `tests/Feature/PageBuilderApiTest.php` — **two tests landed in 218 use BarChart's `'data'` slot for the legacy slot-strip behavior** ("strips query_config slots that are not declared on the widget type" and "update endpoint contract that the JS selective merge depends on"). After 219 removes `BarChartDefinition::collections()`, these will need to swap to BoardMembers (`'members'`) or LogoGarden (`'logos'`). Mechanical sibling-test fallout — same pattern as 218's Carousel-to-BarChart swap.
   - `tests/Feature/CarouselContractRetrofitTest.php`, `tests/Feature/BlogListingContractRetrofitTest.php`, `tests/Feature/EventsListingContractRetrofitTest.php` — the shapes to mirror for the new `BarChartContractRetrofitTest`. Test A asserts the contract-declared key set on each projected row; Test B asserts query-count discipline (one collections + one collection_items + one media select).
9. Read `resources/docs/widget-types.md` and `resources/docs/widget-development.md` — confirm whether any prose still references BarChart's `collections()` slot or the `$collectionData['data']` template path. Edit conservatively if so.
10. Open `sessions/219. Widget Primitive — Phase 4d (BarChart Retrofit).md` and read the session prompt carefully.
11. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216, 217, and 218. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. The admin bundle, the public site CSS, and the page-builder Vue app all compile through Vite, so this one command covers all three surfaces.
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. This rebuilds the public widget asset bundle on the build server and regenerates `public/build/widgets/manifest.json` (which the admin panel and public layout both read).
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/220. base-prompt.md`. Expected next session: continue Phase 4 retrofit. Plausible candidates after BarChart: (a) BoardMembers or LogoGarden — the two remaining `SOURCE_WIDGET_CONTENT_TYPE` retrofit candidates, similar shape to BarChart but with simpler dataContract declarations (no per-instance field selection); (b) a system-model retrofit — BlogPager (post-shaped, projector-locked already) is the cheapest pick, EventCalendar / EventDescription / EventRegistration share the Event projector landed in 217. Scope to be agreed with the user.
- **Next session prompt**: draft `sessions/220. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/219. Widget Primitive — Phase 4d (BarChart Retrofit) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: under the Widget Primitive track stub, mark Phase 4d complete (BarChart retrofit-locked: contract added, template rewired to `$widgetData['items']`, legacy `collections()` removed, test-lock pair landed, sibling tests retargeted). Note that Phase 4 retrofit continues, with ~16 page-builder widgets remaining (refined estimate after 219: ~11 are genuine retrofit candidates; the rest are pure-config widgets that need no contract).
- **Archive the previous session**: move all files matching `sessions/218. *.md` into `sessions/archived/`. This includes the previous session's base prompt, session prompt, and log. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-219/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
