# Base Prompt — Session 235

We are about to begin a new session: **235. Widget Primitive — Phase 5e (Permissions in the Contract Layer)**.

The Phase 5d arc landed three concrete record-detail widgets (Recent Notes, Membership Status, Recent Donations) and with them three structurally identical inline `Gate::denies` permission checks at the head of `resolveNote` / `resolveMembershipOne` / `resolveDonationList`. Comparison at 234's close-gate found all three arms reduce to "single permission name → fail-closed return that's a function of cardinality alone." Phase 5e lifts that pattern into a declared `requiredPermission` field on `DataContract` and centralizes enforcement at the resolver dispatch level — turning permissions into a declarative artifact alongside fields/filters/source/cardinality, and bringing the contract layer into line with the track's "concentrated enforcement at one boundary" stance.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top.
3. Read `sessions/tracks/widget-primitive.md` — this is the active track for this session. The status snapshot, the `Phase 5` sequence, the `Phase Retrospectives` for 5a / 5b / 5c / 5c.5 / 5d-1 / 5d-2 / 5d-3, the `Pending paradigm questions` section ("Permissions in the contract layer"), and the `Design decisions locked in` section are all directly load-bearing.
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. No schema docs read against this session — pure code-layer refactor consuming the existing `permissions` / Spatie infrastructure already in place.
5. Read the most recent session logs that established the per-arm-gate pattern this session lifts:
   - `sessions/archived/231. Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget) — Log.md` — first evidence point of the per-arm `Gate::denies('view_note')` pattern.
   - `sessions/archived/232. Widget Primitive — Phase 5d-2 (Membership Status — Single-Row Record-Detail Widget) — Log.md` — second evidence point (`view_membership`); first single-row contract on a record-detail Slot.
   - `sessions/234. Widget Primitive — Phase 5d-3 (Recent Donations — Third Concrete Record-Detail Widget) — Log.md` — third evidence point (`view_donation`); the forcing function for this session.
6. Read the full state of every file this session edits or creates:
   - **Resolver + contract (extend, lift):** `app/WidgetPrimitive/DataContract.php` (gain a `?string $requiredPermission` constructor parameter), `app/WidgetPrimitive/ContractResolver.php` (add a top-of-`resolve()` enforcement gate; lift the three inline gates from `resolveNote` / `resolveMembershipOne` / `resolveDonationList`).
   - **Three widget definitions whose contracts gain the field:** `app/Widgets/RecentNotes/RecentNotesDefinition.php`, `app/Widgets/MembershipStatus/MembershipStatusDefinition.php`, `app/Widgets/RecentDonations/RecentDonationsDefinition.php`.
   - **Existing tests that exercise the per-arm gates:** `tests/Unit/WidgetPrimitive/ContractResolverNoteTest.php`, `tests/Unit/WidgetPrimitive/ContractResolverMembershipTest.php`, `tests/Unit/WidgetPrimitive/ContractResolverDonationTest.php`. Each carries a `lacks view_X` test case that should remain green byte-equivalent after the refactor — the visible behavior is unchanged.
   - **Existing widget definition tests** (assert contract shape — will need an additional assertion for the new field): `tests/Unit/Widgets/RecentNotesDefinitionTest.php`, `tests/Unit/Widgets/MembershipStatusDefinitionTest.php`, `tests/Unit/Widgets/RecentDonationsDefinitionTest.php`.
7. Open `sessions/235. Widget Primitive — Phase 5e (Permissions in the Contract Layer).md` and read the session prompt carefully.
8. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. **No migrations are expected this session — pure resolver/contract refactor.**
- **Update `docs/schema/`** after writing any migration — N/A this session.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session touches no front-end source — pure PHP refactor in the contract / resolver layer. Neither `npm run build` nor `build:public` should be required.**
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session's change is partly internal (the centralization mechanism) and partly load-bearing (the gate must still fail-closed). New tests cover the centralized enforcement path; the existing per-arm gate tests stay as evidence the visible behavior is unchanged.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents. **236 candidate slots (locked in at session-235 prompt-drafting time):**

- **DashboardConfig → DashboardView retrofit.** Cheap follow-up named in the track doc as scheduled-ad-hoc post-5d. Convert `DashboardConfig` (session 215) to a `DashboardView` model implementing `IsView`. Brings dashboards into the unified View vocabulary. Small surface; Eloquent overlay vs rename-to-`dashboard_views` table is the call. **Lean candidate** — narrow, well-scoped, ready-to-go.
- **Phase 5d-4 — Recent Activity widget**, if pursued. Aggregates across multiple system models (notes, memberships, donations, event registrations, transactions) — would be its own design surface and a fourth permission-gate evidence point that Phase 5e's grammar can be stress-tested against. Defer unless the design conversation at 235 close surfaces a need.
- **Phase B — Admin gating generalization** from the Financial Data Origin track. Lifting the `! $record->stripe_id` action-visibility pattern from `TransactionResource` into a shared trait keyed off `source`. Lands when a Refund/Cancel/Sync action surface is imminent — not yet.
- **Importer→widget status mismatch fix** (surfaced during 234 manual testing). Donations importer's `mapDonationStatus()` writes `'completed'` for `active`/`paid`/`succeeded`/blank, which falls outside the canonical four-value enum (`pending`/`active`/`cancelled`/`past_due`) and outside the Recent Donations widget filter. Small, focused — could pair with another small session if needed.

The user picks among these at 235 close.

- **Next session base prompt** (whatever 236 ends up being): copy `sessions/template-base-prompt.md` to `sessions/236. base-prompt.md`.
- **Next session prompt**: draft `sessions/236. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/235. Widget Primitive — Phase 5e (Permissions in the Contract Layer) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 235's row.
- **Update `sessions/session-outlines.md`** (the roadmap): collapse references to 5e to reflect it as complete. Adjust the active-tracks block accordingly.
- **Update the Widget Primitive track doc** at `sessions/tracks/widget-primitive.md`: bump the status snapshot (last update date, what's complete, what's active). Move the "Permissions in the contract layer" entry out of `Pending paradigm questions` into the `Phase Retrospectives` section as a Phase 5e retrospective. Per the close-gate phase-expiry rule, **if the per-session detail of 5d-1/5d-2/5d-3 has not yet been compressed** (it remained uncompressed at 234 close because Phase 5e itself was queued), compress those three sessions into a Phase 5d retrospective at this session's close.
- **Archive the previous session**: move all files matching `sessions/234. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the resolver/contract edits, the three widget definition edits, the test additions, the log, the updated outlines, the track doc, the completed-sessions index, the 236 base prompt + session prompt, and any files moved into `sessions/archived/`), commit on the current `session-235/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
