# 395. Member Portal Packaged & Extracted

The D5 block closes in one combined session (decision 8, resolved at the 394 close — the D4/393 compression precedent): `nonprofitcrm/member-portal` becomes the sixth path-repo package, the two portal tables leave core's dump for plugin-owned migrations (the fourth squash-boundary redraw — the second with a sequence), the portal-absent composition-safety hunt runs against the identity check, and the sixth extraction-runbook run moves the plugin to `crm-plugin--member-portal`, emptying `plugins/` a fourth time.

---

## Stub reference

Arc position D5 per `sessions/plugin-architecture-plan.md` § Stage D — the block's closing session. The 394 log's **block handoff** section is the kickoff input; this prompt front-loads it.

---

## The redraw input (ground truth, captured from the dump at the 394 close)

1. **`portal_accounts`** (dump ~lines 1039–1053): bigint PK on `portal_accounts_id_seq` — **the sequence travels complete** (`CREATE SEQUENCE` ~1059, `OWNED BY` ~1071, `ALTER COLUMN id SET DEFAULT nextval` ~1574 — the `donation_receipts_id_seq` mechanics, 390). Columns: `id`, `contact_id` (uuid, nullable), `email`, `password`, `email_verified_at`, `remember_token`, `created_at`, `updated_at`, `is_active` (boolean default true NOT NULL). Constraints: PK (~2073), **unique on email** (~2065). Index: `portal_accounts_contact_id_index` (~2615). One outbound FK: `contact_id → contacts(id) ON DELETE SET NULL` (~3052) — **no RESTRICT-as-behavior case this time** (SET NULL orphans the account row on contact force-delete; existing behavior, travels as-is).
2. **`portal_password_reset_tokens`** (~1078): `email` (PK, ~2082), `token`, `created_at`. No FKs, no sequence, no secondary indexes.
3. **Creation order:** `portal_accounts` → `portal_password_reset_tokens` (no inter-table dependency; keep dump order). **Zero inbound FKs from core tables** (survey finding — verify with a dump grep before authoring).
4. **Raw `DB::unprepared` DDL verbatim from the dump** (surface 5 — pg_dump round-trips its own output; the identity check is the proof).

---

## The portal-absent composition-safety survey (the hunt's candidate list)

The five core `auth('portal')` **guard** reads were gated at 394 and are proven by the removal mirror — this session's hunt is the portal-***table*** reads and the seeded topology, which only become reachable failures once the schema is plugin-owned. Candidates, in likelihood order:

1. **`ContactResource`'s Portal Access section** (~line 135) — `PortalAccount::where(...)` runs when the contact edit form builds. Portal-absent: crashes on the missing table on **every contact edit screen**. Disposition: gate the whole section on `present('member-portal')` (render a nothing-state or omit the section — the section is meaningless without a portal).
2. **`EditContact`'s four portal actions** (grant ~216, suspend ~257, reactivate ~274, mark-verified ~290) — the `hidden()` closures read `$this->record->portalAccount` at page render (lazy-load fires the query). Same disposition: hide all four on capability absence.
3. **`DatabaseSeeder`'s unconditional `PortalPageSeeder` + `SystemPageSeeder` calls** (~185–186) — neither touches a portal table (pages/widgets only, and `seedWidget` already skips missing widget types silently), so the identity check may not *crash* here; the question is **topology**: should a portal-absent install seed `/members`, `/members/account`, `/members/event-registrations` (type `member` — 404 shells after the 394 gate) and the three `/system/*` auth pages (empty of their widgets)? Disposition: gate both calls on `present('member-portal')` (the `MembershipTierSeeder` gate precedent, ~179 — note that one keys on route presence; use the capability here and leave the memberships gate as-is). **The identity check arbitrates** — check (b)'s smoke list must then expect 404s for those slugs instead of 200s.
4. **`DemoPortalMemberSeeder`** — writes `PortalAccount` rows (demo data for the portal widgets' thumbnail context). Its invokers are dev/demo surfaces; portal-absent it would crash on the missing table. Disposition: check invokers at session; likeliest is registry/capability-gating at the call site or an early-return in the seeder.
5. **`Contact::portalAccount` relation** — lazy HasOne, definition stays (models are core); confirm no core surface eager-loads it unconditionally (`grep -rn "with('portalAccount')\|portalAccount" app/` minus the items above).
6. **Already safe (verify, don't re-gate):** `FormSubmissionController`'s `PortalAccount` query sits inside the 394 capability gate; `SystemModelProjector::projectPortalMember` only runs behind the gated arm; the three portal mailables' senders are gated or plugin-side.

Untripped candidates and admin/dev-only reaches that check (b) does not exercise go to **recorded debt** (the 383/390/393 discipline) — no speculative gates.

---

## Front-loaded decisions

1. **Block shape: one combined session** (resolved at 394 close). Package + redraw + hunt + extract, phases A→C below. If the hunt blows past its survey mid-session (multiple unsurveyed trips), the honest stopping point is after Phase B (package + redraw landed, identity checks clean) — extraction slides to a short closer; decide-and-log, no pause needed.
2. **Package name `nonprofitcrm/member-portal`**, explicit `version: 0.1.0`, P6 mechanics verbatim. Manifest entry `folder` → `path` same-commit with the lock (the 386 guard interlock).
3. **Fixtures gain migrator paths vendor-relative directly** (`vendor/nonprofitcrm/member-portal/database/migrations`) — the 393 same-session compression nuance: the path-repo symlink makes the vendor path valid from the package step, extraction follows in-session, only the final form ships. The expiring-caveat docblocks are replaced with the standard wording (the `MembershipsRemovedTestCase` text).
4. **The capability is the gate signal for every new gate** (`app(CapabilityRegistry::class)->present('member-portal')`) — the 394 precedent. Route presence stays the signal only where it already ships (the memberships gates).
5. **New schema-twin tests** (the 393 pair shape): `MemberPortalSchemaSession395Test` (tables exist on the full composition; migrator path registered; a portal flow writes through the plugin-owned schema) and `MemberPortalSchemaRemovalSession395Test` (tables kept via the fixture path — data-kept assertions against plugin-owned schema; the gated admin section renders a portal-less contact edit screen clean).
6. **Extraction is the runbook, sixth run:** owner-created public `crm-plugin--member-portal`; `version` field dropped in the traveling copy; the two `script.js` asset declarations rewritten vendor-relative; grep the traveling tests for in-plugin path assertions and re-point (the 391/393 step-3 nuance — the enabled twin's view-namespace assertions are path-free, but verify); README on the Memberships model (contract 0.19.0, tags-are-releases, tests-travel); tag `v0.1.0`; core swap + `git rm -r plugins/MemberPortal` + lock pin + Dockerfile COPY-line removal + phpunit re-point to `vendor/nonprofitcrm/member-portal/tests`; docs re-points (`docs/app-reference.md` portal controller rows → vendor path wording, `docs/runbooks/permission-matrix.md` line 291).
7. **Schema docs:** ownership headers on `docs/schema/portal_accounts.md` + `docs/schema/portal_password_reset_tokens.md` (the 393 pattern); guard owner-map +2 tables; the standing guard pins both tables out of the core dump.
8. **Contract doc at close: 0.18.0 → 0.19.0** — Package shape sixth consumer; surface 5 fourth redraw consumer (second sequence-carrying; the SET-NULL-FK note — travels as plain referential behavior, no net required; the topology-seeder gate note); External repo sixth consumer; changelog. Plan doc D5 ✅ block closed, arc header advanced to 396 / D6.
9. **D6 kickoff (Blog/Posts) surveyed at close** — the next block opener's survey captured from live code into the 396 prompts (the 393→394 pattern): the posts/blog surface (PostController, the blog widgets, `posts` table ownership, the `blog_prefix` routes).

---

## Open questions to resolve at session start

1. The owner has created public `crm-plugin--member-portal` (blocks Phase C only).
2. Sibling checkouts current (`git pull` ×5); no sibling edit expected this session (the Events rider shipped at 394).
3. Verify zero inbound FKs into the two portal tables with a dump grep before authoring the migrations (the survey says none; the dump is ground truth).

---

## Phases

### Phase A — Package + squash redraw

P6 mechanics (decision 2); then the redraw per the input above: author the two raw-DDL migrations, drop the tables from the dump against a scratch database, `loadMigrationsFrom` in `boot()`, fixtures gain vendor-relative migrator paths (decision 3), schema docs + guard rows (decision 7), the schema-twin tests (decision 5). **Identity check (a)** — full composition byte-identical modulo the two migrations rows + sequence + dump token, seeded-data spot counts exact.

### Phase B — The portal-absent hunt

**Identity check (b)** — fresh install under `PLUGINS_DISABLED=member-portal`: boots, seeds, smokes (public pages 200, the portal slugs 404 post-topology-gate, `/admin/login` 200, zero portal tables). Fix what it trips per the survey (capability gates — decisions in the survey list); untripped candidates → recorded debt. The contact-admin gates get their removal-mirror coverage (decision 5's second test).

### Phase C — Extraction (the runbook, sixth run)

Decision 6 end to end; gate 4 re-run on the vendor-relative assets; in-image proof (`docker build --target app` — six packages VCS-resolved, `plugins/` absent); dev-stack smoke + the mark-as-applied step for the walked dev database (the 390/393 deployment-notes pattern for existing databases).

---

## Out of scope (binding)

- Model/factory moves (`PortalAccount` stays core — § 6.7); the two core portal mailables stay core (the 394 disposition).
- The `portal_password_reset` handle migration (follows the model, D12 territory — contract 0.18.0 wording).
- Portal-vertical feature work; per-plugin CI (D11); root-PSR-4 work (D12); any FM surface (CRM contract stays v2.7.0).
- D6 (Blog/Posts) work beyond the close-time survey capture.
- Adjacent findings → housekeeping inbox, one line, move on.

---

## Testing

- **Slow test groups to run this session:** none locally (CI runs them).
- **New tests expected:** yes — the schema twin pair (decision 5); fixture migrator paths added; existing assertions unchanged beyond those re-points. Expected delta: additive.
- **Front-end:** gate 4 at extraction (`widgets:sync` + `build:public` + bundle proof); no `npm run build`.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific:

- **Log file:** `sessions/395. Member Portal Packaged & Extracted — Log.md` — include the **D6 kickoff input** section (the Blog/Posts survey, decision 9).
- **Branch:** `session-395/N`; VERSION `0.395.<N>` at close.
- **Boundary: NO** (FM contract untouched at v2.7.0; plugin contract → 0.19.0).
- **Production deployment notes** must carry the mark-as-applied step for existing databases (the two new migration rows vs. already-present tables — the 390/393 pattern).
