# 066. Public API Endpoints

Read-only public API: event calendar feed and blog roll at minimum. Discuss authentication model (none vs API key), response format (JSON), and rate limiting at session start. Scope conservatively — this is a utility endpoint, not a full API.
