# Base Prompt — Session 283

We are about to begin a new session: **283. Stripe Checkout Branding**.

This is **execution-order position 33** in `sessions/release-plan.md` — **E4 Stripe Checkout Branding**, the **first session of the Public Website Complete milestone** (Rule 12, lifted at session 282 audit). All pre-milestone work front-loads here; non-public-website work waits below the `── PUBLIC WEBSITE COMPLETE ──` divider in the execution-order list.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** feature-style. Audit current Stripe-session-creation patterns across the four checkout controllers, lift a shared helper if they diverge, ship branding fields + admin copy-string config + a help doc + an Onboarding Checklist item for Stripe Dashboard branding.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/release-plan.md` § E4 — canonical for scope, success criterion, prerequisites, and artifact. Also read **Rule 12** (the new Public Website Complete milestone rule) and the **"Public Website Complete — milestone work"** sub-section of the execution-order list to understand the current sequencing.
3. Read `sessions/session-outlines.md` Active-tracks block and the **Public Website Complete milestone** bullet in the roadmap intro.
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings.
5. Read `docs/schema/README.md` and the `site_settings.md` schema file (the new checkout-copy strings will land there).
6. Read the four checkout-creating controllers — `DonationCheckoutController`, `EventRegistrationController`, `ProductCheckoutController`, `MembershipCheckoutController` — to assess whether they diverge on Stripe-session-creation patterns. The shared-helper decision lands here.
7. Read `sessions/archived/249. Onboarding-Install Dashboard Widget — Log.md` (if archived) or grep for the Onboarding Checklist implementation to understand the band/item structure for the new "Configure Stripe Dashboard branding" item.
8. Open `sessions/283. Stripe Checkout Branding.md` and read the session prompt carefully.
9. Note any drift between the session prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

---

## Starting state inherited from 282

- **CRM contract at v2.3.0** — unchanged. Session 282 was a Phase C + D audit + planning-doc cleanup; no boundary-touching work and no schema changes.
- **Schema baseline:** no schema changes since 280-close.
- **Fast test suite green at 280-close baseline (2371/0 sequential)** — confirm at session start. Session 281 was scheduled but never executed; session 282 did pure documentation work with no test additions. The parallel-runner flake is carried.
- **Playwright suite** — baseline preserved from 280-close. No Playwright in this session (Stripe Checkout pages are third-party-hosted; not a stable test target).
- **Release plan refit at 282 audit:** Phase C significantly reshuffled. Phase A items (A2–A5) moved post-milestone. The Public Website Complete milestone (Rule 12) is new. This session is the first pre-milestone entry.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — feature-style sessions per Rule 2 do **not** absorb housekeeping items. Touch this only if a finding directly tied to the four checkout flows surfaces; otherwise carry forward to a batch session (E12 / E15-17).

---

## Testing-protocol transition note

Runner experiment remains not active. Draft a test-handoff sketch into the close-gate commit body at session close.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. *Session-283 note: Stripe is already integrated. Confirm the existing patterns before lifting any new abstraction.*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** "Should I lift a shared helper here" is a question for the user when the answer isn't obvious from the surrounding code, not something to commit to and unwind later.
- **Adapt to drift; don't ask about it.** Small inaccuracies in field names, payload shapes, method signatures are expected. Default to silent adaptation + a one-line work-log note. Only flag drift when it requires a decision.
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that are expensive to reverse (data model shape, new abstractions that propagate, framework patterns). Local decisions with an obvious answer — just decide.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. *Session-283 note: this session may not need migrations if SiteSetting handles the new copy strings (SiteSetting is key-value; no schema change required to add new keys).*
- **Update `docs/schema/`** after writing any migration. (Likely a no-op this session per the above.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 280-close baseline + new E4 tests.
- **Front-end builds.** If the session adds anything to admin SCSS/JS (the new settings-page section, the Onboarding Checklist item) → `npm run build`. No `build:public` expected unless a widget surface changes.
- **Tests for new behaviour:** every controller change ships Pest coverage asserting the new fields reach `Stripe\Checkout\Session::create`. Follow existing Stripe-client mock conventions.
- **Fast vs slow classification:** new tests stay in the fast suite (Stripe client is mocked; tests are fast).
- **Playwright artifact hygiene:** N/A — no Playwright this session.
- **Cross-repo coordination — Fleet Manager.** N/A — no FM surface touched.
- **Verify objective outcomes yourself; pull the user in for judgment.** Field-passing to `Session::create` is objectively verifiable via Pest with mocked client — close that loop yourself. The visual result on Stripe's hosted checkout pages requires manual operator-side verification (operator follows the help doc, sets Dashboard config, runs a real test transaction) — that's a user-judgment surface, surface it at session-end for manual testing.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that the API-side work is verified by Pest; the visual branding result requires the operator to (a) configure Stripe Dashboard per the new help doc and (b) run a test transaction to see the result. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/284. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to the next execution-order entry (per current plan: **E5 Mobile Type Scaling**, position 34).
- **Next session prompt** (`sessions/284. Mobile Type Scaling.md`) — draft against the E5 release-plan entry and the relevant `session-outlines.md` stub.

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/283. Stripe Checkout Branding — Log.md` using `template-session-log.md` as the format reference. Document: shared-helper decision + rationale, per-controller field additions, SiteSetting copy keys added, new admin settings-page section, new Onboarding Checklist item, new help doc, Pest test count delta, manual-testing observations from the operator's Stripe Dashboard run.
- **Update `sessions/completed-sessions.md`:** append session 283's row.
- **Update `sessions/session-outlines.md`:** add "Stripe Checkout Branding ✅" closed entry; mark E4 forward stub (if present) as resolved.
- **Update `sessions/release-plan.md`:** mark E4 ✅ closed at session 283. Update execution-order list entry at position 33.
- **Archive the previous session:** move all `sessions/282. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-283/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Shared StripeCheckoutBuilder + custom_text + statement descriptors across four checkout controllers".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Help doc + Onboarding Checklist item** are the user-facing pieces — make sure the help-doc copy is operator-friendly (PM level, not engineer level) and the checklist item's "Configure →" link goes to the right Stripe Dashboard URL.
