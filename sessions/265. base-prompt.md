# Base Prompt — Session 265

We are about to begin a new session: **265. Donation Credits — Soft-Credit Layer**.

This session lifts the soft-credit half of working-set entry **B1b** (Affiliations Junction & Soft-Credit Layer) per the post-263 execution-order revision. The structural half (affiliations + Org gap-fill + Contact-admin repeater + Org-side panel) shipped at session 264. With B1b's checkmark dropping at this session's close, Track B's Beta-1 entries (B1a / B1b / B1c / B2 / B2a / B2b / B2b') are all complete — the next session in the execution order is **E10 — Full-Width Architecture Enforcement**.

This session is **NOT boundary-touching** — no Fleet Manager contract change. CRM contract stays at v2.2.0 (per session 263).

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. Canonical format reference.
2. Read `sessions/release-plan.md`:
   - Track B § B1b — confirm the soft-credit half is what closes here; the structural half is annotated as "shipped at 264."
   - Execution-order list — confirm position 19 (B1b) closes at this session; the next entry is E10 at position 20 (or 21, depending on how 264's close updated the numbering).
3. Read `sessions/session-outlines.md` § "Organizations Model Overhaul" — the B1b stub there.
4. Read `sessions/264. Affiliations Junction — Schema, Migration, Admin UI — Log.md` end-to-end — context for what shipped 264-side, particularly the `Affiliation` model + Contact admin form repeater shape (this session mirrors the repeater shape on the Donation form for soft-credits, with the morph-target swap), the deletion-guard count update, and any in-session findings that reshape this session's plan.
5. Read `docs/schema/README.md`, then `docs/schema/donations.md`, `docs/schema/contacts.md`, `docs/schema/organizations.md`, and the new `docs/schema/affiliations.md` from 264.
6. Read `app/Models/Donation.php` end-to-end. Pay attention to existing relations, casts, custom-field schema integration, source-policy posture.
7. Read `app/Filament/Resources/DonationResource.php` plus its `Pages/` subdir end-to-end. The Donation admin form is where the soft-credit repeater lands. Pay attention to: existing form layout, post-264 contact-org binding (walks through affiliations now), validation patterns, custom-field schema integration.
8. Read the Contact "Donations" panel surface — wherever donations attributed to a contact are surfaced (likely under `app/Filament/Resources/ContactResource/Pages/...` or a partial referenced by ViewContact). After this session, the panel needs to also surface soft-credits received by the contact.
9. Read at least one existing polymorphic-relation example in the codebase end-to-end — confirms the `morphTo` naming convention 265's `donation_credits.attributable_type` declaration matches. The Notes polymorphic Timeline pattern from 255 (`app/Filament/Concerns/RecordTimelinePage.php` and the `Note` model's `notable` morph) is the canonical reference; Spatie Media Library is another. Pay attention to whether the codebase calls `Relation::enforceMorphMap()` anywhere — if so, this session registers Contact + Organization in the map.
10. Read `sessions/258. Onboarding Rehearsal Cluster — Log.md` § findings register — soft-credit was surfaced as a B1b component during B2; check whether any specific soft-credit shape requirements were lifted from real-world data scrub.
11. **Verify-at-start questions** the session prompt names but the implementation should resolve before going deep:
    - **`credit_pct` precision and shape.** `decimal(5, 2)` (0.00 to 999.99) covers >100% matching-gift cases. Confirm against existing `donations.amount` shape (likely `decimal(12, 2)`) for reporting cohesion. Pick before authoring the migration.
    - **Default `credit_pct` at the form layer.** When operator adds a row without specifying a value, default to 100 (common case: "this gift is fully soft-credited to this person"). Confirm.
    - **`credit_role` enum vs free-text.** The plan says free-text in v0, paralleling `affiliations.role` from 264. Confirm; lean free-text.
    - **Filament `MorphToSelect` availability.** The polymorphic-target Select primitive lives in `Filament\Forms\Components\MorphToSelect` for Filament 3. Confirm it's available in this codebase's version. If not (or if the version's behaviour is awkward), fall back to a `Select::make('attributable_type')` + dependent `Select::make('attributable_id')` two-step.
    - **Existing morph-map registration.** Check whether `app/Providers/AppServiceProvider.php` (or similar) calls `Relation::enforceMorphMap([...])`. If yes, register `'contact' => Contact::class` + `'organization' => Organization::class` (or whatever the convention names are). If no, default class-name persistence is fine for v0.
12. Open `sessions/265. Donation Credits — Soft-Credit Layer.md` and read the session prompt carefully.
13. Summarise your understanding and confirm you are ready to proceed.

---

## Starting state inherited from prior sessions

- **CRM contract at v2.2.0** (per session 263) — unchanged.
- **Fast test suite green** at the **264 close baseline** — the actual count lands at 264 close. Target growth this session: **~+12 to ~+18 net new tests** (model, polymorphic morph, repeater Livewire, soft-credit panel display, importer-not-touched assertion).
- **Slow test suite green** at **15 passed** (assumed unchanged at 264 close).
- **B1b structural half ✅ closed at 264** — affiliations table, `contacts.organization_id` dropped, Contact admin repeater, Org gap-fill (industry / EIN), deletion-guard count update, Org-side Affiliated Contacts panel. This session inherits that surface; the soft-credit repeater on the Donation form mirrors the affiliations repeater shape.
- **Importer Org-as-source sentinels (B1a)** unchanged at 264; this session does not touch them either.

---

## Process rules for every session

The base-prompt rules (process, close gate, style) in `sessions/template-base-prompt.md` apply. Session-specific notes:

- **CRM-side schema changes — yes.** New `donation_credits` table with polymorphic target. Update `docs/schema/donations.md` (referenced relations) and add `docs/schema/donation_credits.md`. Register the new table in `docs/schema/README.md`.
- **No Composer changes.**
- **No `npm run build` or `build:public`.** Models + migrations + admin forms; no frontend touches.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt. Particularly: the five verify-at-start questions in step 11 are decision points — surface answers before going deep.
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **Tests required for new behaviour.** Polymorphic morph relation, `credit_pct` >100% acceptance, repeater form save with both Contact + Organization targets, soft-credit display on Contact-side panel, soft-credit display on Org-side panel.
- **When implementation is complete**, run the fast test suite. **Target after this session: 264-close-baseline + ~12 to ~18 new tests.**
- **Verify objective outcomes yourself; pull the user in for judgment.**
  - **Objective:** schema migration runs cleanly; polymorphic relation loads in both directions; admin repeater saves with mixed-type targets; tests green.
  - **Judgment-required:** soft-credit display layout on the Contact panel (one combined chronological list with a "(soft-credit)" tag vs separate sections); whether the Donation list-view should expose any soft-credit indicator (lean: no, defer); the row-preview format on the collapsed soft-credit repeater.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and verification are complete, Claude stops and waits.

The natural next session after 265 is **E10 — Full-Width Architecture Enforcement** per the post-263 execution order (after the B1b split). E10 is a UI-architecture session — distinct shape from B1b. Plan to draft 266's prompt fresh against `sessions/session-outlines.md` § "Full-Width Architecture Enforcement" and `release-plan.md` § E10 at 265 close.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/265. Donation Credits — Soft-Credit Layer — Log.md`. Document the polymorphic shape, the `credit_pct` default decision, the morph-Select primitive choice (first-class `MorphToSelect` vs fallback), the soft-credit display layout decision, the test surface, and any in-session absorptions.
- **Update `sessions/completed-sessions.md`:** append session 265's row.
- **Update `sessions/release-plan.md`:**
  - **B1b ✅** at "closed at session 265 (structural half at 264; soft-credit half at 265)." Update the success criterion in place to reflect both halves shipped.
  - Confirm Track B's all-green status; the next entry in the execution order is E10.
- **Update `docs/schema/`:** ensure `donation_credits.md` is registered in `README.md` and `donations.md` references the new relation.
- **Findings register** — flag any surprises (morph-loading pitfalls, polymorphic-search UX gotchas, repeater + cross-type Select complications, morph-map registration discoveries).
- **Help-doc update** — none expected; soft-credit editing surfaces inline on the Donation admin form. Carry-forward if operator-facing UX warrants a note.
- **Archive the previous session:** move all files matching `sessions/264. *.md` into `sessions/archived/`.
- **Commit:** stage all changed files and commit on `session-265/N` branch. Notify the user. Do not push.
- **No FM-side handoff** — non-boundary-touching.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly. The Affiliation model + Contact admin repeater from 264 is the canonical reference for the new `DonationCredit` model + Donation admin repeater shape (with the morph-target swap). The Notes polymorphic Timeline pattern from 255 is the canonical reference for the morph relation declarations.
- Do not add features, refactor, or improve anything beyond what the session specifies. Specifically: do **not** add a `credit_pct` sum-validation rule (operator owns the invariant); do **not** add soft-credit-aware reporting / aggregation surfaces; do **not** add Donations-importer soft-credit support; do **not** thread soft-credit into the year-end statement / acknowledgment-loop logic (that's C4 territory if/when it lifts); do **not** add Org-Org soft-credit chains (Org-Org relationships are out-of-scope at B1b entirely).
- Prefer editing existing files to creating new ones — except for the new `DonationCredit` model + migration + (likely) a soft-credit repeater partial.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
