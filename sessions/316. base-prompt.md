# Base Prompt — Session 316 (Record-Duplicate Affordance)

**Start now.** Execute the reading list below, open the session prompt at `sessions/316. Record-Duplicate Affordance.md`, then stop at the Open Gate before beginning task work.

> Note: session 314's first housekeeping batch did spill leftover items into a second batch, so this prompt was bumped to session 316; the second housekeeping batch occupies session 315. Execution-order position is unchanged — record-duplicate is still position 44 (the E18 plan entry); only the session number moved.

---

We are about to begin a new session: **316. Record-Duplicate Affordance — Events / Pages / Posts / Products.**

This is **execution-order position 44** in `sessions/release-plan.md` — § E18 Record-duplicate affordance. Sequenced inside the Public Website Complete milestone block (Rule 12), promoted from the housekeeping inbox at session 313 close after the user noted "everything is in place for it." Touches four resources × two surfaces (list + record).

This session is **NOT boundary-touching** — no Fleet Manager surface in scope. CRM contract stays at v2.3.0. No schema changes expected; duplicate logic operates over existing models.

**Session shape:** plan-backed feature. Build a Duplicate action on Events, Pages, Posts, and Products at both the list level (bulk-style or per-row action) and the record-page level (header action on the edit view). Pattern should match the closest existing duplicate-style action in the codebase; if no per-record duplicate pattern exists yet, the first resource done sets the pattern for the other three.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). They are the canonical format reference — do not infer format from previous session logs.
2. Read `sessions/session-outlines.md` for active-tracks and any stub or open gap referencing record-duplication.
3. Read the `sessions/release-plan.md` entry at execution-order position 44 (§ E18) — canonical for scope.
4. Read `docs/app-reference.md` for the four target resources' Filament page paths; read `docs/schema/` for each of `events`, `pages`, `posts`, `products` to confirm column shapes (timestamps, slugs, relationships) before designing the copy semantics.
5. Open `sessions/316. Record-Duplicate Affordance.md` and read the session prompt.
6. Note any drift between the session prompt and the actual code in a brief work-log entry.

---

## Starting state inherited from session 315

- **CRM contract at v2.3.0** — unchanged through 313, 314, and 315 (all housekeeping-only).
- **Schema baseline:** no schema changes since session 308; 315 added none. Record-duplicate uses existing tables.
- **Fast Pest at 315 close:** 2613 passed (314 baseline 2594 + 19 from the five shipped Batch 2B items). Confirm at session start.
- **Playwright suite** running in CI on every push against the isolated stack. `npm run test:e2e` defaults to the isolated stack as of s313. A capture-buffer item (Playwright check for ordered/unordered-list rendering in the page builder) was folded into the housekeeping inbox at 315 close — not 316 scope.
- **Planning state:** E12 (Housekeeping Batch 2) **closed** at 315 — both batches done. E18 promoted at s313 close; placement at execution-order 44 between E15 (Table widget, prompt rewrite pending) and E19 (Accessibility). A new post-Beta stub landed in `session-outlines.md` at 315 close — *Content Export — Unified Options Modal* (CMS & Page Builder section), out of pre-Beta scope.
- **Relevant 315 precedent:** s315 introduced the `HasPageSecondaryActions` trait (`app/Filament/Resources/PageResource/Concerns/`) sharing a Filament header `ActionGroup` across `EditPage` and `EditPageDetails`. If record-duplicate adds record-level header actions across resources, that trait is the closest in-repo pattern for sharing an action without divergence.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this session does not absorb inbox items. Batch 2B left the link-colour consolidation, per-widget colour-picker defaults, kept widget hardcoded values, contact-hero readability annotation, and the `[test-integrity]` cluster for a future batch.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **First resource sets the pattern.** Implement Duplicate on whichever resource has the cleanest existing pattern to mirror (or the simplest model if there's no precedent). Land that iteration, get user sign-off on the pattern, then port to the remaining three resources in subsequent iterations.
- **Per-resource semantics get explicit walkthrough.** What carries forward on duplicate? Slug (almost certainly modified — append `-copy` or similar), timestamps (reset?), `created_by` (current user vs. original author?), relationships (which copy vs. which point at the same target?), media (re-use the same media records vs. duplicate them — almost certainly re-use). Resolve at session start, not mid-iteration.
- **List-level vs. record-level affordances both ship per resource.** Eight affordances total; commit message identifies which.
- **Tests per resource.** Each resource's duplicate path gets at least one feature test pinning the produced row's shape vs. the original.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`.
- **Questions are not instructions** per `feedback_questions_are_not_instructions`.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list above and the Starting state are read, surface a one-paragraph orientation — which resource the first iteration targets, the per-resource duplicate semantics that need user sign-off, the proposed iteration order across the remaining three resources — and wait for the user to confirm before starting. Do not pipeline straight from reading into implementation.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- Copy `sessions/template-base-prompt.md` to `sessions/317. base-prompt.md`.
- Draft `sessions/317. Accessibility Pass — Aria + Usability + Lighthouse.md` from `sessions/template-session-prompt.md`.

Then stop. The user reviews the work and the next session's drafts, and initiates close when ready.

### Phase 2 — Close

When the user says to close (after the next session's prompts are agreed):

- Write `sessions/316. Record-Duplicate Affordance — Log.md` from `sessions/template-session-log.md`. Per resource: pattern lifted, per-resource semantics resolved, tests added.
- Update `sessions/completed-sessions.md` with the 316 row.
- Update `sessions/release-plan.md` § E18 marking the entry closed.
- **Archive the previous session's files** per the archive rule.
- Commit on `session-316/N`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages name the resource + surface (e.g. "Pages — list + record duplicate"); no docstrings/comments on code you did not write; follow the existing Filament action patterns for visual + interaction consistency.
