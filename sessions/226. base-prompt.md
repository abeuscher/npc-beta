# Base Prompt — Session 226

We are about to begin a new session: **226. Date Display — Format Hints + Projector-Owned Assembly**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session does not add or remove any columns; no schema docs need updating.
3. Read the framing for this session — the **Date Display — Format Hints + Projector-Owned Assembly** stub in `sessions/session-outlines.md` (added at session 225 close, immediately after the Widget Primitive track stub). The stub captures the why and the architectural rule the session enforces; the session prompt assumes you have read it.
4. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — established the projector field-naming convention this session deliberately retires (`_label` suffix on column-named fields). The new convention is concept-named display fields (`event_date`, `event_time`, `post_date`).
   - `sessions/archived/221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer) — Log.md` — established the `querySettings` field on `DataContract` as the precedent for per-instance config flowing through the contract. This session's `formatHints` is a sibling axis on the same contract.
   - `sessions/archived/223. Widget Primitive — Phase 4g (Event Single-Row Pair) — Log.md` — established EventDescription's structural same-day vs cross-day branching that this session removes (the session lock-in is "we don't support multi-day events meaningfully," so the branching is solving a problem we don't have).
   - `sessions/archived/225. Date Handling — Centralized Vocabulary + Schema Audit — Log.md` — established `App\Support\DateFormat` as the closed-set vocabulary this session's hint mechanism picks from. Read for the vocabulary entries, the lowercase-pm canonical decision, and the deferred-extension framing (per-widget format selection was explicitly listed as a future story this session delivers).
5. Read the full state of every file this session edits or touches. Specifically:
   - `app/WidgetPrimitive/DataContract.php` — gains a `formatHints` field (associative array, default `[]`). Constructor signature change.
   - `app/Support/DateFormat.php` — gains a new vocabulary entry (`EVENT_TILE_DATE` = `'D, M j'` → "Sat, May 2") and per-widget option-set helper methods (`eventDateOptions()`, `postDateOptions()`) that return the curated `[constant => human-label]` maps used by widget schemas.
   - `app/WidgetPrimitive/Projectors/SystemModelProjector.php` — `projectEvent()` retires `starts_at_label` and `ends_at_label`, emits `event_date` and `event_time` (concept-named display fields). `projectPost()` retires `published_at_label`, emits `post_date`. The dispatcher `projectorFor()` threads `$contract->formatHints` into the per-model closures. Allowlist enforcement: hint values are validated against the option-set helpers; unknown values fall back to the per-field default.
   - `app/Widgets/EventsListing/EventsListingDefinition.php` — gains a `date_format` schema field (select, options from `DateFormat::eventDateOptions()`). `dataContract()` reads `$config['date_format']` and populates `formatHints['event_date']`. The `fields` whitelist swaps `starts_at_label`/`ends_at_label` for `event_date`/`event_time`. The default `content_template` updates accordingly — and **the tile cleanup (drop `Ends:` prefix, drop `{{item.slug}}` debug, drop `{{item.starts_at}}` ISO debug, reconcile the two disagreeing default templates) lands as part of this update.**
   - `app/Widgets/EventsListing/template.blade.php` — fallback default at line 3 reconciles to match the definition's default.
   - `app/Widgets/ThisWeeksEvents/ThisWeeksEventsDefinition.php` — same shape: gains `date_format`, contract whitelist gains `event_date`/`event_time`.
   - `app/Widgets/ThisWeeksEvents/template.blade.php` — drops the inline `Carbon::parse + DateFormat::format` block; reads `{{ $event['event_date'] }}` and `{{ $event['event_time'] }}` directly.
   - `app/Widgets/EventDescription/EventDescriptionDefinition.php` — gains `date_format`, contract whitelist swaps `starts_at`/`ends_at`/`description`/etc. for the new shape (keep raw `starts_at`/`ends_at` for the `<time datetime="...">` machine attribute; swap the display fields).
   - `app/Widgets/EventDescription/template.blade.php` — drops the 14-line `@php` block entirely. Becomes a straight token render: `<time datetime="{{ $item['starts_at'] }}">{{ $item['event_date'] }}, {{ $item['event_time'] }}</time>`. The same-day vs cross-day branching is removed (multi-day events not supported).
   - `app/Widgets/BlogListing/BlogListingDefinition.php` — gains `date_format` (using `DateFormat::postDateOptions()`). Whitelist swaps `published_at_label` for `post_date`. Default `content_template` updates and drops the existing `{{item.slug}}` and `{{item.published_at}}` debug-looking lines (mirror cleanup of the EventsListing tile).
   - `app/Widgets/BlogListing/template.blade.php` — fallback default at line 3 reconciles.
   - `app/Widgets/BlogPager/BlogPagerDefinition.php` — gains `date_format`. Whitelist swaps `published_at_label` for `post_date`. Default `prev_template` and `next_template` token references update.
   - `app/Widgets/BlogPager/template.blade.php` — fallback defaults at lines 23-24 reconcile.
6. Read the relevant existing tests that this session's migration affects (every contract-retrofit test asserts a field-shape canonical list; each needs the `_label`/`event_*`/`post_date` swap):
   - `tests/Feature/EventsListingContractRetrofitTest.php` — Test A's `toEqualCanonicalizing` field list at line 67 swaps `starts_at_label`/`ends_at_label` for `event_date`/`event_time`. Any rendered-HTML assertions that referenced the old labels update to the new fields.
   - `tests/Feature/EventDescriptionContractRetrofitTest.php` — Test A's field list updates; the rendered HTML test that asserted `$dateString` shape from the `@php` block becomes a check on `event_date` + `event_time` rendering.
   - `tests/Feature/BlogListingContractRetrofitTest.php` — Test A's field list swaps `published_at_label` for `post_date`. Test B's leak-test sentinels stay in non-display columns.
   - `tests/Feature/BlogPagerContractRetrofitTest.php` — same shape.
   - `tests/Feature/ThisWeeksEventsDashboardRenderTest.php` — its rendered-output assertions move from inline-formatted dates to `event_date`/`event_time` via the projector.
   - `tests/Unit/PageContextTokensTest.php` — unaffected (PageContextTokens uses `DateFormat::format()` directly with vocabulary constants, not `_label` projector fields). Verify by grep before declaring.
7. Open `sessions/226. Date Display — Format Hints + Projector-Owned Assembly.md` and read the session prompt carefully.
8. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first. **This session writes no schema migrations** — `published_at` already lives on `events` and `products` from session 225.
- **Update `docs/schema/`** after writing any migration. Not applicable this session.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session touches widget Blade templates (rendered server-side) but no widget asset files** — `build:public` runs once at end-of-session as a safety check. `npm run build` not required.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands new behaviour in three places: the `formatHints` field on `DataContract` (unit-tested for round-trip), the projector's hint-validated format selection (feature-tested for flow-through and unknown-value fallback), and the per-widget schema option curation (feature-tested for each widget × format option pair).
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/227. base-prompt.md`. Expected next session: **Phase 5 — Record-Detail Slot + Ambient Context Refactor** (the next architectural beat for the Widget Primitive track, deferred from Phase 4's close in 224 and bumped past sessions 225 and 226). `PageContext` generalizes to `SlotContext` with subtypes (page, dashboard, record-detail). Record-detail sidebar becomes a slot. Widgets that need "the current contact" get it from the slot's ambient context, not from controller plumbing. Three to six sessions estimated.
- **Next session prompt**: draft `sessions/227. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/226. Date Display — Format Hints + Projector-Owned Assembly — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: mark the Date Display stub complete. Confirm the next stub (Phase 5 — Record-Detail Slot) follows.
- **Archive the previous session**: move all files matching `sessions/225. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-226/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
