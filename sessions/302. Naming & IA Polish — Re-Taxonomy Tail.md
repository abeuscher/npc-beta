# 302. Naming & IA Polish — Re-Taxonomy Tail (Theme/Template Re-Taxonomy 4 of 4, arc-closing)

Execute the arc's catalogued naming/IA mismatches that 297/300/301 did not already resolve: relabel/regroup the CMS-Settings surface, rename the mislabeled "Header & Footer Code Snippets" section, move Stripe-checkout branding into the existing Finance settings page, give page-templates vs content-templates a clear IA split, and consolidate the widget-dev token doc (stale session numbers + the now-resolved interim note). **Relabel/regroup/move/doc only — no behaviour, schema, or contract change.** This closes the Theme & Template Re-Taxonomy arc and fully retires `release-plan.md` § E6.

---

## Stub reference

No `release-plan.md` entry of its own — this is **arc point 4 (final)** of the Theme/Template Re-Taxonomy, which **supersedes & fully retires `release-plan.md` § E6** on close. Canonical scope: the **"Theme & Template Re-Taxonomy" stub in `sessions/session-outlines.md`**, specifically the **"Naming/IA mismatches to fix along the way"** catalogue and stub-point 4. This prompt grounds each open item to its actual file.

---

## Session-specific deltas

- **The catalogue is partly already resolved — do not re-do.** From the stub's catalogued list: the "Theme page can't set colour" mismatch was fixed by 297 (Colors editor); brand-colour-on-a-template-column was fixed by 297 (relocation); "template inheritance invisible / Default-template-as-Theme" wording was fixed by 301 (the "the Theme header/footer" reframing on the chrome sub-pages). **Confirm these as done; leave them.** Only the items in "Work items" below are open.
- **Behaviour-preserving by construction.** Every change is a label, a nav group, a section move, or doc prose. No `SiteSetting` key is renamed; a moved field reads/writes the identical key. A user reaches the same data and outcomes — only where it lives in the admin and what it is called change.
- **The page-vs-content split is IA, not model.** `templates.type` (`page` | `content`) already exists and already drives separate edit pages (`EditPageTemplate` vs `EditContentTemplate`). The mismatch is that one nav entry/label ("Templates") hides the distinction. The split is presentation (nav + label + table clarity), not a new resource model, not a data change.
- **Small and possibly sub-0.5-session.** Do not pad. If an item turns out already-resolved or trivial, record that and move on.

---

## Work items (each grounded to its file; all open as of 301 close)

1. **CMS-Settings surface relabel/regroup** — `app/Filament/Pages/Settings/CmsSettingsPage.php` sits in nav group `Settings` with label `CMS`, but holds site identity + SEO + auto-publish + build server + code snippets. Give it a label/grouping that matches what it is (the user's mental model: site identity & defaults), consistent with the arc's three-bucket model (Theme = skin, Template = structure, **CMS/Site Settings = identity & defaults**). Do not move General/Mail/Finance settings pages — only correct this page's own label/group placement.
2. **"Header & Footer Code Snippets" → accurate name** — same file, the `Section::make('Header & Footer Code Snippets')` (~line 236) actually contains the **favicon upload + default OG image** (and any head/body snippet fields). Rename the section (and split if a clean two-section shape is clearer: site-icon/OG vs raw code snippets) so the label matches the contents. Fields and keys unchanged.
3. **Stripe-checkout branding → Finance** — same file holds the `stripe_checkout_*`, `stripe_dashboard_branding_confirmed`, `stripe_statement_descriptor*`, and `stripe_default_*_image_upload` fields/section (~lines 65–75 + their form section). Move that whole section into the existing **`app/Filament/Pages/Settings/FinanceSettingsPage.php`** (Finance group — no new page). Same `SiteSetting` keys, same upload collections, same validation; only its home changes. Verify in a browser that the moved section still loads current values and saves.
4. **Page-templates vs content-templates IA split** — `app/Filament/Resources/TemplateResource.php` (nav `CMS`, label `Templates`) lists both types under one name. Make the distinction legible: clearest minimal approach is a label/heading + the existing `type` table column shown by default (and/or default-filtered views / distinct nav labels) so "page templates" (structure/chrome) and "content templates" (widget-stack presets) read as the two different things they are. Keep one resource + the existing routes/edit pages; this is labelling/surfacing, not a second resource.
5. **Token-doc consolidation** — `docs/theme-color-tokens.md`: fix stale session numbers (`session 298` widget audit → 300; `session 299` schemes/chrome → 301), and correct/remove the now-false "There is intentionally no per-template colour between sessions 297 and 299" interim note (301 restored deliberate per-page deviation via schemes — point at the scheme model). Consolidate it as the single published widget-dev token contract (it remains the fixed contract; **no token added/renamed** — wording only).

---

## Open questions to resolve at session start

1. **CMS-Settings label/group.** Recommendation: keep one page; relabel to read as site **identity & defaults** and place it in the nav group consistent with the three-bucket model rather than a bare `Settings`/`CMS` mismatch. Confirm the exact label/group string before editing (it is user-facing IA).
2. **"Header & Footer Code Snippets" shape.** Recommendation: rename to reflect favicon + default OG image (+ snippets); split into two clearly-named sections only if it reads cleaner. Confirm the section name(s).
3. **Stripe-branding placement inside Finance.** Recommendation: a self-contained "Checkout & Statement Branding" section appended to `FinanceSettingsPage` (its own section, not interleaved). Confirm.
4. **Page/content split presentation.** Recommendation: one resource, distinct labelling + the `type` column surfaced (and/or split nav labels), not two resources. Confirm the approach before touching `TemplateResource`.

These are IA wording/placement confirmations, not architecture — batch them in plain prose at start, proceed on the recommendations if no objection (`memory/feedback_no_multiple_choice_questions`, `memory/feedback_dont_manufacture_planning_distinctions`). No mid-session judgment gates (`memory/feedback_dont_gate_on_taste_defaults`).

---

## Phases

### Phase 1 — Admin IA (items 1–4)

Relabel/regroup `CmsSettingsPage`; rename the snippets section; move the Stripe-checkout-branding section into `FinanceSettingsPage` (same keys/collections); give `TemplateResource` the page-vs-content IA clarity. Behaviour-preserving throughout — every moved field reads/writes its existing `SiteSetting` key.

### Phase 2 — Doc consolidation (item 5) + verify

`docs/theme-color-tokens.md` session-number + interim-note consolidation. Run the fast suite; update any test that asserts an old label/nav-group/section title to the new string (expected IA churn, every delta explained). Browser spot-check: the moved Stripe section loads + saves under Finance; the renamed sections show the same fields; page/content templates read distinctly. No scheme/multi-surface captures (301 owned that).

---

## Out of scope

- **Any token-list / scheme / chrome / resolver / widget-SCSS change** — 297/300/301 are done and guarded; the token doc change is wording only.
- **Schema changes** — none; if an item seems to need one it has drifted out of scope, stop and surface.
- **New features or behaviour** — strictly relabel/regroup/move/doc.
- **Renaming `SiteSetting` keys or storage** — only UI home/label moves.
- **Moving other settings pages** (General/Mail) or redesigning the settings IA wholesale — only the catalogued items.
- **The Theme + Media Portability session** — separate post-arc session, paired with `sessions/media-portability-prompt-draft.md`; not this.
- **Any Fleet / `/api/health` / response-schema change** — non-boundary; CRM stays v2.3.0.

---

## Testing

- **Slow test groups to run this session:** none.
- **New tests expected:** minimal/none. This is IA. Expected churn: tests asserting an old nav group, label, or section title are updated to the new string in the same pass (explain each). A light Pest assertion that the moved Stripe-branding fields persist under the Finance page (same keys) is reasonable if a test already covered them under CMS Settings — move/adjust rather than add net-new where possible. Fast-suite target: the 301 baseline ± only label-driven edits, all green, every delta explained. Pest and Playwright sequential, never parallel.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific:

- **Log file:** `sessions/302. Naming & IA Polish — Re-Taxonomy Tail — Log.md`.
- **Artifact:** the relabeled/regrouped CMS-Settings surface; the accurately-named snippets section; the Stripe-checkout branding relocated under Finance (same keys); the page-vs-content template IA clarity; the consolidated `docs/theme-color-tokens.md`; label-driven test updates; `VERSION` `0.302.x`; **`release-plan.md` § E6 fully retired and the "Theme & Template Re-Taxonomy" stub marked complete (all four points resolved)**.
- **Branch:** `session-302/N` (final iteration).
- **Next session:** the arc is complete — return to the `release-plan.md` execution order (Public Website Complete milestone continues; D2 unblocked). Draft only when the user names it; reflect the actual execution-order state, do not assume.
