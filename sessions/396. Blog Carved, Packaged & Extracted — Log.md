# Session 396 — Blog Carved, Packaged & Extracted — Log

**Date:** 2026-08-20
**Branch:** session-396/1

---

## What was built

Arc D6 of the Plugin Architecture arc — **the block opened and closed in one session**, the first single-session carve → package → extract run (the 393/395 compression taken one step further; the thinnest surface yet made it safe). The Blog behavioral surface carved into `plugins/Blog/`, packaged as `nonprofitcrm/blog` (the seventh path-repo package, **with no squash redraw — the first vertical owning no schema**), and moved to `github.com/abeuscher/crm-plugin--blog` in the seventh extraction-runbook run (public, tag `v0.1.0`), emptying `plugins/` a fifth time. The dead `posts` table dropped (owner-ratified decision 3). Behavior-preserving throughout: routes 272/272 byte-identical against main, fast suite 3391 → **3411/0** purely additive, no visual change. One iteration, four work commits + one docs commit + the 397-prompts commit.

### Phase A — The carve

- **Plugin shape:** `plugins/Blog/` + `BlogServiceProvider` (namespace `Plugins\Blog\`, handle `blog`); `distribution.json` gains the seventh entry (source `folder`, appended last); `plugins:manifest-sync` regenerated `config/plugins.php` with the seventh provider line. Zero guard edits — the 388 tolerate-empty relaxations tolerated the refill by construction.
- **Moved with namespace change, behavior identical:** `PostController` to `Plugins\Blog\Http\Controllers\`; the two `blog_prefix` routes (`posts.index`, `posts.show`) to the plugin route file registered at provider `boot()` ahead of the page-slug catch-all — names/URIs/middleware byte-identical; `PostResource` + four pages + the `HasPostSecondaryActions` trait (a fifth file the survey's "four pages" missed — logged drift) to `Plugins\Blog\Filament\Resources\`, discovered through the admin socket, **no permission names declared** (the 381 extracted-vertical nuance); the `edit-post` builder view to the `plugin-blog::` namespace; both widgets nested under `Widgets/` with `template()` overrides via `plugin-blog-widgets::`.
- **The pager-styles lift (ADR 0007's worked case, decision 2) — with a logged drift:** `app/Widgets/BlogPager/styles.scss` was **entirely** the shared listing pager bar (`.widget-listing__*` — consumed by the BlogListing and EventsListing templates), and **BlogPager itself never declared any assets** (the base `assets()` returns `[]`; the survey attributed the declaration to BlogPager). The pager's own `pager-link` styles live in core `_custom.scss`. Adapted: the whole file lifted to core-owned `resources/scss/widgets/_shared-pager.scss`; **BlogListing** (plugin side) and **EventsListing** (the rider) declare the core path; BlogPager keeps declaring nothing — its `widget_types` row stays byte-identical to pre-carve.
- **The Events rider — `crm-plugin--events` v0.1.3** (the 389/394 rider mechanics: sibling edit → tag → core lock bump same-commit, `^0.1` unchanged): EventsListing's second SCSS input re-pointed from the departing `app/Widgets/BlogPager/styles.scss` to the core-owned shared path. Without it, `collectSources()` would have silently dropped the pager rules from the bundle the moment the file moved.
- **Core reaches re-pointed by route name** (the two class reaches the step-0 grep found beyond the survey): QuickActions' `new_post` entry (the in-file `new_event` pattern — `Route::has(...) ? route(...) : null`) and `PageBuilder`'s post-details URL. `app/` now carries zero `Plugins\Blog` references (boundary guard proves it, zero allowlist).
- **No capability ships** — nothing consumes blog presence (survey item 9 held); the presence signal, were one ever needed, is route presence.
- **Rule-5 membership pass:** new phpunit `Blog` testsuite at `plugins/Blog/tests` — the three blog widget tests moved (`BlogListingContractRetrofitTest`, `BlogPagerContractRetrofitTest`, `BlogPagerEndToEndTest`) **plus `PostDuplicateTest`** (not on the survey's list — its subject is the duplicate action in the moving resource; logged drift). Stayed core with import/path re-points: `QuerySettingsContractRoundtripTest`, `ContentListingWidgetsTest` (+7 template reads), `AppearanceConfigRenameTest` (+1), `Session156Test`, `Session167Test`, `PageLockTest`, `TrashManagementTest`, `ContentTemplateEditorTest`.
- **New tests (+20 fast):** enabled twin `BlogPluginSession396Test` (provider/no-capability/routes incl. catch-all ordering + the four admin routes/widgets/shared-pager declaration/QuickActions resolution/view namespaces), removal mirror `BlogPluginRemovalSession396Test` on `BlogRemovedTestCase` (named routes + admin routes vanish; **`/news` 200 via the catch-all and a post row still served as a plain page — listing-absent, not page-absent**; widget rows drop on sync; QuickActions yields null), flag mirror `BlogDisabledViaFlagSession396Test` on its fixture, boundary guard `BlogModuleBoundaryTest`. **The twin fixtures permanently register no migrator path** — the plugin owns no schema; their docblocks say so explicitly (not the expiring-caveat wording).
- **Two step-0 misses fixed** (logged): `WidgetAssetsCollectionRetiredGuardTest` read the controller's old core path (re-pointed, landed vendor-relative at the package step — the 393/395 compression nuance); `WidgetRegistryTest`'s `widgets::` namespace assertion used BlogPager as its example core widget (swapped for Hero — the assertion's subject is the namespace mechanics, not the blog).

### Phase A′ — The dead-table drop (decision 3, its own commit)

- Guarded core drop migration (`Schema::dropIfExists('posts')` — a no-op on fresh installs, drops the relic on existing databases via plain `migrate`; the dev database was walked in-session). Re-verified before dropping: zero readers (every `app/` "posts" reference is `Page`-typed), zero rows.
- Dump regenerated against a scratch database (database recreated, table dropped first); **the 348-precedent identity check is clean** — the diff is exactly the posts DDL (table, pkey, slug unique, author index, the author SET-NULL FK) plus pg_dump's per-run token; zero `posts` mentions remain.
- Fresh install verified: 126 migration rows (125 + the drop row), no posts table, spot counts exact (widgets 41, pages 13).
- **Logged drift:** `docs/schema/posts.md` never existed and the schema README carries no posts row — the dead table was never documented, so decision 3's doc-removal half was vacuous.

### Phase B — Package (no redraw)

- **P6 mechanics verbatim:** `plugins/Blog/composer.json` (`nonprofitcrm/blog`, explicit `version: 0.1.0`, PSR-4 `Plugins\Blog\` → `""`, PHP `^8.2`, no third-party dependencies); root path repository + bound `^0.1` require; manifest `folder` → `path` same-commit with the lock (the 386 guard interlock held, zero guard edits); one Dockerfile manifest-COPY line per composer stage; `composer validate --strict` clean; `plugins:manifest-sync` a provider-list no-op.
- **The redraw category is empty and recorded** (the 387/393 wording discipline): the plugin owns no tables — no migrations travel, no identity-check procedure runs, no mark-as-applied deployment step exists. The one schema event this session is Phase A′'s core drop.

### Phase C — Extraction (the runbook, seventh run)

- **The new repo:** `plugins/Blog/.` copied to the owner-created sibling checkout (27 files); `version` field dropped; BlogListing's two asset declarations rewritten vendor-relative (**the core-owned shared-pager input stays a core repo-relative path** — the surface-11 cross-input rule); README on the member-portal model (contract 0.20.0, tags-are-releases, tests-travel, the no-owned-schema statement); the owner's empty `readme.md` replaced; committed, tagged `v0.1.0`, pushed.
- **Core swap:** `distribution.json` source `path` → `vcs`; root repository entry swapped; `git rm -r plugins/Blog`; `composer update nonprofitcrm/blog` (composer before artisan — the 387 caveat); lock pinned to the tag at the exact commit (`b8f6a35c`, dist zip); `composer validate --strict` clean. Dockerfile COPY lines removed (zero path-sourced entries — `plugins/` empty a fifth time until D7's carve). phpunit `Blog` suite → `vendor/nonprofitcrm/blog/tests`; comment mentions (`routes/web.php`, the boundary-guard docblock) + `docs/app-reference.md` (4 rows) / `docs/runbooks/permission-matrix.md` (1 link) re-pointed.
- **Zero new guard relaxations; no runbook deltas** — the seventh run ran as written; the no-schema worked-example lines folded into steps 0 and 9.

### Verification (all gates)

- Route parity: **272/272 byte-identical** on method+URI+name+middleware (stash-capture diff against main; the only diffs are the six moved action FQCNs — the 381 exception).
- Gate 4 live, twice (carve + extraction): `widgets:sync` (41 rows; `blog_listing` on vendor paths + the core shared-pager path, `blog_pager` empty as pre-carve) → `build:public` → bundle proven to carry the shared pager rules, the blog-listing rules, the `blogListing` component, and the events-listing rules from the rider's re-pointed declaration.
- In-image proof: `docker build --target app` clean — **all seven plugin packages fetched from GitHub inside the build**, `BlogServiceProvider` autoloads through the optimized `--no-dev` autoloader, `plugins/` absent from the image.
- Dev-stack smoke: home + `/news` (the plugin route live from the vendor path) + `/admin/login` all 200; dev database walked (posts table gone via plain `migrate`).
- `TemplateSeeder`'s `blog_pager` content-template entry verified composition-safe (the missing-type `continue` — seeds skip cleanly on a blog-absent install).
- CI: **`Tests` aggregate green** on the pushed branch (fast + slow — the authoritative signal; Playwright non-gating).
- Docs: contract **0.20.0** (Package shape + External repo seventh consumer; surface 5's no-owned-schema note with the inverted vanish semantics; surface 11's cross-input resolution case; surface 2's at-scale note re-pointed; changelog); runbook seventh worked-example lines; plan doc D6 ✅ block closed, arc header → 397/D7.

### Notable drift and bookkeeping (adapted, logged)

- **BlogPager never declared assets** — its folder stylesheet was consumed only by BlogListing's and EventsListing's declarations (detail in Phase A). The lift therefore has three declaration outcomes, not four: BlogListing + Events on the core path, BlogPager unchanged at none.
- **`PostDuplicateTest` joined the movers** and the `HasPostSecondaryActions` trait joined the moving file set — both absent from the survey's lists.
- **The posts DDL sat at dump line ~1042**, not ~1084; `docs/schema/posts.md` never existed (decision 3's doc half vacuous).
- The `crm-plugin--blog` repo did not exist at session open (surfaced at the gate; the owner created it before Phase C — no pause needed, the carve ran meanwhile).
- Two step-0 grep misses surfaced by the first full-suite run (the retired-guard file read, the `widgets::` example widget) — both fixed the same hour, both green since.

---

## D7 kickoff input (what the Forms block — 397 — inherits)

The full survey is front-loaded into `sessions/397. Forms Carved, Packaged & Extracted.md` (captured from live code at this close; prompts drafted and committed on this branch). Highlights:

1. **The surface:** `FormSubmissionController` + one route (`forms.submit`, POST, throttled) — the controller carries the member-portal collision seam verbatim (it already consumes the capability registry); `FormResource` + three pages + `FormSubmissionsRelationManager`; the `WebForm` widget, whose real render surface is the **`<x-public-form>` Blade component** (sole consumer is the widget template — travel makes it the first plugin-owned Blade component, front-loaded decision 4).
2. **The observer detach is the design freight:** core `FormSubmission` wires its observer via `#[ObservedBy]` — the attribute comes off and the plugin registers `observe()` at `boot()` (the Memberships precedent), so the observer + `FormSubmissionMailable` travel. Unlike the 394 portal mailables (senders are model framework hooks), this sender is detachable.
3. **The fifth squash redraw, the first with two sequences:** `forms` → `form_submissions` (FK order), both bigint PKs with sequences; `contact_id → contacts ON DELETE SET NULL` (plain behavior); the plugin-internal CASCADE FK; **zero inbound core FKs**.
4. **Both form email handles migrate to the plugin registry** (surface-7 donations mechanics) — absorbing the `portal_form_collision` half of the standing housekeeping-inbox item (the registry becomes the single source of its defaults); the `admin_invitation` half stays core.
5. **Composition-safety candidates:** `PageBuilderDataSources`' forms picker options, QuickActions' `new_form` (the route-name pattern), `PageContext::form()` (caller set looked empty at survey — verify), `FormSeeder`'s unconditional `DatabaseSeeder` call (route-presence gate, the tier-seeder precedent). `TemplateSeeder`'s `web_form` entry skips cleanly when absent (verified this session).
6. **No sibling edit expected**; no capability expected (route presence is the signal).

---

## Security considerations

No new endpoint or input path. The carve moved files verbatim — the two public routes keep their middleware, the admin surface stays behind the panel's auth stack (socket-discovered resources land inside Filament's `Authenticate` wrap), and the resource's permission checks (`view_any_post`, `update_page`, `edit_locked_pages`, `edit_page_snippets`) travel unchanged. The dead-table drop removes unused attack surface (an orphaned table with a users FK). The two core reaches that formerly named the resource class now resolve by route name and yield null on blog-absent compositions — narrowing, never widening. Supply-chain: the seventh package moves to a public GitHub repo lock-pinned to an exact tag commit; the Events v0.1.3 rider is a one-line asset-path re-point, lock-pinned the same way (both deliberate, reviewed lock updates). `deploy.yml` untouched; FM surfaces untouched; CRM contract stays v2.7.0.

---

## Schema changes

One real change, owner-ratified: **the dead `posts` table dropped** — a guarded core drop migration (`database/migrations/2026_08_20_000001_drop_dead_posts_table.php`, the sole live core migration) plus dump regeneration whose diff is exactly the posts DDL (348-precedent identity check clean). The table had zero readers and zero rows; posts are `Page` rows of type `post` and always were, post-CMS-pivot. No schema *docs* change — the table was never documented (no `docs/schema/posts.md`, no README row). **No boundary move**: the Blog plugin owns no tables, so the core dump's owner map is unchanged and no plugin migrations exist.

---

## Production deployment notes

Plain `migrate` is sufficient everywhere — the drop migration removes the dead `posts` table from existing databases and no-ops on fresh installs (the regenerated dump already excludes it). **No mark-as-applied step this session** (no new plugin migrations — the first extraction without one). The deploy image build now fetches `nonprofitcrm/blog` v0.1.0 and `nonprofitcrm/events` v0.1.3 from GitHub (public, credential-free) — no pipeline change. Deploy already runs `widgets:sync` + `build:public`, which picks up the vendor-relative blog paths and the core shared-pager stylesheet (verified locally; handles unchanged). VERSION bumped 0.395.01 → **0.396.01**.

---

## Deferred decisions

- **D7 Forms block** — session 397 (prompts drafted and committed at this close; five gate ratifications: the one-session shape, the observer inversion, the handle migration + inbox absorb, the `<x-public-form>` component travel, the `FormSeeder` route-presence gate; owner creates `crm-plugin--forms`).
- **The `portal_form_collision` housekeeping item** — unchanged this session; its collision half is slated for absorption by 397's handle migration (the item splits at that close).
- **D8 products/store disposition** (§ 6.8) — resolves at D8's prompt draft; the D8 kickoff survey captures at the 397 close.
- **Model/factory moves** (§ 6.7 stands — `Page` is core engine; there was no Post model to move); **per-plugin CI** (D11); **root PSR-4 retirement** (D12); **events-handle registry migration** (a future `crm-plugin--events` release); **`baseDir()`-derived asset declarations** (standing backlog); **Playwright chrome-parity park** — all unchanged.

---

## Test result

- **Fast Pest:** 3411 passed, 0 failed (parallel) — 3391 baseline + 20 additive (twin 8, removal mirror 7, flag mirror 4, boundary guard 1); every pre-existing blog assertion unchanged beyond the surveyed re-points and the two additive count/list updates (`PluginActivationTest`, the superset count).
- **`Blog` testsuite:** 23 passed (6 files) from `vendor/nonprofitcrm/blog/tests` — identical counts from the plugin path, the path-repo symlink, and the vendor path.
- **Route parity:** 272/272, byte-identical on method+URI+name+middleware (stash-capture diff against main; six action-FQCN diffs — the 381 exception).
- **Dead-table identity check:** dump diff exactly the posts DDL + the per-run token; fresh install 126 migration rows, zero posts table, spot counts exact.
- **Composer:** `validate --strict` clean; lock pins `v0.1.0` at the exact tag commit, dist zip, no `version` field in the vendor manifest; `nonprofitcrm/events` at v0.1.3.
- **Gate 4:** bundle content proven twice (carve + extraction) — shared pager rules, blog rules + component, events rules from the rider path.
- **In-image proof:** clean (all seven packages VCS-resolved inside the build; `plugins/` absent).
- **CI:** `Tests` aggregate **green** on the pushed branch (fast + slow — the authoritative signal; Playwright non-gating, the parked chrome-parity spec remains the known failure).
