# Base Prompt — Session 223

We are about to begin a new session: **223. Widget Primitive — Phase 4g (Event Single-Row Pair: EventDescription + EventRegistration + PageContext::event Retirement)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session writes no migrations and no schema changes — `events` and `event_registrations` are the surfaces involved; read `docs/schema/events.md` and `docs/schema/event_registrations.md` to confirm column shape and the registrations status enum used by `Event::isAtCapacity()`.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise.
   - `sessions/widget-primitive-migration.md` — the migration sketch. Phase 4 retrofit is mid-campaign; the Event single-row pair is the **first non-list-shaped contract** to land. The single-row DTO shape, the aggregate-derived field pattern (`is_at_capacity` via `withCount`), and the `cardinality` declaration on `DataContract` all originate in this session and propagate to ProductDisplay (224) and every Phase 5 record-detail-sidebar widget afterward.
4. Read the most recent Phase 4 session logs to understand the trail of decisions this session lands on top of:
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — established the `_label` projector-key naming convention and the `eager-load-skips-when-all-FKs-null` Test B caveat. Established the precedent for retiring a `PageContext` helper in the same session that retired its sole callers (`upcomingEvents()` retired alongside EventsListing). 223 mirrors that move with `event()` and `eventCache`.
   - `sessions/archived/221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer) — Log.md` — the query-settings architecture and the `QuerySettings` value object on `App\WidgetPrimitive`. Single-row contracts do not declare `querySettings` (no panel — there is no admin-meaningful "limit" or "order_by" for a unique-slug lookup).
   - `sessions/222. Widget Primitive — Phase 4f (BlogPager Retrofit + PageContext::posts Retirement) — Log.md` (or `sessions/archived/` if 223 close has moved it) — the BlogPager retrofit's PageContextTokens-namespace fix surfaced the convention that token names inside richtext config fields should be `{{item.foo}}`-prefixed to survive WidgetRenderer's preprocessing. Not directly relevant to single-row Event widgets (their templates do not use richtext token substitution), but worth knowing about as a recently-grooved pattern.
5. Read the full state of every file this session edits. Specifically:
   - `app/Widgets/EventDescription/EventDescriptionDefinition.php` — 47 lines. No `dataContract()` today. Add `dataContract()` returning `SOURCE_SYSTEM_MODEL`/`'event'`/`cardinality: CARDINALITY_ONE` with the field whitelist used by the template's render. Do not declare `querySettings()`.
   - `app/Widgets/EventDescription/template.blade.php` — 48 lines. Line 1 walks `$pageContext->event($config['event_slug'] ?? null)`. The template reads `$event->title`, `$event->starts_at`, `$event->ends_at`, `$event->is_in_person`, `$event->is_virtual`, `$event->city`, `$event->state`, `$event->description`, with conditional Carbon formatting (`'ga'` vs `'g:ia'` based on `minute === 0`). Rewrite to read from `$widgetData['item']`; conditional formatting moves to template-side `Carbon::parse($item['starts_at'])`.
   - `app/Widgets/EventRegistration/EventRegistrationDefinition.php` — 59 lines. No `dataContract()` today. Add `dataContract()` returning the same single-row event contract with a wider field whitelist (the registration form template reads `status`, `registration_mode`, `is_free`, `is_in_person`, `mailing_list_opt_in_enabled`, `external_registration_url`, `slug`, `title`, `price`, `is_at_capacity`).
   - `app/Widgets/EventRegistration/template.blade.php` — 161 lines. Walks `$pageContext->event(...)` then reads many model fields plus calls `$event->isAtCapacity()` (a method that runs a COUNT subquery on `event_registrations`). Rewrite to read from `$widgetData['item']`. The auth/session/request access (`auth('portal')->user()`, `session('registration_success')`, `request()->query(...)`, `$errors->has(...)`) stays unchanged — that is session/request state, not data shape.
   - `app/WidgetPrimitive/DataContract.php` — 49 lines. Add `cardinality` field (`CARDINALITY_MANY` default, `CARDINALITY_ONE` for single-row contracts). Constants `CARDINALITY_MANY = 'many'` and `CARDINALITY_ONE = 'one'` on the class.
   - `app/WidgetPrimitive/Projectors/SystemModelProjector.php` — 110 lines. The existing `projectEvent()` map emits 15 fields. Add 8 new fields needed by the registration template: `description`, `is_in_person`, `is_virtual`, `is_at_capacity`, `registration_mode`, `mailing_list_opt_in_enabled`, `external_registration_url`, `price`. Add a new public method `projectOne(DataContract, ?Model)` that reuses the same per-model dispatch as `project()` and returns `['item' => row | null]`.
   - `app/WidgetPrimitive/ContractResolver.php` — 316 lines. Extend `resolveSystemModel()` to dispatch by cardinality. Add a new private method `resolveEventOne(DataContract, &$cache)` that runs `Event::published()->where('slug', $slug)->withCount(['registrations as registered_count' => fn ($q) => $q->whereIn('status', ['pending','registered','waitlisted','attended'])])->with(['media', 'landingPage'])->first()`. Cache key `'event:one:' . $slug` so two single-row contracts targeting the same event on one page share a query.
   - `app/Services/PageContext.php` — `event(?string $slug)` method (lines ~66–79) and `array $eventCache = []` property become unused after both Event widgets retrofit. Delete them. Verify by repo-wide grep before deletion. Mirror of 217's `upcomingEvents()` retirement and 222's `posts()` retirement.
   - `resources/docs/widget-types.md` lines 117 (the `event(string $slug)` row in the Record Methods table) and 125 (a `$pageContext->event(...)` usage example). Edit conservatively — remove the row, update or remove the example.
   - `resources/docs/widget-development.md` line 105 (a prose reference to `$pageContext->event($slug)` in the "Direct data access" list). Edit conservatively.
6. Read the two contract retrofit test files most relevant to the event arm:
   - `tests/Feature/EventsListingContractRetrofitTest.php` — Test B's three-select pin (one events select, one media select, one landingPage select). Adding a new `'event:one'` arm to the resolver does not change EventsListing's query shape — its test should remain byte-equivalent. Spot-check at the end of implementation.
   - `tests/Feature/BlogPagerContractRetrofitTest.php` — for the `PageContext` instance-binding pattern used in tests where a widget needs the host page in `pageContext->currentPage`. Single-row Event tests do not need this (they bind `event_slug` from config, not from currentPage), but the binding pattern is established there.
7. Read `tests/Feature/PageBuilderApiTest.php` — confirm neither `event_description` nor `event_registration` has specific tests there; nothing to update unless `apiWidget(..., 'event_description'|'event_registration')` shows up.
8. Confirm `app/Widgets/EventDescription/script.js` and `app/Widgets/EventRegistration/script.js` do not exist (no front-end JS to retrofit). Verified in the read pass — neither widget has a script file, neither declares JS in `assets()`. No `build:public` expected this session.
9. Open `sessions/223. Widget Primitive — Phase 4g (Event Single-Row Pair: EventDescription + EventRegistration + PageContext::event Retirement).md` and read the session prompt carefully.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216–222. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. **Not expected this session.**
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. **Not expected this session — neither Event widget declares assets.**
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/224. base-prompt.md`. Expected next session: **Phase 4h — Product pair retrofit + `WidgetDataResolver` shell retirement** (ProductCarousel + ProductDisplay + the cleanup vertical that retires `WidgetDataResolver::resolve()`/`resolveProducts`/`resolveBlogPosts`/`resolveEvents` and `PageContext::collection()`/`product()`/`form()` if their last consumers are reachable through the contract path). 224 introduces a new `'product'` arm in both `ContractResolver` and `SystemModelProjector` plus the first nested DTO projection (Product.prices).
- **Next session prompt**: draft `sessions/224. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/223. Widget Primitive — Phase 4g (Event Single-Row Pair: EventDescription + EventRegistration + PageContext::event Retirement) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: mark Phase 4g complete under the Widget Primitive track stub. Note the 2 remaining widgets in Phase 4 (the Product pair). Note that `PageContext::event()` and `eventCache` are retired this session, leaving `PageContext` with `currentPage`, `currentUser`, `collection()`, `product()`, and `form()` — three of which retire in 224. Confirm the Date Handling stub still scheduled to land after Phase 4 completes.
- **Archive the previous session**: move all files matching `sessions/222. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-223/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles. (Captured in `feedback_no_collapse_ui` memory.)
