# Base Prompt — Session 342 (Housekeeping — Infra Hygiene and Page-Builder Polish)

**Start now.** Execute the reading list below, open the session prompt at `sessions/342. Housekeeping — Infra Hygiene and Page-Builder Polish.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **342. Housekeeping — Infra Hygiene and Page-Builder Polish.**

This is **not a `release-plan.md` entry** — it is a **housekeeping batch** (Rule 2) draining verified items from `sessions/housekeeping-inbox.md`, scheduled after session 341. With the roadmapped tracks largely complete and several now gated on external/designer input, design-independent housekeeping is the available work — this batch is deliberately scoped to items that need **zero outside input** and are self-verifiable. Items are **pre-Public-Website-Complete** unless individually tagged `[post-milestone]`.

This session is **NOT boundary-touching** — no Fleet Manager / `/api/*` surface is in scope and no `CONTRACT_VERSION` change. **CRM contract stays at v2.3.0.** (One adjacency to note, not a contract touch: the `.env` / `.env.example` audit reconciles deploy/provisioning config, but the `/api/*` surface and the FM agent contract are untouched.)

**Session shape:** housekeeping batch. Drain ~8–9 pre-verified inbox items across three clusters — (1) **dependency & config hygiene** (remove Laravel Debugbar, prune the stale `.env`/`.env.example`, add branded error pages), (2) **page-builder operator-safety + copy** (confirm-before-deleting-a-populated-column, an inline disabled-checkbox hint, the page-form terminology copy pass, Duplicate-action consistency), and (3) **test backfill** (two deterministic forward-queue Pest gaps). Each item is independently shippable; commit in cluster-sized changesets.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. Read `sessions/session-outlines.md` — the **active-tracks** block (to see what's gated/paused) and the **Housekeeping & Review — Beta 1 Scope** section. No Cross-Repo update is expected (non-boundary), but confirm.
3. **Read the inbox items this session executes** in `sessions/housekeeping-inbox.md` — each is listed by capture-stamp in the session prompt. **Re-verify each premise against current code before implementing** (the staleness guard); session 341 already pre-verified the core set, but re-confirm at the walk.
4. **Cluster 1 reading:** `composer.json` (the `barryvdh/laravel-debugbar` dep) + `grep -ri debugbar` across `config/`, `bootstrap/`, `app/` to find every reference + any published config/asset; `.env.example` and `app/Providers/AppServiceProvider.php` (`boot()` — the `SiteSetting`-override mechanism that makes many `.env` integration keys dead) to ground the `.env` audit; how Laravel resolves error views + an existing public Blade layout to mirror for branded `resources/views/errors/{404,403,500}.blade.php`.
5. **Cluster 2 reading:** `app/Filament/Resources/PageResource.php` + `app/Filament/Resources/PageResource/Pages/EditPageDetails.php` (the "Page Type" / "Page Template" / "Content Template" labels); an existing Duplicate/replicate action to mirror (`PostResource.php`, `ProductResource.php`, `EventResource.php`, `PageResource.php` all carry one) and the resources that lack it (Organizations / Templates / Navigation / Mailing Lists); `resources/js/page-builder-vue/components/LayoutColumnSettingsTab.vue` (the hover-only disabled "Background fills page width" checkbox); the page-builder Vue column-count / column-removal path (where a layout's column count changes — the guard hooks here).
6. **Cluster 3 reading:** `tests/Feature/ImportFinancialTest.php` + the invoice-details importer grouping logic (for the first-row-wins parent-field conflict-resolution Pest); `tests/Feature/DonationCheckoutTest.php` + the donation checkout controller (for the create-pending-then-delete-on-Stripe-failure path).
7. Read `docs/app-reference.md` for container names + the build/deploy pipeline (Cluster 1 touches deploy-adjacent config; no pipeline change is expected, but know it).
8. Note any drift between this prompt and the actual code in a brief work-log entry; proceed unless something needs a decision per the drift / decision-threshold rules.

---

## Starting state inherited from session 341

- **CRM contract at v2.3.0** — unchanged. 341 was deploy-infra (added `widgets:sync` to every deploy path); non-boundary, no contract touch. This batch is also non-boundary.
- **Schema baseline:** no change since 328. 341 added none. **This batch expects no schema change** — it is config/copy/UX-affordance/test work; no migration. Flag immediately if any item turns out to need one.
- **Fast Pest suite green at the 341 close baseline** (2786 passed, 0 failed). **New Pest expected: yes** — the two Cluster 3 backfill cases, plus a test for any behaviour change (the column-delete confirm guard, the new Duplicate actions). Pure copy / config / Blade-view items may skip tests.
- **Playwright:** baseline preserved; **no new specs this session.** The inbox's two e2e guard items (ordered/unordered list rendering; widget "knockout" controllability) are **explicitly out of scope** — they are e2e-authoring work for a dedicated test-hardening pass, not this batch.
- **Front-end build:** Cluster 2 edits `resources/js/page-builder-vue/**` → **`npm run build`** is required before announcing completion. Branded error views are Blade (no build). Debugbar removal is a PHP dependency (no build). Run `npm run build` once the Vue changes land; add `build:public` only if a widget asset/SCSS actually changes (not expected).
- **Planning state:** 341 folded the single ⭐ `[0.324.01]` deploy-sync item out of the inbox. No release-plan or outlines structural changes since.
- **Housekeeping inbox:** this session drains the specific items enumerated in the session prompt. All other inbox items (design-gated, post-milestone, or the e2e guards) stay for future batches.

---

## Process rules for every session

Base-prompt process rules apply in full (`sessions/template-base-prompt.md`). Session-specific notes:

- **Verify each item's premise at the walk before touching it.** The inbox is a snapshot; 341 pre-verified the core set but re-confirm against current code (already fixed? superseded? references gone?). Drop or re-scope dead items as found, and note the disposition.
- **Debugbar removal must prove the public-site outcome.** The owner's explicit ask is that the toolbar never render on public pages in any environment. After pulling the dependency, confirm objectively it is gone (no provider registered, no asset injected, public page source clean) — don't just remove the composer line.
- **`.env` audit edits the contract for self-hosters.** Prune only vars confirmed unread against current code (341 verified the named dead set has zero references; re-confirm any you remove). Keep `.env.example` accurate as the standalone/self-host contract; annotate the FM-provisioned-vs-standalone split rather than deleting vars a self-host install still needs.
- **Bind-mount caution does not apply here** (no nginx/php single-file config edits expected) — but if an item drifts into one, recall that editing a single-file bind-mount breaks the container mount (`up -d --force-recreate <svc>`, not `restart`).
- **Commit in cluster-sized changesets** — each cluster is independently deployable; one or more commits per branch is fine.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation each listed item still holds against current code (flag any that went stale); (2) the proposed cluster ordering and whether the two optional stretch items are in or out. (The Duplicate-action scope is already settled — all four of Organizations / Templates / Navigation / Mailing Lists; see the session prompt's Settled decisions.) One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 + Phase 2. Session-specific details:

- **Log file:** `sessions/342. Housekeeping — Infra Hygiene and Page-Builder Polish — Log.md`. Record each item drained, its disposition (shipped / dropped-as-stale / deferred), and any item that turned out invalid at the walk.
- **Bump `VERSION`** to `0.342.<iteration>`.
- **Update** `sessions/completed-sessions.md` + `sessions/session-outlines.md`; **fold every drained item out of `sessions/housekeeping-inbox.md`** (record the drain under "Recently dispositioned").
- **Archive the previous session's files** — move `sessions/341. *.md` → `sessions/archived/`.
- **Next session:** owner confirms at close (likely guided by non-roadmap pressure — designer feedback, external input — per the owner's standing note; another housekeeping batch is the fallback).
- **Commit on `session-342/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: the win is **a clutch of small, verified, design-independent fixes shipped cleanly** — dead dependency gone, stale config pruned, branded error pages, a few page-builder safety/clarity touches, and two test gaps closed. Resist scope creep into the design-gated items deliberately excluded (slide/popout repeater editor, Logo-widget rich-text, link-color consolidation, image-editing toolbar, BoardMembers→people-list, full-size text editing, alignment-preset defaults) and into the e2e guards. Commit messages name the deliverable ("342/N — {cluster}: {scope}").
