# Base Prompt — Session 387 (Payments Packaged & Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/387. Payments Packaged & Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **387. Payments Packaged & Extracted**.

This is **arc position D1** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary) — **the opening session of Stage D, full decomposition**. P10 (386) closed Stage C-lite: `distribution.json` is the plugin-set + ordering authority, `config/plugins.php` its generated artifact, and composition-by-manifest is proven. D1 runs the first per-vertical block of the committed Stage D ladder: Payments — today a plain in-repo folder on the root `"Plugins\"` PSR-4 — becomes a composer package (the 382 Stage B mechanics) and then leaves the repo for `crm-plugin--payments` via `docs/plugin-extraction-runbook.md`, the first runbook run since the manifest layer landed and the first extraction of a **foundation** plugin (capability provider, webhook route, no widgets, no schema). Its novel element: Payments is the first plugin that owns a real third-party dependency (`stripe/stripe-php`), so this session establishes the pattern for a package carrying its own requires out of the root manifest.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**. No `deploy.yml` change is expected at all (the 385 precedent: a VCS-resolved package needs nothing from the deploy pipeline; the repo is public so `composer install` stays credential-free in every image-building context). The plugin contract (`docs/plugin-contract.md`) bumps 0.10.0 → 0.11.0 at close: reference implementations updated (first extracted foundation plugin), the Package shape's dependency posture recorded (a package owns its third-party requires; root drops its direct line), and any runbook deviations folded back. (The client-billing surface remains out of scope and guard-protected as always.)

**Session shape:** infrastructure, behavior-preserving, two-phase. Runtime behavior is byte-identical throughout: routes 272/272 in the dev/test environment, suite at the 386 baseline with assertions unchanged (only file-path re-points and guard-set migrations), no visual surface, no schema work. **Expect two iterations** (Stage B packaging → extraction), each independently committable and CI-green. **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (the ladder D1 opens — settled decisions: individual `crm-plugin--*` repos, everything leaves the main repo, per-vertical blocks run carve → package → extract end-to-end) and § 7 P6/P9/P10 lines (the three mechanics layers this session composes: path-repo packaging, the extraction runbook, the manifest).
3. Read the 386 session log (`sessions/386. Distribution Manifest & Central Build — Log.md`), especially its **Stage D kickoff input** section — what D1 needs from the manifest layer and the friction notes on the remaining hardcoded surfaces.
4. Read `docs/plugin-contract.md` at 0.10.0 — **The distribution manifest** section (the source-kind model this session walks Payments through: `folder` → `path` → `vcs`), Package shape (Stage B) for the packaging mechanics, External repo (Stage C-lite) for the extraction shape, and surface 13 (the capability layer Payments provides — must survive both moves with assertions unchanged).
5. Read `docs/plugin-extraction-runbook.md` end to end — this session is its second run and the first since step 5 gained the manifest edit. The worked-example lines are LogoGarden; Payments diverges where noted in the session prompt's survey.
6. Open `sessions/387. Payments Packaged & Extracted.md` — survey and front-loaded decisions settled at prompt review; the Open-Gate items are the owner-created repo and the Stripe-require move (decision 4).
7. Read the plugin being moved: all six files under `plugins/Payments/` (provider, `StripeCheckoutService`, `StripeWebhookController`, `ProductPriceObserver`, `StripeMode`, `routes/web.php`) — confirm the provider's `__DIR__`-relative loading and that nothing is location-keyed.
8. Read the guard + test surfaces that touch Payments: `tests/Feature/Infrastructure/PaymentModuleBoundaryTest.php` and `tests/Feature/Infrastructure/ConventionDriftTest.php` (the three `base_path('plugins/Payments/…')` file reads that re-point at extraction), `tests/Feature/PaymentsPluginSession380Test.php` + `PaymentsPluginRemovalSession380Test.php` (the behavior net that must pass unchanged), `tests/Feature/LogoGardenPaymentsDisabledViaFlagSession384Test.php` (Payments' vanish coverage — proves activation location-independence after the move), `tests/Feature/DistributionManifestGuardTest.php` + `tests/Feature/PluginPackageGuardTest.php` (the guards that automate what used to be manual runbook steps).
9. Read the build surface: root `composer.json` (the `stripe/stripe-php` require that moves into the package; the repositories block gaining a path then a vcs entry), `composer.lock` mechanics from 385's log if unclear, `Dockerfile` (the Events COPY lines are the ×2-stages pattern Phase 1 replicates for Payments and Phase 2 removes), `distribution.json` (the Payments entry whose source kind this session walks forward).
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry) and note the housekeeping-inbox posture for this shape.
11. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 386

- **CRM contract at v2.7.0** — 386 was non-boundary (last boundary touch: 372); `deploy.yml` untouched at 386.
- **Plugin architecture state:** P1–P10 complete; Stages A, B, and C-lite closed. `distribution.json` is the plugin-set + ordering authority (per-entry provider FQCN, source kind `folder`/`path`/`vcs`, package + constraint where one exists; array order = load order); `config/plugins.php` is generated by `plugins:manifest-sync` (single reader `App\Plugins\DistributionManifest`); the standing guard `DistributionManifestGuardTest` pins regeneration idempotence, per-source-kind lock pinning, composer repository/require alignment, and no-unmanifested-surfaces (lock packages, `plugins/` dirs, Dockerfile COPY lines, phpunit testsuites). `PluginPackageGuardTest`'s extracted set derives from the manifest's vcs entries — extraction is declared once, in the manifest. Composition-by-manifest proven by the 386 verified run. Plugin contract **v0.10.0**.
- **Payments today:** a `folder` entry in the manifest — six files at `plugins/Payments/`, no composer.json, no assets, no migrations, no in-plugin tests, no Filament pages; provider loads routes via `__DIR__`; capability `payments` + core-owned contracts (surface 13) are the consumption seam. `stripe/stripe-php ^16.6` is a **root** require. Events remains the only in-repo path package; LogoGarden remains the vcs precedent.
- **Planning state:** the 385 reframe stands — no launch; the finish line is the Stage D end state, then Stage E (niche distributions). D1 is the first of ~15–21 Stage D sessions; D2 (Events extracted) follows.
- **Schema baseline:** unchanged from 383. No schema work this session.
- **Fast Pest suite green at 3263 / 0** (386 close: 3249 + 4 manifest-guard + 10 manifest-reader unit tests). Expected delta this session: zero-to-small — path re-points and guard-set migration only; no new behavior.
- **Route baseline: 272 routes** (dev/test environment) — byte-identical this session. (386 note: the production image lists 270 — the two `dev.widgets.*` routes are dev-environment-conditional; not composition drift.)
- **Playwright suite** — CI signal; the known parked chrome-parity failure stands (pre-existing, non-gating). No local Playwright.
- **Housekeeping inbox** — the 386 capture buffer was empty at close. This session absorbs no inbox items (infrastructure shape; the open `[test-integrity]` items stay).

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Every Payments behavior test, the capability semantics, the webhook route, the removal mirror, and the flag-vanish coverage pass with assertions unchanged; only file paths and guard-set membership move. Routes 272/272 (dev/test). No visual surface, no schema.
- **The manifest is edited, never the generated file.** Both source-kind walks (`folder`→`path`, then `path`→`vcs`) happen in `distribution.json`; `plugins:manifest-sync` regenerates (a no-op for the provider list — same three FQCNs, same order); the guards verify the rest. Hand-editing `config/plugins.php` is a suite failure by construction.
- **Each phase lands CI-green.** Phase 1 (path package) needs the Dockerfile manifest COPY lines ×2 stages to build; Phase 2 (extraction) removes them. Lockfile changes ship in the same commit as their composer.json changes (the 385 discipline).
- **The dependency move is deliberate:** the package owns `stripe/stripe-php`; the root require drops in the same commit; the lock must prove identical resolution (same stripe version, now transitive). If resolution shifts, stop and surface it.
- **The runbook is the procedure and the deliverable check:** run it top to bottom for Phase 2; where Payments diverges from the LogoGarden worked example (no assets → gate 4 is n/a-with-a-note; no version-field history; dependency-carrying manifest), fold the deltas back into the runbook at close rather than leaving them oral.
- **Scope is binding.** No Events extraction (D2); no model/factory moves; no per-plugin CI (D11); no webhook branch inversions or Payments feature work; no FM surface; client billing stays guard-protected. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Front-end builds: none expected** (no widget/asset changes — no `build:public`, no `npm run build`).
- **Contract doc update at close:** 0.10.0 → 0.11.0 — reference implementations (first extracted foundation plugin), Package shape dependency posture (package-owned third-party requires), runbook deltas; changelog entry. Plan doc § Stage D D1 line marked.
- **D2 kickoff input at close:** record in the session log what D2 (Events extracted) inherits — the known Events-specific edges (fixture migration paths, the `Events` phpunit testsuite location, the EventsListing→BlogPager cross-plugin asset input, thumbnails) plus anything this run adds.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code, state the phase order, note any drift found — and confirm the two owner-facing items: **(1) the owner has created the empty public `crm-plugin--payments` repo** (the 385 precedent — owner-created, public for credential-free composer, checked out as a sibling of this repo), and **(2) the Stripe-require move** (decision 4: the package owns `stripe/stripe-php`, root drops its direct line). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
