# 304. UI/UX Sprint — Page-Builder Full-Screen + Quill Height Handle

Two batched authoring-ergonomics improvements to the admin tools: a page-builder full-screen toggle and a drag-resize height handle on the Quill rich-text editor. **No behaviour-removal, no schema change, no boundary touch, no contract bump — per-user state is localStorage only.** This resolves the "UI / UX Sprint" stub.

---

## Plan reference

This session executes `sessions/release-plan.md` **§ E8 (UI/UX Sprint)**. Read that entry first — gate `release`, no prerequisites, success criterion "Per existing stub," ~1 session. The canonical scope is the E8 entry **plus** the **"UI / UX Sprint" stub** in `sessions/session-outlines.md` (the two item descriptions). This prompt is the delta + grounding + the session-156 reconciliation.

---

## The two items (canonical text from the stub)

1. **Page-builder full-screen toggle.** A toggle that maximizes the page-builder canvas + inspector to the full viewport, hiding the Filament admin chrome (sidebar, top bar, breadcrumbs) for the duration. Persists per-user via localStorage. Exit returns to the previous chrome state.
2. **Adjustable height handle on the Quill editor.** The rich-text editor renders at a fixed height today; long content forces the wrapping page to scroll while the editor's internal area stays small. A drag-resize handle on the bottom edge so admins can grow the editor to whatever height fits the content. Per-user height persisted via localStorage. No schema change.

---

## Session-specific deltas

- **Session-156 prior art is the load-bearing reconciliation.** `resources/js/admin/fullscreen-toggle.js` already ships an admin-wide topbar fullscreen/chrome toggle (session 156, "Form Polish, Image Fields & Chrome Toggle"). The `session-outlines.md` "Layer Explorer" note explicitly asserts "the full-width toggle were delivered admin-wide in session 156's topbar fullscreen button — no longer needs a builder-specific implementation." **Before building item 1, read `fullscreen-toggle.js` and establish exactly what it hides/maximizes and how it persists.** Item 1 is the builder-specific *delta*: maximize the **builder canvas + inspector** (not just hide chrome generically), persisted per-user, exit restores prior state. If 156's toggle already covers the builder need on the four host pages, item 1 collapses to a verify-and-confirm (possibly a thin builder affordance, e.g. surfacing/scoping the existing toggle inside the builder) — **this is the first open question; do not rebuild 156's toggle.**
- **Two Quill surfaces exist.** The Filament-form editor (`app/Forms/Components/QuillEditor.php` → `resources/views/forms/components/quill-editor.blade.php` → `resources/js/admin/quill-editor.js`) and the Vue builder's `resources/js/page-builder-vue/components/fields/RichTextField.vue`. The stub describes the admin authoring editor. Whether item 2's handle lands on both surfaces or just the Filament-form one is an open question (recommendation below).
- **Four builder host pages.** The page-builder mounts in `edit-page`, `edit-post`, `edit-content-template`, `edit-page-template-chrome` blades. Item 1's toggle must behave consistently across all four (and degrade cleanly where the builder isn't mounted).

---

## Open questions to resolve at session start

Batch in plain prose at start; proceed on the recommendations if no objection (`memory/feedback_no_multiple_choice_questions`, `memory/feedback_dont_gate_on_taste_defaults`).

1. **What does session 156's `fullscreen-toggle.js` already deliver, and what is item 1's remaining delta?** Read it first. Recommendation: keep 156's admin-wide toggle as the chrome-hiding mechanism; item 1 adds only the builder-specific *maximize canvas+inspector* + per-user persistence + restore-prior-state that 156 does not already provide. If 156 fully covers it, surface that — item 1 may reduce to a confirmation + a small ergonomic (e.g. a builder-local toggle entry point) rather than new chrome logic.
2. **Quill height handle: both surfaces or just the Filament-form editor?** Recommendation: the Filament-form `QuillEditor` (the primary authoring surface the stub describes) first; apply to `RichTextField.vue` only if it's the same primitive and trivially shared — otherwise scope-fence the Vue builder Quill to a one-line follow-up note rather than expand the session.
3. **localStorage key shape.** Per-user persistence with no schema. Recommendation: a stable, namespaced key (e.g. `npc:builder-fullscreen`, `npc:quill-height:<editor-stable-id>`); height keyed so multiple editor instances on a page don't collide and a sensible min/max clamp is enforced. Keep it self-contained; no `SiteSetting`.

---

## Out of scope

- **Session 156's admin-wide toggle** — reused/extended, never rebuilt or duplicated.
- **Inspector Column — Drag-Resize Splitter** and **Layer Explorer** — separate `session-outlines.md` stubs; adjacent but not E8.
- **No schema / no per-user DB column** — localStorage only (stub-mandated).
- **No Fleet / `/api/health` / response-schema change** — non-boundary, v2.3.0.
- **No new admin page** expected — both items enhance existing surfaces (so no new help-doc stub / `getBreadcrumbs()` unless the work unexpectedly introduces a page).

---

## Phases (one session; natural Rule-11 cut-line between the two items)

### Phase 1 — Page-builder full-screen toggle

Reconcile against session 156 (open question 1), then deliver the builder-specific maximize (canvas + inspector to full viewport, Filament chrome hidden, exit restores prior state), persisted per-user via localStorage, consistent across the four host blades. Shippable on its own.

### Phase 2 — Quill drag-resize height handle

Drag-resize handle on the bottom edge of the Quill editor; per-user height persisted via localStorage with a sane min/max clamp; restores on reload. Filament-form `QuillEditor` is the primary target (open question 2).

**Cut-line:** if the session overflows Phase 1, stop cleanly; Phase 2 is the documented continuation and the stub carries forward Quill-handle-only.

---

## Testing

- **Slow test groups to run this session:** none expected.
- **New tests expected:** **yes**, for the objective bits — Playwright is the right tool here and stays in the suite: the toggle hides the expected chrome and restores prior state on exit; the full-screen state and the Quill height round-trip via localStorage across a reload; the Quill handle clamps to its min/max. Pure feel (does the maximize/resize *feel* right) is human-judgment — announce ready and stop.
- `memory/feedback_test_runs_not_parallel` — Pest and Playwright never run together. The session-303 e2e-CI migrate-before-run fix is in place; the isolated stack is `npm run test:e2e:isolated`.
- **Front-end build:** Vue/JS/SCSS source is touched — run `npm run build` before announcing completion. Confirm at close.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific:

- **Log file:** `sessions/304. UI-UX Sprint — Page-Builder Full-Screen + Quill Height Handle — Log.md`.
- **Artifact:** the builder-specific full-screen toggle (per-user localStorage, chrome hide/restore, consistent across the four host pages, reconciled with session 156) + the Quill drag-resize height handle (per-user localStorage, clamped) + new Playwright coverage; `VERSION` `0.304.x`.
- **Stub housekeeping (required on close):** mark the **"UI / UX Sprint" stub resolved** in `session-outlines.md` (or rewrite to Quill-handle-only if the Rule-11 cut-line was taken).
- **Branch:** `session-304/N` (final iteration).
- **Next session:** the execution order resumes at the next un-closed `release-plan.md` position after E8 unless the user elects another lift. Draft only when the user names it.
