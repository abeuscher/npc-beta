# 064. Public Theme Builder

Allow admins to configure public frontend colors, fonts, and logo from site settings. Implement as CSS custom properties driven from the database. No visual drag-and-drop designer — just the data-to-CSS pipeline.
