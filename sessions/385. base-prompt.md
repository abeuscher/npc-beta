# Base Prompt — Session 385 (One Plugin to Its Own Repo & the Extraction Runbook)

**Start now.** Execute the reading list below, open the session prompt at `sessions/385. One Plugin to Its Own Repo & the Extraction Runbook.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **385. One Plugin to Its Own Repo & the Extraction Runbook**.

This is **arc position P9** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary) — the opening Stage C-lite session. Stage C's full vision says everything eventually lives in its own repo; C-lite proves it with **one** round trip: one plugin package moves to its own repository, versioned and contract-disciplined exactly like the Fleet Manager relationship (the in-house precedent). **The extraction runbook is as much the product as the split itself** (owner directive, 376) — the owner intends to run the remaining extractions rapidly afterwards, so the repeatable step-by-step write-up is a first-class deliverable, not an afterthought.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**. (Plan § 8: Stage-C touches to `/api/health` or `deploy.yml`'s FM-facing behavior are future coordination items; P10, not P9.) The plugin contract (`docs/plugin-contract.md`) bumps 0.8.0 → 0.9.0 at close — the Package shape section gains the external-repo stage (VCS repository + tagged versions replacing the path repository for the extracted plugin), and surface 11 gains the vendor-path note. (The client-billing surface remains out of scope and guard-protected as always.)

**Session shape:** infrastructure / migration, behavior-preserving. The extracted plugin's runtime behavior, routes (272/272), widget output, and suite totals are unchanged — only where its code lives and how composer resolves it changes. The diff is: the plugin's files leave this repo for their own repository, root `composer.json` swaps a path repository for a VCS repository + tag, the Dockerfile drops the plugin's manifest COPY lines, guard globs extend to the vendor path, and `docs/plugin-extraction-runbook.md` is written from the run. **Expect one or two iterations** (extraction + green → runbook + docs). **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § 7 Stage C-lite (P9's line and the extraction-runbook directive), § 6 disposition 3 (per-plugin test suites; the central build runs core + the distribution's plugin suites), and § 4d (where P9 sits relative to the P10 distribution/central-build layer — P9 is the repo split only).
3. Read `docs/plugin-contract.md` at 0.8.0 — the **Package shape (Stage B)** section (the shape being promoted to an external repo; the explicit-`version`-field rationale that *inverts* under a VCS repository), surface 1 (activation is namespace-keyed and location-independent — nothing there changes), surface 11 (front-end assets; the `collectSources()` silently-skips-missing-paths caveat is the extraction's sharpest edge), and normative rule 5 (test-membership discipline).
4. Read the 384 session log (`sessions/384. Per-Install Activation & Workspace Assembly — Log.md`), especially its **P9 input** section — the inventory of what the in-repo posture bakes in (path repositories + explicit version fields, Dockerfile manifest COPY lines, guard globs over `plugins/*`, repo-relative asset paths, `basePath('plugins/…')` reaches in fixtures, the root `"Plugins\\"` PSR-4 overlap) and which of those LogoGarden does and doesn't trip.
5. Open `sessions/385. One Plugin to Its Own Repo & the Extraction Runbook.md` — the survey and front-loaded decisions there are settled at prompt review; do not reopen them. There are no open questions: the repo home (`abeuscher/crm-plugin--logo-garden`, public, empty, checked out at `../crm-plugin--logo-garden`) and the test-membership call (stay core) were both resolved by the owner at 384 close.
6. Read the extraction subject end to end: everything under `plugins/LogoGarden/` (provider, definition, template, `styles.scss`, `script.js`, `DemoSeeder`, `thumbnails/`, `composer.json`), plus its consumers: root `composer.json` (path repositories + the `^0.1` require), `composer.lock` (the path-package entry), `Dockerfile` (its two manifest COPY lines), `config/plugins.php` (unchanged), and `app/Services/AssetBuildService.php` `collectSources()` (how declared asset paths resolve off disk).
7. Read the nets: `tests/Feature/PluginPilotSession377Test.php`, `LogoGardenContractRetrofitTest.php`, `LogoGardenWidgetTest.php` (the LogoGarden-subject tests and the membership question they pose), `PluginPackageGuardTest` (lock-pinning + no-auto-discovery assertions that must survive the VCS swap), `WidgetTemplateBoundaryTest` + `PluginAdminCssGuardTest` (the `plugins/*` globs that must extend to the vendor path), and the 384 activation tests (`PluginActivationTest`, `LogoGardenPaymentsDisabledViaFlagSession384Test` — LogoGarden's vanish coverage, which must pass unchanged against the vendor'd package).
8. Read the image/ops surface: `Dockerfile` (both composer stages — what changes when one package arrives from a VCS repo instead of a path repo), `.github/workflows/tests.yml` + `deploy.yml` (where `composer install` runs and what network/auth it has), and the 382 log's in-image proof procedure (reused to prove the vendor'd package resolves in the built image).
9. Skim `sessions/session-outlines.md` (Plugin Architecture track entry) and `docs/fleet-manager-agent-contract.md`'s versioning discipline header (the FM relationship P9 is told to mirror — version header, changelog, both sides update before a boundary-touching change).
10. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 384

- **CRM contract at v2.7.0** — 384 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** arc P8 complete (384) — the per-install activation layer is live: `PLUGINS_DISABLED` (comma-separated kebab handles, derived from each provider FQCN's plugin-namespace segment — location-independent) read through `config/plugin-activation.php` and subtracted by `PluginServiceProvider`'s filter at the strip-the-line seam; unknown handles fail the bootstrap loudly; `config/plugins.php` untouched as the installed superset + sole ordering authority. The superset-image run proved one image / multiple compositions / enable-later `migrate`, end to end. Plugin contract **v0.8.0** (surface 1 two-layer rewrite + the tier-1 flip runbook). LogoGarden and Events are composer path-repo packages; Payments is still a plain in-repo folder (root `Plugins\` PSR-4 mapping still required).
- **Repo-boundary state:** everything is one repo. Both packages resolve through root path repositories with explicit `version` fields (the no-git-metadata determinism device — inverted by a VCS repo, where tags are the metadata); the Dockerfile carries per-plugin manifest COPY lines ×2 stages; guard globs scan `plugins/*`; LogoGarden's asset paths are declared repo-relative (`plugins/LogoGarden/styles.scss`). This is the posture P9 breaks for one plugin.
- **Schema baseline:** unchanged from 383 — core dump core-only (114 rows); the three events tables plugin-owned. LogoGarden owns no schema, no migrations — deliberately the simplest extraction subject.
- **Fast Pest suite green at 3248 / 0** (384 close: 3227 + 21 activation tests), three testsuites (Unit, Feature, Events). Expected delta this session: additive guard extensions only — no test moves (LogoGarden-subject tests stay core, owner ruling at 384 close).
- **Route baseline: 272 routes** — byte-identical this session (LogoGarden registers no routes).
- **Playwright suite** — baseline preserved; CI is the signal; the e2e stack builds the image, so it re-proves the VCS-resolved composer install cold.
- **Planning state:** 384 close marks P8 ✅ in the plan doc + track entry; Stage B is complete; the owner accepted the superset-image reading of workspace assembly at the 384 Open Gate (build-side generalization is P10).
- **Housekeeping inbox** — the 384 capture buffer was empty at close. This session absorbs no inbox items (infrastructure shape; the open `[test-integrity]` items stay).

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Routes 272/272, widget bundle content, suite assertions, fresh-install identity, and every composition of the activation layer are byte-identical. Only the resolution path of one package changes.
- **The remove-the-line and flag guarantees survive verbatim.** `config/plugins.php` still lists the provider FQCN; `PLUGINS_DISABLED=logo-garden` still vanishes it (the 384 generalization test is the net and must pass with assertion content unchanged). Activation stays namespace-keyed — the extraction must not introduce any location-keyed activation logic.
- **The runbook is a deliverable, not a log.** `docs/plugin-extraction-runbook.md` is written as the generic repeatable procedure (numbered steps, decision points, verification gates) with LogoGarden as the worked example — the owner runs the next extraction from it without this session's context.
- **The surface-11 caveat is the sharpest edge:** `collectSources()` silently skips asset paths that don't exist on disk. After the vendor move, run `widgets:sync` + `build:public` and verify the manifest and bundle actually carry LogoGarden's SCSS/JS content — a green suite alone does not prove the bundle (the pilot's paths-exist-on-disk sync test is the standing net; make sure it covers the new paths).
- **Composer hygiene:** `composer validate --strict` clean; lockfile updated in the same commit as the manifest change; `PluginPackageGuardTest`'s pinning assertion updated to accept the VCS dist type for the extracted package while still enforcing no-auto-discovery.
- **Scope is binding.** No distribution manifest, no central-build generalization, no `deploy.yml` FM-facing changes (all P10); no Payments packaging or Events extraction; no per-plugin npm workspace; no new third-party dependencies. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Front-end build: `build:public` runs this session** (asset paths move), plus the bundle-content verification above. `npm run build` is not expected (no Vite-compiled source changes).
- **Contract doc update at close:** Package shape gains the external-repo stage (VCS repository, tagged versions, the dropped explicit `version` field and why, auth posture); surface 11 gains the vendor-path note; bump to 0.9.0, changelog entry.
- **P10 input at close:** record in the session log what the extraction revealed for the distribution manifest + central build (P10): what the root repo still hardcodes about which plugins exist (composer require lines, config/plugins.php, guard lists), and what the runbook shows a manifest must generate.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code AND the target repo's state (still empty, still public, remote reachable), state the phase order, and note any drift found — then **wait for the owner to confirm before starting.** No open questions remain (repo + test membership were settled at 384 close). One confirmation, then run the session to its binary finish.
