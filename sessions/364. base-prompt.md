# Base Prompt — Session 364 (Test Audit — Cycle 2: Mutation Slice + Pest Relevance)

**Start now.** Execute the reading list below, open the session prompt at `sessions/364. Test Audit — Cycle 2 (D3 Mutation Slice + D4 Pest Relevance).md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **364. Test Audit — Cycle 2 (D3 Mutation Slice + D4 Pest Relevance)**.

This is **not a `release-plan.md` entry** — a cadence-triggered cycle of the **Test Audit track** (`sessions/tracks/test-audit.md`, canonical for cycle shape, dimensions, and cadence; the track doc's note that release-plan carries scheduled cycle entries is deliberately not exercised here, matching Cycle 1/A005 which also ran without one). The trigger was ≈ session 365; the owner pulled it to 364 at the 363 close — housekeeping is drained, the guided-tour track is still paused awaiting external demo feedback, and A2 (Fleet Manager node-operations parity) waits on owner readiness to coordinate the FM repo.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope (a D3 mutation slice may *read* the Fleet controllers; it changes only tests). CRM contract stays at **v2.5.0**. No Cross-Repo block or `docs/fleet-manager-agent-contract.md` update.

**Session shape:** **audit-style, single session** (the track's default; carve out a successor only if a dimension's dispositions overflow — never truncate under cadence pressure). Cycle 2 runs the two dimensions Cycle 1 queued: **D3 (mutation-testing slice)** and **D4 (Pest file relevance + fast/slow classification refresh)**, plus drainage of in-scope `[test-integrity]` inbox items. Test-code and doc changes only — no application code.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference.
2. Read `sessions/tracks/test-audit.md` **in full** — it is canonical for this session: the cycle shape, the D1–D4 dimension procedures, the D3 forward queue (from s241), the standing improvements, and the Cycle 1 retrospective (A005).
3. Read `docs/testing/mutation-audits.md` (the D3 workflow — Infection + PCOV + the `bin/pest-for-infection.php` shim) and `docs/testing/playwright-discipline.md` (Cycle 1's D1 deliverable; consult if any disposition touches a Playwright spec).
4. Read `sessions/session-outlines.md`'s Active-tracks block, then the `[test-integrity]` items in `sessions/housekeeping-inbox.md` (the localization-readiness scoping item is the live actionable one; the A003/A005 deletion records are informational and stay).
5. Read the **session 363 log** (`sessions/363. Housekeeping — Mailing-List Filter Repair + Inbox Batch — Log.md`) — it carries the three-branch close state (session-363/1 … /3, all independent off main), the expected post-merge Pest baseline, and the **unreproduced single-failure fast-suite run** observed at 363 (one uncaptured intermittent among four otherwise-green runs) that this cycle should try to identify via CI history.
6. Read `docs/app-reference.md` for the test runner and environment names. **Pest and Playwright run sequentially, never in parallel** (agent memory `feedback-test-runs-not-parallel`).

---

## Starting state inherited from session 363

- **CRM contract at v2.5.0** — unchanged; 363 was non-boundary (housekeeping batch). 364 is non-boundary.
- **Schema baseline:** no migration since 359's `users` 2FA columns. 364 expects **no schema change** (test-code + docs only).
- **Fast Pest baseline:** 363 closed as three independent branches — /1 anchor (2976/0), /2 batch picks (2984/0), /3 stretch (2977/0, run twice). **Expected post-merge baseline ≈ 2993 / 0** (2972 main + 4 + 12 + 5 new tests) — verify with a fresh `./dev test` at session start before auditing anything.
- **Playwright suite:** unchanged by 363 — the three standing per-tour specs (6 tests) + the `@on-demand` storyboard regenerator, green vs the isolated stack at the 362 close. D4/D3 work should not touch them; any Playwright disposition goes through `docs/testing/playwright-discipline.md`.
- **Deploy state:** `0.362.01` live on the demo node awaiting external tour feedback; 363's `0.363.03` awaiting the owner's merge (all three branches, one push). **Feedback lands mid-flight → it interrupts** (tour copy rulings + rework take precedence when the owner says so).
- **Housekeeping inbox:** drained to ~8 items at 363 (7 shipped + 1 promoted). Remaining `[test-integrity]`-flavoured: the localization-readiness scoping sweep, plus the untagged-but-test-shaped `[0.324.01]` widget-knockout Playwright guard — both candidates for this cycle's drainage at the gate.
- **Planning state:** guided-tour track paused awaiting feedback; template-conversion tool sits in the inbox's promotion candidates with its design sketch (needs a release-plan entry when scheduled); Code Shape & Fit trigger ≈ 379; Code Review & Cleanup trigger ≈ 398.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The track doc's dimension procedures are the method.** D3 follows `docs/testing/mutation-audits.md` verbatim (slice from the s241 forward queue; net-new coverage for surviving mutants is out of scope — the deliverable is fewer, sharper tests via mutation evidence, `// guards:` override-keep markers where redundancy is deliberate). D4 gives each examined Pest file a keep / update / remove verdict and verifies the fast/slow >5s boundary + the pinned `design`-group list.
- **Bound D4 before running it.** The suite is ~200+ Feature files; a full per-file walk is not one session. Bring a bounding proposal to the gate (see Open Gate item 2).
- **Every disposition is recorded** — deletions and renames land `[test-integrity]` paper-trail lines naming the coverage tradeoff, per the Cycle 1 pattern.
- **The A005 lesson applies:** a `[test-integrity]` entry naming a cluster still needs per-test verification at drain time — no blanket actions.
- **No application code.** If the audit surfaces a production bug (not a test defect), capture it via `npm run logbug` for the inbox rather than fixing it mid-audit.
- **Close-gate artifacts:** this session closes a cycle → the track doc gets a Cycle 2 retrospective + refreshed status snapshot (next trigger ≈ 414), per the phase-expiry compression rule.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) the **D3 slice pick** from the s241 forward queue (`AppearanceStyleComposer`, Importer `FieldMapper`, page-builder save flow, Filament resource action visibility, Fleet Manager controllers) with a lean — the Fleet controller slice has grown most since the queue was written (health / logs / backup trigger / blob / admin recover, v2.1.0 → v2.5.0) and is security-adjacent; (2) the **D4 bounding proposal** (which file set gets the keep/update/remove walk this cycle, and what defers to Cycle 3); (3) which `[test-integrity]` items this cycle drains (localization scoping sweep; the knockout-guard item if the owner wants it treated as D1 work); (4) whether to spend a timebox identifying the **363 unreproduced fast-suite failure** via CI run history before deciding anything about it. One confirmation, then proceed.
