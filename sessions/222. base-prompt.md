# Base Prompt — Session 222

We are about to begin a new session: **222. Widget Primitive — Phase 4f (BlogPager Retrofit + PageContext::posts Retirement)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session writes no migrations and no schema changes — `pages` and `media` are the surfaces involved; read `docs/schema/pages.md` and `docs/schema/media.md` only if you need to confirm column shape during projector work.
3. Read the two framing documents for this architectural track. **These are the north star; everything in the session prompt assumes you have read them.**
   - `sessions/widget-primitive.md` — the premise.
   - `sessions/widget-primitive-migration.md` — the migration sketch. Phase 4 retrofit is mid-campaign; BlogPager is the seventh of ~12 candidate widgets and the first one that retrofits a **non-list-shaped post-consumer** — its template walks the post list to find prev/next of the current page rather than rendering all items.
4. Read the most recent Phase 4 session logs to understand the trail of decisions this session lands on top of:
   - `sessions/archived/216. Widget Primitive — Phase 4a (BlogListing Retrofit — Audit, Test-Lock, Cleanup) — Log.md` — the post projector (`SystemModelProjector::projectPost()`) was first built here; 222 extends it with one author field.
   - `sessions/archived/217. Widget Primitive — Phase 4b (EventsListing Retrofit) — Log.md` — established the `_label` projector-key naming convention. Established the precedent for retiring a `PageContext` helper in the same session that retired its sole caller (`upcomingEvents()` retired alongside EventsListing). 222 mirrors that move with `posts()` and `pagesCache`.
   - `sessions/221. Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer) — Log.md` (or `sessions/archived/` if 222 close has moved it) — the query-settings architecture and the `QuerySettings` value object on `App\WidgetPrimitive`. BlogPager is **not** list-shaped and **does not** declare a `QuerySettings`; it returns null from `querySettings()` (the parent default).
5. Read the full state of every file this session edits. Specifically:
   - `app/Widgets/BlogPager/BlogPagerDefinition.php` — 49 lines. No `dataContract()` today. Add `dataContract()` returning `SOURCE_SYSTEM_MODEL`/`'post'` with the field whitelist used by the template's prev/next render. Do not declare `querySettings()` — BlogPager has no panel.
   - `app/Widgets/BlogPager/template.blade.php` — 72 lines. Lines 6–14 walk `$pageContext->posts()` and search for the current page's id to find prev/next. Lines 28–37 build a token map from a raw `Page` model (`$post->getFirstMediaUrl`, `$post->title`, `$post->author?->name`, etc.). Rewrite the template to walk `$widgetData['items']` instead — the current page is identified by the contract DTO's `id` field; prev/next are derived from adjacent positions in the items list. The token resolver builds tokens from the projected DTO row, not from a model.
   - `app/WidgetPrimitive/Projectors/SystemModelProjector.php::projectPost()` — currently emits 8 fields (`id`, `title`, `slug`, `url`, `published_at`, `published_at_label`, `excerpt`, `image`). Add `author_name` (a derived string from the `User` relation, fail-closed empty when null). The contract whitelist BlogPager declares includes `author_name`; other contracts (BlogListing) do not declare it and so do not receive it (per-field fail-closed).
   - `app/Models/Page.php` — confirm the `author` relation exists and is `belongsTo(User::class)`. The projector's `$post->author?->name` access will need an eager-load to avoid N+1 — extend `ContractResolver::resolvePost()`'s `with(...)` list from `'media'` to `['media', 'author']`.
   - `app/WidgetPrimitive/ContractResolver.php::resolvePost()` — extend the eager-load to `['media', 'author']`. The COALESCE default ordering established in 221's resolveOrderBy() preservation stays — no other change.
   - `app/Services/PageContext.php` — `posts(?int $limit = null)` (lines 29–40) and `?Collection $postsCache = null` (line 16) become unused after BlogPager retrofits. Delete them. Verify by repo-wide grep before deletion. (Mirror of 217's `upcomingEvents()` retirement.)
   - `resources/docs/widget-types.md` lines 94, 135, 153 — three prose blocks reference `$pageContext->posts()`. Edit conservatively to remove or update.
6. Read the four contract retrofit test files most relevant to the post arm:
   - `tests/Feature/BlogListingContractRetrofitTest.php` — Test B's COALESCE-preservation pin (`$postSelects[0]['query'])->toContain('COALESCE(published_at, created_at)')`). This must remain byte-equivalent — adding `'author'` to the eager-load list adds **one new query** (a `users` select) to BlogListing's render; bump that test's expected query count by one or assert the users-select count separately. Mid-session verification.
   - `tests/Feature/EventsListingContractRetrofitTest.php` — for the analogous query-count adjustment pattern (the eager-load on `landingPage` was bumped in 217 with the same shape).
7. Read `tests/Feature/PageBuilderApiTest.php` — confirm BlogPager has no specific tests there; nothing to update unless `apiWidget(..., 'blog_pager')` shows up.
8. Read `app/Widgets/BlogPager/script.js` if it exists — confirm whether any front-end JS reads post fields. If yes, the field-name whitelist on the contract must accommodate the JS's reads.
9. Open `sessions/222. Widget Primitive — Phase 4f (BlogPager Retrofit + PageContext::posts Retirement).md` and read the session prompt carefully.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal (Spatie GD image processing) documented since session 212 and confirmed working in 216–221. Fix any failures before proceeding. If the session prompt specifies slow test groups to run, run those separately as well.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime (widget CSS/JS, a new or renamed widget asset, a row in `widget_types.assets`, a manifest-lib declaration), run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* Match the touched path to its build command:
  - `resources/js/**` or `resources/scss/**` (including `public.scss` and any partial it imports) → `npm run build`. The admin bundle, the public site CSS, and the page-builder Vue app all compile through Vite, so this one command covers all three surfaces. **Not expected this session unless `BlogPager/script.js` is touched.**
  - `resources/scss/widgets/**`, the `css` / `js` columns of any `widget_types` row, any file referenced in a `WidgetType.assets` array, or a new/renamed widget folder under `app/Widgets/{PascalName}/` whose definition declares assets → `docker compose exec app php artisan build:public`. Not expected this session — no widget asset files touched.
  - `public/css/admin.css` is hand-edited and served directly — **no build** required.
  - When in doubt and a widget's runtime output could have changed, run `build:public` — it's cheap, and a stale manifest silently breaks downstream consumers.
- **Tests for new behaviour:** every session that changes application behaviour (new models, controllers, routes, scopes, or logic changes) should include tests unless the session prompt explicitly says otherwise. Pure CSS, template, or copy sessions may skip tests. Follow existing test conventions — Pest syntax, `RefreshDatabase`, factory-based setup.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite. The fast suite should complete in under 5 minutes.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing or structural changes that affect CI).

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/223. base-prompt.md`. Expected next session: continued Phase 4 retrofit on the remaining 4 candidates — the Event single-row pair (EventDescription + EventRegistration; both walk `$pageContext->event($slug)` and need a "find one by slug" pattern in `ContractResolver`) or the Product pair (ProductCarousel + ProductDisplay; require a new `'product'` arm in `ContractResolver` and `SystemModelProjector`). Scope to be agreed with the user.
- **Next session prompt**: draft `sessions/223. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/222. Widget Primitive — Phase 4f (BlogPager Retrofit + PageContext::posts Retirement) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: mark Phase 4f complete under the Widget Primitive track stub. Note the 4 remaining widgets in Phase 4 (the Event pair + the Product pair). Note that `PageContext::posts()` and `pagesCache` are retired this session.
- **Archive the previous session**: move all files matching `sessions/221. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-222/N` branch, and notify the user. Do not push — the user will push and merge when ready.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive. Move friction to the UX layer, not the protection layer. (Captured in `feedback_security_over_usability` memory.)
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles. (Captured in `feedback_no_collapse_ui` memory.)
