# Base Prompt — Session A001 (Content Import/Export — Extend to Events, Products, Navigation, Collections — Agentic)

> **Scheduled as session A001 — agentic.** Runs autonomously in an isolated worktree alongside regular session work; the `A###/N` branch prefix distinguishes it from the user-driven `NNN/N` sequence. Backend-only pattern-matching: replicate the page/template/design export+import shape across four more entity types (events, products, navigation, collections). No UI work, no architectural calls. Pest round-trip per entity is the binary acceptance signal.

**Start now.** Execute the reading list below, open the session prompt at `sessions/A001. Content Import-Export — Extend to Events, Products, Navigation, Collections.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **A001. Content Import/Export — Extend Bundle Coverage to Events, Products, Navigation, Collections.**

This is **autonomous backend work.** The session prompt fully specifies the deliverable, the four phases, the hard-no list, and the acceptance criteria. No mid-session UX decisions; no architectural calls; no UI surface. Either the four round-trip Pest tests pass or they don't.

This session is **NOT boundary-touching** — no Fleet Manager surface, no widget changes, no UI work, no theme/typography touches. **CRM contract stays at v2.3.0.** **No DB schema change.** **No bundle envelope `format_version` bump** (stays at 1.1.0; growth is additive opt-flags + new payload keys).

**Session shape:** four phases, one per entity. Each phase: add exporter method → extend analyzer → add importer method → add round-trip Pest test → fast suite green. Each phase is self-contained on the `session-A001/N` branch. Past six commits, stop and surface — that's a carve-out smell.

Before doing anything else:

1. **Read the full session prompt** at `sessions/A001. Content Import-Export — Extend to Events, Products, Navigation, Collections.md` — per-entity scope, hard-no list, acceptance criteria, phase order. The session prompt is the load-bearing document.
2. **Read session 309's close-log** + the resulting `app/Services/ImportExport/ContentExporter.php` + `ContentImporter.php` — the existing page / template / design shape is the template for the four new entities.
3. **Read `app/Services/ImportExport/ContentImporter.php` in full** — specifically the `import_id_maps` / `findByExternalId` / `importPage` / `importTemplate` patterns. The four new entities follow these patterns exactly.
4. Read the relevant Eloquent models per entity:
   - Events: `app/Models/Event.php`, `app/Models/TicketTier.php`, `app/Models/EventRegistration.php`. Relationships to contacts via the existing id-map plumbing.
   - Products: `app/Models/Product.php`, `app/Models/ProductPrice.php`. Images flow through the existing media payload.
   - Navigation: identify the `SiteSetting` row(s) holding the navigation tree. Read the Navigation editor controller for the canonical write path; mirror it.
   - Collections: `app/Models/Collection.php`, `app/Models/CollectionItem.php`. Custom `data` JSON travels verbatim.
5. Read `tests/Feature/ContentExporterTest.php` + `tests/Feature/ContentImporterTest.php` post-309 — the test shape for the four new round-trip cases. Mirror exactly.
6. **Confirm the hard-no list** in the session prompt before any edit. If a planned change would touch any file under the hard-no list, STOP and surface — scope has drifted.

---

## Starting state inherited from session 309

- **CRM contract at v2.3.0** — unchanged.
- **Schema baseline:** none added by 309.
- **Existing exporter + importer surface** covers pages, templates, design (theme_colors/typography/button_styles), and media. The analyzer's manifest shape is established.
- **Opt-flag pattern** is established (`merge_design`, `replace_duplicate_pages`, `import_media`). This session adds analogues per new entity if and only if 309's existing flags don't already cover (most new entities slot into the existing flag set; only navigation may need a flag of its own — judge at session start).
- **Build/version discipline:** bump `VERSION` to `0.A001.<iteration>` each iteration. (Note the `A001` slot in the version string — distinguishes agent work from numeric sessions.)

---

## Process rules for every session

Base-prompt process rules apply, with **agentic-specific tightening**:

- **Hard-no list is binding.** Per the session prompt: do not touch `WidgetRenderer`, `TypographyCompiler`, `AssetBuildService`, `ColorTokenCompiler`, anything under `app/Widgets/`, anything under `resources/scss/`, anything under `app/Filament/Pages/`, the Fleet Manager surface, or migration files. If any phase requires it, STOP and surface.
- **Acceptance is binary.** Pest fast suite green + four round-trip tests pass + no hard-no file modified + `git diff main…HEAD` touches only `app/Services/ImportExport/`, `tests/Feature/`, the session log, and `completed-sessions.md`. If any check fails, iterate; if iteration count exceeds six, STOP and surface.
- **No UI work.** This session does not create, modify, or delete any Filament page, Vue component, Blade template, or SCSS file. UI for the new entities lives in the per-entity admin Resources (which exist already and are not in this session's scope).
- **No envelope changes.** `format_version` stays 1.1.0. New entities slot into `payload` as new top-level keys (`payload.events`, `payload.products`, etc.); the analyzer manifest grows additively.
- **Phase order is recommended, not required.** Recommendation: Navigation → Products → Events → Collections (smallest blast radius first). The agent may reorder if shared plumbing emerges that wants extraction first.
- **PM-level tone for status summaries.**
- **Sequential test runs** — Pest then Playwright (no UI work here, so Playwright is just a sanity re-run of the existing suite).

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Agentic close gate.** The agent self-closes when all four phases are complete and acceptance criteria are met. No user "say to close" — the work either lands or it doesn't. The agent's final message reports:

1. The four new round-trip test names + pass status.
2. The Pest fast suite count + delta vs. session 309 close.
3. The list of files modified (must match the diff scope expectation).
4. Confirmation that no hard-no file has been touched.
5. Whether any phase surfaced a smell that warrants a follow-up note in the close log.

If acceptance fails after six iterations on any single phase, the agent stops and surfaces a partial-completion report instead of attempting more iteration.

### Phase 2 — Close

Write `sessions/A001. Content Import-Export — Extend to Events, Products, Navigation, Collections — Log.md`. Update `completed-sessions.md` — new A001 row, dated, tagged "(agentic)" in the description. Do not archive any numeric session. Commit on the final `session-A001/N` branch, notify, do not push. Never push to main / force-push / merge to main. The user merges A001 manually after review.

---

## Style rules

Base-prompt style rules apply. Agentic-specific:

- Iteration commit messages describe the deliverable per phase ("A001/N — events exporter + importer + round-trip Pest").
- PM-level tone in the final status report.
- No docstrings/comments/type-annotations on code the agent did not write.
- **Extend, don't reinvent.** The four new entities follow the page/template/design pattern exactly. Any deviation needs justification in the session log.
- **No new abstractions.** If a shared helper looks tempting, write the four entity methods explicitly first; extract on the second iteration if the duplication is real. Don't speculate.
