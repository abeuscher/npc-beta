# 065. CDN Integration

Add CDN integration points for static assets and uploaded media. Configure asset URLs to route through a CDN when a CDN base URL is set in site settings, with a transparent fallback to local serving when it is not.
