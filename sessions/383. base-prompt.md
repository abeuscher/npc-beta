# Base Prompt — Session 383 (Events Vertical as a Package & Squash Boundary Redraw)

**Start now.** Execute the reading list below, open the session prompt at `sessions/383. Events Vertical as a Package & Squash Boundary Redraw.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **383. Events Vertical as a Package & Squash Boundary Redraw**.

This is **arc position P7** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary) — the second Stage B session. P7 applies the P6-proven composer path-repository mechanics to the Events vertical (`nonprofitcrm/events`, own `composer.json`, activation still owned by `config/plugins.php`), **redraws the squash boundary** (the three events tables leave core's schema dump and become plugin-owned create-table migrations; deterministic install order core dump → enabled plugins' migrations → seeders; per-composition fresh-install identity checks per plan doc § 6 disposition 7 and the § 6.7 annotation), and **begins the test-monolith dismantling** (the events-slice test files move to `plugins/Events/tests/` as their own testsuite, reduction pass riding along per § 6 disposition 3).

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**. The plugin contract (`docs/plugin-contract.md`) bumps 0.6.0 → 0.7.0 at close — surface 5 (Migrations) is the session's promotion to PROVEN, with the disabled-vs-never-installed semantics recorded (see the session prompt's decision 4). (The client-billing surface remains out of scope and guard-protected as always.)

**Session shape:** infrastructure / behavior-preserving. No route or visual change; the schema change is a boundary redraw, not a shape change — every composition that includes Events must produce today's exact schema. The diff is a package manifest, plugin-owned migrations, a shrunken core dump, moved test files, and docs. **Expect two or three iterations** (package → schema redraw → test move are each independently committable). **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § 6 dispositions 3 and 7 (tests per plugin; migrations mechanics + the § 6.7 events-owned annotation — the prepared input this session executes) and § 7 (the arc), and `docs/plugin-contract.md` v0.6.0 — surfaces 1, 5, 6, 13, the **Package shape (Stage B)** section (the P6 mechanics this session mirrors), and the changelog's 0.6.0 entry.
3. Read the 382 session log (`sessions/382. First Composer Package & Migration Squash — Log.md`), especially its **P7 input** section — the package-mechanics learnings (explicit version field, bound constraints vs `composer validate --strict`, the Docker COPY glob-flattening caveat) are load-bearing for Phase 1.
4. Open `sessions/383. Events Vertical as a Package & Squash Boundary Redraw.md` — the survey and front-loaded decisions there are settled at prompt review; do not reopen them.
5. Read the schema surface: the three events-table DDL blocks in `database/schema/pgsql-schema.sql` (including FKs and indexes — the migrations must reproduce them byte-faithfully), `docs/schema/{events,event_registrations,ticket_tiers}.md`, `docs/schema/README.md` (the 382 squash note), and `database/factories/{Event,EventRegistration,TicketTier}Factory.php` (staying core, per decision 2).
6. Read the packaging surface: root `composer.json` (the existing path repository + require), `plugins/LogoGarden/composer.json` (the shape to mirror), `config/plugins.php`, `app/Providers/PluginServiceProvider.php`, `plugins/Events/EventsServiceProvider.php` (gains `loadMigrationsFrom`), both manifest-COPY blocks in `Dockerfile`, and `tests/Feature/PluginPackageGuardTest.php` (already covers the new manifest via glob).
7. Read the nets whose mechanics this session touches: `tests/Fixtures/Plugins/EventsRemovedTestCase.php` (+ `PaymentsRemovedTestCase` for contrast — Payments' fixture must NOT change), `tests/Feature/EventsPluginRemovalSession381Test.php` (the data-kept assertions that force decision 4), `tests/Feature/EventsPluginSession381Test.php`, `tests/Feature/EventsWithoutPaymentsSession381Test.php`, `phpunit.xml` (testsuites), and the standing guards (`EventModuleBoundaryTest`, `PaymentModuleBoundaryTest`, `ConventionDriftTest`, `WidgetTemplateBoundaryTest`, `PluginAdminCssGuardTest`, `WidgetColorTokenConsumptionTest`, `PluginPackageGuardTest`).
8. Inventory the test move before moving anything: list the events-titled Feature files and grep `tests/` for event-factory usage; apply the session prompt's decision-6 membership rule and record the move list in the work log before the first `git mv`.
9. Skim `sessions/session-outlines.md` (Plugin Architecture track entry) and `docs/app-reference.md` § Build server / § Environments.
10. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 382

- **CRM contract at v2.7.0** — 382 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** arc P6 complete (382) — LogoGarden is the first composer package (`nonprofitcrm/logo-garden` 0.1.0, path repository, relative vendor symlink, committed lock, `composer validate --strict` clean); `config/plugins.php` confirmed as the sole activation + ordering authority through Stage B (contract surface 1 amended — auto-discovery deliberately unused, now guard-enforced by `PluginPackageGuardTest`); Dockerfile carries the manifests-before-source plugin-COPY rule in both stages, proven by a local image build and CI. Plugin contract **v0.6.0**. Payments and Events are still plain in-repo folders.
- **Schema baseline:** the T1 squash landed at 382 — **zero live migrations**; `database/schema/pgsql-schema.sql` regenerated (migrations table at 114 core rows); squash note at 382 / 2026-08-18. The three events tables are still in the dump — moving them out is this session's Phase 2.
- **Fast Pest suite green at 3229 / 0** (382 close; +3 over the 381 baseline: `PluginPackageGuardTest`). Expected delta this session: files *move* to the `Events` testsuite; total shifts only by logged reduction deletions and any additive redraw guard.
- **Route baseline: 272 routes** — byte-identical before/after this session (nothing touches routing).
- **Playwright suite** — baseline preserved; does not run this session, CI is the signal. Note: Playwright's `beforeAll` runs `migrate:fresh --seed` on the e2e stack — with Events enabled there, the deterministic install order covers it; no spec changes expected.
- **Planning state:** 382 close marks P6 ✅ in the plan doc + track entry; the plan doc's P6/P7 lines already carry the owner-approved squash-split wording (aligned at 381/382).
- **Housekeeping inbox** — the 382 capture buffer was empty at close; nothing folded. This session absorbs no inbox items (infrastructure shape; the open `[test-integrity]` items (2)–(3) stay in the inbox).

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** No route changes (272/272 byte-identical), no visual change, no new third-party dependencies. The schema redraw is boundary-only: the full composition's schema is identical before and after (the identity check is the proof); the events-absent composition is a *new* provable state, not a change to an old one.
- **The config list is sacred.** Same rule as 382: `config/plugins.php` remains the sole activation + ordering authority; no auto-discovery; every strip-the-line test passes with its assertion content unchanged.
- **The migrations must reproduce the dump DDL byte-faithfully.** Author the three create-table migrations from the current dump blocks (columns, defaults, FKs, indexes, sequence ownership); the full-composition identity check (schema diff modulo migrations-table rows) is the oracle. Run both identity checks before deleting anything from the dump in your working tree.
- **Decision 4's fixture mechanics are the one delicate seam.** `EventsRemovedTestCase` must keep the events schema on the test database while stripping activation — Payments' fixture is untouched, and no existing mirror assertion changes. If the mechanism you pick can't satisfy that, stop and surface it — that is a genuine design fork, not drift.
- **The test move is `git mv` + path/namespace updates, then reduction — in that order.** Move first with the suite green, reduce second with each deletion logged. Never combine a move and a behavior edit in one commit.
- **Scope is binding.** One package (Events). No Payments packaging, no model/factory moves, no per-install activation, no separate repos, no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Front-end build: none expected.** Widget folders do not move; asset paths do not change. If that assumption breaks, stop — it means scope drifted.
- **Contract doc update at close:** surface 5 → PROVEN (plugin-owned migrations, deterministic install order, both vanish semantics, per-composition identity procedure); surface 6 seeder-posture note; package-shape section gains the second consumer + per-plugin manifest-COPY rule; bump to 0.7.0, changelog entry.
- **P8 input at close:** record in the session log what the events-absent composition revealed for the per-install activation layer (P8) — the first fresh-install-without-a-vertical is that session's dress rehearsal.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code (the events-table DDL blocks, the Events plugin file set, the fixture mechanics, the test-move inventory), state the phase order and any drift found — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
