# 366. Multi-Node Operational Readiness — Four Production Nodes + Post-Deploy Restore Verification

Stand up (or regularize) the four production nodes the release plan requires — marketing site, demo install, test/deploy instance, spare-for-first-customer — document the fleet in a node inventory, point Fleet Manager at all four, and use the fresh provisioning as the vehicle for the live verification session 365 deferred: prove a cross-node restore completes end-to-end on the fixed backup format.

---

## Plan reference

This session executes `sessions/release-plan.md` § A3 (execution-order position 47). That entry is canonical for the success criterion — four production nodes, each with purpose + URL + creds-location documented, FM monitoring all four, the test/deploy instance established as the rehearsal target — and for the artifact (the node inventory doc). Prerequisites met: A2 closed CRM-side at 365; E1 closed at 249.

---

## Session-specific deltas

What the plan entry doesn't know:

- **The fleet partially exists.** A demo node runs live (last known `0.362.01`); FM 047's public-website provisioning profile shipped for the marketing-site case; other droplets may exist in various states (the FM client registry is ground truth — first gate item). The session is as much *regularize + document what exists* as *provision what's missing*. Expect the split to be settled at the gate once the registry is read.
- **The 365 verification rider.** Session 365 fixed cross-node restore portability but deferred live proof (owner ruling — no mid-session deploy). The full sequence lives in `/home/al/fleetmanager/sessions/a2-runbook-handoff-from-crm.md`: fleet on ≥ 0.365.01 → fresh backup on one node → restore that blob on a different node (or the FM 042 demo-baseline loop) → the read-only-role post-restore check → record the FM 020 backup-trigger sign-off. Fresh A3 nodes are ideal restore targets — nothing on them to lose.
- **The read-only-role check decides a runbook line.** After the first successful cross-node restore, connect as the target node's read-only DB role and `SELECT` from a restored table. Works → the FM runbook gets "no action needed"; fails → it gets a "re-run the provision grant step after restore" line. Either way, record the answer in the handoff doc so the FM-side runbook session inherits it.
- **The scheduler-runner gap becomes load-bearing here.** Freshly provisioned nodes register nightly backup commands that never fire (no `schedule:run` runner — the standing carry-forward since session 242). Four production nodes with silently-dead nightly backups is not "operational readiness." Gate decision: close it this session (host-level cron or a worker variant — small, but touches the compose files / provision template) or document it as a known per-node manual step in the inventory. Lean: close it — the gap has aged out of acceptable.
- **Demo-baseline re-record.** Old blobs are non-portable by design. If the demo-baseline loop is the chosen restore proof, the owner re-authors/re-snapshots the baseline on a fixed-image node and re-pushes it through FM before the restore. This also un-blocks the FM 042 verification that has been pending since June.

---

## Open questions to resolve at session start

1. Which of the four target nodes already exist, and which get provisioned fresh? (Read the FM client registry at the gate.)
2. Is `main` at ≥ 0.365.01 with a green deploy? If not, that merge happens first (VERSION already bumped at 365 close).
3. Scheduler-runner: close it this session or document the gap? (Lean: close.)
4. Restore-proof route: fresh-node restore or the demo-baseline loop? (Either satisfies the criterion; the demo loop also retires the FM 042 blocker.)
5. Inventory doc home: FM repo sessions dir (lean) or CRM `docs/runbooks/`?

---

## Out of scope

- **A4 (the timed wipe-and-restore drill)** — next entry; it *depends* on this session's test/deploy instance but does not start here.
- **FM application code** — FM-side work belongs to FM sessions; sessions-dir writes only.
- **The billing/account track** — A012's planning output is under owner review; nothing from it builds here.
- **Contract surface changes** — stays v2.5.0.

---

## Testing

- **Slow test groups to run this session:** none beyond the fast suite (only if CRM-side code changes at all; a pure-ops outcome skips the suite with a note).
- **New tests expected:** none planned; a bounded in-scope fix (if live exercise surfaces one) brings its own tests per convention.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file:** `sessions/366. Multi-Node Operational Readiness — Four Production Nodes + Post-Deploy Restore Verification — Log.md`
- **Branch:** `session-366/N` (final iteration)
- **Boundary:** NOT boundary-touching — contract stays **v2.5.0**. (If the scheduler-runner close touches provision-template files that FM renders, that's FM-side coordination via a handoff note, not a contract change.)
- **Track doc:** update `sessions/tracks/fleet-manager-agent.md` (A2 outstanding items absorbed here: live verification + sign-off recorded; note what Phase A has left — the backup-recovery drill at position 48).
- **Sweep:** add the missing ✅ to execution-order line for the onboarding widget (E1, closed 249).
