# Base Prompt — Session 377 (Plugin Pilot — Widget Module & Contract v0.1)

**Start now.** Execute the reading list below, open the session prompt at `sessions/377. Plugin Pilot — Widget Module & Contract v0.1.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **377. Plugin Pilot — Widget Module & Contract v0.1**.

This is **arc position P1** of the **Plugin Architecture arc** — the new spine installed at session 376 (the launch schedule is parked; `sessions/plugin-architecture-plan.md` is canonical for the arc, `release-plan.md` § Plugin Architecture arc is the summary). P1 opens Stage A (in-repo module boundaries): the first session whose output is judged against the plugin contract rather than producing it from scratch — because producing that contract's v0.1 is this session's deliverable.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**. (The arc first touches FM territory at P10, under the Two-Repo Coordination Protocol — many sessions away.)

**Session shape:** feature-style / behavior-preserving extraction. One clean leaf widget moves from core's hardcoded registration into a self-registering `plugins/` module, and `docs/plugin-contract.md` v0.1 is drafted from what the extraction proves. **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions are front-loaded in the session prompt — mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` — §§ 2–4 (principles, core/plugin map, the 13 contract surfaces) and § 7 (the arc + execution posture). It is canonical for this arc.
3. Open `sessions/377. Plugin Pilot — Widget Module & Contract v0.1.md` — the front-loaded decisions there are settled; do not reopen them.
4. Read the code being touched: `app/Providers/WidgetServiceProvider.php`, `app/Services/WidgetRegistry.php`, `app/Widgets/Contracts/WidgetDefinition.php`, the pilot-candidate folders (`app/Widgets/LogoGarden/`, fallback `app/Widgets/BarChart/`), `app/Console/Commands/WidgetsSyncCommand.php`, `app/Services/AssetBuildService.php` (`collectSources()`), `composer.json` (autoload), and `docs/schema/widget_types.md`.
5. Skim `sessions/session-outlines.md` (Plugin Architecture track entry + Cross-Repo block) and `docs/app-reference.md` for environment/build-server context.
6. Note any drift between the session prompt and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 376

- **CRM contract at v2.7.0** — 376 was a planning session, non-boundary; last boundary touch was session 372.
- **Schema baseline:** no schema changes at 375 or 376.
- **Fast Pest suite green at 3111 / 0** (375 close; 376 added no tests). Expected delta this session: small — plugin registration tests + render-parity evidence.
- **Playwright suite** — baseline preserved from 375 (zero-console spec + the parity nets standing). Playwright does not run this session; CI is the signal.
- **Planning state:** the July launch schedule was formally parked at 376 with per-position dispositions (S4/S5, node/rehearsal/docs/licensing items parked; T1's squash absorbed into arc Stage B). The Plugin Architecture arc P1–P11 is installed as the spine in `release-plan.md` and `session-outlines.md`; `sessions/plugin-architecture-plan.md` is canonical.
- **VERSION:** `0.376.01` (376 close marker). This session's close bumps to `0.377.<N>`.
- **Housekeeping inbox:** the validation-loudness item remains parked in the inbox — a candidate rider on a later Stage-A session, not P1. This session absorbs nothing.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior-preserving is the bar.** Same handle, same rendered output, same `widget_types` row content post-sync. The fast suite + standing guards are the oracle; if a change needs a human eyeball to validate, it is out of scope by definition.
- **The front-loaded decisions in the session prompt are settled.** Implementation details within them (e.g. the exact bootstrap wiring for `config/plugins.php`) are yours to decide and log.
- **Scope is binding.** The other 40 widgets, activation, packaging, and admin surfaces belong to later arc sessions. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc honesty:** every surface in `docs/plugin-contract.md` is marked PROVEN (exercised this session) or DECLARED (specified only). Do not mark aspirationally.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — pilot-widget pick (with the asset-type check result), the extraction plan in a few sentences, and any drift found — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
