# 284. Public Marketing Website — Audit + Home Cleanup

First phase of the Public Marketing Website track. Produces the system-understanding summary the brief calls for, then applies it to the existing home page to validate the export → edit → import workflow on the lowest-risk page first. **Execution-order position 34** in `sessions/release-plan.md`. Track doc at `sessions/tracks/public-marketing-website.md` (carries the project-intent framing — no need to repeat it here).

---

## Plan reference

This session executes `sessions/release-plan.md` § PMW1. Read that entry first — it is canonical for scope, success criterion, prerequisites, and artifact.

The PMW1 entry calls for: audit deliverable (type scales, button styles, appearance config, available widget inventory, sample image library) + cleaned home page export aligned with the brief's section-band / padding / appearance-config-as-text-color conventions + screenshot + first gap-report rows.

The track doc carries the broader four-phase shape, system conventions inherited from the brief, and gap-resolution discipline. Read it before starting.

---

## Session-specific deltas

Lifted at session 283 mid-session as the first phase of a new track:

- **The brief was authored by an outside agent collaborating with the user** without live codebase access. Expect drift in the brief's references to specific file paths, helper names, or admin URLs. Default to silent adaptation per the base-prompt rules; flag drift only when it changes intent.
- **Natural deliverable order is audit → cleanup → screenshot + gap report scaffold.** The audit is the input that makes the cleanup correct; the cleanup is what gets screenshot-captured; the gap report absorbs anything that surfaced along the way. All three land in the working folder and surface together at the session-end manual-test pause per the base-prompt rule. No mid-session checkpoint.
- **The existing home page export is canonical for the JSON format.** `sessions/public website/existing-home-page-export.json` is the ground-truth example. Per the brief's authority hierarchy, the existing export wins over the copy doc when they disagree.
- **No copy in the existing home is to be paraphrased or rewritten.** The home cleanup is structural (section-band conventions, padding values, color-model standardization) — not a content edit.
- **Screenshot capture is informal this phase.** The proper Playwright page-capture harness lands at PMW4. For PMW1, a one-off screenshot — taken by hand, browser screenshot, whatever's quickest — is enough to confirm the cleaned home renders. The capture-harness work is explicitly deferred to PMW4 to keep this session scoped.

---

## Open questions to resolve at session start

- **Audit summary lands at** `sessions/public website/audit-summary.md` with five subsections: type scales / button styles / appearance config / widget inventory / sample image library. Inventory only — no design recommendations. Adjust subsection count if the codebase doesn't carve cleanly along those lines.
- **Cleaned-home output filename:** `home.json` in the working folder; the original `existing-home-page-export.json` stays put as the pre-cleanup baseline.
- **Color-model standardization rule:** the brief mandates `appearance_config.text.color` as source of truth. Default behavior on the home cleanup — strip inline Quill colors that overlap with what the appearance-config layer should carry; preserve inline colors used for true single-phrase emphasis. Flag any case where the call isn't obvious.
- **Page JSON import mechanism.** The brief assumes import-without-errors but doesn't pin where the import action lives. Locate at session start (admin Tools → Importer? page-builder API? a CLI command?). Inventory finding for the audit summary regardless.

---

## Out of scope

- **PMW2, PMW3, PMW4 work.** Pricing / About / Contact / Demo authoring lives in subsequent phases.
- **Gap-resolution implementation.** Per the track doc and brief: gaps are surfaced for discussion, not unilaterally resolved. Each gap that warrants a code change becomes its own follow-on session if the user approves.
- **Building any new widget, button style, type scale, or appearance config option.** Same rule.
- **The Playwright page-capture harness.** Scheduled for PMW4. Informal screenshot is enough this phase.
- **The two in-product demo LPs (`/my-nonprofit`, `/my-nonprofit-workshop`).** Those live inside demo nonprofit accounts and are not authored by this track.
- **Real photography.** Sample image library serves as placeholders; real photos come post-track.
- **Real `mailto:` address.** Track surfaces a single gap asking the user to provide the real address; not a track-side decision.

---

## Testing

- **Slow test groups to run this session:** none.
- **New tests expected:** no — content-only session per base-prompt convention.
- **Regression check:** run the fast Pest suite once after the home re-import to confirm nothing perturbed (target: 283-close baseline 2390 / 0). Single run, not a watch loop.

---

## Closing steps

Standard close gate. Session-specific details:

- **Log file:** `sessions/284. Public Marketing Website — Audit + Home Cleanup — Log.md`.
- **Artifact:** `sessions/public website/audit-summary.md` + `sessions/public website/home.json` + `sessions/public website/gap-report.md` (first rows) + an informal screenshot of the rendered home (placement: working folder; filename TBD at session time, e.g. `home-after-cleanup-screenshot.png`).
- **Update `sessions/release-plan.md`:** mark PMW1 ✅ closed at session 284. Update execution-order list entry at position 34.
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (PMW1 marked complete; PMW2 marked next). No phase-expiry compression yet — the track closes at PMW4, not at the end of any single phase.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status from "active, not yet started" to "active — PMW1 closed at session 284; PMW2 next."
- **Update `sessions/completed-sessions.md`:** append session 284's row.
- **Pre-PMW2 ask:** in the close handoff, ask the user to export current About and Pricing pages from admin into `sessions/public website/` as `existing-about-page-export.json` and `existing-pricing-page-export.json`. PMW2 needs them as ground truth.
- **Archive the previous session:** move all `sessions/283. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-284/N` branch. Notify the user. Do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Audit summary and gap report are operator-readable.** Plain language, no framework-primitive jargon — the user returns to these documents across the track.
- **Iteration commit message describes the deliverable** — e.g. "Iteration /1 — PMW1 audit summary + cleaned home.json + first gap-report rows + screenshot".
