# Base Prompt — Session 386 (Distribution Manifest & Central Build)

**Start now.** Execute the reading list below, open the session prompt at `sessions/386. Distribution Manifest & Central Build.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **386. Distribution Manifest & Central Build**.

This is **arc position P10** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary) — the closing Stage C-lite session and the last unbuilt layer of the original vision. P9 (385) proved one plugin living in its own repository; P10 makes the composition itself declarative: a committed **distribution manifest** becomes the authority for which plugins a build composes and in what order, `config/plugins.php` becomes a generated artifact of it, standing guards pin every remaining hardcoded plugin surface to it, and a verified run proves the central build produces an image whose composition follows the manifest and nothing else. **Context for the arc's shape:** the 385 close also ratified the owner's reframe — there is no launch; the finish line is the end-state architecture via **Stage D — full decomposition** (plan doc § Stage D: every plugin a package in its own `crm-plugin--*` repo, ~15–21 sessions, positions D1–D12), with niche distributions as the Stage E follow-on arc. P10 is the hinge between the proven concept and that execution run.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**. `deploy.yml` may be touched only in ways that leave its FM-facing behavior byte-identical (GHCR image names, immutable-VERSION enforcement, `/api/health` untouched); a plugin manifest in the health payload stays a future FM-contract item (plan § 4d, Two-Repo Coordination Protocol). If implementation discovers an FM-visible change is genuinely needed, that is a **pause-and-ask**. The plugin contract (`docs/plugin-contract.md`) bumps 0.9.0 → 0.10.0 at close (surface 1 amended: installed = the manifest's set, `config/plugins.php` generated; Package shape gains the distribution-manifest layer; the Stage E per-distribution-composer-sets boundary recorded). (The client-billing surface remains out of scope and guard-protected as always.)

**Session shape:** infrastructure, behavior-preserving. With the manifest as committed, runtime behavior is byte-identical: routes 272/272, suite at the 385 baseline plus additive guard tests, no visual change, fresh-install identity untouched. The diff is: new `distribution.json` (root), a `plugins:manifest-sync` artisan command, `config/plugins.php` regenerated-with-header, one new standing alignment guard, contract/runbook/plan-doc updates. **Expect one or two iterations** (manifest + generation + guards → verified run + docs). **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § 4d (the distribution/manifest layer and the three-tier changeability model P10 implements the middle of), § 7 Stage C-lite (P10's line) **and § Stage D** (the committed follow-on ladder — P10's log must hand D1 its kickoff input), and § 6 disposition 8 (the Products open question — NOT this session's to resolve, just know it exists).
3. Read `docs/plugin-contract.md` at 0.9.0 — surface 1 end to end (the two-layer activation model whose "installed" layer this session re-roots in the manifest; the ordering guarantees that must survive generation verbatim), the Package shape section including External repo (Stage C-lite), and normative rule 4.
4. Read the 385 session log (`sessions/385. One Plugin to Its Own Repo & the Extraction Runbook — Log.md`), especially its **P10 input** section — the inventory of what the root repo still hardcodes about the plugin set (the config list, composer repositories + requires, the Dockerfile's in-repo manifest COPYs, `EXTRACTED_PLUGIN_PACKAGES`, the phpunit Events testsuite) — this is the surface list the manifest must own or be guard-pinned against.
5. Open `sessions/386. Distribution Manifest & Central Build.md` — the survey and front-loaded decisions are settled at prompt review; the one Open-Gate confirmation is decision 5's scope note (the manifest governs the *composed set* at P10; per-distribution code absence is Stage E).
6. Read the mechanism being re-rooted: `config/plugins.php`, `app/Providers/PluginServiceProvider.php` (the loader + activation filter — must not change), `config/plugin-activation.php`, and `bootstrap/providers.php` (ordering context).
7. Read the guard nets this session extends or must not break: `tests/Feature/PluginPackageGuardTest.php` (the `EXTRACTED_PLUGIN_PACKAGES` const + path/vcs pinning assertions decision 3(d) folds into the manifest), `tests/Unit/PluginActivationTest.php` and the two disabled-via-flag test files (they read `config('plugins')` — generation must leave their assertions true verbatim), and `tests/Support/helpers.php` (`extractedPluginPackageDirs()`).
8. Read the build/ops surface: root `composer.json` + the `nonprofitcrm/*` entries in `composer.lock` (what the alignment guard verifies), `Dockerfile` (both composer stages; Events' manifest COPY lines are the in-repo-package pattern the manifest models), `.github/workflows/deploy.yml` (the FM-facing behavior that must stay byte-identical — read it to know what "byte-identical" means concretely) and `tests.yml`.
9. Read `docs/plugin-extraction-runbook.md` — the runbook gains a manifest step at close, and its step list shows what future extractions will expect the manifest to carry.
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry, including the Stage D/E framing added at 385) and `sessions/fleet-manager-planning-spec.md` § Two-Repo Coordination Protocol (the discipline behind the non-boundary posture).
11. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 385

- **CRM contract at v2.7.0** — 385 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** arc P9 complete (385) — Stage C-lite entered. LogoGarden lives at `github.com/abeuscher/crm-plugin--logo-garden` (public, tag `v0.1.0`), consumed via a root `vcs` repository + bound `^0.1` require, lock-pinned to the tag; its manifest declares no `version` field (tags are the metadata). Events remains an in-repo path-repo package; Payments remains a plain in-repo folder (root `"Plugins\\"` PSR-4). `docs/plugin-extraction-runbook.md` is the shipped repeatable procedure. Plugin contract **v0.9.0** (External repo section; surface 11 vendor-path note). Guard coverage extends over `vendor/nonprofitcrm/*` via the shared `extractedPluginPackageDirs()` helper.
- **Planning state (the 385 reframe — read before assuming launch-era context):** there is no launch; Security Hardening S4/S5 and the demo-polish floater are reframed out (resurrect only on a real client). The arc runs through **Stage D — full decomposition** (plan doc § Stage D: settled decisions — individual `crm-plugin--*` repos, no umbrella; everything a package, everything leaves the main repo; vertical widgets ride with verticals; generic widgets as `content-widgets` + `admin-widgets` packs; Nav stays core) to the end state, then Stage E (niche distributions). P10 is the last session before the Stage D run begins at D1 (Payments).
- **Activation layer (P8):** unchanged and load-bearing — `PLUGINS_DISABLED` subtracts from the installed set at the loader seam; the installed set + ordering authority is `config/plugins.php`, which this session re-roots as a generated artifact of the manifest. The generation must preserve the list byte-for-byte in behavior (same FQCNs, same order).
- **Schema baseline:** unchanged from 383 — core dump core-only; the three events tables plugin-owned. No schema work this session.
- **Fast Pest suite green at 3249 / 0** (385 close: 3248 + the extracted-VCS-pinning guard test). Expected delta this session: additive — the manifest alignment guard.
- **Route baseline: 272 routes** — byte-identical this session.
- **Playwright suite** — CI signal; the known parked chrome-parity failure stands (fails identically on the 384 baseline — pre-existing, non-gating). No local Playwright.
- **Housekeeping inbox** — the 385 capture buffer was empty at close. This session absorbs no inbox items (infrastructure shape; the open `[test-integrity]` items stay).

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** The generated `config/plugins.php` carries the same provider FQCNs in the same order; the loader, the activation filter, every remove-the-line fixture, and every flag test pass with assertions unchanged. Routes 272/272. No visual surface.
- **The manifest is the authority; the guard makes drift loud.** `composer.json` and the Dockerfile stay hand-maintained — the new standing guard asserts alignment (regeneration idempotence; lock-pinning per declared source kind; no unmanifested `nonprofitcrm/*` packages; extracted-set consistency). Do not build composer.json generation.
- **FM-facing behavior is byte-identical.** Any `deploy.yml` diff must be justified line-by-line in the log as FM-invisible; when in doubt, don't touch it. An FM-visible need = pause-and-ask.
- **The verified run is the proof, not the suite.** Composition-by-manifest is demonstrated by building the image from the committed manifest (full composition) and from an Events-entry-removed manifest (the 384 events-disabled composition facts), then restoring. Reuse the 384 superset-image procedure and the 382 in-image proof mechanics.
- **Scope is binding.** No per-niche distribution repos or per-distribution composer sets (Stage E — record the boundary in the contract doc); no Payments packaging or Events extraction (D1/D2); no new third-party dependencies; no FM surface; client billing stays guard-protected. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Front-end builds: none expected** (no widget/asset changes — no `build:public`, no `npm run build`).
- **Contract doc update at close:** surface 1 amended (installed = the manifest's set; generated config list; remove-the-entry guarantee), Package shape gains the distribution-manifest layer, Stage E boundary recorded; bump to 0.10.0, changelog entry. Runbook gains the manifest step.
- **Stage D kickoff input at close:** record in the session log what D1 (Payments packaging + extraction) needs from the manifest layer, and any friction the manifest revealed for the remaining hardcoded surfaces (Dockerfile COPYs, phpunit testsuites, guard consts).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code, state the phase order, note any drift found, and **re-confirm decision 5's scope note with the owner** (manifest governs the composed set at P10; per-distribution code absence is Stage E) — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
