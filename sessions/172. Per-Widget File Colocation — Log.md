# Session 172 — Per-Widget File Colocation — Log

**Date:** 2026-04-12
**Branch:** session-172/1

---

## What was built

### Phase 1 — Audit

- Inventoried 30 widgets (29 seeder-declared + `nav` already promoted), 12 SCSS partials, 11 shared Blade fragments (4 components + pager partial + 6 share-icons).
- Identified 3 orphan blade files under `resources/views/widgets/` with no callers: `column-widget.blade.php`, `site-header.blade.php`, `site-footer.blade.php`.
- Identified 2 dead shared fragments with no callers: `components/widget-placeholder.blade.php` (retired session 152) and `partials/pager.blade.php` (tested but not `@include`d anywhere).
- Identified one widget with a non-`@include` inline template body (`text_block`) and one with inline JS (`portal_signup`). No `code` or `variable_name` entries in the seeder.
- Confirmed `AssetBuildService::collectSources()` resolves paths via `base_path()` — path-agnostic, no change required.
- Confirmed five test files referenced old paths: `WidgetRegistryTest`, `ProductCarouselTest`, `WidgetTypesSession098Test`, `AppearanceConfigRenameTest`, `ContentListingWidgetsTest`, `SocialSharingTest`, `DesignSystemButtonsTest`.
- Deleted the `2026_04_09_120000_rebuild_chrome_pages_with_logo_and_nav.php` migration per user direction (no longer needed; no live installs depend on it).

### Phase 2 — Blade namespace & base-class template default

- Registered `View::addNamespace('widgets', app_path('Widgets'))` in `WidgetServiceProvider::boot()`.
- Replaced abstract `WidgetDefinition::template()` with concrete default: `"@include('widgets::" . Str::replaceLast('Definition', '', class_basename(static::class)) . ".template')"`.
- The session prompt's stated `class_basename(static::class)` would produce `NavDefinition`, not `Nav`. Stripped the `Definition` suffix so the default matches the folder convention (`app/Widgets/Nav/` for class `NavDefinition`). User-approved.

### Phase 3 — Nav reference migration

- Moved `resources/views/widgets/nav.blade.php` → `app/Widgets/Nav/template.blade.php`.
- Moved `resources/scss/widgets/_navigation.scss` → `app/Widgets/Nav/styles.scss`.
- Removed `NavDefinition::template()` override — base-class default now applies.
- Updated `NavDefinition::assets()` to the new path.
- `WidgetRegistryTest` assertion updated from `@include('widgets.nav')` to `@include('widgets::Nav.template')`.

### Phase 4 — AssetBuildService update

- No change required — `AssetBuildService::collectSources()` already reads asset paths via `base_path()`, is path-agnostic.

### Phase 5 — Promote every remaining widget

- Added `css(): ?string`, `js(): ?string`, `code(): ?string`, `variableName(): ?string` optional methods on `WidgetDefinition` base class, each defaulting to `null`.
- Extended `toRow()` to include `css`, `js`, `code`, `variable_name` keys sourced from those methods.
- Created 29 widget folders at `app/Widgets/{PascalName}/` and ported every seeder entry to a `{PascalName}Definition.php` class. Moved each widget's blade to `template.blade.php` and each widget's SCSS partial to `styles.scss`.
- `text_block` — ported its inline template body verbatim into `template.blade.php`.
- `portal_signup` — ported its inline password-match JS via the new `js()` method.
- Registered all 29 definitions in `WidgetServiceProvider::boot()`.

### Phase 6 — Shared-fragment migration

- Created `resources/views/widget-shared/` directory.
- Moved shared fragments: `buttons.blade.php`, `icon-github.blade.php`, `icon-linkedin.blade.php`, `share-icons/{bluesky,copy_link,email,facebook,linkedin,mastodon}.blade.php`.
- Deleted dead shared fragments: `components/widget-placeholder.blade.php` and `partials/pager.blade.php`.
- Updated every `@include('widgets.components.X')` → `@include('widget-shared.X')` site-wide.
- Updated every `@include('widgets.partials.share-icons.X')` → `@include('widget-shared.share-icons.X')`.
- Updated `DesignSystemButtonsTest` `view('widgets.components.buttons')` → `view('widget-shared.buttons')`.
- Deleted the corresponding "pager partial renders pagination markup" test case in `ContentListingWidgetsTest` (tested dead code).

### Phase 7 — Retire the old directories

- Deleted orphan blade files: `resources/views/widgets/column-widget.blade.php`, `site-header.blade.php`, `site-footer.blade.php`.
- Deleted empty `resources/views/widgets/components/`, `resources/views/widgets/partials/`, and `resources/views/widgets/` directories.
- Deleted empty `resources/scss/widgets/` directory.

### Phase 8 — Seeder trivialisation

- Stripped every `WidgetType::updateOrCreate(...)` block from `WidgetTypeSeeder::run()`.
- Seeder now only: cleans up legacy handles (`event_dates`, `hero_fullsize`, `site_header`, `site_footer`) and calls `app(WidgetRegistry::class)->sync()`.
- Coexistence branch in `WidgetConfigResolver::baseDefaults()` (falls back to `WidgetType::getDefaultConfig()`) retained as safety net for handle drift / uninstall scenarios. `WidgetType::getDefaultConfig()` method retained.

### Phase 9 — Documentation

- Rewrote `docs/widget-system.md`: new File-layout and Blade-namespace sections; added `css/js/code/variableName` optional methods; updated Adding-a-new-widget recipe; noted Stage 3 complete; new Shared-fragments section describing `resources/views/widget-shared/`.
- Updated `resources/docs/widget-development.md`: directory table reflects `app/Widgets/{PascalName}/` and `resources/views/widget-shared/`; template/SCSS authoring sections rewritten; quick-start checklist rewritten.
- Updated `docs/app-reference.md`: widget templates pointer now points at `app/Widgets/{PascalName}/` and `resources/views/widget-shared/`.

### Tests added

- `WidgetRegistryTest::widgets:: Blade namespace resolves to app/Widgets/{Folder}/template.blade.php` — asserts `view()->exists()` for registered and non-registered widget paths.

### Tests updated

- `WidgetRegistryTest` — updated expected nav template string.
- `ProductCarouselTest` — `view('widgets.product-carousel', ...)` → `view('widgets::ProductCarousel.template', ...)`.
- `WidgetTypesSession098Test` — same pattern for `video-embed` (5 call sites).
- `DesignSystemButtonsTest` — `view('widgets.components.buttons', ...)` → `view('widget-shared.buttons', ...)` (3 sites).
- `SocialSharingTest` — `resource_path('views/widgets/social-sharing.blade.php')` → `base_path('app/Widgets/SocialSharing/template.blade.php')` (6 sites).
- `AppearanceConfigRenameTest` — same path-fix pattern across 8 widgets.
- `ContentListingWidgetsTest` — same path-fix pattern for blog-listing (8 sites) and events-listing (5 sites); removed the orphan pager-partial test.

---

## Security checklist — verified

- [x] Blade namespace `widgets` points at `app/Widgets/` — a directory under the app root, not user-writable. No path-traversal surface.
- [x] No widget template accepts a user-controlled path for its own `@include`.
- [x] The coexistence branch in `WidgetConfigResolver` still handles gracefully the case of a `PageWidget` whose widget type has no registered definition (handle drift / uninstall scenario) — retained intentionally.
- [x] No new public routes introduced.

---

## Schema changes

None. `widget_types` table is unchanged; every row still sourced from `WidgetRegistry::sync()`. The `2026_04_09_120000_rebuild_chrome_pages_with_logo_and_nav.php` migration was deleted (user-approved; it was a one-off chrome rebuild that had already run on every environment and referenced paths that no longer exist after this session).

---

## Production deployment notes

None beyond standard `migrate`. Seeder must run after deploy so `WidgetRegistry::sync()` writes updated asset paths for every widget. Run `php artisan build:public` after seeding to recompile the public widget bundle against the new SCSS paths.

---

## Deferred decisions

- Authoring physical screenshot files for widgets (planned as content-pass task after Stage 5 browser UI surfaces them).
- Moving inline `css` / `js` / `code` columns out of the DB and into physical files inside each widget folder — deferred to a later stage.

---

## Test result

951 passed, 0 failed. Duration: 239 seconds. One new test (`widgets:: Blade namespace resolves …`) adds 3 assertions.
