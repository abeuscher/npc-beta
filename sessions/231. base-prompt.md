# Base Prompt — Session 231

We are about to begin a new session: **231. Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `sessions/session-outlines.md` (the roadmap) — note the active-tracks block at top.
3. Read `sessions/tracks/widget-primitive.md` (the canonical Widget Primitive track doc — premise sits alongside at `widget-primitive-premise.md`). Specifically:
   - Status snapshot (top of doc)
   - Phase Retrospectives — at minimum 5c / 5c.5 (just landed) and **4a (216, BlogListing audit-style retrofit)** — 5d-1 mirrors 4a's shape (contract design + projector + test-lock pair). Skim 4g (223) for the single-row DTO + non-list contract pattern, and 4f (222, BlogPager) for the `SOURCE_SYSTEM_MODEL` retrofit shape against an Eloquent model.
   - "Phase 5" entry under "The 2.0 transition — rough sequence," especially the 5d+ bullet
   - "Pending paradigm questions" section — Phase 5e (permissions in the contract layer). 5d-1 is the **forcing function** for 5e: Notes carry resource-level permission gates (`view_note`) that the contract layer has no story for. The session prompt's design-decisions block names whether 5e lands here in a minimal form or stays carved out.
   - "Design decisions locked in" — particularly **source-as-capability**, the **single-row DTO shape** locked in 4g, the **`a+` View framing** (relevant because 5d-1 may extend `EditContact` to surface multiple Views via sub-nav), and the **field-naming convention** (concept-named display fields per 226).
4. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, plus `docs/schema/notes.md`, `docs/schema/record_detail_views.md`, and `docs/schema/page_widgets.md`. **This session writes no migrations** — Notes already exists; record_detail_views already exists from 5c.
5. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/archived/230. Widget Primitive — Phase 5c.5 (Admin UI for Authoring per-Record-Type View Sets) — Log.md` — establishes the `RecordDetailViewResource`, the `RecordDetailViewBuilder` Livewire shell, the Vue `record_detail` mode, the `manage_record_detail_views` permission, and the cascade observer. **5d-1 lands the first concrete widget that consumes this primitive — Recent Notes — and resolves the "first View wins on EditContact" gap surfaced during 5c.5 manual testing by wiring `EditContact::recordDetailViewSubPageClass()` to a concrete `RecordDetailViewPage` subclass.**
   - `sessions/archived/216. Widget Primitive — Phase 4a (BlogListing Contract Retrofit) — Log.md` — establishes the test-lock pattern this session mirrors: fail-closed field whitelist + resolver-is-only-path query-count pin + projector field-naming convention.
   - `sessions/archived/223. Widget Primitive — Phase 4g (EventDescription + EventRegistration) — Log.md` — establishes the single-row contract DTO shape (`['item' => row | null]`, `cardinality: 'one' | 'many'`). Recent Notes is list-shaped (cardinality: 'many'), so 4g is reference for resolver dispatch but not the canonical shape match. 4f (BlogPager) is closer.
6. Read the full state of every file this session edits or creates (see the session prompt's Phase descriptions for the file list). At minimum: `app/WidgetPrimitive/ContractResolver.php`, `app/WidgetPrimitive/Projectors/SystemModelProjector.php`, `app/WidgetPrimitive/AmbientContexts/RecordDetailAmbientContext.php`, `app/Models/Note.php`, `app/Filament/Resources/Pages/RecordDetailViewPage.php`, and `app/Filament/Resources/ContactResource/Pages/EditContact.php`.
7. Read related existing tests to anchor patterns:
   - `tests/Feature/Phase4a/BlogListingContractTest.php` (or the closest 4a test file present) — the test-lock template.
   - `tests/Feature/RecordDetailViewBuilderApiControllerTest.php` (just landed in 5c.5) — the host pattern for record-detail API gating.
   - `tests/Feature/EditContactRecordDetailViewTest.php` — the regression-lock that must continue to pass after the EditContact sub-nav wiring lands.
8. Open `sessions/231. Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. Do not pause to ask first. **This session writes no migrations.**
- **Update `docs/schema/`** after writing any migration — none expected this session.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session adds a new widget at `app/Widgets/RecentNotes/` with a Blade template and possibly per-widget SCSS — `build:public` will be required if the widget declares assets, otherwise `npm run build` is sufficient. The session prompt names which.**
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands a new widget, a new resolver arm, a new projector, and a new sub-page class — every piece gets test-locked.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/232. base-prompt.md`. Expected next session: **Phase 5d-2 — second concrete record-detail widget**, choice between Recent Donations (forces full Phase 5e perm-in-contract architecture) and Membership Status (lighter, no new perm gates beyond what 5d-1 establishes). Pick at 231 close based on what 5d-1 surfaced.
- **Next session prompt**: draft `sessions/232. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/231. Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update `sessions/completed-sessions.md`**: append session 231's row.
- **Update `sessions/session-outlines.md`** (the roadmap): the Active-Tracks block's Widget Primitive entry needs its remaining-session count bumped (one fewer; ~4–6 left).
- **Update `sessions/tracks/widget-primitive.md`**: bump the status snapshot (last update date, what's complete, what's active). **Phase 5d-1 opens a new phase boundary** — write a fresh compressed retrospective entry under Phase Retrospectives. The 5d arc spans multiple sessions; 5d-1 closes the first sub-phase.
- **Archive the previous session**: move all files matching `sessions/230. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines/track docs, the completed-sessions index, and any files moved into `sessions/archived/`), commit on the current `session-231/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
