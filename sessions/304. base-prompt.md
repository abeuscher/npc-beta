# Base Prompt — Session 304 (UI/UX Sprint — Page-Builder Full-Screen + Quill Height Handle)

**Start now.** Execute the reading list below, open the session prompt at `sessions/304. UI-UX Sprint — Page-Builder Full-Screen + Quill Height Handle.md`, then begin the work. No confirmation needed.

---

We are about to begin: **304. UI/UX Sprint — Page-Builder Full-Screen + Quill Height Handle**.

This is **execution-order position 38** in `sessions/release-plan.md` — **§ E8 (UI/UX Sprint)**, the default next item after the Theme + Media Portability lift. It sits in the **Public Website Complete** milestone band (Rule 12), before the `── PUBLIC WEBSITE COMPLETE ──` divider. Canonical scope is the **E8 release-plan entry** ("Per existing stub") plus the **"UI / UX Sprint" stub** in `sessions/session-outlines.md` — two authoring-ergonomics items batched into one session.

This session is **NOT boundary-touching** — no Fleet Manager surface. **CRM contract stays at v2.3.0.** **No schema change** (the stub says so explicitly: per-user persistence is localStorage, not a column). If a step appears to need a migration, a Fleet/`/api/health`/response-schema touch, or a contract bump — stop and surface; scope has drifted.

**Session shape:** feature-style, small, authoring-ergonomics. Two independent items: (1) a page-builder full-screen toggle that maximizes the builder canvas+inspector and hides Filament chrome, persisted per-user via localStorage; (2) a drag-resize height handle on the Quill rich-text editor, per-user height persisted via localStorage. One iteration expected; if it overflows, the **natural Rule-11 cut-line is between the two items** (full-screen toggle = shippable on its own; Quill handle = documented continuation).

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — stable since 276.
2. Read `sessions/session-outlines.md` — the Active-tracks block and the **"UI / UX Sprint" stub** (the two item descriptions are canonical for scope). **Also read the "Layer Explorer" stub note** in that file: it states the full-width/chrome toggle "were delivered admin-wide in session 156's topbar fullscreen button." That prior art is the single most important thing to reconcile before building item 1 — see reading-list step 5 and the session prompt's open questions.
3. Read `sessions/release-plan.md` § E8 — canonical for gate/success-criterion/artifact ("Per existing stub"; page-builder full-screen toggle persisted per-user via localStorage + Quill drag-resize handle on the bottom edge with per-user height persistence; ~1 session).
4. This session does **not** belong to an active track. The E8 plan entry + the UI/UX Sprint stub are jointly canonical for scope.
5. **Read the actual surfaces before touching them and reconcile against session 156:**
   - `resources/js/admin/fullscreen-toggle.js` — **session 156's admin-wide topbar fullscreen/chrome toggle.** Read this first. Determine exactly what it already hides/maximizes and how it persists. Item 1 is the *page-builder-specific* maximize (canvas + inspector to full viewport, Filament sidebar/topbar/breadcrumbs hidden, exit restores prior state, per-user localStorage). If 156's affordance already substantially covers this on the builder pages, that is a scope question to surface at start — do **not** rebuild 156's toggle; build only the builder-specific delta (or confirm it's already covered).
   - `resources/js/page-builder-vue/App.vue` + `resources/js/page-builder-vue/main.ts` — the page-builder Vue island (the `grid-template-columns: minmax(0, 3fr) min(28rem, 25%)` canvas/inspector layout lives here).
   - The four host blades the builder mounts in: `resources/views/filament/resources/page-resource/pages/edit-page.blade.php`, `.../post-resource/pages/edit-post.blade.php`, `.../template-resource/pages/edit-content-template.blade.php`, `.../template-resource/pages/edit-page-template-chrome.blade.php` — item 1's toggle must behave consistently across all four.
   - `app/Forms/Components/QuillEditor.php` + `resources/views/forms/components/quill-editor.blade.php` + `resources/js/admin/quill-editor.js` — the Filament-form Quill editor (item 2's primary target).
   - `resources/js/page-builder-vue/components/fields/RichTextField.vue` — the *second* Quill surface (inside the Vue builder). Confirm at start whether item 2's height handle is expected on both Quill surfaces or just the Filament-form one — surface as an open question.
6. Read `docs/app-reference.md` (container/env names, view-to-file mappings, the front-end build commands). No `docs/schema/` reading needed — no schema in scope.
7. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules. Expect surface drift in the Vue/JS; the session-156 reconciliation is the one that genuinely needs a decision if 156 already covers item 1.

---

## Starting state inherited from session 303

*(Session 303 — Theme + Media Portability — closed immediately before this session. Re-confirm against 303's actual log at start; adapt silently to drift.)*

- **CRM contract at v2.3.0** — unchanged; 303 was non-boundary (import/export services, queued jobs, a gated download route, Filament actions, a notifications-table migration). 304 is also non-boundary.
- **Schema baseline:** 303 added the standard Laravel **`notifications`** table (migration `2026_05_18_130000_create_notifications_table.php`; `data` column is **`json`**, not stock `text`, so Filament's `data->>'format'` bell query works on Postgres) and enabled Filament database notifications on the admin panel. No other schema since the session-301 `templates` migration. **304 adds no schema.**
- **Fast suite green at 303 close: ≈2499 / 0** (302 baseline 2476 + the net-new `BundleMediaPortabilitySession303Test`, ~23 tests incl. the editor-colour-round-trip and notifications-json regressions). Parallel (`./dev test`). `DesignGroupIntegrityTest` pinned-list count **19**, unchanged (303 added no `design`-group tests). Re-confirm the exact number against 303's log. New tests expected this session for the objective bits (localStorage persistence, chrome hidden) — see the session prompt's Testing block.
- **Playwright** — isolated stack via `npm run test:e2e:isolated`. **Session 303 landed a CI fix** (`0217d45`): the Playwright job now runs `migrate:fresh --seed --force` against the e2e app container *after* the readiness gate and *before* the run — the prior gap (no pre-test migrate; the DB-independent `/admin/login` probe green-lit an empty DB → `relation "site_settings" does not exist` 500s → exit 1) is closed. `globalSetup`'s own reset stays (idempotent). `memory/feedback_test_runs_not_parallel` applies — Pest and Playwright never run together; `docker compose restart nginx` recovers stale upstreams.
- **Planning state:** the **"Theme + Media Portability" stub is resolved** at 303 close (both halves shipped, no Rule-11 split — nothing carried forward). 303 also folded in, at user request: the Postgres `notifications.data`-json fix, an operator-facing notification-visibility pass (solid-red pulsing bell badge + filename-led export title), and the e2e-CI migrate-before-run fix. The execution order resumes — **E8 is position 38**; the item after E8 is the next default unless the user elects another lift.
- **Housekeeping inbox** — walk `sessions/housekeeping-inbox.md` at start/close per Rule 2. 304 is a **feature session, not a batch** — it does **not** absorb the inbox. Only fold an existing inbox item if it is literally in scope (the "Inspector Column — Drag-Resize Splitter" and "Layer Explorer" stubs are adjacent but **out of E8 scope** — do not absorb them; the session prompt disambiguates).

---

## Process rules for every session

Base-prompt process rules (`sessions/template-base-prompt.md`) apply in full. Session-specific:

- **Reconcile against session 156 before building item 1.** `resources/js/admin/fullscreen-toggle.js` already exists (session 156, "Form Polish, Image Fields & Chrome Toggle"). The session-outlines Layer-Explorer note asserts the admin-wide toggle already covers the full-width need. **Read it, determine what it actually does, and surface the scope delta at start** — build only the page-builder-specific behaviour the stub asks for that 156 does not already provide. Do not rebuild or duplicate 156's toggle.
- **Existing infrastructure first.** The page builder is an established Vue island (`page-builder-vue/`); Quill is wired through `app/Forms/Components/QuillEditor.php` + `resources/js/admin/quill-editor.js`. Per-user persistence is **localStorage** (the stub mandates it; no `SiteSetting`, no column). Build on these; do not introduce a new persistence mechanism or a new abstraction.
- **Scope fences — do not cross them:**
  - **No schema change.** Per-user state is localStorage only. If a step seems to need a column/migration, stop and surface.
  - **No Fleet / `/api/health` / response-schema change** — non-boundary, v2.3.0.
  - **Not the adjacent stubs.** "Inspector Column — Drag-Resize Splitter" and "Layer Explorer" are separate session-outlines stubs — out of E8 scope.
  - **Don't rebuild session 156.** Item 1 is the builder-specific delta over the existing admin-wide toggle, not a re-implementation.
- **Front-end build required.** This touches Vue/JS/SCSS source (`resources/js/**`) — run `npm run build` before announcing completion (per the base-prompt front-end build rule). Pest does not catch Vue/TS/SCSS failures.
- **Verify objectively where you can; pull the user in for feel.** The objective bits — toggle hides the right chrome and restores it, localStorage key written/read, Quill height persists across reload — close those loops yourself (a short Playwright spec is the right tool and stays in the suite). The *feel* of the toggle and the resize ergonomics are human-judgment: announce ready, then stop and wait. No manufactured judgment gates (`memory/feedback_dont_gate_on_taste_defaults`); but the resize/maximize UX is a genuine "is this right?" — that one is the user's call.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close.** Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

On 304 close the **"UI / UX Sprint" stub is resolved** (both items shipped) — or, if the Rule-11 cut-line between the two items is taken, the stub carries forward **only** the Quill-height-handle continuation. Either way the execution order resumes at the next un-closed `release-plan.md` position after E8. Draft the next session's documents per the base-prompt template **only when the user names it** — do not auto-pipeline; reflect the actual state (full-resolve vs. item-2 continuation).

### Phase 2 — Close

When the user says to close: session log per `template-session-log.md`; update `session-outlines.md` (**mark the "UI / UX Sprint" stub resolved**, or rewrite to the item-2-only continuation if split); append to `completed-sessions.md`; add help-doc/`getBreadcrumbs()` updates only if a new admin page was introduced (not expected for E8 — both items are enhancements to existing surfaces); archive the immediately-preceding session's files into `sessions/archived/` (`sessions/303. *`); commit on the final `session-304/N` branch; notify; do not push; do not begin the next session until the user starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — Page-builder full-screen toggle (builder-specific maximize + chrome hide, per-user localStorage) + Quill drag-resize height handle (per-user localStorage) + VERSION 0.304.1".
- **PM-level tone for status reports** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Build on the existing pattern; do not reinvent.** Extend the existing page-builder Vue app and Quill wiring; reuse session 156's toggle infrastructure rather than parallel it. No new admin page expected (so no new help-doc stub / `getBreadcrumbs()` unless the work unexpectedly introduces one).
