# Completed Sessions Index

A flat index of every completed session by number and title. Per-session detail lives in the matching `sessions/(archived/)NNN. … — Log.md` files; this index is just the at-a-glance lookup.

---

| # | Title |
|---|-------|
| 001 | Initial Setup |
| 002 | Admin Route Conflict, Contact Model |
| 003 | First Public Page |
| 004 | Admin Information Architecture |
| 005 | Site Settings, Public Frontend, and Blog |
| 006 | Collections |
| 007 | Widget System |
| 008 | Widget Type System + CMS Tags |
| 009 | Page Builder |
| 010 | Widget Config Schema + Page Builder UI Rework |
| 011 | CRM Taxonomy & Contact Model Clarity |
| 012 | User Roles & Permissions |
| 013 | Role Management UI |
| 014 | Events: Foundation |
| 015 | Events: Registration Model, Landing Pages, and Event Widgets |
| 016 | Page Types, Event Routing & Base Pages Seeder |
| 017 | Event Notifications & Registrant Export |
| 018 | Event Model Enhancements & Contact Auto-Creation |
| 019 | Import/Export: Core |
| 020 | Bug Fixes & Planning |
| 021 | Trix Toolbar Bug in Page Builder |
| 022 | Replace Trix with Quill in the Page Builder |
| 023 | Custom Contact Fields |
| 024 | Help System |
| 025 | Migrate Blog Posts into the Pages Table |
| 026 | Deployment — Get Live on a Public Server |
| 030 | List & Table UI Overhaul — Bulk Actions Everywhere |
| 031 | Site Chrome — Named Menus, Header & Footer |
| 032 | Navigation Model and UI Restructure |
| 033 | Admin Branding & Dashboard |
| 034 | Transactional Email — Resend Integration |
| 035 | System Email Templates |
| 036 | Mailing List Manager |
| 037 | MailChimp Integration |
| 038 | MailChimp Webhook Debugging |
| 039 | Admin Dashboard & Branding Polish |
| 040 | Tags — Unified Tag System |
| 041 | Importer Phase 2 — Accountability, Source Mapping & Filter UI |
| 042 | Codebase Audit — Fields, Schema, Permissions & Help Coverage |
| 043 | Importer — Phase 3 |
| 044 | Importer — Phase 4: Staged Updates & Queue Control |
| 045 | Public Frontend Foundation |
| 046 | Site Theme Admin |
| 047 | Web Forms — Foundation |
| 048 | Web Forms — Security Review |
| 049 | Codebase Audit — Fields, Schema, Permissions & Help Coverage |
| 050 | Roadmap Planning & Prioritisation |
| 051 | Minor Tweaks & Polish |
| 052 | CRM Polish — Roles, Contacts & Users |
| 053 | Duplicate Contact Detection |
| 054 | Event Registrant Cleanup |
| 055 | Quill Fix, Page Layout & Event Date Simplification |
| 056 | Secure Public Signup Flows |
| 057 | Portal Chrome & Member Page Type |
| 058 | Routing Consolidation, Page Type Locking & Portal Widgets |
| 059 | Password Reset |
| 060 | Member Portal |
| 061 | System Page Type — Infrastructure and Migration |
| 062 | Codebase Audit & Migration Squash |
| 063 | Admin UI Polish, CMS Navigation Sort & Settings Consolidation |
| 064 | Sample Data Generator Library |
| 065 | Member Event Registration |
| 066 | Promote Contact to Member |
| 067 | Contact Actions & Notes Sub-Page |
| 068 | Roadmap Planning & Help Content |
| 069 | Minor Tweaks, Fixes & Git Hygiene |
| 070 | Admin User Invitations |
| 071 | Household Linking |
| 072 | Activity Log |
| 073 | Stripe Foundation |
| 074 | Products & Checkout |
| 075 | Donations — Foundation |
| 076 | Tax Receipts |
| 077 | Mailing List from Donors |
| 078 | Finance Data Boundary |
| 079 | Debug Generator — Donations, Products & Purchases |
| 080 | System Email Preview Wizard |
| 081 | Minor Tweaks & Polish |
| 082 | Codebase Audit & Migration Squash |
| 083 | Bug Fixes & Polish |
| 084 | Widget Render Context |
| 085 | Widget Migration |
| 086 | Column Widget & Widget Picker UX |
| 087 | Inspector Panel & Shared Page Builder Form |
| 088 | Image Optimization & SVG Support |
| 089 | Image & Carousel Widgets |
| 090 | Tailwind Migration — Layouts & Infrastructure |
| 091 | Tailwind Migration — Widget Templates |
| 092 | Media Library Manager |
| 093 | WYSIWYG Toolbar & Inline Image Insert |
| 094 | Test Audit & Bug Fixes |
| 095 | Test Coverage — Portal, Stripe & Integrations |
| 096 | Per-Page SEO & Header Snippets |
| 097 | Header & Footer Widget System |
| 098 | Additional Widget Types — Calendar, Chart & Video |
| 099 | Template System — Data Model & Migration |
| 100 | Template Manager UI & Page Creation Flow |
| 101 | Code Review & Cleanup |
| 102 | Stripe Payment Methods & QuickBooks Connection |
| 103 | QuickBooks Transaction Sync |
| 104 | QuickBooks Customer Matching |
| 105 | QuickBooks Per-Type Account Mapping |
| 106 | Transaction Ledger Cleanup & Seeder |
| 107 | Paid Event Registration & Membership Checkout |
| 108 | Beta-One Bug Fixes & Migration Squash |
| 109 | Deletion Guards & Archive Pattern |
| 110 | Data Retention & Cascading Delete Audit |
| 111 | Trash Management UI |
| 112 | Password Generator & Data Generator Audit |
| 113 | Local Dev Environment — WSL2 Migration |
| 114 | Permissions Audit & Coverage |
| 115 | Housekeeping & Consistency Audit |
| 116 | Code Review & Framework Alignment Audit |
| 117 | Permissions Framework & Coverage Audit |
| 118 | Help System — Search, Standalone Pages & Related Articles |
| 119 | Widget Picker — Categories, Page-Type Filtering & Search |
| 120 | Filament Custom Theme — Admin Panel Tailwind Build |
| 121 | Widget — Hero |
| 122 | Widget — Hero Enhancements (Fullscreen, Video Background, Full Bleed, Semantic Markup) |
| 123 | Tailwind Removal & Build Server Integration |
| 124 | Custom Form Fields & Site Styles |
| 125 | Build Server Settings & CI Pipeline |
| 126 | Logo Garden Widget & Demo Collection Seeding |
| 127 | Board Members & Three Buckets Widgets |
| 128 | Product Carousel Widget |
| 129 | Embed Map Widget |
| 130 | Social Sharing Widget |
| 131 | Content Listing Widgets |
| 132 | Widget Audit |
| 133 | Admin Layout & Chrome |
| 134 | Design System Editor: Buttons |
| 135 | Widget Data Contract & Demo Data |
| 136 | Widget Edit & Preview Modes |
| 137 | Inline Text Editing |
| 138 | Widget JS Dependencies & Interactive Preview |
| 139 | Unified Preview & Edit Layout |
| 140 | Properties Panel & Config Split |
| 141 | Code Review & Cleanup |
| 142 | Inline JS/CSS Extraction & Migration Squash |
| 143 | Codebase Hygiene Audit |
| 144 | Column Widget — Drag-and-Drop Reorder |
| 145 | Editor Vue Migration — Decision & Planning |
| 146 | Editor Architecture Review |
| 147 | Editor API & Vue Scaffold |
| 148 | Editor Canvas in Vue |
| 149 | Editor Inspector in Vue — Part 1 |
| 150 | Editor Inspector in Vue — Part 2 |
| 151 | Widget Delete & Livewire Inspector Removal |
| 152 | Preview Inline Controls & Drag-and-Drop |
| 153 | Column Layout System |
| 154 | Nav Widget & Footer |
| 155 | Template & Page Import-Export |
| 156 | Form Polish, Image Fields & Chrome Toggle |
| 157 | Page Builder Preview Reactivity Bug Fix |
| 158 | Inspector Panel Reactivity |
| 159 | Roadmap Planning — Chrome Fixes & Appearance Controls Project |
| 160 | Page Builder & Admin Chrome Fixes |
| 161 | Appearance Controls for Widgets — Docs Refresh & Shared Primitives |
| 162 | Appearance Controls — Schema Rename & Widget Dedupe Sweep |
| 163 | Page Builder Bug Fixes |
| 164 | Appearance Controls — Background Panel |
| 165 | Appearance Controls — Text & Section Layout Panels |
| 166 | Appearance Controls — Docs Finalization & Widget Spacing Harmonization |
| 167 | Page Builder as Main Edit View |
| 168 | Most Used Widgets & Text Drop Shadow |
| 169 | Navigation Widget |
| 170 | Widget Definition Class & Registry |
| 171 | Defaults Binding & Sovereign Rendering |
| 172 | Per-Widget File Colocation |
| 173 | Widget Manifest & Metadata |
| 174 | Sovereign Widget Demos & Static Thumbnails |
| 175 | Preset Inspector UI & Hero Preset Pilot |
| 176 | Designer Preset Drafts & Export |
| 177 | Preset Thumbnails |
| 178 | Code Review & Cleanup (Audit) |
| 179 | Code Review & Cleanup (Apply) |
| 180 | Defaults Authoring & Housekeeping |
| 181 | Migration Squash & Inline JS/CSS Audit |
| 182 | Design System Editor — Typography |
| 183 | Page Builder Typography Inheritance & Theme Editor Polish |
| 184 | Sample Image Library & Demo Seeder Integration |
| 185 | Image Support Across Content Types |
| 186 | Bug Fixes & Widget Tuning |
| 187 | Content Template Editor & Per-Type Defaults |
| 188 | CRM Importer — Contacts: Presets, Dry-Run & Match Keys |
| 189 | CRM Importer — Events, Registrations & Transaction Linking |
| 190 | CRM Importer — Donations, Memberships & Invoice Details |
| 191 | CRM Importer — Polish, Shared Code & Source Templates |
| 192 | CRM Importer — Data Review Step |
| 193 | CRM Importer — Commit Progress, Loading States & Large-Import Handling |
| 194 | CRM Importer — Update-Existing Strategy for All Content Types |
| 195 | Playwright E2E Harness & Contacts Importer Coverage |
| 196 | Playwright Importer Regression Coverage — Events, Donations, Memberships & Invoice Details |
| 197 | Quill Toolbar Visual Language — Reference-Image Pilot |
| 198 | Notes — Structured Interactions Schema & Timeline Surfaces |
| 199 | Admin Secondary Button Colour & Header-Action Polish |
| 200 | Admin CSS Consolidation — Tightening Pass |
| 201 | Widget Template Tokens — Per-Item Namespace Split |
| 202 | Importer CSV Generator & Playwright Edge-Case Coverage |
| 203 | CRM Importer — Notes & Interactions (Standalone Path) |
| 204 | Admin UX Polish — Page Builder Selection, Focus Scroll & Settings Save |
| 205 | Code Review & Cleanup (Audit) |
| 206 | Code Review & Cleanup (Apply) |
| 207 | Column Layout Inspector — Appearance Unification |
| 208 | Migration Squash & Code Optimization |
| 209 | Widget Primitive — Data Contract Prototype |
| 210 | Widget Primitive — Contract Refinements |
| 211 | Widget Primitive — Phase 2 (Slot Taxonomy) |
| 212 | Widget Primitive — Phase 3a (Dashboard Grid — Slot Plumbing & Hosted Render) |
| 213 | Widget Primitive — Phase 3b (Data Sink & Source Policy) |
| 214 | Widget Primitive — Phase 3c (Dashboard-Native Widgets) |
| 215 | Widget Primitive — Phase 3d (Dashboard Config UI & Storage) |
| 216 | Widget Primitive — Phase 4a (BlogListing Retrofit — Audit, Test-Lock, Cleanup) |
| 217 | Widget Primitive — Phase 4b (EventsListing Retrofit) |
| 218 | Widget Primitive — Phase 4c (Carousel Retrofit — Audit, Test-Lock, Cleanup) |
| 219 | Widget Primitive — Phase 4d (BarChart Retrofit) |
| 220 | Widget Primitive — Phase 4e (BoardMembers + LogoGarden Retrofit) |
| 221 | Widget Primitive — Phase 4 Follower (Query Settings in the Contract Layer) |
| 222 | Widget Primitive — Phase 4f (BlogPager Retrofit + PageContext::posts Retirement) |
| 223 | Widget Primitive — Phase 4g (Event Single-Row Pair: EventDescription + EventRegistration + PageContext::event Retirement) |
| 224 | Widget Primitive — Phase 4h (Product Pair Retrofit + WidgetDataResolver Shell Retirement) — Phase 4 Close |
| 225 | Date Handling — Centralized Vocabulary + Schema Audit |
| 226 | Date Display — Format Hints + Projector-Owned Assembly |
| 227 | Widget Primitive — Phase 5a (Typed Ambient Context Primitive) |
| 228 | Widget Primitive — Phase 5b (Record-Detail Filament Mount + Ambient Wiring + IsView Primitive) |
| 229 | Widget Primitive — Phase 5c (record_detail_views Table + IsView Registry + Sub-Nav Rendering Primitive) |
| 230 | Widget Primitive — Phase 5c.5 (Admin UI for Authoring per-Record-Type View Sets) |
| 231 | Widget Primitive — Phase 5d-1 (Recent Notes — First Concrete Record-Detail Widget) |
| 232 | Widget Primitive — Phase 5d-2 (Membership Status — Single-Row Record-Detail Widget) |
| 233 | Financial Data Origin & Lifecycle Discipline — Phase A (`source` Column + Write-Path Discipline) |
| 234 | Widget Primitive — Phase 5d-3 (Recent Donations — Third Concrete Record-Detail Widget) |
| 235 | Widget Primitive — Phase 5e (Permissions in the Contract Layer) |
| 236 | Widget Primitive — DashboardConfig → DashboardView Retrofit |
| 237 | Widget Primitive — Phase 6 (Page-as-View Adapter, Placeholder Sweep, Track Closure) |
| 238 | Fleet Manager Agent — Phase 1 (CRM-Side MVP + v1.0.0 Contract) |
| 239 | Donation Status Enum Alignment (Importer + Backfill) |
| 240 | Fleet Manager Agent — Contract v1.1.0 (`unknown` Status for `last_backup_at`) |
| 241 | Test Audit (Mutation Testing) — First Slice |
| 242 | Fleet Manager Agent — Phase 2 (Backup Pipeline) |
| 243 | Housekeeping Batch 1 |
| 244 | Pre-Beta Release Plan — Rehearsal Vetting & Success Criteria |
| 245 | Random Data Generator as Dashboard Widget |
| 246 | Housekeeping Batch 2 — UI Fixes Bundle |
| 247 | Housekeeping Batch 2 — app:reset Artisan + Orphan Media Cleanup |
| 248 | Fleet Manager Contract v2.0.0 — mTLS Migration |
| 249 | Onboarding/Install Dashboard Widget |
| 250 | Rich Text Custom Fields |
| 251 | Fleet Manager Contract v2.1.0 — Log-Fetch Endpoint |
| 252 | CI Test Suite Cascade — Root-Cause & Fix |
| 253 | Fleet Manager Pivot Planning Session |
| 254 | Importer Mapping Page UX |
| 255 | Organizations Model Overhaul (Min) |
| 256 | Organizations Importer |
| 257 | Importer Test-Fixture Generator — CSV Foundation |
| 258 | Onboarding Rehearsal Cluster |
| 259 | Importer Auto-Mapping Pattern Lift |
| 260 | First-Deploy Migrate Race Fix |
| 261 | List Resource Exports — CSV and JSON |
| 262 | List Resource Exports — XLSX Format Add |
| 263 | Fleet Manager Contract v2.2.0 — Backup Trigger Endpoint |
| 264 | Affiliations Junction — Schema, Migration, Admin UI |
| 265 | Donation Credits — Soft-Credit Layer |
| 266 | Backup notification hardening — FM 020 finding |
| 267 | Full-Width Architecture Enforcement — Background and Content Split |
| 268 | Fleet Manager Contract v2.3.0 — Backup Blob Download Endpoint |
| 269 | Page Builder Focus-Scroll Clamp (closed without shipping; 204-rationale reaffirmed) |
| 270 | PostgreSQL Major-Version Skew Fix |
| 271 | Code Review & Cleanup (Audit Part 1 — Foundations + Quantitative) |
| 272 | Code Review & Cleanup (Audit Part 2 — Subsystem Deep Walks) |
| 273 | Code Review & Cleanup (Apply) |
| 274 | Migration Squash & Code Optimization |
| 275 | Rich-Text Surface Sanitization Hardening |
| 276 | Notes Permissions (feature half) |
| 277 | Widget Help Authoring & Help-System Integration |
| 278 | Event Ticket Tiers |
| 279 | Multi-Quantity Event Ticket Purchase |
| 280 | Permission Audit |
| 282 | Phase C + D Audit + Planning-Doc Cleanup |
| 283 | Stripe Checkout Branding |
| 284 | Public Marketing Website — Audit + Home Cleanup |
| 285 | Public Marketing Website — CMS Fixes Before Home Rebuild |
| 286 | Public Marketing Website — Home Layout Rebuild |
| 287 | Public Marketing Website — About Layout Rebuild |
| 288 | Public Marketing Website — Pricing Chart Widget Build |
| 289 | Public Marketing Website — Pricing Layout Rebuild |
| 290 | Public Marketing Website — Contact Layout Rebuild |
| 291 | Public Marketing Website — Form Submission Notifications |
| 292 | Public Marketing Website — Demo Page Build |
| 293 | Public Marketing Website — Page-Capture Harness & Track Close |
| 294 | Column-Layout Mobile Collapse + Swiper Blanket — Playwright-Verified |
| 295 | Mobile Type Scaling — Per-Breakpoint Type + Build-Pipeline / .np-site Delivery Correction |
| 296 | Post-Incident Test Integrity — Stale-Stylesheet Drift Guard & Fixture-Test Remediation |
| 297 | Theme Color System — Palette, Token Contract & Relocation |
| 298 | Test Feedback Loop — Scoped Inner Loop & Async Full-Suite Verification |
| 299 | Test Suite Parallelization & Isolation — Pest --parallel default + Isolated e2e Stack + Playwright in CI (non-gating) |
| 300 | Widget Color-Token Consumption Audit & Migration — 39 widget-SCSS hex/$color-* lines → var(--np-color-*) + no-hardcoded-colour Pest guard |
| 301 | Schemes, Template Page-Shell & Chrome, Shared Resolver — Default/Inverse content scheme + per-template selector + header/footer suppression + single shared public↔preview resolver + content-region bg/text gap closed |
| 302 | Naming & IA Polish — Re-Taxonomy Tail (arc-closing) — CMS-Settings → "Site Settings"; snippets section split into "Site Icon & Social Image" + "Custom Code Snippets"; Stripe-checkout branding relocated to FinanceSettingsPage; page-vs-content template IA clarity; theme-color-tokens doc consolidated; § E6 fully retired / Theme & Template Re-Taxonomy arc complete |
| 303 | Theme + Media Portability — self-contained content+media zip bundle (BundleArchive + archive-first import, zip-slip/bomb guards); queued export→stored-artifact→bell→gated-download + queued zip/JSON import (sync fallback removed); theme/design export-import (payload.design, deep-merge-not-sweep, build trigger, Theme-page pair); ID-preserving media seed (payload.media, explicit-id insert, media_id_seq reset, collision/orphan policy, queued conversion regen) + Media Library export/import; standard `notifications` table + Filament bell (user-approved infra); FORMAT_VERSION unchanged at 1.1.0, v2.3.0, both halves/phases in one iteration |
| 304 | Page Builder — Inline Editing Foundation (PARTIAL — Phase 2b not done) — Phase 1 interaction model (top-left drag grip, top-right Edit affordance→Inspector, body-click select, whole-panel select removed, labelled-chip drag proxy) + Phase 2 in-page text editing on TextBlock/Hero/ThreeBuckets/PricingChart: code-declared `inlineEditable()` gate plumbed live via WidgetResource (no migration); editors seed from / write RAW config (never serialized DOM); contenteditable + no-toolbar Quill, rAF-deferred mount, refresh-suppression/Quill lifecycle; shared inline-prose partial + WidgetRenderer `$inlineEditing` builder-only empty wrappers (public byte-safe); recursive nested-richtext sanitizer; discoverability + Quill-chrome-strip WYSIWYG; Tier-B exempt-set Pest guard; token-bearing prose made editable (owner decision, revises prompt's "mandatory token gate"). Phase 2b (repeater/column control) DEFERRED. Unplanned CI/e2e infra arc: /up gate, pg healthcheck, worker vendor-volume race, Vite-manifest un-shadow, migrate retry, admin-login readiness gate; e2e found to be ONE systemic cold-SCSS cause → 14 specs quarantined + playwright job parked + handed to parallel effort (sessions/playwright-test-fix.md); Pest merge-gate green. Off-local large-upload fix folded in (baked php uploads.ini 768M, nginx 768M/1200s, livewire temp-upload rule). v2.3.0, no schema, VERSION 0.304.1 |
| 305 | Page Builder — Inline Formatting Toolbar & Builder↔Public Parity (PARTIAL — toolbar rebuild + parity tests + gate widening deferred to 306+307) — First-cut on-page formatting toolbar built and discarded as under-specified + lifecycle-buggy (bound to a torn-down editor → corner-ghost). User produced full visual + behaviour design spec at **`docs/inline-formatting-toolbar-spec.md`** (820 lines, the implementation contract). Spec-aligned lifecycle pieces kept (outside-pointerdown teardown, reconcile-skip on within-widget switch, store active-editor handle stub); rejected first-cut bits stripped (component deleted, App.vue mount removed, speculative `.ql-font-*` SCSS removed, `inlineToolbarHoldingFocus` flag removed). Foundational companion changes landed for the rebuild: HtmlSanitizer extended to allow `target="_blank"`/`_self` + `rel` tokens `noopener`/`noreferrer` on `<a>` (strict allow-list; 8 new Pest tests); `refreshPreview()` centralises the `inlineActiveWidgetId` short-circuit so spec A13 is literally true (every refresh path automatically honours the suppression; legitimate reconciling refresh after teardown unaffected); page-builder bootstrap now carries `theme_heading_family` + `theme_body_family` (resolved via TypographyResolver) and a resolved `url` on every PageRef; `lucide-vue-next` installed (18 toolbar icons verified). Buttons-inline-label experiment built and **reverted** per owner decision — buttons stay Inspector-only this session (dedicated button-tool session implied later). Inspector hides content `text`/`richtext` fields for inline-editable widgets (RichTextField path kept for non-inline widgets). Simplified pricing column control replaces deferred 304 Phase 2b — `column_count`/`attribute_row_count`/`emphasized_column` dropdowns with `'auto'` default that derives from stored data → byte-identical render on the deployed marketing pricing page verified; collapse-and-keep semantics (lowering hides extras without deleting). Public-vs-builder render conflation split (latent 304 condition): `WidgetRenderer::render()` got an explicit `bool $inlineEditing = false` parameter, only `WidgetPreviewRenderer` passes true; public output no longer carries dormant editing markers / empty editable wrappers, visible content unchanged. Fast suite 2514 passed + 1 pre-existing parallel-order flake (DashboardBuilderApiController; 11/11 in isolation). v2.3.0, no schema, VERSION 0.305.1. Inline-editing arc continues: **306** rebuilds the toolbar to spec; **307** lands the builder↔public parity check + widens the eligibility gate (arc close). |
| 306 | Page Builder — Inline Formatting Toolbar Rebuild — Built `InlineFormatToolbar.vue` to `docs/inline-formatting-toolbar-spec.md`: app-level `<Teleport to="body">` mount (§A1/§A2/§C15), all 18 controls in 8 groups with Lucide glyphs, single shared active-editor handle in store extended to spec §A4 shape (`{ quill, hostEl, widgetId, configPath, getRect }`), §A14 MutationObserver on the preview-html container clears the handle synchronously on wholesale `v-html` replacement, format-state reflection via direct `selection-change`+`text-change` (editor-change aggregate unrolled to avoid getSelection re-entry), `mousedown.preventDefault` selection preservation, position tracking with viewport-top pinning + canvas-edge clamping, link popover with page picker + target/rel persistence through 305's sanitiser allow-list, colour/highlight popovers reuse `ColorPicker.vue` in panelOnly + new 2-D arrow-key grid navigation (§6.1/§K18), heroicon picker + image upload reuse existing endpoints, `prefers-reduced-motion` honoured, Alt+F10 keyboard entry, Cmd/Ctrl+K opens link. Two-row default layout (max-width 480px + flex-wrap) replaces spec §J collapse-ladder per owner UX preference (collapse math retained in version control for future amend). Manual UX walkthrough surfaced **eight bugs** in the inline-edit substrate that wouldn't have been caught by Pest alone, all fixed against Playwright reproductions: (1) initial getSelection race fixed by focusing before publishing the handle, (2) format buttons silently no-op'd because every Quill format API re-enters `selection.update→normalizedToRange` which throws on a native range whose anchor isn't in the editor scroll tree — fixed by `clearNativeSelection()` before every Quill call + switching format handlers to `formatText`/`formatLine` on the explicit `savedRange`, (3) right-click activates inline edit — fixed by gating activate + outside-teardown on `PointerEvent.button === 0`, (4) popover triggers don't toggle closed — added `togglePopover` helper, (5) toolbar persistently visible after click-outside / right-click / scroll because `cur.quill === quill` always returned false (Vue 3 wraps refs in reactive Proxies → Proxy never equals raw instance) — fixed by `markRaw(payload)` in store.setActiveInlineEditor, (6) bullet lists vanish on click into a field containing `<ul><li>` because direct-innerHTML seeding bypasses Quill's blot parser and Quill v2 strips unrecognised markup on next normalisation — fixed by seeding via `quill.clipboard.convert({ html }) → setContents(delta, 'silent')`, (7) Ctrl+Z wipes all editor content because direct-innerHTML seeding leaves Quill's Delta empty so undo replays back to that empty base — fixed by the same clipboard.convert seed path + `quill.history.clear()`, (8) popover clicks (link URL field, colour swatches) torn down the editor because teleported popovers sit outside both preview and bar DOM — fixed by extending `useInlineEdit`'s outside-pointerdown skip-list to include `[data-inline-toolbar-popover]`. Standalone Playwright iteration suite at `/tmp/diag-suite.mjs` (20 cases) drove diagnosis + verification; parked spec at `tests/e2e/page-builder/inline-formatting-toolbar.spec.ts` lands the fix-set in the e2e suite for re-enablement when the broader parallel e2e effort un-parks. Pest fast suite 2515 passed (same baseline as 305 — parallel-order flake passed this run). v2.3.0, no schema, VERSION 0.306.01. Architectural foundation for **307** (arc close — builder↔public parity tests + eligibility-gate widening) is load-bearing-tested. Inspector RichTextField has the same seed-via-innerHTML latent bug (deferred to housekeeping). |
| 307 | Page Builder — Inline-Editing Arc Close (Builder↔Public Parity + Gate Widening) — Two deliverables originally on 305 that 305 didn't run: (a) a standing HTML-layer builder↔public render-parity guard in the `design` Pest group (`tests/Feature/BuilderPublicRenderParityTest.php`, 16 cases across the inline-eligible widget set), asserting `WidgetRenderer::render($pw, ..., inlineEditing: true)` is appearance-equivalent to `inlineEditing: false` modulo three documented diffs (`data-config-placeholder` attribute, empty inline-prose wrappers, EventCalendar per-render random `id="cal-XXXXXXXX"`); `DesignGroupIntegrityTest` pinned list 19 → 20; complementary to the 296 served-bundle drift guard, distinct layer; (b) widened the code-declared inline-eligibility gate from 4 widgets (TextBlock / Hero / ThreeBuckets / PricingChart) to 13 by adding heading-only annotations on BarChart, BlogListing, BoardMembers, DonationForm, EventCalendar, EventsListing, MapEmbed, ProductCarousel, SocialSharing (DonationForm + EventsListing migrated from dormant 137-era literal annotations to the `inline-prose` partial; the other seven are new annotations); each widening edit kept the Tier-B exempt-set guard green (BarChart `x_label`/`y_label`, BoardMembers `image_aspect_ratio`, et al. remain un-annotated). Documented dual-purpose coupling on SocialSharing (heading also feeds share-link text on Bluesky / Mastodon / email subject — analogous to the Logo.text/alt coupling 304 accepted) — flagged for user confirm-or-revert. Doc/memory cleanup: archived `sessions/playwright-test-fix.md` per its self-archival instruction; updated two memory entries to reflect e2e un-parking on 2026-05-19. Fast Pest 2530 passed + the documented pre-existing `RecordDetailViewSeederTest` parallel-order flake (passes 5/5 in isolation; called out in 304/305/306 logs); +16 vs 306 baseline matches the 16 new parity cases exactly. v2.3.0, no schema, VERSION 0.307.01. **Inline-editing arc (304 / 305 / 306 / 307) fully retired** — see § E8 in `release-plan.md` and the "Page Builder Inline Editing" arc stub in `session-outlines.md`. Successor concern (visual / computed-style parity at the rendered-DOM layer) drafted as session 308. |
| 308 | Page Builder ↔ Public View — Visual Parity (Content Inside Widgets) — Diagnostic harness + six durable CSS/PHP fixes against the divergences it surfaced. Authored a Playwright visual-parity spec walking each inline-editable widget's DOM on both surfaces and diffing per-element `getComputedStyle` — used as session-internal diagnostic, torn down at close per the new template-base-prompt rule (Playwright is the CI signal; mid-session diagnostic specs don't survive). Fixes shipped: (a) TypographyCompiler `ul_li`/`ol_li` selectors gained `:not([data-list])` carve-out — Quill items no longer pick up `list-style-type: decimal/disc` from the typography defaults, resolving the double-bullet root cause; (b) `.np-site ol/ul { margin: 1em 0 }` restores browser-default outer margin against Tailwind admin preflight's reset, equalising the inactive canvas with public; (c) canvas typography scope dropped the redundant `.page-builder` ancestor (`.widget-preview-scope` / `.ql-snow .ql-editor` only) so canvas-side specificity matches public-side `.np-site …` — widget-specific rules at (0,2,0) like `.pricing-chart__subheading > :last-child` no longer lose only on the canvas; (d) `.np-site .ql-editor li[data-list]::before { content: none }` suppresses our marker while Quill's snow-theme `.ql-ui::before` draws its own — fixes the active-editor double-bullet/double-number; (e) `list-style-position: inside` on `.np-site li[data-list]` pins the value across surfaces (user-observed in DevTools, invisible to computed-style walk); (f) CSS-drawn solid-disc bullet replaces the U+2022 glyph (~40% larger, font-independent, matches the native-disc size the lists had before the content migration), and Quill snow theme overrides under `.np-site .ql-editor` (`font-family/size/line-height: inherit` + `li { padding-left: 0 }`) neutralise the active-editor heading-shrink + double-indent. **BarChart + ProductCarousel demoted from inline-eligibility** — `heading` field removed from both widgets (schema, defaults, demo configs, templates, dead SCSS); inline-eligibility roster 13 → 11 (`bar_chart` + `product_carousel` join `image` + `nav` in the excluded set). Cascading test updates: `BuilderPublicRenderParityTest` −2 heading-parity cases, `InlineEditingFoundationSession304Test` roster updated, `ProductCarouselTest` + `WidgetTypesSession098Test` heading assertions updated, `TypographyCompilerTest` selector substrings updated. Process artifacts: three session prompts + base prompts drafted for the content-import/export arc (309 opt-in theme + interactive import; 310 unified Import Site / Export Site UI rollup; A001 agentic backend extension to events/products/navigation/collections — first use of the `session-A###/N` async-agent branch convention). `sessions/tracks/code-review-and-cleanup.md` gained a new "Integration-seam sweep" standing improvement for Cycle 3+. Memory: `feedback_audit_integration_seams.md` (most app-level bugs are two existing systems wired slightly wrong, not greenfield gaps) + `feedback_questions_are_not_instructions.md` (questions get answers, not autonomous action). `template-base-prompt.md` updated with both the questions-not-instructions rule and an explicit "Playwright is CI signal; do not run locally as close-gate; mid-session diagnostic specs torn down at close" rule. **Destructive incident:** one-off Playwright spec was run with default env vars and wiped the local dev DB via `global-setup.ts`'s `resetDatabase()`; user lost a custom test page + any hand-edits to the seeded home page since last seed. Documented in the log + the new local-Playwright rule in `template-base-prompt.md` addresses the gap. Fast Pest 2529 passed (−1 net vs 307 baseline, accounted for by the two retired parity cases); Playwright run only during diagnostic development, not as part of close. v2.3.0, no schema, VERSION 0.308.01. Content-import/export arc (309 / 310 / A001) drafted. |
| 309 | Content Import/Export — Opt-in Theme + Interactive Import Prompts — First piece of the content-import/export remediation arc; integration-seam fix to the implicit theme-overwrite path surfaced during 308 typography debugging. **Exporter opt flags:** `exportPages`/`exportTemplates`/`exportBundle` accept `array $opts` with `with_design` + `with_media` (both default false); shared `attachOptIns()` helper attaches `payload.design` (from the three SiteSetting design rows) and a posture-B `payload.media` seed list (walks serialized page/template widget media descriptors → Media rows in `exportMedia()` shape); `ExportBundleJob` forwards opts to the exporter. **Importer analyzer + opts:** new pure-inspection `ContentImporter::analyze(array $bundle): array` returns a manifest (`has_design`, `design_keys`, `has_media`, `media_count`, `pages[{slug, exists_locally}]`, `templates[{name, type, exists_locally}]`) via batched `withTrashed` slug + template name/type lookups, no DB writes; `import()` signature gained `array $opts`, four flags gate the existing branches: **`merge_design` defaults to FALSE (the documented behaviour flip — old callers importing bundles with `payload.design` no longer silently overwrite Theme editor settings)**, `import_media` defaults true, `import_pages` defaults true (new flag — skipping all page entries can produce a 0-import per the user's design intent), `replace_duplicate_pages` defaults true at the service layer (UI defaults the checkbox OFF). Skip paths log `info` into ImportLog. `ImportBundleJob` constructor gained `public array $opts = []`. **Importer UI (live-reveal):** `ImportBundleAction` started as a two-step Filament Wizard and was collapsed to a single-page live-reveal at the user's call after the Wizard's internal "Next" button contradicted the modal's "Import" submit — `FileUpload->live()->afterStateUpdated()` runs `analyze()` on the temp upload via `resolveUploadedAbsPath()` (handles both Livewire `TemporaryUploadedFile` mid-flight and finalized-path-string post-submit shapes), seeds manifest into form state, pre-populates toggle defaults; bad bundles surface a danger notification and reset state (no throw — operator can swap files in place). Four `data-testid`-tagged toggles render conditionally below the upload, each visibility-gated on the manifest: **Import N pages** (default ON, `->live()` for cascading disable), **Replace duplicate pages** (default OFF, `->disabled()` when import_pages is off), **Replace site theme** (default OFF — the session's forcing function), **Include N media files** (default ON). Modal's single "Import" submit dispatches `ImportBundleJob` with the four-key opts derived from toggle state. **Export UI:** four entries per surface (page list bulk, post list bulk, page edit, post edit, page template edit, content template edit) — existing JSON + with-media (zip), plus NEW **Export with theme (JSON)** (`exportPages($ids, ['with_design' => true])`, streamDownload) and **Export with theme & media (zip)** (`ExportBundleJob` dispatch with both opts); both new entries `requiresConfirmation()` with the inbound-theme-override warning copy. **Tests:** Pest fast 2542 passed (+13 vs 308 baseline) — exporter opt-in defaults (2), with_media descriptor emission (1), combined design+media (1), `analyze()` on full / bare / malformed bundles (3), each importer opt's gating behaviour (4 — including the import_pages skip-all guard), and an end-to-end queue-path guard that asserts `ExportBundleJob`'s `handle()` forwards opts so a "theme & media" dispatch emits both `payload.design` and `payload.media` (1). 4 existing `BundleMediaPortabilitySession303Test` design-merge cases updated to pass `['merge_design' => true]` per the documented flip; 3 positional-`$mediaRoot` calls migrated to named-arg form. **Playwright:** `tests/e2e/importer/content-bundle-two-step.spec.ts` — one spec for the live-reveal happy path (upload triggers analyze, manifest summary appears in place, four toggles render with correct defaults and gating, no "Next" button exists). Filename retains "two-step" for git-blame continuity; describe text reads "live-reveal opt-in UI" — minor housekeeping carry-forward for 310. **Mid-session UX issue:** user's first end-to-end test produced a bundle with `payload.media` but no `payload.design` because the long-running queue worker was holding pre-`$opts` class state — worker container restart added to "Production deployment notes" in the log; the in-session queue-path Pest guard pins the dispatch→worker→exporter chain. v2.3.0, no schema, no `format_version` bump (still 1.1.0 — every change additive or restrictive). VERSION 0.309.01. Arc continues: **310** (unified Import Site / Export Site UI rollup) next; **A001** (agentic backend extension to events/products/navigation/collections) queued. |
| 310 | Content Import/Export — Unified Import Site / Export Site UI — Second piece of the content-import/export remediation arc; rollup surface built on top of 309's analyzer + opt-flag plumbing. **Four architectural calls resolved at session start, in the log, not relitigated at apply time:** (1) backup-grade vs button-grade → **button-grade v1** (users / roles / audit log / mailing-list-state out of scope; backup-grade is a future session); (2) diff preview → **no** (the analyzer's per-entity collision summary is sufficient for v1; row-by-row include/skip is a future session if workflow demands); (3) entity collision keys pinned per entity in the analyzer manifest (pages by `slug` via `Page::withTrashed()->whereIn`, templates by `(name, type)` composite, theme as a single conceptual row over the three SiteSetting design keys, media by `id` via the existing id-map plumbing; events / products / collections / navigation keys deferred to A001's call); (4) `format_version` → **no bump**, stays at 1.1.0. **`ContentExporter::exportSite(array $opts = []): array`** new wrapper — enumerates `Page::pluck('id')` + `Template::pluck('id')`, delegates to `exportBundle()`, defaults `with_design`/`with_media` to true so the rollup carries the theme + referenced-media seed list out of the box, callers can override either flag explicitly; assembly delegated to existing per-entity exporters, no new payload shape. **`ExportBundleJob` `'site'` kind:** new `case 'site'` arm in `handle()` routing to `exportSite($this->opts)`; constructor unchanged, no worker class-cache restart required (mid-session noted). **`SiteImportExportPage` Filament page** at `app/Filament/Pages/SiteImportExportPage.php` — Tools group, `manage_cms_settings` gate (heavier blast radius than per-entity `update_page` because rollup writes theme + media + templates + pages as one operation), two narrative sections (Export Site / Import Site) in the blade view, two header actions. Export Site: confirmation modal with `getSnapshotCounts()`-driven body ("X pages, Y templates, theme (N design keys), Z media files will be exported"), dispatches `ExportBundleJob('site', [], userId, 'site-snapshot', ['with_design' => true, 'with_media' => true])`, existing notification/download flow reused. Import Site: reuses 309's `ImportBundleAction::make()` live-reveal verbatim via a new `ability` / `name` / `label` parameter signature on the action (backwards-compatible — existing call sites unchanged at `update_page` gate; rollup page passes `manage_cms_settings`); same FileUpload→analyze→manifest summary→four toggle gating shape, no fork. **Tests:** Pest fast 2547 passed (+5 vs 309 baseline 2542) — five new `exportSite()` cases (enumerates pages+templates; default `with_design`/`with_media` to true; explicit opt override honoured; `ExportBundleJob` `'site'` kind forwards opts end-to-end queue-path guard; full `exportSite()` → `import()` round-trip with row counts intact). **Playwright:** `tests/e2e/importer/site-import-export.spec.ts` (snapshot-counts on Export Site + live-reveal happy path on Import Site, mirrors 309's spec shape) — ships as CI regression artifact, not run locally per the template-base-prompt Playwright-is-CI rule. **In-session housekeeping:** mid-session deploy was a GHCR `unknown blob` transient (registry-side, not code) — recovered on workflow re-run. Failing pre-existing Playwright spec `inline-editing-phase2.spec.ts:171 'data-driven widget is gated off at the widget level'` removed at user direction along with its `events_listing`/`dataDriven` setup, as a separate iteration commit on the branch (testing a non-issue per the user). **Planning artifacts landed alongside (not committed in iteration 1):** Code Shape & Fit track doc at `sessions/tracks/code-shape-and-fit.md` + pre-seeded Deliberately Kept register at `sessions/refactor-track-deliberately-kept.md`; session 311 prompts (Table Widget — picked for parallel-safe execution alongside A001 per A001's explicit out-of-bounds list); refactor-track Cycle 1 audit + apply prompts stored with placeholder filenames (`NNN.` / `NN2.`) pending slot assignment after cleanup Cycle 3. v2.3.0, no schema, VERSION 0.310.01. **Arc closure (user-driven side):** the Content Import/Export remediation arc closes here for the user-facilitated work; **A001** remains queued as a separate async-agent session — when it lands, the rollup automatically covers events / products / navigation / collections via the existing analyzer + `exportSite()` wrapper; only the manifest summary copy in `SiteImportExportPage` needs an additive note. |
| A001 | Content Import/Export — Extend Bundle Coverage to Events, Products, Navigation, Collections (agentic) — Backend-only pattern-extension of 309's exporter/importer + analyzer shape to four new entity types, executed agentically on a `session-A001/N` async branch alongside the user-facilitated arc. **Two architectural calls documented at session start, not relitigated at apply time:** (1) navigation lives in two relational tables (`navigation_menus` + `navigation_items`), not a SiteSetting row as the prompt's draft assumed — confirmed by reading `NavigationMenuResource` + the schema; the bundle key adjusted to `payload.navigation_menus` (plural list of `{menu, items}`) per the prompt's own "adjust the export shape to match" instruction; (2) no `import_id_maps` / `findByExternalId` plumbing exists in the 309 baseline (verified) — the A001 entities follow the 309 pattern: handle / slug as identity, denormalised name/email/slug as cross-entity references with graceful warn-and-null degrade. **Phase 1 — Navigation.** `ContentExporter::exportNavigation(array $menuIds)` emits `payload.navigation_menus: [{menu: {handle, label}, items: [{label, url, page_slug, target, is_visible, sort_order, children: [...]}]}]` (two-deep tree matching the editor); `ContentImporter::importNavigationMenu()` mirrors `NavigationMenuResource::saveItems()` exactly — upsert by handle, wipe items, two-pass parent-then-children insert with `page_slug → page_id` resolve-or-null; runs after `importPage()` so freshly-imported pages are visible to the slug resolver; `import_navigation` opt-flag (default true). **Phase 2 — Products.** `exportProducts(array $productIds)` emits `payload.products: [{product, prices, media}]` with the single `product_image` collection rolled in alongside (mirrors `serializePageMedia()`); `importProduct()` upserts by `slug`, wipe + re-insert prices canonical-from-bundle, `rewireProductMedia()` re-attaches the single media file; `import_products` (default true). **Phase 3 — Events.** `exportEvents(array $eventIds, array $opts = [])` emits `payload.events: [{event, tiers, media, registrations?}]` + `payload.pages: [...landing pages...]` (landing pages travel alongside via the existing page serializer so `landing_page_slug` resolves on re-import). Registrations are operator data: the `registrations` key is **omitted entirely** when `with_registrations=false` (default) — a re-import of an event without that key leaves any existing registrations on the target untouched; presence of the key (even when empty) means "registrations are canonical from bundle, wipe + replace." Tier back-references on registrations travel as `ticket_tier_name`; contact / org back-references as `contact_email` / `organization_name`; all three resolve-or-null gracefully. `importEvent()` runs after `importPage()` so the landing-page slug-lookup resolves; `import_events` (default true). **Phase 4 — Collections.** `exportCollections(array $collectionIds)` emits `payload.collections: [{collection, items}]` with item `data` JSON travelling verbatim (the collection's `fields` config is the shape's source of truth — the exporter never policies field shape) and per-item tags as `[{name, type}]`; `importCollection()` upserts by `handle`, wipe + re-insert items, per-item `tags()->sync()` after `Tag::firstOrCreate(['name', 'type'])`; `import_collections` (default true). Item-level media is **deferred** — `CollectionItem` implements `HasMedia` with dynamic media collections driven by the `fields` config, but A001's minimum-viable scope was three items / one tagged; future session can walk `fields` for image types and emit per-item media descriptors keyed by field, mirroring `serializeWidgetMedia()`. **Analyzer extensions:** four new top-level keys on the manifest — `navigation_menus[{handle, label, items_count, exists_locally}]`, `products[{slug, name, prices_count, exists_locally}]`, `events[{slug, title, tiers_count, registrations_count, exists_locally}]`, `collections[{handle, name, items_count, exists_locally}]` — all batched via `whereIn` on the identifier column, no DB writes. **Import-loop wiring:** four new conditional blocks in `ContentImporter::import()`, all inside the existing `DB::transaction()`. Default opts grew from five keys to eight (`import_navigation`, `import_products`, `import_events`, `import_collections` added, all default true). **Tests:** 11 new round-trip / analyze / opt-flag cases in `tests/Feature/ContentImportExportTest.php` (4 navigation, 4 products, 3 events including the registrations leave-alone-on-default guard, 3 collections including the verbatim-data + tag-attachment assertions). **Process notes:** mid-session the user's 310 work landed on origin/main between session start and Phase 2; rebased `session-A001/1` onto origin/main, resolved two trivial textual conflicts (test-file insertion before the same `'skips payload.media'` anchor + VERSION precedence) with no code-level overlap (310's `exportSite()` and A001's `exportNavigation()` are in physically separate locations). Diff scope is exactly the four allowed files: `VERSION`, `app/Services/ImportExport/ContentExporter.php`, `app/Services/ImportExport/ContentImporter.php`, `tests/Feature/ContentImportExportTest.php`. **No hard-no file touched** (no widget code, no SCSS, no Filament Pages, no asset/typography/color compiler, no migrations, no envelope `format_version` bump — stays 1.1.0). v2.3.0, no schema. VERSION 0.901.04 (agentic-session band — see log architectural call #3 for why `0.A001.X` from the base-prompt draft was renumbered to the `0.9XX.X` band). Agent self-close — user merges manually after review. |
| A001/3 | Content Import/Export — Manifest Layer + SiteSettings (agentic) — Third iteration of A001 arc. Branched off main after the user merged /1 (PR #10) and /2 (VERSION hotfix PR #11). **Four architectural calls resolved in conversation before any code, not relitigated at apply time:** (1) manifest shape — top-level block on existing envelope (Shape A) over multi-file zip directory tree (Shape B), chosen because additive on existing 309/310/A001 envelopes, preserves check-into-git single-file JSON shape, and Shape B's "visible structure" advantage is mostly mooted because operators interact through the UI; (2) scope — manifest + SiteSettings together (Option 2 of three phasing options) so the manifest design gets real-world exercise with its first consumer; forms / widget_presets / unified-rollup UI deferred to follow-ups; (3) manifest is documentation + integrity + provenance, NOT a security gate — bundle-borne policy_hints are UX hints only, deny-list lives in importer code, count mismatches log warnings rather than reject the import (a malicious bundle could claim "0 pages, all clean" while smuggling 50); (4) `import_site_settings` opt defaults to FALSE matching 309's `merge_design` posture (SiteSettings carry brand/identity values an operator may not want overwritten). **Phase 1 — Manifest layer.** `ContentExporter::envelope()` extended with `?string $sourceAction = null, array $policyHints = []`; new top-level `manifest: {sections: [{key, count}], policy_hints: object, exported_with: string, exporter_version: string}` stamped alongside `payload`. All 11 `exportX()` methods pass their own name as source action; `exportSite()` uses a new `?string $sourceActionOverride` parameter on `exportBundle()` so the manifest correctly stamps `exportSite` rather than the delegate `exportBundle`. `ContentImporter::verifyManifest()` cross-checks declared section counts against payload reality, returning human-readable warnings (`"Manifest section 'pages' declared 99 items, payload has 1."`). Stray payload keys not declared in manifest tolerated (forward-compat for new section types). `analyze()` return shape extended with `manifest: array|null` (full bundle manifest passed through verbatim) + `manifest_warnings: string[]`. `import()` runs the verifier ahead of the `DB::transaction()` and logs each mismatch into `ImportLog` (warning-level, never reject — import proceeds). Backwards-compatible — bundles without a manifest skip the check entirely. **Phase 2 — SiteSettings exporter + importer.** Security constants on `ContentImporter` (the security boundary): `SITE_SETTINGS_ALLOW_LIST` (~30 explicit visual/SEO/routing/publishing-default keys, new keys land here on review only), `SITE_SETTINGS_DENY_PREFIXES` (`stripe_`, `qb_`, `quickbooks_`, `mailchimp_`, `mail_`, `resend_`, `build_server_`), `SITE_SETTINGS_DENY_REGEX` (`/(_api_key|_secret|_token|_password|_webhook)/i` tail-guard), `SITE_SETTINGS_DENY_NAMED` (`installation_completed_at`, `horizon_enabled`). `isSiteSettingKeyDenied(string $key): bool` helper used by both exporter (pre-emptive filter at emit time) and importer (defensive re-check at write time) — neither side trusts the other. `ContentExporter::exportSiteSettings()` reads `whereIn('key', ALLOW_LIST)`, drops anything matching the deny-check (defence-in-depth in case of accidental overlap), drops `type === 'encrypted'` rows, emits `payload.site_settings: {key: {value, type, group}}`, stamps `policy_hints: {allow_list: [...]}` for transparency (bundle is self-documenting about what slice it carries). `ContentImporter::importSiteSettings()` re-applies `isSiteSettingKeyDenied()` to every inbound key regardless of presence on any allow-list (tampered-bundle defense), hard-skips encrypted-type entries with a warning, sanitises values for keys in `SiteSetting::RICH_TEXT_KEYS` via `HtmlSanitizer::sanitize()` (matching the model's `set()` boundary), writes via `updateOrCreate` + `Cache::forget`. Runs ahead of design/templates/pages in the import loop so downstream `SiteSetting::get()` reads see the imported values. `import_site_settings` opt-flag (default FALSE per security posture). **Analyzer extension:** `site_settings: {keys, count, blocked_keys}` — UI can surface "this bundle contains 28 keys (3 blocked: stripe_secret_key, mail_password, …)" without operator inspecting the JSON. **Tests:** 12 new round-trip / guard / opt-flag / analyze cases in `tests/Feature/ContentImportExportTest.php` (5 manifest: stamping on every exporter, `exportSite` source-action override, analyze surfaces manifest + tampered-count warnings, `ImportLog` warning emission, backwards-compat no-manifest import; 7 SiteSettings: positive round-trip across six representative keys, secret-leak guard with 9 secret-shaped seeded rows + `json_encode` sentinel-string assertion, importer re-block of tampered bundle, encrypted-type skip even when not deny-listed, default-OFF opt respects existing values, analyze allow + deny surfaces, allow-list snapshot in manifest policy_hints). **Default opts:** `defaultImportOpts()` grew from 8 keys to 9 (`import_site_settings` added at default FALSE). **No envelope `format_version` bump** — manifest is additive sibling, not a breaking change. **Hard-no list audit:** diff scope exactly four files (`VERSION`, `ContentExporter.php`, `ContentImporter.php`, `ContentImportExportTest.php`) plus log + index — no widget code, no SCSS, no Filament Pages, no API controllers, no asset/typography/color compiler, no migrations, no envelope `format_version` bump. **Targeted Pest: 68 passed (+12 over /1 baseline of 56).** Full fast suite to be re-run by operator against canonical `./dev test` Docker infra before merge (agent's remote-execution env lacks Redis / asset pipeline / third-party sandbox credentials). v2.3.0, no schema, VERSION 0.901.06. **Carry-forward:** forms (next content-shaped section, identical pattern), widget_presets (depends on whether install actually uses them vs seeded-only), custom_field_defs (needs CRM-vs-CMS scope call), unified rollup UI extension (SiteImportExportPage toggle + exportSite call), per-section integrity hashes in manifest (additive, no envelope churn needed). |
| 311 | Page Builder Picker Hardening + Typography Bucket Fix (originally scoped as Table Widget; rewound mid-session) — The Table widget scope was reset off the branch after the user identified the original prompt had captured the wrong feature shape (intended: a Quill toolbar "Insert Table" affordance; built: a standalone Page Builder widget). E15 remains open. Session pivoted to three pre-existing bug fixes the user surfaced during the rewind window. **Iteration 1 — Page Builder picker slot filter (4 lines, 2 files):** `app/Livewire/PageBuilder.php` and `app/Http/Controllers/Admin/PageBuilderApiController.php` were the only `WidgetType::forPicker()` callers not passing a slot handle, so the eight backend-only widgets (`recent_donations`, `membership_status`, `recent_notes`, `this_weeks_events`, `memos`, `quick_actions`, `random_data_generator`, `setup_checklist`) leaked into the Page Builder picker and could be attached to public Pages. Fix passes `'page_builder_canvas'` from both call sites (matching the long-standing `DashboardBuilder` + `RecordDetailViewBuilder` patterns). Defense-in-depth in `WidgetRenderer::render()` deliberately deferred — picker fix closes the leak at the source. **Iteration 2 — typography bucket → element cascade + nav font CSS path (5 files):** two pre-existing bugs. *Bug 1 (heading/body):* `TypographyPanel.vue::onBucketChange` cascade was gated on `previous && next`, so a fresh install (`buckets.heading_family = null`, elements at the shipped `'Inter', system-ui, sans-serif`) skipped the cascade entirely on the first bucket change — per-element `font.family` values stayed at Inter, the compiler kept emitting `font-family: 'Inter'`, the literal element rule beat the `--font-family-heading` :root variable. Fix: cascade now runs on any null→value transition; elements matching either the previous bucket value OR the shipped default family follow the bucket. Default family sourced via the bootstrap (`DesignSystemPage::getTypographyBootstrap()` now emits `defaultFamily`) to avoid PHP/JS drift. *Bug 2 (nav):* `buckets.nav_family` was saved + loaded the Google Font but no CSS path consumed it (compiler's per-element output excludes nav via `:not(nav h1)` carve-outs; `_base.scss` has no nav rule; layout didn't emit `--font-family-nav`). Fix: `public.blade.php` now emits both `--font-family-nav` alongside heading/body AND an inline `.np-site nav, .np-site nav a { font-family: <stack> }` rule — direct, no SCSS rebuild required. Closes the same gap A001/4 had filed for a numeric session (`sessions/theme-typography-bucket-rendering-gap.md` deleted as stale). **Iteration 3 — small cleanup:** `.site-footer { padding: 1.5rem 0 }` rule removed from `_custom.scss` (was overriding the layout-tools control of footer spacing); stale typography planning doc deleted. **Merge from main:** A001/4 (cloud session — `exportSite()` enumeration of all A001 entities + nav widget config rewiring) merged in mid-session with one VERSION conflict (resolved to 311 track at `0.311.02`; A001 stays on `0.901.NN`). **Housekeeping queue prep (uncommitted per user direction):** the two placeholder code-shape session prompts (`NNN.` audit / `NN2.` apply) renumbered to 312/313 and base prompts authored. Cycle was originally scheduled for ~324 in the track doc; pulled forward at user direction. Calibration delta is flagged in 312's base prompt. **Tests:** fast Pest at 310 close baseline 2547 → 2580 after A001 merge (+33 from cloud iterations) → 2580 after iteration-2 typography fix (no test delta — typography group at 24 cases verified green standalone) → 2580 after iteration-3 cleanup. Pre-existing `RecordDetailViewSeederTest > it is idempotent across re-runs` parallel-DB key-order flake (documented in 304 / 305 / 306 / 308 / 309 logs) tripped twice mid-session and cleared on rerun, same shape as before. Playwright not run locally per the template-base-prompt CI-is-signal rule. **Branch + version:** `session-311/1`, three iteration commits + A001 merge commit, not pushed per user direction. VERSION 0.311.02. v2.3.0, no schema. **Carry-forward:** E15 (Table widget) stays open with a prompt rewrite needed; renderer-side allowed-slot guard informally queued; Code Shape & Fit Cycle 1 next pair (312 audit / 313 apply, base prompts already in place). |
| A002/2 | Public-Bundle Consolidation — One CSS, One JS (agentic) — Removal-driven cleanup pass on the public-render asset pipeline; diagnosed at A001/4 close and scheduled as an emergent agentic session, not against any `release-plan.md` entry. The public layout's intended shape is exactly one bundled `<link rel="stylesheet">` + one bundled `<script>` sourced from `AssetBuildService::build()` → `public/build/widgets/manifest.json`, plus a small set of intentional inline blocks (font-family `:root` vars, template `custom_scss` `<style>`, raw operator head/body snippets). Three redundant paths shadowed that contract: (1) per-instance inline emission via `WidgetRenderer::render()` concatenating `widget_types.css` / `widget_types.js` into accumulators threaded through `PageBlockRenderer` / `ChromeRenderer` / `PageController` / `PostController` and dumped into a catch-all `<style>` + `<script>` block in the layout — no-op for seeded widgets but a per-instance double-emit footgun the moment any operator filled the Filament `WidgetTypeResource` css/js textareas (every widget instance leaked the bundled content back inline per page render); (2) the dead `$widgetAssets = ['css' => [], 'js' => [], 'scss' => []]` collection plumbing populated by `WidgetRenderer::collectAssets()` in four places with zero consumers anywhere in `app/` / `resources/` / `tests/`. **Phase 1 — retire `$widgetAssets`.** `WidgetRenderer::collectAssets()` deleted + its `WidgetType` import; `&$widgetAssets` reference parameter removed from `PageBlockRenderer::renderLayoutBlock()` + `ChromeRenderer::renderLayoutBlock()` (with the matching `collectAssets()` call removed in each); `$widgetAssets` locals + `compact()` keys + `WidgetRenderer` import removed from both controllers; `$assets` accumulator + `'assets'` return-shape key + matching PHPDoc clauses removed from `ChromeRenderer::renderPage` / `render` / `renderById`. Guard test (`tests/Feature/WidgetAssetsCollectionRetiredGuardTest.php`) — five source-grep assertions, one per edited file — protects against re-introduction. **Phase 2 — retire per-instance inline widget CSS/JS emission.** The three `if ($widgetType->css) { $styles .= … }` / `if ($widgetType->js) { $scripts .= … }` appends in `WidgetRenderer::render()` (two in the `server` branch, one in the `client` branch) deleted; the `<style>$__allInlineStyles</style>` + `<script>$__allInlineScripts</script>` blocks (and the `@php` preludes that folded chrome-render carry-over into them) removed from `resources/views/layouts/public.blade.php`. `WidgetRenderer::render` still returns the `['html', 'styles', 'scripts']` shape but the two string keys are always-empty post-Phase 2 (no callee writes to them). The bundle continues to ship the same widget CSS/JS content via `AssetBuildService::build()` at lines 477-495 — unchanged this session. Close-gate regression (`tests/Feature/PublicLayoutBundleConsolidationTest.php`): seeds two widget types with distinctive marker CSS + JS strings, attaches them to a published Page, writes a fake `public/build/widgets/manifest.json` (snapshot+restore in `beforeEach`/`afterEach`), GETs the page through real routing + controller stack, asserts exactly one `<link [...] href="/build/widgets/...">` + one `<script [...] src="/build/widgets/...">` emitted from the manifest and neither marker string appears anywhere inline. **Phase 3 — Option B (defer).** Template `custom_scss` stays request-time inline at `public.blade.php:177-191` for now; moving it into the bundle would introduce a `body.page-using-template-{handle}` class contract rippling through the layout and possibly the page-builder canvas, and the agentic remote-execution environment has no Docker daemon so the canonical `./dev test` PostgreSQL-backed suite cannot run locally — landing a body-class contract change blind to the full-suite signal was not a trade worth taking. The in-code "moves to build server post-beta" comment remains accurate; deferred to a numeric session. **Diff scope:** 8 files: `app/Services/WidgetRenderer.php`, `app/Services/PageBlockRenderer.php`, `app/Services/Media/ChromeRenderer.php`, `app/Http/Controllers/PageController.php`, `app/Http/Controllers/PostController.php`, `resources/views/layouts/public.blade.php`, `tests/Feature/WidgetAssetsCollectionRetiredGuardTest.php` (new), `tests/Feature/PublicLayoutBundleConsolidationTest.php` (new). Plus `VERSION` + session log + completed-sessions row. No hard-no file touched: zero changes to `app/Services/TypographyCompiler.php`, `app/Services/ColorTokenCompiler.php`, `app/Widgets/*`, `resources/scss/*`, `app/Filament/Pages/*`, Fleet Manager surface, migrations, or `app/Services/ImportExport/*`. **Notes for follow-up.** (a) The `string &$inlineStyles, string &$inlineScripts` reference params on `PageBlockRenderer::renderLayoutBlock` + `ChromeRenderer::renderLayoutBlock` are always-empty accumulators after Phase 2 — explicitly out-of-scope this session per the session prompt's "do not expand scope" rule; a cleanup pass can drop them. (b) `resources/views/layouts/portal.blade.php` still carries an `@if (!empty($inlineStyles)) <style>{!! $inlineStyles !!}</style> @endif` block (and the script mirror) — the portal layout is a separate render path and not the public layout this session targeted; the `@if` no-ops gracefully when the accumulator is empty, so no broken HTML, but a follow-up can remove it for the same always-empty reason. (c) "Watch this on first deploy" — if any operator had filled the Filament `WidgetTypeResource` css/js textareas, their content continues to ship via the build-server bundle (unchanged); the only behavioural difference is the disappearance of the duplicate inline emission. No regression risk for the no-operator-customisation case (the accumulators were no-ops there anyway). **Tests:** CI is the authoritative full-suite signal — the agent's remote-execution environment has no Docker daemon and cannot run the canonical `./dev test` PostgreSQL-backed Pest suite locally. Each changed PHP / Blade file syntax-checked clean under `php -l` before commit. New cases: +5 it-blocks in `WidgetAssetsCollectionRetiredGuardTest` + 1 it-block (6 expectations) in `PublicLayoutBundleConsolidationTest` = +6 to the fast suite. **Session shape:** A002/1 (prep — base prompt + session prompt drafted) merged via PR #14 before this iteration; A002/2 is the implementation iteration. v2.3.0, no schema, VERSION 0.902.02. |
| A001/4 | Content Import/Export — Site Rollup Coverage + Nav Widget Rewiring (agentic) — Fourth iteration of A001 arc. Surfaced by user's first real end-to-end round-trip of A001/3's site-import/export pipeline (export → wipe DB → import) which reported two bugs: (1) nav widget unassigns itself from its menu on round-trip, (2) header/nav fonts rewrite to Georgia. Investigation showed bug 2 is a pre-existing **bucket-vs-element rendering gap** in the theme typography pipeline (heading + nav buckets emitted as CSS variables but never consumed by any SCSS rule; per-element families win on specificity when set; export/import round-trip is faithful). Filed bug 2 as `sessions/theme-typography-bucket-rendering-gap.md` for a numeric session — fix is in `resources/scss/_base.scss` + layout, outside agentic hard-no scope. This iteration fixes bug 1 + the structural underpinning that made it manifest. **Phase 1 — `exportSite()` enumerates A001 entity types.** A001/3 deliberately left `exportSite()` covering only pages + templates (310's original scope). Real round-trip surfaced the consequence: full-site bundle had no `navigation_menus` payload, so menus weren't restored, so even a correctly-rewired widget would have nothing to reference. `ContentExporter::exportSite($opts)` rewritten to enumerate `Page` + `Template` + `NavigationMenu` + `Product` + `Event` + `CollectionModel` ids directly (no longer delegates to `exportBundle()`), assembling a payload with all six sections plus opt-in design + media. New opt `with_registrations` (default false) mirrors the per-entity `exportEvents()` flag — registrations are operator data, not template content. Manifest section list covers all six entity types automatically at envelope-build time. **Phase 2 — Nav widget config rewiring.** Verified via grep that the Nav widget is the ONLY entity-referencing widget storing its reference as a UUID rather than slug/handle: `Nav.navigation_menu_id` is the lone outlier, while `BoardMembers/LogoGarden/Carousel/BarChart` use `collection_handle`, `EventDescription/EventRegistration` use `event_slug`, `WebForm` uses `form_handle`, `ProductDisplay` uses `product_slug`. When a nav menu is recreated on import with a fresh UUID (because `importNavigationMenu` upserts by handle, not id), the widget's stored `navigation_menu_id` becomes dangling. Fix mirrors the existing `template_name` / `page_slug` patterns. **Export:** `ContentExporter::serializeWidgetConfigReferences()` (new) walks each widget's config and, when widget handle is `nav` and config has `navigation_menu_id`, looks up the menu and adds `navigation_menu_handle` to serialized config alongside the UUID (additive — bundle carries both for legibility + legacy-importer fallback). `serializeWidget()` now routes config through this helper. **Import:** `sanitizeWidgetConfig()` attempts handle resolution during widget hydration (best-effort; menu may already exist on target), but the authoritative resolution happens in a new post-pass `relinkNavWidgetMenus()` that runs after both pages AND navigation menus have landed inside the same transaction. Post-pass walks every nav widget on the install, re-resolves its handle to current menu_id, warns + nulls if the handle can't be matched anywhere. Mirrors the existing `relinkTemplateChrome` two-pass pattern. Legacy bundles (pre-A001/4, UUID only, no handle) — importer verifies UUID against existing menus; keeps it if found, warns + nulls if not. `relinkNavWidgetMenus()` runs unconditionally when `import_pages` is on (covers the case where `import_navigation` is off but pages still need their nav widget refs reconciled against existing menus on target). **Tests:** 6 new Pest cases (74 total, +6 over A001/3 baseline of 68) — `exportSite` includes all 6 entity types in payload + manifest; `exportSite` with `with_registrations=true` includes registrations; exporter emits `navigation_menu_handle` alongside the UUID on nav widgets; **end-to-end round-trip proof:** create page with nav widget pointing at menu, `exportSite`, delete menu + widget, import bundle, assert menu recreated with NEW UUID and widget's config rewired to new UUID; warn-and-null on bundle that references a ghost menu (no menu in bundle, none on target); legacy bundle (pre-A001/4 UUID-only) handling. **Diff scope:** 4 code files (`VERSION`, `ContentExporter.php`, `ContentImporter.php`, `ContentImportExportTest.php`) + 2 session-docs (`A001-4 log`, `theme-typography-bucket-rendering-gap.md` writeup) + completed-sessions row. No file under hard-no list touched (no widget code, no SCSS, no Filament Pages, no API controllers, no asset/typography/color compiler, no migrations, no envelope `format_version` bump — stays 1.1.0). **Deployment notes:** `exportSite` bundles are now larger (include menus + products + events + collections). Legacy bundles continue to work unchanged. **Carry-forward:** bug 2 (theme typography bucket-vs-element gap) filed for numeric session; SiteSettings still not in `exportSite()` (one-line add when operator chooses; default-OFF posture worth considering); forms / widget_presets / custom_field_defs still queued. v2.3.0, no schema, VERSION 0.901.07. |
| 312 | Code Shape & Fit (Audit) — Cycle 1 — First audit of the Code Shape & Fit track. Pulled forward from track-doc-anticipated trailing-pair-after-cleanup-Cycle-3 slot (≈ session 327+) to slot 312 at user direction; consumed cleanup Cycle 2 outputs (271 / 273) as W11 outlier pre-loaded findings instead. **No code changes per the track's audit rule.** Five workstreams walked (W-R1 Cardinality / W-R2 Locality / W-R3 Directness / W-R4 Necessary vs accidental / W-R5 Seam quality). **Two inline findings landed:** W-R1-1 (`TypographyResolver::resolve()` called twice for sibling typography keys in `PageBuilder.php:407` + `:413` page-builder bootstrap; hoist one call above both closures; cited from the comment block at `PageBuilder.php:400-405` referencing session 305 §6.3), W-R4-1 (source-context + session-label re-derived across `ImportSessionActions::approve()` and `::rollback()`; extract `sessionContext()` helper; cited from the class docstring at `ImportSessionActions.php:20-32`). **Three Open Refactor Flags:** W-R1/A (`PageBuilderApiController::buildTree()` returns triple-key superset; each of 7 endpoints destructures a different slice; `WidgetType::requiredForPage()` derived at 156 + 527; `collectLibs()` parallel loops at 553 + 567; default leaning instance-fix — pick most-wasteful caller and introduce `buildTreeMinimal()`), W-R2/A (`ImportContactsPage` 1014 LOC standing won't-fix from cleanup Cycle 1 Flag B re-affirmed at Cycle 2; Locality lens affirms the responsibilities are contact-specific and not extractable to a per-type config without losing cohesion; default leaning promote to Deliberately Kept register), W-R4/A (pattern-fix scale of W-R4-1 across three description-generator methods on `ImportSessionActions`; default leaning instance-fix only). **Two intent-unrecoverable findings:** `WidgetType::requiredForPage()` re-derivation in `PageBuilderApiController::destroy()` + `buildTree()` (real structural duplication within a single request lifecycle but no recoverable design intent for the controller-seam derivation); `WidgetPreviewRenderer::collectLibs()` parallel loops at lines 553 + 567 (real structural duplication but the loop-order intent is undocumented). Both carry forward to next cycle's W-R5 documentation-gap walk. **W-R3 (Directness) and W-R5 (Seam quality) clean:** ~200+ public methods scanned for W-R3 (single-caller candidates ImportSessionPreview / MapEmbedParser / HelpArticleService all skipped on semantic-weight or multi-path grounds); four class pairs walked for W-R5 (`ImportSessionActions` + `InteractsWithImportProgress`, `PageBuilderApiController` + Vue store at `resources/js/page-builder-vue/stores/editor.ts`, `HealthController` + `BackupController`, observer-granularity scan across `ContactObserver` / `DonationObserver` / `EventObserver`) — all correctly placed. The `PageBuilderApiController` + Vue store seam surfaced briefly as a candidate (22 endpoints, mostly per-action CRUD-shape mapping 1:1 to Vue store actions) but was promoted to a Deliberately Kept register addition rather than left as a candidate — per-action endpoints support optimistic UI / per-action revert / per-action retry / undo, collapsing to a bulk save would be a regression in those dimensions. **Proposed Deliberately Kept register revisions for 313 walkthrough:** (a) accuracy fix on the FieldMapper carve-out (entry says "ContactFieldMapper" but actual file is unprefixed `FieldMapper.php` per the legacy naming-asymmetry; no `OrganizationFieldMapper` exists despite Organizations importer landing at 256 — walkthrough decides deliberate-exclusion vs. follow-on-candidate); (b) new addition: `PageBuilderApiController` per-action endpoint shape (with citation to 273 apply log § /6 editor.ts composable extraction + `api.ts:82-90` baseUrl documentation). No retirements proposed — all three existing register entries' citations spot-checked and resolved cleanly (session 259 + memory entries + per-type FieldMapper files exist; session 304-308 logs + active Page Builder code exist; Page vs View memory entry + Pages/Views in codebase). **Permanent inter-cycle artifact:** `sessions/tracks/code-shape-and-fit-finding-template.md` created — four-column row format with worked examples per workstream + intent-citation discipline (valid forms: test name, docstring quote, commit message, session log path, memory entry slug; invalid forms: "I think it's trying to…", inferred-from-variable-names, cargo-culted-from-google) + class-finding row shape + W-R5 fifth-column variant + intent-unrecoverable shape + interaction with the Deliberately Kept register + Open Refactor Flag format + W-R5 candidate sub-section rule. Audit operates on **post-A002 codebase state**: cloud session A002 (public-bundle widget asset consolidation) landed on main between session start and the W-R1/W-R4 walks; merged into `session-312/1` via fast-forward after user confirmed no parallel work running. A002 retired the `$widgetAssets` collection plumbing + per-instance inline CSS/JS emission — pre-emptively closed what would have been W-R3 / W-R4 findings in the widget-asset path; net effect zero W-R3 findings landed and the W-R4 walk did not surface widget-asset accidental work. **Citation-discipline calibration (process incident, instructive for next cycle):** the W-R1 walking agent produced three findings with citations that looked like docstring quotes but did not match the actual file content on spot-check. Two reclassified to intent-unrecoverable; one landed as W-R1-1 with a corrected citation (the real intent-comment exists at lines 400-405, not where the agent placed it). The W-R4 agent's claimed `Event::getIsFreeAttribute` docstring did not exist; that finding was dropped (the work IS necessary — the underlying value can change — and caching would be accidental complexity in the model layer). Next refactor cycle's audit-agent prompt scaffolding should add an explicit "quote the citation verbatim and include line numbers of the quoted text" rule. **Drift:** session prompt references `sessions/tracks/code-shape-and-fit-deliberately-kept.md`; actual file is `sessions/refactor-track-deliberately-kept.md` — minor doc drift, not edited this session. Session prompt's "Before writing any code" pointer to `resources/js/page-builder/editor.ts` had the wrong path; actual store lives at `resources/js/page-builder-vue/stores/editor.ts` (W-R5 agent failed to find it on the prompted path; resolved by targeted fill on the corrected path). **Tests:** fast Pest at session start 2580 / 0 (311 close baseline), 2586 / 0 after A002 merge (+6 from A002's regression-guard tests `PublicLayoutBundleConsolidationTest` + `WidgetAssetsCollectionRetiredGuardTest`); audit changed no code so 2586 carries to 313 as baseline. No new tests this session. Playwright not run (audit-only). **Branch + version:** `session-312/1`, single close commit, not pushed. VERSION 0.902.02 (carried from A002 close — no VERSION bump because audit changes no code). v2.3.0 contract, no schema, no migrations. **Carry-forward:** 313 session prompt placeholder sections (Open Refactor Flags, W-R1 through W-R5 backlogs, Deliberately Kept register walkthrough, intent-unrecoverable, suggested iteration shape) populated with the audit's findings + default leanings; Cycle 1 retrospective in the track doc lands at 313 close, not here (per the track doc's "When a cycle closes" rule); Deliberately Kept register edits are 313 walkthrough work — the audit captured proposals, not edits. |
| A003/2 | Playwright e2e Re-Stabilization (agentic) — Cloud-agent stabilization of the `Playwright e2e (isolated stack)` CI job after sessions 305–311 + A001/4 + A002/2 left it red on every recent merge. Methodology mirrored `sessions/archived/playwright-test-fix.md` — evidence-first, committed digest channel published to a `ci-logs/playwright` branch readable via GitHub MCP, restore-on-handback — but the prior session's cold-runtime-SCSS diagnosis was correctly excluded by the first digest (slowest-15 specs clustered naturally; no uniform-timeout signature). **Phase 1 — diagnostic plumbing.** `.github/workflows/tests.yml` gained DIAGNOSTIC blocks (Pest `if: false`, Playwright `needs: slow` dropped, JSON+stdout+rc capture, digest publisher with in-container probes for `storage/logs/laravel.log` / `public/build` state / delivered HTML head); new `scripts/ci/pw-digest.mjs` rendered the JSON report. First digest landed 1 unexpected failure: `site-import-export.spec.ts:18` — `[data-testid="site-export-section"]` not found. **Phase 2 — diagnosis (one spec, one structural call).** The s310 spec navigated `/admin/site-import-export`, but Filament's auto-slug convention preserves the `Page` suffix as `-page` for every other Filament page in the codebase (`ImporterPage` → `/admin/importer-page`, etc.); actual route is `/admin/site-import-export-page`. URL fix (decision-tree a) at `0936647`; spec then failed on the modal-open step. Three follow-on diagnostics + a final in-spec instrumentation push (`3155bbb`) captured the full DOM: the `requiresConfirmation()` modal mounts server-side correctly (innerText contains live snapshot counts "15 pages, 6 templates, theme (2 design keys), 44 media files"), but Alpine's `isOpen` toggle never fires in headless chromium. The same `requiresConfirmation()` pattern works for table actions (importer approve) and `->form([...])` modals work for Resource ListPage header actions (Import Bundle on `/admin/pages`); only the **header-action `requiresConfirmation()` modal on a custom `Filament\Pages\Page`** combination fails in CI. **Phase 3 — disposition + flake-zero policy.** User decision-tree (b) — delete the failing test; document the coverage tradeoff in `[test-integrity]` housekeeping. Surfaced full flake history across six A003 runs; user stance: flaky tests can't stay if no working fix exists, per-spec `waitForTimeout` bumps aren't the answer when CI variance is the root cause. Five additional flaky tests deleted at `bc6f541` (full-width-matrix Editor describe block; events-update-strategy whole file; content-bundle-two-step whole file; donations-happy-path whole file; donations-mapping-indicator line-24 only — kept line-80 Choices.js search-input test which never flaked). Each deletion landed a `[test-integrity]` housekeeping line with the coverage tradeoff. Coverage preserved via `FixtureRunner` Pest (`tests/Feature/Generated/ImportFixtureRunnerTest.php` runs all 7 importers × 4 shapes off-Livewire) for data paths, plus sibling Playwright specs for UI-required surfaces. **Phase 4 — Test Audit track lift.** A003's flake-chasing surfaced a class of bug — Playwright specs committed without running locally, ~half the importer Playwright suite duplicating FixtureRunner Pest coverage, an accumulated `[test-integrity]` housekeeping backlog dating to s296 — that warrants its own recurring audit track. Lifted `sessions/tracks/test-audit.md` (50-session cadence, single-session-by-default shape with carve-out option, four audit dimensions D1 Playwright spec discipline / D2 Spec-claim integrity / D3 Mutation slice via existing `docs/testing/mutation-audits.md` / D4 Pest file relevance refresh). s094 + s241 named as precursors without retroactively retconning them. Drafted A005 prompts (Cycle 1 = D1 + D2 only; D3 and D4 queue for later cycles); `sessions/session-outlines.md` Active-tracks block gained the Test Audit row. **Phase 5 — main merge + handback.** Merged `origin/main` (s313 close + A004 prep + 314-317 stubs + s313 template + workflow consolidation `test:e2e:isolated` → `test:e2e`) into `session-A003/2`; one workflow conflict resolved in favour of main's consolidation. Handback strip restored `on: push: branches: ["**"]`, removed all DIAGNOSTIC blocks, restored Playwright job to `needs: slow`, collapsed Run-Playwright back to `npm run test:e2e -- --project=chromium`, deleted `scripts/ci/pw-digest.mjs` + the empty `scripts/ci/` directory. YAML re-validated (4 jobs, 13 Playwright steps); final diff vs main contains only the intended permanent deltas. **Tests:** Playwright post-deletion verification on `bc6f541` — 51 passed, 0 failed, 1 retry-recovered flaky (`inline-editing-foundation.spec.ts:79` cold-load, deferred to A005's D1 sweep), 8 skipped (684s). Fast Pest unchanged (no `tests/Feature/` or `tests/Unit/` touched; CI `Tests` aggregate is the authoritative full-suite signal). **Diff scope:** `.github/workflows/tests.yml` (clean after handback — zero net delta from main); 5 e2e spec files (deletions + URL/selector fix in site-import-export); `sessions/housekeeping-inbox.md` (6 new `[test-integrity]` entries); `sessions/session-outlines.md` (Test Audit track row); `sessions/tracks/test-audit.md` (new); `sessions/A005. base-prompt.md` + `sessions/A005. Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain).md` (new); session log + completed-sessions row + VERSION. No application code touched. **No hard-no file touched:** zero changes to `app/Widgets/*`, `resources/scss/*`, `app/Filament/Pages/*`, Fleet Manager surface, migrations, `app/Services/ImportExport/*`. **Carry-forward:** `inline-editing-foundation:79` + the broader page-builder cluster's cold-load flakes go to A005 D1; Filament header-action `requiresConfirmation()` modal investigation deferred (out of stabilization scope); local-first Playwright workflow codification deferred to A005 close. PR #17 opened from `session-A003/2` to `main`. v2.3.0, no schema, VERSION 0.903.02. |
| 313 | Code Shape & Fit (Apply) — Cycle 1 — Apply half of the Code Shape & Fit refactor track's first cycle. **Iteration /1 (byte-equivalent bundle):** W-R1-1 hoisted a single `TypographyResolver::resolve()` call above two sibling closures in `PageBuilder.php::getBootstrapData()`; W-R4-1 extracted a `sessionContext()` helper in `ImportSessionActions` consumed by `approve()` + `rollback()` in place of duplicated four-statement context blocks. Both moves byte-equivalent for produced output; existing Pest coverage carries. Fast Pest 2586 (16140 assertions, 113s). **Iteration /2 (W-R1/A pattern fix):** Lifted `app/Services/PageTreeBuilder.php` exposing `tree()` / `items()` / `requiredLibs()` as separately memoized computed methods. Rewired all seven `buildTree()` callers in `PageBuilderApiController` to instantiate the service and pull what they need — index/store/destroy/copy/reorder use all three methods, storeLayout/destroyLayout call only `items()` + `requiredLibs()` (legacy widgets key dropped from those layout endpoints). `formatLayout()` lifted to public static so single-layout responses share the in-tree formatter source. Controller `buildTree()` / `formatLayout()` / `formatWidgetWithPreview()` removed; unused imports dropped. Two intent-unrecoverable findings (dual `collectLibs` loops, `requiredForPage` re-derivation) preserved structurally — moved verbatim into `PageTreeBuilder::compute()`, no consolidation, carry to Cycle 2's documentation-gap walk. JSON response shapes byte-equivalent (`assertJsonStructure` assertions across `PageBuilderApiTest.php` pass). Fast Pest 2586 (16139 assertions, 116s). Playwright `tests/e2e/page-builder/`: 28 passed, 4 skipped. Controller diff -112 / +32 LOC; service +127 LOC new. **Walkthrough disposition of 312's outputs:** Flag W-R1/A → applied as pattern fix; Flag W-R2/A `ImportContactsPage` long file → promoted to Deliberately Kept register; Flag W-R4/A → instance fix only, description-generator pattern fix deferred; FieldMapper register entry → accuracy fix (file inventory corrected to `FieldMapper.php` unprefixed + five sibling files), Organizations omission filed in housekeeping inbox; `PageBuilderApiController` per-action endpoint shape → register entry added; both intent-unrecoverable findings → carry forward unmodified. **Track doc updates:** Cycle 1 retrospective landed (window 274 → 311, sessions list, quantitative outcomes table, five load-bearing decisions, blind spots, three process incidents, won't-fixes reaffirmed and promoted). Status snapshot updated: Cycle 1 complete, next trigger ≈ session 379. Standing-improvements section populated with two Cycle 1 → 2 carries (audit-agent citation-verbatim rule, sequence-after-cleanup-cycle preference). **Side-work landed in same session (3 separable streams):** (1) **Playwright isolation default flip** — `package.json` `test:e2e` and all variants now default to the isolated stack (`localhost:8090`, separate DB on port 5442); new `test:e2e:local` is the explicit escape hatch for runs against the dev DB; CI workflow + e2e README updated; addresses recurring "tests blew away my local data" issue without behavior change. (2) **Template hardening** — removed the "templates stable as of 276 don't need a re-read every session" carveout from `template-base-prompt.md`, added a new `── SESSION OPEN GATE ──` banner section mirroring the close gate's visual, updated the opening "Start now" line to point at the Open Gate. Addresses recurring drift where per-session base prompts overrode template sections without a checkpoint. (3) **Next-session prompts pre-drafted** — at user direction "no need to spread that work out," drafted base-prompt + session-prompt pairs for 314 (Housekeeping Batch 2 — Batch 2A, with explicit Batch 2B spillover provision), 315 (Record-Duplicate Affordance, E18 — events/pages/posts/products × list+record), 316 (Accessibility Pass — Aria + Usability + Lighthouse, E19), 317 (Media Duplicate + Unused Finder, E20), plus the cloud-session A004 (Cycle 1 Review). Three new release-plan entries (E18 / E19 / E20) promoted from housekeeping at slot 44-46 between E15 and E16/E17; five new housekeeping items added (Organizations importer helper, logo reset, backup download cleanup, toolbar `title` attrs, ellipsis menu port, columns alignment labels); three new post-Beta backlog items added at top of `session-outlines.md` (client-screenshot custom-widget reproduction, slug-change 301 + redirect tool, anchor nav widget). Memory writes: `feedback_session_end_steps` (after final iteration commit, stop for smoke-test handoff + next-session prompt draft before close-out artifact edits), and update to `feedback_pm_level_tone` calling out project-internal bookkeeping codes ("W-R2/A promotion", "FieldMapper carve-out") as impenetrable in user-facing prose. **Branching variance from CLAUDE.md.** Per explicit user direction at iteration /2 ("we are not iterating against a deploy server you can keep the changes on the same branch and proceed"), both iterations + the close commit landed on a single `session-313/1` branch rather than splitting into per-iteration branches. Iteration /1 was pushed mid-session before the user's directive landed (commit on origin/session-313/1); iteration /2 + close stayed local per the prompt's "do not push at close" rule. **Tests:** fast Pest 2586 baseline carried unchanged across both iterations; design Pest group at /1 close 235 passed; targeted slice (ImportSessionActions + PageBuilder filter) at /1 close 66 passed; Playwright page-builder at /2 close 28 passed / 4 skipped. No new tests this session — both inline findings byte-equivalent extractions; W-R1/A pattern fix byte-equivalent at JSON response surface, existing query-count regression guard covers the memoization invariant. **Branch + version:** `session-313/1`, three commits (iteration /1, iteration /2, close), iteration /1 pushed earlier in session, /2 + close stayed local. VERSION 0.902.02 (carried from 312 close — refactors don't bump VERSION). v2.3.0 contract, no schema, no migrations. **Carry-forward:** both intent-unrecoverable findings carry to next refactor cycle (≈ session 379); Flag W-R4/A pattern fix deferred to next cycle's pre-loaded findings; `OrganizationFieldMapper` filed in housekeeping inbox; citation-discipline rule + cleanup-cycle-sequence preference folded into the track doc's standing improvements; A004 cloud session queued out-of-band to review Cycle 1 against stated goals before Cycle 2 plans land. |
| A004 | Code Shape & Fit — Cycle 1 Review (agentic) — Planning-only cloud review of the Code Shape & Fit refactor track's first cycle (312 audit + 313 apply), evaluating it against stated track goals; write surface confined to the track's three planning docs, zero code/schema/test changes. **Phase 1 — ten-axis pre-discussion evaluation.** Verdicts: anti-performative discipline *partially worked* (four fabricated citations — three W-R1, one W-R4 — all caught at reviewer spot-check, not by agent self-discipline, so the structural guard ran at review time not inside the agent); citation-verbatim-with-line-numbers *right fix but symptom-level* (raises fabrication cost, doesn't address the finding-pressure that made agents reach for absent citations); five-workstream taxonomy *carrying weight with asymmetric yield* (W-R3/W-R5 zero-yield reads as discipline working in a mature codebase); intent-unrecoverable shape *worked with a near-miss* (indefinite-carry risk); six-iteration ceiling *inconclusive* (two iterations used, ceiling never bound); pattern-vs-instance default *held* (user opted into pattern-fix, default produced a question not friction); pulled-forward calibration *worked on a narrow margin* (1 of 2 inline findings touched importer code a later cleanup may move); no-squash cycle shape *held*; permanent artifacts *finding-template used in spirit, carve-out register load-bearing*; PM-tone re-coaching *general behavior, track-flavored*. **Phase 2 — discussion ran wider than the pre-loaded axes.** User switched the working branch from the cloud default to `session-A004/1` per project convention, then redirected first into cadence + coverage before returning to the axes. **Cadence:** keep ~50-session default; don't tighten (creates finding-pressure in a mature codebase); loosen only if a future cycle yields thinly AND the carve-out register stops accumulating. **Coverage gaps (all five accepted):** two became new workstreams, three sharpened existing ones; user required the data/operation-marshalling gap to carry a hard quantified threshold so an eager agent can't trigger it on instinct. **Three efficacy observations accepted** for adoption (subsystem-coverage rotation, active register-walk, intent-unrecoverable two-cycle lifecycle). Mid-session PM-tone re-coaching recurred (jargon-dense status sentence flagged as unreadable; unabbreviated project-manager-level delivery requested) — same pattern as `feedback_pm_level_tone`, fed Topic 10's edit. **Phase 3 — track-document edits.** `code-shape-and-fit.md`: added anti-performative guard #3 (two-part citation enforcement: prompt requires verbatim-with-line-numbers, reviewer spot-checks); removed the now-redundant citation-discipline standing-improvement entry (rule is permanent in guard #3); added the "empty workstream table is a valid result" note (incl. directness/seam low-yield expectation, Topics 2+3); added the "bookkeeping codes stay out of user-facing prose" note (Topic 10); added "one-shot vs iterative pattern fix" to class-findings (Topic 6); tightened "sequence-after-cleanup-cycle preference" into a concrete auditable test (count apply-backlog findings excluding intent-unrecoverable, define stale-touched, 30% threshold, log the count — Topic 7); added the "intent-unrecoverable lifecycle" rule (survives two consecutive documentation-gap walks without recoverable intent → closes as "load-bearing by inability to prove otherwise", Axis 4/Obs 3); added the "subsystem coverage" rotation rule (audit logs walked-vs-skipped, next cycle walks longest-unwalked first, Obs 1); added active register-walk to the Retirement subsection (Obs 2); **added two new workstreams — W-R6 Temporal shape and ordering invariants (with the W-R5-style fifth breakage-analysis column) and W-R7 Naming-intent fidelity (four-column)** (gaps 2+1); sharpened W-R1 with a callee-side cardinality pattern (gap 3); sharpened W-R4 with failure-mode-disagreement + the quantified data/operation-marshalling pattern (gaps 4+5); reconciled all "five workstreams" → "seven" / W-R5-only → W-R5+W-R6 / "W-R1 through W-R5" → "W-R1 through W-R7" references; **split the Cycle 1 retrospective out** to a new companion doc (intro now lists two things, end-of-doc pointer directs the reader there and notes the placement is deliberate — read before planning a cycle, not frontloaded). New `sessions/tracks/code-shape-and-fit-retrospectives.md` carries the full Cycle 1 retrospective verbatim. `code-shape-and-fit-finding-template.md`: row-format note updated (four-column for W-R1–W-R4 + W-R7, fifth for W-R5 + W-R6; deviation vocabulary +ordering/naming); added W-R6 + W-R7 worked-example rows; generalized the candidate sub-section to W-R5/W-R6 with a "an unsupported reorder is the most dangerous thing the track can ship" note; updated intent-unrecoverable references to the seven-workstream set. **All doc additions written as forward instruction + reason, no references to specific past sessions/events, per user direction.** Deferred: six-iteration ceiling re-evaluation (untested), carve-out register retirement loop (unexercised). Open gate cleared; ten review topics all resolved. No code findings routed to the housekeeping inbox or release plan. Next cloud-session prompt already written by the user. v2.3.0, no schema, no tests run (cloud env can't run Pest; planning-only). VERSION 0.904.01 (agentic-session band). PR opened from `session-A004/1`. Agent self-close — user merges manually after review. |
| 314 | Housekeeping Batch 2 — Batch 2A — First of the two batches the E12 plan entry spans (split per Rule 11). Proposed a six-item inbox subset; **shipped three, dropped three** as no-longer-valid. **Iteration /1 (Hero widget):** `button_alignment` select (default `auto` = derive from text position) + `text_max_width` select (default `42rem`, Narrow/Medium/Wide/Full) bound via a new `--hero-text-max-width` CSS var on the wrapper consumed by `.hero-inner`; schema-keys lock test updated, +4 tests. **Iteration /2 (Logo widget):** `public/images/default-logo.svg` placeholder rendered when neither logo image nor wordmark text is set (the fresh-install posture the SystemPageSeeder leaves on `_header`); +2 tests. Companion "default href = home" item caught as drift — already shipped (`link_url` default `/`). **Iteration /3 (Logo widget):** wordmark responds to a full-bleed hero's `appearance_config.text.color` — plumbed through PageController/PostController as `__navOverlayTextColor`, emitted as `--overlay-text-color` on `.site-nav-wrapper--overlay`, consumed by the logo wordmark in the overlay scope (mirrors the existing `nav_link_color` reach-over); placeholder SVG + raster logos untouched; +2 integration tests. **Iteration /4 (process tooling, added mid-session at user direction; retcon-noted in log):** `npm run logbug -- "…"` (`scripts/logbug.mjs`) appends a VERSION+date-stamped line to a new `sessions/housekeeping-incoming.md` buffer (pure node, fires mid-loop, never touches the canonical inbox mid-session); close gate (`template-base-prompt.md`) now digests the buffer — Phase 1 surfaces a validity-checked review before close, Phase 2 folds survivors into the inbox and clears it; `housekeeping-inbox.md` documents the flow and adds the **verify-the-premise-at-walk** staleness guard. Buffer digest exercised at close (user-planted test entry caught + removed). **Dropped (no code):** paragraph/list-item padding default (typography system already owns paragraph rhythm via margin-bottom — adding padding would double it); button-style refresh (as far as it goes pre-designer; belongs to the Design System Editor track); editor text-colour legibility (obsoleted by the inline editor giving operators direct colour control). The three dead items were the forcing function for the capture-flow + staleness-guard work. **Planning:** E12 marked split (Batch 2A = 314 ✅, Batch 2B = 315); the pre-drafted downstream feature sessions bumped one slot — record-duplicate 315→316, accessibility 316→317, media finder 317→318 (file renames + internal session-number references + release-plan execution-order note; positions unchanged — they are execution-order, not session numbers). 315 Batch 2B base-prompt + session-prompt drafted. **Process feedback captured to memory:** sharpened the "no bare outline/version codes in user-facing prose" rule (repeat correction, high friction); new "ask the short question — don't pad a decision with every alternative out of caution" rule. **Tests:** fast Pest 2594 / 0 (313 baseline 2586 + 8 from the three shipped items); public bundle rebuilt for the SCSS changes. **Branch + version:** `session-314/1`, four commits + close, not pushed. VERSION 0.903.02 → 0.314.01. v2.3.0 contract, no schema, no migrations. **Carry-forward:** inbox remainder (logo→rich-text, column-layout background-width parity, TTL sweeper + downloaded-backup pair, Organizations importer helper, logo-remove affordance, columns-alignment labels, link-colour consolidation, test-integrity cluster) → Batch 2B (315); capture buffer is a tracked file, Drive-integration read at close deferred. |
| A005 | Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain) (agentic) — First cycle of the newly-lifted Test Audit track (`sessions/tracks/test-audit.md`), picking dimensions **D1 (Playwright spec discipline)** and **D2 (spec-claim integrity drain)**; D3 (mutation slice) + D4 (Pest file relevance) queued for later cycles. Test-code + planning-doc changes only, **zero application code**; no carve-out (both dimensions fit one session). **D1 — Playwright suite 28 → 16 specs.** Deleted 10 redundant importer specs whose data assertions duplicate the service-layer Pest suite (`ImportEventsTest` / `ImportFinancialTest` / `ImportOrganizationsTest` / `NotesImporterTest` / `ImportSession194Test` / `ImportSessionActionsTest`) + the FixtureRunner (`tests/Feature/Generated/ImportFixtureRunnerTest.php`, 7 importers × 4 shapes); the browser-only wizard contract (FilePond, Choices.js search, multi-step nav, progress polling, downloads, approve modal) is preserved by the kept contacts/donations/site-import specs. Also deleted the parked `inline-formatting-toolbar` (`describe.skip` since s306) and the CI-unstable `ticket-tier-picker` (per-test fresh published event hits an un-warmed public page depending on the unbuilt `event_registration` widget bundle — deleted-not-requarantined per A003 policy; spinner *e2e* coverage tradeoff noted to forward queue). Each deletion landed a `[test-integrity]` housekeeping line naming its absorbing Pest coverage. Page-builder cluster (`inline-editing-foundation`, `inline-editing-phase2`, `layout-inspector`) kept as browser-required; `inline-editing-foundation:79` cold-load flake stays on passive watch. Shipped `docs/testing/playwright-discipline.md` (durable workflow doc mirroring `mutation-audits.md`: browser-only criterion, Keep/Redundant/Mixed decision rule, local-first authoring discipline, A003 anti-pattern catalog, FixtureRunner-is-the-safety-net rule); updated `tests/e2e/README.md`. **D2 — drained the s296 `[test-integrity]` backlog.** Deleted 2 vacuous Pest tests: `DonationCheckoutTest` factory-echo ("creates a pending donation record on checkout initiation" — posted then discarded the response, asserted a factory's own attributes) and `ProductCarouselTest` magic-count ("seeder total widget count includes product_carousel" — `toHaveCount(39)` change-detector). Renamed 2 `QuickBooksSyncTest` sync-job tests to match their mocked-client bodies (createSalesReceipt / createRefundReceipt job-routing + id-persistence); `QuickBooksCustomerMatchingTest` needed no change (s296 lumping was imprecise). Rewrote 2 `TaxReceiptTest` tests to route through the real `DonorsPage::buildBreakdown()` (reflection idiom) instead of inline `Donation` query copies. Renamed 2 e2e titles that overclaimed (`layout-inspector` "persists…" → "writes…to the layout appearance config"; `dashboard-settings` "loads, adds a widget…" → "loads the dashboard arrangement view…no preset/spacing tabs"). **Forward-queue Pest gaps recorded** (not silently dropped): donation checkout-initiation controller path (create-pending-then-delete-on-Stripe-failure, mock the boundary); invoice first-row-wins parent-conflict resolution. **Deferred:** local-first Playwright discipline codified in the workflow doc, CLAUDE.md/base-prompt promotion deferred to next cycle. **Verification:** CI `Tests` aggregate green on the merged commit `4e2d575` (Fast ✅ / Slow ✅ / Playwright e2e ✅ / Tests ✅) — cloud env has no Docker, CI is the authoritative signal. **Branch + version:** work iteration on `claude/base-prompt-review-CxSV5` (merged via PR #20); close docs on `session-A005/2` (second PR). VERSION 0.314.01 → 0.905.02 (agentic-session band). v2.3.0 contract, no schema, no migrations. Agent self-close — user merges manually after review. |
| 315 | Housekeeping Batch 2 — Batch 2B — Second and final batch of the E12 plan entry (closes E12). Walked the post-314 inbox, premise-checked each candidate, shipped five and adjusted three at close. **Iteration /1 (Organizations importer):** extracted `ImportOrganizationsPage`'s inline `guessDestination` match into a dedicated `OrganizationFieldMapper` modeled on the five sibling mappers, accepting the `$preset` the wizard already passed (the inline version silently dropped it, leaving Organizations preset-blind); added industry/ein aliases + standard spacing/underscore variants; generic preset preserves every prior mapping; +11 tests. **Iteration /2 (Export-artifact TTL sweeper):** new `exports:clean` command (default 48h, also reaps empty token dirs) scheduled daily at 02:00 in `bootstrap/app.php` alongside `backup:clean`/`media-library:clean`; the paired backup-download-cleanup item resolved by verification (Fleet `blob()` streams off the already-reaped backup disk — no separate artifact; Fleet controller untouched, non-boundary); +4 tests. **Iteration /3 (Toolbar accessibility):** shared `applyToolbarTitles` helper labels both Quill toolbars (admin editor + page-builder inspector) with `title`+`aria-label`; the inline-format toolbar (already aria-labelled) mirrors its labels into `title` on mount for hover tooltips; JS/template-only, `npm run build` clean. **Iteration /4 (Ellipsis menu port):** extracted the page-details secondary-actions `ActionGroup` (export variants, save-as-template, snippets, delete) into a `HasPageSecondaryActions` trait used by both `EditPageDetails` and the page-builder `EditPage`, so operators reach those actions without bouncing to detail; page-scoped (Post resource untouched); +2 tests. **Iteration /5 (Column-layout parity):** flipped `AppearanceStyleComposer::resolveFullWidthForLayout` fallback to `true` (parity with widget defaults; covers legacy layouts) and wrote concrete `background_full_width=true`/`content_full_width=false` at layout creation in both the page-builder and record-detail-view API controllers per the concrete-values rule; +3 tests. **Dropped/deferred (no code):** logo-remove affordance dropped as premise-stale (the referenced settings logo upload was removed in a prior CMS-settings refactor — only the admin-panel logo remains, a different control); logo-widget rich-text/appearance examined but deferred with a design-call note (Quill toolbars expose no colour/size; a colour control collides with s314 hero-inheritance). **Surfaced:** Content Export — Unified Options Modal promoted to a post-Beta stub in `session-outlines.md` (collapse the four export menu rows into one action + a two-toggle modal across content types). **Process note:** the five iterations were committed onto local `main` by mistake (session started checked-out on main, never branched); caught at close — recovered by branching `session-315/1` at the tip and resetting local `main` to `origin/main`, no commits pushed. **Merge:** `origin/main` (A005 Test Audit Cycle 1 — `[test-integrity]` drain + redundant importer e2e deletions) merged into the close branch, conflict-free. **Tests:** fast Pest 2613/0 on the branch before merge (+19 from the five items over the 2594 314 baseline); re-confirm post-merge. **Branch + version:** `session-315/1`, five item commits + merge + close, not pushed. VERSION 0.314.01 → 0.315.01. v2.3.0 contract, no schema, no migrations. **Carry-forward:** inbox remainder (logo-widget rich-text [deferred w/ design note], columns-alignment labels, link-colour consolidation, widget hardcoded values, per-widget colour-picker defaults, contact-hero readability annotation, post-milestone RDG Organizations, the A005 + folded list-rendering `[test-integrity]` items). |
| 316 | Record-Duplicate Affordance — Events / Pages / Posts / Products (E18). Single-record **Duplicate** at two surfaces each (list-row + record-page header) across all four resources — eight affordances. A `duplicate()` method per model holds the semantics; Filament actions are thin wrappers (confirm → `$record->duplicate()` → notify → redirect into the editor). Every copy lands **draft**, "Copy of …", slug `-copy` (uniqueness-incremented), authored by the current user. **Pages** (pattern source): clones widgets/layouts via `copyOwnedStack`, re-points tags, copies own media; record action added to the existing `HasPageSecondaryActions` ellipsis. **Posts**: reuse `Page::duplicate()` (type stays `post`). **Products**: clones price tiers (Stripe `price_id` dropped so the observer re-mints), copies image, un-archives. **Events**: clones ticket tiers + tags + media, **carries `starts_at`/`ends_at` forward** (recurring-event use case, per user sign-off), drops `landing_page_id`/registrations/`import_session_id`. **Two `copyOwnedStack` bugs found via user testing + fixed** (also repaired the existing Save-as-Template / content-template flows): `copyMediaTo` only read Spatie's `default` collection so all widget background/config media was dropped → iterate `$source->media`; the layout-clone branch omitted `appearance_config` so column/section backgrounds (color/gradient) were dropped → carry it. Verified on the isolated e2e stack (duplicated Hero page renders pixel-identical; buttons were never lost — they're `config` jsonb, the symptom was the missing background collapsing the Hero). **Builder-UX pass (user-requested at close):** replaced the floating "Save as Template" footer button in the page-builder Vue app with a primary **"Save changes"** button bottom-right on both page+post edit screens (cosmetic "Saved" toast via a Vue→Livewire bridge `PageBuilder::saveChanges`; builder still auto-saves — affordance + hook seated for a future explicit-save model; old placement caused accidental template-saves); Save-as-Template now lives only in the header ellipsis; new `HasPostSecondaryActions` trait applied to `EditPost`+`EditPostDetails` gives posts the ellipsis they lacked, matching pages item-for-item. **Tests:** fast Pest 2626/0 (+12 over the 315 baseline 2614 effective on-branch). **Branch + version:** `session-316/1`, seven commits + close, not pushed. v2.3.0 contract, no schema, no migrations; `npm run build` required (Vue change). **Carry-forward:** capture buffer folded (button-row widget, remove Laravel Debugbar) + a new finding (unquoted `url()` breaks `background-image` for filenames with parens/spaces, public-site-wide, pre-existing). |
| 317 | Accessibility Pass — Aria + Usability + Lighthouse (E19). Audit-then-remediate: three sweeps (Aria / usability / Lighthouse) surfaced inclusively, agreed subset applied across four iteration commits. Non-boundary, v2.3.0, no schema. **Two folded inbox items verified already-resolved at s315** (inline-toolbar `title`/`aria-label`; page-builder ellipsis-menu parity — header actions auto-render via `<x-filament-panels::page>`); one agent "blocking" finding (ellipsis missing on builder edit view) was a false positive disproved by reading `EditPage::getHeaderActions()`. **/1 Public render:** skip-to-content link + `id="main"` (focus-revealed `.skip-link` in `_base.scss`); logo link always named (img `alt` → app-name fallback, decorative `""` when wordmark shown); ProductCarousel arrows + MapEmbed/VideoEmbed iframe titles + SocialSharing icon `aria-label`; MapEmbed activate-overlay → keyboard-operable `role="button"`. **/2 Page-builder Vue:** inspector leaf controls (text/url/textarea/number/select) `aria-label` from field label (sibling `<label>` has no `for`); InspectorTabs `role=tablist/tab` + `aria-selected`; icon-only buttons `aria-label` (InspectorHeader, ButtonList/Repeater move-remove, ColorPicker compact trigger, LayoutRegion selector, viewport presets + `aria-pressed`); redundant hover layout-handle `aria-hidden` (action reachable via selector button). **/3 Admin + portal:** DesignSystem tabs `role=tablist/tab`+`aria-selected`; per-field `@error` spans (PortalSignup / reset-password / EventRegistration guest / invitation-set-password) get `id`+conditional `aria-describedby`/`aria-invalid`; PortalChangePassword + invitation confirm wire to JS match-error; **incidental bug** — duplicate `class` attr on PortalChangePassword match-error div fixed. PortalLogin/ForgotPassword/ContactEdit already adequate, untouched. **/4 Tooling:** `@axe-core/playwright` + standing `tests/e2e/accessibility.spec.ts` (home + blog index, WCAG 2 A/AA, color-contrast disabled, admin shell not scanned) — both green on e2e stack. **Lighthouse deltas** (accessibility, first baseline): home 91→96, about 90→100, admin login 88→88 (remaining = stock-Filament + color-contrast, won't-fix). **Carved out:** B5/B6 page-builder focus management (modal focus-trap + inspector focus-move-on-open) → `session-outlines.md` stub. **Deferred to inbox:** A8 nav landmark, D2 page/template terminology, D3 duplicate-action consistency, D4 hover-only disabled-checkbox, `.sr-only` undefined in public SCSS. **Won't-fix (cited):** A7 EventDescription `<h1>` (owner-deferred, landing-page revamp), D1 columns alignment labels (owner-planned preset shape (b)), B7 drag handle, C4 stock Filament. Source-map Lighthouse ding discussed + declined (no end-user benefit, exposes source; revisit with error monitoring). Surfaced: e2e stack boot blocked locally on stale `Barryvdh\Debugbar` provider (in `require` not `require-dev`, absent from `--no-dev` image) — manifest cleared locally, CI unaffected; overlaps existing inbox item. **Tests:** fast Pest 2626/0 (unchanged baseline — no regressions); axe spec 2/0. **Branch + version:** `session-317/1`, four commits + close, not pushed. VERSION 0.316.01 → 0.317.01. `npm run build` required. |
| 318 | Media Duplicate + Unused Finder (E20). Operator-facing **Media Finder** page (Tools group, `manage_cms_settings`) with three read-only on-demand scans + confirmed per-row delete. **Mid-session prompt correction:** the original "media-id columns in JSON" framing was wrong — media here is owned via **Spatie collections** (`model_type`/`model_id`/`collection_name`), not id columns; prompt rewritten in place before coding. **`MediaReferenceInventory`** encodes the real model: rule A = live collection ownership per owner type (fixed sets for Page/Event/Product/EmailTemplate/WidgetType; PageWidget dynamic = `appearance_background_image` + `config_{key}` for current image/video schema fields; CollectionItem/SampleImage treated all-live, conservative); rule B = embedded `/storage/{id}/` URLs in rich-text (`page_widgets.config`, `collection_items.data`, `events.description`/`meeting_details`, `email_templates.body`), extracted from the canonical path token (superset of img-only — catches href/srcset/conversions, keeps `inline-images` alive). **`MediaFinderService`** runs: **duplicate** (SHA-256 content hash, then filename+size for un-clustered rows — fixed a bug where the name+size pass excluded all hashed rows, not just clustered ones), **unused** (non-live media tagged dead-collection vs orphan-owner; orphan-owner only *reported*, deletion left to nightly `media-library:clean`), **missing-file/404** (file gone from disk, tagged broken-referenced vs dead-record — **user-requested addition**). **Merge deliberately skipped** v1: each duplicate is owned by a distinct owner+collection, so "merge into canonical" has no safe meaning under Spatie single-file collections; only embedded-URL dupes are mergeable and seed data has none — surfaced + agreed. **Sample run:** 178 media → 8 unused (all orphan-owner), 18 identical-content clusters, 0 missing, 0 embedded refs. **Tests:** fast Pest 2642/0 (+16). Non-boundary, v2.3.0, no schema, no `npm run build`. Runbook `docs/runbooks/media-finder.md` + help stub. **Branch + version:** `session-318/1`, not pushed. VERSION 0.317.01 → 0.318.01. **Follow-on (user-directed):** 319 = upload-time dedup prevention (content_hash + warn/use-existing/replace-existing across all upload surfaces, most-referenced-first disambiguation on multi-match); 320 = content-addressed storage + refcounted delete (disk reclamation, folds in the UUID-path stub). Both 319 prompts drafted at close. |
| 320 | Content-Addressed Storage + Refcounted Delete. The *reclamation* leg of the media trilogy (318 → 319 → **320**); trilogy complete. **Open-gate decisions:** URLs follow the content-addressed path (`/storage/cas/{shard}/{hash}/{file_name}`) with embedded `/storage/{id}/` URLs rewritten in the relocation — *chosen over the prompt's id-stable lean* because media serves statically via the `public/storage` symlink (URL **is** the path, so a decoupled id-stable URL would 404 without a PHP route or non-portable per-id symlinks); refcount **computed** over `content_hash` (**no schema column**); conversions dedupe as a consequence of whole-directory sharing; relocation is a one-shot idempotent command run inline by the migration (irreversible `down()`). **Phase 1:** `ContentAddressedPathGenerator` (base dir = `cas/{hash[0:2]}/{hash}/`, legacy-id fallback before the hash is known) + `MediaRelocator` (move id→cas, delete-on-collision dedup); the `MediaHasBeenAddedEvent` listener now hashes **then** relocates (event fires after the original copy, before conversions, so conversions follow to the shared dir). Consequence: `Media::copy()` — the 319 "use-existing" reuse path — became **real byte-sharing with no endpoint change**. **Phase 2:** `media:relocate-cas` (chunked; moved/deduped/noop counts; rewrites embedded `/storage/{id}/` URLs across the five rich-text surfaces to the CAS form) invoked by migration `2026_05_22_130000`; `MediaReferenceInventory` Rule B resolves identity from `/storage/cas/{shard}/{hash}/` (hash→rows) plus the legacy numeric form. **Phase 3:** `ContentAddressedFileRemover` removes files only when no other row shares the `content_hash` (computed refcount, runs in `MediaObserver::deleted()`), so the 318 Finder's per-row delete is refcount-safe. **CAS↔portability fix:** s303 bundle hardcoded `{id}/{file_name}` as byte path + id source — repaired (`ContentExporter` records real `getPathRelativeToRoot()` + `content_hash`; `collectReferencedMedia` reads an explicit `id`; importer seeds `content_hash` + writes bytes to the descriptor path); 7 pre-existing tests updated. **Dev relocation:** 251 MB/1310 files → 179 MB/936 files across 45 hash dirs (~72 MB / ~29% reclaimed, 46 moved + 19 deduped), all 65 referenced originals verified present (0 broken), one image confirmed 200 via nginx. **Known v1 limit:** same-content/different-filename pairs share the hash dir but keep separate named originals (one cluster). Fast Pest **2667/0** (+12). Non-boundary, v2.3.0, **no schema**, no `npm run build`. **Branch + version:** `session-320/1`, not pushed. VERSION 0.319.01 → 0.320.01. **Next:** 321 = Demo Node (lockdown verify + baseline reset + local dress-rehearsal); CRM 321 prompts drafted + FM-side handoff note dropped into the FM repo at close. |
| 319 | Media Dedup — Upload-Time Prevention. The *prevention* leg of the media trilogy (318 visibility → **319** → 320 reclamation). **Phase 1:** added **`media.content_hash`** (SHA-256 hex, nullable, indexed), populated on add via a single **`MediaHasBeenAddedEvent`** listener (`MediaContentHasher`) that fires for every `addMedia*` path (uploads, `Media::copy()`, importer seeding), backfilled by **`media:backfill-hashes`** (chunked, idempotent, called from the migration); 318's duplicate scan now reads the column → instant. **Phase 2:** **`MediaDedupService::findMatches`** (surface-agnostic) — content-hash matches collapse to one candidate + `duplicate_count`, filename matches surface the iteration case (skipping randomised `->hashName()` basenames), sorted referenced-first via `MediaReferenceInventory`. **`media-dedup-check`** endpoint registered for both front-ends; **`useExistingImage`/`useExistingAppearanceImage`** on `PageBuilderApiController` do **copy-first-then-evict** (caught a clear-before-read bug in test). **Front end:** dedup gate in `useUploadActions` (client hashes via Web Crypto, degrades gracefully) → shared store `dedupPrompt` → **`DedupPromptModal.vue`** (Use this / Upload as new / Cancel); inline rich-text (`quill-editor.js`) offers reuse via `confirm()` and inserts the existing `/storage/{id}/` URL with **no new upload** (the one zero-copy surface). **Honest scope:** reuse still copies bytes under the current path generator — behavioural win, not disk reclamation (that's 320). **Surfaces:** page-builder image + appearance + inline rich-text; **Filament `SpatieMediaLibraryFileUpload` fields deferred to a post-Beta stub** (no idiomatic form-save hook; backend already surface-agnostic). **Drift:** prompt's `useLibraryLoader.ts` is actually a widget-lib loader — real path is `useUploadActions.ts`. **Dev-DB cleanup for UAT:** traced bloat to the Random Data Generator copying pool images per fake record; after the scrub wipe, swept 63 owner-orphaned rows → 65 referenced rows / 45 distinct files. Fast Pest **2655/0** (+13). Non-boundary, v2.3.0. Schema: +`content_hash`. `npm run build` done. **Branch + version:** `session-319/1`, not pushed. VERSION 0.318.01 → 0.319.01. **Next:** 320 = content-addressed storage + refcounted delete (disk reclamation; resolves the UUID-path stub). Both 320 prompts drafted at close. |
| 322 | Header / Footer Defaults Overhaul (E16). **Header:** the prompt's "full-width header" premise was stale — rendered seeded + live chrome, both already wrap content in `.site-container` (the 284 `public.blade.php` chrome-wrapper fix); owner confirmed already-addressed → no change. **Nav widget Columns preset:** new **Layout** field (`orientation`: horizontal default / columns) on `NavDefinition`; `template.blade.php` gained a dedicated **no-JS, no-dropdown** columns render path — each top-level item is a heading column with its children listed beneath as visible links ("kitchen-sink" footer); `.widget-nav--columns` flex-row CSS (wraps on narrow widths, follows `--nav-justify`); `is-active` guarded against `#` placeholders. Replaced an earlier intra-session `stacked` take after the owner clarified the target (Stripe-style heading columns, all links visible) with a reference image. **Seeded default footer:** `SystemPageSeeder::seedFooterWidgets()` now ships a centered columns Nav (seeded `footer` menu of headed groups — Organization/Activities/Legal) above the copyright; copyright centered with 25px top / 150px bottom padding via partial `appearance_config`. Fresh `migrate:fresh --seed` → three-column footer; existing installs untouched (early-return guard). **Drift:** seeder comment cites a squashed `rebuild_chrome_pages_with_logo_and_nav` migration (the seeder methods are the real source); Nav had `alignment` not `layout`; old seeded footer was copyright-only. **Planning:** logged "Nav widget variants" (dropdown behaviors + separate mobile/desktop presets) to post-Beta backlog; **E15 (Table widget) deferred post-milestone** (no Beta-1 table blocker); **E17 retitled/refined to the border-tool design** worked out at close → E17 is now the last pre-Public-Website-Complete item; session-323 prompts drafted. **Tests:** fast Pest 2680/0 (+3). Non-boundary, v2.3.0, no schema. `build:public` done. **Branch + version:** `session-322/1`, not pushed. VERSION 0.321.01 → 0.322.01. `TmpFooterTestSeeder` (lorem test menu wiring) removed at close, not committed. **Next:** E17 border tool (session 323, drafted) — last item before the milestone divider; A006 demo-site build still async in the cloud *(A006 later retired at 323 close, replaced by session 324 — interactive Demo Site Plan)*. |
| 321 | Demo Node — Lockdown Verify + Baseline Reset + Local Dress-Rehearsal. CRM-side half of the demo work (FM-side — node identity, version pin, egress firewall — handed off separately via the FM-repo note). **Phase 1 — standing lockdown guard** (`DemoRoleLockdownTest`): a `demo`-role user is bounced at every Filament `canAccess()` wall (settings mail/finance/routing/CMS — where the Stripe/QuickBooks/Mailchimp/Resend keys live — user+role mgmt, import tools, site export + Media Finder) and the scrub-data wipe routes 403; guards `canAccess()` not just the permission vocabulary, so a permission swap can't silently widen the public demo; also asserts the intended granted width. **Phase 2 — width tuned** (`PermissionSeeder`): events + donations widened view-only → full CRUD (prospect can drive those flows; abuse class capped by the FM egress firewall + daily reset); transactions/funds/campaigns stay view-only; binding deny-list intact. **Phase 3 — `demo:reset`** (`DemoResetCommand` + `DemoBaselineSeeder`): idempotent known-good baseline (full `migrate:fresh --seed` + curated `RandomDataGenerator` set ≈40 contacts/6 events/25 regs/30 donations/15 memberships/5 posts/6 products), `--soft` re-seeds CRM only, **refuses unless `isDemoMode()`** so a stray cron can't wipe prod; baseline runs under a resolved-or-minted super_admin actor (events/pages NOT NULL author_id + generator super_admin gate). **Phase 4 — local dress-rehearsal:** isolated e2e stack flipped to `APP_ENV=demo` via a transient uncommitted override (dev DB untouched) — `demo:reset` rebuild+seed clean, baseline counts exact (+37 generated txns), `/demo/enter`→302→`/admin`, mail→log, live authenticated lockdown (users/roles 403, contacts/events 200); torn down after. No demo-mode Playwright spec (wall is permission-gated → the Pest guard is the faithful check). **Bonus:** recorded the definitive node-wipe procedure in `docs/app-reference.md`. **⚠ Deploy blocker flagged:** `demo:reset`'s baseline uses model factories → `fakerphp/faker` (in `require-dev`); the prod `--no-dev` image lacks it, so the real demo node needs a dev-deps image or faker→`require` before the reset's baseline step works (the `migrate:fresh --seed` half is fine). **Tests:** fast Pest 2677/0 (+10 — 7 lockdown + 3 reset). Non-boundary, v2.3.0, no schema. **Branch + version:** `session-321/1`, not pushed. VERSION 0.320.01 → 0.321.01. **Next:** A006 (Demo Site Page Design — cloud, drafted at close) designs the demo public site; 322 (E16 header/footer defaults overhaul — drafted at close) runs locally meanwhile *(A006 later retired at 323 close, replaced by session 324 — interactive Demo Site Plan)*. |
| 323 | Border Tool — Universal Widget Borders (E17). **`── PUBLIC WEBSITE COMPLETE ──` milestone REACHED at this close** (E16 at 322 + E17 at 323). **Phase 1 — `BorderInput` primitive** (`resources/js/page-builder-vue/components/primitives/BorderInput.vue`): SVG 4-edge toggle box + compact `ColorPicker` swatch handle + width input + radius input, in one labeled row mirroring `NinePointAlignment`/`SpacingInput`; `vector-effect: non-scaling-stroke` so off/on stroke widths render at exact px; hit lines separate from visible lines with `stroke-width: 14` + butt linecap so neighboring edges don't fight over the corner. **Phase 2 — inspector mount + concrete defaults:** mounted in `SectionLayoutPanel.vue` below padding/margin; emits the full border object atomically at `appearance_config.layout.border`. The concrete block (all sides false, width 0, color `#000000`, radius 0) added to `WidgetDefinition::defaultAppearanceConfig()` and to the three full-override widgets (TextBlock, PricingChart, Carousel) so the concrete-values invariant holds. **Phase 3 — render-surface parity via one shared emitter:** new public-static `AppearanceStyleComposer::composeBorderProps()` consumed by both `AppearanceStyleComposer::compose()` (public site + builder preview, which both delegate to `compose()`) and `ChromeRenderer::buildInlineStyle()` (system / chrome pages) — one source of truth; emits `border-{side}:Wpx solid #color` per enabled side, `border-radius` independently when radius > 0, and `box-sizing:border-box` whenever any side is on (load-bearing — the public SCSS has no global border-box reset). **Phase 4 — tests:** 8 composer cases + 1 chrome parity + 3 Text-widget render (standalone, in-layout, default-off) + 2 concrete-default invariants. **Phase 5 — standing Playwright spec** `tests/e2e/text-widget-border.spec.ts` asserts computed `border-*` + `box-sizing` on a bordered Text widget on the seeded home page. **Phase 6 — visual / UX polish iterated with the owner:** color picker moved out of the row to a panel-only below-row pattern; 2rem swatch + 2rem inputs for clean column heights; **labeled "Sides" column** for the SVG box (the material discoverability fix — without it the toggle read as decoration and directly caused a "width isn't being assigned" report since no side was ever turned on); `#ccc`→`#000` off/on contrast at 2px/3px stroke; widened hit zones (9px rendered); **stillness pattern** in both `BorderInput` and `BackgroundPanel` — on open `scrollIntoView({block:'nearest'})`, on close keep an invisible spacer at the measured popover height so subsequent open/close cycles are dead still (`BorderInput` uses max-ever measured; `BackgroundPanel` uses last-opened-panel because color/gradient/image have different intrinsic heights). **Drift:** prompt said the control mounts "alongside background" — `SectionLayoutPanel` carries padding/margin only, background is its own panel (`BackgroundPanel.vue`); the mount is still the right spot. No global `box-sizing: border-box` reset on public SCSS — emitter inlines it whenever a side is on. **Planning:** A006 (cloud Demo Site Page Design) **retired** — its premise (cloud-autonomous fictional-nonprofit 5–7 page marketing site) was wrong for the actual demo work; both A006 files deleted at owner direction; all forward-looking references rerouted to session 324. **Session 324 (Demo Site Plan)** drafted as an interactive multi-session scoping conversation — picks: driver.js for the tour, vue-cal for the new calendar widget, `sessions/demo website/scrap/` for reference images, per-page-permission (not exception) for the locked-landing mechanism; both demo-website specs live at `sessions/demo website/{events-index-spec,event-lp-spec}.md` as negotiable inputs. **Tests:** fast Pest 2694/0 (+14). Non-boundary, v2.3.0, no schema. `npm run build` done; no public widget bundle changes. **Branch + version:** `session-323/1`, not pushed. VERSION 0.322.01 → 0.323.01. **Next:** session 324 (Demo Site Plan) drafted at close — interactive planning conversation; A006 deleted from the lineup. |
| 324 | Demo Site Plan — multi-session scoping conversation (planning-only, no application code). First post-milestone slot, lifted ahead of A2 (blocked on FM 012). Walked both demo-website specs with the owner and produced a **v1 cut**: events index keeps day-grouped list / Event-Type+Date-Range filter row / featured hero / server-rendered mini-calendar / status badges / schema, with the art-fallback simplified to event→type-default→**site default**→solid color (the **generated text-tile renderer cut** — it defends the no-art real-org case the demo never hits, since we own the seed art); event LP keeps header / registration card (only the variants the existing widget already does) / About / Share / Related / `Event` schema, with Practical-info folded into About. **Deferred LP sections routed to existing roadmap owners** (donations→C4/C3b, member pricing→C6, volunteer→Volunteer Management, hosts→Orgs/B1b, sponsors→B1a field, rich registration→C5) — recorded as pointers, not new demo promises. **Three architecture decisions settled:** (1) per-page permission = `restricted_roles` jsonb deny-list on `pages` (default `[]`), enforced in `PagePolicy::update()` **and** the page-builder API route, super_admin bypass via existing `Gate::before`, re-applied by demo:reset — chosen over a `page_permissions` join table and over an `is_locked_from_demo` bool; (2) calendar = server-rendered Carbon month grid + vanilla JS (no Vue/vue-cal on the public side — **vue-cal retired from the plan**, since the public side has zero Vue and the old EventCalendar already used a vanilla lib); (3) driver.js = reserve a `data-tour` anchor convention now, build sessions add anchors, the tour itself is downstream (noted why this doesn't contradict s249's admin-DOM ruling). **Six-session build plan (A–F):** A calendar swap, B listing upgrade, C event LP, D per-page-permission + locked landing, E demo-node assembly (header/footer duplication via versioned export + curated-page demo:reset seeding + **Faker/`--no-dev` fix carried from 321**), F portal demo (back-end-only v1). **Downstream follow-ons:** driver.js tour build + real seeded portal login (verified seeded user + demo-mode gating of email-dependent flows + its own lockdown audit). **Deliverables:** `sessions/demo website/demo-site-plan.md` (canonical), `sessions/325. base-prompt.md` + `sessions/325. Events — Calendar Swap.md` (owner picked **A** for 325); `.gitignore` += `sessions/demo website/scrap/` (local-only reference images). Non-boundary, v2.3.0, no schema, no tests (fast Pest unchanged 2694/0). **Branch + version:** `session-324/1` (created from the 323-close HEAD — main was still at 322, 323 unmerged), not pushed. VERSION 0.323.01 → 0.324.01. **Next:** session 325 (Events — Calendar Swap) drafted at close. |
| 325 | Events — Calendar Swap (first demo-site build session, "session A"). **Retired the `EventCalendar` widget** in a full sweep — folder, `WidgetServiceProvider` registration, the `jcalendar` lib in `AssetBuildService`, `public.js` + `useLibraryLoader` references, `AdminPanelProvider` comments, the `jcalendar.js` npm dep, the help doc, and a seventh referencing test beyond the six enumerated; `widget_types` row dropped via a `WidgetTypeSeeder` block (seeder edit, not a migration — open-gate call). **Built `event_mini_calendar`** — server-rendered Carbon month grid, no Vue, pre-rendered ±1-month nav window (open-gate call); density dots from a windowed published-events `DataContract`; two back-end list modes (selected-day with today/soonest-on-load + clear-on-nav, and visible-month); a **self-contained expandable event detail** (date/time, location, status pills, optional description, conditional "Event page"/"Register" links) so a no-LP event is never a dead end; inline-editable rich-text heading; theme-driven accents (dots/today/selected all `--np-color-brand`, text/link/bg respect the standard appearance controls, zero hardcoded colours); width cap + square cells (no full-width morph); optional max-height scroll toggle; `data-tour="events-index.mini-calendar"`. Added `has_landing_page` to `projectEvent`. **LP-model pivot decided at close:** the optional/no-LP model retired — every event will get a Simple/Standard landing page at creation; demo-site plan reshaped + reordered (A ✅ → C event-LPs = **326** → B listing = **327** → D → E → F), 326 + 327 prompts drafted. Non-boundary, v2.3.0, **no schema**. Fast Pest 2694 → **2706 / 0**. Three inbox items logged (deploy-time widget-schema sync gap; `ChromeRenderer` per-layout-padding bug; PricingChart CTA-edit gap) + four capture-buffer folds. **Branch + version:** `session-325/1`, not pushed. VERSION 0.324.01 → 0.325.01. **Next:** session 326 (Event Landing Pages) or 327 (listing) — owner picks. |
| 326 | Event Landing Pages — Simple & Standard (demo-site session **C**, reshaped + moved ahead of B). **Every event now gets a real landing page at creation** so downstream widgets always have a resolvable URL. `EventResource::createLandingPageForEvent()` is invoked from every creation path (admin create, edit-backfill, `RandomDataGenerator`, the events importer). **Registration-gating is automatic, not an operator toggle** (owner-confirmed at close): the reg card is seeded only when the event is paid/capacity-capped (`eventNeedsRegistration()`); no persisted Simple/Standard flag, no schema change. The preset is a **two-column layout** (image left; description + optional reg + share right). **Deletion guarded** (`PageResource::guardLandingPageDeletion()` — an event's LP can't be deleted out from under it). New **`event_image` widget**. `SystemModelProjector::projectEvent` dead-end `/events` URL fallback removed; mini-calendar relinked to the always-present LP. Non-boundary, v2.3.0, **no schema**. Fast Pest **2716 / 0** (1 unrelated parallel-isolation flake, green in isolation). **Branch + version:** `session-326/1`, not pushed. VERSION 0.325.01 → 0.326.01. *(Index row added retroactively at 327 close — omitted at 326 close.)* |
| 327 | Events — Listing Upgrade (demo-site session **B**). Additive upgrade to `EventsListing`; scope grew via owner iteration. **Two orthogonal layout toggles** — `side_by_side_rows` (item layout) + `group_by_day` (grouping) — beside the unchanged grid/carousel default; **`columns`** now drives every layout incl. the row grid. **Featured-event hero** (picker; renders in all layouts, pulled out of the listing to avoid duplication). **Event-Type filter** as a controls-row dropdown built from the **tags** on listed events (no `event_type` column; **Date-Range cut**). **Art-fallback** thumbnail→header→solid-colour date tile (no text-tile). Free/Sold-Out badges; inline `ItemList`/`Event` JSON-LD (OG/canonical stay page-level); empty/sparse states; `data-tour` anchors. **Styleable day headings** via a rich-text `day_heading_template` with namespaced `{{day.*}}` tokens (bare tokens collide with `WidgetRenderer`'s page-token substitution). Carousel-only inspector controls now **hide** in static layouts via new **array-form `shown_when`/`hidden_when`** in `InspectorField.vue`. **`sold_out` event flag** (the one schema change: `events.sold_out` bool): operator toggle, closes registration in the widget **and both registration controllers**, excluded from the importer (`EventFieldRegistry`). Infra: isolated `bootstrap/cache` volumes for the e2e stack (`docker-compose.e2e.yml`) so local Playwright runs aren't broken by a dev manifest in the `--no-dev` image. Mid-session Playwright verification spec written + **torn down at close** (not CI-viable — isolated stack has no widget-bundle build server). Non-boundary, v2.3.0; schema: `events.sold_out`. Fast Pest 2716 → **2735 / 0**. One `[post-milestone]` inbox item (automatic `sold_out` toggling) + two capture-buffer folds. **328/329 prompts drafted** (328 = page-edit lock, pivoted to a `locked` flag + permission; 329 = demo upload lockdown). **Branch + version:** `session-327/1`, not pushed. VERSION 0.326.01 → 0.327.01. **Next:** 328 (page-edit lock). |
| A007 | Playwright e2e Re-Stabilization (Round 2) (agentic) — Cloud-agent stabilization of the `Playwright e2e (isolated stack)` CI job after it went red on `main` again since A005 closed. Same recurring pattern A003/A005 named: specs committed without being run + product drift against existing specs; the aggregate `Tests` check is Pest-only by design (e2e non-gating), so the red merged unnoticed. Method inherited from A003 (evidence-first via a committed-digest read channel, classify, lightest-fix, restore-on-handback); no prior diagnosis carried forward. **Phase 0 — A007 prompts drafted** (`sessions/A007. base-prompt.md` + `sessions/A007. Playwright e2e Re-Stabilization (Round 2).md`, modeled on A003's pair). **Phase 1 — read channel + first digest.** Cloud env has no Docker, no local Playwright, and the GitHub MCP exposes no Actions logs — so re-created the A003 `DIAGNOSTIC (temporary, A007)` plumbing (Pest jobs `if: false`, Playwright `needs: slow` dropped, `on.push` excludes `ci-logs/**`, JSON+stdout+rc capture, digest publisher with in-container probes for `laravel.log` / `public/build` state / delivered HTML head of `/`, `/news`, `/admin/login`) + `scripts/ci/pw-digest.mjs` to a `ci-logs/playwright` branch readable via MCP. **First digest: 46 specs, 44 passed, 1 unexpected, 1 flaky** — no uniform-timeout signature (slowest-15 clustered on heavy importer specs), so no shared environmental cause. **Phase 2 — classify.** The sole unexpected: `page-builder/layout-inspector.spec.ts:26` timed out on `getByRole('button', { name: 'Column Settings' })`. Root cause: the page-builder Aria sweep (`458caac`) made the inspector tab strip a proper ARIA tablist — `InspectorTabs.vue` now gives each tab `<button>` `role="tab"` + `aria-selected`, and the explicit role overrides the implicit `button` role so the three tabs no longer match `getByRole('button', …)`. Spec last touched at A005 (`4e2d575`), before the sweep — a test not re-run after an accessibility improvement landed. **Verdict: decision-tree (a)** — product change is deliberate and correct, spec updates. The two specs that triggered this round both **passed**: `text-widget-border.spec.ts` (E17, `appearance_config.layout.border` shape matches `AppearanceStyleComposer::composeBorderProps`) and `accessibility.spec.ts` (axe: zero WCAG 2 A/AA violations on `/` + `/news`). `inline-editing-foundation.spec.ts:79` flaky = the known cold-load `waitForURL` flake (A003-deferred, A005 passive-watch); retry-recovered, within `retries: 1`, no action. **Phase 3 — fix.** `layout-inspector.spec.ts`: three tab-visibility assertions + two tab-switch clicks switched `getByRole('button', …)` → `getByRole('tab', …)` with a comment recording the tablist contract; spec-only. **Verification digest: 46 passed / 0 failed / 0 flaky / 0 skipped** (cold-load flake did not recur). **Phase 4 — handback.** Stripped every `DIAGNOSTIC (temporary, A007)` block, deleted `scripts/ci/pw-digest.mjs` + the empty `scripts/ci/`; `tests.yml` confirmed byte-identical to the branch base (GitHub `main` `8172d0d`) — zero net workflow delta. **Diff scope:** 1 e2e spec fix (`layout-inspector.spec.ts`); 2 A007 prompt docs; session log + completed-sessions row + VERSION. No application code, no schema, no Fleet Manager surface (v2.3.0 throughout). **No deletions** (decision-tree (a) only) — no `[test-integrity]` housekeeping additions. **Branch + version:** work + close on `claude/modest-franklin-7ARkS` (cloud agentic branch); pushed; PR opened to `main` at user request. VERSION 0.325.01 → 0.907.01 (agentic-session band). **Carry-forward:** the page-builder cold-load flake stays on passive watch; A003's Filament header-action `requiresConfirmation()`-on-custom-Pages investigation remains open. |
| 328 | Per-Page Permission — Locked Demo Landing (demo-site session **D**). A per-page **edit lock** on the `Page` model (pages, posts, event LPs, header/footer system pages — one model). **Shipped as a `locked` boolean + `edit_locked_pages` permission**, pivoted from §2a's `restricted_roles` deny-list (a `status` enum value would have dropped locked pages out of `Page::scopePublished()` and made them vanish — kept `status='published'` + a separate orthogonal flag, displayed "Published & Locked"). `pages.locked` migration (default `false`, concrete). Permission granted to the real page-editing roles — `cms_editor`/`blogger`/`developer` (there is **no literal `admin` role**; the prompt's "admin + super_admin" mapped to those + the `Gate::before` super_admin bypass); **`demo` excluded**. **One rule, two enforcement points** (`locked && ! can('edit_locked_pages')` ⇒ deny): `PagePolicy::update()`/`delete()` **and** the page-builder save API — a new `PageBuilderApiController::assertOwnerEditable()` guard wired into **every** write route (store/update/destroy/copy/reorder, all image endpoints, layout CRUD), covering both the owner-scoped `pages/{owner}/...` shape and the widget/layout-keyed shape (owner via `$widget->owner`); the builder previously did no per-page check. **403** on a locked page. Permission-gated **"Lock editing (Published & Locked)"** toggle on the page edit/create forms; **extended to blog posts** (PostResource form + EditPostDetails) at owner request — cheap, since posts are the same model and the policy/save-API/demo-reset already covered them. Lock-icon + tooltip on the status badge in both lists (combined, not a second column). **`demo:reset` locks every page** idempotently after the baseline (inside the `isDemoMode()`-guarded command) so it survives the daily wipe and auto-covers session E's curated pages; demo role's **`navigation_menu` trimmed to view-only** (header/footer link structure lives in NavigationMenu/NavigationItem, which the page lock can't reach). **Deletion hardening beyond the two named points:** since `demo` holds `delete_page` and Filament's resource delete paths bypass the policy, made `PageResource::canDelete()` lock-aware and added a locked-record skip to the bulk-delete closures on both PageResource/PostResource. Out of scope (noted for 329 / pen-test): `form`/`collection` public-widget edits, the demo media-upload surface (329). Non-boundary, **v2.3.0 unchanged**; schema: `pages.locked`. Fast Pest 2735 → **2749 / 0** (+14: PageLockTest 12, DemoResetCommandTest +1, DemoRoleLockdownTest +1; one unrelated `RecordDetailViewSeederTest` parallel-isolation flake, green in isolation). One capture-buffer fold (blog template: social-share + hero with title/date). **Branch + version:** `session-328/1`, not pushed. VERSION 0.327.01 → 0.328.01. **Next:** session 329 (Demo Upload Lockdown) — prompt already drafted. |
| 329 | Demo Upload Lockdown (demo-site hardening, shipped between D and E). A who-can-upload gate blocking **new file uploads for the `demo` role** at every server endpoint that introduces a new file, while leaving existing-media reuse and all other demo CRUD intact. New **`BlockDemoUploads` middleware** (403 when `hasRole('demo')` — role-scoped, not demo-mode-scoped, so an admin/super_admin maintaining the node still uploads) applied at **three** new-file chokepoints (the audit found three, not the two the prompt named): the **Livewire temp-upload endpoint** (via `config/livewire.php`, covers every Filament `FileUpload` field at once; existing throttle default preserved explicitly), the **two page-builder upload routes** (`uploadImage` / `uploadAppearanceImage`; the `useExisting*` reuse routes left open), and the **rich-text inline-image route** — which had *no* prior permission gate, so this also closes a small pre-existing hole. UX layer: `FileUpload::configureUsing` disables the field + "File uploads are disabled in the demo." helper text for the demo role (the Vue page-builder + inline-image surfaces fall back to the endpoint 403). `DemoRoleLockdownTest` +5 cases. Non-boundary, **v2.3.0**, **no schema**, no seeder change. Fast Pest 2749 → **2754 / 0**. **Branch + version:** `session-329/1 … /2`, not pushed. VERSION 0.328.01 → 0.329.02. *(Index row backfilled at 330 close — omitted at 329 close.)* **Next:** demo-track paused; session 330 (Widget Styling Boundary). |
| 330 | Widget Styling Boundary — Audit & Cleanup (first session of the **Widget Styling Contract** arc, Widget Autonomy track; deliberate switch off the paused demo-site track). **Phase 1 audit** (no app code) surveyed all `app/Widgets/*/*.scss` + the host seam by five matrices (spacing / type / breakpoints / specificity-cascade / general host-var leaks); findings at `sessions/330. Widget Styling Boundary — Audit & Cleanup — Audit.md`. Result: the leak surface is far smaller than the source spec implied — only `$gutter` (one widget) + viewport `@media`; **no type leak, no other host-var leak**; the `@container` migration is *not* behavior-preserving for column-capable widgets; `@layer` carries the most blast radius → defer. **Phase 2** executed the agreed fixes: (1) **`ThreeBuckets` deleted entirely** (owner call — removed the sole `$gutter` leak): dir, `WidgetServiceProvider` registration, registry-synced seeder, tests (`BoardMembersThreeBuckets`→`BoardMembersSeederTest` minus its 2 three_buckets blocks, the parity block, InlineEditing304 roster 11→10 + Tier-B, `WidgetPicker` count 40→39), `_custom.scss` comment, `widget-system.md` example; verified no seeded page referenced it. (2) **`@container` migration for the two always-full-width widgets** (`pricing_chart`, `product_carousel`): a **per-handle** `np-widget` named-container rule in `resources/scss/_layout.scss` (scoped, not universal — `container-type` carries a containing-block/stacking blast radius) + `@media`→`@container np-widget`; verified objectively via real-wrapper Playwright injection (`pricing_chart` 3 columns at a 900px container, collapses to 1 at ≤768px). (3) **`EventMiniCalendar` option 3** (owner call, prompted by a reported `events-demo` bug): removed its `@media(max-width:1023px){display:none}` so it stacks below on column collapse instead of vanishing in the **pre-existing** 769–1023px dead-column window (its 1023px hide sat above the layout's 768px collapse) — confirmed pre-existing via disposable read-only Playwright; fix verified at 1000/768/375px. (4) **Enforcement gate** `WidgetStyleBoundaryConsumptionTest` (design group, mirrors `WidgetColorTokenConsumptionTest`): bans bare `$gutter` (baseline empty) + new viewport `@media` (reviewed baseline = the 4 deferred column-capable widgets); `DesignGroupIntegrityTest` 20→21. (5) `widget-development.md` SCSS section rewritten (boundary, `@container`-not-`@media`, `np-widget` opt-in, token vocab). **Deferred:** column-capable `@container` (`MapEmbed`/`LogoGarden`/`BoardMembers`/`EventsListing`) → **331**; `@layer` cascade isolation → **332** (both prompts drafted at close). Non-boundary, **v2.3.0**, no schema. Fast Pest 2754 → **2753 / 0** (−3 deleted three_buckets tests, +2 new boundary cases). Builds: `build:public` + `npm run build`. Unrelated working-tree thumbnail churn (owner-run admin thumbnail regen, not 330) excluded from the commit. Two `logbug` captures folded (widget-thumbnail audit; event-calendar margin-collapse). **Branch + version:** `session-330/1`, not pushed. VERSION 0.329.02 → 0.330.01. **Next:** 331 (remaining-widgets container-query migration). |
| 331 | Widget Container-Query Migration — Remaining Widgets (second session of the **Widget Styling Contract** arc, Widget Autonomy track). Finished the viewport-`@media` → `@container np-widget` migration for the column-capable widgets 330 deferred — **BoardMembers** (collapses 4-up→2-up off its *own* width, verified by real-wrapper injection), **EventsListing** (+ the hardcoded `48rem` literal normalized to `#{$bp-md}`), **LogoGarden** — emptying the `WidgetStyleBoundaryConsumptionTest` viewport-`@media` baseline so the gate now asserts **zero** widget-SCSS `@media`. Two owner-directed growths: **MapEmbed heading field removed entirely** — its only responsive rule was inert (out-specified by host `.np-site h2`, 0,1,3 vs 0,1,0); demoted `inlineEditable()`→false per the session-308 precedent (eligibility roster 10→9), dropped from the `np-widget` rule, `WidgetColorTokenConsumptionTest` baseline line 39→34, vacuous `MapEmbed — heading parity` case removed. **LogoGarden rebuilt** to stop real logos **overlapping** (a partner-logo contract concern): a `logo-garden__media` bounding box with `object-fit: contain` (0/10 overflowing, 0/10 distorted across ten varied aspect ratios incl. a 30×120 sliver + 600×100 wordmark), **flexbox + `gap` + `justify-content: center`** layout (CSS grid tried then **walked back** — grid left-aligns a partial last row, logos must center), single-source `--logo-gap` driving both flex gap and the per-cell width share. Verified via disposable real-wrapper Playwright injection harnesses (torn down at close — not CI-viable). Non-boundary, **v2.3.0**, **no schema**. Fast Pest 2753 → **2752 / 0** (−1 = the removed vacuous parity case); design group 244. Builds: `build:public` + `npm run build`. Planning: `mobile-collapse-brief.md` reconciled to app structure (viewport-vs-container mechanism map, the sanctioned viewport-`@media` spacing primitive, breakpoint single-sourcing approach B); `session-outlines` Mobile Public-Site Readiness pre-Beta push added; **332 `@layer` prompt pair drafted**. **Branch + version:** `session-331/1`, not pushed. VERSION 0.330.01 → 0.331.01. **Next:** 332 (`@layer` cascade isolation), then 333 (Mobile Nav). |
| 332 | @layer Cascade Isolation **+ widget container-query rollback** (closing session of the **Widget Styling Contract** arc, Widget Autonomy track). **Planned piece (commit `3fdf754`):** rolled all host CSS into cascade layers — `@layer reset, host, widgets;` (owner chose 3 layers over 4, to preserve host-vs-host cascade exactly), declared via a new `_layers.scss` `@use`'d first in `public.scss`; reset/vendor (normalize+swiper) into `@layer reset` via `meta.load-css()`; every host partial wraps its body in `@layer host` (covers both the Vite bundle and the build-server bundle, which read the same files); `collectSources()` wraps the runtime emit (typography/colour/buttons) in `host` and each widget source in `widgets`; nav-family inline `<style>` in `host`; operator `custom_scss` left **unlayered** by design. Specificity workarounds retired as proof (EventMiniCalendar/EventsListing `display:block`→`list-style:none`; EventsListing filter `flex:0 0 14rem`→`width:auto`). Standing `WidgetStyleLayerDisciplineTest` (design 21→22). Proven behavior-preserving by a source-level `collectSources()` diff (only layer wrappers + the 3 reverts) + clean Vite host-bundle diff. **Owner-directed growth (commit `33278b6`):** live Playwright investigation showed the 330/331 `@media`→`@container` migration regressed the tool — it keyed widget collapse to mobile *viewport* breakpoints (768/576) applied to *container* width, so a column-placed widget rendered its phone layout on desktop **and overrode the operator's configured count** (logos-per-row, items-per-row, column_count). **Rolled it back per the preserve-user-control principle:** removed all `@container np-widget` blocks (5 widgets), the `np-widget` context in `_layout.scss` (kept `.page-layout`'s separate page-collapse), and LogoGarden's Swiper viewport `breakpoints` ramp; `slidesPerView`/counts now honor config flat at every width; `widget-development.md` updated. No `@media` reintroduced → consumption gate stayed green. Public re-verified clean in a real browser (2-col, working pager, counts honor config). **Three editor↔public fidelity bugs found & scoped to 333** (none from 330/331): C — editor Swiper load *race* (intermittent broken pager, confirmed); B — `.content-card` missing `min-width:0` (uneven `1fr` tracks); E — a layout column lost at some point (label "3 Column Layout" but `columns=2`), bounded-attempt-then-report. Non-boundary, **v2.3.0**, **no schema**. Fast Pest 2752 → **2756 / 0** (+4 = the layer-discipline guard); design group 248. Builds: `npm run build` + `build:public`. One capture-buffer fold (alert before deleting a column with content on a columns change — corroborates E). **333 promoted ahead of Mobile Nav (→ 334+);** prompt pair drafted. The Widget Styling Contract arc concludes here (`@layer` kept; the `@container` migration rolled back). **Branch + version:** `session-332/1`, not pushed. VERSION 0.331.01 → 0.332.02. **Next:** 333 (Page Builder ↔ Public — Widget Parity). |

| 333 | Page Builder ↔ Public — Widget Parity (Behavior + Layout). Fixed the editor↔public fidelity bugs surfaced by the 332 container-query rollback, plus the real WYSIWYG bug the owner found mid-session. **B — uneven grid:** `.content-card` lacked `min-width:0`, so `repeat(N,1fr)` distorted to content min-width at narrow widths (measured `111/133/100px`); one-line fix on BlogListing + EventsListing, re-measured even through the real `build:public`. **The real fix — editor pager parity:** the page-builder rendered Swiper's *default* pagination (tiny round dots) while public rendered the designed numbered pill pager; root cause = the admin head (`AdminPanelProvider` `panels::head.end`) injected swiper's vendor CSS as a **bare unlayered `<link>`**, and unlayered CSS beats every `@layer` regardless of specificity, so Swiper's defaults won the editor only (332 put public's swiper in `@layer reset` so `@layer widgets` wins). Confirmed via live CSSOM + computed style; fixed by emitting the vendor CSS into `@layer reset` (`<style>@import url(…) layer(reset)</style>` in the head hook + `useLibraryLoader` fetch-and-wrap), re-driven through `npm run build` → editor now matches public. One residual **owner-accepted out of scope**: pager nav-arrow border differs because Filament's unlayered Tailwind preflight (`*{border-width:0}`) leaks into the canvas — a host-cascade problem unfixable from inside a layer. **C — Swiper "race":** could NOT reproduce (throttled swiper 0–3000ms across full-reload + `wire:navigate`, guarded + unguarded widgets — initialized every run); owner agreed it was a **stale-build artifact from 332**. Kept cheap defense-in-depth guards (`window.Swiper`/`SwiperModules` bail) on the five Swiper widgets anyway — a genuine latent crash; no happy-path change. **E — layout-column drop:** one bounded repro (change a middle-column widget's `config.columns` via the real save endpoint) → `page_layouts.columns` unchanged; the "3 Column Layout / columns=2" mismatch is a stale free-text label, not a dropped column. **Standing guard:** a Playwright behaviour-parity harness isn't viable on the isolated e2e stack (no widget build server in CI, per the full-width-matrix note), so added a build-pipeline-independent **Pest guard** in `WidgetJsLibsSession138Test` (already design-group) that renders the real admin head with a fixture manifest and locks the `@layer reset` emission. `WidgetColorTokenConsumptionTest` baseline line `:123`→`:128` (B shifted it). Logged the **editor↔public architecture** future-work stub (homogenize hydration + layer the admin reset, ~3 sessions; iframe ruled out — block drag can't cross a frame). Non-boundary, **v2.3.0**, **no schema**. Fast Pest 2756 → **2757 / 0** (+1 = the layer-parity guard); design group 249. Builds: `npm run build` + `build:public`. Unrelated thumbnail churn + `.claude/settings.json` excluded from the commit. **334 (Mobile Nav) prompt pair drafted.** **Branch + version:** `session-333/1`, not pushed. VERSION 0.332.02 → 0.333.01. **Next:** 334 (Mobile Nav), heading the Mobile Public-Site Readiness push. |

| 334 | Mobile Nav — Hamburger + Footer Columns (first session of the pre-Beta **Mobile Public-Site Readiness** push). First established the **live surface**: the homepage chrome is a `ChromeRenderer` CMS page, so the live header/footer are the **CMS Nav widget** (`widget-nav--horizontal` / `widget-nav--columns`), **not** the hardcoded `components.site-header` Blade chrome (dead on this install, left untouched). **Hamburger — rebuilt CSS-only (the owner's morph-don't-swap pattern).** Original break, root-caused live: `.widget-nav__mobile` had a stylesheet `display:none` that beat Alpine `x-show` (which only clears *inline* display), so the menu never opened; it was also trapped in the header's narrow `auto 1fr` grid cell. A first attempt — `position:fixed` full-screen overlay + separate close-X + `html`/`body` scroll-lock — was **rejected** (full-screen "looks simpler but is more complicated": scrollbar/X hop from `inset:0` shifting when the page scrollbar vanished, double-gutter from `scrollbar-gutter:stable` beside the overlay's own scrollbar, covered hamburger). **Final:** three siblings (`input.widget-nav__toggle` checkbox + `<label>` hamburger + `.widget-nav__mobile`) driven by `:checked ~`, no JS; **one hamburger that morphs to the X in place** (verified x=323/y=24 closed *and* open, no separate close button); **absolute drop anchored full-width below the header** — removed `position:relative` from `.widget-nav` so the drop anchors to the full-width `.page-layout` (`container-type:inline-size`), `top:100%` → x=0/w=390; **no `position:fixed`, no scroll-lock** (nav is never fixed/sticky → the drop lives in the document and scrolls with the page; tall menus extend page scroll). Desktop dropdowns stay on Alpine (anchor to `.widget-nav__item`, unaffected); sub-menu expanders are nested `:checked` checkboxes (mobile path fully JS-free); `aria-controls` on the checkbox. **Footer columns** — left-aligned via the existing alignment control: `.widget-nav__columns` keeps `justify-content: var(--nav-justify)` (already mapped left/center/right → flex-start/center/flex-end), so the fix is config-side (set the footer nav's alignment to "left"). A viewport-`@media` single-column collapse was **reverted** (widget-SCSS `@media` ban / 332 principle); a `flex-start` hardcode was reverted to keep the operator's control. **Guard** `NavMobileCollapseTest` (design group; `DESIGN_GROUP_FILES` 22→23): source-level, build-pipeline-independent — asserts the CSS reveal (`:checked ~ .widget-nav__mobile { display:block }`) + `position:absolute` (not fixed), locking the in-document-drop decision. Non-boundary, **v2.3.0**, **no schema**. Fast Pest 2757 → **2759 / 0** (+2 = the two guard cases); design group 249 → 251. Build: `build:public` only. Unrelated thumbnail churn + `.claude/settings.json` excluded from the commit. **335 (Mobile Vertical Spacing) prompt pair drafted** — the Mobile Collapse Ruleset narrowed (owner call) to vertical section-spacing reduction only; per-breakpoint editor authoring is the deferred correct fix. **Branch + version:** `session-334/1`, not pushed. VERSION 0.333.01 → 0.334.01. **Next:** 335 (Mobile Vertical Spacing), continuing the Mobile Public-Site Readiness push. |

| 335 | Mobile Vertical Spacing — Reduce Section Whitespace (Mobile Public-Site Readiness push, after 334). Built the **section-spacing primitive (vertical axis only)** — the one sanctioned new primitive from the mobile-collapse brief. Problem, reproduced live: operator section padding/margin was written as **literal inline px** (`padding-bottom:150px`), and inline beats any stylesheet, so phones got desktop-sized gaps nothing could compress (dominant offender: a chrome footer text-block at 150px bottom padding, identical at 375px and 1280px). **The "concrete 0 = no override" design call:** keep the non-zero emit gate, but emit the **vertical** axis (top/bottom) as `--np-pad-top`/`--np-pad-bottom`/`--np-mar-top`/`--np-mar-bottom` custom properties instead of literal declarations; a host-layer rule consumes each with a **`var(--np-pad-top, 0px)` fallback** that reproduces today's no-override exactly (verified the `.widget`/`.page-layout`/chrome wrappers carry **no intrinsic** padding/margin in any SCSS, so `0px` = unchanged). Horizontal (left/right) stays literal. **Shared helper** `AppearanceStyleComposer::composeVerticalSpacingVars()` is the single emit source used by all three surfaces in lockstep: **widgets** (`compose()`, also flows to the page-builder preview), **column layouts** (`composeForLayout()`), and **chrome** (`ChromeRenderer::buildInlineStyle()` + the flat-key layout path; chrome **root** widgets render as a bare `<div style>` with no `.widget` class, so they gained a **`.np-chrome-section`** hook). **Host rule + scaling** in `resources/scss/_layout.scss` (`@layer host`, ships in both compile paths per 332): `.widget, .page-layout, .np-chrome-section` consume the properties, then two cascading viewport steps scale the vertical axis — `@media (max-width:$bp-md)` ×ratio, `@media (max-width:$bp-sm)` ×ratio² — off a **single-sourced `$section-spacing-ratio: 0.8`** (mobile = Sass-computed 0.64). **Before/after, re-driven live:** footer `padding-bottom` 150px desktop → 120px tablet → 96px mobile; desktop byte-identical. **Standing guard** `SectionSpacingPrimitiveTest` (design group; `DESIGN_GROUP_FILES` 23→24): build-pipeline-independent — locks the helper's `--np-*` emission + concrete-0 omission and the host rule's `0px` fallback + the two scaling steps off the single ratio. Five existing render-layer tests updated from the old literal contract to the custom-property one. **The `.np-site` page-bottom margin (32→96px on mobile)** surfaced as the largest remaining mobile whitespace but is **owner-confirmed intentional** (thumb-reach affordance) — left as-is. Also authored this session at owner direction: a **no-bump documentation revision** to the FM agent contract (new § Demo-node reset coordination + CHANGELOG) recording the demo node's `RestoreFromBlob` reset decision for the FM side — documentation only, **contract stays v2.3.0**. Non-boundary, **no schema**. Fast Pest 2759 → **2763 / 0** (+4 = the guard's four cases); design group 251 → 255. Builds: `npm run build` + `build:public`. Unrelated thumbnail churn + `.claude/settings.json` excluded from the commit. **336 (Demo Node Restore Wiring) prompt pair drafted** — the reshaped E (demo node assembly): a demo-mode-gated `demo:restore` command + export/import fidelity pass. **Branch + version:** `session-335/1`, not pushed. VERSION 0.334.01 → 0.335.01. **Next:** 336 (Demo Node Restore Wiring), resuming the Demo Site track. |

| 336 | Demo Node Restore Wiring + Export Fidelity (resumed the **Demo Site track**, the reshaped **E — demo-node assembly**; CRM half of the demo-node reset coordination the 335 doc revision recorded). Built the demo-mode-gated **`demo:restore`** artisan command (`app/Console/Commands/DemoRestoreCommand.php`): reads a **local pushed blob** (arg, default `storage/app/backups/demo-baseline.zip`; never fetches — keeps the node's deny-outbound egress posture), **hard-gated to `isDemoMode()`** at the top (mirrors `DemoResetCommand`/`DemoRoleLockdownTest` — a restore-from-arbitrary-blob primitive must never reach a customer node), then automates the documented manual restore: extract (ZipArchive, applies `BACKUP_ARCHIVE_PASSWORD` if set) → `db:wipe --force --drop-views --drop-types` → `gzopen`-stream the dump + replay via `psql -v ON_ERROR_STOP=1 -f` (conn params + `PGPASSWORD` from config; dump file resolved by **glob** so an authoring-vs-node DB-name mismatch doesn't matter) → `File::cleanDirectory`+`copyDirectory` the `storage/app/public` tree → `DB::reconnect()`+`Cache::flush()` → **fix `base_url` from the node's own `APP_URL`** so the authoring value never leaks. **`demo:reset` retained** for local/dev; the node's reset cron points at `demo:restore`. **Fidelity pass — reproduced one real round-trip first (observe, don't assert):** a `backup:run` blob (408 MB) carries the full DB dump + the entire `storage/app/public` tree wholesale, so the site-identity `SiteSetting`s (`site_name`/`base_url`/`admin_logo_path`/`dashboard_welcome`), the logo **file** (`storage/app/public/site/…`), and `appearance_background_image` media (in the `cas/` tree) all carry **for free** — the "background images don't carry" complaint is the **selective session-303 portability-bundle path**, which the demo does **not** use; **no fidelity fix needed**. Drove a real `demo:restore` on the local demo install: every count round-tripped exactly (75 contacts / 106 events / 249 pages / 585 media / 24 settings / 2000 CAS files), site served after (home 200, admin 200). **Faker `--no-dev` blocker confirmed moot** under restore-from-blob. **Standing guard** `DemoRestoreCommandTest` (2 cases): refuses outside demo mode + refuses on a missing blob, wiping nothing. Doc: `demo:restore` subsection added to `app-reference.md`. **Owner-directed side cleanup:** investigated the large blob — found **228/280 `cas/` dirs orphaned** (re-seeding truncates `media` rows without firing the per-model file-removal event; **not** a backup/restore artifact), swept them + deprecated conversions + two stale 4.3 GB backup zips (media tree 416→308 MB, backups 9.0 GB→573 MB, live media intact); logged the `media:prune-orphans` gap to the housekeeping inbox. Added three owner-authored project slash commands (`/open-session`, `/close-session`, `/write-session`) to `.claude/commands/`, reviewed + verified. Non-boundary, **contract stays v2.3.0**, **no schema**. Fast Pest 2763 → **2765 / 0** (+2 = the guard cases); design group 255. **337 (Portal Pages — sign-in / dashboard / account styling, demo arc F) prompt pair drafted.** **Branch + version:** `session-336/1`, not pushed. VERSION 0.335.01 → 0.336.01. **Next:** 337 (Portal Pages), the last CRM build session of the demo arc before the driver.js tour framework. |

| 337 | Portal Pages — Sign-in / Dashboard / Account Styling (Demo Site arc **F**, the last CRM build session before the driver.js tour). **Open-Gate discovery:** two parallel portal page systems were seeded side-by-side — `system/*` (newer, s322; post-login landed on `system/account`) and `members/*` (older `PortalPageSeeder`, s187; the `portal` nav menu that `layouts.portal` actually renders pointed here) — so the post-login page wasn't in its own nav and nothing highlighted. Owner-confirmed **consolidation onto `/members`** (auth stays at `/system`): retired the duplicate `system/account` dashboard (now a redirect **alias** preserving `route('portal.account')` across 6 controllers + tests with zero churn) and the split `edit-account`/`change-password` pages; combined both account forms onto **`/members/account`**; rewired the `portal` nav to Dashboard / Account / Event Registrations (active `aria-current` now resolves). **One visual language:** a `portal-hero` branded band defined once in `layouts.portal` (dashboard / account / event-regs) + the same band/tokens behind the sign-in / sign-up / reset **auth cards** in `layouts.public`, so logged-out + logged-in read as one designed surface across both chromes. **`layouts.portal` now loads the widget asset bundle** (was silently missing) so the dashboard `BarChart` renders (illustrative `chart-demo` data — not member-scoped, per the portal-security rule). `PortalPageSeeder`/`SystemPageSeeder` made idempotent + **self-reconciling** (create new, force-delete retired, rewire nav, ensure `chart-demo`); `MembershipCheckoutController` account URL → `/members`. `portal.*` `data-tour` anchors reserved (login / nav / dashboard / account); no tour code. Documented the **locked-auth-routing rationale** (login bound to fixed controllers; only URL prefixes operator-editable) in `settings-general` help + `app-reference`. **Close housekeeping:** fixed a pre-existing **`RecordDetailViewSeederTest` ordering flake** (idempotency assertion read widget order from a query with no `orderBy` → nondeterministic on Postgres; one-line `orderBy('sort_order')`) — surfaced once on the deploy CI run, not a 337 defect. Non-boundary, **v2.3.0**, **no schema**. Fast Pest 2765 → **2772 / 0** (+7 `PortalPagesSession337Test`); design group 255. Builds: `npm run build` + `build:public`. Unrelated thumbnail churn + `.claude/settings.json` excluded. **338 (driver.js guided-tour framework) prompt pair drafted.** **Branch + version:** `session-337/1…/3`; VERSION 0.336.01 → 0.337.02 (owner deploy bump on /2) → **0.337.03** (close); 337/1 + /2 merged to main + deployed by the owner mid-session. **Next:** 338 (driver.js tour), the marketing-ready gate that completes demo v1. |

| 338 | Guided Product Tour — driver.js framework + demo CRM walkthrough (Demo Site arc **marketing-ready gate**; **demo v1 complete**). Wired a **multi-page driver.js tour** into the Filament admin so a cold, anonymous prospect on the demo node understands the product unattended. **Reusable engine** (`resources/js/admin/tour/`, split so a later in-app-help mode reuses the primitives): `state.js` (localStorage cross-page resume), `anchors.js` (resolves only **owned** selectors — `data-tour` markers + sidebar links matched by our own route URLs, never Filament classes; the standing **249** mitigation), `controller.js` (drives one page-group per driver instance, persists the step before each hard navigation, resumes on load), `product-tour.js` (step script), `index.js` (launcher + resume wiring). Owned anchors injected via `AdminPanelProvider` render hooks + a **role-gated URL map** (emits a page's URL only when the current viewer can reach it → the tour **skips, never 404s**, inaccessible steps). **Demo-safe showcases** for the features the public `demo` role is deliberately locked out of (lockdown **unchanged** — `DemoRoleLockdownTest` still green): **Roles** renders the **real permission matrix fed sample data** (matrix markup extracted to a shared partial reused by the real role form — no drift), **Import** shows a **captured screenshot** (committed PNG, thumbnail-style; an `@on-demand` spec regenerates it), **Financial** points at the demo-viewable **Transactions ledger** (real Stripe rows, no credential page). **Owner feedback round (/3) reworked the tour into an interactive walk through a real supporter:** click a seeded **hero contact** → Membership Status → click **View transactions** (promoted from the ellipsis to a visible header button — the tour anchor) → that supporter's Stripe/QuickBooks ledger, collapsing the standalone Donations + Memberships list beats; closes on the dashboard highlighting the help search + the `?` flyout. Engine gained **click-to-advance** interactive steps, a `navRow` anchor, **live-selector re-query** (immune to the Livewire DOM morph that detached captured elements between same-page steps), a stale-spotlight-ring sweep, and `RecordDetailViewWidget` set **`$isLazy = false`** (its panel must be in the initial paint to be anchorable). `DemoBaselineSeeder` designates a **hero contact** (active membership + donations, reusing the generator's correct transactions) floated to the top of the list. **Fixes:** importer no scroll-hop (centered step); Roles popover no longer falls beneath (bounded anchor); dark-mode spotlight gained a bright amber ring (Playwright-verified contrast); popover type +30%; a distinct **"Exit Tour"** button; launcher reads **"Tour the Product"**; mailing-list copy says **Mailchimp only**. **Standing** Playwright spec `tests/e2e/tour/product-tour.spec.ts` walks the whole interactive flow (Faker-free hero, both click-throughs, every anchor a visible spotlight, cross-page resume across a hard reload, clean finish). Non-boundary, **v2.3.0**, **no schema**. Fast Pest 2772 → **2777 / 0** (+5 `TourShowcasePagesTest`). Built `npm run build` (driver.js v1.4.0 in the admin bundle). Also captured mid-conversation (out of tour scope): the **`deploy.yml` immutable-VERSION rule** documented in `CLAUDE.md` + a deploy/version-recognition note in `app-reference.md`, and an **in-app contextual-help tour** stubbed into the Post-Beta backlog. **339 (non-roadmapped public-site restyle — designer comp) prompt pair drafted.** **Branch + version:** `session-338/1 … /3`; 338/1 merged + deployed by the owner mid-session (with a /2 version bump); VERSION 0.337.03 → 0.338.02 (owner deploy bump) → **0.338.03** (close). **Next:** 339 (public-site restyle). |

| 339 | Public Site Restyle — Designer Comp (home page + site-wide theme colours) — **non-roadmapped design-revision pass**; opened the **Page Composition Fidelity** track. Executed the designer's comp against the imported `home` page + local theme; **deliverable is local CMS/theme DB state the owner exports** (not committed). **Theme (`theme_colors`):** `brand`/`link`/`nav-hover`/`nav-active` `#0172ad` → **`#dd6a18`** (orange). **Buttons (`button_styles`):** `primary.bg_color` + `text.text_color` `#0172ad`→`#dd6a18`, `secondary.border_color` `#111827`→`#dd6a18`, `secondary-dark.border_color` `#ffffff`→`#dd6a18` (buttons are `--btn-*`-driven, **not** the brand token — the integration-seam finding). `build:public` recompiled both. **Home Bands (`page_layouts`/`page_widgets` appearance):** What-it-Does (heading+grid) + Pricing bg `#e5e7eb`→`#e9f3fa`; not-a-SaaS `#373c44` + final-CTA `#000000` both → `#252b33` (comp unifies the two dark tones); **all Band vertical padding halved** (spacing lived on the **column**, not the widget — page had been double-padding, ~20% too tall) + home widget vertical padding zeroed; Band-4 grid `grid_auto_rows` `1fr`→`auto` (even mobile stacking); **hero widget swapped to text_block** (it self-wraps a 2nd `.site-container`, so it was not flush to the container); **final CTA split into two dark Bands** (orange "Try it free!" heading band over a white-subtitle+CTAs band — two text blocks cannot share a grid column), subtitle copy taken from the comp. Verified desktop+mobile region-by-region vs the comp across owner review rounds. Recurring correction: **comp is the truth, not just its margin notes** — colour + layout/alignment/spacing/button-outlines all read off the design. **Planning/cleanup (committed):** new **Page Composition Fidelity** track (track doc + finding-template + outlines pointer; gap backlog G1–G4 → Widget Autonomy); **340 (Public Chrome Parity)** + **341 (Widget Schema Sync in Deploy Path)** prompt pairs drafted; housekeeping mobile-menu items corrected (owner: fixed) + `[0.338.03]` collapse-toggle folded to 340; **`sessions/public website/` pruned** to its 3 durable files (buildout paper trail deleted; `demo.json`→`tests/Fixtures/` with `DemoLoginTest` repointed; dead references cleaned across `release-plan.md` + the PMW track doc). Non-boundary, **v2.3.0**, **no schema**, no app code. Fast Pest **2777/0** (unchanged — restyle is data); design group 255; `DemoLoginTest` 6/0 after the fixture move. **Branch + version:** `session-339/1`, not pushed. VERSION 0.338.03 → **0.339.01**. **Next:** 340 (Public Chrome Parity), then 341; public-site restyle resumes when the designer delivers comps for the other pages. |

| 340 | Public Chrome Parity — header/footer mobile collapse + per-layout appearance + render polish — **non-roadmapped housekeeping batch** (six consumed inbox items + two owner-raised operator blockers), run while the 339 restyle waits on designer comps. Non-boundary, **v2.3.0**, **no schema**. **Chrome parity (⭐ core):** `ChromeRenderer::renderLayoutBlock` was a stale duplicate — it read **flat legacy keys** (`padding_*`/`background_color`) the 207 migration deleted and never emitted `data-collapse-mobile`, so column-layout padding/background set on a header/footer showed in the builder + on page bodies but **vanished on public chrome**, and chrome columns never stacked on mobile. Routed it through `AppearanceStyleComposer::composeForLayout` (nested `appearance_config.layout.*`) + `resolveFullWidthForLayout`, emitted `data-collapse-mobile` (default true), **and** switched the grid track literal `grid-template-columns` → the `--layout-cols` custom property (the literal would otherwise beat the `_layout.scss` `@container` collapse rule — inline wins; both halves required). Full-width default for a key-less layout flips contained → full-bleed-bg, matching page bodies. Three `ChromeWidgetTest` cases rewritten off the old flat-key/literal-grid contract; standing drift guard added (collapse attr + nested padding reach HTML, + opt-out). Verified on real HTTP chrome (`_header` 2-col logo|nav padding 12/24/12/24, the install's real `_footer` `page_layout`). *Footer nuance:* the install's footer is a `page_layout` (fix applies), but the **seeder default** footer is a root Nav `orientation:columns` flex-wrap (not a `page_layout`) — fresh-install footer-column mobile behavior is a separate Nav-widget concern, out of scope. *Verification incident (local-only, fully recovered):* `_header`/`_footer` were empty locally (live site used the fallback Blade chrome); I seeded canonical chrome to verify, then mistakenly deleted the rows believing they were my seed — they were pre-existing operator chrome the seeder had skipped. Restored byte-faithfully from the 2026-06-02 backup via `COPY` (2 layouts + 4 widgets, FK targets intact); restored render matched the pre-delete state; no net data change, no production impact. **Render polish:** nav landmark (`role="group"` dropped from `<nav>`); `url()` background-image now quoted/escaped in `composeBackgroundImage` (`photo (1).jpg` etc. no longer break CSS — single emission site); `.sr-only` added to the public bundle sharing the existing `.visually-hidden` rule; **hero left-flush** (closes Page Composition Fidelity **G2**) — a default hero double-contains (renderer wraps it + the hero template nests its own `.site-container`), so the `.hero--pos-*-left` variants now neutralize the inner container's width/centering/horizontal-padding so left content sits flush (left variants only). **Spacing-inheritance fix (owner blocker, latent s335 bug, not an inbox item):** setting vertical padding/margin to **0** had no effect (prior value persisted; only `1`+ worked) — the `--np-*` properties **inherit**, the composer emits nothing for a 0 side, so a child at 0 inherited the parent container's value (a column layout's 50px padding bled onto every child). Fix in `_layout.scss`: the host rule **resets** the four `--np-*` props to a concrete `0px` on each `.widget`/`.page-layout`/`.np-chrome-section`, so each wrapper resolves to its own 0 unless a non-zero inline override wins; no composer change. Guard added to `SectionSpacingPrimitiveTest`. **Button link target (inbox `[0.324.01]`):** added an explicit **Same tab / New tab** control to the shared `ButtonListField` + `widget-shared.buttons` (explicit target wins; no-target buttons keep the legacy external-auto `_blank`; new buttons default `_self`). No schema (target sub-key survives `filterConfigToSchema`); ships in the JS bundle, no widgets-sync needed; covers Hero/TextBlock/ThreeBuckets. **PricingChart CTA editing (inbox `[0.324.01]`):** CTAs were unreachable (columns repeater `inspector:false`, buttons not inline-editable). `RepeaterField` now respects per-sub-field `inspector:false` (mirrors top-level `InspectorPanel`; data preserved — `defaultRow()` keeps the unfiltered list; no-op for every existing repeater); the `columns` repeater is inspector-visible scoped to **Title + CTAs**, inline text/feature fields `inspector:false`. **Depends on a widget-schema sync to reach a server** (the inspector reads `widget_types.config_schema` from the DB — the `widgets:sync` gap, session 341); local schema re-synced via `WidgetTypeSeeder`. **Inbox fold:** 8 items shipped (chrome per-layout-appearance ⭐, footer-collapse `[0.326.01]`+`[0.338.03]`, nav landmark, unquoted `url()`, `.sr-only`, hero left-flush, button target, PricingChart CTA). Fast Pest 2777 → **2784 / 0** (+7); design group 255 → 256. Builds: `npm run build` (gitignored `public/build`, CI rebuilds). Unrelated thumbnail churn + `.claude/settings.json` excluded from the commit. **341 (Widget Schema Sync in the Deploy Path) prompt pair already drafted** (owner-written). **Branch + version:** `session-340/1`, not pushed. VERSION 0.339.01 → **0.340.01**. **Next:** 341 (Widget Schema Sync), which closes the deploy-path gap that gates the PricingChart fix above on real servers. |

| 341 | Widget Schema Sync in the Deploy Path — **non-roadmapped deploy-infra fix**; the ⭐ owner-designated `[0.324.01]` inbox item (the deploy-pipeline half of the pair whose chrome-render half shipped at 340). Non-boundary, **v2.3.0**, **no schema**. **Root cause (re-verified at open):** the page-builder control list *and* the public render both read a widget's fields from the `widget_types.config_schema` DB column (`WidgetType::forPicker`, `WidgetResource`, `WidgetRenderer`), not the live `WidgetDefinition` at request time; that column is only (re)written by `WidgetRegistry::sync()`, whose sole caller was `WidgetTypeSeeder::run()` — and every deploy path runs `migrate --force` + `build:public` but **never** the seeder, so a pure-schema widget change (no migration) ships in the image yet stays invisible on installed servers (first bites: s322 Nav `orientation` control; 340's PricingChart CTA inspector). **Fix:** added `app/Console/Commands/WidgetsSyncCommand.php` — `widgets:sync`, wraps `app(WidgetRegistry::class)->sync()` (idempotent `updateOrCreate` by handle), prints a synced-count; mirrors the `widgets:lint`/`widgets:manifest-json` commands, auto-discovered (no Kernel). Wired into **every** deploy path after `migrate --force`, before `build:public`: `deploy.yml`, `deploy-demo.yml` (SHA-tagged/VERSION-exempt but same gap). **Docs:** `digital-ocean-droplet-install.md` (step-10 pipeline list + step-11 seeder-vs-sync reframe + manual-unstick), `app-reference.md` (deploy-step narration). **Cross-Repo note (non-contract):** FM's node-upgrade routine must run `widgets:sync` wherever it runs `migrate --force` outside `deploy.yml`, or the bug reproduces on FM-driven upgrades — flagged in the outlines Cross-Repo block; FM-side incorporation is a separate FM-repo task. **Manual unstick for an already-stale node:** `docker exec nonprofitcrm_app php artisan widgets:sync`. **Test:** `WidgetsSyncCommandTest` (new, +2) — corrupts a `config_schema` row → runs the command → asserts it matches the live `NavDefinition` again; + idempotency. Fast Pest 2784 → **2786 / 0**. No front-end build (console command + workflow YAML + docs). Deploy-workflow edits are CI-affecting but only provable by a real deploy run (deferred to owner). **342 (Housekeeping — Infra Hygiene + Page-Builder Polish) prompt pair drafted** (Duplicate-action scope settled to all four resources per owner). **Branch + version:** `session-341/1`, not pushed. VERSION 0.340.01 → **0.341.01**. **Next:** 342 (housekeeping batch) or owner's pick guided by non-roadmap pressure (designer feedback / external input). |
