# Base Prompt — Session 227

We are about to begin a new session: **227. Widget Primitive — Phase 5a (Typed Ambient Context Primitive)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session does not add or remove any columns; no schema docs need updating.
3. Read the framing for this session — the **Widget Primitive Phase 5** entry in `sessions/widget-primitive-migration.md` (lines 67–69) and the matching Phase 5 line in `sessions/session-outlines.md`'s Widget Primitive Track. This session is the **first slice** of the 4–6-session Phase 5 arc — the typed-abstraction refactor that lays the primitive that 5b–5c–5d build on. Phase 5 runs to completion before any other architectural beat (Fleet Manager Agent in particular waits on Phase 5 close).
4. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/archived/211. Widget Primitive — Phase 2 (Slot Taxonomy) — Log.md` — established `Slot` abstract + `SlotRegistry` singleton + the three concrete slot declarations (`PageBuilderCanvasSlot` live, `DashboardGridSlot` + `RecordDetailSidebarSlot` declaration-only). The "intentionally not declared on the abstract" comment on `Slot::ambientContext` (Slot.php docstring lines 8–16) is what this session works around — it stays "not declared" because the per-slot **arg** shapes still differ; what changes is the **return** type.
   - `sessions/archived/212. Widget Primitive — Phase 3a (Dashboard Grid — Slot Plumbing & Hosted Render) — Log.md` — established `WidgetRenderer::render`'s `$slotHandle` parameter and the introduction of `SlotContext` as the typed container. Read for the dispatch shape: `page_builder_canvas` calls `ambientContext($pageContext, $tokenPage)`; everything else calls `ambientContext()`. This dispatch survives this session unchanged — only the returned shape becomes typed.
   - `sessions/archived/214. Widget Primitive — Phase 3c (Dashboard-Native Widgets) — Log.md` — added the `publicSurface: bool` flag to `SlotContext`. This flag is the load-bearing security gate for SOURCE_WIDGET_CONTENT_TYPE collections: public surfaces filter `is_public = true`, admin surfaces (dashboard_grid, eventually record_detail_sidebar) pass `false`. Phase 5a preserves this gate byte-equivalent — `publicSurface` stays on `SlotContext`, not on the new typed ambient (different concern).
   - `sessions/226. Date Display — Format Hints + Projector-Owned Assembly — Log.md` — most recent. The `formatHints` field on `DataContract` plus the projector-owned date assembly closed the date-handling loop and locked in the "templates are dumb token renderers" rule. No direct dependency for 227, but read for the current state of the contract pattern.
5. Read the full state of every file this session edits or touches. Specifically:
   - `app/WidgetPrimitive/Slot.php` — abstract base. The `ambientContext` docstring updates this session: "the per-slot arg shapes differ but every implementation now returns `SlotContext`."
   - `app/WidgetPrimitive/SlotContext.php` — refactored. `pageContext + currentPageOverride` constructor pair is replaced by a single `AmbientContext $ambient` field. The `currentPage(): ?Page` helper survives as a delegate that asks the ambient. `publicSurface` stays on `SlotContext`.
   - `app/WidgetPrimitive/Slots/PageBuilderCanvasSlot.php` — `ambientContext(PageContext, ?Page): SlotContext` body updates to construct `new PageAmbientContext($currentPage)` and pass it to `SlotContext`.
   - `app/WidgetPrimitive/Slots/DashboardGridSlot.php` — `ambientContext(): SlotContext` body updates to construct `new DashboardAmbientContext()` (placeholder, no payload this session) and pass it to `SlotContext` with `publicSurface: false` preserved.
   - `app/WidgetPrimitive/Slots/RecordDetailSidebarSlot.php` — return type tightens from `object` to `SlotContext`. Body still throws the same `RuntimeException` — wiring is 5b. No accidental render path is created this session.
   - `app/WidgetPrimitive/ContractResolver.php` — every internal `$context->currentPage()` call survives byte-equivalent (the helper delegates to the ambient). `publicSurface` gating preserved.
   - `app/Services/WidgetRenderer.php` — the slot-handle dispatch at lines 73–76 stays. SlotContext construction site updates to the new ambient-typed constructor.
   - `app/Services/PageContext.php` — **survives unchanged this session.** Templates continue to receive `$pageContext` as the container singleton; the typed ambient is a slot/resolver-layer abstraction only. Template-side migration is deferred.
6. Read every file this session **creates**:
   - `app/WidgetPrimitive/AmbientContext.php` — new abstract sealed-by-convention class. Empty body; subtypes carry the payload. Sibling to `Slot` / `DataContract` / `QuerySettings` in the namespace organization.
   - `app/WidgetPrimitive/AmbientContexts/PageAmbientContext.php` — final class extending `AmbientContext`, carries `public readonly ?Page $currentPage`.
   - `app/WidgetPrimitive/AmbientContexts/DashboardAmbientContext.php` — final class extending `AmbientContext`, no payload this session (placeholder).
   - `app/WidgetPrimitive/AmbientContexts/RecordDetailAmbientContext.php` — final class extending `AmbientContext`, no payload this session (5b plugs the body).
7. Read the relevant existing tests:
   - `tests/Unit/WidgetPrimitive/Slots/PageBuilderCanvasSlotTest.php` — return-type assertion updates. Existing `currentPage()` behaviour preserved.
   - `tests/Unit/WidgetPrimitive/Slots/DashboardGridSlotTest.php` — return-type assertion updates; `publicSurface: false` invariant preserved.
   - `tests/Unit/WidgetPrimitive/Slots/RecordDetailSidebarSlotTest.php` — the existing "throws RuntimeException" assertion stays exactly as-is. The return type tighten doesn't change runtime behaviour.
   - `tests/Unit/WidgetPrimitive/SlotTest.php`, `SlotRegistryTest.php` — verify by grep whether any test references the legacy `pageContext + currentPageOverride` constructor shape. Update assertions where they live.
   - `tests/Unit/WidgetPrimitive/ContractResolverTest.php` — same. The `currentPage()` helper survives, so most assertions stay byte-equivalent.
   - `tests/Feature/EventDescriptionContractRetrofitTest.php`, `EventRegistrationContractRetrofitTest.php`, `BlogPagerContractRetrofitTest.php`, `ProductDisplayContractRetrofitTest.php` — these construct `new SlotContext(new PageContext(null), $host)` directly to inject a "current page" into a single-row contract. The constructor change ripples here. Each call site updates to `new SlotContext(new PageAmbientContext($host))`.
8. Open `sessions/227. Widget Primitive — Phase 5a (Typed Ambient Context Primitive).md` and read the session prompt carefully.
9. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first. **This session writes no schema migrations.**
- **Update `docs/schema/`** after writing any migration. Not applicable this session.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session touches PHP only — no widget asset files, no SCSS, no TS.** No build expected.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands the new `AmbientContext` abstraction and three subtypes; expect new unit tests for each subtype's construction shape, plus updates to the slot-and-resolver tests where the SlotContext constructor signature ripples through.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/228. base-prompt.md`. Expected next session: **Phase 5b — Record-Detail Sidebar Slot: Filament Mount Point + Ambient-Context Wiring**. The session opens the slot's `ambientContext()` body (currently throwing), defines a Filament hosting mechanism (the migration doc's named risk: "low probability the contract layer cannot coexist with Filament's Livewire model"), and lands the first concrete consumer at the lightest level (probably a placeholder widget that just confirms the ambient flows). Concrete starter widgets and per-record-type config storage land in 5c+.
- **Next session prompt**: draft `sessions/228. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/227. Widget Primitive — Phase 5a (Typed Ambient Context Primitive) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: mark Phase 5a complete. Confirm Phase 5b follows.
- **Archive the previous session**: move all files matching `sessions/226. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-227/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
