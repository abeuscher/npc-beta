# 344. Code Review & Cleanup (Cycle 3 — Audit)

The audit session of Code Review & Cleanup Cycle 3 — one analysis pass over ~70 sessions of accumulated drift (since Cycle 2 closed at 274) that produces the findings document the apply + squash sessions execute against. No code changes; analysis only.

---

## Stub reference

Not a `release-plan.md` entry. Driven by the **Forward plan — Cycle 3 shape** in `sessions/tracks/code-review-and-cleanup.md` — canonical for the cycle shape, cadence, standing improvements, and the won't-fixes already reaffirmed. Cycle 2's full workstream taxonomy (W1–W12) and the two prior retrospectives live in the same doc. The three formal release-plan rows for the cycle (audit/apply/squash) are added when the cycle is queued — see the track-vs-release-plan split in the track doc.

**Cadence.** Cycle 2 closed at 274; the ~50-session trigger lands near 324; at 344 the cycle is past due — lifted now by cadence (Forcing-function rule: also satisfied by the long deferral). This is the **recurring mid-cycle cleanup**, distinct from the **terminal pre-Beta-1 T1 review + squash**, which still runs later per Rule 10.

---

## Session-specific deltas

- **Compressed shape.** Cycle 3 runs audit → apply → squash in **3 sessions** (Cycle 2 was 4). This session folds Cycle 2's audit-pair (271 horizontal sweeps + 272 subsystem deep walks) into one pass; the sweep findings feed the deep walks within the same context window.
- **Three Cycle-3 standing additions** the prior cycles did not run, lifted from Cycle 2's blind-spot list and design discussions:
  - **Static-reflection scan** — compare resource subclass method signatures against the Filament v3 base class, to catch inert dead code that compiles cleanly (the `getRelationManagers` → `getRelations` class of bug that 275 surfaced). Scripted + reusable in future cycles.
  - **Boolean-settings / config-shape consistency sweep** — every boolean-shaped tenant setting + per-model boolean field, read and write, for storage-shape drift (`'1'`/`'0'` vs `'true'`/`'false'` vs native `bool` cast vs `filter_var`). Codify the convention; surface every diverging call site.
  - **Integration-seam sweep** — multiple write paths into the same data + implicit defaults that disagree across paths (the session-308 typography pattern: two writers into `SiteSetting::typography`, importer silently won).
- **Drift to reconcile.** The track-doc-referenced `sessions/code-review-large-files-partial-extraction.md` (the W11 large-file advisory) is missing at that path; regenerate the file-length grep counts fresh and fix the dangling reference.

---

## Phases

Analysis-only; one session, three passes. The output is the findings log, not commits.

### Phase 1 — Horizontal sweeps
- **Dead code + orphaned files** (PHP / Blade / Vue-TS / SCSS / tests) and **dead imports** — the broad grep + sweep (W5). Note: greps miss inert dead code that compiles and breaks no tests — that gap is Phase 3's static-reflection scan.
- **Naming + outdated-comment + framework-convention drift.**
- **Permission-gate uniformity re-verify** (W6) — Cycle 2 took `canAccess()` to 21/21 Resources; confirm no regression and that new Resources since 274 conform.
- **File-length quantitative** (W11) — per-language LOC averages (split PHP/Blade/Vue/TS/SCSS; Blade skews long) + the >2×-average outlier list, applying the **auto-skip-with-note** rule for two-cycle-old outliers in no active work.
- **Inline-code-in-markup grep** (W12) — inline `<script>` / `<style>` in Blade beyond a trivial threshold, multi-line CSS/HTML strings in PHP/TS, inline JS event handlers with non-trivial logic, widget-template inline assets that should live in asset declarations. Output: an extraction-candidate table.

### Phase 2 — Subsystem deep walks
Per-subsystem passes (W2 / W3 / W4): CRM models + observers, the importer subsystem, the **page-builder Vue subsystem** (W4b — `editor.ts` store growth, composables-vs-utils convention, the appearance-panel polymorphism), and **rich-text** (W4c — re-check the post-275 sanitization surface). Produces the bulk of the **duplication (W7)** + **framework-alignment (W8)** table rows and the **Open Flags** block. Cross-reference W11 for size-driven extraction candidates.

### Phase 3 — Cycle-3 standing additions
The static-reflection scan, the boolean-settings consistency sweep, and the integration-seam sweep (all described under Session-specific deltas). Each surfaces apply picks; the convention-drift gate test and the seam pre-flight checks are **apply** deliverables, scoped here. The static-reflection scan may land as a reusable script artifact (read-only) in this session.

---

## Out of scope

- **Applying any fix.** All actionable picks go into the findings log for the apply session (345). The audit ships no behaviour change.
- **Migration squash.** The squash is its own session (346) — analysis here may note migration-history size, but the collapse is deferred to keep the transactional risk profile separate.
- **Boundary edits.** Fleet Manager contract findings are flagged into Open Flags, not fixed; no `CONTRACT_VERSION` change; CRM stays v2.3.0.
- **Relitigating settled won't-fixes** (the three large importer PHP files, the Filament-idiomatic importer action closures, the chunked-tick → queue migration, the Contacts-importer namespaced-pattern divergence) — note as standing, don't re-argue.

---

## Testing

- **Slow test groups to run this session:** none.
- **New tests expected:** no — analysis-only, no behaviour change. The convention-drift gate test ships in the apply session; the static-reflection scan may land as a reusable script artifact (not a behaviour test).
- **Front-end build:** none — no front-end source touched.
- **Baseline:** fast Pest 2799 / 0 at the 343 close; expected unchanged at this session's close.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file:** `sessions/344. Code Review & Cleanup (Cycle 3 — Audit) — Log.md` — **this log IS the deliverable** (merged W7/W8 tables, Open Flags block, W11 quantitative output, W12 candidate table, carve-out decisions).
- **Branch:** `session-344/N` (final iteration).
- **Track doc:** update `sessions/tracks/code-review-and-cleanup.md` status snapshot (Cycle 3 audit done) and add the three Cycle-3 release-plan rows.
- **Next:** draft the apply session (345) prompt + base prompt from the findings.
- **Archive:** `sessions/343. *` → `sessions/archived/`.
