# Base Prompt — Session 363 (Housekeeping — Mailing-List Filter Repair + Inbox Batch)

**Start now.** Execute the reading list below, open the session prompt at `sessions/363. Housekeeping — Mailing-List Filter Repair + Inbox Batch.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **363. Housekeeping — Mailing-List Filter Repair + Inbox Batch**.

This is **not a `release-plan.md` entry** — a housekeeping batch session per the standing inbox discipline (`sessions/housekeeping-inbox.md` header: batch when 5–10 items accumulate; the inbox is well past that). It slots into the gap opened at the 362 close: the guided-tour track is **paused awaiting external demo feedback** (deployed at `0.362.01`), and the owner called housekeeping as the next move rather than starting the next numbered Beta-1 entry (A2 Fleet Manager node-operations parity, which needs FM-repo coordination). Note the **Test Audit track's cadence trigger is ≈ session 365** — two sessions out; do not absorb `[test-integrity]` inbox items here that the audit would handle better.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at **v2.5.0** (set by session 360; unchanged through 362). No Cross-Repo block or `docs/fleet-manager-agent-contract.md` update.

**Session shape:** **housekeeping batch** — one anchor bug fix (the mailing-list simple-filter NULL-value defect, reproduced and diagnosed at 362) plus an Open-Gate walk of the inbox to pick 4–7 further small items. Rule 2 applies in full: this shape **drains the inbox**; every item taken leaves via the disposition rules.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference.
2. Read `sessions/session-outlines.md` — the Active-tracks block (demo-site arc paused; guided-tour work paused awaiting feedback; Test Audit trigger ≈ 365) — then read **`sessions/housekeeping-inbox.md` in full**: this session's scope IS the inbox. Re-verify every candidate against current code at the walk (the staleness guard) before committing it to an iteration.
3. **No `release-plan.md` entry.** Read the **session 362 log** (`sessions/362. Guided Tour Redesign — Three Focused Single-Area Tours (Dashboard, CRM, CMS) — Log.md`) — it carries the mailing-list-filter diagnosis (the anchor bug: three same-named `value` components in the filter repeater clobber the chosen value to NULL at save; reproduced end-to-end on the e2e stack; likely inert since session 041), the tour redesign's standing Playwright specs (do not regress them), and the deploy state (`0.362.01` live on the demo node).
4. Read `docs/app-reference.md` for the front-end build command (`npm run build`, WSL-path npm — agent memory `reference-frontend-build-command`), the e2e Playwright runner (`npm run test:e2e`, isolated stack, `docker cp` the build into its `public/build` volume — agent memory `reference-e2e-playwright-runner`), and the test runner. Read `docs/schema/` table files only as picks demand.
5. **Read the anchor-bug surfaces before proposing the fix:** `app/Filament/Resources/MailingListResource.php` (the Simple Filters repeater — the three same-named `value` components), `app/Services/MailingListQueryBuilder.php` (NULL value → `WHERE field IS NULL`), `app/Services/MailingListFieldRegistry.php`, `app/Models/MailingListFilter.php`. The 362 log records the repro recipe (drive the real create form; opt-in equals Yes → DB row `value: null`, matches 0).
6. **Pest and Playwright run sequentially, never in parallel** (agent memory `feedback-test-runs-not-parallel`).

---

## Starting state inherited from session 362

- **CRM contract at v2.5.0** — unchanged; 362 was non-boundary (front-end tour redesign). 363 is non-boundary.
- **Schema baseline:** no migration since 359's `users` 2FA columns. 363 expects no schema change unless an inbox pick demands one (flag it at the walk if so).
- **Fast Pest green at 2972 / 0** (362 close; −5 from the 360 baseline = the deleted `TourShowcasePagesTest`). New Pest expected this session: a mailing-list filter round-trip case at minimum, plus whatever the picks warrant.
- **Playwright suite:** 362 replaced `tour/product-tour.spec.ts` with three standing per-tour specs (`dashboard-tour` / `crm-tour` / `cms-tour`, 6 tests) + an `@on-demand` storyboard regenerator (`tour/storyboard.spec.ts`). All green vs the isolated stack at close. Do not regress them; local Playwright runs this session only if a pick's verification demands it.
- **Deploy state:** `0.362.01` merged and live on the demo node for external tour feedback. **Feedback lands mid-flight → it interrupts:** tour copy rulings (the storyboard's flagged typos are still unapplied — copy is owner-script verbatim) and any feedback-driven tour rework take precedence over remaining batch picks when the owner says so.
- **Housekeeping inbox:** the mailing-list filter item was folded at the 362 close (capture stamp `[0.362.01 · 2026-07-07]`) with the full diagnosis. The inbox also carries ~14 further open items; the Open-Gate walk picks this session's batch.
- **Planning state:** guided-tour track paused awaiting feedback; demo-site arc paused; Test Audit cadence trigger ≈ session 365.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The Open-Gate inbox walk is the scoping act.** Bring a verified candidate list (each item premise-checked against current code) with a lean per item: take / defer / drop / promote. The owner confirms the batch; then build. Don't absorb items mid-session that the walk didn't take.
- **Anchor first.** The mailing-list filter fix lands as the first iteration regardless of the rest of the batch — it is diagnosed, reproduced, and blocks a live demo-facing feature (the CRM tour points prospects at Mailing Lists).
- **One dehydrating component per state path** is the fix shape lean for the anchor bug (or split state paths mapped onto the `value` column) — confirm at the gate, then pin it with a round-trip test (form save → DB value → query match → form refill).
- **Verify objective outcomes yourself** (agent memory `feedback-verify-objective-outcomes`): the 362 repro recipe is the template — drive the real form, read the real DB row, count the real matches.
- **Front-end build before announcing completion** if any pick touches Vite-compiled source (`npm run build`, WSL-path npm).
- **If the owner signals a deploy-to-test,** bump `VERSION` to `0.363.<iteration>` proactively (agent memory `feedback-deploy-version-bump`).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the anchor fix shape for the mailing-list filter (one dehydrating component per state path vs split state paths); (2) the **verified inbox candidate list** with a take/defer/drop/promote lean per item and a proposed batch of 4–7 (size to what fits the session with the anchor fix first); (3) which `[test-integrity]`-flavoured items to **leave for the ≈365 Test Audit** rather than absorb here; (4) whether any pick is big enough to promote instead of batch (the staleness-guard walk sometimes reveals this). One confirmation, then proceed.
