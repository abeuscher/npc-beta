# Base Prompt — Session 299 (Test Suite Parallelization + Isolation — incl. Isolated e2e Stack for Playwright)

> **Reshuffled into the numbered sequence at session-297/2: this is session 299.** Emergent, iteration-cost forcing function. **Session 2 of the test-cost split — scope expanded by user decision at session-298/1: the Playwright isolated-stack work is folded in.** **Session 1 (`298`) landed first** — it provides the hardened CI + scoped loop — but this session is independent in code. This is the explicitly-sanctioned early-lift of § D4's parallelization slice + the D4-anticipated test-isolation cleanup (Rule 11); D4's mutation/coverage-shape stays release-gated. **May split per Rule 11** if the combined surface is too wide for one clean session.

**Start now.** Execute the reading list, open `sessions/299. Test Suite Parallelization & Isolation Cleanup.md`, then begin. No confirmation needed.

---

We are about to begin a new session: **299. Test Suite Parallelization & Isolation — incl. Isolated e2e Stack (Playwright Parallel + CI)**.

No execution-order position in `sessions/release-plan.md` — emergent. It lifts the `php artisan test --parallel` slice of § D4 + the test-isolation cleanup that is its prerequisite, **and** (folded in by user decision) the Playwright isolated-stack work — give e2e its own Docker instance + throwaway DB so it runs collision-free locally and in CI. It does **not** take D4's mutation/coverage-shape work.

This session is **NOT boundary-touching** — no Fleet surface. **CRM contract stays at v2.3.0.** No DB schema change. **Pure speed, zero coverage loss** — parallelization runs the same tests faster; isolation fixes make them order-independent, not fewer; the isolated stack moves *where* Playwright runs, not *what* it asserts.

**Session shape:** infrastructure — the dig-out from testing having become the majority of dev-loop time (full corpus ≈20–30 min today). Two coupled goals: (1) `php artisan test --parallel` (Laravel paratest) green and the default fast invocation by fixing the test-isolation defects (shared `storage/app/private/` paths, the non-isolated `seedWidgetCollections` flake, other cross-test state); (2) a second isolated Docker stack so Playwright runs parallel-safe and enters CI. After (1), Pest drops to ~3 min and Playwright (~7–10 min) is the long pole — (2) is the critical-path fix, not creep.

Before doing anything else:

1. Re-read the session templates only if uncertain — stable since 276.
2. Read `sessions/session-outlines.md` Active-tracks + the "Test Suite Audit — Cost, Coverage, and Shape" stub, and `sessions/release-plan.md` § D4 — for the scope boundary (this is the `--parallel`+isolation slice **only**; mutation/coverage-shape stays in D4).
3. Not part of an active track.
4. Read `docs/app-reference.md` (containers/env, test invocation) and `docs/schema/README.md` (no schema).
5. **Read Session 1's log** (`298 …`) if it ran — the CI workflow it added is where the parallelized suite should become the default; adapt to what it actually shipped. If Session 1 has not run, note it and proceed standalone (parallelization still stands; wire it into whatever CI exists or leave a clear hook).
6. Read the leverage surfaces:
   - `phpunit.xml`, `composer.json`, the `dev` wrapper — where `--parallel` / paratest gets installed and wired as the default fast invocation.
   - Tests touching the filesystem: anything writing under `storage/app/private/`, `public/build/**` (the stale-stylesheet drift tests `BuildServerSettingsSession125Test` / `WidgetJsLibsSession138Test` — known real-artifact writers, a parallel-safety hazard *and* the subject of the separate drift-guard session; coordinate, don't collide), temp paths, or shared fixtures.
   - The `seedWidgetCollections` path and any seeder/fixture shared across tests — the release-plan-named flake.
   - Laravel parallel testing docs behaviour: per-process test databases are handled by the framework; the residual risk is **shared non-DB state** (filesystem, static caches, time, env) — that is the actual work.
   - The e2e harness coupling: `tests/e2e/helpers/db.ts` (`resetDatabase()` shells `docker compose exec -T app …`; `withClient()` pg `localhost:5432`), `tests/e2e/helpers/auth.ts`, `playwright.config.ts` (`APP_URL` default, `workers:1`/`fullyParallel:false`), and the dev `docker-compose.yml` (hardcoded `container_name`s + host ports) — these are the three targets to parameterise + the topology to duplicate. Read memory `project_playwright_ci_cost`.
7. Open `sessions/299. Test Suite Parallelization & Isolation Cleanup.md` and read it.
8. Note drift in a brief work-log entry; proceed unless a decision is needed per the drift/decision-threshold rules.

---

## Starting state inherited from Session 1 (or the preceding session if run standalone)

*(Required. Confirm against the actual preceding log at start; adapt to drift.)*

- **CRM contract at v2.3.0** — unchanged; not boundary-touching. **VERSION:** 298 closed at `0.298.1`; this session bumps `0.299.x`.
- **Schema baseline:** no schema change.
- **Fast suite green at 2464 / 0** (session-298/1 baseline; was 2465 — one >5s test was reclassified into `->group('slow')`, see below). Full serial wall time measured **675s (~11.25 min), Pest-reported 656s**. After this session the *same* 2464 must pass under `--parallel` (coverage identical; only wall time and order-independence change).
- **CI (shipped by 298):** `.github/workflows/tests.yml` is hardened to **fast → slow sequential jobs + a single aggregate job named `Tests`** (`needs: [fast, slow]`); triggers unchanged (`push: ["**"]` + PR→`main`). This session makes the **fast job** run `--parallel`. **Branch protection on `main` requiring the `Tests` status check is now enabled (user, 2026-05-17)** — keep the aggregate job named exactly `Tests`; renaming it silently breaks the merge gate.
- **Scoped inner loop exists (298):** `->group('design')` across a reviewed 17-file cluster; `./dev test:design` (`= php artisan test --group=design`) and `./dev test` (`= --exclude-group=slow`); `tests/Feature/DesignGroupIntegrityTest.php` pins group membership to an explicit list. When wiring `--parallel` as the default, preserve `--group=design` and do not break the integrity guard.
- **Known isolation defects (release-plan-named):** shared `storage/app/private/` paths; the `seedWidgetCollections` flake. **Nuance from 298:** the heavy `RandomDataGeneratorServiceTest :: it seedWidgetCollections — runs each widget definition that declares a demoSeeder` test (10.5s) was moved to `->group('slow')` at test granularity — that is *speed classification, not an isolation fix*. The release-plan-named `seedWidgetCollections` isolation flake is still this session's to root-cause; because it now lives in the slow group it will **not** surface in a parallel *fast* run — exercise it under `--parallel --group=slow` too. Treat the named list as the starting set, not the whole; `--parallel` surfaces the rest.
- **Playwright-in-CI is an unresolved open design problem (298 did NOT add it).** The e2e harness is hard-coupled to dev Docker Compose (`tests/e2e/helpers/db.ts` `resetDatabase()` → `docker compose exec -T app …`; Playwright needs the served stack; no `webServer` in `playwright.config.ts`). 298 deliberately left the Playwright CI job out; see memory `project_playwright_ci_cost`. **User decided (session-298/1): fold it in.** The chosen direction is a **second isolated Docker stack** (separate compose project / throwaway test DB) so Playwright runs collision-free locally *and* in CI — the post-parallelization long-pole fix, explicitly accepted scope (not creep). The crux is parameterising three hardcoded targets (`resetDatabase()` exec target, `db.ts` pg `host:port`, `APP_URL`). The **one open sub-question** for session start is the stack *shape* — faithful full duplicate vs minimal separate-DB + `php artisan serve` (recommendation: faithful; see the session prompt's Open Q3). May split per Rule 11 if the combined surface is too wide — decide *with* the user.
- **Session 296 already shipped the build-pipeline isolation (verified on `main`).** `AssetBuildService` is now `__construct(?string $outputDir = null, ?string $libsDir = null)`; `BuildServerSettingsSession125Test` and `WidgetJsLibsSession138Test` already build into injected temp dirs under `try/finally` and no longer write real `public/build/**`; `tests/Feature/AssetBundleDriftGuardTest.php` exists and builds only into a temp dir with an unconditional `afterEach` FS-pollution assertion. So the largest known parallel-safety hazard is **already removed** — this session will find that work done. Do **not** re-solve it; verify it holds under `--parallel` and build on the existing injectable constructor as the pattern for any remaining shared-FS offenders.
- **Build/version discipline:** bump `VERSION` per-iteration for this session's commits; don't touch deploy/immutable-tag logic.
- **Housekeeping inbox** — focused infra session; do not absorb inbox items.

---

## Process rules for every session

Base-prompt rules apply. Session-specific:

- **Zero coverage loss is the invariant.** A test that fails only under `--parallel` is an *isolation defect to fix*, never a test to delete, skip, or move to a serial-only carve-out without explicit user sign-off. Fixing shared state is the job.
- **Root-cause isolation, don't paper over.** Prefer making a test create/own its own paths/fixtures over global serialization. A serial-only escape hatch for a genuinely unparallelizable test is allowed only with a one-line logged rationale and user awareness.
- **Don't collide with the drift-guard session.** The build-pipeline tests' real-artifact writes are owned there; coordinate the isolation approach rather than re-solving it differently here.
- **Verify objectively, yourself.** `--parallel` green with the identical test count, run repeatedly to prove order-independence (run it ≥2× / shuffled). Report before/after wall time.
- **Run the fast suite (parallel) + Playwright (sequential, never concurrent with Pest) when complete.** Green, count unchanged, every delta explained.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

This is terminal for the test-cost split. Draft a successor only if the user names one, **or** if Rule 11 forced the Playwright-isolation remainder out of this session (the residual isolation surface or the isolated-stack/Playwright surface being larger than one clean session) — that is a logged carry-forward note decided *with* the user, not an auto-draft.

### Phase 2 — Close

Log `sessions/299. Test Suite Parallelization & Isolation Cleanup — Log.md` per the template (paratest wiring; every isolation defect found + how fixed; any serial-only carve-out + rationale; before/after wall time; the identical pass count proof; `VERSION` bump; drift; coordination notes with the drift-guard session). Update `completed-sessions.md`, archive the previous session's files, commit on the final `session-299/N` branch, notify, do not push. Never push to main / force-push / merge to main.

---

## Style rules

Base-prompt style rules apply. Iteration commit messages describe the deliverable; PM-level status tone per `memory/feedback_pm_level_tone.md`; no docstrings/comments/type-annotations on code you did not write. **Extend, don't reinvent** — use Laravel's first-party parallel testing (paratest), fix isolation at the source, wire into the existing `dev`/CI. No new runner, no test deletions for speed, no coverage-shape work (that is D4).
