# Session 368 — Client Billing — Demo Conversion Cleanup Command — Log

**Date:** 2026-07-17
**Branch:** session-368/1

---

## What was built

No application code. The session's planned artifact — the demo-conversion cleanup command (release-plan entry CB3) — was **withdrawn at the Open Gate**, and the session became a planning correction plus an owner-driven launch replan setup.

### Phase 1 — CB3 withdrawal + the "demo" vocabulary correction

- At the gate, pre-work surfaced that deleting `demo@demo.local` would violate `ON DELETE RESTRICT` foreign keys (`pages.author_id`, `events.author_id` — both `NOT NULL`) the moment the demo account had authored a page; verified empirically against the running database (probe rows force-cleaned after).
- The owner then corrected the underlying model: **every node in the field is an ordinary production server**; "demo" names exactly one thing — the shared public sandbox (`APP_ENV=demo` → `isDemoMode()`, the only demo switch in the CRM). A prospect's trial node is *not* a demo node: no demo account, no locked pages, no demo-mode behaviors. The track doc's § Design decision 4 had conflated the two ("personalized demo" as a demo-profile node flipped to production at conversion), which is what CB3's premise rested on. **CB3 withdrawn — there was no work in it.**
- Corrections written: `sessions/tracks/client-billing-and-account.md` (§ Design decision 4 rewritten with a new **§ Vocabulary** table; conversion sequence 7 steps → 4 — nothing is pushed to the node but the billing-state document; the FM-side `is_demo` write-once relaxation dissolved; Appendix A § CB3 marked withdrawn; Appendix B § FM-B4 rescoped smaller with no CRM dependency), `sessions/release-plan.md` (CB3 entry + execution-order line 48c marked withdrawn with rationale), `sessions/session-outlines.md` (active-tracks block + a superseded-in-part note on the 366 Cross-Repo record warning FM-B4 off the old sequence), and one scope-note line in `docs/fleet-manager-agent-contract.md` (documentation only — no surface change, no version bump; FM fetches this file, so the stale "CB3 is the cleanup command" pointer would have misled the FM agent).
- **The CRM lane of the Client Billing track is complete at CB1 + CB2.** All remaining billing work is FM-side (FM-B1 → FM-B5).

### Phase 2 — Launch replan setup (owner-initiated meta-conversation)

- Owner context: funding not secured; bringing the product to market solo, no champion customer; donations + real donor PII from day one; public site + demo to be exposed to search and paid ad traffic; target end of July 2026, budget ~30–40 sessions; **security confidence promoted from post-Beta afterthought to launch-gating** (bar: self-driven internal review — a paid audit is unaffordable).
- Two evidence artifacts produced by parallel audit and persisted for session 369:
  - `sessions/368. roadmap-remaining-inventory.md` — all 20 remaining release-plan items (20–23 estimated sessions, all `release`-gated), the pre-release register (Privacy/ToS — Stripe requires both URLs), the deferred reservoir, and stale plan-doc markers owed a sweep.
  - `sessions/368. security-surface-map.md` — surface-by-surface map with a top-5 risk ranking: (1) fleet mTLS endpoints (`/api/backup/blob`, `/api/admin/recover`) have no app-layer second lock; (2) single-layer stored-XSS posture (save-time sanitizer, raw render, **no CSP**); (3) no security headers anywhere + config-dependent prod hardening; (4) the public anonymous write surface (defended, but the biggest attacker-reachable path to donor PII); (5) admin Quill loaded from a third-party CDN + `is_active`-only panel entry. Also a "genuinely well-built" list (Stripe hosted checkout + verified webhook, portal scoping, SVG sanitization, mandatory 2FA) so future passes don't re-audit solid ground.
- Owner decisions locked: three launch gates replace "Beta 1 complete" (1 — professional to paid traffic; 2 — safe for one real client's money + PII; 3 — owner believes it's secure); **store/products deferrable, events core (ticketing must be present)**; donations the must-work money path; session 369 is a full planning session with a hard mid-session ratification checkpoint before the release plan is rewritten.
- Session 369 prompts drafted (`369. base-prompt.md` + `369. Launch Replan — Three Gates, the Security Track, and the July Cut.md`).

---

## Security considerations

The session's whole second half was security-shaped: the attack-surface map above is the standing input to the session-369 security track. Nothing was fixed this session by design — findings were scheduled, not patched. One durable code fact recorded: row-deleting any `User` who has authored a page or event is blocked by `ON DELETE RESTRICT`; nothing in the system row-deletes users today (the demo server rebuilds by dropping tables), so this is a constraint on future work, not a live defect. **Standing priority change:** the pre-Beta "security findings are not gating" posture is superseded — real PII + donations from day one make security launch-gating.

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`. (No application change — documentation and planning files only.)

---

## Deferred decisions

- **The entire remaining-work re-sort** — deliberately deferred to session 369's ratification checkpoint; nothing was re-gated this session beyond CB3's withdrawal.
- **Demo `role` seeding on non-demo nodes** — owner-logged to the capture buffer; folded to the housekeeping inbox at this close with the premise corrected (the *account* is already `isDemoMode()`-gated in `DatabaseSeeder`; it's the *role* that `PermissionSeeder` creates unconditionally). Needs its own small pass.
- **Form spam / CAPTCHA inbox item** — untouched; now naturally lands inside the security track's public-surface work rather than a standalone sizing pass.
- **FM-B4 rescope** — recorded in the track doc + contract doc for the FM repo to absorb; no CRM follow-up.

---

## Test result

3045 passed, 0 failed. (Fast suite, parallel — identical to the 367 baseline; no application code changed. Run after the documentation edits; the contract-doc parity test and the demo-surface + convention-drift clusters were also run individually and passed.)
