# Base Prompt — Session 367 (Client Billing — Contract v2.6.0 + Node Suspension Gate)

**Start now.** Execute the reading list below, open the session prompt at `sessions/367. Client Billing — Contract v2.6.0 + Node Suspension Gate.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **367. Client Billing — Contract v2.6.0 + Node Suspension Gate**.

This is the **first build session of the Client Billing & Account track** (`sessions/tracks/client-billing-and-account.md`, authored at agentic session A012 and merged by the owner) — draft release-plan entry **CB1** in that doc's Appendix A. It has no `release-plan.md` entry yet: folding the CB1–CB3 draft entries into the plan is a close-time step of this session (owner confirms placement + the proposed `first-customer` gate label at the Open Gate). The owner slotted this as 367, directly after multi-node operational readiness (366 / release-plan position 47) and ahead of the backup-recovery drill (position 48) — sequencing is the owner's call; note it in the fold-in.

This session **IS boundary-touching** — it authors the CRM↔FM contract bump **v2.5.0 → v2.6.0 (additive)**: the billing-state document schema, the `SUSPENSION_STATE` env-key semantics, and a new informational `suspension` subcheck on `/api/health`. Everything a v2.5.0 consumer relies on is unchanged; a node with no pushed state behaves exactly as today (absent key = `none`, absent document = page/subcheck report "no billing state"). Full version-bump checklist applies: contract doc body + CHANGELOG + `Contract Version` field, Cross-Repo block in `sessions/session-outlines.md`, FM-side absorption noted as pending (it lands with FM-side session FM-B2 per the track doc's Appendix B).

**Session shape:** feature-style, boundary-touching. Ships the node half of billing enforcement and display — the suspension middleware, the billing-state reader, the health subcheck — plus the contract revision that defines all three. **No vendor-Stripe anything exists CRM-side after this session, by design** — the node enforces a flag and renders a document; Stripe stays FM-repo-only.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read **`sessions/tracks/client-billing-and-account.md` in full** — it is canonical for this session's design: § Design decision 3 (the suspension state machine and what each state means on the node), § Design decision 7 (the v2.6.0 contract sketch this session turns into contract text), § Design decision 2 (what the account page will later read — CB2 consumes what you ship), Appendix A § CB1 (the success criterion), and the Open questions (1 and 6 gate this session's copy and middleware scope).
3. Read `docs/fleet-manager-agent-contract.md` **in full** — the surface you are extending. Study the `data_hygiene` subcheck section closely: it is the precedent for an informational, never-red, excluded-from-worst-of subcheck, and the `suspension` subcheck mirrors its posture exactly. Also § Demo-node reset coordination for the house style of documenting non-HTTP (pushed-artifact) coordination in the contract doc.
4. Read the demo-mode enforcement grain the suspension gate deliberately mirrors: `app/helpers.php` (`isDemoMode()`), `app/Http/Middleware/EnsureTwoFactorAuthenticated.php` (the panel-middleware precedent), and `config/fleet.php` (where the new `suspension` config entry lives, beside the existing agent config).
5. Read `app/Http/Controllers/Api/Fleet/HealthController.php` (subcheck composition, `CONTRACT_VERSION` via the shared trait, worst-of derivation and its `data_hygiene` exclusion) and its test `tests/Feature/Api/Fleet/HealthEndpointTest.php` (the shape-pinning conventions your subcheck tests follow).
6. Read `app/Providers/Filament/AdminPanelProvider.php` (panel middleware stack — where the enforcement middleware registers) and skim `routes/admin-api.php` + siblings (the in-panel API groups the lock must also cover).
7. Read `docs/app-reference.md` §§ Environments + Backups (confirm `storage/app/private/fleet/` stays outside the backup include set — the billing-state document's home depends on it).
8. Read `sessions/session-outlines.md` top-of-file Cross-Repo block — you update it at close.
9. Note any drift between this prompt / the track doc and the code in a brief work-log entry; proceed per the drift rules.

---

## Starting state inherited from session 366

*(366 — multi-node operational readiness — is drafted and expected to run before this session. These bullets assume its expected outcome; refresh them at 366 close if reality differs.)*

- **CRM contract at v2.5.0** — 366 was non-boundary (ops/verification). **This session bumps to v2.6.0 (additive).**
- **Schema baseline:** no migration since 359's `users` 2FA columns. This session expects **no schema change** — suspension state is env-derived; billing state is a pushed file; the permission lands at CB2 and is a seeded row anyway.
- **Fast Pest baseline: 2991 / 0** (parallel, 365 close; 366 expected zero-to-small delta — take 366's log as ground truth). Expected delta this session: **new tests** across the middleware matrix, the reader service, and the subcheck.
- **Playwright suite:** not expected to run — the suspension notice is a server-rendered gate, Pest-verifiable; a standing spec is justified only if manual testing surfaces a browser-level gap.
- **Fleet state (from 366):** four production nodes inventoried and FM-monitored; the fleet at ≥ 0.365.01. Relevant here only as context — nothing in this session touches live nodes; the flag is inert everywhere until FM-side FM-B2 ships the push.
- **Planning state:** Client Billing & Account track doc merged (A012); its Appendix A entries are not yet in `release-plan.md` — this session folds them in at close. A012's open questions 1 and 6 need owner answers at the gate (working defaults exist).
- **Housekeeping:** feature shape — no inbox absorption. One parked A012 finding (`DashboardIntegrationStatusWidget` reads the never-set `services.stripe.key`) stays parked; it is donation-Stripe surface, out of scope here.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The track doc's locked decisions govern.** Design questions this session surfaces get resolved against `sessions/tracks/client-billing-and-account.md`; genuine conflicts go to the owner at the gate, not silently overridden.
- **Enforcement rides the env flag; display rides the pushed document.** This is the design line — keep it. The middleware must lock correctly with **no document present** (generic copy); the document only ever improves the copy. The document is display-only data: nothing in it may alter enforcement, routes, or config.
- **Additive by construction.** Absent `SUSPENSION_STATE` = `none`; absent document = "no billing state." Every existing install must behave identically before and after this ships. A test pins each default.
- **What stays up under `admin_locked` is a hard invariant:** public pages, donation/event/membership checkout (the client org's own Stripe), the member portal, backups, the scheduler, and all five FM contract endpoints. Under `site_off`: the FM `/api/*` endpoints stay up. Tests pin both lists.
- **No vendor-Stripe identifiers CRM-side.** No `services.stripe.*` reference, no Stripe SDK class, no vendor config key anywhere in the new code. (The mechanical convention-guard cases land at CB2; the discipline starts now.)
- **Boundary checklist at close** (standing rule, live this session): contract doc body + CHANGELOG + version field → `2.6.0`; `HealthController::CONTRACT_VERSION` bump via the shared trait; Cross-Repo block updated (last boundary-touching session: 367; FM absorption: pending at FM-B2).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation (per the CLAUDE.md reporting rules) and **wait for the owner to confirm before starting.** Bring: (1) confirmation of track-doc open questions 1 and 6 as they bind this session — the lock-screen copy's grace framing, and whether the member portal stays up under an admin lock (working default: yes); (2) the release-plan fold-in — CB1–CB3 entries into `release-plan.md` under the proposed `first-customer` gate label, placement after position 48, owner may adjust; (3) the naming leans (config key `fleet.suspension.state` ← `SUSPENSION_STATE`; reader service under `app/Services/Billing/`; one enforcement middleware) — cheap to confirm, expensive to rename later; (4) anything the 366 close changed that these drafts assume wrongly. One confirmation, then proceed.
