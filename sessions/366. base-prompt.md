# Base Prompt — Session 366 (Fleet Manager — A3 Multi-Node Operational Readiness)

**Start now.** Execute the reading list below, open the session prompt at `sessions/366. Multi-Node Operational Readiness — Four Production Nodes + Post-Deploy Restore Verification.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **366. Multi-Node Operational Readiness — Four Production Nodes + Post-Deploy Restore Verification**.

This is **execution-order position 47** in `sessions/release-plan.md` — **A3. Multi-node operational readiness**, the next release-gated Phase A entry now that A2 (node operations parity) closed CRM-side at session 365. Prerequisites are met: A2 substantially complete (365) and E1, the onboarding/install dashboard widget (closed at session 249 — its execution-order line 611 is missing its ✅; sweep that at close). The success criterion is operational, not code: **four nodes running on production** — marketing site, demo install, test/deploy instance, spare-for-first-customer — each documented (purpose + URL + where its access creds live), all four monitored by Fleet Manager. The test/deploy instance becomes the target environment for later rehearsal sessions (the backup-recovery drill at position 48 depends on it). *(Owner note: this draft assumes the ops session is the pick. If the demo-feedback interrupt fires, or the A012 billing-planning review lands first and re-orders things, this prompt swaps cheaply — nothing below is wasted except sequencing.)*

This session is **NOT boundary-touching** — no `/api/*` surface or schema changes expected; CRM contract stays at **v2.5.0**. The work is droplet provisioning through the FM UI (owner-driven), inventory documentation, and live verification of already-shipped surfaces. If provisioning surfaces a real CRM-side defect (the pattern from past ops sessions: one small integration-seam bug per live exercise), fix it as a bounded in-scope item per the drift rules; anything larger splits per Rule 11.

**Session shape:** ops/verification hybrid — the owner drives all live-node operations (provisioning, upgrades, restores) through the FM UI; the agent verifies CRM-side outcomes (health endpoints, versions, restored state), documents the inventory, and records results. **This session also carries the session-365 post-deploy verification rider:** the cross-node-restore fix shipped at 365 needs the fleet on version ≥ 0.365.01, a fresh backup, and one live cross-node restore — provisioning fresh nodes is the natural vehicle (the full sequence is in the FM-repo handoff `a2-runbook-handoff-from-crm.md`).

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. Note the template's reporting-rules additions (session 365): all human-facing output follows `CLAUDE.md` § Reporting to the human.
2. Read the **A3 entry** in `sessions/release-plan.md` (execution-order position 47; entry under "#### A3") — canonical for the success criterion (four nodes + inventory doc) — and the **A4 entry** right after it (position 48), because A3's test/deploy instance is A4's precondition; don't start A4.
3. Read `sessions/tracks/fleet-manager-agent.md` — status snapshot (current as of 365 close) and the A2 outstanding items this session absorbs: the post-deploy live verification sequence and the FM 020 backup-trigger sign-off.
4. Read the **FM-repo handoff** `/home/al/fleetmanager/sessions/a2-runbook-handoff-from-crm.md` — the post-deploy verification sequence (steps 1–6) and the read-only-role check whose outcome decides a runbook line. FM ground truth: `/home/al/fleetmanager/sessions/completed-sessions.md` (048 complete / 050 drafted).
5. Read `docs/app-reference.md` §§ Environments (node wipe procedure, deploy pipeline, demo node) + Backups (bucket setup per install, scheduler-runner gap — **provisioned nodes need the scheduler runner question answered before nightly backups actually fire; surface it at the gate**) + the mTLS cert-paths section.
6. Read `docs/fleet-manager-agent-contract.md` § Auth + § `/api/health` — the surfaces you'll verify per node.
7. Note any drift between this prompt and the code in a brief work-log entry; proceed per the drift rules.

---

## Starting state inherited from session 365

- **CRM contract at v2.5.0** — unchanged; 365 was non-boundary (backup dump-config fix + documentation revision in the contract doc, no bump). 366 expects no contract change.
- **Schema baseline:** no migration since 359's `users` 2FA columns. 366 expects **no schema change**.
- **Fast Pest baseline: 2988 → 2991 / 0** (parallel; +3 dump-option tests at 365). Expected delta this session: zero to a handful — ops session; new tests only if a live-exercise defect gets a CRM-side fix.
- **Playwright suite:** untouched by 365; not expected to run this session.
- **The 365 fix ships in `0.365.01`.** Cross-node restore portability (dump-side `--no-privileges --no-owner`) — merged by the owner post-365-close (verify: is `main` at ≥ 0.365.01? Is the deploy green?). **Backups dumped by older images stay non-portable** — every baseline (including the FM 042 demo baseline) must be re-recorded from a node on the fixed image before a cross-node restore is attempted.
- **Deferred from 365 into this session:** the live verification sequence (handoff doc steps 1–6) — deploy/upgrade nodes to ≥ 0.365.01 → fresh backup → cross-node restore (the FM 042 demo-baseline loop or a fresh-node restore) → the read-only-role post-restore check → record the FM 020 backup-trigger sign-off.
- **Parallel work in flight:** cloud session A012 (client billing & account — planning) may have landed or be pending owner review; it produces planning docs only, no code — no collision with this session. The owner may re-order 366 behind that review or the demo-feedback interrupt.
- **Planning state:** guided-tour track still paused on external feedback; Code Shape & Fit ≈ 379; Code Review & Cleanup ≈ 398; Test Audit Cycle 3 ≈ 414. Demo node last known at `0.362.01`.
- **Housekeeping:** capture buffer (`sessions/housekeeping-incoming.md`) is empty at 365 close. Ops shape doesn't absorb inbox items.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The owner drives all live-node operations.** Provisioning, upgrades, baseline pushes, restores — all through the FM UI, owner at the wheel. The agent's role: say what to exercise next, verify CRM-side outcomes yourself (health JSON, version strings, restored rows, dump contents), and record per-node / per-capability results in the work log. Nothing destructive without the owner; no production customer nodes exist — say so before any restore drill anyway.
- **FM repo access is read + sessions-dir-write only.** No FM application code.
- **Credentials never land in the inventory doc.** The doc records *where* creds live (FM vault, DO console), never values.
- **Mid-session deploys need a VERSION bump first** (standing rule). If a CRM-side fix must reach a node mid-session, bump VERSION before the owner merges.
- **Watch for the scheduler-runner gap** on freshly provisioned nodes: registered backup commands do not fire without a runner (known carry-forward). A3 is the natural place to either accept-and-document or finally close it — surface at the gate, owner decides scope.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a plain-English orientation (per the CLAUDE.md reporting rules) and **wait for the owner to confirm before starting.** Bring: (1) the current fleet picture — which of the four target nodes already exist vs need provisioning, and whether `main` is at ≥ 0.365.01 yet; (2) the proposed order of operations (provision/upgrade → fresh backup → cross-node restore verification → inventory doc) and what the owner needs ready (DO account access, FM running, ~time estimate); (3) the scheduler-runner question — document the gap or close it this session; (4) where the node inventory doc should live (lean: the FM repo's sessions dir alongside the other fleet docs, since it's fleet ops — but owner's call). One confirmation, then proceed.
