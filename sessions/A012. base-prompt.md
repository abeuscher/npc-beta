# Base Prompt — Session A012 (Client Billing & Account Track — Planning)

> **Scheduled as session A012 — agentic, planning-only.** Runs autonomously in a cloud session alongside regular session work; the `session-A###/N` branch prefix and the `0.912.x` VERSION band distinguish it from the user-driven `NNN/N` sequence. **This session produces planning documents only — no application code, no migrations, no contract changes.** The deliverable is a new track doc that designs the client-billing-and-account feature end to end and decomposes it into buildable sessions for owner review.

**Start now.** Execute the reading list below, open the session prompt at `sessions/A012. Client Billing & Account — Planning.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **A012. Client Billing & Account Track — Planning (FM-Owned Stripe Billing + Node Account Page).**

This is **execution-order position: emergent**, not tied to any `release-plan.md` entry. The forcing function: the product has no "My Account" surface for the organization it serves (plan, billing status, billing contact), no mechanism to convert a personalized demo instance into a paying client (or suspend it un-deleted at trial end), no operator visibility into who is delinquent, and no home for billing the owner's hourly project work. The owner wants a designed plan before any build. Architecture direction was settled with the owner in conversation on 2026-07-08 — the session prompt carries those decisions as **locked**; the session designs within them, it does not relitigate them.

This session is **NOT boundary-touching** — it changes no `/api/*` surface, no response schema, no contract doc. CRM contract stays at **v2.5.0**. The *plan it produces* will describe a future contract bump (billing state crossing the FM↔node boundary is contract surface), but authoring that bump belongs to the build sessions, not this one. **No DB schema change, no migration, no code.**

**Session shape:** planning/design (analysis-only, like the Code Shape & Fit review sessions) — read the relevant CRM surfaces, absorb the FM ground-truth context embedded in the session prompt, design the feature within the locked decisions, and write the track doc + session decomposition + open-questions list.

Before doing anything else:

1. **Read the full session prompt** at `sessions/A012. Client Billing & Account — Planning.md` — the locked decisions, the FM ground-truth context (the cloud environment cannot read the FM repo; the prompt embeds what you need), deliverables, phases, and the hard-no list. It is the load-bearing document.
2. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference for the log at close.
3. Read `sessions/tracks/fleet-manager-agent.md` **as the format precedent for the track doc you will write** (status snapshot / design decisions / forward plan / retrospectives shape), and `sessions/fleet-manager-planning-spec.md` as the precedent for a two-repo planning document (responsibility split, coordination protocol).
4. Read `docs/fleet-manager-agent-contract.md` in full — the existing FM↔node contract surface (v2.5.0), its version-negotiation discipline, and its security posture. The billing plan must extend this world, not invent a parallel one.
5. Read the **existing Stripe surface** (this is the client's donation Stripe, NOT the vendor billing Stripe — the plan must keep them strictly separate; map what exists so the separation is concrete): `config/services.php` (stripe block), `app/Support/StripeCheckout.php` (or wherever the shared checkout helper lives — locate via `grep -ri "stripe" app/ config/ --include="*.php" -l`), the donation/event checkout controllers, and the Stripe-related SiteSettings on the CMS settings page.
6. Read the **demo-mode machinery**: `app/helpers.php` (`isDemoMode()`), `app/Console/Commands/DemoResetCommand.php` + `DemoRestoreCommand.php`, and grep for `isDemoMode()` call sites — the suspension gate will follow this pattern's grain (env-derived mode, code-level hard gates).
7. Read the **permission/role model** far enough to place a "manage account" ability: `docs/runbooks/permission-matrix.md`, one representative Filament page gate (e.g. the CMS settings page's `manage_cms_settings`), and the roles seeder.
8. Read `sessions/release-plan.md` §§ "Multi-node operational readiness" + "Execution order" (post-milestone block) — the billing track's sessions must slot relative to Beta 1 and the first-customer work; your deliverable proposes the slotting, the owner decides it.
9. Read `docs/app-reference.md` for environment names and deploy topology. No `docs/schema/` reads needed — no schema change this session.
10. Note drift between this prompt and the code; adapt silently per the drift rules and note adaptations in the log.

---

## Starting state inherited from session 365 (in flight on the numeric track)

- **CRM contract at v2.5.0** — 365 is non-boundary (backup dump-option fix + docs); A012 is also non-boundary. No collision: 365 touches `config/database.php`, backup tests, planning docs, and the contract doc's backup sections; A012 writes a **new** track doc + its own log only. Do not edit `sessions/release-plan.md`, `sessions/session-outlines.md`, or `docs/fleet-manager-agent-contract.md` (365 has open edits in all three).
- **Schema baseline:** no migration since 359; A012 adds none.
- **Fast Pest baseline: 2991 / 0** (365 in-flight, parallel). **Expected delta: zero — docs-only session; do not run the suite.**
- **Playwright suite:** untouched, not run.
- **Cloud-session caveat:** `vendor/` and `node_modules/` clone empty. **No stack bring-up needed** — this session reads source and writes markdown; it never boots the app.
- **The FM repo is NOT available in the cloud environment.** All FM ground truth you need is embedded in the session prompt (§ "FM ground-truth context"). Do not guess beyond it; where FM detail is missing, write the question into the open-questions list instead of inventing an answer.
- **Build/version discipline:** bump `VERSION` to `0.912.<iteration>` per iteration (agentic band). Branch `session-A012/1`.

---

## Process rules for every session

Base-prompt process rules (`sessions/template-base-prompt.md`) apply in full. Session-specific:

- **Planning-only, hard boundary.** No application code, no migrations, no edits to the contract doc, no edits to `release-plan.md` / `session-outlines.md` / existing track docs. New files only: the track doc, the session log, and (optionally) draft per-session prompt stubs under `sessions/` if the decomposition warrants them.
- **Locked decisions are locked.** The session prompt's "Settled direction" section was agreed with the owner. Design *within* it. If analysis genuinely surfaces a conflict with one of them, do not silently override — write the conflict into the open-questions list with your recommendation and design both branches shallowly.
- **Human-facing output follows the reporting rules in `CLAUDE.md` (the "Reporting to the human" section) — and in this session that governs the primary deliverable itself, not just status updates.** The track doc, its open-questions list, and the session log are read cold by the owner: plain-English lead, internal IDs translated on first use (FM = Fleet Manager; release-plan item IDs said in words; contract versions named as the CRM↔FM contract version). The dense process register of this prompt is how you *operate* — it is not how you *report*. Name things in plain language ("the suspension grace period", not a letter-number tag); session decompositions get descriptive names.
- **Don't renarrate; compress.** Follow the planning-artifact conventions: the track doc carries decisions and forward plan, pointers elsewhere for history. No restating of FM session logs beyond what the design needs.
- **Two-Stripes separation is a design invariant.** Every place the plan touches payments, state explicitly which Stripe account is in play (vendor billing vs. the client org's donation Stripe) and how the code paths stay disjoint.
- **No tests to run** — docs-only. The close-gate test run is waived; state "docs-only, suite not run (baseline 2991/0)" in the log's Test result section.

---

## ── SESSION CLOSE (autonomous) ──────────────────────────────────────────────

When the deliverables are complete:

- Write the session log at `sessions/A012. Client Billing & Account — Planning — Log.md` per `sessions/template-session-log.md`.
- Bump `VERSION` to `0.912.01` (or the highest iteration landed).
- Commit everything on `session-A012/1` and push the branch. **Never push to main; never merge.**
- The owner reviews the track doc and open-questions list, answers the questions, and decides slotting; the build sessions are drafted after that review, not by this session.
