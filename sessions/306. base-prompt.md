# Base Prompt — Session 306 (Page Builder Inline Editing — Session C: Toolbar Rebuild to Design Contract)

> **Scheduled as session 306.** **Critical-path, Beta-1-blocking.** **Session C of the four-session inline-editing arc** (A=304 shipped / B=305 partial / C=306 / D=307). **Session B (`sessions/305. *`) is a hard prerequisite** — C cannot run until B has shipped (the design contract is saved at `docs/inline-formatting-toolbar-spec.md`; the foundation companion changes §6.2/§6.3/§6.4/§6.5 are landed; the spec-aligned lifecycle pieces in `useInlineEdit` + the store's active-editor handle stub are in place). Recorded in the roadmap: `sessions/release-plan.md` (the inline-editing arc at the retired § E8 slot, position 38) + the `session-outlines.md` "Page Builder Inline Editing" arc stub — both canonical for scope.

**Start now.** Execute the reading list below, open the session prompt at `sessions/306. Page Builder — Inline Formatting Toolbar Rebuild.md`, then begin the work. No confirmation needed.

---

We are about to begin a new session: **306. Page Builder — Inline Formatting Toolbar Rebuild.**

This is the centerpiece of the inline-editing arc — the on-page formatting toolbar that 305's first cut failed to deliver. The first cut was discarded as under-specified on the visual/behaviour layer with a real lifecycle bug (the bar binding to a torn-down Quill instance → corner-ghost). The user produced a complete visual + behaviour design contract at **`docs/inline-formatting-toolbar-spec.md`**. **That document is the implementation contract.** A build is conformant when every rule in spec §5 passes its testable form, every visual in §4 matches, every must-NOT in §M holds, and the one outstanding §6 companion change (ColorPicker arrow-key grid navigation, §6.1) is made. There are no open product questions — every visual and behaviour decision is settled by the spec.

This session is **NOT boundary-touching** — no Fleet Manager surface. **CRM contract stays at v2.3.0.** **No DB schema change.** If the work appears to need a Fleet/`/api/health`/schema touch, stop and surface — scope drifted.

**Session shape:** UX-heavy build-to-spec. One substantive deliverable: the toolbar, with its lifecycle support in `useInlineEdit` and the one remaining ColorPicker enhancement. There are no judgment-surface choices left — the design spec resolved them. The session pause point is end-of-session manual UX testing against the spec's `§4.15` mockup-state checklist, not mid-build look/feel review.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — stable since 276.
2. **Read `docs/inline-formatting-toolbar-spec.md` end to end. This is the contract.** Every rule referenced below is in that document.
3. Read `sessions/session-outlines.md` — the **"Page Builder Inline Editing" arc stub** (A=304 / B=305 partial / C=306 / D=307; canonical for arc shape). Note: 304 is archived; B's log is the authoritative record of what 306 inherits.
4. Read `sessions/release-plan.md` — the inline-editing arc at the **retired § E8** slot (position 38).
5. Not part of an active track.
6. **Read session B's log** (`sessions/305. Page Builder — Inline Formatting Toolbar & Builder↔Public Parity — Log.md`) first — authoritative record of what 306 inherits: which companion changes landed (sanitiser target/rel allow-list, `refreshPreview` suppression centralised, theme-font + page-URL bootstrap data, lucide-vue-next installed), which spec-aligned lifecycle pieces survived the first-cut cleanup (outside-pointerdown teardown in `useInlineEdit`; reconcile-skip on within-widget switch; store's active-editor handle stub), and what was stripped (the rejected first-cut toolbar component, App.vue mount, `.ql-font-*` SCSS, `inlineToolbarHoldingFocus` flag). Adapt to what 305 actually shipped, not what 305's prompt planned.
7. Read `docs/app-reference.md` — the single render path. Read `docs/schema/README.md`; no schema this session.
8. Read the leverage surfaces before touching them (the spec references each by name):
   - `resources/js/page-builder-vue/composables/useInlineEdit.ts` — current handle publisher (stub shape `{quill, widgetId, path}`); 306 extends it to the full spec `§A4` shape (`{quill, hostEl, widgetId, configPath, getRect}`) and adds the `§A14` MutationObserver fallback on the host element's removal.
   - `resources/js/page-builder-vue/stores/editor.ts` — `activeInlineEditor` ref + `setActiveInlineEditor` / `clearActiveInlineEditor`; extend the type accordingly.
   - `resources/js/page-builder-vue/components/PreviewRegion.vue` — anchors the active-editor binding; the spec's `§C14` requires hiding the widget's drag-grip + Edit affordance while a field inside it is being edited.
   - `resources/js/page-builder-vue/components/primitives/ColorPicker.vue` — already has the `panelOnly` prop the spec relies on; 306 adds the arrow-key grid navigation per spec `§6.1` / `§K18`.
   - `resources/js/page-builder-vue/components/fields/RichTextField.vue` — the Inspector Quill integration (API-usage reference for `quill.format`/`getFormat`/`editor-change`/`insertEmbed`); the new toolbar drives the inline Quill via the same API surface.
   - `resources/js/admin/heroicon-blot.js` + `resources/js/admin/heroicon-picker.js` — reused unchanged by the toolbar's Insert-icon button (spec `§4.12`).
   - `app/Support/HtmlSanitizer.php` — already permits `target` + `rel` per spec `§6.2` (305).
   - `resources/js/page-builder-vue/composables/useRefreshPreview.ts` — already centralises the `inlineActiveWidgetId` short-circuit per spec `§6.5` (305).
   - `app/Livewire/PageBuilder.php` — bootstrap payload already carries `theme_heading_family` / `theme_body_family` and per-page `url` per spec `§6.3` (305); the toolbar reads them through the store.
9. Note drift in a brief work-log entry; proceed unless something needs a decision per the drift/decision-threshold rules.

---

## Starting state inherited from session 305

*(305 was PARTIAL — read its log in full. Highlights below are load-bearing.)*

- **CRM contract at v2.3.0** — unchanged; 306 does not touch it.
- **Schema baseline:** none added; none added by 306.
- **The design contract (load-bearing):** `docs/inline-formatting-toolbar-spec.md` (820 lines). Every rule in `§5` is independently testable; every visual in `§4` is fully specified; the must-NOT rules in `§M` are non-negotiable. The reconciliation table in `§7` records reference-image conflicts that were resolved against the constraints — do not relitigate.
- **Companion changes landed (305):** HtmlSanitizer permits `target="_blank"`/`_self` + `rel` tokens `noopener`/`noreferrer` (strict allow-list); `refreshPreview()` centralises the active-widget suppression so spec `§A13` is literally true; bootstrap data carries resolved theme fonts + per-page URLs; `lucide-vue-next` installed (all 18 toolbar icons verified). The one remaining §6 companion change is **§6.1 — ColorPicker arrow-key grid navigation** (a small enhancement to the existing component); 306 includes it because it's part of the colour-popover surface.
- **Lifecycle pieces preserved from the first cut:** `useInlineEdit` already does outside-pointerdown teardown (matches spec `§B4`) and reconcile-skip on within-widget node-switch (matches `§B10`). The store's active-editor handle stub is `{quill, widgetId, path}` — 306 extends it to the full `§A4` shape and adds the `§A14` MutationObserver.
- **First-cut bits stripped (305 cleanup):** the rejected toolbar component, its App.vue mount, the speculative `.ql-font-*` SCSS (font-family is out per spec `§M8`), and the unused `inlineToolbarHoldingFocus` flag are gone. The next session opens to a tidy foundation.
- **Public-vs-builder render conflation split (305):** `WidgetRenderer::render()` now has an explicit `bool $inlineEditing = false` parameter — only `WidgetPreviewRenderer` passes true. The public site no longer carries dormant editing markers / empty editable wrappers; visible content unchanged. The upcoming parity check (session 307) is crisper as a result.
- **Out of scope explicitly:** font-family control (spec `§M8`); HTML source view, word count, reading-time estimate (spec `§M16`); touch-specific affordances (spec `§K19`, `§9` deferral); internal page-reference link storage as a token (spec `§9` deferral); subscript/superscript/indent/outdent (spec `§9` deferral); last-used-color split-button (spec `§9` deferral).
- **Pest is the gate; the e2e suite is PARKED.** Fast suite green at 305's close (2514 + 1 pre-existing parallel-order flake passing in isolation). The `playwright` CI job remains parked. **C cannot rely on a running e2e suite for verification** — write Playwright per the spec's testable rules but it will be quarantined/non-running until the parallel stabilisation effort lands; gate on Pest. `feedback_test_runs_not_parallel` applies.
- **Build/version discipline (load-bearing):** bump `VERSION` to `0.306.<iteration>` each iteration. Vue/SCSS → `npm run build`; `public/css/admin.css` hand-edited/served directly.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **The spec is the contract; the build is mechanical against it.** There are no open product questions; do not relitigate visual or behaviour decisions. If a real ambiguity surfaces, stop and surface to the user — the spec author can clarify. Do not invent a behaviour the spec doesn't specify.
- **`§A13` + `§A14` are the load-bearing safety pair.** 305 made `§A13` literally true (centralised suppression). 306 adds the `§A14` MutationObserver fallback on the active host element's removal. Together they make the corner-ghost bug (the bug that killed the 305 first-cut) structurally impossible. Do not skip `§A14` on the grounds that `§A13` already covers it; the spec explicitly requires both as belt-and-suspenders.
- **Selection preservation (`§D`) is non-negotiable.** Every toolbar control uses `mousedown.preventDefault()` so focus does not leave the editor. The link popover's URL field is the *only* element that legitimately takes focus on a pointer-driven action (`§D4a`); keyboard-driven entry to the toolbar (Alt+F10 per `§K9`) is a separate, intentional focus transfer (`§D4b`). Implement both correctly.
- **Single shared toolbar, app-level mount (`§A1`/`§A2`/`§C15`).** Exactly one toolbar in the DOM; created at page-builder mount; destroyed only at page-builder unmount. Mounted into a fixed-position layer at the app root, not into any preview HTML — it never participates in `v-html` swaps.
- **The toolbar holds NO direct reference to a Quill instance (`§A7`/`§M3`).** Every operation reads `handle.value.quill` at call time. This is the structural defence against late-teardown bugs.
- **Pest fast suite + Playwright run sequentially when complete.** Green, every count delta explained. `feedback_test_runs_not_parallel`.
- **Pause for manual UX testing at session end against the spec's `§4.15` mockup-state checklist** (the 10 named states). Announce, then stop and wait. Do not suggest closing.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

After implementation + manual UX testing, the natural next session is **307** (Inline-Editing Arc Close — builder↔public render-parity tests + gate widening). 306's log should explicitly confirm 307's preconditions are met (the toolbar binds to a stable active-editor handle; the public-render output is clean per the 305 conflation split).

### Phase 2 — Close

When the user says to close: write `sessions/306. Page Builder — Inline Formatting Toolbar Rebuild — Log.md` per `template-session-log.md` (rebuild summary: which spec rules implemented, which visual states verified against the §4.15 checklist; the §A4 handle refactor + §A14 MutationObserver; the §6.1 ColorPicker keyboard nav; new Pest/Playwright; `VERSION` bump; drift). Update `completed-sessions.md`, archive session 305's files (`sessions/305. *` → `sessions/archived/`), commit on the final `session-306/N` branch, notify, do not push. Never push to main / force-push / merge to main. Do not begin 307 until the user starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific: iteration commit messages describe the deliverable; PM-level status tone per `memory/feedback_pm_level_tone.md`; no docstrings/comments/type-annotations on code you did not write. **Build to the contract, do not improvise.** The toolbar is being built to the spec at `docs/inline-formatting-toolbar-spec.md`; if a visual or behaviour question isn't answered there, the answer is "ask the user," not "infer."
