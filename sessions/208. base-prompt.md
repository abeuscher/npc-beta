# Base Prompt — Session 208

We are about to begin a new session: **208. Migration Squash & Code Optimization**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index and the squash-note block at the top — the latter marks the prior baseline cutoff (session 181, 2026-04-13).
3. Read the squash-procedure precedents. These five sessions established and iterated the squash recipe — read them in order so the procedure's evolution is clear:
   - `sessions/archived/062. Codebase Audit & Migration Squash — Log.md` — first squash, set the pattern.
   - `sessions/archived/082. Codebase Audit & Migration Squash — Log.md` — second pass.
   - `sessions/archived/108. Beta-One Bug Fixes & Migration Squash — Log.md` — third pass.
   - `sessions/archived/142. Inline JS&CSS Extraction & Migration Squash — Log.md` — fourth pass, introduced the companion inline-JS extraction pattern.
   - `sessions/archived/181. Migration Squash & Inline JS&CSS Audit — Log.md` and its prompt (`sessions/archived/181. Migration Squash & Inline JS&CSS Audit.md`) — most recent pass, and the structural model for this session's prompt.
4. Read the predecessor context — session 207's log and prompt:
   - `sessions/207. Column Layout Inspector — Appearance Unification — Log.md` (or the same path without `— Log.md` for the prompt) for the `page_layouts.appearance_config` migration that the squash baseline must absorb, plus the `api.ts` client-factory refactor and the two-Vue-apps-on-one-page diagnosis that frame the "pre-beta license to cut" posture.
5. Read the current migration-and-schema landscape:
   - `database/schema/pgsql-schema.sql` — existing baseline (session-181 state plus anything merged since).
   - Run `ls -la database/migrations/` — the files here are the candidates for absorption into the new baseline.
6. Open `sessions/208. Migration Squash & Code Optimization.md` and read the session prompt carefully. Pay particular attention to the **pre-beta license** note in the design-decisions block — 208's cleanup posture is intentionally more aggressive than a typical production-grade refactor, because no live installations exist yet and the wipe-and-reinstall path is safe.
7. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first.
- **Update `docs/schema/`** after writing any migration — add, modify, or remove the relevant table file(s) under `docs/schema/` to reflect the final column state. If adding a new table, create a new file and add it to `docs/schema/README.md`.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Fix any failures before proceeding.
  - This session touches `resources/js/` and / or `resources/scss/` only if the optimization phase surfaces something worth extracting. Run `npm run build` whenever that happens.
  - Widget asset rebuild (`php artisan build:public`) is not expected unless a cleanup pick touches widget SCSS/CSS/JS. If one does, run it.
  - Playwright: `npm run test:e2e` is **required** before pausing for manual test — the squash forces a full `migrate:fresh --seed` which several e2e specs depend on as a baseline.
- **Tests for new behaviour:** this session is mostly zero-behaviour-change (the squash collapses history; the optimization pass is net-reduction). Any pick that changes user-observable behaviour needs a test. The perf-optimization picks specifically need test coverage of the before/after query counts or payload shapes if the change is non-trivial.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll` — standalone layouts, test-only pages, ad-hoc records not tied to the feature being exercised. Rows that are the natural output of the feature under test (e.g. contacts imported by a happy-path importer spec, notes created by a notes spec) may stay. **The Housekeeping stub's importer-spec audit is explicitly in this session's scope** — see Phase 3.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready. No merge, push, or deploy happens at this stage unless the user explicitly requests it (e.g. for deploy-server testing).

Claude then drafts the next session's documents. The session that follows 208 will be picked from the remaining pre-Beta-1 queue in `session-outlines.md` based on what the user brings up at close time (likely candidates: the Template Editor sub-navigation refactor stubbed in 207, the Rich Text Custom Fields stub, or one of the Importer Source Presets batches).

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/209. base-prompt.md`, updating the session number, title, and session-specific read-list items.
- **Next session prompt**: draft `sessions/209. Session Title.md` using `sessions/template-session-prompt.md` as the format reference, working from the relevant stub.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/208. Migration Squash & Code Optimization — Log.md`. Use `sessions/template-session-log.md` as the format reference. The log must include: the migration count that was squashed, the new `docs/schema/README.md` squash-note cutoff, a per-pick breakdown of the optimization phase (what was applied, what was deferred and why), the help-doc coverage delta (files added or stubs created), and production deployment notes (how to re-baseline prod schema after a squash — the one-time clean replay).
- **Update session-outlines.md**: move this session's title into the Completed Sessions table. Update the Housekeeping stub to reflect which items were picked up here. Remove any stub items that 208 fully resolved (e.g. the orphan-`tailwind.config.js` note from 181's log, if addressed).
- **Archive the previous session**: move all files matching `sessions/207. *.md` into `sessions/archived/`. Skip silently if already moved.
- **Commit**: stage all changed files, commit on the current `session-208/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern: `[ParentPage::getUrl() => 'Parent', 'Current Page']`.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Pre-beta license (this session's defining posture):** no backward-compatibility shims, no deprecation periods, no column-rename-with-data-migration-then-drop-later dance. If a cleanup pick calls for removing a column, renaming a method, or breaking an internal API, just do it. The squash absorbs whatever new migrations this session commits, so "drop unused column" = one migration that the squash immediately collapses into the baseline. No live installations exist; the wipe-and-reinstall path is safe.
