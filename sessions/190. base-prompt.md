We are about to begin a new session: **190. CRM Importer — Donations, Memberships & Invoice Details**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index, then read the individual table files under `docs/schema/` relevant to this session's scope — at minimum `contacts.md`, `donations.md`, `memberships.md`, `membership_tiers.md`, `transactions.md`, `funds.md`, `campaigns.md`, `import_sessions.md`, `import_sources.md`, `import_id_maps.md`, `import_logs.md`, `event_registrations.md` (for Transaction linkage context).
3. Read the session 189 prompt (`sessions/189. CRM Importer — Events, Registrations & Transaction Linking.md`) and skim `app/Filament/Pages/ImportEventsPage.php` + `ImportEventsProgressPage.php` — this session's pages are siblings of those.
4. Open `sessions/190. CRM Importer — Donations, Memberships & Invoice Details.md` and read the session prompt carefully.
5. Confirm the WCG working data is present in `local/import-fixtures/wcg/` and verify the path is `.gitignored` before touching any real data. Never stage real exports, never paste real rows into committed docs or fixtures. Anonymised fixtures for tests live under `tests/Fixtures/import/`.
6. Summarise your understanding and confirm you are ready to proceed.
