# Base Prompt — Session 248

We are about to begin a new session: **248. Fleet Manager Contract v2.0.0 — mTLS Migration**.

This session authors the v2.0.0 contract bump that swaps bearer authentication for mTLS at the TLS layer. Forcing function: FM-side session 011 raised the bump as a formal coordination request, blocking FM session 012 (mTLS absorption) and re-sequencing A2 (Fleet Manager — Node Operations Parity). The bump is non-additive — the bearer path is removed in the same cutover, justifiable pre-Beta-1 because there are no live clients yet.

The FM-side `last_backup_at` threshold-derivation ownership question is **explicitly NOT in scope for v2.0.0** — it stays pending in FM's coordination-requests block and lands at a future additive v2.x bump. v2.0.0 stays narrowly the auth-handshake change.

A2's FM-side capability work (install / backup-trigger / restore / log-fetch UI) does not land in this session. It resequences to FM 013+ on the mTLS foundation, after FM 012 absorbs v2.0.0.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the canonical format reference. Do not infer format from previous session logs.
2. Read `sessions/release-plan.md` — the 11 discipline rules + § A2. Note that A2's prerequisite (this v2.0.0 bump) is not yet a working-set entry; adding it is part of the close gate.
3. Read `sessions/session-outlines.md`:
   - Active tracks block (line ~18 — Fleet Manager Agent reopened for A2).
   - Cross-Repo block (line ~22 — currently states "Pending boundary changes: none," which is stale; FM has two pending requests).
   - Fleet Manager — Node Operations Parity stub (line ~243).
4. Read `sessions/tracks/fleet-manager-agent.md` — the track doc. Status snapshot is pre-244 and needs refreshing at close.
5. Read `sessions/fleet-manager-planning-spec.md` § "Two-Repo Coordination Protocol" — the discipline this session is bound by; this session IS the boundary-touching session.
6. Read FM-side coordination requests at `/home/al/fleetmanager/sessions/session-outlines.md` (Cross-Repo block). Both pending requests live there. This session resolves the **mTLS migration** request only; the **threshold-derivation ownership** request stays pending, deferred to a future additive v2.x bump (deliberate scoping decision — keeps v2.0.0's retrospective focused on the auth swap).
7. Read FM session 012 prompt at `/home/al/fleetmanager/sessions/012. mTLS Migration — Absorb v2.0.0 Contract.md` — what FM expects to find on the CRM side after v2.0.0 ships, including its Phase 0 pre-flight checks.
8. **For the contract surface bump** (Phase 1):
   - Read `docs/fleet-manager-agent-contract.md` (current v1.2.0).
   - Read `app/Http/Middleware/AuthenticateFleetManagerAgent.php` (the middleware to retire).
   - Read `app/Http/Controllers/Api/Fleet/HealthController.php` (response shape stays; only the surrounding auth changes).
   - Read `bootstrap/app.php` (the `'fleet.agent'` alias).
   - Read `config/fleet.php` (`agent.api_key` to remove).
   - Read `routes/api.php` (the `/api/health` route + middleware group).
   - Read `.env.example` (the `FLEET_MANAGER_AGENT_KEY` line to remove).
9. **For the nginx mTLS configuration** (Phase 1):
   - Identify where the project's nginx config lives. Likely candidates: `docker/nginx/`, `nginx/`, or inline in `docker-compose.yml`. Confirm at session start.
   - Read `docker-compose.yml` and `docker-compose.prod.yml` for the nginx service shape — current bind-mounts, command, exposed ports.
   - Read `docs/app-reference.md` for the production environment + container names + the Backups section's `.env` propagation note (the bind-mount pattern for the cert mirrors the `.env` pattern).
10. **For the test rewrite** (Phase 3):
    - Read `tests/Feature/Api/Fleet/HealthEndpointTest.php` — 16 cases today, 6 of which are auth-related and delete; the rest update the `contract_version` literal.
11. Open `sessions/248. Fleet Manager Contract v2.0.0 — mTLS Migration.md` and read the session prompt carefully.
12. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration**, ask the user what existing system handles it. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** "Should I use X or Y here" is a question for the user, not something to commit to and unwind later. This includes data model shape, new abstractions, framework patterns that will propagate, and any decision that's expensive to reverse. Cheap and local — just decide.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. (No migrations expected this session — the bearer key was an env var, not a DB column.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. Baseline at 247 close: 1740 fast tests / 9235 assertions. Expect a small drop — auth tests delete.
- **Front-end builds — when to run which:** No front-end touch points expected this session. Pure backend + nginx config + spec doc.
- **Cross-repo coordination — this IS a boundary-touching session.** Every protocol step in `sessions/fleet-manager-planning-spec.md` § Two-Repo Coordination Protocol fires:
  - WebFetch of canonical URL is a no-op here — this session AUTHORS the canonical. Skip the fetch step.
  - Update spec doc body + CHANGELOG before commit.
  - Bump `Contract Version` field at top of spec doc.
  - Update Cross-Repo block in `sessions/session-outlines.md`.
  - At close, confirm that once the user pushes main, the canonical raw-GitHub URL will return v2.0.0. FM 012 unblocks at that moment.
- **Verify objective outcomes yourself; pull the user in for judgment.** When you can observe the result directly, do — don't ask the user to run it and report back. Close the loop yourself for CLI output, file contents, exit codes, logs, and anything Playwright can verify. The importer is the model: success is "the right rows exist," and Playwright sees that better than the user does. Console errors, network responses, data round-trips — same idea. **Playwright is a verification mechanism, not just an existing test suite** — when an objective outcome lives in the browser (form submissions, list rendering, data round-trips), a quick spec written for the moment is faster than a user round-trip and stays in the suite afterward; reach for it without being told. If the success criterion is objective, the user doesn't need to be in the loop. Pull them in for things only a human can judge: visual design, UX, whether an interaction feels right. Heuristic: *"does this work?"* you can usually answer; *"is this right?"* usually needs the user.
- **Manual testing for this session is fully self-verifiable.** The three mTLS curl scenarios + the public-route spot-check all return objective results observable from Bash. Run them yourself and report outcomes. No visual or UX surface in scope this session; the user does not need to be pulled in unless a result is ambiguous. Do not suggest closing the session — wait for the user to initiate close.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews and brings up the next session's prompt when ready.

The next session after 248 has two natural shapes — pick at 248 close:

- **If FM 012 is ready to run:** that session is FM-side, not CRM-side. The CRM-side wait is for FM 012's outcome. The next CRM-side session pulls forward another no-prereq item from the release-plan execution order — strongest candidates remain E1 (Onboarding/Install Dashboard Widget), A5 (2FA), or E11 (Page Builder Focus-Scroll Clamp).
- **If FM 012 surfaces CRM-side fix-ups during its absorption:** the next CRM-side session lands those.

Decide at 248 close based on FM-side state. Draft the 249 base prompt + session prompt against whichever shape is agreed.

### Phase 2 — Close

When the user says to close (after 249's prompt is agreed):

- **Session log**: write `sessions/248. Fleet Manager Contract v2.0.0 — mTLS Migration — Log.md` against `template-session-log.md`.
- **Update `sessions/completed-sessions.md`**: append session 248's row.
- **Update `sessions/release-plan.md`**: insert a v2.0.0 mTLS migration entry between A1 and A2 (decision: separate working-set entry vs annotation under A2 — make the cleanest version). Update the execution order to reflect the prerequisite.
- **Update `sessions/session-outlines.md`**: bump the Cross-Repo block (v2.0.0; this session as last boundary-touching; resolve both FM-side coordination requests). Update the Fleet Manager — Node Operations Parity stub to note the v2.0.0 prerequisite shipped at 248. Update the Active tracks block's Fleet Manager Agent line if its phrasing has drifted.
- **Update `sessions/tracks/fleet-manager-agent.md`**: refresh status snapshot (last update date, the 244 reopen, this session's v2.0.0 work). Add a Phase 3 retrospective entry under "Phase Retrospectives." The forward plan section should reflect the mTLS-shipped state.
- **Production deployment notes in the log**: the operator must paste the FM-supplied cert into `/opt/nonprofitcrm/nginx-certs/fm-client.crt` and restart the nginx container before the next FM poll. List the exact commands.
- **Archive the previous session**: move all files matching `sessions/247. *.md` into `sessions/archived/`.
- **Commit**: stage all changed files and commit on `session-248/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.
- **FM 012 unblocks** the moment the user merges this branch to main and pushes. The canonical raw-GitHub URL must return v2.0.0 before FM 012 can pass its Phase 0 pre-flight.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md`. (Not expected this session.)
- Every new Filament page must include `getBreadcrumbs()`. (Not expected this session.)
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. (Out of scope this session — no portal code touched.)
