# Base Prompt — Session 327 (Events — Listing Upgrade)

**Start now.** Execute the reading list below, open the session prompt at `sessions/327. Events — Listing Upgrade.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **327. Events — Listing Upgrade.**

This is the **third build session of the demo-site work** — the **"session B"** of `sessions/demo website/demo-site-plan.md` (§3), now running **after** the landing-page work (session 326) because the listing's fixes depend on every event having a resolvable landing-page URL, which 326 delivered. It sits after the `── PUBLIC WEBSITE COMPLETE ──` milestone.

This session is **NOT boundary-touching** — no Fleet Manager surface, no `/api/health` change. CRM contract stays at **v2.3.0**. **A schema change is possible this session** (an event-type taxonomy and/or a "featured" flag — see the open questions); if one lands it is a normal CRM migration, not a contract change.

**Session shape:** feature-style, additive to the existing `EventsListing` widget. Add a **day-grouped list-mode preset** (the "luma" shape) alongside the current grid/swiper; a **filter row** (Event Type + Date Range); a **featured-event hero**; and the **art-fallback chain** (§1a). Event rows link to the landing page every event now has. `data-tour` anchors per §2c.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). Canonical format reference — do not infer format from previous logs.
2. **Read the demo-site plan** `sessions/demo website/demo-site-plan.md` — **§1a** (the events-index v1 cut: row anatomy, filter row, featured hero, art-fallback chain, empty/sparse states, schema/OG), **§3 row B**, and **§2c** (tour anchors) are load-bearing.
3. Read `sessions/session-outlines.md` (active-tracks + milestone framing) and the **session 326 log** (`sessions/326. … — Log.md`) — it is the authoritative record of how event LPs/URLs now resolve, which this session links to.
4. Read `docs/app-reference.md` and `docs/schema/README.md` + the `events.md` table file (confirm what categorisation an event actually has today — **note: there is no `event_type`/`category` column; only `recurrence_type` and the tag system**).
5. **Read the surfaces this session touches:** `app/Widgets/EventsListing/` (Definition, template, script, styles — the additive preset goes here), `app/WidgetPrimitive/Projectors/SystemModelProjector.php` (`projectEvent` — the fields the list/filters/art-fallback consume), and the `QuerySettings`/filter plumbing the widget already exposes. Read `app/Widgets/EventMiniCalendar/` only as a reference for the day-key + density pattern if a day-grouped anchor is wanted.
6. Note any drift between this prompt and the actual code in a brief work-log entry; proceed unless something needs a decision per the drift / decision-threshold rules.

---

## Starting state inherited from session 326 *(forward projection — confirm at 326 close)*

- **CRM contract at v2.3.0** — unchanged.
- **Schema baseline:** through 326, no event/page schema change (LP is a `Page`; `landing_page_id` pre-existed). **This session may add the first events-table column(s)** if the open questions resolve toward a real event-type field and/or featured flag.
- **Fast Pest baseline:** the 326 close count (confirm at start). New tests expected for the list-mode preset, the filter logic, the featured selection, and the art-fallback chain.
- **Every event has a landing page (326):** Simple or Standard, linked via `events.landing_page_id`; `EventsListing` rows already point at the LP URL with no dead-end fallback. This session builds on that — rows stay linked; no no-LP handling needed.
- **Planning state:** the demo-site plan was reshaped at 325 close (no-LP model retired; LP work reordered ahead of this). See `demo-site-plan.md` §3.
- **Housekeeping inbox** — feature-style session; absorb inbox items only if directly in the path.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Additive, not a rewrite.** The list-mode preset sits *alongside* the existing grid/swiper layout on `EventsListing` — existing pages keep rendering exactly as they do. Mirror how the widget already branches on config.
- **Lean on what exists before adding schema.** Events already support **tags** and the widget already has tag-aware `QuerySettings`. Prefer the existing taxonomy for the "Event Type" filter unless the gate decides a first-class `event_type` column is worth the migration. Same discipline for "featured" — an operator pick or "next upcoming" may beat a new column.
- **If a migration lands:** run it via Docker and **update `docs/schema/`** (the relevant table file + README) in the same pass.
- **Front-end build:** `EventsListing` asset/template/SCSS changes → `build:public`; any Vite source → `npm run build`.
- **`data-tour` anchors** per §2c: the list, the featured hero, the filter row each declare their stable anchor (e.g. `events-index.list`, `events-index.featured`, `events-index.filters`).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting**. Settle these, which genuinely shape the build (bring a recommendation for each):

1. **Event Type filter source** — reuse the **tag** system (no schema) vs add a first-class `event_type` column (migration). Lean: tags, unless a reason for a dedicated field surfaces.
2. **Art-fallback "event-type default" tier** — there are no per-event-type default images today. Resolve the §1a chain accordingly: drop that tier (event image → **site default thumbnail** → solid colour), or tie a default to the tag/type choice above.
3. **"Featured" selection** — operator flag (migration) vs an implicit rule (e.g. next upcoming / first in sort). Lean: the lightest rule that reads well for the demo.
4. **Date Range filter shape** — preset ranges (Upcoming / This month / Past) vs a picker. Lean: a small preset set.

One confirmation, then go.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate in `sessions/template-base-prompt.md` Phase 1 + Phase 2. Session-specific details:

- **Log file:** `sessions/327. Events — Listing Upgrade — Log.md`. Capture the list-mode preset, the filter decisions (and any migration), the featured mechanism, and the art-fallback chain as built.
- **Bump `VERSION`** to `0.327.<iteration>`.
- **Update** `sessions/completed-sessions.md` + `sessions/session-outlines.md`; mark "Events — listing upgrade" complete in `demo-site-plan.md` §3.
- **Archive the previous session's files** — move `sessions/326. *.md` → `sessions/archived/`.
- **Next session:** per the plan, D (per-page permission + locked landing) or E (demo-node assembly) — owner picks at close.
- **Commit on `session-327/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: keep the new layout a *preset* of the existing widget (no parallel widget); mirror `EventsListing`'s existing config-branching + token-only SCSS; commit messages name the deliverable ("327/N — Events listing: day-grouped list mode + filter row + featured hero + art-fallback").
