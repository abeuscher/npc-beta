# Base Prompt — Session A004 (Code Shape & Fit — Cycle 1 Review)

**Start now.** Execute the reading list below, open the session prompt at `sessions/A004. Code Shape & Fit — Cycle 1 Review.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **A004. Code Shape & Fit — Cycle 1 Review.**

This is **an agentic cloud session** with no release-plan execution-order slot. The A-prefix series carries autonomous-or-discussion-shaped work that runs in the agent's remote-execution environment, scheduled out-of-band with the numeric Beta-1 track. A001 (Content Import/Export coverage), A002 (Public-Bundle Consolidation), and A003 (if it has run) are the prior A-series sessions; A004 picks up the first review of the Code Shape & Fit refactor track's first cycle (sessions 312 audit + 313 apply).

This session is **NOT boundary-touching** — no Fleet Manager surface in scope. CRM contract stays at v2.3.0. No schema changes; no code changes expected. **The session's only writes are to planning artifacts** (track doc + register + possibly the standing-improvements list).

**Session shape:** track-phase / review. Reads 312 + 313 outputs end-to-end, evaluates them against the Code Shape & Fit track's stated goals, surfaces process observations, discusses them with the user, and once user and agent are aligned, edits the track documents to reflect the agreed conclusions. The discussion is the load-bearing piece — the doc edits at the end are bookkeeping.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`). They are the canonical format reference — do not infer format from previous session logs.
2. Read `sessions/session-outlines.md` for the active-tracks block. The Code Shape & Fit row carries the track's current status.
3. **Read the track doc end-to-end:** `sessions/tracks/code-shape-and-fit.md`. This is the canonical source for the stated goals of the track — the five workstreams, the anti-performative-change discipline, the cycle shape, the cadence rule, the standing-improvements list. Cycle 1 retrospective lands here at 313 close and is the centerpiece of this review.
4. **Read the Cycle 1 audit log:** `sessions/(archived/)312. Code Shape & Fit (Audit) — Cycle 1 — Log.md`. The 312 file may have already moved to `sessions/archived/` at 313 close — check both locations. This is the audit-side input to the review.
5. **Read the Cycle 1 apply log:** `sessions/313. Code Shape & Fit (Apply) — Cycle 1 — Log.md`. This is the apply-side input.
6. **Read the permanent inter-cycle artifact:** `sessions/tracks/code-shape-and-fit-finding-template.md`. The finding-template shape itself is part of what's being reviewed.
7. **Read the carve-out register:** `sessions/refactor-track-deliberately-kept.md`. The Cycle 1 register edits (the two additions, the FieldMapper accuracy fix) are part of what's being reviewed.
8. Open `sessions/A004. Code Shape & Fit — Cycle 1 Review.md` and read the session prompt.
9. Note any drift between the session prompt and the actual artifacts in a brief work-log entry.

---

## Starting state inherited from session 313

- **CRM contract at v2.3.0** — unchanged through 313 (refactor-only).
- **Schema baseline:** no schema changes since session 308; 312/313 added none.
- **Fast Pest at 313 close:** 2586 passed (baseline carried unchanged through both 313 iterations).
- **Playwright suite** running in CI on every push against the isolated stack. `npm run test:e2e` defaults to isolated as of s313.
- **Planning state:** the Code Shape & Fit track's Cycle 1 retrospective landed in the track doc at 313 close, next trigger ≈ session 379 per the +50 cadence rule. Standing improvements list now carries two Cycle 1 → 2 carries (audit-agent citation-verbatim rule; sequence-after-cleanup-cycle preference). Deliberately Kept register gained two entries at 313 close (per-action endpoint shape, `ImportContactsPage` long-file). FieldMapper register entry carries the accuracy fix + the missing-`OrganizationFieldMapper` note pointing at the housekeeping inbox.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this session does not absorb items.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific notes:

- **Read-only on code.** A004 makes zero code changes. Writes are confined to planning artifacts: the track doc, the carve-out register, the finding-template artifact, and possibly the standing-improvements list. If during review the agent identifies a finding that would require code work, it goes to the housekeeping inbox or a release-plan stub — not into A004.
- **The discussion is the artifact.** Surface process observations as a structured review with the user; capture the discussion's conclusions before editing any track doc. Premature doc edits constrain the conversation — wait until alignment is reached before writing.
- **Cloud-session constraints.** No Docker daemon access; cannot run the canonical `./dev test` Pest suite locally. Not relevant for A004 (no code changes) but stated explicitly so the agent doesn't try to run tests as a verification step.
- **Hard-no files for this session.** No edits to `app/`, `resources/`, `database/`, `tests/`, `routes/`, `config/`, `docs/`, or `bin/`. The session's write surface is `sessions/tracks/code-shape-and-fit.md`, `sessions/refactor-track-deliberately-kept.md`, `sessions/tracks/code-shape-and-fit-finding-template.md`, and (if process observations land there) `sessions/housekeeping-inbox.md` or `sessions/release-plan.md`. Everything else is read-only.
- **PM-level tone for status summaries** per `feedback_pm_level_tone`. Bookkeeping codes (W-R1/A, Flag B successor) stay in the doc edits and the log; the discussion uses plain language.
- **Questions are not instructions** per `feedback_questions_are_not_instructions`. The review surfaces options and observations; the user picks which conclusions land in the docs.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list above and the Starting state are read, surface a one-paragraph orientation — what stood out from reading the 312 + 313 logs, what the agent considers the headline questions for the review discussion, and what (if anything) needs clarification on the review's scope — and wait for the user to confirm before starting. Do not pipeline straight from reading into the discussion.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the user explicitly says to close.** Do not ask, suggest, or pipeline.

### Phase 1 — Attenuate and prepare next session

A004 has no immediate successor in the A-series; the next A-prefixed session will be scheduled out-of-band when the next autonomous-shaped need arises. The numeric track continues with session 314 (Housekeeping Batch 2 — Batch 2A) per the release plan, whose prompts are already drafted; A004 does not pre-empt or push that ordering.

No next-session prompts to draft. The agent should confirm with the user that no further A-series work is queued before proceeding to close.

### Phase 2 — Close

When the user says to close:

- Write `sessions/A004. Code Shape & Fit — Cycle 1 Review — Log.md` from `sessions/template-session-log.md`. Capture: the agent's pre-discussion evaluation, the discussion's conclusions per topic, every track-doc edit made (with before/after where meaningful), any new standing-improvements item added, any item that promoted from the discussion to the housekeeping inbox or to a release-plan stub.
- Update `sessions/completed-sessions.md` with the A004 row.
- Update `sessions/session-outlines.md` if the active-tracks block's Code Shape & Fit row needs adjustment (e.g. the next-trigger date, the standing-improvements summary).
- **Archive any prior A-series session files** per the archive rule if applicable. The most recent prior is A002/2 (already archived at this point); A003 status varies.
- Commit on `session-A004/1`, notify, do not push.

---

## Style rules

Base-prompt style rules apply. Session-specific: the log narrates the discussion in prose (not a bullet-soup of decision points); track-doc edits land as discrete diff-able blocks (don't restructure the doc to make small changes); no docstrings/comments on code — there's no code in scope.
