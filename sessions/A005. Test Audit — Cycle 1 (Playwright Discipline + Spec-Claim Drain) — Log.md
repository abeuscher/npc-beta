# Session A005 — Test Audit — Cycle 1 (Playwright Discipline + Spec-Claim Drain) — Log

**Date:** 2026-05-22
**Branch:** `claude/base-prompt-review-CxSV5` (work iteration, merged via PR #20) → `session-A005/2` (close docs)

First cycle of the newly-lifted Test Audit track (`sessions/tracks/test-audit.md`). Picked dimensions **D1 (Playwright spec discipline)** and **D2 (spec-claim integrity drain)**; D3 (mutation slice) and D4 (Pest file relevance) queued for later cycles per the track's forward plan. Test-code and planning-doc changes only — **no application-code changes**.

---

## What was built

### Phase 1 — D1 walk: Playwright spec discipline (verdict table)

Inventoried all 28 specs under `tests/e2e/` and characterized each against the browser-only criterion. Verdict summary by cluster:

| Spec | Verdict | Applied disposition |
|------|---------|---------------------|
| `importer/contacts-happy-path` | Keep | full wizard flow — browser-required |
| `importer/contacts-update-strategy` | Keep | two-pass + approve modal UI |
| `importer/contacts-duplicate-header` | Keep | review-step findings UI |
| `importer/contacts-pii-rejection` | Keep | PII phase + download |
| `importer/donations-error-report` | Keep | error table + download |
| `importer/donations-mapping-indicator` | Keep | Choices.js search init |
| `importer/site-import-export` | Keep | FilePond + manifest + gated toggles |
| `importer/donations-update-strategy` | Redundant | **deleted** — data covered by `ImportFinancialTest` |
| `importer/events-happy-path` | Redundant | **deleted** — covered by `ImportEventsTest` / FixtureRunner |
| `importer/invoice-details-happy-path` | Redundant | **deleted** — covered by `ImportFinancialTest` |
| `importer/invoice-details-grouping` | Redundant | **deleted** — covered by `ImportFinancialTest` |
| `importer/invoice-details-update-strategy` | Redundant | **deleted** — covered by `ImportFinancialTest` |
| `importer/memberships-happy-path` | Redundant | **deleted** — covered by FixtureRunner |
| `importer/memberships-update-strategy` | Redundant | **deleted** — covered by `ImportSession194Test` / `ImportSessionActionsTest` |
| `importer/notes-happy-path` | Redundant | **deleted** — covered by `NotesImporterTest` + `ImportSessionActionsTest` |
| `importer/organizations-happy-path` | Redundant | **deleted** — covered by `ImportOrganizationsTest` / FixtureRunner |
| `importer/organizations-update-strategy` | Redundant | **deleted** — covered by `ImportOrganizationsTest` |
| `page-builder/inline-formatting-toolbar` | Delete | **deleted** — `describe.skip` since s306, parked dead weight |
| `event-registration/ticket-tier-picker` | Delete | **deleted** — CI-unstable (per-test fresh published event hits an un-warmed public page depending on the unbuilt `event_registration` widget bundle); per user delete-not-requarantine policy |
| `page-builder/inline-editing-foundation` | Keep (flaky-watch) | kept — `:79` cold-load flake stays on passive watch (browser-required) |
| `page-builder/inline-editing-phase2` | Keep | browser-required Vue-editor surface |
| `page-builder/layout-inspector` | Keep | browser-required (D2 rename applied — see below) |
| remaining specs (dashboard-settings, public-render, etc.) | Keep | browser-required |

**Importer-cluster safety net:** each "Redundant" verdict was checked against `tests/Feature/Generated/ImportFixtureRunnerTest.php` (7 importers × 4 shapes off-Livewire) plus the namespaced per-importer Feature tests before deletion. The kept set preserves every browser-only affordance (FilePond, Choices.js, multi-step nav, progress polling, download buttons, the approve modal).

### Phase 2 — D1 apply: deletions + workflow doc

- Deleted 12 specs (**28 → 16**). Each deletion recorded as a `[test-integrity]` housekeeping line with its absorbing Pest coverage.
- Shipped `docs/testing/playwright-discipline.md` — the durable workflow doc, mirroring `docs/testing/mutation-audits.md`'s shape (browser-only criterion, Keep/Redundant/Mixed decision rule, local-first authoring discipline, the A003 anti-pattern catalog, the FixtureRunner-is-the-safety-net rule, forward queue → track doc).
- Updated `tests/e2e/README.md` for the reduced suite.

### Phase 3 — D2 drain: spec-claim integrity

Drained the s296 `[test-integrity]` backlog:

| Item | Disposition |
|------|-------------|
| `DonationCheckoutTest` "creates a pending donation record on checkout initiation" | **Deleted** — factory-echo (posted then discarded the response, asserted a factory's own attributes); round-trip already covered by "stores one-off donation type correctly" |
| `QuickBooksSyncTest` "creates a sales receipt…" | **Renamed** → "syncs an inbound transaction via createSalesReceipt and records the returned quickbooks_id" (mocked client; body asserts job routing + id persistence) |
| `QuickBooksSyncTest` "creates a refund receipt…" | **Renamed** → "syncs an outbound refund via createRefundReceipt and records the returned quickbooks_id" |
| `QuickBooksCustomerMatchingTest` "attaches CustomerRef…" | **No change** — titles already match bodies; the s296 lumping was imprecise |
| `ProductCarouselTest` "seeder total widget count includes product_carousel" | **Deleted** — magic-count change-detector; presence already proven by "seeder creates product_carousel with correct config schema" |
| `full-width-matrix.spec.ts` "every widget handle renders…" | **No action** — already resolved (smoke test deleted at A003) |
| `layout-inspector.spec.ts` "…persists background + padding" | **Renamed** → "drives the three-tab layout inspector and writes background + padding to the layout appearance config" |
| `TaxReceiptTest` "includes only active donations…" / "excludes donations from other tax years" | **Rewritten** to route through the real `DonorsPage::buildBreakdown()` (reflection idiom) instead of inline `Donation` query copies |

### Phase 4 — Forward scan

One additional mismatch found and fixed: `dashboard-settings.spec.ts` "loads, adds a widget, and the DB reflects the new arrangement" — the body never adds a widget; **renamed** → "loads the dashboard arrangement view with the expected widgets and no preset/spacing tabs".

### Phase 5 — Cycle 1 retrospective + workflow-doc verification

Retrospective landed in `sessions/tracks/test-audit.md` (Cycle Retrospectives); status snapshot bumped. `docs/testing/playwright-discipline.md` read end-to-end.

---

## Security considerations

None beyond standard convention. No application code, routes, auth, or data-handling touched — the session's writes are confined to test code and planning docs. The `QuickBooksSyncTest` token-redaction test was untouched.

---

## Schema changes

None.

---

## Production deployment notes

None beyond standard `migrate`. No application-code, migration, or config changes.

---

## Deferred decisions

- **Local-first Playwright discipline → CLAUDE.md.** Codified in the new `docs/testing/playwright-discipline.md` workflow doc this cycle; promotion to a hard CLAUDE.md / base-prompt process rule deferred until the doc settles (next cycle), per the track's standing-improvement note.
- **Forward-queue Pest gaps** (recorded in `sessions/housekeeping-inbox.md`, not silently dropped): (a) the real donation checkout-initiation controller path (create-pending-then-delete-on-Stripe-failure, mock the Stripe boundary); (b) invoice first-row-wins parent-conflict resolution. Address when a donations/financial session next opens.
- **`inline-editing-foundation.spec.ts:79` cold-load flake** — kept on passive watch (browser-required); the CI run on this branch is the evidence. Re-triage if it recurs.
- **D3 (mutation slice) and D4 (Pest file relevance)** — queued for later cycles per single-cycle scope discipline.

---

## Test result

CI `Tests` aggregate green on the merged commit (`4e2d575`): Fast suite ✅, Slow suite ✅, Playwright e2e (isolated stack) ✅, **Tests** (aggregate) ✅. No application-code changes; full Pest suite pass count unchanged modulo the 2 deleted vacuous Pest tests. Playwright suite: 16 specs, green.
