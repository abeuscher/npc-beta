# Session 391 — Donations Extracted — Log

**Date:** 2026-08-19
**Branch:** session-391/1

---

## What was built

Arc D3 of the Plugin Architecture arc, third and final session of the carve → package → extract block — **the block is closed**. The fourth extraction-runbook run: `nonprofitcrm/donations` left the repo for `github.com/abeuscher/crm-plugin--donations` (public, tag `v0.1.0`), consumed back as a composer VCS dependency. Behavior-preserving throughout: fast suite at the 390 baseline exactly (3314/0, path re-points only), routes 272, no schema work, no visual change. One iteration, one core commit (`b6983f1a`) plus the owner's ADR revision riding the branch (`c2e2bece`).

### Phase 1 — The extraction (the runbook, top to bottom)

- **The new repo:** `plugins/Donations/` copied to the owner-created sibling checkout (62 files); the extracted `composer.json` dropped its `version: "0.1.0"` field (the path-repo determinism device inverts — tags are the version metadata); DonationForm's one asset declaration rewritten vendor-relative (`vendor/nonprofitcrm/donations/Widgets/DonationForm/script.js`); README written on the Events model (contract version, tags-are-releases, tests-travel note); committed, tagged `v0.1.0`, pushed.
- **The four in-plugin test path reads re-pointed in the extracted copy** (they travel with the package): `DonationsSchemaSession390Test`'s migrator-path assertion and `DonationCreditsImporterUntouchedTest`'s three file reads, all `base_path('plugins/Donations/…')` → the vendor path.
- **Core swapped onto the VCS repository:** `distribution.json` source `path` → `vcs` (provider/package/constraint unchanged; `plugins:manifest-sync` a provider-list no-op as predicted); root `composer.json` repository entry swapped; `git rm -r plugins/Donations`; `composer update nonprofitcrm/donations` (composer before artisan — the 387 ordering caveat held); lock pins the tag exactly (`version v0.1.0`, git source at the tag commit, dist zip); `composer validate --strict` clean. Manifest + composer + lock one commit.
- **Dockerfile:** both Donations manifest COPY lines removed — zero path-sourced entries remain, so no plugin COPY lines at all; the two stage comments updated to the all-VCS reality.
- **Core-side re-points (the surveyed set, nothing more):** the two fixture migrator reaches (`DonationsRemovedTestCase`, `DonationsDisabledViaFlagTestCase`) → `vendor/nonprofitcrm/donations/database/migrations` (disabled ≠ uninstalled proven against the vendor'd migrations, assertions unchanged); `PluginPackageGuardTest`'s four-table owner map → the vendor path; phpunit's `Donations` testsuite → `vendor/nonprofitcrm/donations/tests`.
- **Zero new guard relaxations:** `plugins/` emptied a second time and the 388 tolerate-empty set held as designed. Root `"Plugins\\"` PSR-4 stays (D12).
- **Doc wording:** `docs/app-reference.md` (5 rows), `docs/schema/README.md` + the four table ownership headers, `DonationModuleBoundaryTest` docblock — all to the extracted wording.

### Verification (all runbook gates)

- Gate 4 live on the one asset: `widgets:sync` (the `widget_types` row now carries the vendor path) → `build:public` → the built bundle proven to carry `window.NPWidgets.donationForm` (minified `donationForm=function(`). RecentDonations declares no assets; no cross-plugin asset input existed (as surveyed).
- In-image proof: `docker build --target app` clean — all four plugin packages fetched from GitHub inside the build (no COPY lines), `DonationsServiceProvider` autoloads through the optimized `--no-dev` autoloader, the vendor'd migrations present, `plugins/` absent from the image.
- Dev-stack smoke: home + admin login 200; provider autoloads from the vendor path; 4 plugins registered.

### Docs

- Plugin contract → **0.15.0**: External repo gains the fourth consumer (second migration-owning + testsuite-carrying extraction — the D2 postures re-proven); surface 5's fixture note and the test-membership posture gain their second consumers; reference implementations add Donations as the second full-vertical reference; changelog.
- Runbook: worked-example lines for the fourth run added (step 0 + step 9). **No procedure deltas** — the runbook ran as written. One recorded nuance: a plugin's *own* traveling tests can carry `plugins/…` path reads that re-point in the extracted copy (step 3's edit, not step 7's).
- Plan doc: D3 ladder line marked ✅ (block closed), arc header advanced to D4.
- ADRs 0001–0006 revised by the owner on this branch (peer register, one-word Status lines, cross-ADR tensions named) — owner commit `c2e2bece`.

### Notable drift and bookkeeping (adapted, logged)

- The owner-created repo carried one empty committed `readme.md`; replaced by the real `README.md` in the extraction commit (first push not perfectly clean — no consequence).
- Comment-only rider: the stale Events route-file comment in `routes/web.php` (still saying `plugins/Events/…` since 388) fixed alongside the Donations one.

---

## D4 kickoff input (what the Memberships block — 392+ — inherits)

The full survey is front-loaded into `sessions/392. Memberships Carved.md` (captured from live code at this close). Highlights:

1. **The two membership webhook branches are still inline in `crm-plugin--payments`** (`handleMembershipCheckout`, `handleMembershipInvoice`) — the 389 inversion deliberately left them; subscription ids are disjoint across verticals. Inverting them is a Payments **v0.3.0** release (the proven 389 mechanics). Membership invoice *failures* were never handled — preservation means not adding a listener.
2. **The vertical is small:** no mailables, no email-template handles, no scheduled commands; two tables (`memberships`, `membership_tiers`); one widget asset (PricingChart scss). Models `Membership`/`MembershipTier`, `MembershipObserver`, `MembershipPolicy` (no tier policy exists), `MembershipCheckoutController` (already fully on the capability layer), three admin resources (`MembershipResource`, `MembershipTierResource`, `MemberResource` — the last a Contact-model resource, the DonorsPage analogue), the importer set, the MembershipStatus + PricingChart widgets.
3. **A portal seam exists (D4/D5 boundary freight):** the core `PortalSignup` widget renders `route('membership.checkout')` and declares a `membership_tier` contract; `Portal/SignupController` *creates* pending `Membership` rows. The carve preserves the route name byte-identically; memberships-absent composition-safety (the fund-row route-gate precedent) is package-phase work; any deeper inversion is D5's.
4. **Block-shape call deferred to the 392 close:** Memberships may compress package + redraw + extract into one closing session (the plan estimates the block at ~1–2 sessions after the carve).

---

## Security considerations

No new endpoint, gate, or input path. The move is representational — identical code, different repository; every auth/throttle/policy surface travels byte-identically and its nets pass unchanged. Supply-chain surface: the fourth plugin package moves from in-repo path resolution to a public GitHub repo lock-pinned to an exact tag commit (the same posture as the other three — core bumps deliberately via `composer update`). `deploy.yml` untouched; FM surfaces untouched; CRM contract stays v2.7.0.

---

## Schema changes

None. The four donation tables' create-table migrations moved repos with the package (byte-identical files); the fixtures and guards re-point to the vendor path. No migration ran, no dump changed.

---

## Production deployment notes

None beyond standard `migrate`. Existing deployed databases already carry the four donation migration rows (the 390 one-time step); the vendor'd migrations have identical names, so `migrate` is a no-op for them. The deploy image build now fetches `nonprofitcrm/donations` from GitHub (public repo, credential-free) — no pipeline change needed (the 385/387/388 precedent held). VERSION bumped 0.390.01 → **0.391.01**.

---

## Deferred decisions

- **D4 Memberships block shape** — carve at 392; whether package + extract combine into one closing session decides at the 392 close (decision 8 in the 392 prompt).
- **Model/factory moves** (§ 6.7 stands); **per-plugin CI** (D11); **root PSR-4 retirement** (D12); **events-handle registry migration** (a future `crm-plugin--events` release); **Playwright chrome-parity park** — all unchanged.
- **`baseDir()`-derived asset declarations** (would remove the per-extraction asset-path rewrite) — standing backlog, not this block.

---

## Test result

- **Fast Pest:** 3314 passed, 0 failed (parallel) — the 390 baseline exactly; every donation assertion unchanged beyond the surveyed path re-points.
- **`Donations` testsuite:** 104 passed (103 fast + 1 slow) from `vendor/nonprofitcrm/donations/tests` — same 14 files, identical counts.
- **Routes:** 272 (dev/test).
- **Composer:** `validate --strict` clean; lock pins `v0.1.0` at the exact tag commit, dist zip, no `version` field in the vendor manifest.
- **Gate 4:** bundle content proven (`donationForm` from the vendor path).
- **In-image proof:** clean (all four packages VCS-resolved inside the build; `plugins/` absent).
- **CI:** `Tests` aggregate **green** on the pushed branch (fast + slow, cold runner — the e2e job re-proved the VCS resolve building the image); Playwright 62/1 — the one failure is the pre-existing parked chrome-parity spec (non-gating).
