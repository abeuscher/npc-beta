# Base Prompt — Session 356 (Media Picker — Browse or Upload in the Page Builder)

**Start now.** Execute the reading list below, open the session prompt at `sessions/356. Media Picker — Browse or Upload in the Page Builder.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **356. Media Picker — Browse or Upload in the Page Builder.**

This is **not a `release-plan.md` entry** — it is an **emergent, owner-driven pick** taken at the 355 close (designer-pressure feature gap). With the roadmapped tracks largely complete, the next picks are non-roadmap-pressure-driven; this is one. It is **not** part of the editor↔public parity stub (355 closed that stub's Track 1; Track 2 stays open but externally gated on Filament v4 — not this session). The one-sentence framing: authors can attach a widget image only by **uploading** today; this session adds a **media-picker modal** so they can **visually browse the site's existing images as thumbnails and pick one**, or upload — in one surface. Not fancy; thumbnails are the hard requirement.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract **stays at v2.4.0** (inherited; 355 was non-boundary). No `docs/fleet-manager-agent-contract.md` or Cross-Repo-block changes.

**Session shape:** feature-style, **Playwright-guarded**. The build is two pieces over existing media infrastructure: (1) a browsable-media **list endpoint** (none exists today), and (2) a **Vue browser modal** (thumbnail grid + search + click-to-select + upload) wired into the page-builder image fields. The attach-existing-media-by-id backend (`useExistingImage` / `useExistingAppearanceImage`) **already exists** — verify and build on it, do not rebuild.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is an **emergent/feature** session, so use the fuller session-prompt shape (explicit phases, open questions, out-of-scope, drift signals).
2. Read `sessions/session-outlines.md` — the active-tracks block. Note this session is **not** a stub there (emergent pick); the media work has no standing stub. Confirm the editor↔public parity stub shows **Track 1 resolved (355)** and Track 2 still open/gated.
3. **Read the session prompt carefully** (`sessions/356. Media Picker — Browse or Upload in the Page Builder.md`) — it carries the **verified media-subsystem map** (what already exists), the gap, and the five open questions. **Build from that map; do not re-derive it** — but verify each cited path against current code as you go.
4. **Read the attach/upload seam (backend):**
   - `app/Http/Controllers/Admin/PageBuilderApiController.php` — `uploadImage`, `useExistingImage`, `removeImage`, and the appearance-image trio (`uploadAppearanceImage` / `useExistingAppearanceImage` / `removeAppearanceImage`). `useExistingImage`/`useExistingAppearanceImage` are the **already-wired** select-an-existing action the new UI will call.
   - `routes/admin-api.php` (the `api/page-builder` group) — the media routes; the new **list** endpoint slots in here.
   - `app/Http/Middleware/BlockDemoUploads.php` — the demo-role new-file-upload gate. Existing-media reuse is deliberately ungated; the picker must keep that split.
5. **Read the picker seam (frontend):**
   - `resources/js/page-builder-vue/components/fields/ImageUploadField.vue` — the widget image field (current preview + remove + file input) the "Browse library" entry point wires into.
   - `resources/js/page-builder-vue/components/DedupPromptModal.vue` — the post-upload candidate picker; the **precedent** for a thumbnail-grid + select modal calling `useExistingImage`. Decide at the gate whether it folds into the new browser (Q3).
   - `resources/js/page-builder-vue/composables/useUploadActions.ts`, `resources/js/page-builder-vue/api.ts`, `resources/js/page-builder-vue/stores/editor.ts`, and `resources/js/page-builder-vue/components/InspectorPanel.vue` — the upload/use-existing composable + API client + store, and where the appearance image field lives.
6. **Read the existing media surfaces + render (build on, don't rebuild):**
   - `app/Filament/Pages/MediaLibraryPage.php` — the browse table (thumbnail column, owner, collection, conversions); its query + thumbnail approach is the reusable reference for the new list endpoint.
   - `app/Services/Media/MediaReferenceInventory.php`, `MediaDedupService.php`, `app/Http/Controllers/Admin/MediaDedupController.php` — reference classification + hash-keyed dedup.
   - `resources/views/components/picture.blade.php`, `config/media-library.php`, and the `media` table block in `database/schema/pgsql-schema.sql` (`content_hash`, `collection_name`, `mime_type`, conversions).
7. Read `docs/app-reference.md` for the **front-end build command** (use the WSL node PATH per the build-command note), the **isolated Playwright e2e stack** (`./dev e2e:up` / `e2e:down`, the image-baked `public/build` named volume that a host build must be `docker cp`'d into), and the **canonical e2e runner** caveat (run via `npm run test:e2e`, which sets the compose-targeting env — a bare `playwright test` with only `APP_URL` sends `resetDatabase()`'s `migrate:fresh --seed` to your **dev** DB; learned at 355).

---

## Starting state inherited from session 355

- **CRM contract at v2.4.0** — unchanged (355 non-boundary; the hydration refactor + the nav mobile-editor fix touched no Fleet surface). **This session is non-boundary; the contract is unchanged.**
- **Schema baseline:** no migrations; `database/migrations/` empty (post-348-squash). **None expected** — the picker reads existing media; the browse/upload feature needs no new columns. (Media *metadata* columns are out of scope — see the session prompt.)
- **Fast Pest green at 2930 / 0** (355 close). Expected delta: **new Pest** for the list endpoint (server-side). The Vue modal is JS/TS (Pest doesn't see it) — verified by Playwright.
- **Playwright** — 355 kept `full-width-matrix.spec.ts` (333) + `chrome-parity.spec.ts` (354) as the standing parity net (both green at 355 close, 19/19). This session adds a **new page-builder media-picker spec** as the regression artifact. Run Pest and Playwright **sequentially**, via `npm run test:e2e`.
- **Planning state:** 355 resolved the editor↔public parity stub's **Track 1 (homogenize widget hydration)** — one shared `resources/js/shared/hydrate.ts` both surfaces call; the bespoke `useLibraryLoader.ts` retired. **Track 2 (clean Tailwind-v4 preflight layering)** stays open, externally gated on Filament v4. A small nav mobile-editor parity fix also landed at 355 (an `@container np-viewport` mirror in `app/Widgets/Nav/template.blade.php`). No release-plan change. The media work has **no standing stub** — it is an emergent pick.
- **Housekeeping inbox** (`sessions/housekeeping-inbox.md`) — **not a housekeeping session;** the inbox is **not** drained here (it is overdue for a dedicated batch — ~50 line-items — but that is a separate pick the owner can take when ready). Capture mid-session finds via `npm run logbug`; digest the buffer at close per the standard gate.

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **Build on the existing media stack — do not rebuild storage, dedup, or attach.** The session prompt's map is verified; the attach-by-id action already exists. The new code is exactly the list endpoint + the browser modal. If you find yourself touching `ContentAddressed*`, the conversions pipeline, or `useExistingImage`'s body, stop and surface — that's out of the intended footprint.
- **Respect the demo-role split.** Browsing and selecting existing media introduces no new file and must stay open to the `demo` role; only the upload affordance is gated (`BlockDemoUploads`). Don't gate the browse path; don't ungate the upload path.
- **Surface the list-endpoint contract + modal-vs-dedup decision at the gate.** The endpoint's shape (dedup-by-hash, scope, pagination — Q1/Q2) and whether the new browser subsumes `DedupPromptModal` (Q3) are the choices worth settling before going deep.
- **Build after front-end changes.** Any `resources/js/**` change → `npm run build`, then `docker cp public/build/. nonprofitcrm_e2e_app:/var/www/html/public/build/` (the e2e build volume is image-baked — a host build alone isn't seen by the stack).
- **Verify objectively with Playwright; pull the owner in for the visual call.** Close the objective loop yourself (thumbnails render, select attaches + persists, upload still works, demo gate holds); surface the look/feel of the picker grid for the owner.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation of the **scoped gap** (browse-and-pick UI is missing; the attach-by-id backend exists) and the **two-piece plan** (list endpoint → browser modal → verify); (2) your **answer to the five open questions** — dedup-by-hash (Q1), browsable scope (Q2), modal home + dedup-modal relationship (Q3), video fields (Q4), thumbnail source (Q5) — with a recommendation on each; (3) the **iteration shape** — whether the endpoint + modal + verification land together or split (Rule 11) if the endpoint contract proves load-bearing. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/356. Media Picker — Browse or Upload in the Page Builder — Log.md` — the list-endpoint contract, the modal + where it wired in (widget field + appearance field), the Q1–Q5 decisions, the demo-gate handling, the Playwright verification (and which spec was kept as a standing guard), the build run, and Pest results.
- **Bump `VERSION`** to `0.356.<iteration>`.
- **Non-boundary:** no contract-doc or Cross-Repo-block change (CRM stays v2.4.0). Confirm `FmContractVersionParityTest` still green if any Fleet file is incidentally touched (it shouldn't be).
- **Housekeeping inbox:** not drained this session; digest the capture buffer (`housekeeping-incoming.md`) per the standard gate.
- **Archive** `sessions/355. *.md` → `sessions/archived/`.
- **Commit on `session-356/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: **the picker is a thin UX layer over solved infrastructure** — a thumbnail grid, a search box, click-to-select, an upload button. Resist scope creep into metadata, folders/tags, or a `MediaLibraryPage` rebuild (all explicitly out of scope). Reuse the existing `useExistingImage` action and the `DedupPromptModal` patterns rather than inventing a parallel attach mechanism.
