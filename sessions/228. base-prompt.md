# Base Prompt — Session 228

We are about to begin a new session: **228. Widget Primitive — Phase 5b (Record-Detail Filament Mount + Ambient Wiring + IsView Primitive)**.

Before doing anything else:

1. Read `sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, and `sessions/template-session-log.md`. These files are the **canonical format reference** for all session documents. Do not infer format from previous session logs — the templates take precedence.
2. Read `docs/app-reference.md` for environment names, container names, view-to-file mappings, and key dependencies. Read `docs/schema/README.md` for the table index. This session does not add or remove any columns; no schema docs need updating.
3. Read the framing for this session:
   - `sessions/widget-primitive-migration.md`'s **Phase 5** entry (now expanded with the 5a–5d sequence and the **"a+" View framing** locked-in decision in the "Design decisions locked in" section, plus the **Page-as-View elective hook** in "Forward hooks").
   - The matching **Widget Primitive Track** entry in `sessions/session-outlines.md` — Phase 5 status block including the bullet for 5b. Also read the annotation on the **Template Editor — Record Sub-Navigation Refactor** stub further down in the outlines (the Status update at the top of that stub) — Part A folds into Phase 5c, so the sub-nav primitive 5c eventually builds is shared between record-detail and Templates/Themes.
4. Read the most recent session logs that establish related infrastructure this session lands on top of:
   - `sessions/227. Widget Primitive — Phase 5a (Typed Ambient Context Primitive) — Log.md` — establishes the typed ambient abstraction (`AmbientContext` + three subtypes) and the new `SlotContext(AmbientContext, bool)` constructor. This session opens the `RecordDetailAmbientContext` payload (was empty in 5a) and opens `RecordDetailSidebarSlot::ambientContext()`'s body (still throws after 5a).
   - `sessions/archived/211. Widget Primitive — Phase 2 (Slot Taxonomy) — Log.md` — `Slot` abstract + `SlotRegistry` singleton; the per-slot arg-shape variance pattern this session adds a third real implementation to.
   - `sessions/archived/212. Widget Primitive — Phase 3a (Dashboard Grid — Slot Plumbing & Hosted Render) — Log.md` — establishes `WidgetRenderer::render`'s `$slotHandle` parameter and the dashboard's Filament hosting via `DashboardSlotGridWidget`. This session's `RecordDetailViewWidget` is a near-mirror of that hosting pattern, with a different mount point (per-record-edit-page rather than panel-level) and a different ambient (record, not null page).
   - `sessions/archived/214. Widget Primitive — Phase 3c (Dashboard-Native Widgets) — Log.md` — establishes the `publicSurface: bool` gate on `SlotContext` and the hardcoded-array pattern for widgets in a Filament-hosted slot (the placeholder this session lands follows the same hardcoded shape, by design — config storage waits for 5c, mirror of how 215 replaced 214's hardcoded dashboard array with `dashboard_configs`).
   - `sessions/archived/210. Widget Primitive — Phase 1 Closeout — Log.md` (skim only) — establishes the **source-as-capability** pattern for `SOURCE_PAGE_CONTEXT` (closed token registry, contracts declare no `fields`, full token map returned). `SOURCE_RECORD_CONTEXT` introduced this session is a direct mirror.
5. Read the full state of every file this session edits:
   - `app/WidgetPrimitive/AmbientContexts/RecordDetailAmbientContext.php` — empty payload after 5a; this session adds `public readonly Model $record`.
   - `app/WidgetPrimitive/Slots/RecordDetailSidebarSlot.php` — `ambientContext(): SlotContext` body still throws after 5a; this session changes the signature to `ambientContext(Model $record): SlotContext` and opens the body.
   - `app/WidgetPrimitive/DataContract.php` — adds `SOURCE_RECORD_CONTEXT` constant alongside the existing source constants. Read for the constant naming convention.
   - `app/WidgetPrimitive/ContractResolver.php` — adds a `SOURCE_RECORD_CONTEXT` dispatch arm in the main `match`, calling the new `RecordContextProjector` with the ambient's record (or null if the ambient isn't `RecordDetailAmbientContext`, fail-closed).
   - `app/Providers/WidgetServiceProvider.php` — registers the new `RecordContextProjector` and `RecordContextTokens` singletons + the new `RecordDetailPlaceholder` widget definition.
6. Read every file this session **creates**:
   - `app/WidgetPrimitive/IsView.php` — new PHP interface declaring the View contract (handle, slot binding, widget set, layout config). Top-level in the namespace, sibling to `Slot` / `DataContract` / `AmbientContext`.
   - `app/WidgetPrimitive/Views/RecordDetailView.php` — first concrete `IsView` implementation. PHP class (not Eloquent yet — table lands in 5c), constructed with handle + record-type binding + widget array.
   - `app/WidgetPrimitive/RecordContextTokens.php` — closed token registry mirror of `app/WidgetPrimitive/PageContextTokens.php`. `TOKENS = ['record_id', 'record_type']` (deliberately minimal — `record_label` and any per-record-type tokens wait for the first widget that needs them, in 5d+).
   - `app/WidgetPrimitive/Projectors/RecordContextProjector.php` — mirror of `app/WidgetPrimitive/Projectors/PageContextProjector.php`. Receives the record from the resolver, returns the closed token map. Fail-closed when the record is null (every token returns empty string).
   - `app/Filament/Widgets/RecordDetailViewWidget.php` — Filament Widget hosting the slot on a record edit page. Mirror of `app/Filament/Widgets/DashboardSlotGridWidget.php`. Receives the record via Filament's standard `$this->record` mechanism on the parent EditRecord page; constructs the hardcoded `RecordDetailView`; loops widgets and calls `WidgetRenderer::render($pw, [], [], 'record_detail_sidebar')`.
   - `resources/views/filament/widgets/record-detail-view.blade.php` — Blade view for the Filament widget, mirror of `resources/views/filament/widgets/dashboard-slot-grid.blade.php`.
   - `app/Widgets/RecordDetailPlaceholder/RecordDetailPlaceholderDefinition.php` — new widget. `handle: 'record_detail_placeholder'`. `allowedSlots(): ['record_detail_sidebar']`. `acceptedSources(): [Source::HUMAN]`. `dataContract($config)` returns a `SOURCE_RECORD_CONTEXT` contract with no fields (per source-as-capability).
   - `app/Widgets/RecordDetailPlaceholder/template.blade.php` — minimal token render: `<div class="rd-placeholder">Record detail sidebar — {{record_type}} #{{record_id}}</div>`.
7. Read the Filament mount-point file the session edits:
   - `app/Filament/Resources/ContactResource/Pages/EditContact.php` — adds `getFooterWidgets(): [RecordDetailViewWidget::class]`. Read first to see the current state and confirm there's no existing override that conflicts.
8. Read related existing tests to anchor patterns:
   - `tests/Unit/WidgetPrimitive/AmbientContexts/RecordDetailAmbientContextTest.php` — established in 5a; this session adds payload assertions.
   - `tests/Unit/WidgetPrimitive/Slots/RecordDetailSidebarSlotTest.php` — currently asserts the throws-RuntimeException behaviour; this session replaces it with `ambientContext($record)` happy-path tests.
   - `tests/Unit/WidgetPrimitive/ContractResolverTest.php` — pattern for resolver-dispatch tests.
   - `tests/Feature/MemosDashboardRenderTest.php` — pattern for Filament-hosted-slot rendering tests (the `WidgetRenderer::render($pw, [], [], 'dashboard_grid')` shape this session mirrors with `'record_detail_sidebar'`).
   - `tests/Unit/Widgets/MemosDefinitionTest.php` — pattern for per-widget `allowedSlots` / `acceptedSources` / contract-shape unit tests.
9. Open `sessions/228. Widget Primitive — Phase 5b (Record-Detail Filament Mount + Ambient Wiring + IsView Primitive).md` and read the session prompt carefully.
10. Summarise your understanding and confirm you are ready to proceed.

---

## Process rules for every session

- **Before writing any code**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues, or any other infrastructure), ask the user what existing system handles it. This project has accumulated significant infrastructure — assume something already exists before building new. Do not implement from scratch until you have confirmed no existing system covers the use case.
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate` (or `migrate:fresh --seed` when appropriate). Do not pause to ask first. **This session writes no schema migrations.**
- **Update `docs/schema/`** after writing any migration. Not applicable this session.
- **If the PostgreSQL container becomes unresponsive** (500/exec errors), ask the user to restart it rather than retrying.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php -d memory_limit=2G vendor/bin/pest --exclude-group=slow`. The 2G memory override avoids the pre-existing mid-suite OOM fatal documented since session 212. Fix any failures before proceeding.
- **Front-end builds — when to run which:** Pest does not catch Vue/TS errors or SCSS compile failures, so any change to front-end source must be built before announcing completion. Decision rule: *if this change could affect what ends up in the public-site bundle at runtime, run `build:public`; if it's Vite-compiled admin/public/page-builder source, run `npm run build`; if neither, no build.* **This session is PHP + a single Blade view + an admin CSS rule** — no widget asset pipeline files, no Vue, no SCSS that the build server compiles. The placeholder widget's styling (if any) is a hand-edited rule on `public/css/admin.css` (served directly, no build), or inline. No build expected.
- **Tests for new behaviour:** every session that changes application behaviour should include tests. This session lands the `IsView` interface, the `SOURCE_RECORD_CONTEXT` source/projector/tokens trio, the placeholder widget, the `RecordDetailViewWidget` Filament host, and opens `RecordDetailSidebarSlot::ambientContext($record)` — every one of these gets test-locked.
- **Fast vs slow classification:** tests that individually take >5 seconds belong in a Pest `->group('slow')`. Everything else stays in the default (fast) suite.
- **Playwright artifact hygiene:** `resetDatabase()` fires once per spec in `beforeAll`; anything a test creates after that point persists into the next manual-test session. Delete out-of-band rows in `afterAll`.
- **Pause for manual testing.** Announce that testing is ready. Then stop. Do not suggest closing the session or take any further action.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Do not pipeline into these steps after tests pass or after manual testing is complete. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, Claude stops and waits. The user reviews the changes and brings up the next session's prompt when ready.

Claude then drafts the next session's documents:

- **Next session base prompt**: copy `sessions/template-base-prompt.md` to `sessions/229. base-prompt.md`. Expected next session: **Phase 5c — `record_detail_views` table + IsView registry + sub-nav rendering primitive (with Templates/Themes refactor folded in)**. The session lifts 5b's hardcoded `RecordDetailView` array into a `record_detail_views` table, introduces the registry that maps record types to their bound Views, builds the sub-nav rendering primitive (Filament's `getRecordSubNavigation()` driven by Views), retrofits `EditPageTemplate` and `EditContentTemplate` onto the same primitive (the absorbed Templates/Themes refactor), and lands an admin UI for authoring per-record-type View sets. This is the largest session in the Phase 5 arc by surface area.
- **Next session prompt**: draft `sessions/229. Session Title.md` using `sessions/template-session-prompt.md` as the format reference.

### Phase 2 — Close

When the user says to close (after the next session's prompt is agreed):

- **Session log**: write a log file at `sessions/228. Widget Primitive — Phase 5b (Record-Detail Filament Mount + Ambient Wiring + IsView Primitive) — Log.md`. Use `sessions/template-session-log.md` as the format reference — copy its structure exactly, do not base it on previous logs.
- **Update session-outlines.md**: mark Phase 5b complete. Update the bullet for Phase 5c to reflect any scope changes that surfaced during 5b.
- **Archive the previous session**: move all files matching `sessions/227. *.md` into `sessions/archived/`. Skip silently if those files don't exist or have already been archived.
- **Commit**: stage all changed files (including the log, updated outlines, and any files moved into `sessions/archived/`), commit on the current `session-228/N` branch, and notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

- Follow existing code conventions exactly.
- Do not add features, refactor, or improve anything beyond what the session specifies.
- Prefer editing existing files to creating new ones.
- No docstrings, comments, or type annotations on code you did not write.
- Simple, correct, well-considered solutions over fast or clever ones.
- When adding a new admin page or resource, create a help doc stub at `resources/docs/[handle].md` following the frontmatter convention of existing docs, and register the route(s) in the `routes:` array.
- Every new Filament page must include `getBreadcrumbs()` following the established pattern.
- **Portal security rule:** every portal route and query that reads contact data must be scoped strictly to the authenticated portal user's own `contact_id`. No exceptions without explicit design review.
- **Security over usability:** when they directly conflict, choose security. Fail-closed beats fail-open; explicit consent beats silent default; narrowly-scoped beats broadly-permissive.
- **No collapse-to-summary panels:** inspector/settings panels display fields directly; do not hide behind chevron disclosure toggles.
