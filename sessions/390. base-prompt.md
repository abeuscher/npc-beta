# Base Prompt — Session 390 (Donations Packaged)

**Start now.** Execute the reading list below, open the session prompt at `sessions/390. Donations Packaged.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **390. Donations Packaged**.

This is **arc position D3, second session of the block** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary). 389 opened the block with the carve: the Donations behavioral surface lives in `plugins/Donations/` (folder-sourced in `distribution.json`), surface 7 (email-template registry) and surface 10's donation slice (the cross-repo webhook inversion, Payments `v0.2.0` + the Events `v0.1.1` rider) are PROVEN, contract at 0.13.0. This session is the **package + squash-boundary redraw** — the 383 (P7, Events) playbook applied to Donations: `plugins/Donations/` becomes the composer package `nonprofitcrm/donations` (manifest source kind `folder` → `path`), and the **four donation tables leave core's schema dump** for plugin-owned byte-faithful create-table migrations, with both per-composition fresh-install identity checks run and clean. Extraction to `crm-plugin--donations` is the block's *next* session (391), not this one.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.13.0 → 0.14.0 at close: surface 5 gains its second squash-redraw consumer (the first with four tables and a core→core FK web around them), the Package shape section gains the fourth path-repo consumer.

**Session shape:** infrastructure, behavior-preserving, two-phase (package, then schema redraw + identity checks). Routes 272/272 byte-identical; every donation behavior net passes with assertions unchanged; the schema *moves* but no DDL byte changes (create-table migrations are authored byte-faithfully from the dump they replace). **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse — the composition-safety dispositions (decision 6 of the session prompt) are the flagged cluster.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D2 ✅, D3 block open) and § 6 disposition 7 (migrations vs. the squash discipline — this session's core mechanic) and § 6.7's events-owned schema annotation (the P7 worked example this session mirrors).
3. Read the 389 session log (`sessions/389. Donations Carved — Log.md`), especially its **block handoff** section — the four-table redraw input (owner map, FK order, constraints) and the composition-safety reaches list live there.
4. Read `docs/plugin-contract.md` at 0.13.0 — **surface 5 in full** (the squash boundary, install order, the two vanish semantics, the per-composition identity procedure, the core-model caveat — this session's spine), § Package shape (the P6 mechanics reused verbatim: explicit `version`, bound require, path repository, per-plugin Dockerfile manifest-COPY lines ×2 stages no glob, third-party-deps posture n/a here), § The distribution manifest (the `folder` → `path` walk and the same-commit guard interlock).
5. Read the **383 session log** (`sessions/archived/383. Events Vertical as a Package & Squash Boundary Redraw — Log.md`) — the package + squash-redraw playbook this session repeats: byte-faithful raw-DDL migrations, the identity-check runs, the schema:dump regeneration procedure (drop plugin tables first; recreate the *database*, not the schema), the composition-safety hunt (`PageContextTokens` was 383's find).
6. Open `sessions/390. Donations Packaged.md` — the redraw input, front-loaded decisions 1–8, and the Open-Gate item.
7. Read the four tables' DDL in `database/schema/pgsql-schema.sql` (`funds`, `donations`, `donation_credits`, `donation_receipts` — CREATE TABLE blocks, indexes, and the ALTER TABLE constraint statements) and the schema docs `docs/schema/{donations,funds,donation_credits,donation_receipts}.md`.
8. Read the fixtures that gain migrator paths this session: `tests/Fixtures/Plugins/DonationsRemovedTestCase.php`, `tests/Fixtures/Plugins/DonationsDisabledViaFlagTestCase.php` (their comments already say "no migrator path *this session*" — that caveat expires now), and the Events pair (`EventsRemovedTestCase`, `EventsDisabledViaFlagTestCase`) as the pattern.
9. Read the composition-safety reaches named in the session prompt's survey: `app/Services/Setup/SetupChecklist.php` (`checkDefaultFund`), `app/Services/RandomDataGenerator.php` (donation counts/creates), `app/Models/Contact.php` (the `donations()` relation, `hasDonations()`, the two donor scopes), `app/Filament/Resources/ContactResource.php` + `OrganizationResource.php` (donation reads in delete/scrub paths), `app/Filament/Resources/TransactionResource.php` (subject mapping), `app/WidgetPrimitive/ContractResolver.php` (the `fund` and `donation` arms), `app/Services/Import/{ImportSessionPreview,ImportSessionActions}.php` (model-type-gated reads).
10. Skim the standing guards this session touches: `tests/Feature/Infrastructure/PluginPackageGuardTest.php` (plugin-tables-out-of-the-dump pin + path-repo pinning), `tests/Feature/Infrastructure/DistributionManifestGuardTest.php` (the same-commit interlock this session's manifest edit must satisfy).
11. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 389

- **CRM contract at v2.7.0** — 389 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D2 complete; **D3 block open, carve done (389).** `plugins/Donations/` is the fourth composed plugin, source kind `folder` in `distribution.json`; the three extracted plugins are `crm-plugin--logo-garden` (v0.1.0), `crm-plugin--payments` (**v0.2.0** — the donation webhook branches inverted to `CheckoutSettled` / `InvoiceSettled` / `InvoicePaymentFailed` dispatches), `crm-plugin--events` (**v0.1.1** — listener metadata guard + removal-mirror assertion generalized). Plugin contract **v0.13.0**.
- **Surface 7 live:** core `App\Plugins\EmailTemplateRegistry`; `EmailTemplate::defaults()` resolves core array → registry → empty; the base seeder seeds one row per contribution; the two donation handles are the first contribution.
- **Schema baseline:** the four donation tables (`donations`, `funds`, `donation_credits`, `donation_receipts`) are **still in the core dump** — moving them is this session's Phase 2. No schema changes since 383.
- **Fast Pest suite green at 3309 / 0 parallel** (389 close — 3263 baseline + 46 additive). The `Donations` phpunit testsuite runs from `plugins/Donations/tests` (18 files, 99 fast tests + 1 slow). Expected delta this session: **additive** — identity-check artifacts and any composition-safety gate tests; every existing donation assertion unchanged.
- **Route baseline: 272 routes** (dev/test environment) — byte-identical this session (Phase 1 and 2 touch no routes).
- **The removal/flag fixtures carry an expiring caveat:** `DonationsRemovedTestCase` and `DonationsDisabledViaFlagTestCase` register no migrator path because schema is core — after Phase 2 they MUST register `plugins/Donations/database/migrations` (the Events fixture pattern) or every data-kept assertion fails on a fresh test database.
- **Playwright suite** — CI signal; the known parked chrome-parity failure stands (pre-existing, non-gating). No local Playwright.
- **Housekeeping inbox** — the capture buffer holds two 389 items (`[plugin-arch]` tokenHint + portal_form_collision); infrastructure shape absorbs no inbox items.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Checkout, acknowledgment + idempotency, year-end receipts, recurring donations, soft credits, the importer, admin pages, both widgets — all pass with assertions unchanged. Routes 272/272. No visual change. The schema redraw is representational: same DDL bytes, different owner.
- **Byte-faithful DDL.** The plugin's create-table migrations are authored from the dump DDL they replace, as raw `DB::unprepared` statements — pg_dump round-trips its own output, so the identity check diffs clean; schema-builder calls risk expression-normalization drift (contract surface 5's recorded rationale). FK creation order: `funds` → `donations` → `donation_credits` → `donation_receipts`.
- **Both identity checks are verified runs, not assumptions** (the 383 procedure, recorded in contract surface 5): (a) full composition — fresh install dumps byte-identical to the pre-redraw reference modulo the plugin's `migrations` rows + sequence + pg_dump's `\restrict` token; (b) donations-absent composition — core dump + seeders boots clean, public + admin smoke pass, none of the four tables present. Regenerate the core dump only against a database whose plugin tables were dropped first; recreate the *database*, never `DROP SCHEMA public`.
- **Lockfile changes ship in the same commit as their composer.json changes**; the manifest constraint and composer require move together (`DistributionManifestGuardTest` enforces agreement — the 386 interlock).
- **One Dockerfile COPY line per packaged plugin per stage — never a glob** (the P6 caveat). Composer-before-artisan at any rebuild step (the 387 caveat).
- **Update `docs/schema/`**: the four table docs gain the plugin-owned annotation (the 383 events precedent — annotated, not deleted; they document live tables wherever the DDL lives).
- **The dump-regeneration commit and the migration-authoring commit are one commit** — the guard pinning plugin tables out of the dump must never see an intermediate state.
- **Scope is binding.** No extraction (391), no `crm-plugin--donations` repo, no model/factory moves (§ 6.7 stands), no events-handle migration, no D4+ work, no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.13.0 → 0.14.0 (surface 5 second consumer; Package shape fourth consumer); plan doc § Stage D D3 line annotated (package done); block-handoff notes in the log for the extraction session (391 runs the runbook — note anything the redraw surfaced that gate 4/5 will care about; DonationForm's js asset path goes vendor-relative at 391, not now).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the session prompt's survey still matches the code, state the plan, note any drift found — and resolve the Open-Gate item: **session 389 is merged to main and this branch starts from it** (the carve, the Payments v0.2.0 lock pin, and the Events v0.1.1 lock pin are all inputs to this session; `git log` must show `7d2dc087` reachable). Review the front-loaded composition-safety dispositions (session prompt decision 6) — they are the one expensive-to-reverse cluster; flag disagreement now. Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
