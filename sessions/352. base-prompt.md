# Base Prompt — Session 352 (Fleet Data Hygiene — Phase 1: Prevention + Detection)

**Start now.** Execute the reading list below, open the session prompt at `sessions/352. Fleet Data Hygiene — Prevention & Detection.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **352. Fleet Data Hygiene — Phase 1: Prevention + Detection (CRM).**

This is the **first phase of the Fleet Data Hygiene track** (`sessions/tracks/fleet-data-hygiene.md`), the owner's named **pre-release priority** — scoped at session 349 off the orphaned-event-landing-pages bug, **not started** until now. The track prevents and detects the silent accumulation of derived/cruft data on live instances (orphan event/post landing pages, leftover scrub data, media creep). It has three phases; **this session is Phase 1 only** — the in-repo CRM half: a CI keystone "scrub-wipe leaves no residue" **prevention** test (the launch gate) + a node-local `app:data-hygiene` **detection** audit. Phase 2 (the count-only `/api/health` subcheck + the FM maintenance toggle) and Phase 3 (bounded remediation) are **fenced out** — they are the boundary-touching / future phases.

This session is **NOT boundary-touching** — no Fleet Manager surface, no `/api/health` change, no contract bump. CRM contract stays **v2.3.0**. The privacy boundary that governs the *later* FM phase is load-bearing context (FM must never be *built* to read raw node data; counts only over the wire; deep audit is node-local + consent-gated) — but **nothing in Phase 1 touches the wire**: the audit command is node-local artisan, the CI test is a fresh-DB invariant. **A migration is not expected** — the audit reads existing columns (`pages.source` / `events.source`, `type`, Spatie media ownership); confirm at the gate.

**Session shape:** track-phase (stub-driven, no release-plan entry — the track doc is canonical). The win: a CI invariant that **fails the build** if any future code generates scrub/derived data without cleaning it up, plus an artisan audit that reports what cruft has *already* piled up on a given box (so the owner can run it against the live instances and clean with the existing prune commands). May run **1–2 sessions** — if Phase 1 splits, the audit command and the CI test are independent natural commits.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — the canonical format reference. This is a **stub-driven** session, so use the fuller session-prompt shape (explicit phases, out-of-scope).
2. **Read the track doc `sessions/tracks/fleet-data-hygiene.md` carefully** — it is canonical for this session: the premise, the **privacy boundary** (governs the later FM phase, not this one), the **prevent-vs-detect split** (the key framing), the **component design** (A = CI keystone test, B = node-local audit + count-only signal, C = FM visibility — C is out of scope), and the **sequenced forward plan** (Phase 1 = A + B). Read the **Fleet Data Hygiene** stub in `sessions/session-outlines.md` (~line 57) for the one-paragraph roll-up.
3. **The down-payment already shipped (session 349)** — read so you build on it, not over it: the *cause* fix (event landing pages inherit the event's `source`; `EventObserver::deleted` cascades the landing page) and the two prune commands. Read `app/Console/Commands/PruneOrphanEventPagesCommand.php` and `app/Console/Commands/PruneOrphanMediaCommand.php` (the cleanup half — the audit *detects*, these *delete*; both are dry-run-default, `--force` to act). Skim the event-page section of `sessions/349. Table Widget — Log.md` (in `sessions/archived/` if rotated by the 351 close).
4. **Prevention (component A):** read `app/Services/RandomDataGenerator.php` — it already has `generate(array $counts)`, `wipe()`, `scrubCounts()`, and `wipeScrubPages()`. The CI keystone test is: snapshot every relevant table's row count + media count → `generate()` a full scrub spread → `wipe()` → assert **every count returns to baseline and zero derived pages/media remain.** Understand exactly what `wipe()` does and does not clean today (it is the system under test — the test must catch a *future* leak, so don't paper over a current one; surface it if `wipe()` already leaves residue).
5. **Detection (component B):** the new `app:data-hygiene` command. Read an existing artisan command for the house conventions (the two prune commands above; `app/Console/Commands/` generally for the signature/output/`--force` idiom). The audit reports, for the box it runs on: orphan event pages (`type=event`, no backing event), any `source=scrub_data` records still present (pure cruft on a live box — **note the enum value is `scrub_data`, not `scrub`**, per `Page::ACCEPTED_SOURCES` / `Event::ACCEPTED_SOURCES`), orphan media (reuse `PruneOrphanMediaCommand`'s detection), media rows whose owning model is gone. It splits two ways: a **count-only** computation (the safe signal the future health subcheck will read — build it as a reusable method) and a **deep mode** (actual records — node-local; `--deep` or similar).
6. **Phase-2 context only — do NOT modify:** read `app/Http/Controllers/Api/Fleet/HealthController.php` to see the subcheck shape the count-only computation will *later* slot into (Phase 2). This session does **not** touch it, the contract, or VERSION's contract meaning — it only ensures component B's count-only method is shaped to be consumable later.
7. Read `docs/schema/pages.md` + `docs/schema/events.md` for the `source` / `type` columns and `docs/schema/README.md` for the media tables. Read `docs/app-reference.md` for the artisan/container conventions. Note drift vs this prompt; proceed unless a decision is needed.

---

## Starting state inherited from session 351

- **CRM contract at v2.3.0** — unchanged. 351 shipped the **SEO features** (the feature half of the SEO cluster, now closed): the custom JSON-LD slot augmenting `SeoMetaGenerator` (site-wide `SiteSetting` + per-page `custom_fields['json_ld']`, JSON-validated + `JSON_HEX_TAG`-escaped, emitted as separate `ld+json` blocks); the blockquote control was confirmed already-shipped (drift) and locked in with render-shape tests; responsive image `sizes` now derived from the grid column fraction at the `PageBlockRenderer` seam. Non-boundary, no schema. An off-topic Hero `min-height` "Small" fix also landed (the generic `widget--hero` wrapper was pinned by a base `min-height` that's now removed; SCSS-only + `npm run build`).
- **Schema baseline:** no migration at 351; `database/migrations/` empty post-348-squash. **Phase 1 expects no migration** — the audit is read-only over existing columns; the CI test runs on a fresh test DB.
- **Fast Pest baseline:** the **351 close** number (~2895 / 0 — confirm at session start). **New Pest expected:** the CI keystone scrub-residue invariant (likely `->group('slow')` if `generate()`+`wipe()` exceeds the 5s fast-suite ceiling — classify at implementation) + targeted cascade tests (delete-model→media etc., extending the 349 delete-event→page cascade) + the `app:data-hygiene` audit's count computation.
- **Playwright:** 351's standing specs intact. No new Playwright expected — this is artisan/service-layer work, verified by Pest. Run Pest and Playwright **sequentially** if you run Playwright at all.
- **Planning state:** the **SEO cluster is closed** (350 cleanup + 351 features). The **Fleet Data Hygiene track** (`sessions/tracks/fleet-data-hygiene.md`) is now the active pre-release priority; this session opens it at Phase 1. No release-plan entry — track-driven.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — this is **track/feature work, not a housekeeping batch**, so it does **not** drain the inbox (the inbox is over its batch threshold and wants its own session at some point, but not this one).

---

## Process rules for every session

Base-prompt process rules apply in full. Session-specific:

- **The privacy boundary is load-bearing but applies to the FM phase, not this one.** Phase 1 is entirely in-repo: a fresh-DB CI test and a node-local artisan audit. Do **not** build any FM-facing surface, `/api/health` subcheck, or anything that puts node data on the wire — that is Phase 2 and crosses the boundary (contract bump + cross-repo coordination). Shape component B's **count-only** computation as a reusable method so Phase 2 can consume it without a rewrite, but stop at the CLI boundary this session.
- **The audit detects; the prune commands delete.** Reuse the existing `PruneOrphanEventPagesCommand` / `PruneOrphanMediaCommand` detection logic rather than re-deriving it; `app:data-hygiene` is **read-only visibility** (count-only + deep-record modes), it reclaims nothing. Cleanup stays in the prune commands (opt-in, `--force`).
- **The CI test must catch a *future* leak, not paper over a current one.** If `RandomDataGenerator::wipe()` already leaves residue today, the keystone test will fail on the existing state — surface that as a finding and fix the leak (it's in-scope: the test is only a launch gate if `wipe()` is actually clean). Don't weaken the assertion to pass.
- **No migration, no contract bump.** If the work appears to need a schema column (e.g. to track something the audit can't compute from existing data), stop and surface it at the gate — the expectation is read-only computation over `source` / `type` / media ownership.
- **Settle the open questions at session start** (CI keystone test scope + fast/slow classification; the audit command's count-only/deep split + output format; whether to run the audit against the live boxes this session or hand the command to the owner) — log them.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a one-paragraph orientation and **wait for the owner to confirm before starting.** Bring: (1) confirmation this is **Phase 1** of the Fleet Data Hygiene track — the in-repo **prevention** (CI keystone scrub-residue test) + **detection** (`app:data-hygiene` audit) half, **non-boundary**, no `/api/health` change, no contract bump, no migration expected, building on the 349 down-payment; (2) the **prevention plan** — the keystone test's shape (baseline-snapshot → `generate()` full spread → `wipe()` → assert zero residue + counts restored), the targeted cascade tests it pairs with, the fast/slow classification, and **whether `wipe()` is clean today** (a current leak is an in-scope fix, not a reason to weaken the test); (3) the **detection plan** — what `app:data-hygiene` reports (orphan event pages, residual `scrub_data` records, orphan media, dead-owner media), the **count-only vs deep-mode split** (count-only shaped as the reusable method Phase 2 will consume), the output format, and reuse of the prune commands' detection; (4) the **one decision for the owner** — run the audit against the live instances this session, or ship the command and hand the live run to the owner. One confirmation, then proceed.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below happens only when the owner explicitly says to close.** Follow the full close gate. Session-specific:

- **Log file:** `sessions/352. Fleet Data Hygiene — Prevention & Detection — Log.md` — the CI keystone test (what it asserts, fast/slow, any `wipe()` leak found + fixed), the `app:data-hygiene` command (modes, output, what it found locally / on live if run), the cascade tests added, Pest results, and the count-only method's shape (the Phase-2 seam).
- **Bump `VERSION`** to `0.352.<iteration>`.
- **Track doc:** update `sessions/tracks/fleet-data-hygiene.md` status snapshot (last-update date, Phase 1 progress). **If 352 *completes* Phase 1** (both the CI test and the audit shipped), it is the terminal session of Phase 1 within the track — do the **phase-expiry compression** (lift Phase 1's detail into a Phase Retrospectives entry; keep the roadmap roll-up one line). If Phase 1 spills to a second session, bump the snapshot only.
- **Inbox:** not a housekeeping batch — **does not drain the inbox**. (Note any data-hygiene-adjacent inbox/outline items the work touched, but don't fold.)
- **Archive** `sessions/351. *.md` → `sessions/archived/`.
- **Commit on `session-352/N`, notify, do not push.**

---

## Style rules

Base-prompt style rules apply. Session-specific: build the audit logic **once** as the shared core (the track's load-bearing insight — one computation powers the CLI report now, the count-only health subcheck later, the gated deep mode). Reuse the existing prune-command detection and `RandomDataGenerator` surfaces; don't re-derive. Keep the CI test high-leverage and future-proof (one invariant that catches the whole class of "generates-but-doesn't-clean" code, not just today's bug). Don't cross into the FM/boundary phase; don't add a migration; don't weaken the residue assertion to make it pass.
