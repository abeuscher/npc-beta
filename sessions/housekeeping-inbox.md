# Housekeeping Inbox

Small items noticed between sessions. One bullet per item, free-form text. When the inbox accumulates 5–10 items, batch them into a housekeeping session (e.g. Phase E's E12 "Housekeeping Batch 2"). Items that grow into "own session" shape get promoted to `release-plan.md` entries.

This file is the only home for items that are *small enough to bundle but large enough to break scope when absorbed mid-session by a feature*.

**How items get here:** capture them mid-session with `npm run logbug -- "what you noticed"` — it stamps the item with the current VERSION marker + date and parks it in `sessions/housekeeping-incoming.md`. The next session close digests that buffer (validity-checks each item with the user, then folds the survivors here with their capture stamp intact). Items added by hand at a session close should carry the same `[VERSION · date]` stamp so their age stays legible.

**Verify the premise before scheduling (the staleness guard).** A captured item is a snapshot of the app at capture time; the app moves underneath it. When walking this inbox at a session start to pick a batch, **re-verify each candidate against current code before committing it to an iteration** — the older the capture stamp, the more important the check. Items frequently turn out already-fixed, already-addressed by an intervening feature, or redundant with a system that landed since capture. Catch that at the walk, not mid-implementation. Drop or re-scope dead items as you find them.

---

## Inbox

*(Items destined for the next housekeeping batch session. Items default to **pre-Public-Website-Complete** unless tagged `[post-milestone]` — see Rule 12 in `release-plan.md`.)*

- `[post-milestone]` Random Data Generator widget: add Organizations to the entity-generation list. *(Admin tool — doesn't block the public website demo.)*
- Logo widget: text field → rich text + appearance control (small scope; just enough to set color / weight / size). **s315 finding (examined, not taken — needs a design call before it's housekeeping-sized):** neither Quill toolbar in this app (admin or page-builder inspector) exposes colour / size / font-weight, so "rich text" alone does not deliver the three named controls — they have to be discrete appearance fields. And an explicit colour control collides with what s314 shipped (the wordmark inherits a full-bleed hero's text colour via `color: inherit`); its default would have to be either a concrete hex — which silently overrides s314 — or "inherit", which bends the concrete-values rule. Minimal non-regressing version is weight + size selects only, leaving colour to the hero-inheritance; the colour picker reopens the G12 hero↔nav contrast coupling. Resolve the shape with the designer before scheduling.
- Link-color mechanism consolidation (s290): there are now **three** link-recolor mechanisms and they need a clarity pass. (1) Nav widget's own `--nav-link-color` (Nav inspector, sets its own link color). (2) Hero's `nav_link_color` / `nav_hover_color` reach-over — a Hero knob that restyles the *site-chrome nav* (a sibling widget) but only when `overlap_nav: true`; effectively dead on this site because G12 forced overlap off everywhere (logo-contrast). (3) New generic `text.link_color` → `--np-link-color` (s290 G22 resolution) — recolors content links inside any widget; does NOT reach the nav. These are not duplicates (they target different elements), so the s290 link-color work did **not** make #2 removable — #2 is the real smell (a widget reaching across to restyle another widget), and that decision belongs to **G12** (Hero↔Nav overlap-nav / logo-contrast coupling), not to the link-color feature. Also a real UX-confusion point: a Hero inspector showing both "Link Color" (#3, hero content) and "Nav Link Color" (#2, site chrome, overlap-only) will confuse operators — fix is labeling + the G12 overlap decision. Cross-ref G12/G21/G22 in `sessions/public website/gap-report.md`.
- Widget non-colour hardcoded values (s300 survey — scope-fenced, deliberately not migrated): rgba shadows / gradient stops / opacity tints — BlogPager (`--color-surface-hover` hover tint), MapEmbed + SocialSharing (rgba scrims under the kept `#fff` accents), Nav (dropdown `box-shadow` rgba), PricingChart (`rgba(0,0,0,.08)` hairline), ProductCarousel (fade-gradient `#000000` stops + rgba placeholder/mask). Not in the `--np-color-*` contract by design; held as deliberate exceptions in `WidgetColorTokenConsumptionTest`'s allow-list. Revisit only if a shadow/gradient token vocabulary is ever introduced.
- Per-widget colour-picker PHP schema defaults don't flow from the theme (s300 survey): BoardMembers (`pane_color #ffffff`, `border_color #cccccc`), LogoGarden (`container_background_color #ffffff`), Nav (`link_color #1d4ed8`, `drop_link_color #1d4ed8`, etc.) — concrete hex defaults in the widget Definition PHP, so a fresh widget instance ignores the Theme palette until the operator picks a colour. s300 tokenised the unreachable SCSS dead-fallbacks (zero render change); the *schema defaults themselves* are appearance-config territory and 301/scheme-adjacent, not s300's SCSS-consumption scope. Decide later whether per-widget pickers should default to a token rather than a literal.
- Contact hero readability overrides (s290, related): the hero also stacks two coexisting "make content readable on the gradient" workarounds — the WebForm widget's white-card `background.color` (G21 form-label workaround) and the email-fallback widget's `text.link_color: #ffffff` (G22 resolution). Both work side by side, not urgent. Same housekeeping pass: once G21 lands the same `--np-*-color` var pattern the white card may be unnecessary; either way annotate the remaining overrides in `sessions/public website/contact.json` so a future editor understands *why* the hero carries them.
- `[test-integrity]` A005 (Test Audit Cycle 1, D2 spec-claim integrity) — **drained the s296 Tier-3 + incidental backlog.** Dispositions applied:
  - `DonationCheckoutTest` "creates a pending donation record on checkout initiation" — **deleted** (factory-echo: posted to the checkout route but discarded the response, then asserted a factory's own attributes; the amount/type/status round-trip is already covered by "stores one-off donation type correctly"). **Forward-queue Pest gap:** the real checkout-initiation controller path (create-pending-then-delete-on-Stripe-failure) is unverified — add a Pest case (mocking the Stripe boundary) when a donations session next opens.
  - `QuickBooksSyncTest` "creates a sales receipt…" / "creates a refund receipt…" — **renamed** to "syncs an inbound transaction via createSalesReceipt and records the returned quickbooks_id" / "syncs an outbound refund via createRefundReceipt and records the returned quickbooks_id" (the QB client is mocked; bodies assert job routing + id persistence, not receipt creation). `QuickBooksCustomerMatchingTest` needed no change — its "attaches CustomerRef…" titles already match their bodies (the s296 lumping was imprecise).
  - `ProductCarouselTest` "seeder total widget count includes product_carousel" — **deleted** (magic-count `toHaveCount(39)` change-detector; the product_carousel-presence claim is already proven by "seeder creates product_carousel with correct config schema").
  - `tests/e2e/page-builder/full-width-matrix.spec.ts` "every widget handle renders without console errors" — **no action; already resolved** (the smoke test was deleted in A003).
  - `tests/e2e/page-builder/layout-inspector.spec.ts` "…persists background + padding" — **renamed** to "drives the three-tab layout inspector and writes background + padding to the layout appearance config" (drives the inspector UI + asserts DB config; no render round-trip).
  - `TaxReceiptTest` "includes only active donations in receipt calculation" / "excludes donations from other tax years" — **rewritten** to route through the real `DonorsPage::buildBreakdown()` (the in-file reflection idiom) instead of re-implementing the `Donation` query inline; they now verify the active-status + tax-year filters on the production receipt path.
  - Forward-scan find (Phase 4): `tests/e2e/dashboard-settings/dashboard-settings.spec.ts` "loads, adds a widget, and the DB reflects the new arrangement" — **renamed** to "loads the dashboard arrangement view with the expected widgets and no preset/spacing tabs" (the body never added a widget; it asserts the load + tab absence + unchanged count).
- Columns-primitive alignment tools — labels or presets (s313 close): the vertical / horizontal alignment controls inside the columns primitive are powerful but obtuse to non-technical users. Either improve the labels and inline hints, or add a preset dropdown alongside the raw controls (e.g. "Center content / Top-align / Distribute evenly") that resolves to the same underlying values. UX polish; the underlying mechanism stays as-is. **s317 (E19 usability sweep):** examined; owner deferred and confirmed the chosen solution is the **preset-dropdown shape (b)** (planned work to settle sensible defaults), NOT relabeling — so the raw CSS labels stay as-is until that lands. Item kept open for the preset work.
- `[test-integrity]` `[0.314.01 · 2026-05-22]` Page-builder ordered/unordered list rendering — add a Playwright check that pins the correct visual appearance of `<ul>`/`<ol>` lists in the page builder. The user reports it breaks regularly, so it needs a standing regression guard. Browser-only (visual rendering), so a legit e2e candidate; author it local-first per the new `docs/testing/playwright-discipline.md` (A005). Captured mid-s315 via `logbug`.
- `[test-integrity]` ~~e2e `tests/e2e/event-registration/ticket-tier-picker.spec.ts` describe-skip disposition~~ — **resolved at A005 (deleted).** See the A005 D1 deletion record below for the coverage tradeoff; the CI-only instability (per-test fresh published event hitting an un-warmed public page that depends on the unbuilt `event_registration` widget bundle) was the unfixable-without-architectural-change root cause, so per user policy it was deleted rather than re-quarantined.
- `[test-integrity]` A003 deleted the first test of `tests/e2e/importer/site-import-export.spec.ts` — "renders both sections and surfaces snapshot counts on Export Site". Surfaced via the A003 diagnostic digest: after fixing the spec's URL (it navigated to `/admin/site-import-export` but Filament's auto-slug for `SiteImportExportPage` is `/admin/site-import-export-page` — that URL fix is permanent), the click on the Export Site action button correctly mounted the action server-side (the modal HTML rendered into the DOM with the right live snapshot counts: 15 pages, 6 templates, theme (2 design keys), 44 media files), but Alpine never flipped the modal's `isOpen` state to true in headless chromium so the modal stayed hidden. The same `requiresConfirmation()` pattern is exercised cleanly by the importer approve table-action test and by Resource ListPage `->form([...])` header actions (the Import Bundle action on `/admin/pages`). Only the **header-action `requiresConfirmation()` modal on a custom `Filament\Pages\Page`** combination fails in CI — likely belongs to Filament's open-modal dispatch wiring on custom pages but the diagnosis was out of scope for a stabilization session. The companion second test in the same file ("Import Site analyzes the bundle on upload and reveals gated toggles", which exercises the `->form([...])` Import Bundle action, mirroring the working `content-bundle-two-step.spec.ts` shape) is kept. Coverage tradeoff: loses e2e coverage of the Export Site confirmation modal's snapshot-count rendering; the server-side `SiteImportExportPage::getSnapshotCounts()` calculation is straightforward and could carry a one-test Pest covering page/template/media counts if a future session wants the floor.
- `[test-integrity]` A003 deleted five additional flaky e2e tests after surfacing the full flake history across six CI runs on `session-A003/2`. User stance (recorded in the A003 session log) is delete-by-default for any spec that intermittently fails without a working fix; per-test waitForTimeout bumps are not the answer when CI variance is the root cause. The deletions:
  - `tests/e2e/page-builder/full-width-matrix.spec.ts` — removed the `Editor — column bg vs content toggle independence` describe block (lines 318-368). 33% flake rate across the A003 runs on the `(bg:false, content:false)` Editor variant. Coverage tradeoff: page-builder Vue editor's column-layout container/grid class rendering is no longer e2e-verified per state, but the public-side matrix in the same file (`Public-side — column-layout full-width matrix`, plus the `Editor — column bg parity with public side` block at line 245) still covers the rendered output paths.
  - `tests/e2e/importer/events-update-strategy.spec.ts` (whole file) — single-test file. The 120s `import-progress-phase-done` wait was just barely sufficient and tipped over in one A003 run. Coverage tradeoff: re-import-with-update-strategy for the events importer is no longer covered at e2e level; the same workflow shape is covered for contacts / donations / memberships / invoice-details in their respective `*-update-strategy.spec.ts` files, and the events import happy-path stays covered by `tests/e2e/importer/events-happy-path.spec.ts`.
  - `tests/e2e/importer/content-bundle-two-step.spec.ts` (whole file) — single-test file. FilePond `processing-complete` poll just exceeded its 60s timeout. The `ImportBundleAction` it exercises is the **same shared action class** invoked from `tests/e2e/importer/site-import-export.spec.ts` (the kept "Import Site analyzes the bundle on upload" test) — same analyzer flow, same gated-toggle reveal, same data-testid surface — so no actual coverage is lost.
  - `tests/e2e/importer/donations-happy-path.spec.ts` (whole file) — single-test file. The A003 run-5 failure was actually in the shared `resetAndLogin` auth helper's `/admin/login` navigation (45s timeout, not retry-recovered), but the user noted the spec has been chronically flaky since it was added. Coverage tradeoff: donations import end-to-end is no longer covered as a dedicated happy path; `tests/e2e/importer/donations-update-strategy.spec.ts` still exercises donations import (with the re-import-with-update-strategy flow), and `tests/e2e/importer/donations-error-report.spec.ts` covers the error reporting path.
  - `tests/e2e/importer/donations-mapping-indicator.spec.ts` — removed only the first test ("rows render incomplete by default and flip to complete when mapped"). The second test in the file ("mapping select exposes a search input when opened") is kept — it never flaked across the six A003 runs and covers a distinct UI behavior. Coverage tradeoff: the row indicator's incomplete → complete visual transition during column mapping is no longer e2e-verified; the underlying mapping logic is covered by all the importer happy-path and update-strategy specs that complete a full mapping.
- `[test-integrity]` A005 (Test Audit Cycle 1, D1 Playwright spec discipline) deleted ten redundant importer e2e specs whose **data** assertions duplicate service-layer Pest coverage. The browser-only contract (the Filament/Livewire wizard UI — FilePond, Choices.js mapping search, multi-step nav, progress polling, download buttons, the importer approve modal) is preserved by the kept set: `contacts-happy-path` (full wizard flow), `contacts-update-strategy` (two-pass + approve modal), `contacts-duplicate-header` (review-step findings UI), `contacts-pii-rejection` (PII phase + download), `donations-error-report` (error table + download), `donations-mapping-indicator` (Choices.js search), `site-import-export` (FilePond + manifest + gated toggles). The deletions, with the Pest coverage that absorbs each:
  - `events-happy-path.spec.ts` — events / registrations / transactions / id-maps / tags covered by `tests/Feature/ImportEventsTest.php`; clean-shape import count by `ImportFixtureRunnerTest` (events × clean). Admin events-table title render duplicated by `contacts-happy-path`'s table-render check.
  - `invoice-details-happy-path.spec.ts` — line-item grouping into one Transaction's `line_items` JSON covered by `ImportFinancialTest` ("collapses multiple line-item rows…"); count by FixtureRunner (invoice_details × clean).
  - `invoice-details-update-strategy.spec.ts` — invoice update-strategy staging + approval covered by `ImportSession194Test` (invoice-details commit/dry-run); the approve-action UI is retained in `contacts-update-strategy`.
  - `invoice-details-grouping.spec.ts` — line-item **ordering** covered by `ImportFinancialTest` (indexed `line_items[0/1]` assertions). **Coverage gap:** the second test's **first-row-wins parent-field conflict resolution** (conflicting `payment_method` within an invoice group → first row wins) has **no Pest equivalent** — `ImportEventsTest` covers fill-blanks (blank→filled) but not conflict resolution. Flagged below as a forward-queue Pest gap.
  - `memberships-happy-path.spec.ts` — membership + auto-created tier covered by `ImportFinancialTest` ("creates a Membership with auto-created tier"); count by FixtureRunner (memberships × clean).
  - `memberships-update-strategy.spec.ts` — membership update-strategy staging covered by `ImportSession194Test` (memberships dry-run/commit).
  - `donations-update-strategy.spec.ts` — donations commit-with-update-strategy + approval covered by `ImportSession194Test` ("donations: commit with update strategy stages; approval applies").
  - `organizations-happy-path.spec.ts` (was `@on-demand`) — custom-field / tag / note polymorphic morphs covered comprehensively by `ImportOrganizationsTest`.
  - `organizations-update-strategy.spec.ts` (was `@on-demand`) — fill-blanks update + rollback covered by `ImportOrganizationsTest` ("stages a fill-blanks-only update…", "rolls back an organization session…").
  - `notes-happy-path.spec.ts` — note creation + attachment-to-contact covered by `NotesImporterTest` ("creates a Note attached to the matched Contact for each row", "stages updates on second pass…"); the notes **approve** path covered by `ImportSessionActionsTest` ("approve marks a note session approved…"). No unique browser affordance — the generic wizard is proven by `contacts-happy-path` and the approve modal by `contacts-update-strategy`.
- `[test-integrity]` A005 D1 — **forward-queue Pest gap (promote when a financial-import session next opens):** invoice-details importer's **first-row-wins parent-field conflict resolution** is unverified after the `invoice-details-grouping.spec.ts` deletion. Add a Pest case to `tests/Feature/ImportFinancialTest.php` asserting that, given two rows in one invoice group with conflicting parent fields (e.g. `payment_method` Card vs Cash), the first row's value wins on the resulting Transaction. Low-effort, deterministic, service-layer; out of A005's no-new-tests scope by design.
- `[test-integrity]` A005 D1 — deleted `tests/e2e/page-builder/inline-formatting-toolbar.spec.ts` (was entirely `describe.skip`'d since s306, parked for the cold-SCSS render cause that A003 resolved). Per the delete-by-default `describe.skip` policy, removed rather than re-quarantined. Coverage tradeoff: the inline formatting toolbar's e2e behavior (toolbar mount/dismiss, bold-toggle persistence, corner-affordance suppression while editing, link-popover target+rel save) is no longer browser-verified; `inline-editing-foundation` + `inline-editing-phase2` retain the page-builder inline-edit interaction + round-trip coverage. If the formatting toolbar regresses in regular work, that's the forcing function to re-author a spec for it (local-first per the new discipline doc).
- `[test-integrity]` A005 D1 — deleted `tests/e2e/event-registration/ticket-tier-picker.spec.ts` (was `describe.skip`'d at the e2e-stabilization handback). Closes the standing inbox entry above (the "look at it next housekeeping session and probably delete it" lean) — deleted per user policy. Coverage tradeoff: the multi-quantity event-registration spinner's *e2e* coverage (C2a, s279) is dropped; the spinner's service/feature-layer logic remains Pest-covered. The CI-only instability (per-test fresh published event + page hitting an un-warmed public page that depends on the `event_registration` widget bundle the isolated stack never builds) was the unfixable-without-architectural-change root cause.
- `[0.315.01 · 2026-05-22]` Standalone **button-row widget** — either extend the text widget's button affordance or (preferred) add a standalone button-row widget; the standalone form is the more useful/reusable shape. (Captured mid-s316 via `logbug`; the text-block widget already exposes an "Add Button" affordance, so this is the standalone-widget version, not a "text has no buttons" gap.)
- `[0.315.01 · 2026-05-22]` Remove **Laravel Debugbar** — it's installed (visible in admin) but has never been used. (Captured mid-s316 via `logbug`. Verify it's the dev-only `barryvdh/laravel-debugbar` and pull the dependency + any config.)
- `[0.316.01 · 2026-05-22]` **Unquoted `url()` breaks `background-image` for filenames with parens/spaces** — the public renderer emits `background-image:url(...)` without quotes, so a media filename like `photo (1).jpg` or `BACK_TO_THE_ODYSSEY_(Unsplash).jpg` produces invalid CSS and the background silently fails to paint. Affects original pages, not just duplicates (found while verifying s316 record-duplicate). Fix is to quote/escape the URL in the appearance style composer (and check the same emission in `AppearanceStyleComposer` / widget templates). Public-site-wide, pre-existing.
- `[0.317.01 · 2026-05-22]` (E19 deferral) **Nav landmark semantics** — `resources/views/components/site-header.blade.php` wraps the public nav in `role="group"` rather than a `<nav>` landmark. Functional but not a navigation landmark for assistive tech. Minor; swap to `<nav>` (or add `role="navigation"`).
- `[0.317.01 · 2026-05-22]` (E19 deferral) **Page form terminology overlap** — "Page Type" / "Page Template" / "Content Template" on the page create/edit forms (`PageResource` + `EditPageDetails`) read as near-duplicates to operators; "Content Template" helper "applied once at creation" is vague. Copy/labeling pass, not mechanism.
- `[0.317.01 · 2026-05-22]` (E19 deferral) **Duplicate-action consistency** — the **Duplicate** affordance exists on Pages/Posts/Events/Products/Contacts (s316) but not on Organizations / Templates / Navigation / Mailing Lists. Decide which resources should have it for a uniform operator expectation (cross-ref the s316 record-duplicate work).
- `[0.317.01 · 2026-05-22]` (E19 deferral) **Disabled-checkbox affordance is hover-only** — in `LayoutColumnSettingsTab.vue` the "Background fills page width" checkbox fades to disabled when "Content fills page width" is on, but the *why* only appears in a hover tooltip; the fade alone may not read as disabled. Add inline text or a visible disabled hint.
- `[0.317.01 · 2026-05-22]` (E19 finding) **`.sr-only` is not defined in the public SCSS bundle** — widgets use `<span class="sr-only">` (e.g. BoardMembers social-icon labels) but no `.sr-only` rule ships in `resources/scss/public.scss` partials, so that visually-hidden text may render visible on the public site. Add the standard visually-hidden utility to the public bundle.
- `[0.316.01 · 2026-05-22]` **Custom error pages (404 etc.)** — no custom `resources/views/errors/404.blade.php` (verified absent at s317 close); the app serves the framework default. Pre-Beta, low effort — add branded 404 (and likely 403/500) views. (Captured mid-s316 via `logbug`; folded at s317 close.)

---

## Promotion candidates (need release-plan.md entries)

*(Items too big to bundle. Need their own entry before scheduling.)*

- ~~**Table widget.**~~ Promoted at 282 audit → **E15** in `release-plan.md`. Pre-Public-Website-Complete.
- ~~**Header / footer defaults overhaul.**~~ Promoted at 282 audit → **E16**. Pre-Public-Website-Complete.
- ~~**Borders on widget controls + columns.**~~ Promoted at 282 audit → **E17**. Pre-Public-Website-Complete (may fold into Design System Editor track if it lands first).

---

## Folded into existing entries

*(Items absorbed by an existing release-plan entry — captured here as a paper trail.)*

- **Text editor reachability fix + Quill full-screen button + Playwright usability test** → folded into **E8 (UI/UX Sprint)**. E8 already covers Quill drag-resize handle; expand its scope at session start.
- **Default button style long-term parameterization** → folded into the **Design System Editor** track (already on radar — "buttons first").

---

## Recently dispositioned

*(Log of what left the inbox and where it went. Keep the last ~2 batches' worth.)*

- **2026-05-22 (session 317 close — E19 Accessibility Pass):** an audit session, so it mostly *added* to the inbox rather than draining it.
  - **0 existing inbox items removed.** The two items the E19 prompt folded in (text-toolbar `title`/`aria-label`; page-builder ellipsis-menu parity) were verified **already shipped at s315** — they were never live inbox rows, just prompt-named carry-ins, so nothing to remove.
  - **1 existing item annotated, kept open:** columns-primitive alignment labels (D1) — owner deferred; chosen solution is the preset-dropdown shape (b), not relabeling.
  - **6 items added:** 5 E19 deferrals (nav landmark; page/template terminology; duplicate-action consistency; hover-only disabled checkbox; `.sr-only` undefined in public SCSS) + 1 capture-buffer fold (custom 404 pages, original `[0.316.01]` stamp preserved).
  - **1 carve-out** to a `session-outlines.md` stub (page-builder inspector/modal focus management — E19 findings B5/B6), not the inbox.
- **2026-05-21 (session 315 close — Housekeeping Batch 2B):** closes the E12 plan entry (both batches done). 8 items left the inbox:
  - 5 → shipped (Organizations importer auto-mapping helper `OrganizationFieldMapper`; export-artifact TTL sweeper `exports:clean`, daily at 02:00; text-editor toolbar button accessibility — `title` + `aria-label` on both Quill toolbars and the inline-format toolbar; ellipsis secondary-actions menu ported to the page-builder editor view via a shared `HasPageSecondaryActions` trait; column-layout `background_full_width` parity default → true).
  - 1 → resolved by verification (backup download artifact cleanup — the Fleet `blob()` endpoint streams the newest backup off the already-`backup:clean`-reaped disk, so no separate downloaded artifact accumulates; nothing to sweep).
  - 1 → dropped as no-longer-valid (General settings — logo cannot be removed: the referenced settings logo upload was removed in a prior CMS-settings refactor; the only remaining logo upload is the admin-panel logo, a different control — premise stale).
  - 1 → examined but deferred, kept in inbox with a design-call note (Logo widget rich-text/appearance — Quill toolbars don't expose colour/size and a colour control collides with the s314 hero-inheritance; needs a designer decision before it's housekeeping-sized).
  - 1 capture-buffer item folded into the Inbox (page-builder ordered/unordered list rendering Playwright check).
  - 1 → promoted to a post-Beta stub in `session-outlines.md` (Content Export — Unified Options Modal: collapse the four export menu rows into one action + a two-toggle modal across content types; surfaced from the ellipsis-menu work).
  - At close, merged `origin/main` (A005 Test Audit Cycle 1 — `[test-integrity]` drain + redundant importer e2e deletions) into the close branch.
- **2026-05-21 (session 314 close — Housekeeping Batch 2A):** 8 items left the inbox:
  - 3 → shipped (Hero button-alignment + text-block-max-width controls; fresh-install placeholder logo image; logo wordmark responding to a full-bleed hero's text colour).
  - 1 → verified already shipped as drift (logo default link = `/` was already the schema default).
  - 3 → dropped as no-longer-valid (paragraph/list padding default — typography margin rhythm already owns it; button-style refresh — done as far as it goes pre-designer, belongs to the Design System Editor track; editor text-colour legibility — obsoleted by the inline editor).
  - Remainder carries to Batch 2B (session 315). Also shipped the `npm run logbug` capture flow + close-gate digest + verify-at-walk staleness guard.

---

## Disposition rules

When walking the inbox at session start / close, each item leaves via one of:

- **(a)** Fold into the next available housekeeping batch session (the default for inbox items).
- **(b)** Fold into an existing planned entry — note here under "Folded into existing entries" and cross-ref in the target entry's session prompt.
- **(c)** Promote to its own `release-plan.md` entry — move from Inbox to "Promotion candidates" first, then add the entry, then remove from this file.
- **(d)** Drop as no-longer-relevant — note briefly under "Recently dispositioned" with the date.

No states, no priorities, no timestamps beyond "Recently dispositioned." The list is the system.
