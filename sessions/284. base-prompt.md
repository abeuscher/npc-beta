# Base Prompt — Session 284

We are about to begin a new session: **284. Public Marketing Website — Audit + Home Cleanup**.

This is **execution-order position 34** in `sessions/release-plan.md` — **PMW1**, the **first phase of the Public Marketing Website track** (lifted at session 283 mid-session). The track sequences inside the **Public Website Complete milestone** (Rule 12).

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope. CRM contract stays at v2.3.0.

**Session shape:** content + audit, not feature-style. Three deliverables — a system-understanding summary, the existing home page export edited to align with the brief's section-band / padding / appearance-config-as-text-color conventions, and a `gap-report.md` scaffold with first findings.

Before doing anything else:

1. Re-read the session templates only if uncertain about a structural detail — they remain stable.
2. Read `sessions/session-outlines.md` Active-tracks block and the Public Marketing Website entry within it.
3. Read `sessions/release-plan.md` § PMW1 — canonical for scope, success criterion, prerequisites, and artifact.
4. Read `sessions/tracks/public-marketing-website.md` end-to-end — premise, scope, four-phase sequence, system conventions inherited from the brief, gap-resolution discipline, working-folder layout.
5. Read the brief and supporting docs in the working folder — canonical for content and structure: `sessions/public website/brief.md`, `sessions/public website/copy.md`, `sessions/public website/references.md` and the four reference PNGs, and `sessions/public website/existing-home-page-export.json` for the format-1.1.0 JSON shape.
6. Read `docs/app-reference.md` for the page-builder component map (`PreviewCanvas.vue`, `WidgetRegistry`, `AppearanceStyleComposer`, etc.) — locating these is part of the audit. Skim `resources/docs/widget-development.md` for widget definitions, config schemas, and the appearance system (the "Stripe Checkout Integration" section added at session 283 is unrelated to this session). Read `docs/schema/README.md` for the table index — no individual table files are in scope this session (no schema changes), but the index is useful context.
7. Open `sessions/284. Public Marketing Website — Audit + Home Cleanup.md` and read the session prompt.
8. Note any drift between the prompt and the actual code in a brief work-log entry; proceed unless something requires a decision per the drift and decision-threshold rules below.

---

## Starting state inherited from 283

- **CRM contract at v2.3.0** — unchanged. Session 283 shipped Stripe Checkout branding (custom_text + submit_type + statement descriptors + per-flow images). API-half verified via Pest; visual half manually tested on the deploy server and confirmed working.
- **Schema baseline:** no schema changes since 280-close.
- **Fast test suite green at 2390 / 0 sequential** (283 close baseline). No new tests expected this session — content-only work.
- **Playwright suite** — baseline preserved. No Playwright in this session unless the page-capture harness rationale shifts (it's scheduled for PMW4, not here).
- **Public Marketing Website track:** lifted at 283 mid-session; track doc + working folder + four-phase sequence in place. This session executes PMW1, the first phase.
- **Housekeeping inbox** at `sessions/housekeeping-inbox.md` — content-style sessions don't absorb housekeeping items. Touch only if a finding directly tied to home-page rendering surfaces; otherwise carry forward.

---

## Process rules for every session

- **Before writing any code or JSON**, read every file you intend to modify.
- **Before implementing any integration** (email, file storage, authentication, payments, queues), ask the user what existing system handles it. *Session-284 note: this session does not implement integrations. The Form widget configuration scoped for PMW3 is the only integration-adjacent surface in the track, and it's not in this session.*
- **Pause and ask** any time a decision point arises that is not covered by the agreed prompt.
- **Surface architectural choices before going deep.** Standard rule applies. The most likely 284-specific candidate: if the existing home page mixes two color models (`appearance_config.text.color` vs. inline Quill `style="color:rgb(...)"`) in a way that's load-bearing for downstream pages in the track, the choice of which model wins for any ambiguous section is worth a quick check. Local single-widget edits — just decide.
- **Adapt to drift; don't ask about it.** Small inaccuracies in the brief vs. the actual codebase are expected (the brief was authored by an outside agent without live codebase access). Default to silent adaptation + a one-line work-log note. Only flag drift when it requires a decision per the brief's authority hierarchy: existing exported pages > written copy > reference images > agent's own design instinct.
- **Decision threshold scales with project maturity.** Reserve "pause and ask" for decisions that propagate (the audit's findings, conventions across multiple pages, gap-report rows that warrant lifting). Local edits to a single home-page widget — just decide and note.
- **If any external service is unavailable**, stop and ask — do not attempt to troubleshoot.
- **Run migrations via Docker** after writing them: `docker compose exec app php artisan migrate`. *Session-284 note: this session does not need migrations.*
- **Update `docs/schema/`** after writing any migration. (No-op this session.)
- **If the PostgreSQL container becomes unresponsive**, ask the user to restart it.
- **When implementation is complete**, run the fast test suite: `docker compose exec app php artisan test --exclude-group=slow`. Target: 283-close baseline (2390 / 0). No new tests expected; the run is just a regression check that the JSON re-import didn't perturb anything. *If the test suite isn't perturbed by content imports (most likely), this can be a single-run confirmation rather than a watch-loop.*
- **Front-end builds.** No SCSS / JS expected this session. If something forces a build, the rule is: `resources/scss/**` or `resources/js/**` → `npm run build`; widget asset surfaces → `php artisan build:public`. Don't run if not needed.
- **Tests for new behaviour:** no new tests expected. Content-only sessions skip the tests-for-new-behaviour rule per the base-prompt convention.
- **Fast vs slow classification:** N/A.
- **Playwright artifact hygiene:** N/A this session.
- **Cross-repo coordination — Fleet Manager.** N/A.
- **Verify objective outcomes yourself; pull the user in for judgment.** Audit summary is objective (the helpers, settings, and registry rows either exist or they don't) — close that loop yourself. Home page visual rendering is the user-judgment surface — surface for review at session-end.
- **Pause for manual testing only when human judgment is required.** End-of-session: announce that the audit summary is ready for the user's review, and that the cleaned home page is re-imported and ready to view at the home URL. Stop and wait.

---

## ── SESSION CLOSE GATE ──────────────────────────────────────────────────────

**Everything below this line happens only when the user explicitly says to close the session.**

Do not ask whether to close. Do not suggest it. Wait for the user to initiate each phase.

### Phase 1 — Attenuate and prepare next session

After implementation and manual testing are complete, draft the next session's documents:

- **Next session base prompt** (`sessions/285. base-prompt.md`) — copy `template-base-prompt.md`, update session number and title to **PMW2 Public Marketing Website — Pricing build-out + About extend** (execution-order position 39).
- **Next session prompt** (`sessions/285. Public Marketing Website — Pricing + About.md`) — draft against the PMW2 release-plan entry, the relevant `copy.md` sections, and the audit findings + cleaned-home structural patterns from this session.

**Pre-PMW2 ask in the close handoff.** PMW2 needs current exports of About and Pricing from the admin. Surface this as a request to the user during close: *"Before PMW2 starts, please export the current About and Pricing pages from the admin and place them in `sessions/public website/` as `existing-about-page-export.json` and `existing-pricing-page-export.json`."* The user runs the export action; the agent treats those files as ground truth at PMW2 start.

Then stop. User reviews and initiates close when ready.

### Phase 2 — Close

When the user says to close:

- **Session log:** write `sessions/284. Public Marketing Website — Audit + Home Cleanup — Log.md` using `template-session-log.md` as the format reference. Document: audit deliverable shape, type-scale + button + appearance + widget + image-library findings, home cleanup decisions (what was changed, what was preserved), first gap-report rows, manual-testing observations from the user's home-page review.
- **Update `sessions/completed-sessions.md`:** append session 284's row.
- **Update `sessions/session-outlines.md`:** Active-tracks block — bump the Public Marketing Website entry's status from "active, not yet started" to "active — PMW1 closed at session 284; PMW2 next."
- **Update `sessions/tracks/public-marketing-website.md`:** bump status snapshot (last update date; PMW1 marked complete; PMW2 marked next). PMW1 is **not** a phase-closure event for the track (the four phases together close the track at PMW4) — no phase-expiry compression yet. Just status snapshot update.
- **Update `sessions/release-plan.md`:** mark PMW1 ✅ closed at session 284. Update execution-order list entry at position 34.
- **Archive the previous session:** move all `sessions/283. *.md` files into `sessions/archived/`.
- **Commit:** stage all changed files on the final `session-284/N` branch. Notify the user. Do not push.
- **Do not begin the next session** until the user explicitly starts it.

---

## Style rules

Base-prompt style rules apply. Session-specific:

- **Iteration commit messages describe the deliverable** — e.g. "Iteration /1 — PMW1 audit summary + cleaned home.json + first gap-report rows".
- **PM-level tone for status reports to the user** per `memory/feedback_pm_level_tone.md`.
- **No docstrings, comments, or type annotations on code you did not write.**
- **Audit summary and gap report are operator-readable.** PM-level voice; plain language; no framework-primitive jargon. The user returns to these documents across the track.
