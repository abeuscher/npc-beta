# Base Prompt — Session 378 (Widget Boundary — Data Contracts Across All 41)

**Start now.** Execute the reading list below, open the session prompt at `sessions/378. Widget Boundary — Data Contracts Across All 41.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **378. Widget Boundary — Data Contracts Across All 41**.

This is **arc position P2** of the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § 7 is canonical; `release-plan.md` § Plugin Architecture arc is the summary). P2 makes the widget boundary real across all 41 widgets: every template that today reaches models, auth state, or services directly is routed through a declared data contract, and a standing guard test bans such reaches from then on. This is the session that makes `docs/plugin-contract.md` surface 2's "no model calls in templates" rule true rather than aspirational — the contract doc is the arbiter.

This session is **NOT boundary-touching** — no Fleet Manager surface is in scope; the CRM contract stays at **v2.7.0**.

**Session shape:** feature-style / behavior-preserving retrofit. Twelve widget templates (surveyed at 377 close — the list in the session prompt is ground truth) move their data access into declared contracts backed by new resolver arms; a standing guard test pins the boundary. **Execution posture (owner directive, 376):** binary success criteria, no UAT, decisions front-loaded in the session prompt; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` §§ 2–4 and § 7, and `docs/plugin-contract.md` v0.1.0 (surfaces 2 and 13, and the normative rules — this session executes against them).
3. Open `sessions/378. Widget Boundary — Data Contracts Across All 41.md` — the front-loaded decisions there are settled; do not reopen them.
4. Read the code being touched: `app/WidgetPrimitive/ContractResolver.php` (the source arms + the permission gate), `app/WidgetPrimitive/DataContract.php`, `app/Services/WidgetRenderer.php` (how `$widgetData` reaches templates), `app/Widgets/Contracts/WidgetDefinition.php`, the twelve offending templates listed in the session prompt, and `tests/Feature/LogoGardenContractRetrofitTest.php` + `tests/Feature/DesignGroupIntegrityTest.php` as the retrofit-test and pinned-list-guard patterns.
5. Skim `sessions/session-outlines.md` (Plugin Architecture track entry) and `docs/app-reference.md`.
6. Note any drift between the session prompt's survey and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 377

- **CRM contract at v2.7.0** — 377 was non-boundary (last boundary touch: 372).
- **Plugin architecture state:** the LogoGarden pilot lives at `plugins/LogoGarden/`, registered solely via `config/plugins.php` through core's `PluginServiceProvider`; `docs/plugin-contract.md` v0.1.0 exists (surfaces 1, 2, 11, 12 PROVEN). Thumbnail paths resolve via `WidgetDefinition::thumbnailDir()` (reflection on the definition class's location). Standing guard globs already cover `plugins/*` alongside `app/Widgets/*`.
- **Schema baseline:** no schema changes at 375–377.
- **Fast Pest suite green at 3116 / 0** (377 close; +5 plugin registration tests over the 375 baseline). Expected delta this session: moderate — per-arm resolver tests, retrofit render-parity tests for the twelve templates, the new boundary guard.
- **Playwright suite** — baseline preserved; does not run this session, CI is the signal.
- **Planning state:** no planning-doc changes at 377 beyond the arc entry marking P1 complete (done at 377 close).
- **Housekeeping inbox:** the 377 capture item (WidgetManifestTest screenshot-path resolution still assumes `app/Widgets/{Folder}`) is **absorbed by this session as an explicit rider** — it is the same boundary being fixed. The validation-loudness item stays parked.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior-preserving is the bar.** Same rendered output for every retrofitted widget — logged-in and logged-out portal states, empty and populated data states. The fast suite + retrofit parity tests are the oracle; if a change needs a human eyeball to validate, it is out of scope.
- **Fail-closed is load-bearing.** Every new resolver arm projects only contract-declared fields (the LogoGarden/SWCT precedent). PII-adjacent arms (portal member) get non-leak tests, not just happy-path tests.
- **The front-loaded decisions in the session prompt are settled.** Implementation details within them are yours to decide and log.
- **Scope is binding.** Admin-shell sockets (P3), the capability-detection API (P4), activation, packaging — all out. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** promote the affected parts of surface 2 in `docs/plugin-contract.md` per the honesty rule (PROVEN only for what this session exercised), bump the doc's version, changelog entry.

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the twelve-template survey still matches the code, state the retrofit order you'll take and any drift found — then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
