# 315. Housekeeping Batch 2 — Batch 2B

Second of the two batches that the Housekeeping Batch 2 plan entry spans. Walks the inbox at session start, picks the Batch 2B subset from what the first batch left, applies iteration-sliced. Closing this session closes the plan entry.

---

## Plan reference

This session executes `sessions/release-plan.md` § E12 (execution-order position 42). The plan entry is canonical for scope and milestone-block positioning. This prompt is the session-shape delta — the plan entry doesn't enumerate inbox items because the inbox accretes between sessions; the walkthrough at session start does the picking.

---

## Inbox walkthrough (session start)

Open `sessions/housekeeping-inbox.md` with the user. For each item under "## Inbox":

- **`[post-milestone]`-tagged** → skip (deferred past the Public Website Complete milestone).
- **Verify the premise against current code first** — confirm the item still describes a real gap before considering it (the staleness guard in the inbox; the older the capture stamp, the more important the check). Drop or re-scope dead items as you find them.
- **Small + standalone** → candidate for Batch 2B.
- **Pairs with another inbox item by file or surface** → bundle the pair into one iteration; both candidates together or both deferred together.
- **Larger than a single iteration** → defer or flag for promotion to its own session if the size has grown.

Output of the walkthrough: an ordered list of N iterations for Batch 2B (each one inbox item, or a small bundled pair), a list of any items dropped as no-longer-valid, and a leftover list that carries forward.

The items left in the inbox after the first batch (session 314) include, among others: the logo widget's text becoming styled rich text; the column-layout `background_full_width` parity default; the export-artifact TTL sweeper (paired with the downloaded-backup cleanup); the Organizations importer auto-mapping helper; a way to remove an uploaded logo from general settings; the secondary-actions menu missing from the page-builder editor view; friendlier labels or presets on the columns-primitive alignment controls; the link-colour mechanism consolidation; and the test-integrity cluster. Walk the live inbox for the authoritative list — this is a snapshot.

---

## Iteration discipline

- **One iteration per inbox item** by default. Each iteration is its own commit on `session-315/N`. Bundle only when items share a file or surface; the bundling rationale lands in the commit message.
- **Six-iteration ceiling.** Past 6 iterations the next iteration is the carve-out signal — stop and let leftovers move to a later batch.
- **Tests follow the item.** Small bug-fixes that don't change observable behavior at a tested seam rely on the existing suite; behavior-changing items add coverage per the standard test-for-new-behavior rule.
- **Commit message names the inbox item.** Use the inbox bullet's leading clause as the commit subject so a future audit can trace the close to the originating item.

---

## Open questions to resolve at session start

- **Subset size.** How many items does the user want in Batch 2B? Default leaning: as many as fit cleanly under the six-iteration ceiling.
- **Premise re-check outcomes.** Surface any items that fail the verify-against-current-code check at the walk, before committing the subset — these are dropped or re-scoped, not implemented.
- **Bundling.** Default: bundle only when files / surfaces overlap (e.g. the TTL sweeper and the downloaded-backup cleanup likely share the janitor surface).

---

## Out of scope

- Items tagged `[post-milestone]` in the inbox.
- Items the walkthrough flags as having grown past housekeeping size — those get promoted to their own session via a release-plan entry, not absorbed here.
- Anything outside the inbox. New items surfaced mid-session go into the capture buffer (`npm run logbug`), not into 315.

---

## Testing

- **Fast Pest after every iteration.** Each iteration should leave the suite green.
- **New tests:** per the standard rule — behavior changes get coverage, byte-equivalent or trivial fixes rely on the existing suite. Commit message notes which.
- **Playwright:** isolated stack via `npm run test:e2e` only if an iteration touches a frontend surface that has Playwright coverage. CI runs the full Playwright set on every push regardless.

---

## Closing steps

Follow the close gate in the base prompt. Session-specific details:

- **Log file:** `sessions/315. Housekeeping Batch 2 — Batch 2B — Log.md`. Per iteration: which inbox item, what changed, tests added (if any), any drift or premise-failure noted.
- **Branch:** `session-315/N` (final iteration).
- **Inbox file:** remove shipped items at close; digest and clear the capture buffer; leftover items stay.
- **Release plan:** mark § E12 closed (both batches done).
- **Next-session prompt:** 316. Record-Duplicate Affordance is already drafted — review and refresh rather than redraft. If Batch 2B spills over, draft a third batch at 316 and bump record-duplicate to 317.
