# Base Prompt — Session 393 (Memberships Packaged & Extracted)

**Start now.** Execute the reading list below, open the session prompt at `sessions/393. Memberships Packaged & Extracted.md`, then stop at the Open Gate before beginning task work.

---

We are about to begin a new session: **393. Memberships Packaged & Extracted**.

This is **arc position D4, second and final session of the block** in the **Plugin Architecture arc** (`sessions/plugin-architecture-plan.md` § Stage D is canonical; `release-plan.md` § Plugin Architecture arc is the summary). The carve landed at 392 (`plugins/Memberships/`, the second cross-repo inversion via Payments v0.3.0, fast Pest 3349/0, routes 272/272 byte-identical). Per the block-shape call made at the 392 close (decision 8 of that prompt), package + squash redraw + extraction **compress into this one session** — the D1 Payments precedent, justified by what the carve surfaced: a two-table redraw with zero inbound core FKs and no sequences, one already-gate-4-proven asset, and a four-runs-no-deltas runbook. This session closes the D4 block: `nonprofitcrm/memberships` ends the day VCS-resolved from its own repository and `plugins/` is empty again until D5's carve.

This session is **NOT boundary-touching** — the CRM↔FM contract stays at **v2.7.0**; no `deploy.yml` change. The plugin contract (`docs/plugin-contract.md`) bumps 0.16.0 → 0.17.0 at close (Package shape fifth consumer; surface 5 third squash-redraw consumer; External repo fifth consumer; changelog).

**Session shape:** migration-squash + extraction, behavior-preserving, two phases in one session (package + redraw, then the runbook). Runtime behavior identical throughout: routes 272/272, every membership assertion unchanged beyond surveyed path re-points, both per-composition identity checks verified clean, gate 4 proven on the one PricingChart asset, in-image proof clean. **Expect one iteration.** Execution posture (owner directive, 376): binary success criteria, no UAT, decisions front-loaded; mid-session, decide-per-plan-and-log; pause only for contract-surface changes that are expensive to reverse.

Before doing anything else:

1. Re-read the session templates (`sessions/template-base-prompt.md`, `sessions/template-session-prompt.md`, `sessions/template-session-log.md`) — canonical format reference; human-facing output per `CLAUDE.md` § Reporting to the human.
2. Read `sessions/plugin-architecture-plan.md` § Stage D (D1–D3 ✅, D4 open — carve done at 392) and the § 6.7 squash-discipline entry (the redraw mechanics this session runs a third time).
3. Read the 392 session log (`sessions/392. Memberships Carved — Log.md`) — especially its **block handoff** section: the two-table redraw input (FK map, the RESTRICT FK-as-behavior note, no sequences), the portal-seam composition-safety items, and the in-plugin test path read that re-points at extraction.
4. Read `docs/plugin-contract.md` at 0.16.0 — § Package shape (the P6 mechanics this session repeats: explicit version, bound require, path repository, per-plugin Dockerfile manifest-COPY lines, no auto-discovery), § External repo (the extraction shape: version field dropped, vendor paths, guard follow-through, test-membership postures), § The distribution manifest (the two source-kind walks this session makes: `folder` → `path` → `vcs`), surface 5 in full (install order, vanish semantics, identity-check procedure, core-model caveat).
5. Read `docs/plugin-extraction-runbook.md` end to end — this is the fifth run; the 390 package playbook and the 391 extraction are the closest analogues (read both logs, `sessions/archived/390. Donations Packaged — Log.md` and `sessions/391. Donations Extracted — Log.md`, now in `sessions/archived/` if swept).
6. Open `sessions/393. Memberships Packaged & Extracted.md` — the redraw input and front-loaded decisions; note the Open-Gate items (the owner-created repo; sibling checkout freshness).
7. Skim the surfaces being packaged: `plugins/Memberships/MembershipsServiceProvider.php` (no `loadMigrationsFrom` yet — this session adds it), `plugins/Memberships/Widgets/PricingChart/PricingChartDefinition.php` (the one asset declaration that goes vendor-relative at extraction), `tests/Fixtures/Plugins/Memberships{Removed,DisabledViaFlag}TestCase.php` (the expiring no-migrator-path caveat both docblocks carry — it expires this session).
8. Read the portal seam being gated: `app/Widgets/PortalSignup/template.blade.php` (the `route('membership.checkout')` render — throws on a memberships-absent composition) and `app/Http/Controllers/Portal/SignupController.php` (the `exists:membership_tiers,id` validation rule + the complimentary-tier `Membership::create` — both reach plugin-owned schema once the redraw lands). The setup-checklist fund-row gate (390) and the `PageContextTokens` page-type gate (383) are the precedents.
9. Read the dump DDL being moved: `database/schema/pgsql-schema.sql` — `membership_tiers` (~line 772) and `memberships` (~line 793), their constraints (~1985–2005), indexes (~2568–2590), and the five outbound FKs (~3040–3075). The 393 session prompt carries the captured map; verify it against the live dump.
10. Skim `sessions/session-outlines.md` (the Plugin Architecture track entry) and note the housekeeping-inbox posture for this shape.
11. Note any drift between the session prompt's captured input and the actual code in a brief work-log entry; adapt silently per the drift rules unless a decision is genuinely forced.

---

## Starting state inherited from session 392

- **CRM contract at v2.7.0** — 392 was non-boundary (last boundary touch: 372); `deploy.yml` untouched.
- **Plugin architecture state:** P1–P10 + D1–D3 complete, D4 carve done. **Four extracted plugins** (`crm-plugin--logo-garden` v0.1.0, `crm-plugin--payments` **v0.3.0** — the second cross-repo inversion shipped at 392, `crm-plugin--events` v0.1.1, `crm-plugin--donations` v0.1.0) plus the in-repo carve `plugins/Memberships/` (manifest source kind `folder`, handle `memberships`, fifth provider line). Plugin contract **v0.16.0**. Root `"Plugins\\"` PSR-4 stays (D12).
- **Schema baseline:** `memberships` + `membership_tiers` still in core's dump — **this session moves them** (the third squash-boundary redraw). The seven previously moved plugin tables live in their packages' migrations.
- **Fast Pest suite green at 3349 / 0 parallel** (392 close: 3314 baseline + 35 additive — inversion listeners, twin fixtures, removal/flag mirrors, boundary guard). The `Memberships` phpunit testsuite runs from `plugins/Memberships/tests` (6 files: the enabled twin, removal mirror, checkout tests, field-mapper tests, both widget definition tests). Expected delta this session: **small and additive** — identity-check artifacts and any composition-safety gate tests; existing assertions unchanged beyond surveyed path re-points.
- **Route baseline: 272 routes** (dev/test), byte-identical to main at 392 close — unchanged this session.
- **Gate 4 baseline:** the public bundle carries the pricing-chart styles from `plugins/Memberships/Widgets/PricingChart/styles.scss`; the path goes vendor-relative at extraction and the gate runs again.
- **Playwright suite** — CI signal, non-gating; the parked chrome-parity spec remains the known failure. No local Playwright.
- **Housekeeping inbox** — the two 389-folded items stay (`[plugin-arch]` EmailTemplateResource tokenHint, portal_form_collision defaults); the 392 capture buffer was empty. This shape absorbs no items.

---

## Process rules for every session

Base-prompt process rules apply in full (see `sessions/template-base-prompt.md`). Session-specific:

- **Behavior preservation is total.** Every membership net passes with assertions unchanged beyond surveyed path re-points; routes 272/272; no visual change. The schema move is representational: byte-faithful raw-DDL create-table migrations authored from the dump (`DB::unprepared` — the 383 rule; schema-builder calls risk expression-normalization drift in the `billing_interval` check constraint).
- **The package lands with the guard interlock** (the 390 mechanics): plugin `composer.json` (explicit `version: 0.1.0`, bound `^0.1` require, root path repository, PSR-4 `Plugins\Memberships\` → `""`), manifest source `folder` → `path` **same-commit** with the lock, one Dockerfile manifest-COPY line per stage (never a glob), `composer validate --strict` clean, in-image resolution proven.
- **The redraw follows the recorded procedure** (surface 5): creation order `membership_tiers` → `memberships` (FK order); the five outbound FKs travel (note `memberships.contact_id` is **ON DELETE RESTRICT** — verify whether a behavior net covers blocks-contact-force-delete-while-memberships-exist and note the finding either way); indexes travel; **no sequences** (uuid PKs — the first redraw with none). Regenerate the core dump only against a database whose membership tables were dropped first; recreate the database, not the schema. Both per-composition identity checks run and pass: full composition byte-identical modulo the two `migrations` rows (+ pg_dump's per-run token); memberships-absent composition boots + seeds + smokes clean with zero membership tables.
- **The twin fixtures' caveat expires now:** `MembershipsRemovedTestCase` + `MembershipsDisabledViaFlagTestCase` gain the plugin migrator path the moment schema moves (in-repo `plugins/Memberships/database/migrations` at the package step; vendor path at extraction — same session, so land the vendor path form once extraction completes, or step through both).
- **Composition-safety: fix what the check finds** (the 383/390 discipline). The surveyed portal-seam reaches (session prompt decision 4) are the known hunting ground; gate what the memberships-absent check actually trips, record the rest as composition debt.
- **The extraction is the runbook, top to bottom** (fifth run): owner-created public repo `crm-plugin--memberships`, version field dropped, asset path vendor-relative, README on the Donations model, in-plugin test path reads re-pointed in the extracted copy (one known: the PricingChart asset assertion), manifest `path` → `vcs` + composer swap + `git rm` + lock pinned to the tag in one commit, Dockerfile COPY lines removed, fixtures + guards + phpunit re-pointed vendor-relative, gate 4 live (bundle content, not just a green suite), in-image proof (five packages VCS-resolved, `plugins/` absent).
- **The manifest is edited, never the generated file.** Two `plugins:manifest-sync` regenerations this session (both provider-list no-ops — same FQCN, same order); the guards verify the rest.
- **Scope is binding.** No model/factory moves (§ 6.7); no portal-vertical work beyond the composition-safety gates (D5 owns the portal); no per-plugin CI (D11); no FM surface. Discovered adjacent work → housekeeping inbox or the plan doc's backlog, one line, move on.
- **Contract doc update at close:** 0.16.0 → 0.17.0 (Package shape fifth consumer; surface 5's third redraw consumer — the no-sequences note; External repo fifth consumer; changelog); plan doc § Stage D D4 line marked ✅ block closed; runbook gains the fifth worked-example lines (+ any procedure deltas, expected none).
- **D5 kickoff at close:** capture the Member Portal survey from live code into the 394 prompt (the D4→D5 handoff: six portal widgets, the portal auth guard, portal routes, the signup seam this session gated).

---

## ── SESSION OPEN GATE ───────────────────────────────────────────────────────

After the reading list and Starting state are read, surface a short plain-English orientation — confirm the 392 block handoff still matches the code, state the two-phase plan, note any drift found — and resolve the open items: **(1)** the sibling `crm-plugin--payments` / `crm-plugin--events` / `crm-plugin--donations` checkouts are current (`git pull` each; no edits expected this session — verify the 389-find grep for sibling exclusivity assumptions against the new `memberships` importer contribution), and **(2)** the owner has created the public repo `crm-plugin--memberships` (empty or README-only; the runbook's step 0 — extraction cannot start without it). Then **wait for the owner to confirm before starting.** One confirmation, then run the session to its binary finish.
