# Web Form Security Reference

Internal developer reference. Not intended for public distribution.

Covers the public web form submission surface introduced in session 047 and hardened in session 048.

---

## What is protected

### CSRF
All form submissions require a valid Laravel CSRF token generated by the `@csrf` directive in `public-form.blade.php`. Requests without a matching token are rejected by Laravel's `VerifyCsrfToken` middleware before reaching the controller.

### PII screening
Submitted values are passed through `PiiScanner` before storage. The scanner rejects submissions containing:
- Credit card PANs (13–19 digit sequences passing the Luhn check)
- Social Security Numbers (formatted `###-##-####` or bare 9-digit)
- ABA routing numbers (9-digit sequences with a matching prefix range)

Field handle names are also checked against a blocklist (e.g. `ssn`, `creditcard`, `routingnumber`). Rejections return a generic error message that does not disclose which pattern matched. No submission record is written on rejection.

### Honeypot
A hidden `_hp` field is rendered in the form (when `settings.honeypot` is `true`, which is the default). If the field contains any value in the submitted POST body, the controller returns a silent success response without storing a submission record. Legitimate browsers leave the field empty; basic automated bots that fill all visible inputs are caught here.

### Rate limiting
The `throttle:10,1` middleware is applied to the `POST /forms/{handle}` route, allowing a maximum of 10 submissions per minute per IP address. Requests exceeding this limit receive a 429 response.

### Input validation
Field-level validation rules defined in the form are enforced server-side in `Form::fieldValidationRules()`. The supported presets are: `email`, `phone`, `url`, `zip`, `numbers_only`, `letters_only`, and `custom_regex`. Client-side `required` and `pattern` attributes rendered by `public-form.blade.php` are a UX convenience only and provide no security guarantee.

### Regex safety
Custom regex patterns (`validation = 'custom_regex'`) are validated at save time in `FormResource`. The save-time check:
1. Verifies the pattern compiles without error (`preg_match` returning `false` is rejected).
2. Tests the pattern against a known backtrack-triggering input (`aaa…a!` with 50 `a` characters) at a reduced `pcre.backtrack_limit` of 1 000. If the limit is hit, the save is rejected with the message: "This regex pattern is too complex and may cause performance issues. Simplify the pattern."

The existing `try/catch` in `Form::fieldValidationRules()` remains as a secondary safety net in case a pattern reaches the submission handler despite the save-time check.

### XSS
All submitted values are stored as plain text in the `form_submissions.data` JSON column. Every location in the admin UI that renders submission data uses Blade's `{{ }}` escaping. No submitted value is rendered with `{!! !!}`.

### Contact field write allowlist
`FormSubmissionController::syncContact()` restricts writes to a named allowlist of standard columns (`first_name`, `last_name`, `phone`, `address_line_1`, `address_line_2`, `city`, `state`, `postal_code`, `country`) plus prefixed custom field keys (`custom_fields.*`). Arbitrary column names from form field definitions cannot reach a database `UPDATE` statement.

### Hidden field tamper-proofing
Hidden field values in public forms are ignored on submission. `Form::fieldValidationRules()` excludes hidden fields, so they are absent from `$validated`. The controller then writes `default_value` from the form definition after building `$data`, unconditionally overwriting any value that may have been submitted. This was confirmed by Finding 2 analysis and by a dedicated feature test in `FormSubmissionTest`.

---

## Known limitations and accepted risks

### Honeypot predictability (Low)
The honeypot field is named `_hp` — a fixed, predictable name. Sophisticated bots explicitly programmed to avoid known honeypot patterns could bypass it. **Mitigation:** Cloudflare Turnstile (deferred). Accepted for now.

### Email collision identity abuse (Low)
A public form submission that matches an existing contact's email address updates that contact's record with submitted field values. A bad actor who knows another person's email could submit false address or phone data. This is a known limitation of any unauthenticated public form that writes to a contact record. Full mitigation requires authenticated member sessions (deferred to Member Portal). Accepted for now; the note written to the contact record on each submission provides an audit trail.

### Rate limiting behind a reverse proxy (Low)
The `throttle:10,1` middleware keys on `$request->ip()`. If the application is placed behind a reverse proxy (e.g. Cloudflare, nginx upstream), all requests will appear to come from the proxy's IP and the rate limit will be shared across all visitors. **Fix before deploying behind a proxy:** add the proxy's IP ranges to Laravel's `TrustProxies` middleware. Not applicable to the current direct-served deployment.

### No CAPTCHA (Low)
Honeypot and rate limiting provide basic bot deterrence. Cloudflare Turnstile (challenge-free CAPTCHA) is deferred. Accepted for now.

### Admin logo upload (Low — out of scope for this document)
The Site Settings page includes a logo upload field backed by Filament's file upload component and Laravel's storage system. File upload fields are a distinct attack surface: malicious file types, oversized uploads, and path traversal are all possible vectors. The current implementation relies on Filament's default upload handling. A dedicated security review of the file upload path — validating MIME type server-side (not just extension), enforcing maximum file size, storing outside the web root or via a signed URL, and scanning for embedded scripts — is deferred to a future session.

---

## Finding register

| # | Title | Severity | Status |
|---|-------|----------|--------|
| 1 | Regex ReDoS — no save-time validation on custom patterns | High | Fixed (session 048) |
| 2 | Hidden field value tampering | Medium | Refuted — already prevented by controller logic |
| 3 | Submission flow test coverage | Gap (not a vulnerability) | Fixed (session 048) |
